import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Accessibility, Check, Eye, FileImage, LayoutDashboard, LockKeyhole, Palette, RefreshCw, ShieldAlert, SlidersHorizontal, UserRound } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const themes = ["Midnight", "Aurora", "Paper"];
const accents = ["Cyan", "Violet", "Amber", "Rose"];
const densities = ["Comfortable", "Compact"];
export default function ProfileCustomization() {
  const [theme, setTheme] = useState(themes[0]);
  const [accent, setAccent] = useState(accents[0]);
  const [density, setDensity] = useState(densities[0]);
  const [layout, setLayout] = useState("Focus");
  const [avatarIntent, setAvatarIntent] = useState("Placeholder");
  const [saved, setSaved] = useState(false);
  const reset = () => { setTheme(themes[0]); setAccent(accents[0]); setDensity(densities[0]); setLayout("Focus"); setAvatarIntent("Placeholder"); setSaved(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Palette} eyebrow="ProfileCustomization · Appearance preview" title="Shape the profile experience without pretending it is synced." description="Explore local appearance, layout, avatar intent, privacy, and accessibility choices. Preferences are browser state only; no account sync, image storage, publication, or cross-device persistence is connected." badge="Customization workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Preferences saved locally" : "Save preferences locally"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset customization</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Theme", value: theme, hint: "Local preview", icon: Palette, tone: "cyan" }, { label: "Accent", value: accent, hint: "Browser state", icon: SlidersHorizontal, tone: "violet" }, { label: "Density", value: density, hint: "Not account synced", icon: LayoutDashboard, tone: "amber" }, { label: "Avatar", value: avatarIntent, hint: "No upload source", icon: FileImage, tone: "slate" }]} /><ScreenPreviewBanner title="Customization evidence boundary"><strong>This is a local appearance and preference preview.</strong> Theme, accent, density, layout, avatar intent, privacy, and accessibility selections remain in browser state. No account preference, image upload, public profile update, cross-device sync, or accessibility guarantee is claimed.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1fr_0.85fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="space-y-7 p-6"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Appearance controls</p><h2 className="mt-2 text-2xl font-black">Tune the local preview</h2></div><div><p className="text-sm text-slate-400">Theme</p><div className="mt-2 flex flex-wrap gap-2">{themes.map((item) => <button key={item} onClick={() => setTheme(item)} className={`rounded-lg border px-3 py-2 text-xs ${theme === item ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div></div><div><p className="text-sm text-slate-400">Accent</p><div className="mt-2 flex flex-wrap gap-2">{accents.map((item) => <button key={item} onClick={() => setAccent(item)} className={`rounded-lg border px-3 py-2 text-xs ${accent === item ? "border-violet-300/40 bg-violet-300/10 text-violet-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div></div><div><p className="text-sm text-slate-400">Density</p><div className="mt-2 flex flex-wrap gap-2">{densities.map((item) => <button key={item} onClick={() => setDensity(item)} className={`rounded-lg border px-3 py-2 text-xs ${density === item ? "border-amber-300/40 bg-amber-300/10 text-amber-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div></div><div><p className="text-sm text-slate-400">Profile layout</p><div className="mt-2 flex flex-wrap gap-2">{["Focus", "Grid", "Timeline"].map((item) => <button key={item} onClick={() => setLayout(item)} className={`rounded-lg border px-3 py-2 text-xs ${layout === item ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div></div><div><p className="text-sm text-slate-400">Avatar intent</p><div className="mt-2 flex flex-wrap gap-2">{["Placeholder", "Local image later", "Generated concept"].map((item) => <button key={item} onClick={() => setAvatarIntent(item)} className={`rounded-lg border px-3 py-2 text-xs ${avatarIntent === item ? "border-amber-300/40 bg-amber-300/10 text-amber-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Preview</p><h2 className="mt-2 text-2xl font-black">{theme} · {layout}</h2></div><Badge variant="outline" className="border-white/10 text-slate-400">Local</Badge></div><div className="mt-6 rounded-2xl border border-white/10 p-5"><div className="flex items-center gap-3"><div className="flex size-14 items-center justify-center rounded-2xl bg-cyan-300/10"><UserRound className="size-7 text-cyan-200" /></div><div><p className="font-semibold">Skycoin explorer</p><p className="text-sm text-slate-500">{accent} accent · {density}</p></div></div><div className="mt-5 grid grid-cols-3 gap-2">{["About", "Activity", "Preferences"].map((item) => <div key={item} className="rounded-lg border border-white/10 p-3 text-center text-xs text-slate-500">{item}</div>)}</div></div><div className="mt-5 rounded-2xl border border-amber-300/25 bg-amber-300/[0.06] p-5"><div className="flex items-start gap-3"><ShieldAlert className="mt-0.5 size-5 shrink-0 text-amber-200" /><div><p className="font-semibold text-amber-100">Sync and upload are unavailable</p><p className="mt-2 text-sm leading-6 text-slate-400">No account preference API, image storage, public profile update, device sync, or accessibility preferences service is connected.</p></div></div></div></CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Safety and access review</p><h2 className="mt-2 text-2xl font-black">Customization must not weaken user control</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{[{ label: "Privacy", detail: "Visibility, sharing, export, and deletion remain explicit." , icon: Eye }, { label: "Accessibility", detail: "Contrast, keyboard access, reduced motion, and text scaling require testing.", icon: Accessibility }, { label: "Assets", detail: "Avatar rights, moderation, storage, and deletion require a governed path.", icon: FileImage }, { label: "Account", detail: "Preferences require authenticated persistence and recovery behavior.", icon: LockKeyhole }].map((item) => { const Icon = item.icon; return <div key={item.label} className="flex items-start gap-3 rounded-xl border border-white/10 p-4"><Icon className="mt-0.5 size-5 text-slate-500" /><div><p className="font-semibold text-slate-200">{item.label}</p><p className="mt-1 text-sm leading-6 text-slate-500">{item.detail}</p></div></div>; })}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Customization surface preserved", description: "Theme, accent, density, layout, avatar intent, preview, local save/reset, privacy, and accessibility review remain interactive.", icon: Palette, status: "Local preferences" }, { title: "No sync theater", description: "Account persistence, image storage, public updates, device sync, and accessibility guarantees are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "User control first", description: "Real customization needs privacy, asset rights, accessibility testing, authenticated persistence, recovery, and deletion.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
