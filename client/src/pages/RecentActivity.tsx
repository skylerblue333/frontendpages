import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Activity, Check, Clock3, Download, Filter, LockKeyhole, RefreshCw, Search, ShieldAlert, UserRound, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const events = [{ id: 1, name: "Profile change", category: "Account", actor: "Actor unavailable", detail: "A local account-event concept awaiting identity, timestamp, and audit provenance." }, { id: 2, name: "Course opened", category: "Education", actor: "Learner unknown", detail: "A learning activity concept awaiting consent, session identity, and event source." }, { id: 3, name: "Wallet view", category: "Financial", actor: "Identity unavailable", detail: "A sensitive activity concept that must not imply balances, transactions, or custody." }, { id: 4, name: "Community post", category: "Community", actor: "Author unknown", detail: "A community-event concept awaiting privacy controls, moderation, and retention policy." }];
export default function RecentActivity() {
  const [query, setQuery] = useState(""); const [category, setCategory] = useState("All"); const [selected, setSelected] = useState(1); const [window, setWindow] = useState("Window not set"); const [saved, setSaved] = useState(false); const [showGates, setShowGates] = useState(false);
  const categories = ["All", ...Array.from(new Set(events.map((item) => item.category)))]; const filtered = useMemo(() => events.filter((item) => (category === "All" || item.category === category) && `${item.name} ${item.category} ${item.detail}`.toLowerCase().includes(query.toLowerCase())), [category, query]); const event = events.find((item) => item.id === selected) ?? events[0];
  const reset = () => { setQuery(""); setCategory("All"); setSelected(1); setWindow("Window not set"); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Activity} eyebrow="RecentActivity · Event feed preview" title="Verify the event before showing the timeline." description="Explore local activity-event concepts with search, categories, time-window intent, actors, timestamps, details, privacy, pagination, export, audit, save, reset, and evidence gates. No live users, events, dates, locations, or operational outcome is connected." badge="Activity workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "View saved locally" : "Save view locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review activity gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset view</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Events", value: `${events.length} local`, hint: "No activity loaded", icon: Activity, tone: "cyan" }, { label: "Actors", value: "Unavailable", hint: "No identity source", icon: UserRound, tone: "violet" }, { label: "Timestamps", value: "Unavailable", hint: "No event source", icon: Clock3, tone: "amber" }, { label: "Audit", value: "Unconnected", hint: "No provenance", icon: LockKeyhole, tone: "slate" }]} /><ScreenPreviewBanner title="Activity evidence boundary"><strong>This is a local event-feed preview, not evidence of user activity or an operational timeline.</strong> Event labels, categories, filters, window intent, actor posture, timestamp state, saved state, export posture, and audit gates are browser concepts. No user, event, date, location, device, action, notification, security event, or business outcome is asserted.</ScreenPreviewBanner><section className="flex flex-wrap items-end gap-3"><label className="text-sm text-slate-400">Time window<select value={window} onChange={(event) => setWindow(event.target.value)} className="mt-2 rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Window not set</option><option>Today intent</option><option>Last 7 days intent</option><option>Last 30 days intent</option></select></label><Badge variant="outline" className="border-amber-300/20 text-amber-200">Activity source disconnected</Badge></section><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Search className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local activity concepts" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-4 flex flex-wrap gap-2">{categories.map((entry) => <Button key={entry} onClick={() => setCategory(entry)} size="sm" variant="outline" className={category === entry ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-100" : "border-white/10 text-slate-400"}><Filter className="mr-1 size-3" />{entry}</Button>)}</div><div className="mt-6 space-y-3">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{item.name}</p><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Unverified</Badge></div><div className="mt-4 flex gap-2"><Badge variant="outline" className="border-white/10 text-slate-500">{item.category}</Badge><Badge variant="outline" className="border-white/10 text-slate-500">{item.actor}</Badge></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected activity concept</p><h2 className="mt-2 text-2xl font-black">{event.name}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{event.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Category", value: event.category }, { label: "Window", value: window }, { label: "Actor", value: event.actor }, { label: "Timestamp", value: "Unavailable" }, { label: "Location", value: "Unavailable" }, { label: "Audit", value: "Unconnected" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-8 text-center"><Activity className="mx-auto size-7 text-slate-600" /><p className="mt-3 font-semibold">No activity events loaded</p><p className="mt-2 text-sm text-slate-500">Connect a governed event source before showing user actions, timestamps, locations, or operational conclusions.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400"><Download className="mr-2 size-4" />Export unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Load more unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Open detail unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No activity or audit claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local event label does not prove that a person acted, a timestamp is accurate, or an operational outcome occurred.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Activity gates</p><h2 className="mt-2 text-2xl font-black">What a real activity feed must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Event schema, identity, actor, tenant, authorization, consent, source, retention, and deletion", "Timestamp source, clock, time zone, precision, ordering, edits, late events, and provenance", "Privacy, location, device, IP, financial, health, education, and sensitive-action boundaries", "Pagination, deduplication, replay, filtering, search, sorting, exports, and reproducible queries", "Audit integrity, immutable logs, access review, legal requests, notifications, and incident response", "User-visible status, support, localization, accessibility, rate limits, and safe recovery"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Activity surface preserved", description: "Events, search, categories, windows, actors, timestamps, details, privacy, pagination, export, audit, save/reset, and gates remain interactive.", icon: Activity, status: "Local events" }, { title: "No timeline theater", description: "Users, events, dates, locations, devices, actions, notifications, security events, and operational outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Provenance before history", description: "Real activity feeds need governed events, identity, timestamps, privacy, immutable audit, pagination, and recovery.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
