import { useMemo, useState } from "react";
import { AlertTriangle, CheckCircle2, Clock3, Download, EyeOff, FileText, Filter, LockKeyhole, Search, Server, Settings, Shield, Terminal, UserRound, XCircle } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

type LogEntry = { id: string; method: string; path: string; outcome: "Example success" | "Example failure" | "Redacted example"; actor: string; category: string; detail: string };
const ENTRIES: LogEntry[] = [
  { id: "one", method: "GET", path: "/api/trpc/feedback.list", outcome: "Example success", actor: "service account", category: "Read", detail: "Illustrative response envelope with pagination and request correlation requirements." },
  { id: "two", method: "POST", path: "/api/trpc/orders.create", outcome: "Example failure", actor: "authenticated user", category: "Write", detail: "Illustrative validation failure; never log payment details, tokens, or full personal data." },
  { id: "three", method: "POST", path: "/api/oauth/callback", outcome: "Redacted example", actor: "identity provider", category: "Auth", detail: "Illustrative callback event with sensitive parameters omitted." },
  { id: "four", method: "GET", path: "/api/health", outcome: "Example success", actor: "synthetic probe", category: "Health", detail: "Illustrative probe result; no live uptime or latency is implied." },
];
const CATEGORIES = ["All", "Read", "Write", "Auth", "Health"];

export default function APILogs() { const [query, setQuery] = useState(""); const [category, setCategory] = useState("All"); const [selected, setSelected] = useState<LogEntry | null>(null); const [redaction, setRedaction] = useState(true); const visible = useMemo(() => ENTRIES.filter((entry) => (category === "All" || entry.category === category) && `${entry.method} ${entry.path} ${entry.actor} ${entry.outcome}`.toLowerCase().includes(query.toLowerCase())), [category, query]); return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Terminal} eyebrow="Developer Platform · Logs" title="Make audit trails useful without leaking secrets." description="Explore synthetic API log entries, filters, event detail, redaction, retention, and access controls. This screen does not connect to a live log store or claim event volume, retention, or production activity." badge="Preview log explorer"><div className="flex flex-wrap gap-2"><Button onClick={() => setRedaction((value) => !value)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><EyeOff className="mr-2 size-4" />{redaction ? "Redaction on" : "Redaction policy"}</Button><Button variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><Download className="mr-2 size-4" />Export unavailable</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Example entries", value: String(ENTRIES.length), hint: "Synthetic records only", icon: FileText }, { label: "Live log store", value: "Unavailable", hint: "No production events", icon: Server, tone: "amber" }, { label: "Redaction", value: redaction ? "On" : "Policy", hint: "Secrets stay omitted", icon: EyeOff, tone: "violet" }, { label: "Retention", value: "Unverified", hint: "No policy claim", icon: Clock3, tone: "slate" }]} /><ScreenPreviewBanner title="Log privacy boundary">Synthetic entries, category filters, search, selected-event detail, redaction policy, access controls, and unavailable export state are available for UX review. No token, password, seed phrase, payment detail, private personal data, live event, event count, retention period, or production activity is fabricated.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.9fr_1.1fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="relative"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search logs" aria-label="Search API logs" className="border-white/10 bg-black/20 pl-9 text-white placeholder:text-slate-500" /></div><div className="mt-4 flex gap-2 overflow-x-auto pb-1">{CATEGORIES.map((item) => <Button key={item} size="sm" variant={category === item ? "default" : "outline"} onClick={() => setCategory(item)} className={category === item ? "bg-cyan-300 text-slate-950 hover:bg-cyan-200" : "border-white/15 bg-white/5 text-slate-300 hover:bg-white/10"}><Filter className="mr-1 size-3.5" />{item}</Button>)}</div><div className="mt-4 space-y-2">{visible.map((entry) => <button key={entry.id} onClick={() => setSelected(entry)} className={`w-full rounded-xl border p-4 text-left transition ${selected?.id === entry.id ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-black/15 hover:bg-white/[0.05]"}`}><div className="flex items-center gap-2"><Badge variant="outline" className={entry.method === "GET" ? "border-cyan-300/20 text-cyan-200" : "border-violet-300/20 text-violet-200"}>{entry.method}</Badge><code className="truncate text-xs text-slate-300">{entry.path}</code><Badge variant="outline" className={`ml-auto ${entry.outcome === "Example failure" ? "border-red-300/20 text-red-200" : "border-amber-300/20 text-amber-200"}`}>{entry.outcome}</Badge></div><p className="mt-2 text-xs uppercase tracking-wider text-slate-500">{entry.category} · {entry.actor}</p></button>)}{visible.length === 0 && <ScreenStatePanel type="empty" title="No logs match" description="Try another search or category." />}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6">{selected ? <><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Selected log example</p><h2 className="mt-1 text-2xl font-bold">{selected.method} {selected.path}</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{selected.outcome}</Badge></div><div className="mt-5 rounded-xl border border-white/10 bg-black/20 p-4"><div className="flex items-center gap-2 text-sm text-slate-300"><UserRound className="size-4 text-cyan-300" />Actor: {selected.actor}</div><p className="mt-3 text-sm leading-6 text-slate-300">{selected.detail}</p></div><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Correlation ID", "Timestamp", "Redacted request", "Status and error envelope"].map((item) => <div key={item} className="rounded-lg border border-white/10 bg-black/15 p-4"><CheckCircle2 className="size-4 text-emerald-300" /><p className="mt-3 text-sm text-slate-300">{item}</p><p className="mt-1 text-xs text-slate-500">Required evidence</p></div>)}</div></> : <ScreenStatePanel type="empty" title="Select a log example" description="Inspect redaction, correlation, timestamp, and error-envelope requirements." />}<div className="mt-6 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex items-center gap-2 font-medium text-amber-200"><LockKeyhole className="size-4" />Access boundary</div><p className="mt-2 text-sm leading-6 text-slate-300">Real logs require authenticated, role-scoped access with audit records. Export and retention settings stay unavailable until a verified log backend exists.</p></div></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Sensitive-data redaction", description: "Never log tokens, passwords, seed phrases, payment details, or unnecessary personal data.", icon: EyeOff, status: "Required" }, { title: "Audit access", description: "Log viewers and exports need role scope, purpose, audit trails, and retention policy.", icon: Shield, status: "Required" }, { title: "Honest telemetry", description: "Synthetic examples are visibly separate from live production events and metrics.", icon: AlertTriangle, status: "Guardrail" }]} /></main></div>; }
