import { useMemo, useState } from "react";
import { Check, Filter, GitBranch, KeyRound, LockKeyhole, PackageCheck, RefreshCw, ShieldAlert, Tag, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const packages = [{ id: 1, name: "Web client package", category: "Web", detail: "A local package-lifecycle concept requiring source provenance, API contract, compatibility, release notes, and support ownership.", state: "Unconfigured" }, { id: 2, name: "Wallet package", category: "Crypto", detail: "A high-risk lifecycle concept requiring custody boundary, signing policy, network support, transaction safety, dependency review, and recovery.", state: "Blocked" }, { id: 3, name: "HopeAI package", category: "AI", detail: "An AI package concept requiring model contract, evaluation, safety, privacy, rate limits, telemetry, and deprecation policy.", state: "Needs review" }, { id: 4, name: "Learning package", category: "Education", detail: "An education package concept requiring schema, curriculum authority, learner privacy, accessibility, assessment, and support policy.", state: "Preview" }];
export default function SDKManagement() {
  const [query, setQuery] = useState(""); const [category, setCategory] = useState("All"); const [selected, setSelected] = useState(1); const [channel, setChannel] = useState("Channel not configured"); const [release, setRelease] = useState("Release approval not configured"); const [saved, setSaved] = useState(false); const [showGates, setShowGates] = useState(false); const categories = ["All", ...Array.from(new Set(packages.map((item) => item.category)))]; const filtered = useMemo(() => packages.filter((item) => (category === "All" || item.category === category) && `${item.name} ${item.category} ${item.detail}`.toLowerCase().includes(query.toLowerCase())), [category, query]); const pkg = packages.find((item) => item.id === selected) ?? packages[0]; const reset = () => { setQuery(""); setCategory("All"); setSelected(1); setChannel("Channel not configured"); setRelease("Release approval not configured"); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={PackageCheck} eyebrow="SDK management · Release preview" title="Manage the lifecycle before publishing a package." description="Explore local web, crypto, AI, and education package concepts with search, category filters, release-channel and approval intent, integrity and deprecation gates, save/reset, and blocked publishing actions. No artifact, version, registry, access policy, support promise, compatibility, or release is connected." badge="Package governance workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save package locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review lifecycle gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset package</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Packages", value: `${packages.length} local`, hint: "No registry source", icon: PackageCheck, tone: "cyan" }, { label: "Channel", value: "Unconfigured", hint: "No release source", icon: GitBranch, tone: "violet" }, { label: "Integrity", value: "Unverified", hint: "No artifact source", icon: KeyRound, tone: "amber" }, { label: "Publishing", value: "Blocked", hint: "No approval", icon: ShieldAlert, tone: "slate" }]} /><ScreenPreviewBanner title="SDK lifecycle evidence boundary"><strong>This is a local package-lifecycle preview, not a release manager or registry.</strong> Package cards, filters, channel and approval intent, saved state, lifecycle gates, and disabled publishing actions are browser concepts. No artifact, version, checksum, signature, registry entry, compatibility, support level, deprecation, or production release is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Filter className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local packages" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-4 flex flex-wrap gap-2">{categories.map((entry) => <Button key={entry} onClick={() => setCategory(entry)} size="sm" variant="outline" className={category === entry ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-100" : "border-white/10 text-slate-400"}><Filter className="mr-1 size-3" />{entry}</Button>)}</div><div className="mt-6 space-y-3">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{item.name}</p><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.state}</Badge></div><div className="mt-4"><Badge variant="outline" className="border-white/10 text-slate-500">{item.category}</Badge></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected package lifecycle</p><h2 className="mt-2 text-2xl font-black">{pkg.name}</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{pkg.state}</Badge></div><p className="mt-2 text-sm leading-6 text-slate-400">{pkg.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Category", value: pkg.category }, { label: "Channel", value: channel }, { label: "Approval", value: release }, { label: "Version", value: "Unconfigured" }, { label: "Integrity", value: "Unverified" }, { label: "Deprecation", value: "Unconfigured" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2"><label className="text-sm text-slate-400">Release channel<select value={channel} onChange={(event) => setChannel(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Channel not configured</option><option>Preview intent</option><option>Stable intent</option><option>Long-term-support intent</option><option>Deprecation intent</option></select></label><label className="text-sm text-slate-400">Approval intent<select value={release} onChange={(event) => setRelease(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Release approval not configured</option><option>Engineering review intent</option><option>Security review intent</option><option>Compatibility review intent</option><option>Owner approval intent</option></select></label></div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-8 text-center"><Tag className="mx-auto size-8 text-slate-600" /><p className="mt-3 font-semibold">No package lifecycle evidence loaded</p><p className="mt-2 text-sm text-slate-500">Connect governed source, build artifact, version, registry, signatures, compatibility tests, release notes, security review, access, deprecation, support, and rollback before publishing.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Publish unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Promote unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Deprecate unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Export audit unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No release or integrity claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A package concept does not prove an artifact, version, checksum, signature, registry entry, compatibility, security review, support commitment, deprecation, or production release.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Package lifecycle gates</p><h2 className="mt-2 text-2xl font-black">What a real SDK-management system must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Authenticated source, artifact, version, platform, registry, ownership, timestamp, release channel, and decision record", "Checksums, signatures, SBOM, dependencies, reproducible builds, vulnerability review, provenance, and rollback", "API contract, browser/Node/mobile/server compatibility, wallet/payment/AI/education semantics, accessibility, and localization", "Authentication, authorization, rate limits, secret handling, privacy, telemetry, error states, support, and deprecation", "Crypto, financial, AI, educational, marketplace, security, and user-impact claims require domain review", "Publishing, promotion, registry access, release notes, incident response, legal/support commitments, and accountable approval"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "SDK lifecycle surface preserved", description: "Web, crypto, AI, education packages, filters, channels, approvals, integrity, compatibility, deprecation, promotion, publishing, save/reset, and gates remain interactive.", icon: PackageCheck, status: "Local packages" }, { title: "No registry theater", description: "Artifacts, versions, signatures, registry entries, compatibility, support, deprecation, and releases are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before publish", description: "Real lifecycle management requires reproducible artifacts, provenance, security, compatibility, approvals, rollback, and support ownership.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
