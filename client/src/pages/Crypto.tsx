import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ArrowDown, ArrowUp, ArrowUpDown, Check, Coins, Eye, LockKeyhole, RefreshCw, Shield, Sparkles, TrendingUp, WalletCards, WifiOff, Zap } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const TOKENS = [{ symbol: "SKY444", name: "SKYCOIN4444", purpose: "Utility concept", tone: "from-violet-500 to-fuchsia-500" }, { symbol: "DODGE", name: "Dodge", purpose: "Gaming concept", tone: "from-amber-400 to-orange-500" }, { symbol: "BTC", name: "Bitcoin", purpose: "Educational reference", tone: "from-yellow-400 to-orange-500" }, { symbol: "USDT", name: "Tether", purpose: "Stablecoin reference", tone: "from-emerald-400 to-teal-500" }, { symbol: "ETH", name: "Ethereum", purpose: "Network reference", tone: "from-blue-400 to-indigo-500" }, { symbol: "MONERO", name: "Monero", purpose: "Privacy reference", tone: "from-orange-400 to-red-500" }];
const TX_PREVIEW = [{ type: "Deposit", token: "SKY444", state: "Preview only", note: "No hash" }, { type: "Swap", token: "ETH → SKY", state: "Not submitted", note: "No quote" }, { type: "Send", token: "USDT", state: "Blocked", note: "No recipient" }];

export default function Crypto() {
  const [tab, setTab] = useState("Tokens");
  const [selected, setSelected] = useState(TOKENS[0].symbol);
  const [watchlist, setWatchlist] = useState<string[]>([]);
  const [saved, setSaved] = useState(false);
  const toggleWatch = (symbol: string) => setWatchlist((current) => current.includes(symbol) ? current.filter((item) => item !== symbol) : [...current, symbol]);
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={WalletCards} eyebrow="Crypto hub · Wallet preview" title="Explore the ecosystem without inventing a balance." description="Browse a synthetic token catalog, local watchlist, transaction-state preview, and DeFi readiness cards. No wallet is connected, no price or balance is authoritative, and no transaction, mining, yield, or insurance action is available." badge="Crypto preview"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save watchlist"}</Button><Button onClick={() => { setWatchlist([]); setSelected(TOKENS[0].symbol); setSaved(false); }} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset view</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Assets", value: String(TOKENS.length), hint: "Catalog entries", icon: Coins }, { label: "Wallet", value: "Off", hint: "No custody connection", icon: WifiOff, tone: "amber" }, { label: "Prices", value: "Off", hint: "No market source", icon: TrendingUp, tone: "slate" }, { label: "Watchlist", value: String(watchlist.length), hint: "Local only", icon: Eye, tone: "violet" }]} /><ScreenPreviewBanner title="Crypto evidence boundary"><strong>Token names, purposes, watchlist selections, transaction rows, and DeFi labels are presentation fixtures—not proof of balances, prices, holdings, market value, transfers, mining, yield, TVL, coverage, or blockchain state.</strong> Production crypto requires verified wallet custody, network and contract validation, market-data provenance, transaction hashes, signing, confirmations, risk disclosures, and recovery controls.</ScreenPreviewBanner><section className="flex flex-wrap gap-2">{["Tokens", "Transactions", "DeFi"].map((item) => <button key={item} onClick={() => setTab(item)} className={`rounded-xl border px-4 py-2 text-sm ${tab === item ? "border-cyan-300/40 bg-cyan-300/[0.1] text-cyan-200" : "border-white/10 text-slate-400"}`}>{item}</button>)}</section>{tab === "Tokens" && <section className="grid gap-6 lg:grid-cols-[1fr_0.72fr]"><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{TOKENS.map((token) => <button key={token.symbol} onClick={() => setSelected(token.symbol)} className={`rounded-2xl border p-5 text-left ${selected === token.symbol ? "border-cyan-300/40 bg-cyan-300/[0.07]" : "border-white/10 bg-white/[0.04]"}`}><div className={`flex size-11 items-center justify-center rounded-2xl bg-gradient-to-br ${token.tone} font-black text-slate-950`}>{token.symbol[0]}</div><div className="mt-5 flex items-center justify-between gap-2"><span className="font-black">{token.symbol}</span><Badge variant="outline" className="border-white/10 text-[10px] text-slate-400">Preview</Badge></div><p className="mt-2 text-sm text-slate-400">{token.name}</p><p className="mt-3 text-xs text-slate-500">{token.purpose}</p><Button onClick={(event) => { event.stopPropagation(); toggleWatch(token.symbol); }} variant="outline" className="mt-5 w-full border-white/15 text-xs text-white">{watchlist.includes(token.symbol) ? <><Check className="mr-1 size-3" />Watching locally</> : <><Eye className="mr-1 size-3" />Add watchlist</>}</Button></button>)}</div><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Selected asset</p><h2 className="mt-2 text-3xl font-black">{selected}</h2><div className="mt-6 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-200">No balance or price</p><p className="mt-2 text-sm leading-6 text-slate-400">This selected asset has no wallet balance, market price, 24-hour change, cost basis, or portfolio value attached.</p></div><div className="mt-5 space-y-3">{["Wallet address", "Holdings", "Market source", "Network", "Contract address"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-slate-500">Unavailable</span></div>)}</div></CardContent></Card></section>}{tab === "Transactions" && <section className="space-y-3">{TX_PREVIEW.map((tx) => <Card key={tx.type} className="border-white/10 bg-white/[0.04]"><CardContent className="flex items-center gap-4 p-5"><div className="flex size-10 items-center justify-center rounded-xl bg-violet-300/10 text-violet-200">{tx.type === "Deposit" ? <ArrowDown className="size-5" /> : tx.type === "Send" ? <ArrowUp className="size-5" /> : <ArrowUpDown className="size-5" />}</div><div className="flex-1"><p className="font-semibold">{tx.type} · {tx.token}</p><p className="mt-1 text-sm text-slate-500">{tx.note}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{tx.state}</Badge></CardContent></Card>)}<Card className="border-red-300/20 bg-red-300/[0.05]"><CardContent className="p-5"><p className="font-semibold text-red-200">No transaction history connected</p><p className="mt-2 text-sm leading-6 text-slate-400">Preview rows do not contain amounts, timestamps, sender/recipient addresses, transaction hashes, confirmations, or custody records.</p></CardContent></Card></section>}{tab === "DeFi" && <section className="grid gap-4 md:grid-cols-2"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><TrendingUp className="size-6 text-cyan-300" /><h2 className="mt-4 text-xl font-black">Yield planning</h2><p className="mt-2 text-sm leading-6 text-slate-400">Compare the safety, liquidity, smart-contract, and custody requirements for a future yield product.</p><Badge variant="outline" className="mt-5 border-amber-300/20 text-amber-200">No APY quote</Badge><Button onClick={() => setSaved(true)} variant="outline" className="mt-5 w-full border-white/15 text-white">Save risk questions</Button></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><Shield className="size-6 text-violet-300" /><h2 className="mt-4 text-xl font-black">Coverage planning</h2><p className="mt-2 text-sm leading-6 text-slate-400">Map insurer, pool, exclusions, claims, reserves, and custody requirements before presenting coverage.</p><Badge variant="outline" className="mt-5 border-amber-300/20 text-amber-200">No TVL or policy</Badge><Button onClick={() => setSaved(true)} variant="outline" className="mt-5 w-full border-white/15 text-white">Review safeguards</Button></CardContent></Card></section>}<ScreenFeatureGrid features={[{ title: "A token card is not a balance", description: "Catalog entries do not establish ownership, wallet custody, holdings, price, or portfolio value.", icon: WalletCards, status: "Guardrail" }, { title: "Transactions need hashes", description: "A transfer requires signing, network validation, hash, confirmation, failure, and recovery evidence.", icon: Shield, status: "Required" }, { title: "No fake DeFi state", description: "The preview preserves education and planning without inventing APY, TVL, mining, insurance, or yield results.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
