import { useState } from "react";
import { Archive, Check, ClipboardCheck, FileText, Info, LockKeyhole, RefreshCw, Scale, ShieldAlert, UserRound, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

const documents = [
  { name: "Digital-asset activity record", type: "Transaction evidence", state: "Unavailable", detail: "Requires authoritative wallet, exchange, custody, transaction, cost-basis, timestamps, and jurisdiction sources." },
  { name: "Income and expense worksheet", type: "Tax record concept", state: "Unverified", detail: "Requires identity, tax year, classification rules, source documents, deductions policy, and professional review." },
  { name: "Filing support packet", type: "Review package", state: "Draft only", detail: "Requires taxpayer scope, jurisdiction, forms, signed records, retention, delivery, and accountable acceptance." },
];

export default function TaxDocumentation() {
  const [selected, setSelected] = useState(0);
  const [year, setYear] = useState("Tax year not configured");
  const [jurisdiction, setJurisdiction] = useState("Jurisdiction not configured");
  const [saved, setSaved] = useState(false);
  const [showBoundary, setShowBoundary] = useState(false);
  const [refreshing, setRefreshing] = useState(false);
  const refresh = () => { setRefreshing(true); window.setTimeout(() => setRefreshing(false), 500); };
  const reset = () => { setSelected(0); setYear("Tax year not configured"); setJurisdiction("Jurisdiction not configured"); setSaved(false); setShowBoundary(false); setRefreshing(false); };
  const document = documents[selected] ?? documents[0];
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={FileText} eyebrow="Tax documentation · Records preview" title="Organize tax records without inventing calculations or filings." description="Explore a local tax-documentation workspace with document concepts, tax year, jurisdiction, transaction provenance, classification, cost basis, privacy, review, retention, export, retry, save/reset, and explicit evidence boundaries. No taxpayer identity, transaction, calculation, filing, or tax result is connected." badge="Evidence-bounded records"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save records draft"}</Button><Button onClick={() => setShowBoundary((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showBoundary ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showBoundary ? "Close evidence boundary" : "Review records gates"}</Button><Button onClick={refresh} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className={`mr-2 size-4 ${refreshing ? "animate-spin" : ""}`} />{refreshing ? "Retrying local state" : "Retry document source"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><X className="mr-2 size-4" />Reset records</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Records", value: "Local", hint: "No document source", icon: Archive, tone: "cyan" }, { label: "Identity", value: "Unavailable", hint: "No taxpayer source", icon: UserRound, tone: "violet" }, { label: "Calculations", value: "Blocked", hint: "No tax rule source", icon: Scale, tone: "amber" }, { label: "Filing", value: "Unverified", hint: "No filing source", icon: ClipboardCheck, tone: "slate" }]} /><ScreenPreviewBanner title="Tax-records evidence boundary"><strong>This is a local records-design preview, not evidence that a taxpayer, transaction, income, expense, cost basis, deduction, tax calculation, form, filing, refund, liability, or compliance outcome exists.</strong> Document cards, year and jurisdiction intent, local saved state, retry feedback, and disabled calculate/export/file actions are browser concepts. No personal, financial, legal, or tax conclusion is asserted.</ScreenPreviewBanner><section className="grid gap-4 md:grid-cols-3">{documents.map((item, index) => <button key={item.name} onClick={() => setSelected(index)} className={`rounded-2xl border p-5 text-left transition ${selected === index ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10 bg-white/[0.04]"}`}><div className="flex items-start justify-between"><div className="flex size-11 items-center justify-center rounded-2xl bg-cyan-300/10 text-cyan-200"><FileText className="size-5" /></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.state}</Badge></div><h2 className="mt-4 font-bold">{item.name}</h2><p className="mt-1 text-xs text-cyan-200">{item.type}</p><p className="mt-3 text-sm leading-6 text-slate-400">{item.detail}</p></button>)}</section><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200">Selected record concept</p><h2 className="mt-2 text-2xl font-black">{document.name}</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{document.state}</Badge></div><p className="mt-3 text-sm leading-6 text-slate-400">{document.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Taxpayer identity", value: "Unavailable", icon: UserRound }, { label: "Tax year / jurisdiction", value: "Unconfigured", icon: Scale }, { label: "Source documents", value: "Unavailable", icon: Archive }, { label: "Transaction provenance", value: "Unverified", icon: FileText }, { label: "Classification / cost basis", value: "Blocked", icon: ClipboardCheck }, { label: "Review / acceptance", value: "Required", icon: ShieldAlert }].map(({ label, value, icon: Icon }) => <div key={label} className="flex items-center gap-3 rounded-xl border border-white/10 p-4"><Icon className="size-4 text-cyan-300" /><span className="flex-1 text-sm text-slate-300">{label}</span><span className="text-xs text-amber-200">{value}</span></div>)}</div><div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold text-slate-300">Tax year intent<select value={year} onChange={(event) => setYear(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white"><option>Tax year not configured</option><option>Current-year intent</option><option>Prior-year intent</option><option>Amended-year intent</option></select></label><label className="text-sm font-semibold text-slate-300">Jurisdiction intent<select value={jurisdiction} onChange={(event) => setJurisdiction(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white"><option>Jurisdiction not configured</option><option>Federal intent</option><option>State or provincial intent</option><option>Multi-jurisdiction review required</option></select></label></div><div className="mt-6 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Import records unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Calculate unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Export packet unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">File unavailable</Button></div></CardContent></Card><ScreenStatePanel type="unavailable" title="Tax data source unavailable" description="Connect authenticated taxpayer scope, authoritative records, jurisdiction rules, document storage, professional review, retention, and filing controls before enabling tax workflows." /></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><Info className="size-5 text-cyan-300" /><div><p className="font-bold">Tax-documentation evidence contract</p><p className="mt-1 text-sm text-slate-400">Reliable tax records require source provenance, identity and jurisdiction scope, classification rules, cost-basis policy, document integrity, privacy, retention, review, and accountable filing acceptance.</p></div></div><div className="mt-5 grid gap-3 md:grid-cols-2">{["Taxpayer identity, account scope, tax year, jurisdiction, and consent", "Wallet, exchange, custody, transaction, timestamp, and source provenance", "Income, expense, basis, classification, deduction, and jurisdiction rules", "Document integrity, privacy, encryption, retention, access, and export", "Professional review, correction, amendment, approval, and audit history", "Filing forms, submission authority, receipt, deadline, and recovery controls"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3 text-sm text-slate-300"><LockKeyhole className="size-4 text-amber-300" />{item}<span className="ml-auto text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card>{showBoundary && <Card className="border-amber-300/20 bg-amber-300/[0.06]"><CardContent className="p-6"><p className="font-semibold text-amber-100">No tax calculation or filing claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local record concept does not prove taxpayer identity, transaction history, cost basis, income, deduction, calculation, filing, refund, liability, or compliance outcome.</p></CardContent></Card>}<ScreenFeatureGrid features={[{ title: "Tax records surface preserved", description: "Documents, identity, year, jurisdiction, transaction provenance, classification, cost basis, privacy, review, export, retry, save/reset, and filing gates remain visible.", icon: FileText, status: "Local records" }, { title: "No filing theater", description: "Taxpayer facts, transactions, calculations, forms, submissions, refunds, liabilities, and compliance outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before conclusion", description: "Real tax workflows require authoritative records, jurisdiction rules, privacy, review, filing authority, receipts, and professional accountability.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
