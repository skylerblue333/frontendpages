import FeatureUnavailable from "@/components/FeatureUnavailable";

const TaxDocumentation = () => (
  <FeatureUnavailable
    title="Tax Documentation"
    description="Tax Documentation is intentionally held at a truthful release boundary until its real provider, authorization, persistence, monitoring, and acceptance evidence are complete."
    capability="Tax Documentation on /tax-documentation"
    nextStep="Return to the launch hub"
  />
);

export default TaxDocumentation;
