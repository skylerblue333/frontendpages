import { useState } from "react";
import { Activity, Check, Clock3, GitBranch, Info, KeyRound, LockKeyhole, Play, RefreshCw, ShieldAlert, SlidersHorizontal, Workflow, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

const workflows = [
  { name: "Daily content review", trigger: "Schedule intent", state: "Draft", detail: "Requires authenticated owner, task definition, schedule, approval, worker, idempotency, and audit evidence." },
  { name: "Market-alert concept", trigger: "Event intent", state: "Unverified", detail: "Requires an authoritative event source, threshold policy, notification contract, rate limits, and safe recovery." },
  { name: "Support triage concept", trigger: "Webhook intent", state: "Blocked", detail: "Requires signed webhook verification, privacy controls, queue ownership, retry, and escalation policy." },
];

export default function TaskAutomation() {
  const [selected, setSelected] = useState(0);
  const [mode, setMode] = useState("Manual review intent");
  const [saved, setSaved] = useState(false);
  const [showBoundary, setShowBoundary] = useState(false);
  const [refreshing, setRefreshing] = useState(false);
  const refresh = () => { setRefreshing(true); window.setTimeout(() => setRefreshing(false), 500); };
  const reset = () => { setSelected(0); setMode("Manual review intent"); setSaved(false); setShowBoundary(false); setRefreshing(false); };
  const workflow = workflows[selected] ?? workflows[0];
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Workflow} eyebrow="Task automation · Workflow preview" title="Design automations without claiming they executed." description="Explore a local workflow workspace with task definitions, triggers, schedules, approvals, integrations, secrets, idempotency, retries, rate limits, logs, rollback, ownership, save/reset, and explicit execution boundaries. No task, schedule, webhook, integration call, worker, or business outcome is connected." badge="Evidence-bounded automation"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save workflow draft"}</Button><Button onClick={() => setShowBoundary((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showBoundary ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showBoundary ? "Close evidence boundary" : "Review execution gates"}</Button><Button onClick={refresh} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className={`mr-2 size-4 ${refreshing ? "animate-spin" : ""}`} />{refreshing ? "Retrying local state" : "Retry workflow source"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><X className="mr-2 size-4" />Reset workflow</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Workflows", value: "Local", hint: "No task source", icon: Workflow, tone: "cyan" }, { label: "Triggers", value: "Unverified", hint: "No event source", icon: Activity, tone: "violet" }, { label: "Execution", value: "Blocked", hint: "No worker source", icon: Play, tone: "amber" }, { label: "Recovery", value: "Required", hint: "No runbook source", icon: ShieldAlert, tone: "slate" }]} /><ScreenPreviewBanner title="Automation evidence boundary"><strong>This is a local workflow-design preview, not evidence that a task was scheduled, triggered, executed, retried, completed, delivered, or produced a business result.</strong> Workflow cards, trigger and schedule intent, local saved state, retry feedback, and disabled run/publish actions are browser concepts. No integration call, message, financial action, user change, completion, or success is asserted.</ScreenPreviewBanner><section className="grid gap-4 md:grid-cols-3">{workflows.map((item, index) => <button key={item.name} onClick={() => setSelected(index)} className={`rounded-2xl border p-5 text-left transition ${selected === index ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10 bg-white/[0.04]"}`}><div className="flex items-start justify-between"><div className="flex size-11 items-center justify-center rounded-2xl bg-cyan-300/10 text-cyan-200"><Workflow className="size-5" /></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.state}</Badge></div><h2 className="mt-4 font-bold">{item.name}</h2><p className="mt-1 text-xs text-cyan-200">{item.trigger}</p><p className="mt-3 text-sm leading-6 text-slate-400">{item.detail}</p></button>)}</section><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200">Selected workflow concept</p><h2 className="mt-2 text-2xl font-black">{workflow.name}</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{workflow.state}</Badge></div><p className="mt-3 text-sm leading-6 text-slate-400">{workflow.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Owner / authorization", value: "Unverified", icon: KeyRound }, { label: "Task definition", value: "Draft", icon: Workflow }, { label: "Trigger / schedule", value: "Unconfigured", icon: Clock3 }, { label: "Integration / secret", value: "Protected", icon: LockKeyhole }, { label: "Worker / execution", value: "Unavailable", icon: Play }, { label: "Retry / idempotency", value: "Required", icon: RefreshCw }].map(({ label, value, icon: Icon }) => <div key={label} className="flex items-center gap-3 rounded-xl border border-white/10 p-4"><Icon className="size-4 text-cyan-300" /><span className="flex-1 text-sm text-slate-300">{label}</span><span className="text-xs text-amber-200">{value}</span></div>)}</div><label className="mt-6 block text-sm font-semibold text-slate-300">Execution mode intent<select value={mode} onChange={(event) => setMode(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white"><option>Manual review intent</option><option>Scheduled intent</option><option>Event-triggered intent</option><option>Webhook-triggered intent</option></select></label><div className="mt-6 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Run task unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Publish workflow unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Test trigger unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">View logs unavailable</Button></div></CardContent></Card><ScreenStatePanel type="unavailable" title="Automation runtime unavailable" description="Connect governed task storage, scheduler, event sources, worker execution, integration credentials, observability, approvals, and rollback data before enabling automation." /></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><Info className="size-5 text-cyan-300" /><div><p className="font-bold">Automation safety contract</p><p className="mt-1 text-sm text-slate-400">Real automation requires typed inputs and outputs, authenticated ownership, explicit approval, secret custody, signed triggers, idempotency, retries, rate limits, observability, rollback, and accountable recovery.</p></div></div><div className="mt-5 grid gap-3 md:grid-cols-2">{["Task schema, owner, authorization, validation, and versioning", "Schedule, event, webhook signature, freshness, and deduplication", "Server-side secrets, least privilege, rotation, and redacted logs", "Idempotency, concurrency, retry, backoff, timeout, and rate limits", "Worker, integration, delivery, audit, rollback, and incident evidence", "Accessible approval, pause, cancel, delete, recovery, and escalation"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3 text-sm text-slate-300"><LockKeyhole className="size-4 text-amber-300" />{item}<span className="ml-auto text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card>{showBoundary && <Card className="border-amber-300/20 bg-amber-300/[0.06]"><CardContent className="p-6"><p className="font-semibold text-amber-100">No execution or completion claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local workflow draft does not prove an owner, trigger, schedule, integration, worker run, delivery, completion, retry, or business outcome.</p></CardContent></Card>}<ScreenFeatureGrid features={[{ title: "Automation surface preserved", description: "Tasks, triggers, schedules, approvals, integrations, secrets, retries, idempotency, rate limits, logs, rollback, retry, save/reset, and gates remain visible.", icon: Workflow, status: "Local workflow" }, { title: "No execution theater", description: "Schedules, webhooks, worker runs, messages, payments, account changes, completions, and business outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before running", description: "Real automation requires ownership, validation, signed triggers, secret custody, idempotency, observability, rollback, and audit.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
