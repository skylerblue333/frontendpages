import { useState } from "react";
import { Activity, Check, CircleDollarSign, Coins, Info, LockKeyhole, RefreshCw, ShieldAlert, SlidersHorizontal, X, Zap } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

const assets = [
  { name: "Synthetic SKY concept", symbol: "sSKY", state: "Unverified", detail: "Requires oracle feed, collateral asset, issuance policy, redemption rules, and contract verification." },
  { name: "Synthetic USD concept", symbol: "sUSD", state: "Unavailable", detail: "Requires price source, collateral ratio, liquidation policy, reserves, and settlement evidence." },
  { name: "Synthetic index concept", symbol: "sINDEX", state: "Unconfigured", detail: "Requires index methodology, constituent data, rebalancing, custody, and risk governance." },
];

export default function Synthetics() {
  const [selected, setSelected] = useState(0);
  const [collateral, setCollateral] = useState("Collateral not configured");
  const [mode, setMode] = useState("Design intent");
  const [saved, setSaved] = useState(false);
  const [showBoundary, setShowBoundary] = useState(false);
  const [refreshing, setRefreshing] = useState(false);
  const refresh = () => { setRefreshing(true); window.setTimeout(() => setRefreshing(false), 500); };
  const reset = () => { setSelected(0); setCollateral("Collateral not configured"); setMode("Design intent"); setSaved(false); setShowBoundary(false); setRefreshing(false); };
  const asset = assets[selected] ?? assets[0];
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Coins} eyebrow="Synthetics · Protocol preview" title="Design synthetic assets without inventing collateral or settlement." description="Explore a local synthetic-asset workspace with oracle provenance, collateral, mint and burn intent, ratios, liquidation, risk, custody, authorization, settlement, monitoring, retry, save/reset, and explicit evidence boundaries. No asset, price, collateral, position, liquidation, or financial result is connected." badge="Evidence-bounded protocol design"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save protocol plan"}</Button><Button onClick={() => setShowBoundary((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showBoundary ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showBoundary ? "Close evidence boundary" : "Review protocol gates"}</Button><Button onClick={refresh} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className={`mr-2 size-4 ${refreshing ? "animate-spin" : ""}`} />{refreshing ? "Retrying local state" : "Retry oracle source"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><X className="mr-2 size-4" />Reset protocol</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Oracle", value: "Unverified", hint: "No price source", icon: Activity, tone: "cyan" }, { label: "Collateral", value: "Unavailable", hint: "No reserve source", icon: CircleDollarSign, tone: "violet" }, { label: "Risk", value: "Required", hint: "No policy source", icon: ShieldAlert, tone: "amber" }, { label: "Settlement", value: "Blocked", hint: "No contract source", icon: Zap, tone: "slate" }]} /><ScreenPreviewBanner title="Synthetic-asset evidence boundary"><strong>This is a local protocol-design preview, not evidence that a synthetic asset, oracle price, collateral balance, position, mint, burn, liquidation, redemption, transaction, or financial result exists.</strong> Asset cards, collateral selectors, risk intent, local saved state, retry feedback, and disabled mint/burn/redeem actions are browser concepts. No price, reserve, ownership, yield, solvency, or gain/loss is asserted.</ScreenPreviewBanner><section className="grid gap-4 md:grid-cols-3">{assets.map((item, index) => <button key={item.symbol} onClick={() => setSelected(index)} className={`rounded-2xl border p-5 text-left transition ${selected === index ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10 bg-white/[0.04]"}`}><div className="flex items-start justify-between"><div className="flex size-11 items-center justify-center rounded-2xl bg-cyan-300/10 text-cyan-200"><Coins className="size-5" /></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.state}</Badge></div><h2 className="mt-4 font-bold">{item.name}</h2><p className="mt-1 text-xs text-cyan-200">{item.symbol}</p><p className="mt-3 text-sm leading-6 text-slate-400">{item.detail}</p></button>)}</section><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200">Selected asset concept</p><h2 className="mt-2 text-2xl font-black">{asset.name}</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{asset.state}</Badge></div><p className="mt-3 text-sm leading-6 text-slate-400">{asset.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Oracle / price", value: "Unverified", icon: Activity }, { label: "Collateral / reserve", value: "Unavailable", icon: CircleDollarSign }, { label: "Mint / burn policy", value: "Unconfigured", icon: Coins }, { label: "Collateral ratio", value: "Unmeasured", icon: SlidersHorizontal }, { label: "Liquidation rules", value: "Required", icon: ShieldAlert }, { label: "Settlement / redemption", value: "Blocked", icon: Zap }].map(({ label, value, icon: Icon }) => <div key={label} className="flex items-center gap-3 rounded-xl border border-white/10 p-4"><Icon className="size-4 text-cyan-300" /><span className="flex-1 text-sm text-slate-300">{label}</span><span className="text-xs text-amber-200">{value}</span></div>)}</div><div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold text-slate-300">Collateral intent<select value={collateral} onChange={(event) => setCollateral(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white"><option>Collateral not configured</option><option>Stablecoin intent</option><option>Native asset intent</option><option>Multi-asset policy required</option></select></label><label className="text-sm font-semibold text-slate-300">Operation mode<select value={mode} onChange={(event) => setMode(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white"><option>Design intent</option><option>Mint policy intent</option><option>Burn policy intent</option><option>Redeem policy intent</option></select></label></div><div className="mt-6 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Mint unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Burn unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Redeem unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Liquidate unavailable</Button></div></CardContent></Card><ScreenStatePanel type="unavailable" title="Synthetic protocol source unavailable" description="Connect verified oracle, collateral, smart-contract, wallet authorization, risk, liquidation, settlement, monitoring, and audit sources before enabling protocol operations." /></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><Info className="size-5 text-cyan-300" /><div><p className="font-bold">Synthetic-asset safety contract</p><p className="mt-1 text-sm text-slate-400">Real synthetic issuance requires independent oracle validation, collateral custody, solvency and ratio rules, liquidation fairness, contract review, authorization, pause controls, settlement, reconciliation, and incident recovery.</p></div></div><div className="mt-5 grid gap-3 md:grid-cols-2">{["Oracle identity, freshness, deviation, fallback, and manipulation controls", "Collateral custody, valuation, reserves, ratios, and solvency evidence", "Mint, burn, redeem, liquidation, pause, and emergency governance", "Wallet authorization, nonce, replay, duplicate, and transaction controls", "Contract audits, monitoring, reconciliation, failure, and incident recovery", "Risk disclosure, privacy, suitability, access, and user-readable receipts"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3 text-sm text-slate-300"><LockKeyhole className="size-4 text-amber-300" />{item}<span className="ml-auto text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card>{showBoundary && <Card className="border-amber-300/20 bg-amber-300/[0.06]"><CardContent className="p-6"><p className="font-semibold text-amber-100">No synthetic-asset or solvency claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local protocol concept does not prove an oracle price, collateral, reserve, asset ownership, mint, burn, liquidation, redemption, solvency, yield, transaction, or financial outcome.</p></CardContent></Card>}<ScreenFeatureGrid features={[{ title: "Synthetic protocol surface preserved", description: "Assets, oracles, collateral, mint/burn, ratios, liquidation, risk, custody, authorization, settlement, retry, save/reset, and execution gates remain visible.", icon: Coins, status: "Local protocol" }, { title: "No solvency theater", description: "Prices, reserves, positions, collateral, liquidations, transactions, yields, and financial outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before issuance", description: "Real synthetic assets require oracle, collateral, contract, authorization, risk, settlement, monitoring, and audit evidence.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
