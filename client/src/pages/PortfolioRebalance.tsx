import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { AlertTriangle, BarChart3, Check, CircleDollarSign, ClipboardCheck, LockKeyhole, RefreshCw, Scale, ShieldAlert, ShieldCheck, Shuffle, Target, TriangleAlert, Wallet, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const assets = ["SKY444 concept", "BTC concept", "Cash concept"];
const evidence = ["Authenticated holdings and current quantities", "Timestamped prices, currency, fees, and tax lots", "Target allocation and drift methodology", "Liquidity, risk, tax, and suitability review", "Human approval, order validation, and transaction reconciliation"];
export default function PortfolioRebalance() {
  const [selectedAsset, setSelectedAsset] = useState(assets[0]);
  const [mode, setMode] = useState("Target allocation");
  const [drift, setDrift] = useState("5%");
  const [checks, setChecks] = useState<string[]>([]);
  const [acknowledged, setAcknowledged] = useState(false);
  const [saved, setSaved] = useState(false);
  const toggle = (item: string) => setChecks((items) => items.includes(item) ? items.filter((value) => value !== item) : [...items, item]);
  const reset = () => { setSelectedAsset(assets[0]); setMode("Target allocation"); setDrift("5%"); setChecks([]); setAcknowledged(false); setSaved(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Shuffle} eyebrow="PortfolioRebalance · Financial preview" title="Plan allocation changes without pretending a trade is ready." description="Explore target allocation concepts, drift assumptions, trade intent, fee and tax unknowns, review acknowledgment, and execution gates. No holdings, prices, tax lots, account, wallet, order venue, or transaction source is connected." badge="Rebalance preview"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} disabled={checks.length === 0 || !acknowledged} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Plan saved locally" : "Save local plan"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset planner</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Holdings", value: "Unavailable", hint: "No account source", icon: Wallet, tone: "cyan" }, { label: "Drift", value: drift, hint: "Local threshold", icon: Scale, tone: "violet" }, { label: "Trade intent", value: "Blocked", hint: "No venue", icon: Shuffle, tone: "amber" }, { label: "Fees/tax", value: "Unknown", hint: "Not modeled", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="Rebalance evidence boundary"><strong>This is not an execution-ready rebalance recommendation.</strong> Asset labels, drift thresholds, target mode, trade intent, checklist, and acknowledgment are local planning state. They do not represent holdings, prices, allocation drift, tax impact, suitability, order quantities, liquidity, execution, or a recommendation to act. No financial service is connected.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.82fr_1fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Rebalance mode</p><h2 className="mt-2 text-2xl font-black">Set planning assumptions</h2><div className="mt-5 grid gap-2 sm:grid-cols-2">{["Target allocation", "Drift review", "Learning scenario", "Manual review"].map((item) => <button key={item} onClick={() => setMode(item)} className={`rounded-xl border p-3 text-left text-sm ${mode === item ? "border-cyan-300/40 bg-cyan-300/[0.06] text-cyan-200" : "border-white/10 text-slate-400"}`}>{item}</button>)}</div><p className="mt-6 text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Drift threshold</p><div className="mt-3 flex gap-2">{["2%", "5%", "10%", "Unset"].map((item) => <button key={item} onClick={() => setDrift(item)} className={`rounded-lg border px-3 py-2 text-xs ${drift === item ? "border-amber-300/40 bg-amber-300/10 text-amber-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div><div className="mt-6 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex gap-3"><TriangleAlert className="mt-0.5 size-5 shrink-0 text-amber-200" /><p className="text-sm leading-6 text-slate-300">A drift threshold is an educational label. Without timestamped holdings and prices, no drift or trade size can be calculated.</p></div></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Local allocation concepts</p><h2 className="mt-2 text-2xl font-black">Review asset intent</h2></div><Badge variant="outline" className="border-white/10 text-amber-200">Source off</Badge></div><div className="mt-5 grid gap-3">{assets.map((item) => <button key={item} onClick={() => setSelectedAsset(item)} className={`flex items-center gap-3 rounded-xl border p-4 text-left ${selectedAsset === item ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex size-10 items-center justify-center rounded-xl bg-cyan-300/10"><BarChart3 className="size-5 text-cyan-200" /></div><div className="flex-1"><p className="font-semibold">{item}</p><p className="mt-1 text-sm text-slate-500">Current: unavailable · Target: unset · Drift: unmeasured</p></div><span className="text-xs text-amber-200">Unverified</span></button>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2">{[{ label: "Selected asset", value: selectedAsset }, { label: "Current weight", value: "Unavailable" }, { label: "Target weight", value: "Unset" }, { label: "Trade quantity", value: "Blocked" }, { label: "Fees", value: "Unknown" }, { label: "Tax impact", value: "Unknown" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Evidence gate</p><h2 className="mt-2 text-2xl font-black">Before any rebalance action</h2><div className="mt-5 space-y-2">{evidence.map((item) => <button key={item} onClick={() => toggle(item)} className={`flex w-full items-center gap-3 rounded-xl border p-3 text-left ${checks.includes(item) ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><span className="flex size-5 items-center justify-center rounded border border-white/20">{checks.includes(item) ? "✓" : ""}</span><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></button>)}</div><label className="mt-5 flex items-start gap-3 rounded-xl border border-white/10 p-4"><input type="checkbox" checked={acknowledged} onChange={(event) => setAcknowledged(event.target.checked)} className="mt-1 accent-cyan-300" /><span className="text-sm leading-6 text-slate-300">I understand this is a local planning worksheet, not a trade instruction or financial recommendation.</span></label><Button disabled={checks.length === 0 || !acknowledged} className="mt-5 bg-cyan-300 text-slate-950 hover:bg-cyan-200"><ClipboardCheck className="mr-2 size-4" />{checks.length}/{evidence.length} checks · {acknowledged ? "Acknowledged" : "Acknowledgment needed"}</Button></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><LockKeyhole className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake execution</h2><p className="mt-3 text-sm leading-6 text-slate-400">Changing modes, thresholds, asset selection, checks, or acknowledgment changes browser state only. It does not calculate drift, create orders, access a venue, move assets, estimate tax, or provide advice.</p></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Production checklist</p><h2 className="mt-2 text-2xl font-black">Rebalance must prove</h2><div className="mt-5 space-y-3">{["Authenticated holdings, prices, cost basis, tax lots, liquidity, and account permissions", "Target allocation, drift math, rounding, minimum trade, fees, slippage, and tax methodology", "Suitability, risk capacity, horizon, constraints, disclosures, and human approval", "Order validation, idempotency, confirmation, signing, status, failure, rollback, and reconciliation", "Tests for stale/missing data, partial fills, venue outage, conflicting updates, and unsafe actions"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Rebalance surface preserved", description: "Modes, thresholds, asset selection, target/current states, evidence gate, acknowledgment, and save intent remain interactive.", icon: Shuffle, status: "Local planner" }, { title: "Orders and accounts are off", description: "Holdings, prices, venues, balances, trade quantities, fees, taxes, and execution remain unavailable.", icon: ShieldAlert, status: "Guardrail" }, { title: "No recommendation", description: "No rebalance amount, trade instruction, or financial action is fabricated.", icon: WifiOff, status: "Unavailable" }]} /></section></main></div>;
}
