import { useMemo, useState } from "react";
import { Archive, Bot, CheckCircle2, ClipboardList, FileText, Image, Layers3, LockKeyhole, Plus, Search, Shield, Sparkles, Wand2 } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

type Batch = { id: string; name: string; format: string; purpose: string; status: "Draft plan" | "Provider required" | "Review required"; description: string };
const BATCHES: Batch[] = [
  { id: "copy", name: "Campaign copy set", format: "Text", purpose: "Marketing", status: "Provider required", description: "A batch plan for headlines and descriptions requiring prompt versioning, brand review, and model disclosure." },
  { id: "images", name: "Creator thumbnail set", format: "Image", purpose: "Media", status: "Review required", description: "An image batch plan requiring style references, rights review, content safety, and per-output approval." },
  { id: "lessons", name: "Lesson question set", format: "Structured", purpose: "Education", status: "Draft plan", description: "A structured batch plan requiring schema validation, answer-key review, and accessibility checks." },
  { id: "summaries", name: "Research summaries", format: "Text", purpose: "Research", status: "Provider required", description: "A research batch plan requiring source citations, freshness, uncertainty, and human verification." },
];
const REQUIREMENTS = ["Input manifest and schema", "Prompt or template version", "Provider and model evidence", "Per-output validation", "Human review threshold", "Export and retention policy"];

export default function BatchGeneration() { const [query, setQuery] = useState(""); const [batches, setBatches] = useState(BATCHES); const [selected, setSelected] = useState<Batch | null>(null); const [prepared, setPrepared] = useState(false); const visible = useMemo(() => batches.filter((batch) => `${batch.name} ${batch.format} ${batch.purpose} ${batch.status} ${batch.description}`.toLowerCase().includes(query.toLowerCase())), [batches, query]); const addBatch = () => { const draft = { id: `draft-${batches.length + 1}`, name: `Batch plan ${batches.length + 1}`, format: "Choose format", purpose: "Define purpose", status: "Draft plan" as const, description: "Local batch placeholder; no prompt was sent and no output was created." }; setBatches((current) => [draft, ...current]); setSelected(draft); setPrepared(true); }; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Layers3} eyebrow="AI Studio · Batch Generation" title="Design repeatable batches before you generate anything." description="Explore batch templates, input manifests, prompt versioning, per-output validation, human review, and export policy. This page does not claim live model access, generated outputs, token usage, processing time, or completed exports." badge="Preview batch workspace"><div className="flex flex-wrap gap-2"><Button onClick={addBatch} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Plus className="mr-2 size-4" />Add local batch plan</Button><Button variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><Shield className="mr-2 size-4" />Review gates</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Batch plans", value: String(batches.length), hint: "Local generation designs", icon: Layers3 }, { label: "Live generation", value: "Unavailable", hint: "No model provider connected", icon: Bot, tone: "amber" }, { label: "Review gates", value: String(REQUIREMENTS.length), hint: "Evidence per output", icon: CheckCircle2, tone: "violet" }, { label: "Exports", value: "Unverified", hint: "No output package created", icon: Archive, tone: "slate" }]} /><ScreenPreviewBanner title="Batch-generation evidence boundary">Batch templates, search, local plan creation, selected detail, validation requirements, and unavailable generation state are available for UX review. No prompt was sent, no model output, image, token usage, completion count, quality score, or export result is fabricated.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.85fr_1.15fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="relative"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search batch plans" aria-label="Search batch plans" className="border-white/10 bg-black/20 pl-9 text-white placeholder:text-slate-500" /></div><div className="mt-4 space-y-2">{visible.map((batch) => <button key={batch.id} onClick={() => { setSelected(batch); setPrepared(false); }} className={`w-full rounded-xl border p-4 text-left transition ${selected?.id === batch.id ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-black/15 hover:bg-white/[0.05]"}`}><div className="flex items-center gap-2"><Layers3 className="size-4 text-cyan-300" /><span className="font-semibold">{batch.name}</span><Badge variant="outline" className="ml-auto border-amber-300/20 text-amber-200">{batch.status}</Badge></div><p className="mt-2 text-xs uppercase tracking-wider text-slate-500">{batch.format} · {batch.purpose}</p><p className="mt-2 text-sm leading-6 text-slate-400">{batch.description}</p></button>)}{visible.length === 0 && <ScreenStatePanel type="empty" title="No batch plans match" description="Try another format, purpose, or keyword." />}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6">{selected ? <><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Selected batch plan</p><h2 className="mt-1 text-2xl font-bold">{selected.name}</h2><p className="mt-2 text-sm text-slate-400">{selected.format} · {selected.purpose}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{selected.status}</Badge></div><p className="mt-5 text-sm leading-6 text-slate-300">{selected.description}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{REQUIREMENTS.map((item) => <div key={item} className="rounded-lg border border-white/10 bg-black/15 p-4"><CheckCircle2 className="size-4 text-emerald-300" /><p className="mt-3 text-sm text-slate-300">{item}</p><p className="mt-1 text-xs text-slate-500">Evidence required</p></div>)}</div><div className="mt-6 flex flex-wrap gap-2"><Button size="sm" onClick={() => setPrepared(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Wand2 className="mr-2 size-4" />Prepare local batch</Button><Button size="sm" variant="outline" disabled className="border-white/10 text-slate-500"><FileText className="mr-2 size-4" />Outputs unavailable</Button></div>{prepared && <div className="mt-5 rounded-xl border border-cyan-300/20 bg-cyan-300/[0.06] p-4"><div className="flex items-center gap-2 font-medium text-cyan-200"><Sparkles className="size-4" />Local batch plan prepared</div><p className="mt-2 text-sm leading-6 text-slate-300">No model request was sent and no output package was created. Connect an approved provider and review each output before export.</p></div>}</> : <ScreenStatePanel type="empty" title="Select a batch plan" description="Inspect manifest, prompt, provider, validation, review, and export requirements." />}</CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Manifest before generation", description: "Define input schema, limits, privacy, prompt version, and expected output before sending a batch.", icon: ClipboardList, status: "Required" }, { title: "Review each output", description: "Validate structure, safety, accuracy, rights, and human approval thresholds rather than trusting a batch count.", icon: Shield, status: "Required" }, { title: "No fake outputs", description: "A local batch plan is not generated text, an image, a model run, a token bill, or an export.", icon: LockKeyhole, status: "Guardrail" }]} /></main></div>; }
