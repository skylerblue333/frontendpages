import { useMemo, useState } from "react";
import { Check, Crown, Flag, Gamepad2, Hash, Plus, RefreshCw, Shield, ShieldAlert, Swords, Target, Trophy, Users, WalletCards, WifiOff } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

type Clan = { id: string; name: string; tag: string; color: string; note: string };
const CLANS: Clan[] = [{ id: "wolves", name: "Shadow Wolves", tag: "WOLF", color: "from-red-500 to-rose-600", note: "Synthetic clan used to review competitive UI states." }, { id: "sentinels", name: "Sky Sentinels", tag: "SKY4", color: "from-cyan-500 to-blue-600", note: "Synthetic clan; no member roster or match history is connected." }, { id: "forge", name: "Iron Forge", tag: "IRON", color: "from-amber-500 to-orange-600", note: "Synthetic clan; rank and power are intentionally unavailable." }];
const MODES = ["Capture", "Relay", "Build challenge"];
export default function ClanWars() { const [tab, setTab] = useState("Wars"); const [clan, setClan] = useState(CLANS[0]); const [mode, setMode] = useState(MODES[0]); const [opponent, setOpponent] = useState(CLANS[1]); const [name, setName] = useState(""); const [tag, setTag] = useState(""); const [saved, setSaved] = useState(false); const planReady = Boolean(clan && opponent && mode); const tabs = ["Wars", "Clans", "Create"];
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Swords} eyebrow="Games · Clan Wars" title="Stage the match before claiming a victory." description="Explore synthetic clan records, match-plan inputs, and local clan creation. This page does not connect players, run matchmaking, record wins, distribute rewards, transfer tokens, or create a clan on a server." badge="Preview competitive sandbox"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Flag className="mr-2 size-4" />Save local war plan</Button><Button onClick={() => { setSaved(false); setTab("Wars"); setMode(MODES[0]); }} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset board</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Clans", value: String(CLANS.length), hint: "Synthetic records", icon: Shield }, { label: "Wars", value: "1", hint: "Local plan", icon: Swords, tone: "violet" }, { label: "Matchmaking", value: "Off", hint: "No player service", icon: WifiOff, tone: "amber" }, { label: "Rewards", value: "Blocked", hint: "No token transfer", icon: WalletCards, tone: "slate" }]} /><ScreenPreviewBanner title="Competitive evidence boundary">A clan card, score bar, rank, timer, or trophy is not proof of a player, roster, match, victory, reward, token transfer, or live leaderboard. A production game needs authenticated participants, rules, matchmaking, anti-cheat, event sourcing, reconnect and dispute handling, result verification, reward settlement, and safe failure states.</ScreenPreviewBanner><section className="flex flex-wrap gap-2">{tabs.map((item) => <button key={item} onClick={() => setTab(item)} className={`rounded-xl border px-4 py-2 text-sm ${tab === item ? "border-cyan-300/40 bg-cyan-300/[0.1] text-cyan-200" : "border-white/10 text-slate-400"}`}>{item}</button>)}</section>{tab === "Wars" && <section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Local match planner</p><h2 className="mt-2 text-2xl font-black">{mode}</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Synthetic</Badge></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><div><p className="mb-2 text-sm text-slate-300">Your clan</p><div className="space-y-2">{CLANS.map((item) => <button key={item.id} onClick={() => { setClan(item); setSaved(false); }} className={`w-full rounded-xl border p-3 text-left ${clan.id === item.id ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10"}`}><p className="font-semibold">{item.name} <span className="text-xs text-slate-500">[{item.tag}]</span></p><p className="mt-1 text-xs text-slate-500">{item.note}</p></button>)}</div></div><div><p className="mb-2 text-sm text-slate-300">Opponent</p><div className="space-y-2">{CLANS.map((item) => <button key={item.id} onClick={() => { setOpponent(item); setSaved(false); }} className={`w-full rounded-xl border p-3 text-left ${opponent.id === item.id ? "border-violet-300/40 bg-violet-300/[0.08]" : "border-white/10"}`}><p className="font-semibold">{item.name} <span className="text-xs text-slate-500">[{item.tag}]</span></p><p className="mt-1 text-xs text-slate-500">Synthetic opponent</p></button>)}</div></div></div><div className="mt-6 flex flex-wrap gap-2">{MODES.map((item) => <button key={item} onClick={() => { setMode(item); setSaved(false); }} className={`rounded-xl border px-3 py-2 text-sm ${mode === item ? "border-amber-300/40 bg-amber-300/[0.08] text-amber-200" : "border-white/10 text-slate-400"}`}>{item}</button>)}</div><div className="mt-6 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex items-center gap-2 font-semibold text-amber-200"><ShieldAlert className="size-4" />No match execution</div><p className="mt-2 text-sm text-slate-400">The local plan cannot invite players, start a timer, score events, or settle a reward.</p></div>{saved && <div className="mt-4 flex items-center gap-2 text-sm text-cyan-200"><Check className="size-4" />Local war plan saved; no match or reward changed.</div>}</CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Match readiness</p><h2 className="mt-2 text-3xl font-black">{planReady ? "Plan-shaped" : "Incomplete"}</h2><div className="mt-6 space-y-2">{["Player identity and consent", "Rules and scoring function", "Matchmaking and eligibility", "Anti-cheat and event log", "Result verification and dispute", "Reward authorization and settlement"].map((item) => <div key={item} className="flex items-center gap-3 rounded-lg border border-white/10 bg-black/15 p-3"><Target className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}<div className="mt-5 rounded-xl border border-white/10 p-4"><p className="text-sm text-slate-300">{clan.name} vs {opponent.name}</p><p className="mt-1 text-xs text-slate-500">{mode} · no live timer · no verified score</p></div></div></CardContent></Card></section>}{tab === "Clans" && <section className="grid gap-4 md:grid-cols-3">{CLANS.map((item) => <Card key={item.id} className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className={`flex size-12 items-center justify-center rounded-xl bg-gradient-to-br ${item.color}`}><Shield className="size-6 text-white" /></div><div className="mt-4 flex items-center justify-between"><h2 className="font-black">{item.name}</h2><Badge variant="outline" className="border-white/10 text-slate-400">{item.tag}</Badge></div><p className="mt-3 text-sm leading-6 text-slate-400">{item.note}</p><div className="mt-5 grid grid-cols-2 gap-2 text-center"><div className="rounded-lg border border-white/10 p-3"><p className="text-lg font-black text-slate-500">—</p><p className="text-xs text-slate-500">Rank</p></div><div className="rounded-lg border border-white/10 p-3"><p className="text-lg font-black text-slate-500">—</p><p className="text-xs text-slate-500">Power</p></div></div><Button onClick={() => { setClan(item); setTab("Wars"); setSaved(false); }} variant="outline" className="mt-4 w-full border-white/15 text-white">Use in local plan</Button></CardContent></Card>)}</section>}{tab === "Create" && <Card className="mx-auto max-w-xl border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><Plus className="size-5 text-cyan-300" /><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Local creation preview</p><h2 className="mt-1 text-2xl font-black">Draft a clan identity</h2></div></div><div className="mt-6 space-y-4"><div><label htmlFor="clan-name" className="mb-2 block text-sm text-slate-300">Clan name</label><Input id="clan-name" value={name} onChange={(event) => { setName(event.target.value); setSaved(false); }} placeholder="Local clan name" className="border-white/10 bg-black/20 text-white placeholder:text-slate-500" /></div><div><label htmlFor="clan-tag" className="mb-2 block text-sm text-slate-300">Tag</label><Input id="clan-tag" value={tag} onChange={(event) => { setTag(event.target.value.toUpperCase().slice(0, 4)); setSaved(false); }} placeholder="TAG" maxLength={4} className="border-white/10 bg-black/20 font-mono text-white placeholder:text-slate-500" /></div><Button onClick={() => setSaved(Boolean(name.trim() && tag.trim()))} disabled={!name.trim() || !tag.trim()} className="w-full bg-cyan-300 text-slate-950 hover:bg-cyan-200">Save local clan draft</Button><p className="text-center text-xs text-slate-500">No creation fee, roster, leader role, recruiting, or server record is created.</p>{saved && <div className="flex items-center gap-2 text-sm text-cyan-200"><Check className="size-4" />Local clan draft saved; no clan was created.</div>}</div></CardContent></Card>}<section className="grid gap-4 md:grid-cols-3"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><Gamepad2 className="size-5 text-cyan-300" /><h3 className="mt-3 font-semibold">Competition needs rules</h3><p className="mt-2 text-sm leading-6 text-slate-400">Mode names and bars do not prove matchmaking, scoring, or verified outcomes.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><Trophy className="size-5 text-violet-300" /><h3 className="mt-3 font-semibold">Rewards need settlement</h3><p className="mt-2 text-sm leading-6 text-slate-400">A trophy cannot claim victory, rank, reward eligibility, or token transfer.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><WifiOff className="size-5 text-amber-300" /><h3 className="mt-3 font-semibold">No fake war</h3><p className="mt-2 text-sm leading-6 text-slate-400">Synthetic clans and local plans are not players, matches, leaderboards, or rewards.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Match plans precede match execution", description: "Separate roster, rules, matchmaking, scoring, result, dispute, and settlement states.", icon: Swords, status: "Pattern" }, { title: "Anti-cheat and audit matter", description: "Competitive claims need trusted events, reconciliation, and appeal paths.", icon: ShieldAlert, status: "Required" }, { title: "No fake victory", description: "A local plan is not a live war, verified score, rank, reward, or token transfer.", icon: WifiOff, status: "Guardrail" }]} /></main></div>; }
