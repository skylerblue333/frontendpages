import { useState } from "react";
import { Calculator, Check, ClipboardCheck, FileText, Info, LockKeyhole, RefreshCw, Scale, ShieldAlert, SlidersHorizontal, UserRound, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

const scenarios = [
  { name: "Digital-asset reporting scenario", state: "Unconfigured", detail: "Requires authoritative transactions, cost basis, dates, jurisdiction, applicable rules, and professional review." },
  { name: "Income and deduction scenario", state: "Unavailable", detail: "Requires verified income, expenses, eligibility, tax year, documentation, and reviewed assumptions." },
  { name: "Multi-jurisdiction scenario", state: "Blocked", detail: "Requires taxpayer scope, residency, nexus, filing obligations, current rules, and accountable advice controls." },
];

export default function TaxPlanning() {
  const [selected, setSelected] = useState(0);
  const [year, setYear] = useState("Tax year not configured");
  const [jurisdiction, setJurisdiction] = useState("Jurisdiction not configured");
  const [saved, setSaved] = useState(false);
  const [showBoundary, setShowBoundary] = useState(false);
  const [refreshing, setRefreshing] = useState(false);
  const refresh = () => { setRefreshing(true); window.setTimeout(() => setRefreshing(false), 500); };
  const reset = () => { setSelected(0); setYear("Tax year not configured"); setJurisdiction("Jurisdiction not configured"); setSaved(false); setShowBoundary(false); setRefreshing(false); };
  const scenario = scenarios[selected] ?? scenarios[0];
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Calculator} eyebrow="Tax planning · Scenario preview" title="Compare tax scenarios without inventing advice or savings." description="Explore a local tax-planning workspace with scenario concepts, tax year, jurisdiction, income, transactions, deductions, assumptions, rule provenance, sensitivity, privacy, professional review, retry, save/reset, and explicit evidence boundaries. No taxpayer facts, calculation, recommendation, liability, savings, or filing is connected." badge="Evidence-bounded scenarios"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save scenario draft"}</Button><Button onClick={() => setShowBoundary((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showBoundary ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showBoundary ? "Close evidence boundary" : "Review planning gates"}</Button><Button onClick={refresh} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className={`mr-2 size-4 ${refreshing ? "animate-spin" : ""}`} />{refreshing ? "Retrying local state" : "Retry rules source"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><X className="mr-2 size-4" />Reset scenario</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Scenarios", value: "Local", hint: "No facts source", icon: SlidersHorizontal, tone: "cyan" }, { label: "Rules", value: "Unverified", hint: "No jurisdiction source", icon: Scale, tone: "violet" }, { label: "Calculation", value: "Blocked", hint: "No tax engine", icon: Calculator, tone: "amber" }, { label: "Advice", value: "Unavailable", hint: "Professional review required", icon: ShieldAlert, tone: "slate" }]} /><ScreenPreviewBanner title="Tax-planning evidence boundary"><strong>This is a local scenario-design preview, not tax, legal, or financial advice and not evidence of income, transactions, deductions, liability, savings, compliance, filing obligations, or an optimal strategy.</strong> Scenario cards, year and jurisdiction intent, local saved state, retry feedback, and disabled calculate/recommend/file actions are browser concepts. No personal conclusion or tax outcome is asserted.</ScreenPreviewBanner><section className="grid gap-4 md:grid-cols-3">{scenarios.map((item, index) => <button key={item.name} onClick={() => setSelected(index)} className={`rounded-2xl border p-5 text-left transition ${selected === index ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10 bg-white/[0.04]"}`}><div className="flex items-start justify-between"><div className="flex size-11 items-center justify-center rounded-2xl bg-cyan-300/10 text-cyan-200"><Calculator className="size-5" /></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.state}</Badge></div><h2 className="mt-4 font-bold">{item.name}</h2><p className="mt-3 text-sm leading-6 text-slate-400">{item.detail}</p></button>)}</section><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200">Selected scenario concept</p><h2 className="mt-2 text-2xl font-black">{scenario.name}</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{scenario.state}</Badge></div><p className="mt-3 text-sm leading-6 text-slate-400">{scenario.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Taxpayer scope", value: "Unavailable", icon: UserRound }, { label: "Year / jurisdiction", value: "Unconfigured", icon: Scale }, { label: "Income / expenses", value: "Unavailable", icon: FileText }, { label: "Transactions / basis", value: "Unverified", icon: ClipboardCheck }, { label: "Rules / assumptions", value: "Unverified", icon: SlidersHorizontal }, { label: "Review / advice", value: "Required", icon: ShieldAlert }].map(({ label, value, icon: Icon }) => <div key={label} className="flex items-center gap-3 rounded-xl border border-white/10 p-4"><Icon className="size-4 text-cyan-300" /><span className="flex-1 text-sm text-slate-300">{label}</span><span className="text-xs text-amber-200">{value}</span></div>)}</div><div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold text-slate-300">Tax year intent<select value={year} onChange={(event) => setYear(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white"><option>Tax year not configured</option><option>Current-year intent</option><option>Prior-year intent</option><option>Amended-year intent</option></select></label><label className="text-sm font-semibold text-slate-300">Jurisdiction intent<select value={jurisdiction} onChange={(event) => setJurisdiction(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white"><option>Jurisdiction not configured</option><option>Federal intent</option><option>State or provincial intent</option><option>Multi-jurisdiction review required</option></select></label></div><div className="mt-6 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Calculate unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Compare unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Recommend unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">File unavailable</Button></div></CardContent></Card><ScreenStatePanel type="unavailable" title="Tax-planning source unavailable" description="Connect verified taxpayer scope, authoritative transaction records, jurisdiction rules, scenario engine, professional review, privacy controls, and filing authority before enabling planning." /></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><Info className="size-5 text-cyan-300" /><div><p className="font-bold">Tax-planning evidence contract</p><p className="mt-1 text-sm text-slate-400">Responsible planning requires current jurisdiction rules, verified facts, transparent assumptions, sensitivity analysis, privacy, professional review, correction, and a clear separation between education and advice.</p></div></div><div className="mt-5 grid gap-3 md:grid-cols-2">{["Identity, residency, tax year, jurisdiction, account scope, and consent", "Authoritative income, expense, transaction, basis, and document provenance", "Current law and rule source, version, effective date, exceptions, and assumptions", "Scenario calculations, sensitivity, uncertainty, validation, and reproducibility", "Privacy, retention, access, professional review, corrections, and audit", "Recommendations, filing authority, disclosure, receipt, deadline, and recovery"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3 text-sm text-slate-300"><LockKeyhole className="size-4 text-amber-300" />{item}<span className="ml-auto text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card>{showBoundary && <Card className="border-amber-300/20 bg-amber-300/[0.06]"><CardContent className="p-6"><p className="font-semibold text-amber-100">No advice, savings, or liability claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local scenario concept does not prove taxpayer facts, a tax rule, calculation, deduction, liability, savings, optimal strategy, filing obligation, or compliance outcome.</p></CardContent></Card>}<ScreenFeatureGrid features={[{ title: "Tax scenario surface preserved", description: "Scenarios, year, jurisdiction, facts, transactions, deductions, assumptions, rules, sensitivity, review, retry, save/reset, and filing gates remain visible.", icon: Calculator, status: "Local scenarios" }, { title: "No advice theater", description: "Facts, calculations, recommendations, savings, liabilities, filings, and compliance outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before recommendation", description: "Real planning requires authoritative data, current rules, reproducible calculations, privacy, professional review, and filing authority.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
