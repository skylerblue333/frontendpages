import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { AlertTriangle, Calculator, Check, CircleDollarSign, ClipboardCheck, LockKeyhole, RefreshCw, ShieldAlert, ShieldCheck, Target, TrendingDown, TrendingUp, Wallet, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const instruments = ["SKY444 concept", "BTC concept", "Cash concept"];
const evidence = ["Authenticated account and position ownership", "Timestamped quantity, entry, mark, currency, and fees", "Margin, leverage, collateral, and liquidation methodology", "Stop/risk assumptions, suitability, and human review", "Order validation, signing, status, failure, and reconciliation"];
export default function PositionManagement() {
  const [side, setSide] = useState<"Long" | "Short">("Long");
  const [instrument, setInstrument] = useState(instruments[0]);
  const [quantity, setQuantity] = useState("1");
  const [entry, setEntry] = useState("100");
  const [mark, setMark] = useState("100");
  const [stop, setStop] = useState("90");
  const [checks, setChecks] = useState<string[]>([]);
  const [acknowledged, setAcknowledged] = useState(false);
  const [saved, setSaved] = useState(false);
  const numeric = (value: string) => Number.parseFloat(value) || 0;
  const notional = numeric(quantity) * numeric(entry);
  const localChange = side === "Long" ? (numeric(mark) - numeric(entry)) * numeric(quantity) : (numeric(entry) - numeric(mark)) * numeric(quantity);
  const stopLoss = side === "Long" ? (numeric(entry) - numeric(stop)) * numeric(quantity) : (numeric(stop) - numeric(entry)) * numeric(quantity);
  const toggle = (item: string) => setChecks((items) => items.includes(item) ? items.filter((value) => value !== item) : [...items, item]);
  const reset = () => { setSide("Long"); setInstrument(instruments[0]); setQuantity("1"); setEntry("100"); setMark("100"); setStop("90"); setChecks([]); setAcknowledged(false); setSaved(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Target} eyebrow="PositionManagement · Trading preview" title="Review position risk without pretending a position exists." description="Explore long/short assumptions, quantity, entry, mark, stop, local notional and change arithmetic, close intent, and evidence requirements. No account, position, venue, wallet, margin, liquidation, market, or order source is connected." badge="Position risk preview"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} disabled={checks.length === 0 || !acknowledged} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Plan saved locally" : "Save local plan"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset position</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Side", value: side, hint: "Local assumption", icon: side === "Long" ? TrendingUp : TrendingDown, tone: "cyan" }, { label: "Notional", value: `${notional.toFixed(2)}`, hint: "Local arithmetic", icon: Calculator, tone: "violet" }, { label: "Change", value: `${localChange.toFixed(2)}`, hint: "Unverified mark math", icon: CircleDollarSign, tone: "amber" }, { label: "Execution", value: "Off", hint: "No position source", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="Position evidence boundary"><strong>This is not a live position or trading instruction.</strong> Side, instrument, quantity, entry, mark, stop, notional, change, and stop-loss arithmetic are local assumptions only. They do not represent a real holding, market quote, margin, liquidation, P&L, risk assessment, order, execution, or recommendation. No financial service is connected.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.82fr_1fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Position assumptions</p><h2 className="mt-2 text-2xl font-black">Build a local scenario</h2><div className="mt-5 flex gap-2">{(["Long", "Short"] as const).map((item) => <button key={item} onClick={() => setSide(item)} className={`flex-1 rounded-xl border p-3 text-sm font-semibold ${side === item ? "border-cyan-300/40 bg-cyan-300/[0.06] text-cyan-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div><p className="mt-6 text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Instrument concept</p><div className="mt-3 grid gap-2">{instruments.map((item) => <button key={item} onClick={() => setInstrument(item)} className={`rounded-xl border p-3 text-left text-sm ${instrument === item ? "border-amber-300/40 bg-amber-300/[0.06] text-amber-200" : "border-white/10 text-slate-400"}`}>{item}</button>)}</div><div className="mt-6 grid gap-3 sm:grid-cols-2">{[["Quantity", quantity, setQuantity], ["Entry", entry, setEntry], ["Mark", mark, setMark], ["Stop", stop, setStop]].map(([label, value, setter]) => <label key={label as string} className="space-y-2"><span className="text-xs text-slate-500">{label as string}</span><input value={value as string} onChange={(event) => (setter as (value: string) => void)(event.target.value)} inputMode="decimal" className="w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none focus:border-cyan-300/50" /></label>)}</div><div className="mt-6 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex gap-3"><AlertTriangle className="mt-0.5 size-5 shrink-0 text-amber-200" /><p className="text-sm leading-6 text-slate-300">Local arithmetic is not a quote or P&L. Real position state requires authenticated account data, timestamps, fees, currency, and reconciliation.</p></div></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Scenario output</p><h2 className="mt-2 text-2xl font-black">{instrument} · {side}</h2></div><Badge variant="outline" className="border-white/10 text-amber-200">Source off</Badge></div><div className="mt-5 grid gap-3 sm:grid-cols-2">{[{ label: "Notional", value: notional.toFixed(2) }, { label: "Local mark change", value: localChange.toFixed(2) }, { label: "Stop-loss assumption", value: stopLoss.toFixed(2) }, { label: "Fees", value: "Unknown" }, { label: "Margin", value: "Unavailable" }, { label: "Liquidation", value: "Unavailable" }, { label: "Position status", value: "Unverified" }, { label: "Close intent", value: "Blocked" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-4"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-lg font-black text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 rounded-xl border border-white/10 p-4"><p className="text-xs text-slate-500">Evidence boundary</p><p className="mt-2 text-sm leading-6 text-slate-400">The arithmetic is computed locally from editable assumptions. It does not prove a position, quote, return, margin requirement, or loss.</p></div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Evidence gate</p><h2 className="mt-2 text-2xl font-black">Before any position action</h2><div className="mt-5 space-y-2">{evidence.map((item) => <button key={item} onClick={() => toggle(item)} className={`flex w-full items-center gap-3 rounded-xl border p-3 text-left ${checks.includes(item) ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><span className="flex size-5 items-center justify-center rounded border border-white/20">{checks.includes(item) ? "✓" : ""}</span><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></button>)}</div><label className="mt-5 flex items-start gap-3 rounded-xl border border-white/10 p-4"><input type="checkbox" checked={acknowledged} onChange={(event) => setAcknowledged(event.target.checked)} className="mt-1 accent-cyan-300" /><span className="text-sm leading-6 text-slate-300">I understand this is a local position-risk worksheet, not a trading instruction or financial advice.</span></label><Button disabled={checks.length === 0 || !acknowledged} className="mt-5 bg-cyan-300 text-slate-950 hover:bg-cyan-200"><ClipboardCheck className="mr-2 size-4" />{checks.length}/{evidence.length} checks · {acknowledged ? "Acknowledged" : "Acknowledgment needed"}</Button></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><LockKeyhole className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake position</h2><p className="mt-3 text-sm leading-6 text-slate-400">Changing side, instrument, assumptions, checks, or acknowledgment changes browser state only. It does not open, close, modify, value, liquidate, or hedge a position.</p></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Production checklist</p><h2 className="mt-2 text-2xl font-black">Position management must prove</h2><div className="mt-5 space-y-3">{["Authenticated account/position ownership, instrument/network identity, permissions, and privacy", "Timestamped quantity, entry/mark, currency, fees, margin, leverage, collateral, and liquidation math", "Risk limits, stop semantics, suitability, disclosures, human review, and harmful-action prevention", "Order validation, idempotency, signing, status, failure, partial fill, rollback, and reconciliation", "Tests for stale/missing data, price gaps, venue outage, conflicting updates, and unsafe closes"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Position surface preserved", description: "Long/short, instrument, quantity, entry/mark/stop, arithmetic, risk gate, and close intent remain interactive.", icon: Target, status: "Local worksheet" }, { title: "Accounts and execution are off", description: "Positions, quotes, margin, liquidation, venues, orders, fees, and transactions remain unavailable.", icon: ShieldAlert, status: "Guardrail" }, { title: "No financial advice", description: "No P&L, risk rating, liquidation prediction, trade instruction, or action prompt is fabricated.", icon: WifiOff, status: "Unavailable" }]} /></section></main></div>;
}
