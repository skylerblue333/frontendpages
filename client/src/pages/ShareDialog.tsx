import { useState } from "react";
import { Check, Clipboard, Eye, FileText, Link2, LockKeyhole, Mail, RefreshCw, Send, ShieldAlert, Users, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
export default function ShareDialog() { const [audience, setAudience] = useState("Audience not configured"); const [permission, setPermission] = useState("Permission not configured"); const [expiry, setExpiry] = useState("Expiry not configured"); const [copied, setCopied] = useState(false); const [sent, setSent] = useState(false); const [closed, setClosed] = useState(false); const reset = () => { setAudience("Audience not configured"); setPermission("Permission not configured"); setExpiry("Expiry not configured"); setCopied(false); setSent(false); setClosed(false); }; if (closed) return <div className="min-h-screen bg-[#070a16] p-6 text-white"><Card className="mx-auto mt-20 max-w-xl border-white/10 bg-white/[0.04]"><CardContent className="p-8 text-center"><Link2 className="mx-auto size-9 text-slate-600" /><h1 className="mt-4 text-2xl font-black">Share panel closed locally</h1><p className="mt-2 text-sm leading-6 text-slate-500">Closing this preview changes only browser state. No link, recipient, permission, or delivery was created.</p><Button onClick={reset} className="mt-6 bg-cyan-300 text-slate-950 hover:bg-cyan-200">Reopen local panel</Button></CardContent></Card></div>; return <div className="min-h-screen bg-[#070a16] p-4 text-white sm:p-8"><Card className="mx-auto max-w-2xl border-white/10 bg-white/[0.04] shadow-2xl shadow-cyan-950/20"><CardHeader className="border-b border-white/10"><div className="flex items-start justify-between gap-4"><div><div className="flex items-center gap-2"><Send className="size-5 text-cyan-300" /><Badge variant="outline" className="border-amber-300/20 text-amber-200">Local preview</Badge></div><CardTitle className="mt-3 text-3xl font-black">Share content</CardTitle><p className="mt-2 text-sm leading-6 text-slate-400">Review the audience and permission boundary without implying a real link, recipient, access rule, or delivery.</p></div><Button aria-label="Close share dialog" onClick={() => setClosed(true)} variant="outline" size="icon" className="border-white/10 text-slate-300"><X className="size-4" /></Button></div></CardHeader><CardContent className="space-y-5 p-6"><div className="rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4 text-sm leading-6 text-amber-100"><strong>Evidence boundary:</strong> all controls are local. No content, link, recipient, permission, access check, message, email, or share event is connected.</div><div className="rounded-xl border border-white/10 p-5"><div className="flex items-start gap-3"><FileText className="size-5 text-cyan-300" /><div><p className="font-semibold">Content preview</p><p className="mt-2 text-sm leading-6 text-slate-500">No content identifier, owner, visibility, version, redaction state, or access policy is available to this preview.</p></div></div></div><div className="grid gap-5 md:grid-cols-3"><label className="text-sm font-semibold text-slate-300">Audience<select value={audience} onChange={(event) => setAudience(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white outline-none"><option>Audience not configured</option><option>Private intent</option><option>Specific recipients intent</option><option>Workspace intent</option><option>Public intent</option></select></label><label className="text-sm font-semibold text-slate-300">Permission<select value={permission} onChange={(event) => setPermission(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white outline-none"><option>Permission not configured</option><option>View intent</option><option>Comment intent</option><option>Edit intent</option><option>Download intent</option></select></label><label className="text-sm font-semibold text-slate-300">Expiry<select value={expiry} onChange={(event) => setExpiry(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white outline-none"><option>Expiry not configured</option><option>One hour intent</option><option>One day intent</option><option>Seven days intent</option><option>No expiry intent</option></select></label></div><div className="grid gap-3 sm:grid-cols-3">{[{ label: "Audience", value: audience }, { label: "Permission", value: permission }, { label: "Link", value: "Unavailable" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-4"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400"><Link2 className="mr-2 size-4" />Create link unavailable</Button><Button onClick={() => setCopied(true)} variant="outline" className="border-white/10 text-slate-300"><Clipboard className="mr-2 size-4" />{copied ? "Nothing copied" : "Copy preview"}</Button><Button onClick={() => setSent(true)} disabled={audience === "Audience not configured" || permission === "Permission not configured"} variant="outline" className="border-white/10 text-slate-300"><Mail className="mr-2 size-4" />{sent ? "Delivery unavailable" : "Send preview"}</Button><Button onClick={reset} variant="outline" className="border-white/10 text-slate-300"><RefreshCw className="mr-2 size-4" />Reset</Button></div><div className="rounded-2xl border border-dashed border-white/10 p-8 text-center"><Users className="mx-auto size-8 text-slate-600" /><p className="mt-3 font-semibold">No sharing evidence loaded</p><p className="mt-2 text-sm leading-6 text-slate-500">Connect content ownership, recipient identity, authorization, link signing, access enforcement, expiration, revocation, notification, delivery, and audit before sharing.</p></div><div className="rounded-xl border border-white/10 p-4"><p className="flex items-center gap-2 font-semibold"><Eye className="size-4 text-cyan-300" />Preview state</p><p className="mt-2 text-sm leading-6 text-slate-500">{sent ? "A local send intent was recorded; no message or email was sent." : copied ? "No link exists to copy; this state is local only." : "No share action has been recorded."}</p></div><div className="flex items-start gap-3 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><ShieldAlert className="mt-0.5 size-5 text-amber-200" /><p className="text-sm leading-6 text-amber-100"><strong>No access claim:</strong> audience and permission choices do not prove content visibility, recipient access, link security, expiration, revocation, or delivery.</p></div></CardContent></Card><div className="mx-auto mt-6 max-w-2xl rounded-xl border border-white/10 bg-white/[0.04] p-5"><p className="flex items-center gap-2 font-semibold"><LockKeyhole className="size-4 text-slate-500" />Share-governance gates</p><p className="mt-2 text-sm leading-6 text-slate-500">Real sharing requires authenticated content ownership, recipient identity, authorization, CSRF protection, link entropy/signing, expiration, revocation, access checks, rate limits, abuse handling, notification provenance, audit, accessibility, and safe failure states.</p></div></div>; }
