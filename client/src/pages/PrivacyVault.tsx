import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Activity, Check, Clipboard, Cpu, Database, Fingerprint, KeyRound, LockKeyhole, Network, RefreshCw, Server, ShieldAlert, ShieldCheck, WifiOff, X, Zap } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const factors = [{ name: "Identity and session", value: "Not assessed", risk: "Unknown" }, { name: "Data storage", value: "Not assessed", risk: "Unknown" }, { name: "Network exposure", value: "Not assessed", risk: "Unknown" }, { name: "Device surface", value: "Not assessed", risk: "Unknown" }];
const actions = [{ id: "cookies", label: "Clear all cookies", icon: Database }, { id: "dns", label: "Flush DNS cache", icon: Server }, { id: "identity", label: "Generate new identity", icon: Fingerprint }, { id: "clipboard", label: "Encrypt clipboard", icon: Clipboard }, { id: "connections", label: "Kill all connections", icon: WifiOff }];
export default function PrivacyVault() {
  const [scanned, setScanned] = useState(false);
  const [action, setAction] = useState<string | null>(null);
  const [showGates, setShowGates] = useState(false);
  const readiness = useMemo(() => scanned ? "Review required" : "Not assessed", [scanned]);
  const reset = () => { setScanned(false); setAction(null); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={LockKeyhole} eyebrow="PrivacyVault · Privacy readiness preview" title="Review privacy posture without inventing a security score." description="Explore a local readiness checklist for identity, storage, network, device, and recovery surfaces. No scan runs, no network is probed, no cookies or DNS are changed, no identity is generated, no clipboard is encrypted, and no connection is terminated." badge="Privacy vault"><div className="flex flex-wrap gap-2"><Button onClick={() => setScanned(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Activity className="mr-2 size-4" />{scanned ? "Review flagged locally" : "Run local review"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset review</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Readiness", value: readiness, hint: "No security score", icon: ShieldAlert, tone: "cyan" }, { label: "Factors", value: String(factors.length), hint: "Review topics", icon: ShieldCheck, tone: "violet" }, { label: "Network", value: "Unprobed", hint: "No reachability test", icon: Network, tone: "amber" }, { label: "Actions", value: "Blocked", hint: "No device writes", icon: LockKeyhole, tone: "slate" }]} /><ScreenPreviewBanner title="Privacy vault evidence boundary"><strong>This vault is a readiness worksheet, not a security scanner or privacy service.</strong> No privacy score, critical exposure count, Tor/I2P/VPN reachability, relay node count, encryption result, identity rotation, cookie/DNS change, clipboard operation, or connection termination is claimed or performed. Browser labels are local design state only.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Readiness review</p><h2 className="mt-2 text-2xl font-black">Privacy surfaces to verify</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{scanned ? "Local review state" : "Not assessed"}</Badge></div><div className="mt-5 space-y-3">{factors.map((factor) => <div key={factor.name} className="flex items-center gap-3 rounded-xl border border-white/10 p-4"><ShieldAlert className="size-5 text-amber-200" /><div className="flex-1"><p className="font-semibold">{factor.name}</p><p className="mt-1 text-sm text-slate-500">Requires a real source, scope, evidence, and safe handling.</p></div><div className="text-right"><p className="text-sm font-semibold text-amber-200">{scanned ? "Review" : factor.value}</p><p className="text-xs text-slate-500">{factor.risk}</p></div></div>)}</div><div className="mt-5 rounded-xl border border-violet-300/20 bg-violet-300/[0.06] p-4"><div className="flex gap-3"><ShieldCheck className="mt-0.5 size-5 shrink-0 text-violet-200" /><p className="text-sm leading-6 text-slate-400">{scanned ? "Local review state changed only the labels on this page. It did not inspect a device, account, network, or vault." : "Run local review to demonstrate the state transition; the action intentionally cannot claim a real scan."}</p></div></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Network posture</p><h2 className="mt-2 text-2xl font-black">No live probe</h2></div><Network className="size-6 text-slate-500" /></div><div className="mt-5 space-y-3">{[{ label: "Tor network", value: "Not tested" }, { label: "I2P network", value: "Not tested" }, { label: "Relay network", value: "Not configured" }, { label: "VPN tunnel", value: "Not connected" }].map((item) => <div key={item.label} className="flex items-center justify-between rounded-xl border border-white/10 p-3"><span className="text-sm text-slate-400">{item.label}</span><span className="text-sm font-semibold text-amber-200">{item.value}</span></div>)}</div><div className="mt-5 rounded-2xl border border-amber-300/25 bg-amber-300/[0.06] p-5"><div className="flex items-start gap-3"><WifiOff className="mt-0.5 size-5 shrink-0 text-amber-200" /><div><p className="font-semibold text-amber-100">Network actions are unavailable</p><p className="mt-2 text-sm leading-6 text-slate-400">This route does not alter routing, connect to a relay, or assert privacy from network observers.</p></div></div></div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[0.9fr_1.1fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Quick actions</p><h2 className="mt-2 text-2xl font-black">Inspect safely</h2></div><Zap className="size-6 text-cyan-200" /></div><div className="mt-5 space-y-2">{actions.map(({ id, label, icon: Icon }) => <button key={id} onClick={() => setAction(id)} className={`flex w-full items-center gap-3 rounded-xl border p-3 text-left ${action === id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><Icon className="size-4 text-cyan-200" /><span className="flex-1 text-sm text-slate-300">{label}</span><Badge variant="outline" className="border-amber-300/20 text-amber-200">Unavailable</Badge></button>)}</div>{action && <div className="mt-5 rounded-xl border border-cyan-300/20 bg-cyan-300/[0.05] p-4 text-sm leading-6 text-slate-400">Selected action: <strong className="text-cyan-100">{actions.find((item) => item.id === action)?.label}</strong>. No device, browser, network, clipboard, identity, or connection state changed.</div>}</CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Vault gates</p><h2 className="mt-2 text-2xl font-black">What a real vault must prove</h2></div><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/10 text-slate-300">{showGates ? <X className="size-4" /> : <KeyRound className="size-4" />}</Button></div><div className="mt-5 space-y-3">{["Threat model, device scope, and authenticated session", "Secure key management without plaintext secrets", "Verified encryption implementation and recovery path", "Permission boundaries for cookies, DNS, clipboard, and network", "Audit, incident response, user warnings, and reversible controls", "Independent testing before any privacy score or guarantee"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><Check className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div>{showGates && <div className="mt-5 rounded-xl border border-violet-300/20 bg-violet-300/[0.06] p-4 text-sm leading-6 text-slate-400">The gates are engineering requirements, not an audit result or certification.</div>}</CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Vault surface preserved", description: "Readiness review, factors, network posture, quick actions, selected action feedback, gates, local review, and reset remain interactive.", icon: LockKeyhole, status: "Local readiness" }, { title: "No security theater", description: "Scores, exposures, uptime, relay counts, encryption, identity, network reachability, and destructive actions are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Safe by default", description: "Device, network, clipboard, identity, cookie, DNS, and connection controls remain unavailable until permissions and audited services exist.", icon: WifiOff, status: "Blocked" }]} /></main></div>;
}
