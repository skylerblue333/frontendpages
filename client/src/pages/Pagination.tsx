import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ChevronLeft, ChevronRight, ChevronsLeft, ChevronsRight, FileStack, Info, LockKeyhole, RefreshCw, ShieldCheck, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const ROWS = Array.from({ length: 42 }, (_, index) => ({ id: `fixture-${String(index + 1).padStart(3, "0")}`, label: `Synthetic record ${index + 1}`, category: ["Profile", "Order", "Course", "Message"][index % 4], state: ["Draft", "Preview", "Awaiting source"][index % 3] }));
export default function Pagination() {
  const [page, setPage] = useState(1);
  const [pageSize, setPageSize] = useState(8);
  const [selected, setSelected] = useState<string | null>(null);
  const totalPages = Math.ceil(ROWS.length / pageSize);
  const visible = useMemo(() => ROWS.slice((page - 1) * pageSize, page * pageSize), [page, pageSize]);
  const changeSize = (size: number) => { setPageSize(size); setPage(1); setSelected(null); };
  const reset = () => { setPage(1); setPageSize(8); setSelected(null); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={FileStack} eyebrow="Pagination · Data-navigation preview" title="Navigate records without inventing the backend." description="Explore local page windows, page-size choices, boundary states, and selected rows. The record count and rows are fixtures; no remote query, cursor, total count, cache, or authorization is represented." badge="Pagination preview"><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset pagination</Button></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Fixture rows", value: String(ROWS.length), hint: "Local dataset", icon: FileStack, tone: "cyan" }, { label: "Page", value: `${page} / ${totalPages}`, hint: "Local window", icon: Info, tone: "violet" }, { label: "Page size", value: String(pageSize), hint: "Rows per page", icon: FileStack, tone: "amber" }, { label: "Remote source", value: "Off", hint: "No query", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="Pagination evidence boundary"><strong>This laboratory paginates a local fixture array.</strong> It does not claim a server total, cursor, offset, freshness, user authorization, cache state, database result, or complete record set. Production pagination needs typed API contracts, stable ordering, cursor/offset semantics, authorization scope, consistency, and error/retry states.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Local data window</p><h2 className="mt-2 text-2xl font-black">Rows {(page - 1) * pageSize + 1}–{Math.min(page * pageSize, ROWS.length)}</h2></div><div className="flex gap-2">{[5, 8, 12].map((size) => <button key={size} onClick={() => changeSize(size)} className={`rounded-lg border px-3 py-1.5 text-xs ${pageSize === size ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-200" : "border-white/10 text-slate-500"}`}>{size} rows</button>)}</div></div><div className="mt-5 space-y-2">{visible.map((row) => <button key={row.id} onClick={() => setSelected(row.id)} className={`flex w-full items-center justify-between gap-3 rounded-xl border p-3 text-left ${selected === row.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10 hover:border-white/20"}`}><div><p className="font-semibold">{row.label}</p><p className="mt-1 text-xs text-slate-500">{row.id} · {row.category}</p></div><Badge variant="outline" className="border-white/10 text-amber-200">{row.state}</Badge></button>)}</div><div className="mt-5 flex flex-wrap items-center justify-between gap-3"><p className="text-sm text-slate-500">Page {page} of {totalPages} · fixture data only</p><div className="flex gap-1"><Button size="icon" variant="outline" disabled={page === 1} onClick={() => setPage(1)} className="border-white/10 text-white"><ChevronsLeft className="size-4" /></Button><Button size="icon" variant="outline" disabled={page === 1} onClick={() => setPage((value) => Math.max(1, value - 1))} className="border-white/10 text-white"><ChevronLeft className="size-4" /></Button><Button size="icon" variant="outline" disabled={page === totalPages} onClick={() => setPage((value) => Math.min(totalPages, value + 1))} className="border-white/10 text-white"><ChevronRight className="size-4" /></Button><Button size="icon" variant="outline" disabled={page === totalPages} onClick={() => setPage(totalPages)} className="border-white/10 text-white"><ChevronsRight className="size-4" /></Button></div></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6">{selected ? <><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected row</p><h2 className="mt-2 text-2xl font-black">{ROWS.find((row) => row.id === selected)?.label}</h2><p className="mt-3 text-sm leading-6 text-slate-400">Selection is local state; it is not a server detail request.</p><div className="mt-5 space-y-3">{Object.entries(ROWS.find((row) => row.id === selected) ?? {}).map(([key, value]) => <div key={key} className="flex justify-between rounded-xl border border-white/10 p-3"><span className="text-sm capitalize text-slate-500">{key}</span><span className="text-sm font-semibold text-amber-200">{value}</span></div>)}</div></> : <div className="py-10 text-center"><FileStack className="mx-auto size-10 text-slate-600" /><h2 className="mt-4 text-xl font-black">Select a fixture row</h2><p className="mt-2 text-sm leading-6 text-slate-500">The detail panel stays empty until a local row is selected.</p></div>}</CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Production checklist</p><h2 className="mt-2 text-2xl font-black">Pagination needs a contract</h2><div className="mt-5 space-y-3">{["Typed endpoint, authorization scope, stable ordering, and cursor/offset semantics", "Server total or end-of-list semantics with consistent page-size limits", "Loading, empty, stale, retry, partial, rate-limit, and error states", "Duplicate/missing-row handling, filters, sorting, caching, and reconciliation", "Privacy, auditability, observability, support, and incident response"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><LockKeyhole className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake completeness</h2><p className="mt-3 text-sm leading-6 text-slate-400">Changing pages changes the local window only. It does not imply that these rows are the full dataset or that a remote query succeeded.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Navigation is real", description: "First, previous, next, last, page-size, and selected-row states work over typed fixtures.", icon: FileStack, status: "Verified locally" }, { title: "Boundaries are visible", description: "First/last disabled states and fixture-only totals prevent false completeness claims.", icon: ShieldCheck, status: "Guardrail" }, { title: "Remote source is off", description: "No API, database, cursor, cache, authorization, or server total is fabricated.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
