import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { AlertTriangle, Ban, Check, Filter, FileText, LockKeyhole, RefreshCw, Search, ShieldAlert, UserRound, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const subjects = [{ id: 1, name: "Community member concept", category: "Community", detail: "A local subject concept awaiting verified identity, consent-aware evidence, policy classification, and moderation review.", state: "Preview only" }, { id: 2, name: "Creator account concept", category: "Content", detail: "A creator-report concept requiring provenance, privacy, copyright context, appeals, and safe handling.", state: "Needs evidence" }, { id: 3, name: "Player account concept", category: "Gaming", detail: "A player-safety concept that must not imply a real person, conduct, identity, or enforcement action.", state: "Unmeasured" }, { id: 4, name: "Profile security concept", category: "Security", detail: "A security concern requiring authenticated evidence and response ownership without exposing personal data.", state: "Blocked" }];
export default function ReportUser() {
  const [query, setQuery] = useState(""); const [category, setCategory] = useState("All"); const [selected, setSelected] = useState(1); const [reason, setReason] = useState("Reason not configured"); const [evidence, setEvidence] = useState("Evidence not attached"); const [saved, setSaved] = useState(false); const [showGates, setShowGates] = useState(false);
  const categories = ["All", ...Array.from(new Set(subjects.map((item) => item.category)))]; const filtered = useMemo(() => subjects.filter((item) => (category === "All" || item.category === category) && `${item.name} ${item.category} ${item.detail}`.toLowerCase().includes(query.toLowerCase())), [category, query]); const subject = subjects.find((item) => item.id === selected) ?? subjects[0];
  const reset = () => { setQuery(""); setCategory("All"); setSelected(1); setReason("Reason not configured"); setEvidence("Evidence not attached"); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={UserRound} eyebrow="ReportUser · Subject-safety preview" title="Protect the subject before filing a report." description="Explore local user-report concepts with search, categories, subject posture, report reasons, evidence and attachments, privacy, moderation, abuse controls, status, escalation, block, audit, save, reset, and evidence gates. No live user, identity, incident, file, enforcement, or safety outcome is connected." badge="User report workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "View saved locally" : "Save report locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review user-report gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset report</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Subjects", value: `${subjects.length} local`, hint: "No users loaded", icon: UserRound, tone: "cyan" }, { label: "Identity", value: "Unavailable", hint: "No identity source", icon: LockKeyhole, tone: "violet" }, { label: "Evidence", value: "Unattached", hint: "No file source", icon: FileText, tone: "amber" }, { label: "Enforcement", value: "Blocked", hint: "No moderation source", icon: Ban, tone: "slate" }]} /><ScreenPreviewBanner title="User-report evidence boundary"><strong>This is a local user-safety preview, not evidence that a person violated a policy or that enforcement is warranted.</strong> Subject cards, reason and evidence posture, privacy, moderation, abuse controls, status, escalation, block, saved state, and audit gates are browser concepts. No user, identity, incident, file, responsibility, enforcement, or safety outcome is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Search className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local user-report subjects" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-4 flex flex-wrap gap-2">{categories.map((entry) => <Button key={entry} onClick={() => setCategory(entry)} size="sm" variant="outline" className={category === entry ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-100" : "border-white/10 text-slate-400"}><Filter className="mr-1 size-3" />{entry}</Button>)}</div><div className="mt-6 space-y-3">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{item.name}</p><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.state}</Badge></div><div className="mt-4"><Badge variant="outline" className="border-white/10 text-slate-500">{item.category}</Badge></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected subject concept</p><h2 className="mt-2 text-2xl font-black">{subject.name}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{subject.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Category", value: subject.category }, { label: "State", value: subject.state }, { label: "Identity", value: "Unavailable" }, { label: "Reason", value: reason }, { label: "Evidence", value: evidence }, { label: "Enforcement", value: "Blocked" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2"><label className="text-sm text-slate-400">Report reason<select value={reason} onChange={(event) => setReason(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Reason not configured</option><option>Harassment intent</option><option>Spam intent</option><option>Impersonation intent</option><option>Security concern intent</option></select></label><label className="text-sm text-slate-400">Evidence posture<select value={evidence} onChange={(event) => setEvidence(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Evidence not attached</option><option>Screenshot intent</option><option>Link intent</option><option>Document intent</option></select></label></div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-8 text-center"><UserRound className="mx-auto size-8 text-slate-600" /><p className="mt-3 font-semibold">No user subject loaded</p><p className="mt-2 text-sm text-slate-500">Connect a consent-aware identity, safe evidence path, moderation queue, privacy controls, and support owner before filing.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Submit unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Attach unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Block unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Escalate unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No identity or enforcement claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local subject concept does not prove a person exists, a report is true, or that blocking, escalation, or enforcement is appropriate.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">User-report gates</p><h2 className="mt-2 text-2xl font-black">What a real user-report workflow must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Reporter and subject identity, consent, privacy, age boundaries, sensitive data handling, and retaliation controls", "Evidence provenance, timestamp, channel, file safety, redaction, chain of custody, and secure retention", "Policy taxonomy, harassment, spam, impersonation, security, minors, threats, and emergency escalation boundaries", "Moderation ownership, severity, triage, false-positive review, conflicts, appeals, status, and communication", "Block and restrict semantics, access control, notifications, rate limits, privacy leakage, and user safety", "Audit, support, deletion, incident response, rollback, localization, accessibility, and user-visible outcome"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "User-report surface preserved", description: "Subjects, search, categories, reasons, evidence, privacy, moderation, abuse controls, status, escalation, block, audit, save/reset, and gates remain interactive.", icon: UserRound, status: "Local concepts" }, { title: "No person theater", description: "Users, identities, incidents, files, responsibility, enforcement, safety outcomes, and support actions are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Privacy before accusation", description: "Real user reports need safe evidence, identity boundaries, moderation ownership, appeals, support, and auditable status.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
