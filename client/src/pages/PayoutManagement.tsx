import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Banknote, Check, Clock3, FileCheck2, LockKeyhole, RefreshCw, Search, ShieldCheck, TriangleAlert, WalletCards, WifiOff, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

type Status = "Draft" | "Needs evidence" | "Blocked";
type Request = { id: string; destination: string; method: string; amount: string; status: Status; detail: string };
const requests: Request[] = [
  { id: "fixture-request-001", destination: "Bank destination concept", method: "Bank", amount: "$240.00 fixture", status: "Needs evidence", detail: "Ownership, balance, KYC/compliance, and provider transfer are unavailable." },
  { id: "fixture-request-002", destination: "Wallet destination concept", method: "Crypto", amount: "$85.00 fixture", status: "Blocked", detail: "Network, address, fee, signature, and settlement evidence are unavailable." },
  { id: "fixture-request-003", destination: "Provider destination concept", method: "Provider", amount: "$120.00 fixture", status: "Draft", detail: "No authenticated payout rail or transfer intent exists." },
];
export default function PayoutManagement() {
  const [search, setSearch] = useState("");
  const [status, setStatus] = useState<Status | "All">("All");
  const [method, setMethod] = useState("All");
  const [selected, setSelected] = useState(requests[0]);
  const [reviewed, setReviewed] = useState(false);
  const [refreshed, setRefreshed] = useState(false);
  const filtered = useMemo(() => requests.filter((item) => (!search || `${item.id} ${item.destination} ${item.method}`.toLowerCase().includes(search.toLowerCase())) && (status === "All" || item.status === status) && (method === "All" || item.method === method)), [method, search, status]);
  const reset = () => { setSearch(""); setStatus("All"); setMethod("All"); setSelected(requests[0]); setReviewed(false); setRefreshed(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={WalletCards} eyebrow="PayoutManagement · Operations preview" title="Review payout intent without exposing a live queue." description="Search and filter typed payout-request fixtures, inspect evidence gaps, and exercise review states. No payout balance, authenticated request, transfer queue, provider event, wallet signature, bank trace, or settlement is available." badge="Operations preview"><div className="flex flex-wrap gap-2"><Button onClick={() => setRefreshed(true)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />{refreshed ? "Preview refreshed" : "Refresh preview"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">Reset filters</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Fixture requests", value: String(requests.length), hint: "Local only", icon: FileCheck2, tone: "cyan" }, { label: "Visible", value: String(filtered.length), hint: "Filtered locally", icon: Search, tone: "violet" }, { label: "Queue", value: "Off", hint: "No live transfer", icon: WifiOff, tone: "amber" }, { label: "Review", value: reviewed ? "Local" : "Pending", hint: "No approval", icon: ShieldCheck, tone: "slate" }]} /><ScreenPreviewBanner title="Payout operations evidence boundary"><strong>This is not a payout management system.</strong> The rows are typed local fixtures. Search, filters, selection, refresh, and review state do not query a backend, reveal an account balance, approve a payout, invoke a provider, sign a wallet transaction, or confirm a transfer.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1fr_0.85fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex flex-wrap items-center gap-2"><div className="relative min-w-[220px] flex-1"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search fixture requests…" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-9 pr-3 text-sm text-white outline-none focus:border-cyan-300/50" /></div><select value={status} onChange={(e) => setStatus(e.target.value as Status | "All")} className="rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white"><option>All</option><option>Draft</option><option>Needs evidence</option><option>Blocked</option></select><select value={method} onChange={(e) => setMethod(e.target.value)} className="rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white"><option>All</option><option>Bank</option><option>Crypto</option><option>Provider</option></select></div><div className="mt-5 space-y-3">{filtered.length === 0 ? <div className="rounded-xl border border-dashed border-white/15 p-8 text-center"><X className="mx-auto size-7 text-slate-600" /><p className="mt-3 font-semibold">No fixture rows match</p><p className="mt-2 text-sm text-slate-500">Change the local filters; no remote query is issued.</p></div> : filtered.map((item) => <button key={item.id} onClick={() => { setSelected(item); setReviewed(false); }} className={`w-full rounded-xl border p-4 text-left ${selected.id === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10 hover:border-white/20"}`}><div className="flex items-center gap-3"><div className="flex size-9 items-center justify-center rounded-xl bg-cyan-300/10 text-cyan-200">{item.method === "Bank" ? <Banknote className="size-4" /> : <WalletCards className="size-4" />}</div><div className="flex-1"><p className="font-semibold">{item.destination}</p><p className="mt-1 text-xs text-slate-500">{item.id} · {item.method} · {item.amount}</p></div><Badge variant="outline" className="border-white/10 text-amber-200">{item.status}</Badge></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected request</p><h2 className="mt-2 text-2xl font-black">{selected.destination}</h2><p className="mt-3 text-sm leading-6 text-slate-400">{selected.detail}</p><div className="mt-5 grid gap-3 sm:grid-cols-2">{[{ label: "Request ID", value: selected.id }, { label: "Method", value: selected.method }, { label: "Amount", value: selected.amount }, { label: "Status", value: selected.status }, { label: "Balance", value: "Unavailable" }, { label: "Transfer ID", value: "Not issued" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 flex flex-wrap gap-2"><Button onClick={() => setReviewed(true)} variant="outline" className="border-cyan-300/30 bg-cyan-300/[0.06] text-cyan-100 hover:bg-cyan-300/10"><Check className="mr-2 size-4" />{reviewed ? "Reviewed locally" : "Mark reviewed locally"}</Button><Button disabled className="bg-cyan-300/20 text-cyan-100/40">Approve unavailable</Button><Button disabled variant="outline" className="border-white/10 text-white/40">Submit unavailable</Button></div><div className="mt-5 rounded-xl border border-rose-300/20 bg-rose-300/[0.06] p-4"><div className="flex gap-3"><TriangleAlert className="mt-0.5 size-5 shrink-0 text-rose-200" /><p className="text-sm leading-6 text-slate-300">Local review is not authorization, compliance approval, payout approval, or transfer submission.</p></div></div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Operations checklist</p><h2 className="mt-2 text-2xl font-black">Payout operations must prove</h2><div className="mt-5 space-y-3">{["Authenticated balance and earnings ledger with permissions and invariants", "Destination ownership, address/network/decimal/account validation, and consent", "Risk, sanctions/fraud, KYC/compliance, minimum, fee, currency, and tax controls", "Idempotent transfer creation, provider event verification, retry/failure/settlement state", "Audit, privacy, support, monitoring, reconciliation, and incident response"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><LockKeyhole className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake approval</h2><p className="mt-3 text-sm leading-6 text-slate-400">Filtering or reviewing a fixture changes local state only. It does not approve, queue, sign, transmit, reconcile, or settle a payout.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Operations surface preserved", description: "Search, status/method filters, selection, refresh, detail, and review intent are interactive.", icon: Search, status: "Verified locally" }, { title: "Evidence gaps are explicit", description: "Balance, destination, compliance, transfer, and settlement fields remain unavailable.", icon: ShieldCheck, status: "Guardrail" }, { title: "Transfer is blocked", description: "No payout queue, approval, wallet signature, bank trace, or settlement success is fabricated.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
