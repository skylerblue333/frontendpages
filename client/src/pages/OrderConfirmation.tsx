import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ClipboardCheck, FileCheck2, LockKeyhole, RefreshCw, ShieldCheck, TriangleAlert, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

export default function OrderConfirmation() {
  const [quantity, setQuantity] = useState("10");
  const [limitPrice, setLimitPrice] = useState("99.80");
  const [feeMode, setFeeMode] = useState<"unavailable" | "placeholder">("unavailable");
  const [acknowledged, setAcknowledged] = useState(false);
  const numericQty = Number(quantity);
  const numericPrice = Number(limitPrice);
  const subtotal = useMemo(() => Number.isFinite(numericQty * numericPrice) ? (numericQty * numericPrice).toFixed(2) : "—", [numericQty, numericPrice]);
  const valid = numericQty > 0 && numericPrice > 0;
  const reset = () => { setQuantity("10"); setLimitPrice("99.80"); setFeeMode("unavailable"); setAcknowledged(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={ClipboardCheck} eyebrow="OrderConfirmation · Settlement preview" title="Review every assumption before a transaction exists." description="Inspect a synthetic limit-order summary, local arithmetic, fee state, and receipt requirements. Nothing is submitted, charged, signed, broadcast, or recorded as a real order." badge="Confirmation preview"><div className="flex flex-wrap gap-2"><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset review</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Order state", value: "Draft", hint: "Local only", icon: ClipboardCheck, tone: "cyan" }, { label: "Subtotal", value: subtotal, hint: "Synthetic arithmetic", icon: FileCheck2, tone: "violet" }, { label: "Fees", value: feeMode === "placeholder" ? "Unset" : "Unavailable", hint: "No fee source", icon: TriangleAlert, tone: "amber" }, { label: "Settlement", value: "Blocked", hint: "No venue", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="Confirmation evidence boundary"><strong>This is a review fixture, not a real order confirmation.</strong> Product, side, symbol, quantity, limit price, subtotal, fee, total, account, venue, authorization, signature, transaction ID, settlement status, and receipt are not sourced from a broker, exchange, wallet, database, or payment rail. No charge, order, fill, or financial outcome is claimed.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1.05fr_0.95fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Draft review</p><h2 className="mt-2 text-3xl font-black">Buy concept · SKY / USD</h2></div><Badge variant="outline" className="border-white/10 text-slate-400">Venue unset</Badge></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="text-sm text-slate-400">Quantity<input value={quantity} onChange={(e) => setQuantity(e.target.value)} inputMode="decimal" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-white outline-none focus:border-cyan-300/50" /></label><label className="text-sm text-slate-400">Limit price<input value={limitPrice} onChange={(e) => setLimitPrice(e.target.value)} inputMode="decimal" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-white outline-none focus:border-cyan-300/50" /></label></div><div className="mt-5 grid gap-3 sm:grid-cols-2">{[{ label: "Side", value: "Buy concept" }, { label: "Time in force", value: "Not selected" }, { label: "Subtotal", value: `${subtotal} USD (local)` }, { label: "Total", value: "Not computable" }, { label: "Account", value: "Not connected" }, { label: "Order ID", value: "Not created" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div>{!valid && <p className="mt-4 flex items-center gap-2 text-sm text-amber-200"><TriangleAlert className="size-4" />Enter positive numeric values to calculate the local subtotal.</p>}<div className="mt-5 flex flex-wrap gap-2"><Button disabled={!valid || !acknowledged} className="bg-cyan-300/20 text-cyan-100/40">Confirm order unavailable</Button><Button disabled variant="outline" className="border-white/10 text-white/40">Sign / submit unavailable</Button></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Cost and consent</p><h2 className="mt-2 text-2xl font-black">Make uncertainty visible</h2><div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex gap-3"><TriangleAlert className="mt-0.5 size-5 shrink-0 text-amber-200" /><p className="text-sm leading-6 text-slate-300">Fees, taxes, slippage, purchasing power, and settlement are not available. The subtotal is arithmetic only and is not a quote, cost, or financial recommendation.</p></div></div><div className="mt-5 flex gap-3"><button aria-label="Acknowledge preview boundary" onClick={() => setAcknowledged(!acknowledged)} className={`flex size-5 shrink-0 items-center justify-center rounded border ${acknowledged ? "border-cyan-300 bg-cyan-300 text-slate-950" : "border-white/20"}`}>{acknowledged ? "✓" : ""}</button><p className="text-sm leading-6 text-slate-400">I understand this review is local-only and cannot place, sign, charge, or confirm a real order.</p></div><div className="mt-5 grid gap-2"><Button onClick={() => setFeeMode(feeMode === "unavailable" ? "placeholder" : "unavailable")} variant="outline" className="justify-start border-white/10 bg-white/[0.03] text-white hover:bg-white/10"><FileCheck2 className="mr-2 size-4 text-cyan-300" />{feeMode === "unavailable" ? "Mark fee as placeholder" : "Return fee to unavailable"}</Button><div className="rounded-xl border border-white/10 p-4"><p className="text-xs text-slate-500">Fee state</p><p className="mt-2 font-semibold text-amber-200">{feeMode === "placeholder" ? "Placeholder — no source or rate" : "Unavailable — no fee service"}</p></div></div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Required receipt evidence</p><h2 className="mt-2 text-2xl font-black">What a real confirmation must prove</h2><div className="mt-5 space-y-3">{["Authenticated account, permissions, venue and instrument metadata", "Validated quantity, price, decimal precision, fees, taxes, and balance", "Explicit authorization, idempotency key, signature and submission receipt", "Server-verified order ID, transaction hash, status, timestamps and fills", "Failure, cancellation, reconciliation, support, privacy and audit trail"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><LockKeyhole className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake confirmation</h2><p className="mt-3 text-sm leading-6 text-slate-400">A disabled confirm control is intentional. Until settlement infrastructure and evidence exist, the UI cannot claim an order was accepted or completed.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Arithmetic stays local", description: "Quantity and limit price produce a transparent subtotal without presenting it as a quote or charge.", icon: FileCheck2, status: "Verified locally" }, { title: "Fees are honest", description: "The fee state is unavailable by default and can only be marked as a clearly labeled placeholder.", icon: TriangleAlert, status: "Guardrail" }, { title: "Settlement is blocked", description: "No exchange, wallet, broker, signature, payment, order ID, fill, or transaction is fabricated.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
