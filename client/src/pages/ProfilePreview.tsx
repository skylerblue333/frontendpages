import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Accessibility, Check, Eye, FileText, Heart, Link2, LockKeyhole, Play, RefreshCw, ShieldAlert, UserRound, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const views = ["Private draft", "Authenticated viewer", "Public later"];
const tabs = ["About", "Activity", "Media"];
export default function ProfilePreview() {
  const [view, setView] = useState(views[0]);
  const [tab, setTab] = useState(tabs[0]);
  const [saved, setSaved] = useState(false);
  const [showEvidence, setShowEvidence] = useState(false);
  const reset = () => { setView(views[0]); setTab(tabs[0]); setSaved(false); setShowEvidence(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Eye} eyebrow="ProfilePreview · Read-only preview" title="Preview the profile before it becomes visible to anyone." description="Inspect local profile content, viewer intent, accessibility, and evidence disclosure. No identity verification, social activity, public URL, media, follower count, or account publication is asserted or generated." badge="Preview workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Preview saved locally" : "Save preview locally"}</Button><Button onClick={() => setShowEvidence((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showEvidence ? <X className="mr-2 size-4" /> : <FileText className="mr-2 size-4" />}{showEvidence ? "Close evidence" : "View evidence"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset preview</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Viewer", value: view, hint: "Local intent", icon: Eye, tone: "cyan" }, { label: "Identity", value: "Unverified", hint: "No account source", icon: UserRound, tone: "violet" }, { label: "Activity", value: "Unavailable", hint: "No social source", icon: Heart, tone: "amber" }, { label: "Share link", value: "Off", hint: "No public URL", icon: Link2, tone: "slate" }]} /><ScreenPreviewBanner title="Preview evidence boundary"><strong>This is a local read-only profile preview, not a published profile.</strong> Viewer intent, name, bio, tabs, and disclosure are browser state. No identity, verification, privacy decision, public link, social activity, media, follower count, or account publication is connected.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.8fr_1.2fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-4"><div className="flex size-20 items-center justify-center rounded-3xl bg-cyan-300/10"><UserRound className="size-9 text-cyan-200" /></div><div><div className="flex flex-wrap items-center gap-2"><h2 className="text-2xl font-black">Skycoin explorer</h2><Badge variant="outline" className="border-amber-300/20 text-amber-200">Unverified</Badge></div><p className="mt-2 text-sm text-slate-500">@handle-unverified · {view}</p></div></div><p className="mt-6 text-sm leading-7 text-slate-300">A local profile preview for exploring the SKYCOIN4444 ecosystem. No public account or social history is loaded.</p><div className="mt-6 flex flex-wrap gap-2">{views.map((item) => <button key={item} onClick={() => setView(item)} className={`rounded-lg border px-3 py-2 text-xs ${view === item ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div>{showEvidence && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4 text-sm leading-6 text-slate-400">Preview content is authored local concept text. A real profile would need authenticated ownership, visibility enforcement, privacy review, accessible rendering, moderation, and a reversible publication path.</div>}</CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Preview posture</p><h2 className="mt-2 text-2xl font-black">No public audience</h2><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Profile", value: "Local draft" }, { label: "Verification", value: "Unavailable" }, { label: "Followers", value: "Unknown" }, { label: "Share URL", value: "Not created" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-6 rounded-2xl border border-amber-300/25 bg-amber-300/[0.06] p-5"><div className="flex items-start gap-3"><LockKeyhole className="mt-0.5 size-5 shrink-0 text-amber-200" /><div><p className="font-semibold text-amber-100">Sharing is unavailable</p><p className="mt-2 text-sm leading-6 text-slate-400">No account service, public URL, social graph, media source, moderation, or privacy enforcement is connected.</p><Button disabled className="mt-4 bg-slate-700 text-slate-400">Share profile unavailable</Button></div></div></div></CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex flex-wrap gap-2">{tabs.map((item) => <button key={item} onClick={() => setTab(item)} className={`flex items-center gap-2 rounded-lg border px-4 py-2 text-sm ${tab === item ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-200" : "border-white/10 text-slate-500"}`}>{item === "About" && <FileText className="size-4" />}{item === "Activity" && <Heart className="size-4" />}{item === "Media" && <Play className="size-4" />}{item}</button>)}</div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-12 text-center">{tab === "About" && <FileText className="mx-auto size-10 text-cyan-300/40" />}{tab === "Activity" && <Heart className="mx-auto size-10 text-pink-300/40" />}{tab === "Media" && <Play className="mx-auto size-10 text-violet-300/40" />}<h3 className="mt-4 text-xl font-black">{tab} unavailable in preview</h3><p className="mx-auto mt-2 max-w-xl text-sm leading-6 text-slate-500">This tab remains useful as an honest empty state. No {tab.toLowerCase()} source is connected, and no activity or media is fabricated.</p><Button disabled className="mt-5 bg-slate-700 text-slate-400">Load {tab.toLowerCase()} unavailable</Button></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Accessibility and privacy gates</p><h2 className="mt-2 text-2xl font-black">A preview must still be safe to read</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{[{ label: "Accessibility", detail: "Keyboard order, focus, contrast, alt text, reduced motion, and screen-reader structure", icon: Accessibility }, { label: "Privacy", detail: "Visibility enforcement, consent, export, deletion, and no accidental public indexing", icon: LockKeyhole }, { label: "Moderation", detail: "Profile text, image, links, abuse reporting, appeals, and reversible enforcement", icon: ShieldAlert }, { label: "Ownership", detail: "Authenticated editor, audit trail, public link lifecycle, and rollback", icon: UserRound }].map((item) => { const Icon = item.icon; return <div key={item.label} className="flex items-start gap-3 rounded-xl border border-white/10 p-4"><Icon className="mt-0.5 size-5 text-slate-500" /><div><p className="font-semibold text-slate-200">{item.label}</p><p className="mt-1 text-sm leading-6 text-slate-500">{item.detail}</p></div></div>; })}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Preview surface preserved", description: "Viewer intent, profile summary, tabs, evidence disclosure, accessibility/privacy gates, save/reset, and unavailable share remain interactive.", icon: Eye, status: "Local preview" }, { title: "No publication theater", description: "Identity, verification, public URLs, social activity, media, followers, and audience are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Visibility before sharing", description: "Real preview and sharing require account ownership, privacy enforcement, moderation, accessible rendering, and rollback.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
