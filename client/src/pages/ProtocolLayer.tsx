import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Activity, Braces, BookOpen, Check, Code2, KeyRound, LockKeyhole, Network, RefreshCw, ShieldAlert, Webhook, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const tabs = ["Overview", "API", "SDK", "Webhooks", "Keys"];
const integrations = [{ label: "API contract", detail: "Routes and schemas need a governed source", icon: Braces }, { label: "SDK surface", detail: "Installation and auth are not published", icon: Code2 }, { label: "Webhooks", detail: "Events and retries are not connected", icon: Webhook }, { label: "Key management", detail: "No key provider or secret vault", icon: KeyRound }];
export default function ProtocolLayer() {
  const [tab, setTab] = useState(tabs[0]);
  const [saved, setSaved] = useState(false);
  const [showGates, setShowGates] = useState(false);
  const reset = () => { setTab(tabs[0]); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Network} eyebrow="ProtocolLayer · Integration architecture preview" title="Map the protocol before exposing a production claim." description="Review local API, SDK, webhook, key, network, observability, and governance surfaces. No live API, chain, validator, key, payment, user, uptime, throughput, security, or production attestation is connected." badge="Protocol workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Review saved locally" : "Save review locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review protocol gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset review</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Protocol", value: "Design only", hint: "No live contract", icon: Network, tone: "cyan" }, { label: "API", value: "Unpublished", hint: "No route source", icon: Braces, tone: "violet" }, { label: "Keys", value: "Not issued", hint: "No secret provider", icon: KeyRound, tone: "amber" }, { label: "Operations", value: "Unmeasured", hint: "No telemetry source", icon: Activity, tone: "slate" }]} /><ScreenPreviewBanner title="Protocol evidence boundary"><strong>This is a local protocol-architecture worksheet, not an operational control plane.</strong> Tabs, integration cards, event names, key posture, network concepts, and saved state are browser concepts. No API endpoint, SDK, webhook, secret, key, payment, chain, validator, user, uptime, throughput, latency, security control, or production availability is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.8fr_1.2fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Protocol surfaces</p><h2 className="mt-2 text-2xl font-black">Select an integration layer</h2><div className="mt-5 space-y-3">{integrations.map((item) => { const Icon = item.icon; return <div key={item.label} className="flex items-center gap-3 rounded-xl border border-white/10 p-4"><Icon className="size-5 text-cyan-200" /><div className="flex-1"><p className="font-semibold">{item.label}</p><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Unverified</Badge></div>; })}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Operational posture</p><h2 className="mt-2 text-2xl font-black">No production contract loaded</h2><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Network", value: "Unselected", icon: Network }, { label: "Chain", value: "Not connected", icon: Activity }, { label: "Uptime", value: "Unmeasured", icon: Activity }, { label: "Throughput", value: "Unmeasured", icon: Activity }, { label: "Security", value: "Unattested", icon: LockKeyhole }, { label: "Keys", value: "Not issued", icon: KeyRound }].map((item) => { const Icon = item.icon; return <div key={item.label} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><Icon className="size-4 text-slate-500" /><div><p className="text-xs text-slate-500">{item.label}</p><p className="mt-1 text-sm font-semibold text-amber-200">{item.value}</p></div></div>; })}</div><div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">Operations unavailable</p><p className="mt-2 text-sm leading-6 text-slate-400">No RPC, API gateway, secret manager, webhook receiver, metrics, logs, tracing, incident response, or support integration is connected.</p><Button disabled className="mt-4 bg-slate-700 text-slate-400">Open operations unavailable</Button></div></CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex flex-wrap gap-2">{tabs.map((item) => <button key={item} onClick={() => setTab(item)} className={`flex items-center gap-2 rounded-lg border px-4 py-2 text-sm ${tab === item ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-200" : "border-white/10 text-slate-500"}`}>{item === "Overview" && <Network className="size-4" />}{item === "API" && <Braces className="size-4" />}{item === "SDK" && <Code2 className="size-4" />}{item === "Webhooks" && <Webhook className="size-4" />}{item === "Keys" && <KeyRound className="size-4" />}{item}</button>)}</div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-10">{tab === "Overview" ? <div><Network className="size-9 text-cyan-300/40" /><h3 className="mt-4 text-xl font-black">Protocol design preserved locally</h3><p className="mt-2 text-sm leading-6 text-slate-500">No live contract, network, service health, or operational evidence is connected.</p></div> : <div className="text-center"><ShieldAlert className="mx-auto size-10 text-violet-300/40" /><h3 className="mt-4 text-xl font-black">{tab} unavailable in preview</h3><p className="mx-auto mt-2 max-w-xl text-sm leading-6 text-slate-500">No {tab.toLowerCase()} provider is connected. Nothing is fabricated to imply access, installation, delivery, signing, event receipt, or API success.</p><Button disabled className="mt-5 bg-slate-700 text-slate-400">Load {tab.toLowerCase()} unavailable</Button></div>}</div></CardContent></Card>{showGates && <Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Protocol gates</p><h2 className="mt-2 text-2xl font-black">What a real protocol layer must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Versioned API contracts, authentication, authorization, rate limits, and error behavior", "Secret storage, key rotation, least privilege, signing, redaction, and incident response", "Webhook signing, retries, idempotency, replay protection, ordering, and dead-letter handling", "Network, chain, RPC, validator, finality, fees, and failure-state verification", "Metrics, logs, traces, SLOs, uptime, latency, capacity, alerts, and support", "Privacy, data retention, audit, deployment, rollback, security review, and documentation"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card>}<ScreenFeatureGrid features={[{ title: "Protocol surface preserved", description: "API, SDK, webhooks, keys, network, operations, tabs, local review, save/reset, and governance gates remain interactive.", icon: Network, status: "Local architecture" }, { title: "No protocol theater", description: "Endpoints, keys, users, payments, chains, validators, uptime, throughput, security, and production availability are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Proof before exposure", description: "Real protocol access needs contracts, secrets, chain verification, observability, security, deployment, rollback, and support.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
