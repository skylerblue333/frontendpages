import { useMemo, useState } from "react";
import { ArrowRight, BadgeDollarSign, Boxes, Check, ClipboardList, FileCheck2, Globe2, LockKeyhole, MapPin, PackageCheck, Plus, Receipt, RefreshCw, ShieldAlert, ShoppingCart, Truck, UserRound, WifiOff, XCircle } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

type LineItem = { id: number; name: string; quantity: string; unitCost: string };
const parse = (value: string) => { const number = Number(value.replace(/[^0-9.-]/g, "")); return Number.isFinite(number) && number >= 0 ? number : 0; };
const INITIAL: LineItem[] = [{ id: 1, name: "Product or service placeholder", quantity: "0", unitCost: "0" }, { id: 2, name: "Second line item", quantity: "0", unitCost: "0" }];

export default function BulkOrdering() { const [items, setItems] = useState(INITIAL); const [currency, setCurrency] = useState("USD preview"); const [destination, setDestination] = useState("Fulfillment destination not entered"); const [prepared, setPrepared] = useState(false); const subtotal = useMemo(() => items.reduce((sum, item) => sum + parse(item.quantity) * parse(item.unitCost), 0), [items]); const update = (id: number, field: keyof LineItem, value: string) => { setItems((current) => current.map((item) => item.id === id ? { ...item, [field]: value } : item)); setPrepared(false); }; const add = () => setItems((current) => [...current, { id: Date.now(), name: `Line item ${current.length + 1}`, quantity: "0", unitCost: "0" }]); return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={ShoppingCart} eyebrow="Commerce Planning · Batch Procurement" title="Plan the order before money or inventory enter the picture." description="Build a hypothetical bulk-order request, calculate entered line-item arithmetic, and inspect procurement and fulfillment gates. This workspace does not quote live prices, reserve stock, charge payment, create an order, or promise shipment." badge="Preview procurement workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setPrepared(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><ClipboardList className="mr-2 size-4" />Prepare order plan</Button><Button onClick={() => { setItems(INITIAL); setPrepared(false); }} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset plan</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Line items", value: String(items.length), hint: "Local planning rows", icon: Boxes }, { label: "Entered subtotal", value: `$${subtotal.toLocaleString()}`, hint: `${currency} arithmetic`, icon: Receipt, tone: "violet" }, { label: "Inventory", value: "Unavailable", hint: "No stock provider", icon: WifiOff, tone: "amber" }, { label: "Payment", value: "Not connected", hint: "No charge or order", icon: LockKeyhole, tone: "slate" }]} /><ScreenPreviewBanner title="Commerce evidence boundary">A calculated subtotal is not a quote, invoice, tax result, payment authorization, purchase order, inventory reservation, shipment, delivery estimate, or refund policy. A real bulk-order workflow needs merchant identity, catalog and stock truth, pricing validity, tax and shipping rules, approval controls, payment confirmation, order idempotency, fulfillment updates, and reconciliation.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Request builder</p><h2 className="mt-1 text-2xl font-black">Line items and destination</h2></div><ShoppingCart className="size-5 text-cyan-300" /></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><div><label className="mb-2 block text-sm text-slate-300" htmlFor="currency">Currency label</label><Input id="currency" value={currency} onChange={(event) => setCurrency(event.target.value)} className="border-white/10 bg-black/20 text-white" /></div><div><label className="mb-2 block text-sm text-slate-300" htmlFor="destination">Fulfillment destination</label><Input id="destination" value={destination} onChange={(event) => setDestination(event.target.value)} className="border-white/10 bg-black/20 text-white" /></div></div><div className="mt-6 space-y-3">{items.map((item) => <div key={item.id} className="rounded-xl border border-white/10 bg-black/15 p-4"><div className="grid gap-3 sm:grid-cols-[1fr_0.35fr_0.45fr]"><Input value={item.name} onChange={(event) => update(item.id, "name", event.target.value)} aria-label="Item name" className="border-white/10 bg-white/[0.04] text-white" /><Input value={item.quantity} onChange={(event) => update(item.id, "quantity", event.target.value)} aria-label="Item quantity" inputMode="decimal" placeholder="Qty" className="border-white/10 bg-white/[0.04] text-white" /><Input value={item.unitCost} onChange={(event) => update(item.id, "unitCost", event.target.value)} aria-label="Unit cost" inputMode="decimal" placeholder="Unit cost" className="border-white/10 bg-white/[0.04] text-white" /></div><div className="mt-3 flex items-center justify-between text-xs text-slate-500"><span>{parse(item.quantity)} × {currency} {parse(item.unitCost).toLocaleString()}</span><span>Line arithmetic: {currency} {(parse(item.quantity) * parse(item.unitCost)).toLocaleString()}</span></div></div>)}<Button onClick={add} variant="outline" className="w-full border-dashed border-white/15 text-slate-300 hover:bg-white/10"><Plus className="mr-2 size-4" />Add line item</Button></div></CardContent></Card><div className="space-y-6"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><FileCheck2 className="size-5 text-violet-300" /><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Procurement gates</p><h2 className="mt-1 text-xl font-bold">What remains unverified</h2></div></div><div className="mt-5 space-y-3">{[{ label: "Catalog and stock", state: "Unavailable", icon: Boxes }, { label: "Price and tax", state: "Unquoted", icon: BadgeDollarSign }, { label: "Approval authority", state: "Required", icon: UserRound }, { label: "Payment authorization", state: "Not connected", icon: LockKeyhole }, { label: "Fulfillment and carrier", state: "Unavailable", icon: Truck }].map(({ label, state, icon: Icon }) => <div key={label} className="flex items-center gap-3 rounded-lg border border-white/10 bg-black/15 p-3"><Icon className="size-4 text-slate-400" /><span className="flex-1 text-sm text-slate-300">{label}</span><span className="text-xs text-amber-200">{state}</span></div>)}</div></CardContent></Card><Card className="border-amber-300/20 bg-amber-300/[0.05]"><CardContent className="p-6"><div className="flex items-center gap-3"><ShieldAlert className="size-5 text-amber-300" /><div><h2 className="font-semibold text-amber-200">Subtotal is not a purchase</h2><p className="mt-2 text-sm leading-6 text-slate-300">The number is calculated only from your entered quantities and unit costs. No merchant, stock, tax, shipping, payment, or delivery fact is inferred.</p></div></div></CardContent></Card></div></section><section className="grid gap-4 md:grid-cols-3"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><MapPin className="size-5 text-cyan-300" /><h3 className="mt-3 font-semibold">Destination is a plan input</h3><p className="mt-2 text-sm leading-6 text-slate-400">A typed destination is not a validated address, service area, tax jurisdiction, or delivery commitment.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><ShieldAlert className="size-5 text-amber-300" /><h3 className="mt-3 font-semibold">Approval before payment</h3><p className="mt-2 text-sm leading-6 text-slate-400">Procurement needs separation of requester, approver, payer, and fulfillment responsibility.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><PackageCheck className="size-5 text-violet-300" /><h3 className="mt-3 font-semibold">Fulfillment needs evidence</h3><p className="mt-2 text-sm leading-6 text-slate-400">An order plan is not inventory, a tracking number, shipment, delivery, or return authorization.</p></CardContent></Card></section>{prepared && <Card className="border-cyan-300/20 bg-cyan-300/[0.06]"><CardContent className="flex items-center gap-3 p-5"><Check className="size-5 text-cyan-300" /><div><p className="font-semibold">Local order plan prepared</p><p className="mt-1 text-sm text-slate-400">{items.length} line items · {currency} {subtotal.toLocaleString()} entered arithmetic · no order was created.</p></div><Badge variant="outline" className="ml-auto border-amber-300/20 text-amber-200">Preview only</Badge></CardContent></Card>}<ScreenFeatureGrid features={[{ title: "Arithmetic stays inspectable", description: "Show quantity, unit cost, and line subtotal rather than hiding an unexplained total.", icon: Receipt, status: "Pattern" }, { title: "Procurement is multi-party", description: "Catalog, approval, payment, and fulfillment each need their own verified boundary.", icon: UserRound, status: "Required" }, { title: "No fake commerce", description: "A prepared request is not a quote, charge, order, shipment, delivery, or refund.", icon: ShieldAlert, status: "Guardrail" }]} /></main></div>; }
