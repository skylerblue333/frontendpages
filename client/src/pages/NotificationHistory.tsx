import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Archive, Bell, Check, Clock3, FileCheck2, Filter, History, LockKeyhole, Mail, RefreshCw, ShieldCheck, Smartphone, Tag, UserRound, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const SEEDS = [{ id: "h1", title: "Review window opened", category: "Workspace", channel: "Inbox", detail: "Synthetic audit row for a local review reminder." }, { id: "h2", title: "Security checklist due", category: "Security", channel: "Inbox", detail: "Synthetic audit row for a safety checklist concept." }, { id: "h3", title: "Study plan checkpoint", category: "Learning", channel: "Digest", detail: "Synthetic audit row without enrollment or instructor state." }, { id: "h4", title: "Wallet safety note", category: "Finance", channel: "Push", detail: "Synthetic audit row; no device, token, account, or delivery receipt exists." }, { id: "h5", title: "Message concept", category: "Community", channel: "Email", detail: "Synthetic audit row with no sender identity or email transmission." }];
const CATEGORIES = ["All", "Workspace", "Security", "Learning", "Finance", "Community"];
const CHANNELS = ["All", "Inbox", "Digest", "Push", "Email"];

export default function NotificationHistory() {
  const [category, setCategory] = useState("All");
  const [channel, setChannel] = useState("All");
  const [selected, setSelected] = useState(SEEDS[0].id);
  const [archived, setArchived] = useState<string[]>([]);
  const [read, setRead] = useState<string[]>([]);
  const [note, setNote] = useState("");
  const [saved, setSaved] = useState(false);
  const visible = useMemo(() => SEEDS.filter((item) => (category === "All" || item.category === category) && (channel === "All" || item.channel === channel) && !archived.includes(item.id)), [archived, category, channel]);
  const item = SEEDS.find((entry) => entry.id === selected) ?? SEEDS[0];
  const reset = () => { setCategory("All"); setChannel("All"); setSelected(SEEDS[0].id); setArchived([]); setRead([]); setNote(""); setSaved(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={History} eyebrow="NotificationHistory · Audit preview" title="Review the record before calling it delivered." description="Inspect synthetic notification-history rows with category and channel filters, local read/archive state, retention planning, and evidence notes. No delivery receipt, sender, timestamp, unread count, provider, or backend history is claimed." badge="History preview"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Audit note saved locally" : "Save audit note locally"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset history</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Rows", value: String(visible.length), hint: "Synthetic history", icon: History }, { label: "Read", value: String(read.length), hint: "Local state", icon: Check, tone: "violet" }, { label: "Archived", value: String(archived.length), hint: "Local state", icon: Archive, tone: "amber" }, { label: "Receipt", value: "Off", hint: "No provider", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="NotificationHistory evidence boundary"><strong>History rows, categories, channels, read/archive state, and retention language are local audit fixtures—not delivery receipts, timestamps, sender identity, provider status, push/email transmission, backend records, unread counts, or proof that a notification reached a user.</strong> Production history requires durable event storage, immutable identifiers, provider receipts, timestamps, timezone policy, retention/deletion, access controls, privacy, reconciliation, and audit ownership.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Synthetic audit rows</p><h2 className="mt-2 text-3xl font-black">History review</h2></div><Badge variant="outline" className="border-white/10 text-slate-400">No receipt source</Badge></div><div className="mt-4 grid gap-3 sm:grid-cols-2"><label className="text-sm text-slate-400">Category<select value={category} onChange={(event) => setCategory(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-slate-950 px-3 py-3 text-white">{CATEGORIES.map((option) => <option key={option}>{option}</option>)}</select></label><label className="text-sm text-slate-400">Channel<select value={channel} onChange={(event) => setChannel(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-slate-950 px-3 py-3 text-white">{CHANNELS.map((option) => <option key={option}>{option}</option>)}</select></label></div><div className="mt-5 space-y-3">{visible.map((entry) => <button key={entry.id} onClick={() => setSelected(entry.id)} className={`w-full rounded-2xl border p-4 text-left ${selected === entry.id ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-black/10"}`}><div className="flex items-start gap-3"><div className="flex size-9 items-center justify-center rounded-xl bg-cyan-300/10 text-cyan-200"><Bell className="size-4" /></div><div className="flex-1"><div className="flex items-center justify-between gap-2"><p className="font-black">{entry.title}</p><Badge variant="outline" className="border-white/10 text-[10px] text-slate-400">{entry.channel}</Badge></div><p className="mt-2 text-sm text-slate-400">{entry.detail}</p><p className="mt-2 text-xs text-slate-500">{entry.category} · {read.includes(entry.id) ? "Read locally" : "Unread locally"}</p></div></div></button>)}{visible.length === 0 && <p className="py-8 text-center text-slate-500">No synthetic rows match these filters.</p>}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Audit detail</p><h2 className="mt-2 text-3xl font-black">{item.title}</h2><p className="mt-3 text-slate-400">{item.detail}</p><div className="mt-5 grid gap-3 sm:grid-cols-2">{[{ label: "Channel", value: item.channel }, { label: "Timestamp", value: "Not sourced" }, { label: "Provider", value: "Unavailable" }, { label: "Sender", value: "Synthetic" }, { label: "Receipt", value: "Not generated" }, { label: "Retention", value: "Not configured" }].map((detail) => <div key={detail.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{detail.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{detail.value}</p></div>)}</div><div className="mt-5 flex flex-wrap gap-2"><Button onClick={() => setRead((current) => current.includes(item.id) ? current.filter((id) => id !== item.id) : [...current, item.id])} className="bg-violet-500 text-white hover:bg-violet-400"><Check className="mr-2 size-4" />{read.includes(item.id) ? "Mark unread locally" : "Mark read locally"}</Button><Button onClick={() => setArchived((current) => [...current, item.id])} variant="outline" className="border-white/10 text-white"><Archive className="mr-2 size-4" />Archive locally</Button></div><textarea value={note} onChange={(event) => { setNote(event.target.value); setSaved(false); }} placeholder="Write a local audit or retention note…" className="mt-5 min-h-24 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-sm text-white placeholder:text-slate-500" /><div className="mt-4 flex items-start gap-3 text-xs leading-5 text-slate-500"><LockKeyhole className="mt-0.5 size-4 shrink-0 text-cyan-300" />Local actions do not delete, restore, resend, export, or change a provider record.</div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">History readiness</p><h2 className="mt-2 text-2xl font-black">Required before audit claims</h2><div className="mt-5 space-y-3">{["Durable event IDs, provider receipts, timestamps, timezone, and source", "Read/unread semantics, deduplication, retries, and delivery lifecycle", "Retention, deletion, export, access control, privacy, and legal hold", "Reconciliation, corrections, audit ownership, and incident response", "Accessibility, support, channel status, and user-facing explanations"].map((entry) => <div key={entry} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{entry}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><FileCheck2 className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake history</h2><p className="mt-3 text-sm leading-6 text-slate-400">The page changes local state only. It does not record delivery, reconstruct provider events, expose users, or guarantee retention and deletion.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "A row is not a receipt", description: "Synthetic history preserves audit UX without delivery, timestamp, sender, or provider claims.", icon: History, status: "Guardrail" }, { title: "Retention needs policy", description: "Durability, deletion, access, privacy, reconciliation, and audit ownership need evidence.", icon: ShieldCheck, status: "Required" }, { title: "No fake resend", description: "Local archive and read actions do not resend, delete, restore, export, or update a backend record.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
