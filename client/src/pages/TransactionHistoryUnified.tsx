import { ArrowDownToLine, ArrowUpFromLine, History, ShieldCheck } from "lucide-react";
import { PageHeader } from "@/components/PageHeader";

export default function TransactionHistoryUnified() {
  return <div className="container max-w-6xl animate-page-in py-8"><PageHeader icon={History} title="Transaction History" subtitle="One auditable transaction timeline across the ecosystem" /><div className="mb-8 flex items-center gap-3 rounded-3xl border border-cyan-300/20 bg-cyan-300/[0.05] p-6"><ShieldCheck className="size-6 text-cyan-200" /><div><h2 className="text-xl font-black">Event-sourced transaction timeline</h2><p className="mt-1 text-sm text-muted-foreground">Designed to consume verified transaction events from the unified financial/event layer.</p></div></div><div className="grid gap-4 md:grid-cols-2">{[[ArrowDownToLine,"Incoming activity"],[ArrowUpFromLine,"Outgoing activity"]].map(([Icon,label]) => <div key={label as string} className="card p-5"><Icon className="size-6 text-cyan-200" /><h3 className="mt-4 font-bold">{label as string}</h3><p className="mt-2 text-sm text-muted-foreground">No authoritative transaction stream is connected.</p></div>)}</div><div className="mt-8 card p-6"><div className="flex min-h-64 items-center justify-center rounded-2xl border border-dashed border-border/60 text-center"><div><History className="mx-auto size-9 text-muted-foreground/50" /><p className="mt-3 font-semibold">No transactions loaded</p><p className="mt-2 max-w-md text-sm text-muted-foreground">Connect authenticated ledger events, pagination, reconciliation, and audit provenance before displaying transaction records.</p></div></div></div></div>;
}
