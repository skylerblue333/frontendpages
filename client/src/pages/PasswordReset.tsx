import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Check, Eye, EyeOff, KeyRound, LockKeyhole, Mail, RefreshCw, ShieldCheck, TriangleAlert, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

type Step = "request" | "verify" | "set";
export default function PasswordReset() {
  const [step, setStep] = useState<Step>("request");
  const [email, setEmail] = useState("");
  const [token, setToken] = useState("");
  const [password, setPassword] = useState("");
  const [visible, setVisible] = useState(false);
  const [acknowledged, setAcknowledged] = useState(false);
  const emailValid = /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);
  const tokenValid = /^[A-Z0-9-]{6,}$/.test(token);
  const passwordValid = password.length >= 12 && /[A-Z]/.test(password) && /[a-z]/.test(password) && /\d/.test(password) && /[^A-Za-z0-9]/.test(password);
  const status = useMemo(() => step === "request" ? "Request not sent" : step === "verify" ? "Token unavailable" : "Password not set", [step]);
  const reset = () => { setStep("request"); setEmail(""); setToken(""); setPassword(""); setVisible(false); setAcknowledged(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={KeyRound} eyebrow="PasswordReset · Recovery preview" title="Make recovery steps visible without faking delivery." description="Walk through a local request, token verification, and new-password flow. No email is sent, token is issued, account is found, session is changed, or password is updated." badge="Recovery preview"><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset flow</Button></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Step", value: step === "request" ? "1 / 3" : step === "verify" ? "2 / 3" : "3 / 3", hint: "Local flow", icon: KeyRound, tone: "cyan" }, { label: "Delivery", value: "Off", hint: "No email service", icon: Mail, tone: "violet" }, { label: "Token", value: "Unavailable", hint: "No issuer", icon: WifiOff, tone: "amber" }, { label: "Account", value: "Unchanged", hint: "No mutation", icon: LockKeyhole, tone: "slate" }]} /><ScreenPreviewBanner title="Recovery evidence boundary"><strong>Never use a real email, reset token, or password in this preview.</strong> The flow only changes local component state. It does not send messages, reveal whether an account exists, issue or verify a token, change credentials, invalidate sessions, or claim recovery success.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1fr_0.85fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="mb-6 flex flex-wrap gap-2">{([{ id: "request", label: "Request" }, { id: "verify", label: "Verify" }, { id: "set", label: "Set password" }] as const).map((item, index) => <button key={item.id} onClick={() => setStep(item.id)} className={`rounded-full border px-3 py-1.5 text-xs ${step === item.id ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-200" : "border-white/10 text-slate-500"}`}>{index + 1}. {item.label}</button>)}</div>{step === "request" && <><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Request reset</p><h2 className="mt-2 text-3xl font-black">Start with a contact concept</h2><label className="mt-6 block text-sm text-slate-400">Email address<input type="email" autoComplete="off" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="preview@example.invalid" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-white outline-none focus:border-cyan-300/50" /></label><p className="mt-3 text-sm text-slate-500">Use a non-real address. A production flow must avoid account-enumeration leaks and send through a verified provider.</p><Button disabled={!emailValid} onClick={() => setStep("verify")} className="mt-5 bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Mail className="mr-2 size-4" />Continue locally</Button></>}{step === "verify" && <><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Verify reset token</p><h2 className="mt-2 text-3xl font-black">No token has been issued.</h2><p className="mt-3 text-slate-400">Enter a fixture-shaped token only to exercise validation. It will not be checked against a server.</p><label className="mt-6 block text-sm text-slate-400">Token<input value={token} onChange={(e) => setToken(e.target.value.toUpperCase())} placeholder="ABC123" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-white outline-none focus:border-cyan-300/50" /></label><div className="mt-4 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex gap-3"><TriangleAlert className="mt-0.5 size-5 shrink-0 text-amber-200" /><p className="text-sm leading-6 text-slate-300">Token expiry, one-time use, delivery, account binding, and replay protection are unavailable in this local preview.</p></div></div><Button disabled={!tokenValid} onClick={() => setStep("set")} className="mt-5 bg-cyan-300 text-slate-950 hover:bg-cyan-200">Continue locally</Button></>}{step === "set" && <><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Set new password</p><h2 className="mt-2 text-3xl font-black">Prepare a credential concept</h2><label className="mt-6 block text-sm text-slate-400">New password<div className="relative mt-2"><input type={visible ? "text" : "password"} autoComplete="new-password" value={password} onChange={(e) => { setPassword(e.target.value); setAcknowledged(false); }} placeholder="Preview value only" className="w-full rounded-xl border border-white/10 bg-black/20 p-3 pr-12 text-white outline-none focus:border-cyan-300/50" /><button type="button" aria-label={visible ? "Hide password" : "Show password"} onClick={() => setVisible(!visible)} className="absolute right-3 top-1/2 -translate-y-1/2 text-slate-400">{visible ? <EyeOff className="size-4" /> : <Eye className="size-4" />}</button></div></label><div className="mt-4 grid gap-2 sm:grid-cols-2">{["12+ characters", "Upper + lowercase", "Number", "Symbol"].map((item, index) => <div key={item} className="flex items-center gap-2 rounded-xl border border-white/10 p-3 text-sm"><Check className={`size-4 ${passwordValid ? "text-cyan-300" : "text-slate-600"}`} /><span className="text-slate-400">{item}{index === 0 && password.length < 12 ? " — needed" : ""}</span></div>)}</div><div className="mt-5 flex gap-3"><button aria-label="Acknowledge recovery boundary" onClick={() => setAcknowledged(!acknowledged)} className={`flex size-5 shrink-0 items-center justify-center rounded border ${acknowledged ? "border-cyan-300 bg-cyan-300 text-slate-950" : "border-white/20"}`}>{acknowledged ? "✓" : ""}</button><p className="text-sm leading-6 text-slate-400">I understand this cannot update an account or invalidate sessions.</p></div><Button disabled={!passwordValid || !acknowledged} className="mt-5 bg-cyan-300/20 text-cyan-100/40">Reset password unavailable</Button></>}</CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Recovery safety</p><h2 className="mt-2 text-2xl font-black">Do not reveal account state</h2><div className="mt-5 space-y-3">{["Generic request responses to prevent account enumeration", "Short-lived, one-time, hashed, scoped, and rate-limited tokens", "No plaintext secrets in logs, URLs, analytics, screenshots, or support tools", "Session invalidation, MFA/recovery policy, abuse monitoring, and user notifications"].map((item) => <div key={item} className="flex items-start gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="mt-0.5 size-4 text-cyan-300" /><span className="text-sm leading-5 text-slate-300">{item}</span></div>)}</div><div className="mt-5 rounded-xl border border-white/10 p-4"><p className="text-xs text-slate-500">Current local state</p><p className="mt-2 font-semibold text-amber-200">{status}</p><p className="mt-2 text-sm text-slate-500">No server request or credential mutation.</p></div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Production checklist</p><h2 className="mt-2 text-2xl font-black">What recovery must prove</h2><div className="mt-5 space-y-3">{["Verified email provider, generic responses, abuse/rate limits, and delivery monitoring", "Secure random token generation, hashing, expiry, one-time use, binding, and replay protection", "Password policy, server-side hashing, session rotation, MFA, and notification controls", "Audit-safe logs without email/token/password leakage and clear failure handling", "Privacy, support, incident response, and account-enumeration testing"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><WifiOff className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake recovery</h2><p className="mt-3 text-sm leading-6 text-slate-400">Moving through local steps does not send an email, verify a token, update a password, change a session, or prove an account exists.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Three steps preserved", description: "Request, verify, and set-password stages are interactive local states.", icon: KeyRound, status: "Verified locally" }, { title: "Enumeration is avoided", description: "The preview never confirms whether an account or email is registered.", icon: ShieldCheck, status: "Guardrail" }, { title: "Mutation is off", description: "No message, token, password, session, or account update is fabricated.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
