import { useMemo, useState } from "react";
import { Award, CheckCircle2, ChevronRight, Clock3, FileCheck2, Languages, LockKeyhole, Milestone, Send, ShieldCheck, Sparkles, Target, Timer, Trophy, WalletCards, WifiOff, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Textarea } from "@/components/ui/textarea";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

type Difficulty = "Easy" | "Medium" | "Hard";
type Bounty = { id: string; title: string; source: string; target: string; words: number; difficulty: Difficulty; deadline: string; description: string; qualifications: string[]; rewardLabel: string; status: "Open preview" | "Accepted locally" };
const INITIAL_BOUNTIES: Bounty[] = [
  { id: "b-1", title: "AI blog localization", source: "English", target: "Chinese", words: 1200, difficulty: "Medium", deadline: "7 days", description: "Translate a synthetic technical article with terminology and source-context requirements.", qualifications: ["B2 English", "B1 Chinese", "Technical vocabulary"], rewardLabel: "Terms unavailable", status: "Open preview" },
  { id: "b-2", title: "Product documentation pass", source: "English", target: "Spanish", words: 2500, difficulty: "Hard", deadline: "14 days", description: "Prepare a synthetic product guide for linguistic review, consistency, and link preservation.", qualifications: ["C1 English", "B2 Spanish", "Business terminology"], rewardLabel: "Terms unavailable", status: "Open preview" },
  { id: "b-3", title: "Tutorial subtitle review", source: "English", target: "German", words: 800, difficulty: "Easy", deadline: "5 days", description: "Review a synthetic subtitle track for timing notes, clarity, and terminology consistency.", qualifications: ["B2 English", "B1 German"], rewardLabel: "Terms unavailable", status: "Open preview" },
];
const COMPLETED_PREVIEWS = [{ title: "Synthetic tech article", date: "Preview record", rating: "Not verified", evidence: "No payout or XP record" }, { title: "Synthetic marketing email", date: "Preview record", rating: "Not verified", evidence: "No payout or XP record" }];

export default function BountySystem() { const [bounties, setBounties] = useState(INITIAL_BOUNTIES); const [tab, setTab] = useState<"available" | "accepted" | "completed">("available"); const [selected, setSelected] = useState<Bounty | null>(null); const [translation, setTranslation] = useState(""); const [notes, setNotes] = useState(""); const [submitted, setSubmitted] = useState(false); const accepted = bounties.filter((bounty) => bounty.status === "Accepted locally"); const visible = tab === "available" ? bounties.filter((bounty) => bounty.status === "Open preview") : tab === "accepted" ? accepted : []; const canSubmit = translation.trim().length > 20; const accept = (bounty: Bounty) => { setBounties((current) => current.map((item) => item.id === bounty.id ? { ...item, status: "Accepted locally" } : item)); setSelected({ ...bounty, status: "Accepted locally" }); setSubmitted(false); }; const submit = () => { if (!canSubmit) return; setSubmitted(true); }; const difficultyClass = (difficulty: Difficulty) => difficulty === "Hard" ? "border-rose-300/20 text-rose-200" : difficulty === "Medium" ? "border-amber-300/20 text-amber-200" : "border-emerald-300/20 text-emerald-200"; return <div className="min-h-screen bg-[#080712] text-white"><ScreenHero icon={Target} eyebrow="Contributor Missions · Translation" title="Choose a mission, show your work, and verify the terms." description="Explore synthetic translation bounty briefs, qualification gates, local acceptance, and submission preparation. No bounty, payout, XP, rating, ranking, or completion is treated as real without a verified backend record." badge="Preview mission board"><div className="flex flex-wrap gap-2"><Button onClick={() => setTab("available")} className="bg-violet-300 text-slate-950 hover:bg-violet-200"><Target className="mr-2 size-4" />Explore missions</Button><Button onClick={() => setTab("completed")} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><Trophy className="mr-2 size-4" />Review history</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Open missions", value: String(bounties.filter((bounty) => bounty.status === "Open preview").length), hint: "Synthetic briefs", icon: Target }, { label: "Accepted", value: String(accepted.length), hint: "Local only", icon: Milestone, tone: "violet" }, { label: "Payout status", value: "Unverified", hint: "No settlement rail", icon: WalletCards, tone: "amber" }, { label: "Contributor rating", value: "Unavailable", hint: "No review record", icon: Award, tone: "slate" }]} /><ScreenPreviewBanner title="Mission and reward evidence boundary">The mission briefs, language pairs, word counts, deadlines, and qualifications below are synthetic previews. Rewards are intentionally shown as unavailable. A real bounty system must verify the sponsor, scope, acceptance criteria, anti-fraud controls, reviewer identity, submission hash, payout terms, tax/identity requirements, dispute policy, and final settlement before showing completion or earnings.</ScreenPreviewBanner><div className="flex flex-wrap gap-2 rounded-2xl border border-white/10 bg-white/[0.03] p-2">{[{ id: "available", label: "Available missions", count: bounties.filter((bounty) => bounty.status === "Open preview").length }, { id: "accepted", label: "Accepted locally", count: accepted.length }, { id: "completed", label: "Completed history", count: COMPLETED_PREVIEWS.length }].map((item) => <button key={item.id} onClick={() => setTab(item.id as typeof tab)} className={`rounded-xl px-4 py-3 text-sm transition ${tab === item.id ? "bg-violet-300/15 text-violet-200" : "text-slate-500 hover:text-white"}`}>{item.label} <span className="ml-2 text-xs text-slate-500">{item.count}</span></button>)}</div><section className="grid gap-6 lg:grid-cols-[1.15fr_0.85fr]"><div className="space-y-4">{tab === "completed" ? COMPLETED_PREVIEWS.map((item) => <Card key={item.title} className="border-white/10 bg-white/[0.04]"><CardContent className="flex items-center gap-4 p-5"><CheckCircle2 className="size-5 text-slate-500" /><div className="flex-1"><p className="font-semibold">{item.title}</p><p className="mt-1 text-sm text-slate-500">{item.date} · {item.evidence}</p></div><Badge variant="outline" className="border-slate-300/15 text-slate-400">{item.rating}</Badge></CardContent></Card>) : visible.length ? visible.map((bounty) => <button key={bounty.id} onClick={() => setSelected(bounty)} className={`w-full rounded-2xl border p-5 text-left transition ${selected?.id === bounty.id ? "border-violet-300/40 bg-violet-300/[0.08]" : "border-white/10 bg-white/[0.04] hover:bg-white/[0.07]"}`}><div className="flex items-start justify-between gap-4"><div><div className="flex flex-wrap items-center gap-2"><Badge variant="outline" className={difficultyClass(bounty.difficulty)}>{bounty.difficulty}</Badge><span className="text-xs uppercase tracking-wider text-slate-500">{bounty.source} → {bounty.target}</span></div><h2 className="mt-3 text-xl font-bold">{bounty.title}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{bounty.description}</p></div><ChevronRight className="mt-1 size-5 shrink-0 text-slate-500" /></div><div className="mt-5 flex flex-wrap items-center gap-4 border-t border-white/10 pt-4 text-xs text-slate-500"><span className="flex items-center gap-1"><Languages className="size-3.5" />{bounty.words} words</span><span className="flex items-center gap-1"><Clock3 className="size-3.5" />{bounty.deadline}</span><span className="flex items-center gap-1"><WalletCards className="size-3.5" />{bounty.rewardLabel}</span><span className="ml-auto">{bounty.status}</span></div></button>) : <ScreenStatePanel type="empty" title="No local missions in this view" description="Accept a synthetic mission or review the preview history tab." />}</div><div className="space-y-6">{selected ? <Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-300">Mission brief</p><h2 className="mt-2 text-2xl font-black">{selected.title}</h2><p className="mt-2 text-sm text-slate-400">{selected.source} → {selected.target} · {selected.words} words</p></div><Badge variant="outline" className={selected.status === "Accepted locally" ? "border-violet-300/25 text-violet-200" : "border-amber-300/20 text-amber-200"}>{selected.status}</Badge></div><p className="mt-5 text-sm leading-6 text-slate-300">{selected.description}</p><div className="mt-5 flex flex-wrap gap-2">{selected.qualifications.map((qualification) => <Badge key={qualification} variant="outline" className="border-white/15 text-slate-300">{qualification}</Badge>)}</div>{selected.status === "Open preview" ? <Button onClick={() => accept(selected)} className="mt-6 w-full bg-violet-300 text-slate-950 hover:bg-violet-200"><Milestone className="mr-2 size-4" />Accept locally</Button> : <div className="mt-6 space-y-4"><div><label className="mb-2 block text-sm font-medium text-slate-300" htmlFor="translation">Submission draft</label><Textarea id="translation" value={translation} onChange={(event) => { setTranslation(event.target.value); setSubmitted(false); }} placeholder="Enter a local translation draft..." className="min-h-32 border-white/10 bg-black/20 text-white placeholder:text-slate-500" /></div><div><label className="mb-2 block text-sm font-medium text-slate-300" htmlFor="notes">Reviewer notes</label><Textarea id="notes" value={notes} onChange={(event) => { setNotes(event.target.value); setSubmitted(false); }} placeholder="Add terminology or context notes..." className="min-h-20 border-white/10 bg-black/20 text-white placeholder:text-slate-500" /></div><Button onClick={submit} disabled={!canSubmit} className="w-full bg-violet-300 text-slate-950 hover:bg-violet-200"><Send className="mr-2 size-4" />Prepare submission</Button>{submitted && <div className="rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex items-center gap-2 font-medium text-amber-200"><WifiOff className="size-4" />Submission prepared locally, not submitted</div><p className="mt-2 text-sm leading-6 text-slate-300">No reviewer was notified, no XP was issued, no bounty was completed, and no payout was created.</p></div>}</div>}</CardContent></Card> : <Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><Sparkles className="size-5 text-violet-300" /><p className="mt-4 text-lg font-semibold">Select a mission</p><p className="mt-2 text-sm leading-6 text-slate-400">Inspect language scope, qualification gates, terms status, and submission requirements.</p></CardContent></Card>}<Card className="border-amber-300/20 bg-amber-300/[0.06]"><CardContent className="p-5"><div className="flex items-center gap-2 font-medium text-amber-200"><LockKeyhole className="size-4" />Settlement not connected</div><p className="mt-2 text-sm leading-6 text-slate-300">This screen never reports earnings, rankings, XP, ratings, completion, or payout without a verified service record.</p></CardContent></Card></div></section><ScreenFeatureGrid features={[{ title: "Qualification gates", description: "Language pair, domain terminology, source context, and reviewer requirements stay visible before acceptance.", icon: ShieldCheck, status: "Ready" }, { title: "Submission evidence", description: "A real submission needs a durable record, content validation, reviewer identity, and a dispute-safe state transition.", icon: FileCheck2, status: "Required" }, { title: "Rewards need settlement proof", description: "Terms, payout rail, identity checks, tax requirements, and final receipt must be connected before showing earnings.", icon: WalletCards, status: "Guardrail" }]} /></main></div>; }
