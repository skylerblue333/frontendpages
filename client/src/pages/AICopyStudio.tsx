import { useMemo, useState } from "react";
import type { ComponentType } from "react";
import { BarChart3, BookOpen, CheckCircle2, Copy, FileText, Globe, History, Languages, Lightbulb, LockKeyhole, Mail, Megaphone, MousePointer, Newspaper, Search, Share2, Sparkles, Star, Tag, Wand2 } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { Textarea } from "@/components/ui/textarea";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

type CopyType = { id: string; label: string; description: string; icon: ComponentType<{ className?: string }> };
function HashIcon({ className }: { className?: string }) { return <span className={className}>#</span>; }
const COPY_TYPES: CopyType[] = [
  { id: "social", label: "Social post", description: "A platform-ready post brief", icon: HashIcon },
  { id: "ad", label: "Ad headline", description: "A/B headline directions", icon: Megaphone },
  { id: "product", label: "Product description", description: "Benefits and proof points", icon: Tag },
  { id: "email", label: "Email copy", description: "Subject and body structure", icon: Mail },
  { id: "seo", label: "SEO metadata", description: "Title and description options", icon: Globe },
  { id: "blog", label: "Blog intro", description: "Hook and value preview", icon: BookOpen },
  { id: "cta", label: "CTA buttons", description: "Action language variations", icon: MousePointer },
  { id: "press", label: "Press release", description: "Announcement structure", icon: Newspaper },
];
const TONES = ["Professional", "Casual", "Friendly", "Bold", "Technical", "Luxury"];
const LANGUAGES = ["Spanish", "French", "German", "Japanese", "Portuguese"];
const TEMPLATE_NAMES = ["Product launch", "Creator announcement", "Course promotion", "Community update", "Technical changelog", "Newsletter opener"];

function CopyButton({ text }: { text: string }) { const [copied, setCopied] = useState(false); return <Button size="sm" variant="outline" onClick={() => { void navigator.clipboard?.writeText(text); setCopied(true); setTimeout(() => setCopied(false), 1400); }} className="border-white/15 bg-white/5 text-slate-300 hover:bg-white/10">{copied ? <CheckCircle2 className="mr-2 size-3.5 text-emerald-300" /> : <Copy className="mr-2 size-3.5" />}{copied ? "Copied" : "Copy preview"}</Button>; }

export default function AICopyStudio() {
  const [copyType, setCopyType] = useState("social"); const [tone, setTone] = useState("Professional"); const [topic, setTopic] = useState(""); const [keywords, setKeywords] = useState(""); const [prepared, setPrepared] = useState(false); const [improveText, setImproveText] = useState(""); const [improveGoal, setImproveGoal] = useState("More engaging"); const [analysisText, setAnalysisText] = useState(""); const [analysisPrepared, setAnalysisPrepared] = useState(false); const [translateText, setTranslateText] = useState(""); const [targetLanguage, setTargetLanguage] = useState("Spanish"); const [history, setHistory] = useState<string[]>([]); const [templateQuery, setTemplateQuery] = useState("");
  const selected = COPY_TYPES.find((item) => item.id === copyType) ?? COPY_TYPES[0]; const TypeIcon = selected.icon; const templates = useMemo(() => TEMPLATE_NAMES.filter((item) => item.toLowerCase().includes(templateQuery.toLowerCase())), [templateQuery]);
  const prepareCopy = () => { if (!topic.trim()) return; setPrepared(true); setHistory((current) => [`${selected.label}: ${topic.trim()}`, ...current.filter((item) => item !== `${selected.label}: ${topic.trim()}`)].slice(0, 8)); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Sparkles} eyebrow="AI & Automation · Copy Studio" title="Shape clearer words before they go public." description="Explore copy workflows for social, ads, products, email, SEO, blogs, calls to action, and announcements. This screen prepares reviewable briefs; it does not invent generated copy, conversion rates, translations, or performance scores." badge="Preview studio"><div className="flex flex-wrap gap-2"><Button onClick={() => document.getElementById("copy-tabs")?.scrollIntoView({ behavior: "smooth" })} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Sparkles className="mr-2 size-4" />Open copy studio</Button><Button variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><LockKeyhole className="mr-2 size-4" />Review boundaries</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Copy workflows", value: String(COPY_TYPES.length), hint: "Reviewable content formats", icon: FileText }, { label: "Tone presets", value: String(TONES.length), hint: "Selectable voice directions", icon: Wand2, tone: "violet" }, { label: "Draft history", value: String(history.length), hint: "Stored locally in this preview", icon: History, tone: "amber" }, { label: "Live generation", value: "Not connected", hint: "No output is fabricated", icon: LockKeyhole, tone: "slate" }]} /><ScreenPreviewBanner title="Copy claim boundary">The studio’s format selectors, tone controls, keyword input, improve flow, analysis flow, language selection, template search, and local history state are available for UX review. Generated copy, translation quality, conversion performance, SEO ranking, and A/B results appear only after a verified service responds.</ScreenPreviewBanner><section id="copy-tabs"><Tabs defaultValue="generate" className="space-y-4"><TabsList className="grid h-auto w-full grid-cols-3 gap-2 border border-white/10 bg-white/[0.04] p-2 sm:grid-cols-6"><TabsTrigger value="generate" className="gap-2 text-slate-300 data-[state=active]:bg-cyan-300 data-[state=active]:text-slate-950"><Sparkles className="size-4" />Generate</TabsTrigger><TabsTrigger value="improve" className="gap-2 text-slate-300 data-[state=active]:bg-violet-300 data-[state=active]:text-slate-950"><Wand2 className="size-4" />Improve</TabsTrigger><TabsTrigger value="analyze" className="gap-2 text-slate-300 data-[state=active]:bg-amber-300 data-[state=active]:text-slate-950"><BarChart3 className="size-4" />Analyze</TabsTrigger><TabsTrigger value="translate" className="gap-2 text-slate-300 data-[state=active]:bg-emerald-300 data-[state=active]:text-slate-950"><Languages className="size-4" />Translate</TabsTrigger><TabsTrigger value="templates" className="gap-2 text-slate-300 data-[state=active]:bg-pink-300 data-[state=active]:text-slate-950"><FileText className="size-4" />Templates</TabsTrigger><TabsTrigger value="history" className="gap-2 text-slate-300 data-[state=active]:bg-slate-300 data-[state=active]:text-slate-950"><History className="size-4" />History</TabsTrigger></TabsList><TabsContent value="generate"><div className="grid gap-5 lg:grid-cols-[0.75fr_1.25fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Format</p><h2 className="mt-1 text-xl font-bold">Choose a copy workflow</h2><div className="mt-4 grid grid-cols-2 gap-2">{COPY_TYPES.map((item) => { const Icon = item.icon; return <button key={item.id} onClick={() => { setCopyType(item.id); setPrepared(false); }} className={`rounded-xl border p-3 text-left transition ${copyType === item.id ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-black/15 hover:bg-white/[0.05]"}`}><Icon className="size-4 text-cyan-200" /><span className="mt-2 block text-sm font-medium">{item.label}</span><span className="mt-1 block text-xs text-slate-500">{item.description}</span></button>; })}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="flex items-center gap-3"><div className="flex size-10 items-center justify-center rounded-xl bg-cyan-300/10 text-cyan-200"><TypeIcon className="size-5" /></div><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Brief</p><h2 className="mt-1 text-xl font-bold">Prepare {selected.label.toLowerCase()}</h2></div></div><Input value={topic} onChange={(event) => { setTopic(event.target.value); setPrepared(false); }} placeholder="Topic, product, brand, or announcement" aria-label="Copy topic" className="mt-5 border-white/10 bg-black/20 text-white placeholder:text-slate-500" /><Input value={keywords} onChange={(event) => setKeywords(event.target.value)} placeholder="Keywords, comma separated (optional)" aria-label="Copy keywords" className="mt-3 border-white/10 bg-black/20 text-white placeholder:text-slate-500" /><div className="mt-4 flex flex-wrap gap-2">{TONES.map((item) => <Button key={item} size="sm" variant={tone === item ? "default" : "outline"} onClick={() => setTone(item)} className={tone === item ? "bg-cyan-300 text-slate-950 hover:bg-cyan-200" : "border-white/15 bg-white/5 text-slate-300 hover:bg-white/10"}>{item}</Button>)}</div><Button onClick={prepareCopy} disabled={!topic.trim()} className="mt-5 w-full bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Sparkles className="mr-2 size-4" />Prepare copy preview</Button>{prepared ? <div className="mt-5 space-y-3 rounded-xl border border-emerald-300/20 bg-emerald-300/[0.06] p-4"><div className="flex items-center gap-2 font-medium text-emerald-200"><CheckCircle2 className="size-4" />Brief prepared for review</div><p className="text-sm leading-6 text-slate-300">A {tone.toLowerCase()} {selected.label.toLowerCase()} brief for “{topic}” is ready. A connected copy service can generate the actual draft later; no text or performance claim is invented in this preview.</p><Badge variant="outline" className="border-white/15 text-slate-400">Keywords: {keywords || "None supplied"}</Badge></div> : <ScreenStatePanel type="empty" title="No copy brief yet" description="Enter a topic and choose a tone to prepare a bounded workflow." />}</CardContent></Card></div></TabsContent><TabsContent value="improve"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-300">Improve</p><h2 className="mt-1 text-xl font-bold">Refine an existing draft</h2><Textarea value={improveText} onChange={(event) => setImproveText(event.target.value)} placeholder="Paste copy to improve..." aria-label="Copy to improve" className="mt-5 min-h-44 border-white/10 bg-black/20 text-white placeholder:text-slate-500" /><div className="mt-4 flex flex-wrap gap-2">{["More engaging", "More concise", "More persuasive", "More professional", "Add CTA", "SEO optimize"].map((item) => <Button key={item} size="sm" variant={improveGoal === item ? "default" : "outline"} onClick={() => setImproveGoal(item)} className={improveGoal === item ? "bg-violet-300 text-slate-950 hover:bg-violet-200" : "border-white/15 bg-white/5 text-slate-300 hover:bg-white/10"}>{item}</Button>)}</div><Button disabled={!improveText.trim()} className="mt-5 bg-violet-300 text-slate-950 hover:bg-violet-200"><Wand2 className="mr-2 size-4" />Prepare improvement request</Button><p className="mt-3 text-xs text-slate-500">No revised copy is shown until a verified writing service responds.</p></CardContent></Card></TabsContent><TabsContent value="analyze"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-300">Analyze</p><h2 className="mt-1 text-xl font-bold">Review clarity and intent</h2><Textarea value={analysisText} onChange={(event) => { setAnalysisText(event.target.value); setAnalysisPrepared(false); }} placeholder="Paste copy for a structured review..." aria-label="Copy analysis text" className="mt-5 min-h-44 border-white/10 bg-black/20 text-white placeholder:text-slate-500" /><Button onClick={() => setAnalysisPrepared(Boolean(analysisText.trim()))} disabled={!analysisText.trim()} className="mt-5 bg-amber-300 text-slate-950 hover:bg-amber-200"><BarChart3 className="mr-2 size-4" />Prepare analysis request</Button>{analysisPrepared && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex items-center gap-2 font-medium text-amber-100"><CheckCircle2 className="size-4" />Analysis request prepared</div><p className="mt-2 text-sm leading-6 text-slate-300">A connected service can review clarity, tone, structure, and CTA alignment. No score or conversion prediction is fabricated.</p></div>}</CardContent></Card></TabsContent><TabsContent value="translate"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-emerald-300">Translate</p><h2 className="mt-1 text-xl font-bold">Prepare a localization request</h2><Textarea value={translateText} onChange={(event) => setTranslateText(event.target.value)} placeholder="Paste copy to localize..." aria-label="Copy translation text" className="mt-5 min-h-40 border-white/10 bg-black/20 text-white placeholder:text-slate-500" /><div className="mt-4 flex flex-wrap gap-2">{LANGUAGES.map((item) => <Button key={item} size="sm" variant={targetLanguage === item ? "default" : "outline"} onClick={() => setTargetLanguage(item)} className={targetLanguage === item ? "bg-emerald-300 text-slate-950 hover:bg-emerald-200" : "border-white/15 bg-white/5 text-slate-300 hover:bg-white/10"}>{item}</Button>)}</div><Button disabled={!translateText.trim()} className="mt-5 bg-emerald-300 text-slate-950 hover:bg-emerald-200"><Languages className="mr-2 size-4" />Prepare {targetLanguage} request</Button><p className="mt-3 text-xs text-slate-500">Translation output and quality checks require a verified localization service.</p></CardContent></Card></TabsContent><TabsContent value="templates"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-pink-300">Templates</p><h2 className="mt-1 text-xl font-bold">Start from a proven structure</h2></div><div className="relative w-full sm:max-w-xs"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><Input value={templateQuery} onChange={(event) => setTemplateQuery(event.target.value)} placeholder="Search templates" aria-label="Search copy templates" className="border-white/10 bg-black/20 pl-9 text-white placeholder:text-slate-500" /></div></div><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{templates.map((item) => <button key={item} onClick={() => { setTopic(item); setCopyType("social"); }} className="rounded-xl border border-white/10 bg-black/15 p-4 text-left transition hover:border-pink-300/30 hover:bg-white/[0.05]"><FileText className="size-5 text-pink-300" /><h3 className="mt-3 font-medium">{item}</h3><p className="mt-1 text-xs leading-5 text-slate-400">Preview structure with editable tone and topic.</p></button>)}</div></CardContent></Card></TabsContent><TabsContent value="history"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-slate-300">Local history</p><h2 className="mt-1 text-xl font-bold">Recent briefs</h2></div><Badge variant="outline" className="border-white/15 text-slate-400">Preview only</Badge></div>{history.length ? <div className="mt-5 space-y-2">{history.map((item, index) => <div key={`${item}-${index}`} className="flex items-center gap-3 rounded-lg border border-white/10 bg-black/15 p-3"><History className="size-4 text-slate-400" /><span className="text-sm text-slate-300">{item}</span><CopyButton text={item} /></div>)}</div> : <div className="mt-5"><ScreenStatePanel type="empty" title="No local briefs yet" description="Prepare a copy brief in the Generate tab and it will appear here for this preview session." /></div>}</CardContent></Card></TabsContent></Tabs></section><ScreenFeatureGrid features={[{ title: "Human review", description: "Keep copy inspectable before publishing, sending, or making performance claims.", icon: CheckCircle2, status: "Required" }, { title: "Tone and format control", description: "Choose format, voice, keywords, and workflow intent before requesting output.", icon: Wand2, status: "Available" }, { title: "No fake performance", description: "Conversion, SEO ranking, and translation quality stay unclaimed until evidence exists.", icon: LockKeyhole, status: "Guardrail" }]} /></main></div>;
}
