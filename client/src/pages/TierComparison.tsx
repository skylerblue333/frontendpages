import { useState } from "react";
import { CheckCircle2, GitCompareArrows, LockKeyhole, ShieldAlert } from "lucide-react";
import { Card } from "@/components/ui/card";
import { Button } from "@/components/ui/button";

const tiers = [
  { name: "Foundation", summary: "Core account and community capability concept", status: "Needs evidence", details: ["Account-scoped access", "Supported feature inventory", "Accessibility and support policy"] },
  { name: "Creator", summary: "Creator workflow and governed publishing concept", status: "Needs evidence", details: ["Creator identity and ownership", "Provider and usage limits", "Moderation and payout policy"] },
  { name: "Organization", summary: "Managed team and enterprise capability concept", status: "Blocked", details: ["Organization roles and controls", "Security and service ownership", "Contracts, billing, and audit"] },
];

export default function TierComparison() {
  const [selected, setSelected] = useState("Creator");
  const tier = tiers.find(item => item.name === selected) ?? tiers[0];
  return <main className="min-h-screen bg-gradient-to-br from-slate-950 via-slate-900 to-slate-800 p-6 text-white sm:p-10"><div data-ui-polish="batch-204" className="mx-auto max-w-6xl"><div className="flex items-center gap-4"><div className="flex size-12 items-center justify-center rounded-2xl bg-purple-300/10 text-purple-200"><GitCompareArrows className="size-6" /></div><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-purple-200/70">Capability governance</p><h1 className="text-3xl font-black sm:text-4xl">Tier comparison workspace</h1></div></div><div className="mt-8 rounded-3xl border border-amber-300/20 bg-amber-300/[0.05] p-6"><div className="flex items-start gap-3"><ShieldAlert className="mt-1 size-5 shrink-0 text-amber-200" /><p className="text-sm leading-6 text-slate-300"><strong className="text-amber-100">No commercial tier is active.</strong> This local comparison surface does not establish prices, savings, trial periods, entitlements, SLAs, payment, upgrades, or customer outcomes. Each capability requires provider and business evidence before publication.</p></div></div><div className="mt-8 grid gap-5 lg:grid-cols-3">{tiers.map(item => <button type="button" key={item.name} onClick={() => setSelected(item.name)} className={`rounded-2xl border p-6 text-left transition ${selected === item.name ? "border-purple-300/50 bg-purple-300/[0.08]" : "border-white/10 bg-white/[0.04] hover:border-white/20"}`}><span className="rounded-full border border-amber-300/20 px-2 py-1 text-xs text-amber-100">{item.status}</span><h2 className="mt-5 text-2xl font-black">{item.name}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{item.summary}</p><div className="mt-5 space-y-3">{item.details.map(detail => <div key={detail} className="flex items-start gap-2 text-sm text-slate-300"><CheckCircle2 className="mt-1 size-4 shrink-0 text-purple-200" />{detail}</div>)}</div><div className="mt-6 rounded-xl border border-dashed border-white/15 bg-black/20 p-4"><p className="text-xs uppercase tracking-widest text-slate-500">Commercial state</p><p className="mt-2 font-bold text-amber-200">Unconfigured</p></div></button>)}</div><section className="mt-8 grid gap-6 lg:grid-cols-[1.05fr_0.95fr]"><Card className="border-white/10 bg-white/[0.04] p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-purple-200">Selected tier</p><h2 className="mt-2 text-3xl font-black">{tier.name}</h2><p className="mt-3 text-sm leading-6 text-slate-400">{tier.summary}</p><div className="mt-6 grid gap-3 sm:grid-cols-3">{["Price", "Entitlement", "Support"].map(label => <div key={label} className="rounded-xl border border-white/10 p-4"><p className="text-xs text-slate-500">{label}</p><p className="mt-2 font-bold text-amber-200">Unavailable</p></div>)}</div><div className="mt-6 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Choose unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Start trial unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Contact sales unavailable</Button></div></Card><aside className="rounded-2xl border border-white/10 bg-white/[0.04] p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Publication gates</p><h2 className="mt-2 text-2xl font-black">Before comparing live tiers</h2><div className="mt-6 space-y-3">{["Feature availability and limitations", "Identity, entitlement, billing, tax, and refunds", "Accessibility, privacy, security, and support", "Provider SLA and incident ownership", "Pricing approval and legal/compliance review", "Rollback, correction, and customer communication"].map(item => <div key={item} className="flex items-start gap-3 rounded-xl border border-white/10 p-4 text-sm leading-6 text-slate-300"><LockKeyhole className="mt-1 size-4 shrink-0 text-amber-200" />{item}</div>)}</div></aside></section><div className="mt-8 text-center text-sm leading-6 text-slate-500"><strong className="text-amber-100">No pricing, tier, subscription, payment, or entitlement claim is made.</strong> This is a local capability-governance preview.</div></div></main>;
}
