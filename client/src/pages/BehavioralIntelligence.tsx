import { useMemo, useState } from "react";
import { AlertTriangle, Brain, CheckCircle2, EyeOff, Filter, HeartPulse, LockKeyhole, MessageSquare, Search, Shield, SlidersHorizontal, Target, Users, Waves, Zap } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

type Segment = { id: string; name: string; stage: string; evidence: string; risk: "Review required" | "Data unavailable" | "Scenario only"; behaviors: string[]; questions: string[] };
const SEGMENTS: Segment[] = [
  { id: "new", name: "New participant", stage: "Onboarding", evidence: "Example event schema", risk: "Data unavailable", behaviors: ["First-session experience", "Permission choices", "Feature discovery"], questions: ["Was consent clear?", "Which setup step blocked progress?", "What support path is available?"] },
  { id: "explorer", name: "Feature explorer", stage: "Discovery", evidence: "Synthetic scenario", risk: "Scenario only", behaviors: ["Tries multiple surfaces", "Compares workflows", "Requests guidance"], questions: ["Which explanation helped?", "Was the next action obvious?", "Can the user opt out?"] },
  { id: "returning", name: "Returning participant", stage: "Retention", evidence: "Example cohort design", risk: "Review required", behaviors: ["Returns to saved work", "Revisits settings", "Uses support"], questions: ["What changed since last visit?", "Is re-engagement consented?", "Are reminders proportional?"] },
  { id: "inactive", name: "Inactive participant", stage: "Re-engagement", evidence: "No live activity source", risk: "Data unavailable", behaviors: ["No recent event", "Unknown reason", "Privacy review needed"], questions: ["Is inactivity meaningful?", "Should outreach be offered?", "Can personal inference be avoided?"] },
];
const FACTORS = ["Event freshness", "Consent and purpose", "Sampling bias", "Missing context", "False-positive cost", "Human review"];

export default function BehavioralIntelligence() { const [query, setQuery] = useState(""); const [selected, setSelected] = useState(SEGMENTS[0]); const [activeView, setActiveView] = useState("segments"); const [note, setNote] = useState(false); const visible = useMemo(() => SEGMENTS.filter((segment) => `${segment.name} ${segment.stage} ${segment.evidence} ${segment.risk} ${segment.behaviors.join(" ")}`.toLowerCase().includes(query.toLowerCase())), [query]); return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Brain} eyebrow="Research · Behavioral Intelligence" title="Understand behavior without turning people into scores." description="Explore synthetic cohort archetypes, evidence dimensions, privacy questions, and human-review requirements. This page does not claim real users, churn predictions, sentiment, lifetime value, identity profiles, or automated interventions." badge="Privacy-safe research preview"><div className="flex flex-wrap gap-2"><Button onClick={() => setNote(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><MessageSquare className="mr-2 size-4" />Prepare local research note</Button><Button variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><EyeOff className="mr-2 size-4" />Privacy boundary</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Cohort archetypes", value: String(SEGMENTS.length), hint: "Synthetic research examples", icon: Users }, { label: "Live profile data", value: "Unavailable", hint: "No identity or event feed", icon: EyeOff, tone: "amber" }, { label: "Risk outputs", value: "Disabled", hint: "No churn or sentiment score", icon: HeartPulse, tone: "violet" }, { label: "Privacy checks", value: String(FACTORS.length), hint: "Evidence before inference", icon: Shield, tone: "slate" }]} /><ScreenPreviewBanner title="Behavioral-intelligence evidence boundary">Synthetic segments, search, selected detail, explainable research questions, privacy checks, local note preparation, and unavailable live analytics are available for UX review. No user count, risk score, LTV, sentiment, prediction accuracy, retention result, or automated campaign is fabricated.</ScreenPreviewBanner><section><div className="mb-5 flex flex-wrap gap-2 rounded-xl border border-white/10 bg-white/[0.04] p-2">{[["segments", "Cohort archetypes"], ["factors", "Risk factors"], ["insights", "Research prompts"]].map(([value, label]) => <Button key={value} size="sm" variant={activeView === value ? "default" : "outline"} onClick={() => setActiveView(value)} className={activeView === value ? "bg-cyan-300 text-slate-950 hover:bg-cyan-200" : "border-white/15 bg-white/5 text-slate-300 hover:bg-white/10"}>{label}</Button>)}</div>{activeView === "segments" && <div className="grid gap-6 lg:grid-cols-[0.85fr_1.15fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="relative"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search cohort archetypes" aria-label="Search cohort archetypes" className="border-white/10 bg-black/20 pl-9 text-white placeholder:text-slate-500" /></div><div className="mt-4 space-y-2">{visible.map((segment) => <button key={segment.id} onClick={() => { setSelected(segment); setNote(false); }} className={`w-full rounded-xl border p-4 text-left transition ${selected.id === segment.id ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-black/15 hover:bg-white/[0.05]"}`}><div className="flex items-center gap-2"><Users className="size-4 text-cyan-300" /><span className="font-semibold">{segment.name}</span><Badge variant="outline" className="ml-auto border-amber-300/20 text-amber-200">{segment.risk}</Badge></div><p className="mt-2 text-xs uppercase tracking-wider text-slate-500">{segment.stage} · {segment.evidence}</p><div className="mt-3 flex flex-wrap gap-1.5">{segment.behaviors.map((behavior) => <Badge key={behavior} variant="outline" className="border-white/10 text-slate-400">{behavior}</Badge>)}</div></button>)}{visible.length === 0 && <ScreenStatePanel type="empty" title="No archetypes match" description="Try another stage, evidence label, or behavior." />}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Selected archetype</p><h2 className="mt-1 text-2xl font-bold">{selected.name}</h2><p className="mt-2 text-sm text-slate-400">{selected.stage} · {selected.evidence}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{selected.risk}</Badge></div><div className="mt-6 rounded-xl border border-violet-300/20 bg-violet-300/[0.06] p-4"><div className="flex items-center gap-2 font-medium text-violet-100"><Brain className="size-4" />Explainable research only</div><p className="mt-2 text-sm leading-6 text-slate-300">This archetype is a design aid, not a person, identity segment, prediction, or production cohort.</p></div><h3 className="mt-6 text-sm font-semibold uppercase tracking-wider text-slate-400">Research questions</h3><div className="mt-3 space-y-3">{selected.questions.map((question) => <div key={question} className="flex items-center gap-3 rounded-lg border border-white/10 bg-black/15 p-4 text-sm text-slate-300"><Target className="size-4 text-cyan-300" />{question}</div>)}</div>{note && <div className="mt-5 rounded-xl border border-cyan-300/20 bg-cyan-300/[0.06] p-4"><div className="flex items-center gap-2 font-medium text-cyan-200"><SparklesIcon />Local research note prepared</div><p className="mt-2 text-sm leading-6 text-slate-300">No profile, score, campaign, notification, or user record was created.</p></div>}</CardContent></Card></div>}{activeView === "factors" && <Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><SlidersHorizontal className="size-5 text-cyan-300" /><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Risk factors</p><h2 className="mt-1 text-2xl font-bold">Review context before making an inference</h2></div></div><div className="mt-6 grid gap-3 md:grid-cols-3">{FACTORS.map((factor) => <div key={factor} className="rounded-xl border border-white/10 bg-black/15 p-4"><CheckCircle2 className="size-4 text-emerald-300" /><p className="mt-3 font-medium text-slate-200">{factor}</p><p className="mt-2 text-sm leading-6 text-slate-400">Evidence and human review required; no model weight is claimed.</p></div>)}</div></CardContent></Card>}{activeView === "insights" && <Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><Waves className="size-5 text-violet-300" /><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-300">Research prompts</p><h2 className="mt-1 text-2xl font-bold">Ask questions before you score people</h2></div></div><div className="mt-6 grid gap-3 md:grid-cols-3">{["What evidence is missing?", "Who could be harmed by this inference?", "Can the user understand and contest it?"].map((prompt) => <div key={prompt} className="rounded-xl border border-white/10 bg-black/15 p-5"><Zap className="size-4 text-amber-300" /><p className="mt-4 font-medium text-slate-200">{prompt}</p><p className="mt-2 text-sm leading-6 text-slate-400">Prompt for a review session; no actionable recommendation is implied.</p></div>)}</div></CardContent></Card>}</section><ScreenFeatureGrid features={[{ title: "Consent before inference", description: "Behavioral analysis needs purpose, lawful basis, data minimization, access scope, and a user-facing explanation.", icon: Shield, status: "Required" }, { title: "No sensitive scoring", description: "Do not infer identity, health, finances, protected traits, or intent from thin behavioral signals.", icon: LockKeyhole, status: "Required" }, { title: "No fake intelligence", description: "Synthetic archetypes and questions are research scaffolding, not users, predictions, sentiment, or campaigns.", icon: AlertTriangle, status: "Guardrail" }]} /></main></div>; }
function SparklesIcon() { return <span aria-hidden="true">✦</span>; }
