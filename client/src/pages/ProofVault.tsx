import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Archive, Check, Clock3, Download, FileCheck2, FileText, Fingerprint, LockKeyhole, RefreshCw, Search, ShieldAlert, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
type Proof = { id: number; title: string; type: string; source: string; status: string; detail: string };
const initial: Proof[] = [{ id: 1, title: "Architecture decision note", type: "Design evidence", source: "Local draft", status: "Unverified", detail: "A design record awaiting source links, reviewer, and version history." }, { id: 2, title: "Accessibility review checklist", type: "Review evidence", source: "Browser worksheet", status: "Unverified", detail: "A checklist concept with no audit signature or compliance conclusion." }, { id: 3, title: "Integration boundary note", type: "Technical evidence", source: "Local draft", status: "Needs source", detail: "An evidence requirement map without external system verification." }];
export default function ProofVault() {
  const [proofs, setProofs] = useState(initial);
  const [query, setQuery] = useState("");
  const [selected, setSelected] = useState(1);
  const [saved, setSaved] = useState(false);
  const [showGates, setShowGates] = useState(false);
  const filtered = useMemo(() => proofs.filter((proof) => `${proof.title} ${proof.type} ${proof.detail}`.toLowerCase().includes(query.toLowerCase())), [proofs, query]);
  const selectedProof = proofs.find((proof) => proof.id === selected) ?? proofs[0];
  const create = () => { const id = Math.max(...proofs.map((proof) => proof.id), 0) + 1; const item: Proof = { id, title: `Local evidence item ${id}`, type: "Unclassified", source: "Not connected", status: "Draft", detail: "A local proof concept awaiting evidence definition and governance." }; setProofs((current) => [...current, item]); setSelected(id); };
  const reset = () => { setProofs(initial); setQuery(""); setSelected(1); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={FileCheck2} eyebrow="ProofVault · Evidence registry preview" title="Store the question before claiming the proof." description="Explore a local evidence registry with proof types, sources, verification states, timestamp intent, custody requirements, privacy, export, and audit gates. No signatures, storage, identity, legal admissibility, certification, or operational attestation is produced." badge="Proof workspace"><div className="flex flex-wrap gap-2"><Button onClick={create} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><FileText className="mr-2 size-4" />New local proof</Button><Button onClick={() => setSaved(true)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><Check className="mr-2 size-4" />{saved ? "Registry saved locally" : "Save registry locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Hide gates" : "Review proof gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset vault</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Proofs", value: `${proofs.length} local`, hint: "Not persisted", icon: FileCheck2, tone: "cyan" }, { label: "Verified", value: "Zero", hint: "No reviewer source", icon: Fingerprint, tone: "violet" }, { label: "Custody", value: "Unconfigured", hint: "No storage proof", icon: Archive, tone: "amber" }, { label: "Export", value: "Blocked", hint: "No rights service", icon: Download, tone: "slate" }]} /><ScreenPreviewBanner title="Proof evidence boundary"><strong>This is a local proof-registry preview, not a vault or legal evidence service.</strong> Proof cards, sources, status, timestamp intent, selected detail, and saved state are browser concepts. No file storage, hash, signature, identity, timestamp authority, chain of custody, reviewer, certification, compliance, legal admissibility, export, or deletion is asserted.</ScreenPreviewBanner><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Search className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local evidence items" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-6 grid gap-4 md:grid-cols-3">{filtered.map((proof) => <button key={proof.id} onClick={() => setSelected(proof.id)} className={`rounded-2xl border p-5 text-left ${selected === proof.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><h2 className="font-black">{proof.title}</h2><Badge variant="outline" className="border-amber-300/20 text-amber-200">{proof.status}</Badge></div><p className="mt-3 text-sm leading-6 text-slate-400">{proof.detail}</p><div className="mt-5 flex flex-wrap gap-2"><Badge variant="outline" className="border-white/10 text-slate-500">{proof.type}</Badge><Badge variant="outline" className="border-white/10 text-slate-500">{proof.source}</Badge></div></button>)}{filtered.length === 0 && <div className="col-span-full rounded-2xl border border-dashed border-white/10 p-12 text-center text-slate-500">No local proof items match this search.</div>}</div></CardContent></Card><section className="grid gap-6 lg:grid-cols-[0.9fr_1.1fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected proof</p><h2 className="mt-2 text-2xl font-black">{selectedProof.title}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{selectedProof.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Type", value: selectedProof.type }, { label: "Source", value: selectedProof.source }, { label: "Verification", value: selectedProof.status }, { label: "Timestamp", value: "Not issued" }, { label: "Hash", value: "Not computed" }, { label: "Reviewer", value: "Unknown" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Verify unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Export unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No proof claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local card does not establish authenticity, authorship, timing, custody, integrity, admissibility, compliance, or security.</p></div>}</CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Proof gates</p><h2 className="mt-2 text-2xl font-black">What a real evidence service must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Authenticated submitter, reviewer, access controls, and consent", "Content hash, signature, timestamp authority, and immutable version", "Secure storage, retention, export, deletion, and recovery", "Chain of custody, audit trail, provenance, and tamper detection", "Verification method, source reliability, uncertainty, and scope", "Legal, security, compliance, and support review before attestation"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Proof surface preserved", description: "Evidence search, types, source, status, selected detail, timestamp/hash/reviewer posture, local creation, save/reset, and gates remain interactive.", icon: FileCheck2, status: "Local registry" }, { title: "No evidence theater", description: "Signatures, hashes, timestamps, custody, identity, certifications, compliance, admissibility, and security attestations are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Integrity before attestation", description: "Real proof needs controlled storage, provenance, review, privacy, legal scope, audit, recovery, and support.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
