import { useMemo, useState } from "react";
import { Activity, AlertTriangle, BarChart3, Brain, CheckCircle2, Eye, LockKeyhole, Minus, RefreshCw, Search, Shield, Sparkles, TrendingUp, Users, Zap } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

type Agent = { id: string; name: string; role: string; specialty: string; description: string; icon: string; tone: "cyan" | "violet" | "amber" | "emerald" | "slate" };
const AGENTS: Agent[] = [
  { id: "axiom", name: "Axiom", role: "Research", specialty: "Evidence briefs", description: "Organizes market questions into source-aware research plans.", icon: "◈", tone: "cyan" },
  { id: "vega", name: "Vega", role: "Risk", specialty: "Scenario mapping", description: "Frames assumptions, uncertainty, and risk questions without issuing trade instructions.", icon: "✦", tone: "violet" },
  { id: "pulse", name: "Pulse", role: "Signals", specialty: "Trend context", description: "Prepares a trend-context brief when an approved data source is available.", icon: "◌", tone: "amber" },
  { id: "oracle", name: "Oracle", role: "Analysis", specialty: "Explainability", description: "Turns model outputs into reviewable explanations and evidence checklists.", icon: "◎", tone: "emerald" },
  { id: "cipher", name: "Cipher", role: "Safety", specialty: "Guardrails", description: "Checks permissions, data boundaries, and high-impact action requirements.", icon: "◇", tone: "slate" },
];
const SIGNALS = ["Watchlist review prepared", "Data-source provenance needed", "Scenario comparison ready", "Human approval checkpoint required"];
const ACTIVITY = ["Axiom drafted a source checklist", "Cipher flagged an approval boundary", "Oracle prepared an explainability outline", "Pulse is waiting for a verified feed"];

export default function AIMarketAgents() { const [query, setQuery] = useState(""); const [selected, setSelected] = useState<Agent | null>(null); const [refreshed, setRefreshed] = useState(false); const filtered = useMemo(() => AGENTS.filter((agent) => `${agent.name} ${agent.role} ${agent.specialty} ${agent.description}`.toLowerCase().includes(query.toLowerCase())), [query]); return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Brain} eyebrow="AI & Automation · Market Agents" title="Market context you can inspect before acting." description="Explore five market-agent archetypes, their specialties, example signal states, and the evidence required before financial data or actions can be trusted. This workspace does not fabricate prices, investor counts, returns, APY, signals, or trading outcomes." badge="Preview intelligence lab"><div className="flex flex-wrap gap-2"><Button onClick={() => document.getElementById("market-agents")?.scrollIntoView({ behavior: "smooth" })} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Sparkles className="mr-2 size-4" />Explore agents</Button><Button onClick={() => setRefreshed(true)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />{refreshed ? "Boundary checked" : "Check data status"}</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Agent archetypes", value: String(AGENTS.length), hint: "Reviewable market roles", icon: Brain }, { label: "Signal examples", value: String(SIGNALS.length), hint: "Preview states only", icon: BarChart3, tone: "violet" }, { label: "Data feed", value: "Not connected", hint: "No live prices or signals", icon: LockKeyhole, tone: "amber" }, { label: "Execution", value: "Disabled", hint: "No trade or investor action", icon: Shield, tone: "slate" }]} /><ScreenPreviewBanner title="Financial claim boundary">The agent catalog, search, selection, example signal states, activity examples, data-status check, explainability notes, and safety guidance are available for UX review. Live market data, prices, token values, investor counts, returns, staking rewards, APY, and trading signals require verified sources and are not invented here.</ScreenPreviewBanner><section id="market-agents" className="space-y-5"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Agent roster</p><h2 className="mt-1 text-2xl font-bold">Five reviewable roles</h2><p className="mt-1 text-sm text-slate-400">Select an agent to inspect its intended responsibility.</p></div><div className="relative w-full sm:max-w-sm"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search agents" aria-label="Search market agents" className="border-white/10 bg-black/20 pl-9 text-white placeholder:text-slate-500" /></div></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-5">{filtered.map((agent) => <button key={agent.id} onClick={() => setSelected(agent)} className={`rounded-xl border p-4 text-left transition ${selected?.id === agent.id ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-white/[0.04] hover:bg-white/[0.07]"}`}><div className="flex items-center justify-between"><span className="flex size-11 items-center justify-center rounded-2xl bg-white/10 text-2xl text-cyan-200">{agent.icon}</span><Badge variant="outline" className="border-white/15 text-slate-400">{agent.role}</Badge></div><h3 className="mt-4 text-lg font-semibold">{agent.name}</h3><p className="mt-1 text-xs uppercase tracking-wider text-slate-500">{agent.specialty}</p><p className="mt-3 text-sm leading-6 text-slate-400">{agent.description}</p></button>)}</div>{filtered.length === 0 && <ScreenStatePanel type="empty" title="No agents match" description="Try another search term." />}</section>{selected && <Card className="border-cyan-300/20 bg-cyan-300/[0.04]" aria-live="polite"><CardContent className="p-5"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Agent detail</p><h2 className="mt-1 text-2xl font-bold">{selected.name} · {selected.specialty}</h2><p className="mt-2 text-sm leading-6 text-slate-300">{selected.description}</p></div><Button variant="ghost" size="icon" onClick={() => setSelected(null)} aria-label="Close agent detail" className="text-slate-400">×</Button></div><div className="mt-5 grid gap-4 md:grid-cols-3"><div className="rounded-xl border border-white/10 bg-black/15 p-4"><CheckCircle2 className="size-5 text-emerald-300" /><h3 className="mt-3 font-medium">Reviewable output</h3><p className="mt-1 text-sm leading-6 text-slate-400">A bounded brief or checklist can be prepared for human inspection.</p></div><div className="rounded-xl border border-white/10 bg-black/15 p-4"><AlertTriangle className="size-5 text-amber-300" /><h3 className="mt-3 font-medium">Uncertainty visible</h3><p className="mt-1 text-sm leading-6 text-slate-400">Assumptions and missing data stay visible rather than being converted into certainty.</p></div><div className="rounded-xl border border-white/10 bg-black/15 p-4"><LockKeyhole className="size-5 text-cyan-300" /><h3 className="mt-3 font-medium">No execution</h3><p className="mt-1 text-sm leading-6 text-slate-400">No order, transfer, recommendation, or financial side effect is triggered here.</p></div></div></CardContent></Card>}<section className="grid gap-6 lg:grid-cols-2"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-300">Signal board</p><h2 className="mt-1 text-xl font-bold">Example signals</h2></div><Badge variant="outline" className="border-violet-300/20 text-violet-200">Preview only</Badge></div><div className="mt-4 space-y-2">{SIGNALS.map((signal, index) => <div key={signal} className="flex items-center gap-3 rounded-lg border border-white/10 bg-black/15 p-3"><span className="flex size-8 items-center justify-center rounded-lg bg-violet-300/10 text-violet-200">{index % 3 === 0 ? <Eye className="size-4" /> : index % 3 === 1 ? <AlertTriangle className="size-4" /> : <Minus className="size-4" />}</span><span className="text-sm text-slate-300">{signal}</span><Badge variant="outline" className="ml-auto border-white/10 text-slate-500">No live data</Badge></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-emerald-300">Activity</p><h2 className="mt-1 text-xl font-bold">Example agent activity</h2></div><Badge variant="outline" className="border-emerald-300/20 text-emerald-200">Preview</Badge></div><div className="mt-4 space-y-2">{ACTIVITY.map((item) => <div key={item} className="flex items-center gap-3 rounded-lg border border-white/10 bg-black/15 p-3"><Activity className="size-4 text-emerald-300" /><span className="text-sm text-slate-300">{item}</span></div>)}</div></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Data provenance", description: "Show source, timestamp, freshness, and limitations before presenting a market conclusion.", icon: BarChart3, status: "Required" }, { title: "Human approval", description: "Keep financial actions reviewable and explicitly authorized.", icon: Users, status: "Required" }, { title: "Safety-first agents", description: "Use Cipher-style guardrails to prevent unsupported certainty, leakage, or execution.", icon: Shield, status: "Available" }]} /></main></div>; }
