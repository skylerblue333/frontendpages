import { PackageSearch, Search, ShieldCheck, SlidersHorizontal } from "lucide-react";
import { PageHeader } from "@/components/PageHeader";

export default function ProductSearchUnified() {
  return <div className="container max-w-6xl animate-page-in py-8"><PageHeader icon={PackageSearch} title="Unified Product Search" subtitle="Commerce discovery across the canonical storefront" /><div className="mb-8 flex flex-col gap-3 rounded-3xl border border-cyan-300/20 bg-cyan-300/[0.05] p-5 md:flex-row"><div className="flex flex-1 items-center gap-3 rounded-2xl border border-border/60 bg-black/10 px-4"><Search className="size-5 text-muted-foreground" /><span className="text-sm text-muted-foreground">Search verified catalog…</span></div><button type="button" className="rounded-2xl border border-border/60 px-4 py-3 text-sm"><SlidersHorizontal className="mr-2 inline size-4" />Filters</button></div><div className="mb-8 flex items-center gap-2 text-xs text-muted-foreground"><ShieldCheck className="size-4 text-emerald-300" />Results require an authoritative inventory, price, availability, and supplier source.</div><div className="grid gap-4 md:grid-cols-3">{["Catalog", "Supplier comparison", "Order readiness"].map(label => <div key={label} className="card p-5"><PackageSearch className="size-6 text-cyan-200" /><h3 className="mt-4 font-bold">{label}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">Unified frontend surface ready for verified commerce data.</p></div>)}</div><div className="mt-8 card p-6"><div className="flex min-h-56 items-center justify-center rounded-2xl border border-dashed border-border/60 text-center"><div><Search className="mx-auto size-9 text-muted-foreground/50" /><p className="mt-3 font-semibold">No verified products loaded</p><p className="mt-2 max-w-md text-sm text-muted-foreground">No supplier, price, stock, shipping, or margin data is invented.</p></div></div></div></div>;
}
