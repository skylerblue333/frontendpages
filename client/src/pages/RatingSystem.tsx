import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { BarChart3, Check, Filter, LockKeyhole, MessageSquare, RefreshCw, Search, ShieldAlert, Star, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const surfaces = [{ id: 1, name: "Product experience", category: "General", detail: "A local rating surface for an experience, feature, or service." }, { id: 2, name: "Learning module", category: "Education", detail: "A course-feedback surface awaiting learner identity and moderation policy." }, { id: 3, name: "Community contribution", category: "Community", detail: "A contribution-rating concept that must prevent brigading and retaliation." }, { id: 4, name: "Support interaction", category: "Operations", detail: "A support-feedback concept requiring case linkage, privacy, and appeals." }];
export default function RatingSystem() {
  const [query, setQuery] = useState("");
  const [category, setCategory] = useState("All");
  const [selected, setSelected] = useState(1);
  const [scale, setScale] = useState("Scale not configured");
  const [moderation, setModeration] = useState("Moderation not configured");
  const [saved, setSaved] = useState(false);
  const [showGates, setShowGates] = useState(false);
  const categories = ["All", ...Array.from(new Set(surfaces.map((item) => item.category)))];
  const filtered = useMemo(() => surfaces.filter((item) => (category === "All" || item.category === category) && `${item.name} ${item.category} ${item.detail}`.toLowerCase().includes(query.toLowerCase())), [category, query]);
  const surface = surfaces.find((item) => item.id === selected) ?? surfaces[0];
  const reset = () => { setQuery(""); setCategory("All"); setSelected(1); setScale("Scale not configured"); setModeration("Moderation not configured"); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Star} eyebrow="RatingSystem · Feedback preview" title="Define the review before displaying the score." description="Explore local rating surfaces with search, categories, star-scale intent, criteria, moderation, aggregates, sorting, accessibility, reporting, save, reset, and evidence gates. No users, reviews, ratings, averages, rankings, or trust conclusions are connected." badge="Rating workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "View saved locally" : "Save design locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review rating gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset design</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Surfaces", value: `${surfaces.length} local`, hint: "No reviews loaded", icon: Star, tone: "cyan" }, { label: "Average", value: "Unavailable", hint: "No rating source", icon: BarChart3, tone: "violet" }, { label: "Reviews", value: "Unknown", hint: "No user source", icon: MessageSquare, tone: "amber" }, { label: "Moderation", value: moderation, hint: "Browser intent", icon: LockKeyhole, tone: "slate" }]} /><ScreenPreviewBanner title="Rating evidence boundary"><strong>This is a local feedback and rating-design preview, not a trust or reputation report.</strong> Rating surfaces, criteria, scale and moderation intent, empty aggregate states, saved state, and evidence gates are browser concepts. No user, review, score, average, ranking, sentiment, quality, popularity, trust, safety, or business conclusion is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Search className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local rating surfaces" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-4 flex flex-wrap gap-2">{categories.map((entry) => <Button key={entry} onClick={() => setCategory(entry)} size="sm" variant="outline" className={category === entry ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-100" : "border-white/10 text-slate-400"}><Filter className="mr-1 size-3" />{entry}</Button>)}</div><div className="mt-6 space-y-3">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{item.name}</p><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Unrated</Badge></div><div className="mt-4"><Badge variant="outline" className="border-white/10 text-slate-500">{item.category}</Badge></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected rating surface</p><h2 className="mt-2 text-2xl font-black">{surface.name}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{surface.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Category", value: surface.category }, { label: "Scale", value: scale }, { label: "Average", value: "Unavailable" }, { label: "Review count", value: "Unknown" }, { label: "Moderation", value: moderation }, { label: "Ranking", value: "Not configured" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2"><label className="text-sm text-slate-400">Rating scale<select value={scale} onChange={(event) => setScale(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Scale not configured</option><option>5-star intent</option><option>10-point intent</option><option>Thumbs intent</option></select></label><label className="text-sm text-slate-400">Moderation posture<select value={moderation} onChange={(event) => setModeration(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Moderation not configured</option><option>Pre-publish review intent</option><option>Post-publish report intent</option><option>Appeal workflow intent</option></select></label></div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-8 text-center"><Star className="mx-auto size-7 text-slate-600" /><p className="mt-3 font-semibold">No rating data loaded</p><p className="mt-2 text-sm text-slate-500">Connect an authenticated review source before displaying scores, averages, rankings, or recommendations.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Submit rating unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Publish review unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Report unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No trust claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A rating scale or empty review surface does not establish quality, popularity, safety, sentiment, reputation, or user satisfaction.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Rating gates</p><h2 className="mt-2 text-2xl font-black">What a real feedback system must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Reviewer identity, eligibility, consent, anti-abuse controls, privacy, and one-review policy", "Scale, criteria, labels, accessibility, localization, partial completion, and edit rules", "Moderation, reports, appeals, retaliation prevention, fraud detection, and immutable audit", "Aggregation, weighting, Bayesian or confidence rules, missing data, outliers, and time decay", "Sorting, ranking, search, recommendations, exposure bias, personalization, and safety", "Exports, deletion, retention, legal requests, support, incident response, and rollback"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Rating surface preserved", description: "Rating surfaces, search, categories, scale, criteria, moderation, aggregates, sorting, accessibility, reporting, save/reset, and gates remain interactive.", icon: Star, status: "Local feedback" }, { title: "No reputation theater", description: "Users, reviews, scores, averages, rankings, sentiment, quality, popularity, trust, safety, and satisfaction are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Governance before score", description: "Real ratings need reviewer controls, moderation, aggregation policy, anti-abuse systems, privacy, and appeals.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
