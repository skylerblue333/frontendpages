import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Check, Clock3, Eye, Filter, LockKeyhole, MessageSquare, RefreshCw, Search, ShieldAlert, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const threads = [{ id: 1, title: "Workspace handoff", participant: "Participant unavailable", state: "Unread intent", detail: "A local thread concept where read state must be explicit and privacy-aware." }, { id: 2, title: "Course discussion", participant: "Learner identity unknown", state: "Read intent", detail: "A learning-message concept awaiting delivery, identity, and timestamp evidence." }, { id: 3, title: "Support follow-up", participant: "Agent identity unknown", state: "Receipt blocked", detail: "A support thread concept requiring case linkage, access controls, and audit." }];
export default function ReadReceipts() {
  const [query, setQuery] = useState("");
  const [filter, setFilter] = useState("All");
  const [selected, setSelected] = useState(1);
  const [readState, setReadState] = useState("Read state not connected");
  const [saved, setSaved] = useState(false);
  const [showGates, setShowGates] = useState(false);
  const filters = ["All", "Unread intent", "Read intent", "Receipt blocked"];
  const filtered = useMemo(() => threads.filter((thread) => (filter === "All" || thread.state === filter) && `${thread.title} ${thread.participant} ${thread.detail}`.toLowerCase().includes(query.toLowerCase())), [filter, query]);
  const thread = threads.find((item) => item.id === selected) ?? threads[0];
  const reset = () => { setQuery(""); setFilter("All"); setSelected(1); setReadState("Read state not connected"); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Eye} eyebrow="ReadReceipts · Messaging preview" title="Define the receipt before exposing the state." description="Explore local message-thread concepts with search, read/unread filters, participant posture, timestamp states, privacy, mark-read intent, notification posture, save, reset, and audit gates. No live messages, users, delivery events, timestamps, or notifications are connected." badge="Messaging workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setReadState(readState === "Read state not connected" ? "Local read intent" : "Read state not connected")} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />Toggle local read intent</Button><Button onClick={() => setSaved(true)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{saved ? "View saved locally" : "Save view locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review receipt gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset view</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Threads", value: `${threads.length} local`, hint: "No messages loaded", icon: MessageSquare, tone: "cyan" }, { label: "Unread", value: "Unmeasured", hint: "No delivery source", icon: Eye, tone: "violet" }, { label: "Timestamps", value: "Unavailable", hint: "No event source", icon: Clock3, tone: "amber" }, { label: "Privacy", value: "Review needed", hint: "No identity source", icon: LockKeyhole, tone: "slate" }]} /><ScreenPreviewBanner title="Read-receipt evidence boundary"><strong>This is a local messaging-state preview, not evidence of message delivery or user activity.</strong> Thread labels, read/unread intent, participant posture, timestamp states, saved state, notification posture, and audit gates are browser concepts. No message, participant, delivery, read event, timestamp, notification, presence, consent, or privacy conclusion is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Search className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local threads" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-4 flex flex-wrap gap-2">{filters.map((entry) => <Button key={entry} onClick={() => setFilter(entry)} size="sm" variant="outline" className={filter === entry ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-100" : "border-white/10 text-slate-400"}><Filter className="mr-1 size-3" />{entry}</Button>)}</div><div className="mt-6 space-y-3">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{item.title}</p><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.state}</Badge></div><p className="mt-3 text-xs text-slate-500">{item.participant}</p></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected thread</p><h2 className="mt-2 text-2xl font-black">{thread.title}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{thread.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "State", value: readState }, { label: "Participant", value: thread.participant }, { label: "Last message", value: "Unavailable" }, { label: "Read event", value: "Not connected" }, { label: "Notification", value: "Not configured" }, { label: "Audit", value: "Not connected" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-8 text-center"><MessageSquare className="mx-auto size-7 text-slate-600" /><p className="mt-3 font-semibold">No message events loaded</p><p className="mt-2 text-sm text-slate-500">Connect a governed messaging source before showing content, recipients, delivery, or read timestamps.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Mark read unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Open thread unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Notify unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No delivery or presence claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A read-state label does not prove that a message was sent, delivered, opened, acknowledged, or seen by a person.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Receipt gates</p><h2 className="mt-2 text-2xl font-black">What a real messaging state must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Message identity, sender, recipient, thread, content access, consent, retention, and deletion policy", "Delivery semantics, retries, duplication, ordering, offline behavior, and transport acknowledgements", "Read-event semantics, device sync, privacy controls, opt-outs, shared devices, and auditability", "Timestamp source, clock, time zone, precision, edits, clock skew, and event provenance", "Notifications, push permissions, rate limits, deep links, support, and failure recovery", "Moderation, abuse reports, legal holds, export, security review, incident response, and rollback"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Messaging surface preserved", description: "Threads, search, read/unread filters, participant posture, timestamps, privacy, mark-read intent, notification posture, save/reset, and gates remain interactive.", icon: Eye, status: "Local receipts" }, { title: "No messaging theater", description: "Messages, users, delivery, read events, timestamps, notifications, presence, consent, and privacy conclusions are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Event provenance before state", description: "Real receipts need transport semantics, device sync, privacy, timestamps, audit, and support workflows.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
