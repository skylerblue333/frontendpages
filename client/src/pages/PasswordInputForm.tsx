import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Check, Eye, EyeOff, KeyRound, LockKeyhole, RefreshCw, ShieldCheck, TriangleAlert, WifiOff, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const requirements = [{ label: "At least 12 characters", test: (value: string) => value.length >= 12 }, { label: "Uppercase and lowercase letters", test: (value: string) => /[A-Z]/.test(value) && /[a-z]/.test(value) }, { label: "A number", test: (value: string) => /\d/.test(value) }, { label: "A symbol", test: (value: string) => /[^A-Za-z0-9]/.test(value) }];
export default function PasswordInputForm() {
  const [password, setPassword] = useState("");
  const [confirmation, setConfirmation] = useState("");
  const [visible, setVisible] = useState(false);
  const [confirmed, setConfirmed] = useState(false);
  const checks = useMemo(() => requirements.map((item) => ({ ...item, passed: item.test(password) })), [password]);
  const score = checks.filter((item) => item.passed).length;
  const matches = password.length > 0 && password === confirmation;
  const valid = score === requirements.length && matches;
  const reset = () => { setPassword(""); setConfirmation(""); setVisible(false); setConfirmed(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={KeyRound} eyebrow="PasswordInputForm · Credential UX" title="Make secret input clear without making secret claims." description="Exercise visibility, strength, matching, and validation states locally. This preview never stores, logs, transmits, hashes, recovers, or verifies a real credential." badge="Credential preview"><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset fields</Button></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Strength", value: `${score} / ${requirements.length}`, hint: password ? "Local criteria" : "Not entered", icon: ShieldCheck, tone: "cyan" }, { label: "Match", value: matches ? "Yes" : "No", hint: confirmation ? "Compared locally" : "Awaiting confirmation", icon: matches ? Check : X, tone: "violet" }, { label: "Visibility", value: visible ? "Shown" : "Hidden", hint: "Browser state", icon: visible ? Eye : EyeOff, tone: "amber" }, { label: "Submission", value: "Off", hint: "No auth request", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="Credential evidence boundary"><strong>Never enter a real password into this preview.</strong> The field values exist only in component memory for UX testing and are not sent to a server, saved to storage, written to logs, or used for authentication. Production credential flows need secure transport, server-side hashing, breach protection, session controls, recovery, rate limiting, and audit-safe handling.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1fr_0.85fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Local form</p><h2 className="mt-2 text-3xl font-black">Create a credential concept</h2></div><Badge variant="outline" className="border-white/10 text-slate-400">No request</Badge></div><div className="mt-6 space-y-4"><label className="block text-sm text-slate-400">Password<div className="relative mt-2"><input type={visible ? "text" : "password"} autoComplete="new-password" value={password} onChange={(e) => { setPassword(e.target.value); setConfirmed(false); }} placeholder="Use a preview value only" className="w-full rounded-xl border border-white/10 bg-black/20 p-3 pr-12 text-white outline-none focus:border-cyan-300/50" /><button type="button" aria-label={visible ? "Hide password" : "Show password"} onClick={() => setVisible(!visible)} className="absolute right-3 top-1/2 -translate-y-1/2 text-slate-400 hover:text-white">{visible ? <EyeOff className="size-4" /> : <Eye className="size-4" />}</button></div></label><label className="block text-sm text-slate-400">Confirm password<input type={visible ? "text" : "password"} autoComplete="new-password" value={confirmation} onChange={(e) => { setConfirmation(e.target.value); setConfirmed(false); }} placeholder="Repeat locally" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-white outline-none focus:border-cyan-300/50" /></label></div><div className="mt-5 grid gap-2 sm:grid-cols-2">{checks.map((item) => <div key={item.label} className="flex items-center gap-2 rounded-xl border border-white/10 p-3 text-sm"><span className={`flex size-5 items-center justify-center rounded-full ${item.passed ? "bg-cyan-300 text-slate-950" : "bg-white/10 text-slate-500"}`}>{item.passed ? <Check className="size-3" /> : <X className="size-3" />}</span><span className={item.passed ? "text-slate-200" : "text-slate-500"}>{item.label}</span></div>)}</div>{password && confirmation && !matches && <p className="mt-4 flex items-center gap-2 text-sm text-rose-200"><TriangleAlert className="size-4" />The two local values do not match.</p>}<div className="mt-5 flex flex-wrap gap-2"><Button disabled={!valid || !confirmed} className="bg-cyan-300/20 text-cyan-100/40">Create credential unavailable</Button><Button onClick={() => setConfirmed(true)} disabled={!valid} variant="outline" className="border-white/10 text-white hover:bg-white/10">Acknowledge local checks</Button></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Safety panel</p><h2 className="mt-2 text-2xl font-black">Credential hygiene</h2><div className="mt-5 space-y-3">{["Use a unique password and never reuse it across services", "Prefer a password manager and phishing-resistant MFA", "Do not paste seed phrases, API keys, access tokens, or recovery codes", "Never log passwords or send them through chat, analytics, screenshots, or support tickets"].map((item) => <div key={item} className="flex items-start gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="mt-0.5 size-4 text-cyan-300" /><span className="text-sm leading-5 text-slate-300">{item}</span></div>)}</div><div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex gap-3"><LockKeyhole className="mt-0.5 size-5 shrink-0 text-amber-200" /><p className="text-sm leading-6 text-slate-300">A strength meter is a UX heuristic, not a guarantee against guessing, reuse, breaches, malware, or phishing.</p></div></div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Production checklist</p><h2 className="mt-2 text-2xl font-black">What real credential handling must prove</h2><div className="mt-5 space-y-3">{["TLS, CSRF protection, rate limits, bot defense, and safe request boundaries", "Server-side password hashing with modern cost parameters and secret separation", "Credential breach screening, lockout/recovery design, session rotation, and MFA", "No plaintext logging, analytics capture, screenshots, exports, or client persistence", "Monitoring, alerting, support, audit events, privacy, and incident response"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><WifiOff className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake authentication</h2><p className="mt-3 text-sm leading-6 text-slate-400">Passing local criteria only enables an acknowledgment control. It does not create an account, authenticate a user, or prove password security.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Visibility is accessible", description: "Show/hide control uses an explicit label and preserves the local field state.", icon: Eye, status: "Verified locally" }, { title: "Criteria are transparent", description: "Length, case, number, symbol, and match states are shown without overclaiming strength.", icon: ShieldCheck, status: "Guardrail" }, { title: "Submission is off", description: "No password, hash, account, session, recovery, or authentication result is fabricated.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
