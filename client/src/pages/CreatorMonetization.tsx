import { useMemo, useState } from "react";
import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Award, BarChart3, Check, ChevronRight, DollarSign, Gift, Heart, LockKeyhole, RefreshCw, Shield, Sparkles, Star, TrendingUp, Users, WalletCards, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const TABS = ["Streams", "Analytics", "Growth"];
const STREAMS = [{ name: "Subscriptions", description: "Plan recurring supporter access", icon: Users, status: "Draft" }, { name: "Tips and donations", description: "Map one-time support requirements", icon: Heart, status: "Unavailable" }, { name: "Ads and sponsorships", description: "Document partnership and disclosure needs", icon: Star, status: "Planning" }, { name: "Stream gifts", description: "Define live-gift safety and custody gates", icon: Gift, status: "Blocked" }, { name: "Memberships", description: "Compare tier and entitlement ideas", icon: Award, status: "Draft" }, { name: "Affiliate program", description: "Track referral-contract requirements", icon: ChevronRight, status: "Not connected" }];
const DATA = [{ month: "M1", subscriptions: 420, tips: 180, content: 90 }, { month: "M2", subscriptions: 520, tips: 220, content: 120 }, { month: "M3", subscriptions: 610, tips: 260, content: 150 }, { month: "M4", subscriptions: 700, tips: 310, content: 180 }];
const GATES = ["Identity and creator ownership", "Pricing, currency, tax, and disclosure", "Payment custody and provider contract", "Entitlement, refund, and support workflow", "Moderation, abuse prevention, and audit history"];

export default function CreatorMonetization() {
  const [tab, setTab] = useState(TABS[0]);
  const [saved, setSaved] = useState(false);
  const total = useMemo(() => DATA.reduce((sum, row) => sum + row.subscriptions + row.tips + row.content, 0), []);
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={WalletCards} eyebrow="Creator finance · Monetization" title="Plan the offer before moving real money." description="Compare local revenue-stream ideas, deterministic analytics scenarios, payout readiness gates, and growth planning. This screen does not create pricing, subscriptions, tips, payouts, token transfers, tax records, refunds, or payment-provider state." badge="Monetization preview"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save local plan"}</Button><Button onClick={() => { setSaved(false); setTab(TABS[0]); }} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset plan</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Streams", value: String(STREAMS.length), hint: "Local planning paths", icon: Sparkles }, { label: "Scenario", value: `$${total}`, hint: "Illustrative only", icon: DollarSign, tone: "amber" }, { label: "Payout", value: "Off", hint: "No provider connected", icon: WifiOff, tone: "slate" }, { label: "Payment", value: "Blocked", hint: "No transfer created", icon: LockKeyhole, tone: "violet" }]} /><ScreenPreviewBanner title="Financial evidence boundary"><strong>Scenario dollars, “active” badges, revenue charts, milestone progress, and growth advice are planning fixtures—not proof of earnings, subscriptions, tips, payouts, balances, financial performance, or payment success.</strong> Production monetization requires verified ownership, payment custody, provider contracts, currency/tax rules, entitlements, refunds, moderation, support, reconciliation, and audit history.</ScreenPreviewBanner><section className="flex flex-wrap gap-2" aria-label="Monetization sections">{TABS.map((item) => <button key={item} onClick={() => setTab(item)} className={`rounded-xl border px-4 py-2 text-sm ${tab === item ? "border-cyan-300/40 bg-cyan-300/[0.1] text-cyan-200" : "border-white/10 text-slate-400"}`}>{item}</button>)}</section>{tab === "Streams" && <section className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">{STREAMS.map((stream) => <Card key={stream.name} className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="flex items-start justify-between"><div className="flex size-10 items-center justify-center rounded-xl bg-cyan-300/10 text-cyan-200"><stream.icon className="size-5" /></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{stream.status}</Badge></div><h2 className="mt-5 font-black">{stream.name}</h2><p className="mt-2 min-h-10 text-sm leading-6 text-slate-400">{stream.description}</p><Button onClick={() => setSaved(true)} variant="outline" className="mt-5 w-full border-white/15 text-white">Review requirements</Button></CardContent></Card>)}</section>}{tab === "Analytics" && <section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Scenario analytics</p><h2 className="mt-2 text-2xl font-black">Four-month illustrative mix</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Synthetic</Badge></div><div className="mt-6 h-64"><ResponsiveContainer width="100%" height="100%"><BarChart data={DATA}><CartesianGrid strokeDasharray="3 3" stroke="rgba(255,255,255,0.08)" /><XAxis dataKey="month" stroke="#94a3b8" fontSize={12} /><YAxis stroke="#94a3b8" fontSize={12} /><Tooltip contentStyle={{ background: "#10152e", border: "1px solid rgba(255,255,255,0.12)", color: "#fff" }} formatter={(value) => [`$${value}`, "Scenario"]} /><Bar dataKey="subscriptions" fill="#22d3ee" radius={[4, 4, 0, 0]} /><Bar dataKey="tips" fill="#a78bfa" radius={[4, 4, 0, 0]} /><Bar dataKey="content" fill="#fbbf24" radius={[4, 4, 0, 0]} /></BarChart></ResponsiveContainer></div><p className="mt-4 text-xs text-slate-500">Synthetic dollars · no provider source · no revenue claim</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><DollarSign className="size-5 text-amber-300" /><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-300">Payout preview</p><h2 className="mt-1 text-2xl font-black">No payout history</h2></div></div><div className="mt-6 rounded-2xl border border-red-300/20 bg-red-300/[0.05] p-5"><div className="flex items-center gap-2 font-semibold text-red-200"><LockKeyhole className="size-4" />Settlement is unavailable</div><p className="mt-2 text-sm leading-6 text-slate-400">No payout dates, amounts, status, balance, provider, or settlement record exists. The chart is only a deterministic UI fixture.</p></div><div className="mt-5 space-y-3">{["Provider account", "Currency and tax rules", "Refund and dispute path", "Reconciliation ledger"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><Shield className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card></section>}{tab === "Growth" && <section className="grid gap-6 lg:grid-cols-2"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><Award className="size-5 text-amber-300" /><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-300">Milestone planner</p><h2 className="mt-1 text-2xl font-black">Operational readiness</h2></div></div><div className="mt-6 space-y-3">{["Define a supportable offer", "Publish policy and disclosure", "Validate entitlement delivery", "Test refunds and disputes", "Reconcile creator records"].map((item, index) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><div className="flex size-7 items-center justify-center rounded-full bg-white/10 text-xs text-slate-300">{index + 1}</div><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Not verified</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><TrendingUp className="size-5 text-violet-300" /><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-300">Growth guidance preview</p><h2 className="mt-1 text-2xl font-black">Questions before optimization</h2></div></div><div className="mt-6 space-y-3">{["Which audience need is evidenced?", "What is the cost and support burden?", "Can the entitlement be revoked safely?", "How will disputes be reconciled?"].map((item) => <div key={item} className="rounded-xl border border-white/10 bg-black/10 p-4 text-sm text-slate-300">{item}</div>)}</div><p className="mt-5 text-xs leading-5 text-slate-500">No AI recommendation, earnings forecast, or growth promise is generated from the local preview.</p></CardContent></Card></section>}<section><h2 className="mb-4 text-2xl font-black">Production gates</h2><div className="grid gap-3 md:grid-cols-2 lg:grid-cols-5">{GATES.map((gate) => <div key={gate} className="rounded-xl border border-white/10 bg-white/[0.04] p-4"><Shield className="size-4 text-cyan-300" /><p className="mt-3 text-sm leading-5 text-slate-300">{gate}</p></div>)}</div></section><ScreenFeatureGrid features={[{ title: "Scenario is not revenue", description: "Illustrative charts and mix percentages do not establish creator earnings or financial performance.", icon: BarChart3, status: "Guardrail" }, { title: "Payout needs custody", description: "A real payout requires provider identity, settlement status, refunds, tax rules, and reconciliation.", icon: WalletCards, status: "Required" }, { title: "No fake payment state", description: "The screen plans monetization without creating subscriptions, tips, balances, transfers, or claims.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
