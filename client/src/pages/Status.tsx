import { useState } from "react";
import { Activity, AlertTriangle, Check, ClipboardList, Clock3, Info, LockKeyhole, RefreshCw, Search, ServerCog, ShieldAlert, Settings, Users, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";
const services = [{ name: "Core API", state: "Unmeasured", evidence: "No telemetry source", icon: ServerCog }, { name: "Web application", state: "Preview", evidence: "Browser route verified only", icon: Activity }, { name: "Database", state: "Unverified", evidence: "No health probe", icon: ClipboardList }, { name: "Background jobs", state: "Unknown", evidence: "No scheduler source", icon: Clock3 }];
export default function Status() { const [query, setQuery] = useState(""); const [saved, setSaved] = useState(false); const [showBoundary, setShowBoundary] = useState(false); const [refreshing, setRefreshing] = useState(false); const refresh = () => { setRefreshing(true); window.setTimeout(() => setRefreshing(false), 500); }; const reset = () => { setQuery(""); setSaved(false); setShowBoundary(false); setRefreshing(false); }; const filtered = services.filter((service) => `${service.name} ${service.state} ${service.evidence}`.toLowerCase().includes(query.toLowerCase())); return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Activity} eyebrow="Status · Operations preview" title="Inspect service status without inventing uptime." description="Review a local service-status workspace with search, settings intent, service inventory, availability, deployments, incidents, ownership, retry, save/reset, and evidence boundaries. No live telemetry, uptime, incident, deployment, security, or SLA state is connected." badge="Evidence-bounded operations"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save status view"}</Button><Button onClick={() => setShowBoundary((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showBoundary ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showBoundary ? "Close boundary" : "Review evidence boundary"}</Button><Button onClick={refresh} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className={`mr-2 size-4 ${refreshing ? "animate-spin" : ""}`} />{refreshing ? "Retrying local state" : "Retry status"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><X className="mr-2 size-4" />Reset status</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Services", value: `${services.length} local`, hint: "No live probes", icon: ServerCog, tone: "cyan" }, { label: "Availability", value: "Unmeasured", hint: "No uptime source", icon: Activity, tone: "violet" }, { label: "Incidents", value: "Unknown", hint: "No incident source", icon: AlertTriangle, tone: "amber" }, { label: "SLA", value: "Unverified", hint: "No contract source", icon: ShieldAlert, tone: "slate" }]} /><ScreenPreviewBanner title="Status evidence boundary"><strong>This is a local operations preview, not evidence that any service is online, healthy, available, secure, deployed, incident-free, within SLA, or monitored.</strong> Service cards, search, settings intent, local saved state, retry feedback, ownership concepts, and disabled live-query actions are browser concepts. No uptime, latency, capacity, deployment, incident, security, availability, SLA, or production outcome is asserted.</ScreenPreviewBanner><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><label className="text-sm font-semibold text-slate-300"><span className="flex items-center gap-2"><Search className="size-4 text-slate-500" />Search services and evidence</span><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search API, database, jobs..." className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white outline-none" /></label></CardContent></Card><section className="grid gap-4 md:grid-cols-2">{filtered.map(({ name, state, evidence, icon: Icon }) => <Card key={name} className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="flex items-start justify-between"><div className="flex size-11 items-center justify-center rounded-2xl bg-cyan-300/10 text-cyan-200"><Icon className="size-5" /></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{state}</Badge></div><h2 className="mt-4 font-bold">{name}</h2><p className="mt-2 text-sm text-slate-400">{evidence}</p><div className="mt-4 flex items-center gap-2 text-xs text-slate-500"><Users className="size-3" />Owner unverified <Settings className="ml-2 size-3" />Config unavailable</div></CardContent></Card>)}</section><ScreenStatePanel type="unavailable" title="Live status source unavailable" description="Connect authenticated telemetry, health probes, release metadata, incident management, alerting, and ownership data before reporting service state, uptime, latency, availability, security, or SLA performance." /><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><Info className="size-5 text-cyan-300" /><div><p className="font-bold">Operations evidence checklist</p><p className="mt-1 text-sm text-slate-400">Status requires reproducible probes, timestamps, service identity, environment, deployment version, alert state, incident history, access controls, and audit logs.</p></div></div><div className="mt-5 grid gap-3 md:grid-cols-2">{["Authenticated health and readiness probes", "Freshness, latency, capacity, and error-rate telemetry", "Deployment, release, rollback, and migration evidence", "Incident, alert, ownership, and escalation records", "Security controls, access policy, and audit logs", "Availability, SLA, maintenance, and user-impact definitions"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3 text-sm text-slate-300"><LockKeyhole className="size-4 text-amber-300" />{item}<span className="ml-auto text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><div className="flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Load live status unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Acknowledge incident unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Open operations console unavailable</Button></div>{showBoundary && <Card className="border-amber-300/20 bg-amber-300/[0.06]"><CardContent className="p-6"><p className="font-semibold text-amber-100">No uptime or production claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local status surface does not prove service health, deployment, uptime, incident state, security, availability, SLA compliance, or production readiness.</p></CardContent></Card>}<ScreenFeatureGrid features={[{ title: "Status surface preserved", description: "Service inventory, search, settings, availability, incidents, ownership, retry, save/reset, and evidence checklist remain visible.", icon: Activity, status: "Local operations" }, { title: "No uptime theater", description: "Health, uptime, latency, capacity, deployment, incident, security, and SLA outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before reporting", description: "Real status requires probes, freshness, deployment metadata, incident records, access controls, and audit logs.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>; }
