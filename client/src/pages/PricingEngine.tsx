import { useState } from "react";
import { Link } from "wouter";
import { ArrowLeft, CheckCircle2, ClipboardCheck, Lock, RefreshCw, ShieldAlert, TrendingUp } from "lucide-react";
import { Button } from "@/components/ui/button";

const tabs = ["matrix", "value", "governance"] as const;
type Tab = (typeof tabs)[number];

const tiers = [
  { name: "Preview", status: "Local concept", description: "A non-transactional preview of a supported surface with no entitlement or outcome claim." },
  { name: "Standard", status: "Requires evidence", description: "A potential paid capability requiring product scope, access rules, provider contracts, support, and refund policy." },
  { name: "Advanced", status: "Requires review", description: "A multi-step capability requiring execution authorization, data handling, observability, and rollback." },
  { name: "Enterprise", status: "Blocked", description: "A governed offering requiring commercial contracts, security evidence, service ownership, and operational acceptance." },
];

const gates = [
  "Define the actual capability, user entitlement, provider dependency, and measurable outcome before assigning a price.",
  "Validate currency, tax, payment, refunds, chargebacks, regional availability, accessibility, and customer support ownership.",
  "Prove authorization, idempotency, auditability, rate limits, data minimization, and safe failure for every paid action.",
  "Use real demand, conversion, retention, cost, and support evidence; never infer business performance from a pricing mockup.",
  "Keep financial, token, AI, marketplace, education, and enterprise claims separately reviewed and explicitly sourced.",
  "Do not use dark patterns, forced upsells, misleading scarcity, or claims that a user must pay to receive an outcome.",
];

export default function PricingEngine() {
  const [activeTab, setActiveTab] = useState<Tab>("matrix");
  const [reviewed, setReviewed] = useState(false);

  return (
    <main className="min-h-screen bg-[#070a16] text-white">
      <header className="sticky top-0 z-10 border-b border-white/10 bg-[#070a16]/85 px-4 py-3 backdrop-blur-xl">
        <div className="mx-auto flex max-w-5xl items-center gap-3"><Link href="/"><Button variant="ghost" size="icon" aria-label="Back to home" className="text-slate-400 hover:text-white"><ArrowLeft className="size-4" /></Button></Link><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200/70">Commercial governance</p><h1 className="text-lg font-bold">Pricing workspace</h1></div><span className="ml-auto rounded-full border border-amber-300/20 bg-amber-300/[0.06] px-3 py-1 text-xs text-amber-100">Evidence required</span></div>
      </header>
      <section className="mx-auto max-w-5xl px-4 py-10 sm:px-6"><div className="rounded-3xl border border-white/10 bg-gradient-to-br from-cyan-400/[0.12] via-purple-400/[0.08] to-transparent p-6 sm:p-10"><div className="flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200"><ShieldAlert className="size-4" />Truthful pricing boundary</div><h2 className="mt-4 max-w-3xl text-4xl font-black tracking-tight sm:text-5xl">Design fair offers only after the value and delivery are real.</h2><p className="mt-5 max-w-3xl text-base leading-7 text-slate-300">This workspace preserves pricing and commercial-planning concepts without asserting prices, revenue, conversion, subscriptions, AI execution, customer demand, or financial outcomes. No checkout, payment, upsell, or entitlement operation is connected.</p><div className="mt-7 flex flex-wrap gap-2"><Button onClick={() => setReviewed(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><ClipboardCheck className="mr-2 size-4" />{reviewed ? "Review recorded locally" : "Mark review locally"}</Button><Button onClick={() => setReviewed(false)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset</Button></div></div></section>
      <section className="mx-auto max-w-5xl space-y-8 px-4 pb-14 sm:px-6"><div className="grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-white/10 bg-white/[0.04] p-5"><p className="text-xs uppercase tracking-widest text-slate-500">Prices</p><p className="mt-2 text-2xl font-black text-amber-200">Unconfigured</p><p className="mt-1 text-sm text-slate-500">No currency or catalog source</p></div><div className="rounded-2xl border border-white/10 bg-white/[0.04] p-5"><p className="text-xs uppercase tracking-widest text-slate-500">Revenue</p><p className="mt-2 text-2xl font-black text-amber-200">Not claimed</p><p className="mt-1 text-sm text-slate-500">No financial evidence</p></div><div className="rounded-2xl border border-white/10 bg-white/[0.04] p-5"><p className="text-xs uppercase tracking-widest text-slate-500">Checkout</p><p className="mt-2 text-2xl font-black text-amber-200">Blocked</p><p className="mt-1 text-sm text-slate-500">No payment provider</p></div></div><div className="flex gap-1 rounded-2xl border border-white/10 bg-white/[0.03] p-1">{tabs.map(tab => <button key={tab} type="button" onClick={() => setActiveTab(tab)} className={`flex-1 rounded-xl px-3 py-3 text-sm font-semibold capitalize transition ${activeTab === tab ? "bg-white/10 text-white" : "text-slate-500 hover:text-slate-300"}`}>{tab === "matrix" ? "Capability matrix" : tab === "value" ? "Value evidence" : "Governance gates"}</button>)}</div>{activeTab === "matrix" && <div className="grid gap-4 md:grid-cols-2">{tiers.map((tier, index) => <article key={tier.name} className="rounded-2xl border border-white/10 bg-white/[0.04] p-6"><div className="flex items-center justify-between"><div className="flex items-center gap-3"><span className="flex size-8 items-center justify-center rounded-full bg-cyan-300/10 text-cyan-200">{index + 1}</span><h3 className="font-bold">{tier.name}</h3></div><span className="text-xs text-amber-200">{tier.status}</span></div><p className="mt-4 text-sm leading-6 text-slate-400">{tier.description}</p><div className="mt-5 flex items-center gap-2 text-xs text-slate-500"><TrendingUp className="size-4 text-cyan-200" />No conversion or revenue claim</div></article>)}</div>}{activeTab === "value" && <div className="rounded-2xl border border-white/10 bg-white/[0.04] p-6"><h3 className="text-2xl font-black">Value evidence before price</h3><p className="mt-3 max-w-3xl text-sm leading-6 text-slate-400">A fair offer needs a defined capability, user need, delivery path, support commitment, accessibility standard, cancellation path, and evidence that the outcome can be delivered. A price label alone does not prove value or demand.</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{["Capability scope and limitations", "User research and consent", "Provider and fulfillment contract", "Cost and support ownership", "Refund and cancellation policy", "Measured outcome evidence"].map(item => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-4 text-sm text-slate-300"><CheckCircle2 className="size-4 text-cyan-200" />{item}<span className="ml-auto text-xs text-amber-200">Required</span></div>)}</div></div>}{activeTab === "governance" && <div className="rounded-2xl border border-white/10 bg-white/[0.04] p-6"><h3 className="text-2xl font-black">Commercial governance gates</h3><div className="mt-6 space-y-3">{gates.map(gate => <div key={gate} className="flex items-start gap-3 rounded-xl border border-white/10 p-4 text-sm leading-6 text-slate-300"><Lock className="mt-1 size-4 shrink-0 text-amber-200" />{gate}</div>)}</div></div>}<div className="rounded-2xl border border-dashed border-white/15 bg-amber-300/[0.04] p-6 text-sm leading-6 text-slate-400"><strong className="text-amber-100">No pricing or financial claim is made.</strong> This screen is a local governance workspace. It does not establish a live price, subscription, payment, customer, conversion, revenue, valuation, or business-performance result.</div></section>
    </main>
  );
}
