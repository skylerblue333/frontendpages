import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Check, Clipboard, Download, ExternalLink, LockKeyhole, QrCode, RefreshCw, ScanLine, ShieldAlert, Share2, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const patterns = [{ name: "Website", value: "https://skycoin4444.com", note: "Destination is not verified" }, { name: "Text note", value: "SKYCOIN4444 local preview", note: "Plain text only" }, { name: "Contact intent", value: "contact not configured", note: "No contact record" }];
export default function QRCodeGenerator() {
  const [pattern, setPattern] = useState(patterns[0]);
  const [payload, setPayload] = useState(patterns[0].value);
  const [size, setSize] = useState("Medium");
  const [correction, setCorrection] = useState("M");
  const [saved, setSaved] = useState(false);
  const [showGates, setShowGates] = useState(false);
  const chars = payload.length;
  const modules = useMemo(() => Array.from({ length: 169 }, (_, index) => { const x = index % 13; const y = Math.floor(index / 13); return ((x * 7 + y * 11 + chars * 3 + x * y) % 5) < 2; }), [chars]);
  const reset = () => { setPattern(patterns[0]); setPayload(patterns[0].value); setSize("Medium"); setCorrection("M"); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={QrCode} eyebrow="QRCodeGenerator · Visual preview" title="Design the code before trusting the destination." description="Compose a local QR payload and visual preview with content type, size, color, correction, download, sharing, and scanning boundaries. No file, scan result, destination safety, ownership, or delivery is verified." badge="QR workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Design saved locally" : "Save design locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review QR gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset design</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Payload", value: `${chars} chars`, hint: "Local text", icon: Clipboard, tone: "cyan" }, { label: "Format", value: "QR concept", hint: "No file created", icon: QrCode, tone: "violet" }, { label: "Scan", value: "Not tested", hint: "No scanner source", icon: ScanLine, tone: "amber" }, { label: "Destination", value: "Unverified", hint: "No safety check", icon: ExternalLink, tone: "slate" }]} /><ScreenPreviewBanner title="QR evidence boundary"><strong>This is a local QR composition and visual preview, not a generated-file or scanning service.</strong> Payload, module pattern, size, correction intent, saved state, and preview are browser concepts. No QR file, image export, scan result, redirect, destination safety, ownership, malware check, contact record, or share event is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.8fr_1.2fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="space-y-5 p-6"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Local patterns</p><h2 className="mt-2 text-2xl font-black">Choose a payload concept</h2></div><div className="space-y-3">{patterns.map((item) => <button key={item.name} onClick={() => { setPattern(item); setPayload(item.value); }} className={`w-full rounded-xl border p-4 text-left ${pattern.name === item.name ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-center justify-between"><p className="font-semibold">{item.name}</p><Badge variant="outline" className="border-amber-300/20 text-amber-200">Preview</Badge></div><p className="mt-2 truncate text-sm text-slate-400">{item.value}</p><p className="mt-2 text-xs text-slate-500">{item.note}</p></button>)}</div><label className="block text-sm text-slate-400">Payload<textarea value={payload} onChange={(event) => setPayload(event.target.value)} rows={4} className="mt-2 w-full resize-none rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none" /></label><div className="grid gap-3 sm:grid-cols-2"><label className="text-sm text-slate-400">Size<select value={size} onChange={(event) => setSize(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Small</option><option>Medium</option><option>Large</option></select></label><label className="text-sm text-slate-400">Error correction<select value={correction} onChange={(event) => setCorrection(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>L</option><option>M</option><option>Q</option><option>H</option></select></label></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Visual preview</p><h2 className="mt-2 text-2xl font-black">{pattern.name} · {size}</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Not scannable proof</Badge></div><div className="mx-auto mt-8 grid size-64 grid-cols-13 gap-1 rounded-2xl bg-white p-5">{modules.map((filled, index) => <span key={index} className={filled ? "rounded-[2px] bg-slate-950" : "rounded-[2px] bg-white"} />)}</div><p className="mt-5 text-center text-sm leading-6 text-slate-500">Visual pattern only. It has not been encoded, exported, scanned, or verified.</p><div className="mt-6 flex flex-wrap justify-center gap-2"><Button disabled className="bg-slate-700 text-slate-400"><Download className="mr-2 size-4" />Download unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500"><Share2 className="mr-2 size-4" />Share unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500"><ScanLine className="mr-2 size-4" />Scan unavailable</Button></div>{showGates && <div className="mt-6 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No destination trust claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A QR preview cannot establish who controls a destination, whether it is safe, or what a scanner will do.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">QR gates</p><h2 className="mt-2 text-2xl font-black">What a real QR workflow must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Correct encoding, format, color contrast, quiet zone, error correction, and scan testing", "Destination ownership, HTTPS, malware/phishing screening, redirects, and freshness", "File generation, download, storage, access controls, versioning, and deletion", "Share permissions, attribution, privacy, consent, and public-link behavior", "Contact, payment, wallet, event, or deep-link semantics with explicit user confirmation", "Accessibility, print quality, device compatibility, incident response, and support"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "QR surface preserved", description: "Payload patterns, editable text, size, correction, visual preview, download, share, scan, save/reset, and gates remain interactive.", icon: QrCode, status: "Local preview" }, { title: "No scan theater", description: "Files, scan results, redirects, destination safety, ownership, contacts, payments, and shares are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Verify before distribution", description: "Real QR output needs encoding, scanning, destination screening, accessibility, storage, sharing, and support.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
