import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Crown, Eye, Grid, Heart, Image, List, LockKeyhole, Play, Share2, Star, UserRound, Users, Video, WalletCards, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const TIERS = [{ name: "Shadow", price: "$9.99", perks: ["Basic posts", "Monthly Q&A"] }, { name: "Eclipse", price: "$24.99", perks: ["Shadow perks", "Behind the scenes"] }, { name: "Void", price: "$49.99", perks: ["Eclipse perks", "Custom requests"] }];
const CONTENT = [{ title: "Set 44", type: "Photo", access: "Shadow" }, { title: "Behind-the-scenes clip", type: "Video", access: "Preview" }, { title: "Eclipse set", type: "Photo", access: "Eclipse" }, { title: "Void room", type: "Video", access: "Void" }, { title: "Studio preview", type: "Photo", access: "Preview" }, { title: "Exclusive draft", type: "Photo", access: "Shadow" }];

export default function CreatorProfile() {
  const [following, setFollowing] = useState(false);
  const [view, setView] = useState<"grid" | "list">("grid");
  const [selectedTier, setSelectedTier] = useState<string | null>(null);
  const [copied, setCopied] = useState(false);
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={UserRound} eyebrow="Creator surface · Public profile" title="A profile preview with honest edges." description="Review a local creator profile draft, tier comparison, follow/share interactions, and gated-content presentation. No verified identity, follower count, subscriber roster, payment, entitlement, or content ownership is claimed." badge="Profile preview"><div className="flex flex-wrap gap-2"><Button onClick={() => setFollowing((current) => !current)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Users className="mr-2 size-4" />{following ? "Following locally" : "Follow locally"}</Button><Button onClick={() => setCopied(true)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><Share2 className="mr-2 size-4" />{copied ? "Link state saved" : "Share preview"}</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Followers", value: "—", hint: "No audience source", icon: Users, tone: "slate" }, { label: "Posts", value: "6", hint: "Synthetic content cards", icon: Image }, { label: "Verification", value: "Off", hint: "No identity proof", icon: WifiOff, tone: "amber" }, { label: "Checkout", value: "Off", hint: "No subscription charge", icon: WalletCards, tone: "violet" }]} /><ScreenPreviewBanner title="Public-profile evidence boundary"><strong>Creator name, handle, avatar, badges, tier prices, access labels, engagement counts, and locked cards are presentation fixtures—not proof of a real person, verification, follower base, subscription, payment, entitlement, or media ownership.</strong> Production profile pages require identity and ownership controls, privacy choices, content contracts, billing and entitlement records, moderation, and a public/private data policy.</ScreenPreviewBanner><section className="relative overflow-hidden rounded-3xl border border-white/10 bg-gradient-to-br from-indigo-950 via-[#10152e] to-fuchsia-950 p-6 sm:p-8"><div className="absolute inset-0 bg-black/20" /><div className="relative flex flex-col gap-6 md:flex-row md:items-end md:justify-between"><div className="flex items-end gap-4"><div className="flex size-24 items-center justify-center rounded-3xl border-4 border-[#070a16] bg-gradient-to-br from-violet-500 to-fuchsia-500 text-4xl font-black">S</div><div><div className="flex items-center gap-2"><h2 className="text-3xl font-black">ShadowCreator</h2><Badge variant="outline" className="border-amber-300/20 text-amber-200">Unverified preview</Badge></div><p className="mt-1 text-slate-300">@shadow_x · synthetic profile draft</p><p className="mt-3 max-w-xl text-sm leading-6 text-slate-400">Digital artist and content creator. Building in the shadows, shining in the light. This bio is local sample copy, not a verified creator statement.</p></div></div><div className="flex gap-2"><Button onClick={() => setFollowing((current) => !current)} variant="outline" className="border-white/15 bg-black/20 text-white">{following ? "Following locally" : "Follow locally"}</Button><Button onClick={() => setCopied(true)} variant="outline" className="border-white/15 bg-black/20 text-white"><Share2 className="size-4" /></Button></div></div></section><section><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-300">Membership preview</p><h2 className="mt-2 text-2xl font-black">Compare access ideas</h2></div><Badge variant="outline" className="border-red-300/20 text-red-200">No checkout</Badge></div><div className="mt-5 grid gap-4 md:grid-cols-3">{TIERS.map((tier) => <Card key={tier.name} className={`border-white/10 bg-white/[0.04] ${selectedTier === tier.name ? "ring-1 ring-cyan-300/50" : ""}`}><CardContent className="p-5"><div className="flex items-center justify-between"><h3 className="font-black">{tier.name}</h3><Crown className="size-4 text-amber-300" /></div><p className="mt-3 text-2xl font-black text-cyan-200">{tier.price}<span className="text-xs text-slate-500"> / month</span></p><div className="mt-4 space-y-2">{tier.perks.map((perk) => <p key={perk} className="text-sm text-slate-400">• {perk}</p>)}</div><Button onClick={() => setSelectedTier(tier.name)} variant="outline" className="mt-5 w-full border-white/15 text-white">{selectedTier === tier.name ? "Selected locally" : "Preview tier"}</Button></CardContent></Card>)}</div><p className="mt-3 text-xs text-slate-500">Tier names and prices are product-design fixtures. No subscription, role, Discord access, recurring charge, or entitlement was created.</p></section><section><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Content preview</p><h2 className="mt-2 text-2xl font-black">A public/private presentation pattern</h2></div><div className="flex overflow-hidden rounded-xl border border-white/10"><button onClick={() => setView("grid")} className={`p-2 ${view === "grid" ? "bg-white/10 text-white" : "text-slate-500"}`} aria-label="Grid view"><Grid className="size-4" /></button><button onClick={() => setView("list")} className={`p-2 ${view === "list" ? "bg-white/10 text-white" : "text-slate-500"}`} aria-label="List view"><List className="size-4" /></button></div></div><div className={view === "grid" ? "mt-5 grid grid-cols-2 gap-3 md:grid-cols-3" : "mt-5 space-y-3"}>{CONTENT.map((item) => <div key={item.title} className={`group overflow-hidden rounded-2xl border border-white/10 bg-white/[0.04] ${view === "list" ? "flex items-center gap-4 p-3" : ""}`}><div className={`relative flex items-center justify-center bg-gradient-to-br from-slate-800 to-slate-950 ${view === "list" ? "size-16 shrink-0 rounded-xl" : "aspect-square"}`}>{item.type === "Video" ? <Play className="size-7 text-slate-600" /> : <Image className="size-7 text-slate-600" />}{item.access !== "Preview" && <div className="absolute inset-0 flex items-center justify-center bg-black/60"><LockKeyhole className="size-4 text-slate-400" /></div>}</div><div className={view === "list" ? "min-w-0 flex-1" : "p-3"}><p className="truncate font-semibold">{item.title}</p><div className="mt-2 flex items-center gap-2"><Badge variant="outline" className="border-white/10 text-[10px] text-slate-400">{item.access}</Badge><span className="text-xs text-slate-500">{item.type} · local card</span></div></div></div>)}</div><p className="mt-3 text-xs text-slate-500">Locked styling demonstrates UX only. No access-control decision, media URL, license, or subscriber entitlement is active.</p></section><ScreenFeatureGrid features={[{ title: "Verification before visibility", description: "A badge or creator handle must map to verified identity, ownership, and privacy rules before being public fact.", icon: UserRound, status: "Required" }, { title: "Entitlement before unlock", description: "Locked cards need real authorization, billing, refunds, and content delivery controls before they expose media.", icon: LockKeyhole, status: "Guardrail" }, { title: "No fake social proof", description: "Followers, likes, subscribers, and engagement remain unavailable without authoritative events.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
