import { useMemo, useState } from "react";
import { AlertTriangle, Ban, CheckCircle2, Clock3, FileText, Gavel, History, LockKeyhole, MessageSquare, Search, Shield, UserRound, XCircle } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

type Case = { id: string; subject: string; policy: string; severity: "Review required" | "Evidence missing" | "Appeal open"; summary: string };
const CASES: Case[] = [
  { id: "case-1", subject: "Synthetic account A", policy: "Harassment policy", severity: "Review required", summary: "Example report requiring context, affected-user privacy, and an impartial review." },
  { id: "case-2", subject: "Synthetic account B", policy: "Spam policy", severity: "Evidence missing", summary: "Example signal requiring event provenance, repetition threshold, and false-positive review." },
  { id: "case-3", subject: "Synthetic account C", policy: "Financial safety", severity: "Appeal open", summary: "Example high-impact case requiring human approval, notice, and an appeal path." },
  { id: "case-4", subject: "Synthetic account D", policy: "Identity policy", severity: "Review required", summary: "Example identity case requiring scope, legal basis, authorization, and audit." },
];
const REQUIREMENTS = ["Verified subject and authority", "Policy and evidence reference", "Proportional sanction", "Human decision and reason", "Notice and appeal", "Audit and expiry"];

export default function BanSuspendUser() { const [query, setQuery] = useState(""); const [cases, setCases] = useState(CASES); const [selected, setSelected] = useState<Case | null>(null); const [preview, setPreview] = useState<"suspend" | "ban" | null>(null); const visible = useMemo(() => cases.filter((item) => `${item.subject} ${item.policy} ${item.severity} ${item.summary}`.toLowerCase().includes(query.toLowerCase())), [cases, query]); const addCase = () => { const draft = { id: `draft-${cases.length + 1}`, subject: `Synthetic review ${cases.length + 1}`, policy: "Choose policy", severity: "Review required" as const, summary: "Local enforcement-review placeholder; no identity or sanction is connected." }; setCases((current) => [draft, ...current]); setSelected(draft); setPreview(null); }; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Gavel} eyebrow="Trust & Safety · Enforcement Review" title="Protect the community without skipping due process." description="Review synthetic moderation cases, evidence, proportional sanctions, notice, appeals, and audit requirements. This workspace does not identify real users, apply sanctions, or claim enforcement results." badge="Preview enforcement console"><div className="flex flex-wrap gap-2"><Button onClick={addCase} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><FileText className="mr-2 size-4" />Add local review case</Button><Button variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><Shield className="mr-2 size-4" />Policy checklist</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Synthetic cases", value: String(cases.length), hint: "Privacy-safe review examples", icon: Gavel }, { label: "Live enforcement", value: "Unavailable", hint: "No identity or moderation connector", icon: Ban, tone: "amber" }, { label: "Due-process gates", value: String(REQUIREMENTS.length), hint: "Evidence before action", icon: Shield, tone: "violet" }, { label: "Appeals", value: "Required", hint: "Notice and review path", icon: MessageSquare, tone: "slate" }]} /><ScreenPreviewBanner title="Enforcement evidence boundary">Synthetic cases, search, selected detail, local ban/suspend previews, evidence requirements, notice, appeals, and unavailable enforcement state are available for UX review. No real user was identified, banned, suspended, notified, or placed in an account state.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.85fr_1.15fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="relative"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search synthetic cases" aria-label="Search synthetic cases" className="border-white/10 bg-black/20 pl-9 text-white placeholder:text-slate-500" /></div><div className="mt-4 space-y-2">{visible.map((item) => <button key={item.id} onClick={() => { setSelected(item); setPreview(null); }} className={`w-full rounded-xl border p-4 text-left transition ${selected?.id === item.id ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-black/15 hover:bg-white/[0.05]"}`}><div className="flex items-center gap-2"><UserRound className="size-4 text-cyan-300" /><span className="font-semibold">{item.subject}</span><Badge variant="outline" className="ml-auto border-amber-300/20 text-amber-200">{item.severity}</Badge></div><p className="mt-2 text-xs uppercase tracking-wider text-slate-500">{item.policy}</p><p className="mt-2 text-sm leading-6 text-slate-400">{item.summary}</p></button>)}{visible.length === 0 && <ScreenStatePanel type="empty" title="No cases match" description="Try another policy or review state." />}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6">{selected ? <><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Selected review case</p><h2 className="mt-1 text-2xl font-bold">{selected.subject}</h2><p className="mt-2 text-sm text-slate-400">{selected.policy}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{selected.severity}</Badge></div><p className="mt-5 text-sm leading-6 text-slate-300">{selected.summary}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{REQUIREMENTS.map((item) => <div key={item} className="rounded-lg border border-white/10 bg-black/15 p-4"><CheckCircle2 className="size-4 text-emerald-300" /><p className="mt-3 text-sm text-slate-300">{item}</p><p className="mt-1 text-xs text-slate-500">Evidence required</p></div>)}</div><div className="mt-6 flex flex-wrap gap-2"><Button size="sm" onClick={() => setPreview("suspend")} className="bg-amber-300 text-slate-950 hover:bg-amber-200"><Clock3 className="mr-2 size-4" />Preview suspension</Button><Button size="sm" variant="outline" onClick={() => setPreview("ban")} className="border-red-300/20 text-red-200"><Ban className="mr-2 size-4" />Preview ban</Button></div>{preview && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex items-center gap-2 font-medium text-amber-200"><AlertTriangle className="size-4" />{preview === "suspend" ? "Suspension" : "Ban"} preview prepared</div><p className="mt-2 text-sm leading-6 text-slate-300">No account state changed. A real action requires verified identity, policy evidence, human approval, notice, appeal, expiry, and an audit record.</p></div>}</> : <ScreenStatePanel type="empty" title="Select a review case" description="Inspect subject authority, policy evidence, proportionality, notice, appeal, and audit requirements." />}</CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Evidence before action", description: "A report or automated signal is not enough; preserve context, source, policy, and reviewer reasoning.", icon: FileText, status: "Required" }, { title: "Due process is visible", description: "Show notice, appeal, expiry, proportionality, and human review for high-impact enforcement.", icon: Gavel, status: "Required" }, { title: "No fake sanction", description: "A local ban or suspension preview never changes a real account, identity, permission, or profile.", icon: LockKeyhole, status: "Guardrail" }]} /></main></div>; }
