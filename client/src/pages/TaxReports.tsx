import { useState } from "react";
import { Archive, Check, ClipboardCheck, Download, FileBarChart, FileText, Info, LockKeyhole, RefreshCw, Scale, ShieldAlert, UserRound, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

const reports = [
  { name: "Digital-asset activity report", period: "Period unconfigured", state: "Unavailable", detail: "Requires authoritative wallet, exchange, custody, transaction, cost-basis, timestamp, and jurisdiction sources." },
  { name: "Income and expense report", period: "Tax year unconfigured", state: "Unverified", detail: "Requires source documents, classification, reconciliation, reviewed assumptions, and taxpayer scope." },
  { name: "Filing support report", period: "Review period unconfigured", state: "Draft only", detail: "Requires forms, filing authority, signatures, retention, professional acceptance, and receipt evidence." },
];

export default function TaxReports() {
  const [selected, setSelected] = useState(0);
  const [filter, setFilter] = useState("All reports");
  const [saved, setSaved] = useState(false);
  const [showBoundary, setShowBoundary] = useState(false);
  const [refreshing, setRefreshing] = useState(false);
  const refresh = () => { setRefreshing(true); window.setTimeout(() => setRefreshing(false), 500); };
  const reset = () => { setSelected(0); setFilter("All reports"); setSaved(false); setShowBoundary(false); setRefreshing(false); };
  const visible = reports.filter((report) => filter === "All reports" || report.name.toLowerCase().includes(filter.toLowerCase()));
  const report = reports[selected] ?? reports[0];
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={FileBarChart} eyebrow="Tax reports · Report library preview" title="Organize reports without inventing tax figures or filing outcomes." description="Review a local report-library workspace with report concepts, periods, provenance, reconciliation, classification, privacy, review, retention, export, retry, save/reset, and explicit evidence boundaries. No report, tax figure, filing, liability, refund, or compliance result is connected." badge="Evidence-bounded reporting"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save report view"}</Button><Button onClick={() => setShowBoundary((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showBoundary ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showBoundary ? "Close evidence boundary" : "Review report gates"}</Button><Button onClick={refresh} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className={`mr-2 size-4 ${refreshing ? "animate-spin" : ""}`} />{refreshing ? "Retrying local state" : "Retry report source"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><X className="mr-2 size-4" />Reset reports</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Reports", value: "Local", hint: "No source ledger", icon: FileBarChart, tone: "cyan" }, { label: "Reconciliation", value: "Required", hint: "No ledger source", icon: ClipboardCheck, tone: "violet" }, { label: "Figures", value: "Withheld", hint: "No calculation source", icon: Scale, tone: "amber" }, { label: "Export", value: "Blocked", hint: "No document source", icon: Download, tone: "slate" }]} /><ScreenPreviewBanner title="Tax-report evidence boundary"><strong>This is a local report-design preview, not evidence that a report, taxpayer, transaction, income, expense, cost basis, deduction, tax figure, liability, refund, filing, or compliance outcome exists.</strong> Report cards, filters, period intent, local saved state, retry feedback, and disabled calculate/export/submit actions are browser concepts. No personal, legal, financial, or tax conclusion is asserted.</ScreenPreviewBanner><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex flex-wrap gap-2">{["All reports", "Digital", "Income", "Filing"].map((item) => <Button key={item} onClick={() => setFilter(item)} variant={filter === item ? "default" : "outline"} className={filter === item ? "bg-cyan-300 text-slate-950 hover:bg-cyan-200" : "border-white/10 bg-white/5 text-white hover:bg-white/10"}>{item}</Button>)}</div></CardContent></Card><section className="grid gap-4 md:grid-cols-3">{visible.map((item) => <button key={item.name} onClick={() => setSelected(reports.indexOf(item))} className={`rounded-2xl border p-5 text-left transition ${selected === reports.indexOf(item) ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10 bg-white/[0.04]"}`}><div className="flex items-start justify-between"><div className="flex size-11 items-center justify-center rounded-2xl bg-cyan-300/10 text-cyan-200"><FileText className="size-5" /></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.state}</Badge></div><h2 className="mt-4 font-bold">{item.name}</h2><p className="mt-1 text-xs text-cyan-200">{item.period}</p><p className="mt-3 text-sm leading-6 text-slate-400">{item.detail}</p></button>)}</section><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200">Selected report concept</p><h2 className="mt-2 text-2xl font-black">{report.name}</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{report.state}</Badge></div><p className="mt-3 text-sm leading-6 text-slate-400">{report.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Taxpayer / entity", value: "Unavailable", icon: UserRound }, { label: "Period / jurisdiction", value: report.period, icon: Scale }, { label: "Source ledger", value: "Unavailable", icon: Archive }, { label: "Reconciliation", value: "Required", icon: ClipboardCheck }, { label: "Figures / classification", value: "Withheld", icon: FileText }, { label: "Review / receipt", value: "Unverified", icon: ShieldAlert }].map(({ label, value, icon: Icon }) => <div key={label} className="flex items-center gap-3 rounded-xl border border-white/10 p-4"><Icon className="size-4 text-cyan-300" /><span className="flex-1 text-sm text-slate-300">{label}</span><span className="text-xs text-amber-200">{value}</span></div>)}</div><div className="mt-6 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Generate unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Calculate unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Export unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Submit unavailable</Button></div></CardContent></Card><ScreenStatePanel type="unavailable" title="Report source unavailable" description="Connect authoritative records, identity and jurisdiction scope, reconciliation, current rules, document storage, review, and filing controls before enabling reports." /></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><Info className="size-5 text-cyan-300" /><div><p className="font-bold">Reporting evidence contract</p><p className="mt-1 text-sm text-slate-400">Trustworthy reports require source provenance, deterministic rules, reconciliation, document integrity, privacy, retention, professional review, filing authority, receipts, and correction paths.</p></div></div><div className="mt-5 grid gap-3 md:grid-cols-2">{["Identity, entity, account scope, period, jurisdiction, and consent", "Authoritative ledger, transaction, document, timestamp, and source provenance", "Reconciliation, classification, cost basis, rules, assumptions, and validation", "Privacy, encryption, retention, access, export, and deletion controls", "Professional review, correction, amendment, approval, and audit history", "Filing authority, signature, submission, receipt, deadline, and recovery"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3 text-sm text-slate-300"><LockKeyhole className="size-4 text-amber-300" />{item}<span className="ml-auto text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card>{showBoundary && <Card className="border-amber-300/20 bg-amber-300/[0.06]"><CardContent className="p-6"><p className="font-semibold text-amber-100">No figures or filing claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local report concept does not prove taxpayer facts, transactions, calculations, liabilities, refunds, filing status, submission, or compliance.</p></CardContent></Card>}<ScreenFeatureGrid features={[{ title: "Report-library surface preserved", description: "Reports, periods, jurisdiction, source provenance, reconciliation, classification, privacy, review, export, retry, save/reset, and filing gates remain visible.", icon: FileBarChart, status: "Local reports" }, { title: "No number theater", description: "Tax facts, figures, calculations, liabilities, refunds, submissions, and compliance outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before filing", description: "Real reports require authoritative records, rules, reconciliation, privacy, review, filing authority, receipts, and correction.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
