import { useMemo, useState } from "react";
import { AlertTriangle, BarChart3, CheckCircle2, Database, Gauge, PieChart, Plus, Search, Shield, SlidersHorizontal, Target, Wallet } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

type Strategy = { id: string; name: string; profile: string; status: "Draft allocation" | "Data required" | "Risk review"; description: string };
const STRATEGIES: Strategy[] = [
  { id: "balanced", name: "Balanced allocation", profile: "Moderate risk profile", status: "Data required", description: "A planning template that separates liquidity, growth, and reserve objectives without setting real weights." },
  { id: "growth", name: "Growth allocation", profile: "Higher volatility profile", status: "Risk review", description: "A scenario template requiring drawdown, concentration, liquidity, and loss-tolerance evidence." },
  { id: "reserve", name: "Capital reserve", profile: "Liquidity-first profile", status: "Draft allocation", description: "A planning template for cash-equivalent and short-horizon needs; no asset or yield is assumed." },
  { id: "custom", name: "Custom plan", profile: "User-defined", status: "Draft allocation", description: "A local draft surface where the user can define objectives, constraints, and review questions." },
];
const DIMENSIONS = ["Objective and horizon", "Current holdings source", "Target weights and tolerance", "Liquidity and concentration", "Fees, tax, and custody", "Rebalance approval"];

export default function AssetAllocation() { const [query, setQuery] = useState(""); const [selected, setSelected] = useState(STRATEGIES[0]); const [drafted, setDrafted] = useState(false); const [targets, setTargets] = useState<Record<string, number>>({ Liquidity: 40, Growth: 35, Reserve: 25 }); const visible = useMemo(() => STRATEGIES.filter((strategy) => `${strategy.name} ${strategy.profile} ${strategy.status} ${strategy.description}`.toLowerCase().includes(query.toLowerCase())), [query]); const adjust = (key: string, delta: number) => setTargets((current) => ({ ...current, [key]: Math.max(0, Math.min(100, (current[key] ?? 0) + delta)) })); return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={PieChart} eyebrow="Crypto Hub · Portfolio Planning" title="Plan allocation without pretending to know the portfolio." description="Explore allocation templates, target bands, risk dimensions, and local draft controls. This page does not display holdings, balances, prices, returns, performance, or personalized investment advice." badge="Preview allocation workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setDrafted(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Plus className="mr-2 size-4" />Prepare local plan</Button><Button variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><Shield className="mr-2 size-4" />Risk checklist</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Plan templates", value: String(STRATEGIES.length), hint: "Planning surfaces", icon: PieChart }, { label: "Portfolio data", value: "Unavailable", hint: "No wallet or broker source", icon: Database, tone: "amber" }, { label: "Target bands", value: String(Object.keys(targets).length), hint: "Local scenario controls", icon: SlidersHorizontal, tone: "violet" }, { label: "Recommendation", value: "Disabled", hint: "Evidence and suitability required", icon: Shield, tone: "slate" }]} /><ScreenPreviewBanner title="Allocation safety boundary">Allocation templates, search, local target-band controls, selected detail, risk checklists, and unavailable portfolio data are available for UX review. No holding, balance, price, return, performance, rebalancing order, tax outcome, or personalized recommendation is fabricated.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.82fr_1.18fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="relative"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search allocation plans" aria-label="Search allocation plans" className="border-white/10 bg-black/20 pl-9 text-white placeholder:text-slate-500" /></div><div className="mt-4 space-y-2">{visible.map((strategy) => <button key={strategy.id} onClick={() => { setSelected(strategy); setDrafted(false); }} className={`w-full rounded-xl border p-4 text-left transition ${selected.id === strategy.id ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-black/15 hover:bg-white/[0.05]"}`}><div className="flex items-center gap-2"><PieChart className="size-4 text-cyan-300" /><span className="font-semibold">{strategy.name}</span><Badge variant="outline" className="ml-auto border-amber-300/20 text-amber-200">{strategy.status}</Badge></div><p className="mt-2 text-xs uppercase tracking-wider text-slate-500">{strategy.profile}</p><p className="mt-2 text-sm leading-6 text-slate-400">{strategy.description}</p></button>)}{visible.length === 0 && <ScreenStatePanel type="empty" title="No plans match" description="Try another profile or keyword." />}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Selected plan</p><h2 className="mt-1 text-2xl font-bold">{selected.name}</h2><p className="mt-2 text-sm text-slate-400">{selected.profile}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{selected.status}</Badge></div><p className="mt-5 text-sm leading-6 text-slate-300">{selected.description}</p><div className="mt-6 grid gap-3 sm:grid-cols-3">{Object.entries(targets).map(([key, value]) => <div key={key} className="rounded-xl border border-white/10 bg-black/15 p-4"><p className="text-xs uppercase tracking-wider text-slate-500">{key}</p><p className="mt-2 text-2xl font-bold text-cyan-200">{value}%</p><div className="mt-3 flex gap-2"><Button size="sm" variant="outline" onClick={() => adjust(key, -5)} className="border-white/15 text-slate-300">−5</Button><Button size="sm" variant="outline" onClick={() => adjust(key, 5)} className="border-white/15 text-slate-300">+5</Button></div><p className="mt-2 text-xs text-slate-500">Local target only</p></div>)}</div><div className="mt-6 space-y-3">{DIMENSIONS.map((item) => <div key={item} className="flex items-center gap-3 rounded-lg border border-white/10 bg-black/15 p-4 text-sm text-slate-300"><CheckCircle2 className="size-4 text-emerald-300" />{item}<Badge variant="outline" className="ml-auto border-white/10 text-slate-500">Required</Badge></div>)}</div>{drafted && <div className="mt-5 rounded-xl border border-cyan-300/20 bg-cyan-300/[0.06] p-4"><div className="flex items-center gap-2 font-medium text-cyan-200"><Target className="size-4" />Local plan prepared</div><p className="mt-2 text-sm leading-6 text-slate-300">The target bands are local UI state. No portfolio was read, no order was placed, and no financial recommendation was generated.</p></div>}</CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Source before weights", description: "Allocation requires authoritative holdings, prices, timestamps, account scope, and currency definitions.", icon: Database, status: "Required" }, { title: "Risk before action", description: "Review horizon, liquidity, concentration, drawdown, custody, fees, and suitability before any rebalance.", icon: Shield, status: "Required" }, { title: "No fake portfolio", description: "Local target bands are planning controls, not holdings, balances, performance, or investment advice.", icon: AlertTriangle, status: "Guardrail" }]} /></main></div>; }
