import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Eye, FileText, Heart, Link2, LockKeyhole, Play, RefreshCw, ShieldAlert, UserRound, Users, Wallet, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const modes = ["Private draft", "Authenticated viewer", "Public later"];
const tabs = ["Overview", "Activity", "Media", "Wallet"];
export default function ProfileView() {
  const [mode, setMode] = useState(modes[0]);
  const [tab, setTab] = useState(tabs[0]);
  const [saved, setSaved] = useState(false);
  const [showBoundary, setShowBoundary] = useState(false);
  const reset = () => { setMode(modes[0]); setTab(tabs[0]); setSaved(false); setShowBoundary(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Eye} eyebrow="ProfileView · View-mode preview" title="Make viewing explicit before showing account data." description="Explore local viewer modes and profile tabs for overview, activity, media, and wallet intent. No account lookup, identity verification, social history, asset ownership, balances, or public sharing is connected." badge="View workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200">{saved ? "View saved locally" : "Save view locally"}</Button><Button onClick={() => setShowBoundary((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showBoundary ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showBoundary ? "Close boundary" : "Review boundary"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset view</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Viewer", value: mode, hint: "Local intent", icon: Eye, tone: "cyan" }, { label: "Identity", value: "Unverified", hint: "No account lookup", icon: UserRound, tone: "violet" }, { label: "Activity", value: "Unavailable", hint: "No social source", icon: Heart, tone: "amber" }, { label: "Wallet", value: "Unknown", hint: "No wallet source", icon: Wallet, tone: "slate" }]} /><ScreenPreviewBanner title="Profile-view evidence boundary"><strong>This is a local profile-view worksheet, not an account lookup.</strong> Viewer mode, tabs, profile copy, and empty states are browser concepts. No identity, verification, public URL, social history, media, wallet address, balance, ownership, follower count, or share action is measured or persisted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.82fr_1.18fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-4"><div className="flex size-20 items-center justify-center rounded-3xl bg-cyan-300/10"><UserRound className="size-9 text-cyan-200" /></div><div><div className="flex flex-wrap items-center gap-2"><h2 className="text-2xl font-black">Skycoin explorer</h2><Badge variant="outline" className="border-amber-300/20 text-amber-200">Unverified</Badge></div><p className="mt-2 text-sm text-slate-500">@handle-unverified · {mode}</p></div></div><p className="mt-6 text-sm leading-7 text-slate-300">A local profile view for exploring the SKYCOIN4444 ecosystem. No public account or social history is loaded.</p><div className="mt-6 flex flex-wrap gap-2">{modes.map((item) => <button key={item} onClick={() => setMode(item)} className={`rounded-lg border px-3 py-2 text-xs ${mode === item ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">View posture</p><h2 className="mt-2 text-2xl font-black">No account result</h2><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Profile", value: "Local concept" }, { label: "Verification", value: "Unavailable" }, { label: "Followers", value: "Unknown" }, { label: "Share URL", value: "Not created" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-6 rounded-2xl border border-amber-300/25 bg-amber-300/[0.06] p-5"><div className="flex items-start gap-3"><LockKeyhole className="mt-0.5 size-5 shrink-0 text-amber-200" /><div><p className="font-semibold text-amber-100">Lookup and sharing unavailable</p><p className="mt-2 text-sm leading-6 text-slate-400">No account directory, privacy enforcement, social graph, public URL, wallet provider, or moderation service is connected.</p><Button disabled className="mt-4 bg-slate-700 text-slate-400">Open profile unavailable</Button></div></div></div>{showBoundary && <p className="mt-4 rounded-xl border border-cyan-300/20 bg-cyan-300/[0.05] p-4 text-sm leading-6 text-slate-400">Local view modes do not grant access, prove ownership, or create a public audience.</p>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex flex-wrap gap-2">{tabs.map((item) => <button key={item} onClick={() => setTab(item)} className={`flex items-center gap-2 rounded-lg border px-4 py-2 text-sm ${tab === item ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-200" : "border-white/10 text-slate-500"}`}>{item === "Overview" && <FileText className="size-4" />}{item === "Activity" && <Heart className="size-4" />}{item === "Media" && <Play className="size-4" />}{item === "Wallet" && <Wallet className="size-4" />}{item}</button>)}</div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-12 text-center">{tab === "Overview" && <FileText className="mx-auto size-10 text-cyan-300/40" />}{tab === "Activity" && <Heart className="mx-auto size-10 text-pink-300/40" />}{tab === "Media" && <Play className="mx-auto size-10 text-violet-300/40" />}{tab === "Wallet" && <Wallet className="mx-auto size-10 text-amber-300/40" />}<h3 className="mt-4 text-xl font-black">{tab} unavailable in preview</h3><p className="mx-auto mt-2 max-w-xl text-sm leading-6 text-slate-500">This tab is preserved as an honest empty state. No {tab.toLowerCase()} source is connected, and nothing is fabricated to make the profile appear active or funded.</p><Button disabled className="mt-5 bg-slate-700 text-slate-400">Load {tab.toLowerCase()} unavailable</Button></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">View safety gates</p><h2 className="mt-2 text-2xl font-black">Read-only must remain truly read-only</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{[{ label: "Authorization", detail: "Authenticated lookup, field-level visibility, and no IDOR", icon: LockKeyhole }, { label: "Privacy", detail: "Consent, private fields, export, deletion, and no accidental indexing", icon: Eye }, { label: "Wallet safety", detail: "Public address validation without private-key custody or transaction claims", icon: Wallet }, { label: "Moderation", detail: "Profile text, media, links, reports, appeals, and reversible enforcement", icon: ShieldAlert }].map((item) => { const Icon = item.icon; return <div key={item.label} className="flex items-start gap-3 rounded-xl border border-white/10 p-4"><Icon className="mt-0.5 size-5 text-slate-500" /><div><p className="font-semibold text-slate-200">{item.label}</p><p className="mt-1 text-sm leading-6 text-slate-500">{item.detail}</p></div></div>; })}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "View surface preserved", description: "Viewer modes, profile summary, tabs, wallet intent, safety gates, local save/reset, and unavailable states remain interactive.", icon: Eye, status: "Local view" }, { title: "No lookup theater", description: "Identity, verification, social activity, media, followers, wallet, balances, public URLs, and sharing are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Authorization before visibility", description: "Real profile views require authenticated lookup, privacy enforcement, moderation, wallet safety, and read-only guarantees.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
