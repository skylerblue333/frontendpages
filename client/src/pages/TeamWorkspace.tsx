import { useState } from "react";
import { BellOff, CalendarDays, Check, ClipboardList, FileStack, Hash, LockKeyhole, MessageSquare, RefreshCw, ShieldAlert, Users, Video, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";
const surfaces = [{ name: "Launch channel", type: "Channel", detail: "Local channel concept pending verified workspace, member identity, access scope, moderation, retention, and message delivery." }, { name: "Sprint board", type: "Tasks", detail: "Local task concept pending project scope, assignee identity, status persistence, permissions, and audit history." }, { name: "Research vault", type: "Files", detail: "Local file concept pending storage, ownership, malware scanning, versioning, retention, and export controls." }];
export default function TeamWorkspace() { const [selected, setSelected] = useState(0); const [view, setView] = useState("All surfaces"); const [saved, setSaved] = useState(false); const [refreshing, setRefreshing] = useState(false); const [status, setStatus] = useState("Workspace service unavailable. Showing local collaboration structure only."); const retry = () => { setRefreshing(true); setStatus("Retrying workspace source…"); window.setTimeout(() => { setRefreshing(false); setStatus("Workspace service unavailable. No verified collaboration source was connected."); }, 500); }; const reset = () => { setSelected(0); setView("All surfaces"); setSaved(false); setStatus("Workspace service unavailable. Showing local collaboration structure only."); }; const visible = surfaces.filter((item) => view === "All surfaces" || item.type === view); const surface = surfaces[selected] ?? surfaces[0]; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Users} eyebrow="Team workspace · Collaboration preview" title="Coordinate work without inventing messages, tasks, files, meetings, or analytics." description="Explore a local collaboration workspace with channels, tasks, files, meetings, notifications, presence, productivity concepts, privacy, retention, retry, save/reset feedback, and explicit evidence boundaries. No workspace, member, message, task, file, meeting, or notification is connected." badge="Evidence-bounded collaboration"><div className="flex flex-wrap gap-2"><Button onClick={() => { setSaved(true); setStatus("Workspace view saved locally. No collaboration data was changed."); }} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save workspace view"}</Button><Button onClick={retry} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className={`mr-2 size-4 ${refreshing ? "animate-spin" : ""}`} />{refreshing ? "Retrying source" : "Retry workspace source"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><X className="mr-2 size-4" />Reset workspace</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Channels", value: "Unavailable", hint: "No message source", icon: Hash, tone: "cyan" }, { label: "Tasks", value: "Unconfigured", hint: "No project source", icon: ClipboardList, tone: "violet" }, { label: "Files", value: "Blocked", hint: "No storage source", icon: FileStack, tone: "amber" }, { label: "Presence", value: "Unverified", hint: "No identity source", icon: Users, tone: "slate" }]} /><ScreenPreviewBanner title="Workspace evidence boundary"><strong>This is a local collaboration preview, not evidence that a workspace, member, presence, channel, message, task, file, meeting, notification, productivity metric, or access decision exists.</strong> Filters, cards, local saved state, retry feedback, and disabled create/send/upload/invite actions are browser concepts. No employment, productivity, security, or organizational conclusion is asserted.</ScreenPreviewBanner><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex flex-wrap gap-2">{["All surfaces", "Channel", "Tasks", "Files"].map((item) => <Button key={item} onClick={() => setView(item)} size="sm" variant={view === item ? "default" : "outline"} className={view === item ? "bg-cyan-300 text-slate-950 hover:bg-cyan-200" : "border-white/10 bg-white/5 text-white hover:bg-white/10"}>{item}</Button>)}<Button disabled variant="outline" className="ml-auto border-white/10 text-slate-500">Create unavailable</Button></div></CardContent></Card><section className="grid gap-4 md:grid-cols-3">{visible.map((item) => <button key={item.name} onClick={() => setSelected(surfaces.indexOf(item))} className={`rounded-2xl border p-5 text-left transition ${selected === surfaces.indexOf(item) ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10 bg-white/[0.04]"}`}><div className="flex items-start justify-between"><div className="flex size-11 items-center justify-center rounded-2xl bg-cyan-300/10 text-cyan-200">{item.type === "Channel" ? <Hash className="size-5" /> : item.type === "Tasks" ? <ClipboardList className="size-5" /> : <FileStack className="size-5" />}</div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Unavailable</Badge></div><h2 className="mt-4 font-bold">{item.name}</h2><p className="mt-1 text-xs text-cyan-200">{item.type} · Source unavailable</p><p className="mt-3 text-sm leading-6 text-slate-400">{item.detail}</p></button>)}</section><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200">Selected workspace surface</p><h2 className="mt-2 text-2xl font-black">{surface.name}</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{surface.type} unavailable</Badge></div><p className="mt-3 text-sm leading-6 text-slate-400">{surface.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Workspace / tenant", value: "Unavailable", icon: Users }, { label: "Members / presence", value: "Unverified", icon: Users }, { label: "Messages / tasks", value: "Blocked", icon: MessageSquare }, { label: "Files / versions", value: "Unavailable", icon: FileStack }, { label: "Meetings / calendar", value: "Unconfigured", icon: Video }, { label: "Notifications / analytics", value: "Withheld", icon: BellOff }].map(({ label, value, icon: Icon }) => <div key={label} className="flex items-center gap-3 rounded-xl border border-white/10 p-4"><Icon className="size-4 text-cyan-300" /><span className="flex-1 text-sm text-slate-300">{label}</span><span className="text-xs text-amber-200">{value}</span></div>)}</div><div className="mt-6 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Send unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Upload unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Schedule unavailable</Button></div></CardContent></Card><ScreenStatePanel type="unavailable" title="Collaboration source unavailable" description="Connect governed workspace identity, tenant isolation, storage, messaging, project persistence, calendar, notifications, analytics, authorization, retention, and audit controls before enabling collaboration." /></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><LockKeyhole className="size-5 text-cyan-300" /><div><p className="font-bold">Collaboration evidence contract</p><p className="mt-1 text-sm text-slate-400">Production workspaces require tenant isolation, identity, least privilege, message and file integrity, retention, moderation, notification delivery, calendar consent, auditability, and recovery.</p></div></div><div className="mt-5 grid gap-3 md:grid-cols-2">{["Workspace identity, tenant scope, member membership, presence, and consent", "Channel, message, task, file, meeting, notification, and event persistence", "Authorization, role scope, moderation, abuse controls, and sensitive-action confirmation", "File ownership, malware scanning, versioning, retention, export, and deletion", "Calendar, timezone, recording, attendance, notification, and privacy controls", "Analytics definitions, provenance, aggregation, correction, audit, and recovery"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3 text-sm text-slate-300"><LockKeyhole className="size-4 text-amber-300" />{item}<span className="ml-auto text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Collaboration surface preserved", description: "Channels, tasks, files, meetings, notifications, analytics, filters, retry, save/reset, and mutation gates remain visible.", icon: Users, status: "Local preview" }, { title: "No activity theater", description: "Members, messages, tasks, files, meetings, notifications, presence, and productivity metrics are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before collaboration", description: "Real workspaces require identity, tenant isolation, permissions, persistence, privacy, retention, moderation, and recovery controls.", icon: LockKeyhole, status: "Blocked" }]} /><p aria-live="polite" className="rounded-xl border border-white/10 bg-white/[0.03] p-4 text-sm text-slate-400">{status}</p></main></div>; }
