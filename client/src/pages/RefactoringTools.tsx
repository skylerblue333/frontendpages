import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Check, Code2, FileDiff, Filter, GitBranch, LockKeyhole, RefreshCw, Search, ShieldAlert, TestTube2, Undo2, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const plans = [{ id: 1, name: "Component boundary review", category: "Frontend", detail: "A local plan for isolating UI responsibilities without asserting a repository diff or measured performance result.", state: "Plan only" }, { id: 2, name: "API contract cleanup", category: "Backend", detail: "A contract-review concept awaiting authenticated routes, schemas, tests, and dependency provenance.", state: "Evidence needed" }, { id: 3, name: "Query-path audit", category: "Data", detail: "A database-path concept requiring real query plans, migration context, indexes, and measured timings.", state: "Unmeasured" }, { id: 4, name: "Security boundary pass", category: "Security", detail: "A review concept that must not imply a vulnerability finding, remediation, or certification.", state: "Blocked" }];
export default function RefactoringTools() {
  const [query, setQuery] = useState(""); const [category, setCategory] = useState("All"); const [selected, setSelected] = useState(1); const [scope, setScope] = useState("Scope not configured"); const [testPlan, setTestPlan] = useState("Tests not configured"); const [saved, setSaved] = useState(false); const [showGates, setShowGates] = useState(false);
  const categories = ["All", ...Array.from(new Set(plans.map((item) => item.category)))]; const filtered = useMemo(() => plans.filter((item) => (category === "All" || item.category === category) && `${item.name} ${item.category} ${item.detail}`.toLowerCase().includes(query.toLowerCase())), [category, query]); const plan = plans.find((item) => item.id === selected) ?? plans[0];
  const reset = () => { setQuery(""); setCategory("All"); setSelected(1); setScope("Scope not configured"); setTestPlan("Tests not configured"); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Code2} eyebrow="RefactoringTools · Engineering preview" title="Prove the change before calling it a refactor." description="Explore local refactoring plans with code scope, dependency and risk intent, preview/diff posture, tests, rollback, deployment, audit, save, reset, and evidence gates. No repository analysis, code diff, performance gain, security fix, or production deployment is connected." badge="Engineering workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "View saved locally" : "Save plan locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review refactor gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset plan</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Plans", value: `${plans.length} local`, hint: "No repository loaded", icon: Code2, tone: "cyan" }, { label: "Files", value: "Unavailable", hint: "No workspace source", icon: GitBranch, tone: "violet" }, { label: "Tests", value: "Unconfigured", hint: "No test runner source", icon: TestTube2, tone: "amber" }, { label: "Deployment", value: "Blocked", hint: "No release path", icon: Undo2, tone: "slate" }]} /><ScreenPreviewBanner title="Refactoring evidence boundary"><strong>This is a local engineering-planning preview, not evidence of a repository change.</strong> Plans, scopes, dependency and risk intent, preview/diff posture, test intent, rollback posture, saved state, and evidence gates are browser concepts. No file, code change, dependency, vulnerability, performance gain, test result, deployment, or rollback is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Search className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local refactoring plans" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-4 flex flex-wrap gap-2">{categories.map((entry) => <Button key={entry} onClick={() => setCategory(entry)} size="sm" variant="outline" className={category === entry ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-100" : "border-white/10 text-slate-400"}><Filter className="mr-1 size-3" />{entry}</Button>)}</div><div className="mt-6 space-y-3">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{item.name}</p><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.state}</Badge></div><div className="mt-4"><Badge variant="outline" className="border-white/10 text-slate-500">{item.category}</Badge></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected refactor plan</p><h2 className="mt-2 text-2xl font-black">{plan.name}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{plan.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Category", value: plan.category }, { label: "State", value: plan.state }, { label: "Scope", value: scope }, { label: "Diff", value: "Unavailable" }, { label: "Risk", value: "Unassessed" }, { label: "Rollback", value: "Not configured" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2"><label className="text-sm text-slate-400">Refactor scope<select value={scope} onChange={(event) => setScope(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Scope not configured</option><option>Single component intent</option><option>Route boundary intent</option><option>Service module intent</option></select></label><label className="text-sm text-slate-400">Test plan<select value={testPlan} onChange={(event) => setTestPlan(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Tests not configured</option><option>Unit tests intent</option><option>Integration tests intent</option><option>Regression suite intent</option></select></label></div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-8 text-center"><FileDiff className="mx-auto size-8 text-slate-600" /><p className="mt-3 font-semibold">No repository diff loaded</p><p className="mt-2 text-sm text-slate-500">Connect a governed workspace before showing files, dependencies, changes, test results, or deployment status.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Preview unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Run tests unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Apply unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Rollback unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No code or security claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A refactoring plan does not prove that code changed, a vulnerability was fixed, performance improved, tests passed, or deployment succeeded.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Refactoring gates</p><h2 className="mt-2 text-2xl font-black">What a real refactor must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Repository, branch, ownership, dependency graph, generated files, toolchain, and reproducible baseline", "Behavioral contract, API compatibility, data shape, migration, performance, accessibility, and user-impact review", "TypeScript, lint, unit, integration, end-to-end, security, load, accessibility, and regression tests", "Diff review, risk assessment, feature flags, rollback, idempotency, backups, and release approval", "Secrets, permissions, supply chain, vulnerabilities, unsafe calls, logging, privacy, and threat-model evidence", "Build, deployment, observability, incident response, support, reconciliation, and post-release validation"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Refactor surface preserved", description: "Plans, search, categories, scopes, dependency and risk intent, diff preview, tests, rollback, deployment, audit, save/reset, and gates remain interactive.", icon: Code2, status: "Local plans" }, { title: "No engineering theater", description: "Files, code changes, dependencies, vulnerabilities, performance, tests, deployments, and rollbacks are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before change", description: "Real refactoring needs a governed workspace, reproducible tests, security review, release controls, observability, and recovery.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
