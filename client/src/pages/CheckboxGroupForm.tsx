import { useMemo, useState } from "react";
import { Check, CheckSquare, ClipboardCheck, Eraser, Info, RefreshCw, ShieldCheck, Sparkles, WifiOff } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const GROUPS = [{ id: "workflows", label: "Workflows", description: "Choose the areas to include in a local setup draft.", options: ["Learning", "Community", "Creator tools"] }, { id: "signals", label: "Signals", description: "Choose which evidence surfaces to review.", options: ["Accessibility", "Security", "Performance"] }, { id: "updates", label: "Updates", description: "Choose how a future provider could communicate state.", options: ["Product notes", "System status", "Research previews"] }];
export default function CheckboxGroupForm() { const [selected, setSelected] = useState<string[]>(["Learning", "Accessibility"]); const [saved, setSaved] = useState(false); const [touched, setTouched] = useState(false); const toggle = (value: string) => { setSelected((items) => items.includes(value) ? items.filter((item) => item !== value) : [...items, value]); setSaved(false); setTouched(true); }; const all = useMemo(() => GROUPS.flatMap((group) => group.options), []); const valid = selected.length > 0; const selectAll = () => { setSelected(all); setSaved(false); setTouched(true); }; const clear = () => { setSelected([]); setSaved(false); setTouched(true); }; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={CheckSquare} eyebrow="Forms · Checkbox Group" title="Make grouped choices easy to understand and impossible to fake." description="Explore accessible grouped checkboxes, selection rules, and a local review state. This page does not submit preferences, persist an account setting, send notifications, or claim an external form result." badge="Preview form lab"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(valid)} disabled={!valid} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><ClipboardCheck className="mr-2 size-4" />Review local selection</Button><Button onClick={() => { setSelected(["Learning", "Accessibility"]); setSaved(false); setTouched(false); }} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset form</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Selected", value: String(selected.length), hint: "Local choices", icon: CheckSquare }, { label: "Groups", value: String(GROUPS.length), hint: "Semantic sections", icon: Sparkles, tone: "violet" }, { label: "Validation", value: valid ? "Ready" : "Required", hint: "At least one choice", icon: ShieldCheck, tone: valid ? "cyan" : "amber" }, { label: "Persistence", value: "Unavailable", hint: "No account write", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="Form evidence boundary">A checked box is not proof of submitted consent, saved preference, eligibility, enrollment, or notification subscription. A production form needs a field contract, accessible labels and errors, authorization, CSRF protection where relevant, persistence semantics, auditability, confirmation, and a retryable failure state.</ScreenPreviewBanner><section className="flex flex-wrap gap-2"><Button onClick={selectAll} variant="outline" className="border-white/15 text-white"><Check className="mr-2 size-4" />Select all</Button><Button onClick={clear} variant="outline" className="border-white/15 text-white"><Eraser className="mr-2 size-4" />Clear all</Button><Badge variant="outline" className="border-amber-300/20 px-3 py-2 text-amber-200">{selected.length} selected locally</Badge></section><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><fieldset><legend className="text-xl font-black">Choose your local review scope</legend><p className="mt-2 text-sm text-slate-400">Each option is a local draft. Groups are independent and can be changed without side effects.</p><div className="mt-6 space-y-5">{GROUPS.map((group) => <div key={group.id} className="rounded-xl border border-white/10 bg-black/15 p-4"><div className="flex items-start justify-between gap-3"><div><h3 className="font-semibold">{group.label}</h3><p className="mt-1 text-xs text-slate-500">{group.description}</p></div><Badge variant="outline" className="border-white/10 text-slate-400">{group.options.filter((option) => selected.includes(option)).length}/{group.options.length}</Badge></div><div className="mt-4 grid gap-2 sm:grid-cols-3">{group.options.map((option) => <label key={option} className="flex cursor-pointer items-center gap-3 rounded-lg border border-white/10 p-3 text-sm text-slate-300 hover:border-cyan-300/30"><input type="checkbox" checked={selected.includes(option)} onChange={() => toggle(option)} aria-label={option} className="size-4 accent-cyan-300" />{option}</label>)}</div></div>)}</div></fieldset>{touched && !valid && <p role="alert" className="mt-5 rounded-xl border border-rose-300/20 bg-rose-300/[0.06] p-4 text-sm text-rose-200">Choose at least one option before reviewing the local selection.</p>}</CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Selection review</p><h2 className="mt-2 text-2xl font-black">{valid ? `${selected.length} local choices` : "No choices yet"}</h2><div className="mt-5 flex flex-wrap gap-2">{selected.map((item) => <Badge key={item} variant="outline" className="border-cyan-300/20 text-cyan-200">{item}</Badge>)}</div><div className="mt-6 space-y-2">{["Accessible label and group name", "Required and optional semantics", "Validation and error copy", "Consent and authorization", "Persistence and retry behavior", "Confirmation and audit trail"].map((item) => <div key={item} className="flex items-center gap-3 rounded-lg border border-white/10 bg-black/15 p-3"><Info className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}{saved && <div className="mt-5 flex items-center gap-2 rounded-xl border border-cyan-300/20 bg-cyan-300/[0.06] p-4 text-sm text-cyan-200"><Check className="size-4" />Local selection reviewed; no preference was submitted or saved.</div>}</div></CardContent></Card></section><section className="grid gap-4 md:grid-cols-3"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><CheckSquare className="size-5 text-cyan-300" /><h3 className="mt-3 font-semibold">Groups need semantics</h3><p className="mt-2 text-sm leading-6 text-slate-400">Use fieldsets, legends, labels, keyboard access, and group-level error messaging.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><ShieldCheck className="size-5 text-violet-300" /><h3 className="mt-3 font-semibold">Selection is not consent</h3><p className="mt-2 text-sm leading-6 text-slate-400">A checked box does not replace explicit scope, authorization, or confirmation.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><WifiOff className="size-5 text-amber-300" /><h3 className="mt-3 font-semibold">No fake submission</h3><p className="mt-2 text-sm leading-6 text-slate-400">Local review is not account persistence, notification enrollment, or server validation.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Choice has a contract", description: "Make group semantics, requiredness, validation, and result states explicit.", icon: CheckSquare, status: "Pattern" }, { title: "Accessibility is behavior", description: "Labels, focus, keyboard interaction, and errors must work in the target environment.", icon: ShieldCheck, status: "Required" }, { title: "No fake form result", description: "A local selection is not submitted consent, saved settings, or an external record.", icon: WifiOff, status: "Guardrail" }]} /></main></div>; }
