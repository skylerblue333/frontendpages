import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Calculator, Check, ClipboardList, FileText, Filter, LockKeyhole, RefreshCw, Search, ShieldAlert, Truck, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const requests = [{ id: 1, name: "Compute capacity request", category: "Infrastructure", detail: "A local RFQ concept awaiting supplier identity, inventory, quantities, currency, delivery terms, and procurement approval.", state: "Draft" }, { id: 2, name: "Creative services request", category: "Services", detail: "A services quote concept requiring scope, supplier qualification, pricing provenance, taxes, and contract review.", state: "Needs evidence" }, { id: 3, name: "Equipment refresh request", category: "Hardware", detail: "A hardware quote concept that must not imply stock, lead time, warranty, shipping, or purchasing.", state: "Unmeasured" }, { id: 4, name: "Education supplies request", category: "Education", detail: "A supplies quote concept requiring approved vendors, safeguarding, budget ownership, and delivery evidence.", state: "Blocked" }];
export default function RequestQuote() {
  const [query, setQuery] = useState(""); const [category, setCategory] = useState("All"); const [selected, setSelected] = useState(1); const [quantity, setQuantity] = useState("Quantity not configured"); const [deadline, setDeadline] = useState("Deadline not configured"); const [saved, setSaved] = useState(false); const [showGates, setShowGates] = useState(false);
  const categories = ["All", ...Array.from(new Set(requests.map((item) => item.category)))]; const filtered = useMemo(() => requests.filter((item) => (category === "All" || item.category === category) && `${item.name} ${item.category} ${item.detail}`.toLowerCase().includes(query.toLowerCase())), [category, query]); const request = requests.find((item) => item.id === selected) ?? requests[0];
  const reset = () => { setQuery(""); setCategory("All"); setSelected(1); setQuantity("Quantity not configured"); setDeadline("Deadline not configured"); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={ClipboardList} eyebrow="RequestQuote · Procurement preview" title="Prove the quote before committing to a purchase." description="Explore local request-for-quote concepts with search, categories, suppliers, line items, quantities, prices, taxes, totals, attachments, approvals, deadlines, messages, export, procurement controls, save, reset, and evidence gates. No live vendor, inventory, currency, purchasing, contract, or order is connected." badge="Quote workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "View saved locally" : "Save RFQ locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review quote gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset RFQ</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Requests", value: `${requests.length} local`, hint: "No RFQ source", icon: ClipboardList, tone: "cyan" }, { label: "Suppliers", value: "Unavailable", hint: "No vendor source", icon: Truck, tone: "violet" }, { label: "Pricing", value: "Unavailable", hint: "No currency source", icon: Calculator, tone: "amber" }, { label: "Approvals", value: "Unconnected", hint: "No procurement source", icon: LockKeyhole, tone: "slate" }]} /><ScreenPreviewBanner title="Procurement evidence boundary"><strong>This is a local RFQ-design preview, not evidence that vendors, inventory, prices, taxes, totals, delivery, or purchasing commitments exist.</strong> Request cards, suppliers, line items, quantities, price and tax posture, attachments, approvals, deadlines, messaging, export, saved state, and audit gates are browser concepts. No vendor, product, currency, order, contract, or financial outcome is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Search className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local RFQ concepts" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-4 flex flex-wrap gap-2">{categories.map((entry) => <Button key={entry} onClick={() => setCategory(entry)} size="sm" variant="outline" className={category === entry ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-100" : "border-white/10 text-slate-400"}><Filter className="mr-1 size-3" />{entry}</Button>)}</div><div className="mt-6 space-y-3">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{item.name}</p><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.state}</Badge></div><div className="mt-4"><Badge variant="outline" className="border-white/10 text-slate-500">{item.category}</Badge></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected RFQ concept</p><h2 className="mt-2 text-2xl font-black">{request.name}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{request.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Category", value: request.category }, { label: "State", value: request.state }, { label: "Supplier", value: "Unavailable" }, { label: "Line items", value: "Unconfigured" }, { label: "Pricing", value: "Unavailable" }, { label: "Approval", value: "Unconnected" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2"><label className="text-sm text-slate-400">Quantity posture<select value={quantity} onChange={(event) => setQuantity(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Quantity not configured</option><option>Single-unit intent</option><option>Batch intent</option><option>Recurring intent</option></select></label><label className="text-sm text-slate-400">Deadline posture<select value={deadline} onChange={(event) => setDeadline(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Deadline not configured</option><option>Flexible intent</option><option>Time-bound intent</option><option>Urgent intent</option></select></label></div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-8 text-center"><FileText className="mx-auto size-8 text-slate-600" /><p className="mt-3 font-semibold">No quote responses loaded</p><p className="mt-2 text-sm text-slate-500">Connect governed suppliers, product inventory, currencies, taxes, delivery terms, approval policy, and procurement records before comparing quotes.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Send unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Add supplier unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Export unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Approve unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No procurement or financial claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local RFQ concept does not prove a vendor, product, price, tax, total, order, contract, delivery, or payment obligation.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Quote gates</p><h2 className="mt-2 text-2xl font-black">What a real request-for-quote workflow must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Requester identity, budget owner, supplier ownership, procurement authority, tenant isolation, and approvals", "Line-item identity, quantity, unit, specification, inventory, substitutions, versioning, and scope", "Price provenance, currency, tax, fees, discounts, totals, exchange rates, rounding, and financial disclosures", "Delivery, lead time, shipping, warranty, support, contract, terms, sanctions, and supplier due diligence", "Evidence, attachments, privacy, redaction, messaging, deadlines, audit, retention, and export controls", "Fraud, duplicate requests, unauthorized purchase, escalation, rejection, rollback, support, and user-visible status"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "RFQ surface preserved", description: "Requests, search, categories, suppliers, line items, quantities, prices, taxes, totals, attachments, approvals, deadlines, messages, export, and gates remain interactive.", icon: ClipboardList, status: "Local concepts" }, { title: "No purchasing theater", description: "Vendors, inventory, prices, currency, taxes, totals, orders, contracts, delivery, and payments are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before commitment", description: "Real procurement needs identity, scope, supplier verification, price provenance, approvals, audit, and recovery.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
