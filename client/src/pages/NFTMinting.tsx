import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Blocks, Check, CircleDollarSign, FileCheck2, FileImage, KeyRound, LockKeyhole, RefreshCw, ShieldCheck, Sparkles, UploadCloud, WalletCards, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const CHAINS = ["Network unset", "Ethereum-compatible", "Solana-compatible", "Testnet placeholder"];
const ROYALTY_OPTIONS = ["Not configured", "0% placeholder", "2.5% placeholder", "5% placeholder"];

export default function NFTMinting() {
  const [name, setName] = useState("Signal Form #01");
  const [description, setDescription] = useState("A synthetic collectible brief for testing metadata and minting boundaries.");
  const [chain, setChain] = useState(CHAINS[0]);
  const [royalty, setRoyalty] = useState(ROYALTY_OPTIONS[0]);
  const [externalUrl, setExternalUrl] = useState("");
  const [saved, setSaved] = useState(false);
  const reset = () => { setName("Signal Form #01"); setDescription("A synthetic collectible brief for testing metadata and minting boundaries."); setChain(CHAINS[0]); setRoyalty(ROYALTY_OPTIONS[0]); setExternalUrl(""); setSaved(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Sparkles} eyebrow="NFTMinting · Creation planning" title="Prepare the metadata before signing anything." description="Build a local minting brief with media and metadata fields, chain planning, royalty placeholders, and validation gates. No wallet connects, file uploads, contract deploys, gas quotes, signatures, transactions, ownership, or royalties are asserted." badge="Minting preview"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Draft saved locally" : "Save minting draft"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset draft</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Metadata", value: name ? "Draft" : "Empty", hint: "Local fields", icon: FileCheck2 }, { label: "Network", value: chain === CHAINS[0] ? "Unset" : "Planned", hint: "No chain connection", icon: Blocks, tone: "violet" }, { label: "Royalty", value: royalty === ROYALTY_OPTIONS[0] ? "Unset" : "Placeholder", hint: "Not enforceable", icon: CircleDollarSign, tone: "amber" }, { label: "Signing", value: "Off", hint: "No wallet source", icon: WalletCards, tone: "slate" }]} /><ScreenPreviewBanner title="NFTMinting evidence boundary"><strong>Name, description, URL, chain choice, royalty label, and validation checklist are local minting fixtures—not uploaded media, immutable metadata, creator rights, deployed contracts, gas estimates, wallet ownership, signatures, transaction hashes, token IDs, or royalty enforcement.</strong> Production minting requires rights review, secure media storage, metadata integrity, chain/contract validation, wallet safeguards, transaction lifecycle, duplicate prevention, failure recovery, and support.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1fr_0.95fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Minting draft</p><h2 className="mt-2 text-3xl font-black">Define the collectible</h2></div><Badge variant="outline" className="border-white/10 text-slate-400">Local only</Badge></div><div className="mt-5 flex min-h-48 items-center justify-center rounded-2xl border border-dashed border-white/15 bg-black/20"><div className="text-center"><UploadCloud className="mx-auto size-8 text-cyan-300" /><p className="mt-3 font-semibold">Media upload unavailable</p><p className="mt-1 text-xs text-slate-500">No file is stored, hashed, scanned, or pinned</p></div></div><label className="mt-5 block"><span className="text-sm font-semibold text-slate-300">Name</span><input value={name} onChange={(event) => { setName(event.target.value); setSaved(false); }} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-white" /></label><label className="mt-4 block"><span className="text-sm font-semibold text-slate-300">Description</span><textarea value={description} onChange={(event) => { setDescription(event.target.value); setSaved(false); }} className="mt-2 min-h-24 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-sm text-white" /></label><label className="mt-4 block"><span className="text-sm font-semibold text-slate-300">External URL <span className="text-slate-500">(optional)</span></span><input value={externalUrl} onChange={(event) => { setExternalUrl(event.target.value); setSaved(false); }} placeholder="Not connected" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-white placeholder:text-slate-500" /></label><div className="mt-4 flex items-start gap-3 text-xs leading-5 text-slate-500"><LockKeyhole className="mt-0.5 size-4 shrink-0 text-cyan-300" />Do not enter private keys, seed phrases, confidential source material, or rights-sensitive data into an unverified draft.</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-300">Chain planning</p><h2 className="mt-2 text-3xl font-black">Choose the target concept</h2><p className="mt-3 text-slate-400">A selection does not connect a wallet or establish a supported network.</p><label className="mt-5 block"><span className="text-sm font-semibold text-slate-300">Network</span><select value={chain} onChange={(event) => { setChain(event.target.value); setSaved(false); }} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-white">{CHAINS.map((item) => <option key={item} value={item}>{item}</option>)}</select></label><label className="mt-4 block"><span className="text-sm font-semibold text-slate-300">Royalty concept</span><select value={royalty} onChange={(event) => { setRoyalty(event.target.value); setSaved(false); }} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-white">{ROYALTY_OPTIONS.map((item) => <option key={item} value={item}>{item}</option>)}</select></label><div className="mt-5 space-y-3">{[{ label: "Creator rights", value: "Not verified" }, { label: "Metadata storage", value: "Not connected" }, { label: "Contract", value: "Not deployed" }, { label: "Gas estimate", value: "Unavailable" }, { label: "Wallet", value: "Not connected" }].map((item) => <div key={item.label} className="flex items-center justify-between rounded-xl border border-white/10 p-3"><span className="text-sm text-slate-300">{item.label}</span><span className="text-xs text-amber-200">{item.value}</span></div>)}</div><Button disabled className="mt-5 w-full bg-white/10 text-white/40"><KeyRound className="mr-2 size-4" />Connect wallet unavailable</Button></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Mint readiness</p><h2 className="mt-2 text-2xl font-black">Required before a real transaction</h2><div className="mt-5 space-y-3">{["Creator rights, media provenance, and content policy", "Secure upload, hashing, metadata pinning, and recovery", "Network, contract, token standard, and gas validation", "Wallet ownership, signing, replay and duplicate protection", "Transaction receipt, confirmation, failure, support, and royalties"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><FileImage className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake mint</h2><p className="mt-3 text-sm leading-6 text-slate-400">The draft does not upload media, deploy a contract, sign a transaction, create a token, assign ownership, quote gas, or enforce royalties.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Metadata is not immutable", description: "Local fields preserve creator UX without storage, provenance, contract, token, or ownership claims.", icon: FileCheck2, status: "Guardrail" }, { title: "Signing needs custody", description: "Wallet connection, private-key protection, replay resistance, and transaction lifecycle need real infrastructure.", icon: KeyRound, status: "Required" }, { title: "No fake mint", description: "The preview creates no blockchain asset, hash, receipt, token ID, or royalty record.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
