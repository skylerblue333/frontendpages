import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Activity, AlertTriangle, BarChart3, Check, Gauge, LockKeyhole, RefreshCw, Search, ShieldAlert, Siren, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const rules = [{ id: 1, endpoint: "/api/example", description: "Local endpoint policy concept", scope: "Public", window: "Window not set", status: "Unverified" }, { id: 2, endpoint: "/api/session", description: "Authenticated route policy concept", scope: "Session", window: "Window not set", status: "Unverified" }, { id: 3, endpoint: "/api/admin", description: "Privileged mutation policy concept", scope: "Admin", window: "Window not set", status: "Blocked" }];
export default function RateLimitDashboard() {
  const [query, setQuery] = useState("");
  const [selected, setSelected] = useState(1);
  const [range, setRange] = useState("Window not set");
  const [showGates, setShowGates] = useState(false);
  const [saved, setSaved] = useState(false);
  const filtered = useMemo(() => rules.filter((rule) => `${rule.endpoint} ${rule.description} ${rule.scope}`.toLowerCase().includes(query.toLowerCase())), [query]);
  const rule = rules.find((item) => item.id === selected) ?? rules[0];
  const reset = () => { setQuery(""); setSelected(1); setRange("Window not set"); setShowGates(false); setSaved(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Gauge} eyebrow="RateLimitDashboard · Observability preview" title="Observe the control only after the telemetry is real." description="Explore a local rate-limit dashboard with metric cards, rule search, request and block states, latency and errors, offenders, recent blocks, alerts, incidents, range selection, save, and evidence gates. No request telemetry or enforcement result is connected." badge="Dashboard workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "View saved locally" : "Save view locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review dashboard gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset view</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Requests", value: "Unavailable", hint: "No telemetry", icon: Activity, tone: "cyan" }, { label: "Blocked", value: "Unmeasured", hint: "No enforcement source", icon: ShieldAlert, tone: "violet" }, { label: "Error rate", value: "Unmeasured", hint: "No response source", icon: AlertTriangle, tone: "amber" }, { label: "Latency", value: "Unavailable", hint: "No traces", icon: BarChart3, tone: "slate" }]} /><ScreenPreviewBanner title="Dashboard evidence boundary"><strong>This is a local observability and policy-review preview, not a live security dashboard.</strong> Metric cards, rule labels, range intent, empty charts, offender and block states, alerts, incidents, saved state, and evidence gates are browser concepts. No request count, block count, error rate, latency, offender, incident, uptime, protection, or abuse-reduction outcome is asserted.</ScreenPreviewBanner><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-slate-500">Reporting window</p><select value={range} onChange={(event) => setRange(event.target.value)} className="mt-2 rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Window not set</option><option>Last hour intent</option><option>Last 24 hours intent</option><option>Last 7 days intent</option></select></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Telemetry disconnected</Badge></div><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Search className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local endpoint rules" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-6 space-y-3">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-mono text-sm text-cyan-100">{item.endpoint}</p><p className="mt-1 text-sm text-slate-500">{item.description}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.status}</Badge></div><div className="mt-4 flex gap-2"><Badge variant="outline" className="border-white/10 text-slate-500">{item.scope}</Badge><Badge variant="outline" className="border-white/10 text-slate-500">{item.window}</Badge></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected rule</p><h2 className="mt-2 text-2xl font-black">{rule.endpoint}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{rule.description}. Scope: {rule.scope}.</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Rule status", value: rule.status }, { label: "Window", value: range }, { label: "Requests", value: "Unavailable" }, { label: "Blocks", value: "Unmeasured" }, { label: "Errors", value: "Unmeasured" }, { label: "p95 latency", value: "Unavailable" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-8 text-center"><BarChart3 className="mx-auto size-7 text-slate-600" /><p className="mt-3 font-semibold">No telemetry chart loaded</p><p className="mt-2 text-sm text-slate-500">Connect governed request traces before showing volume, blocks, error rate, latency, or trend.</p></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No live dashboard claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A rule label does not prove that the endpoint is protected, monitored, healthy, available, or enforcing a limit.</p></div>}</CardContent></Card></section><section className="grid gap-6 md:grid-cols-2"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><AlertTriangle className="size-5 text-amber-300" /><div><p className="font-semibold">Top offenders</p><p className="text-sm text-slate-500">Population unavailable</p></div></div><div className="mt-6 rounded-xl border border-dashed border-white/10 p-6 text-center text-sm text-slate-500">No offender telemetry loaded. IPs, actors, endpoints, and block totals are not displayed.</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><Siren className="size-5 text-red-300" /><div><p className="font-semibold">Recent blocks and incidents</p><p className="text-sm text-slate-500">Event stream unavailable</p></div></div><div className="mt-6 rounded-xl border border-dashed border-white/10 p-6 text-center text-sm text-slate-500">No blocks, alerts, incidents, timestamps, reasons, or outcomes are loaded.</div></CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Dashboard gates</p><h2 className="mt-2 text-2xl font-black">What real observability must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Authenticated telemetry source, event schema, sampling, timestamps, time zone, and retention", "Request, response, block, retry, error, latency, and incident definitions with aggregation rules", "Privacy, IP handling, identity resolution, access controls, redaction, and sensitive-data policy", "Rule version, rollout, route mapping, distributed state, clock behavior, and config audit", "Alerts, thresholds, baselines, SLOs, false positives, on-call ownership, and incident response", "Exports, reproducibility, dashboards, customer messaging, support, and rollback evidence"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Dashboard surface preserved", description: "Metric cards, range, rule search, request/block/error/latency states, offenders, recent blocks, incidents, alerts, save/reset, and gates remain interactive.", icon: Gauge, status: "Local dashboard" }, { title: "No telemetry theater", description: "Requests, blocks, errors, latency, offenders, incidents, uptime, protection, and abuse reduction are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Observability before outcome", description: "Real dashboards need governed telemetry, privacy, definitions, baselines, alerting, audit, and incident response.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
