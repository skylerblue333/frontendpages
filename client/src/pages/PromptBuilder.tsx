import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Braces, Check, Copy, FileText, FlaskConical, LockKeyhole, RefreshCw, Save, ShieldAlert, SlidersHorizontal, Sparkles, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const templates = [{ name: "Evidence summarizer", purpose: "Structure source-grounded notes", variables: ["source", "audience"] }, { name: "Learning coach", purpose: "Draft reflective practice prompts", variables: ["topic", "level"] }, { name: "Product reviewer", purpose: "Separate claims from evidence", variables: ["product", "criteria"] }];
export default function PromptBuilder() {
  const [template, setTemplate] = useState(templates[0]);
  const [system, setSystem] = useState("You are a careful assistant. Separate observed evidence from assumptions.");
  const [instruction, setInstruction] = useState("Review {{source}} for {{audience}} and return a concise evidence map.");
  const [context, setContext] = useState("No source content entered.");
  const [temperature, setTemperature] = useState("Not connected");
  const [saved, setSaved] = useState(false);
  const [showGates, setShowGates] = useState(false);
  const variableCount = useMemo(() => (instruction.match(/{{[^}]+}}/g) ?? []).length, [instruction]);
  const reset = () => { setTemplate(templates[0]); setSystem("You are a careful assistant. Separate observed evidence from assumptions."); setInstruction("Review {{source}} for {{audience}} and return a concise evidence map."); setContext("No source content entered."); setTemperature("Not connected"); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Sparkles} eyebrow="PromptBuilder · Composition preview" title="Design the instruction before claiming the result." description="Compose local system, instruction, context, variables, preview, parameter, safety, and versioning surfaces. No model is called, no token usage or output quality is measured, and no AI result is generated." badge="Prompt studio"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Save className="mr-2 size-4" />{saved ? "Draft saved locally" : "Save draft locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review AI gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset studio</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Template", value: template.name, hint: "Local concept", icon: FileText, tone: "cyan" }, { label: "Variables", value: `${variableCount}`, hint: "Detected locally", icon: Braces, tone: "violet" }, { label: "Model", value: "Unselected", hint: "No provider source", icon: Sparkles, tone: "amber" }, { label: "Output", value: "Not generated", hint: "Execution blocked", icon: LockKeyhole, tone: "slate" }]} /><ScreenPreviewBanner title="Prompt evidence boundary"><strong>This is a local prompt-composition workspace, not an AI execution result.</strong> Templates, text fields, variables, preview, parameter intent, save state, and version labels are browser concepts. No model identity, prompt submission, token count, cost, latency, output, quality, safety result, privacy guarantee, or user data processing is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.72fr_1.28fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Templates</p><h2 className="mt-2 text-2xl font-black">Start from a local pattern</h2><div className="mt-5 space-y-3">{templates.map((item) => <button key={item.name} onClick={() => setTemplate(item)} className={`w-full rounded-xl border p-4 text-left ${template.name === item.name ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><p className="font-semibold">{item.name}</p><Badge variant="outline" className="border-white/10 text-slate-500">{item.variables.length} vars</Badge></div><p className="mt-2 text-sm text-slate-500">{item.purpose}</p></button>)}</div><div className="mt-6 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4 text-sm leading-6 text-slate-400">Templates are authored design concepts. They are not model instructions with verified behavior.</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="space-y-5 p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">{template.name}</p><h2 className="mt-2 text-2xl font-black">Compose the draft</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Version draft</Badge></div><label className="block text-sm text-slate-400">System guidance<textarea value={system} onChange={(event) => setSystem(event.target.value)} rows={3} className="mt-2 w-full resize-none rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none" /></label><label className="block text-sm text-slate-400">Instruction<textarea value={instruction} onChange={(event) => setInstruction(event.target.value)} rows={4} className="mt-2 w-full resize-none rounded-xl border border-white/10 bg-black/20 px-3 py-3 font-mono text-sm text-white outline-none" /></label><label className="block text-sm text-slate-400">Context<textarea value={context} onChange={(event) => setContext(event.target.value)} rows={3} className="mt-2 w-full resize-none rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none" /></label><div className="grid gap-3 sm:grid-cols-3">{[{ label: "Variables", value: `${variableCount} detected` }, { label: "Temperature", value: temperature }, { label: "Output format", value: "Unconfigured" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400"><Sparkles className="mr-2 size-4" />Run prompt unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500"><Copy className="mr-2 size-4" />Export unavailable</Button></div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-2"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Preview</p><h2 className="mt-2 text-2xl font-black">Rendered prompt draft</h2><pre className="mt-5 max-h-72 overflow-auto whitespace-pre-wrap rounded-xl border border-white/10 bg-black/20 p-4 text-sm leading-6 text-slate-300">{`SYSTEM\n${system}\n\nINSTRUCTION\n${instruction}\n\nCONTEXT\n${context}`}</pre><p className="mt-3 text-xs text-slate-500">Local composition only; no completion has been requested.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">AI safety gates</p><h2 className="mt-2 text-2xl font-black">What execution must prove</h2><div className="mt-5 space-y-3">{["Provider, model, version, region, and tool permissions", "Input privacy, retention, redaction, and user consent", "Prompt injection, untrusted context, and tool-call boundaries", "Token, cost, latency, error, retry, and rate-limit behavior", "Evaluation set, quality rubric, uncertainty, and human review", "Output moderation, citation, audit, export, and deletion"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div>{showGates && <div className="mt-5 rounded-xl border border-cyan-300/20 bg-cyan-300/[0.05] p-4 text-sm leading-6 text-slate-400">A saved draft is not a model call, quality score, safety decision, or user-facing answer.</div>}</CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Prompt surface preserved", description: "Templates, system/instruction/context fields, variables, preview, model and parameter posture, safety gates, local save/reset, and version intent remain interactive.", icon: Sparkles, status: "Local draft" }, { title: "No AI theater", description: "Model identity, execution, tokens, cost, latency, output, quality, safety, privacy, and citations are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Governance before execution", description: "Real AI runs need provider controls, privacy, injection defense, evaluation, moderation, audit, and support.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
