import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { AlertTriangle, Clipboard, ExternalLink, HelpCircle, LockKeyhole, RefreshCw, ShieldAlert, Siren, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
export default function RateLimitError() {
  const [retry, setRetry] = useState(false);
  const [copied, setCopied] = useState(false);
  const [showGates, setShowGates] = useState(false);
  const [channel, setChannel] = useState("Support channel not configured");
  const reset = () => { setRetry(false); setCopied(false); setShowGates(false); setChannel("Support channel not configured"); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={AlertTriangle} eyebrow="RateLimitError · Recovery preview" title="Explain the boundary before asking for another request." description="Explore a local rate-limit error recovery surface with reason, retry intent, request-ID posture, status context, support guidance, escalation, copy details, and evidence gates. No live request, block decision, incident, uptime, or production enforcement is connected." badge="Recovery workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setRetry(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><RefreshCw className="mr-2 size-4" />{retry ? "Retry preview shown" : "Review retry"}</Button><Button onClick={() => setCopied(true)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><Clipboard className="mr-2 size-4" />{copied ? "Details marked copied" : "Copy details"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review recovery gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset state</Button></div></ScreenHero><main className="mx-auto max-w-6xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Status", value: "Preview", hint: "Not from a request", icon: AlertTriangle, tone: "cyan" }, { label: "Retry", value: retry ? "Intent shown" : "Not tried", hint: "No network call", icon: RefreshCw, tone: "violet" }, { label: "Request ID", value: "Unavailable", hint: "No trace context", icon: Clipboard, tone: "amber" }, { label: "Incident", value: "Unknown", hint: "No monitor source", icon: Siren, tone: "slate" }]} /><ScreenPreviewBanner title="Error evidence boundary"><strong>This is a local failure-recovery preview, not evidence of a live rate-limit response.</strong> Error copy, retry intent, request-ID posture, support channel, copied state, and escalation state are browser concepts. No HTTP status, block decision, retry-after value, user identity, incident, outage, security event, or production behavior is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1.08fr_0.92fr]"><Card className="border-red-300/20 bg-red-300/[0.05]"><CardContent className="p-6"><div className="flex items-start gap-4"><div className="rounded-xl bg-red-300/10 p-3"><AlertTriangle className="size-7 text-red-200" /></div><div><Badge variant="outline" className="border-red-300/30 text-red-200">Local error preview</Badge><h2 className="mt-3 text-2xl font-black">Request could not be verified</h2><p className="mt-3 leading-7 text-slate-300">The interface is presenting a recovery state because no governed request context is connected. It does not identify a real limit, actor, route, or server decision.</p></div></div><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Status", value: "Not received" }, { label: "Retry after", value: "Unavailable" }, { label: "Request ID", value: "Not connected" }, { label: "Route", value: "Unknown" }, { label: "Reason", value: "Not established" }, { label: "Impact", value: "Not assessed" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 bg-black/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div>{retry && <div className="mt-6 rounded-xl border border-cyan-300/20 bg-cyan-300/[0.06] p-4"><p className="font-semibold text-cyan-100">Retry preview ready</p><p className="mt-2 text-sm leading-6 text-slate-400">A real retry needs idempotency, backoff, retry-after handling, request context, and a network response. No request was made.</p></div>}</CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Recovery checklist</p><h2 className="mt-2 text-2xl font-black">Help the user recover safely</h2><div className="mt-6 space-y-3">{[{ icon: RefreshCw, title: "Retry intentionally", detail: "Use backoff and an idempotency policy; do not loop blindly." }, { icon: Clipboard, title: "Capture request context", detail: "Expose a trace identifier only when it is supplied by the server." }, { icon: HelpCircle, title: "Explain the next step", detail: "Provide a truthful support path and expected response." }, { icon: Siren, title: "Escalate verified incidents", detail: "Require monitored evidence before declaring an outage or abuse event." }].map((item) => <div key={item.title} className="flex gap-3 rounded-xl border border-white/10 p-4"><item.icon className="mt-0.5 size-5 text-slate-400" /><div><p className="font-semibold">{item.title}</p><p className="mt-1 text-sm leading-6 text-slate-500">{item.detail}</p></div></div>)}</div><label className="mt-6 block text-sm text-slate-400">Support channel<select value={channel} onChange={(event) => setChannel(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Support channel not configured</option><option>Help-center intent</option><option>Admin escalation intent</option><option>Incident channel intent</option></select></label><div className="mt-5 flex flex-wrap gap-2"><Button disabled variant="outline" className="border-white/10 text-slate-500"><ExternalLink className="mr-2 size-4" />Open support unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Escalate unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No live incident claim</p><p className="mt-2 text-sm leading-6 text-slate-400">An error page cannot prove a rate limit, outage, security event, actor, response status, or availability impact without a verifiable server and monitoring record.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Recovery gates</p><h2 className="mt-2 text-2xl font-black">What a real error state must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Server status, response body, retry-after, request ID, route, timestamp, and environment", "Idempotency, backoff, duplicate prevention, cancellation, timeout, and retry budget", "Authentication, authorization, privacy, redaction, support access, and sensitive context handling", "Incident correlation, monitoring, alert thresholds, SLO impact, ownership, and escalation", "User messaging, accessibility, localization, recovery actions, and failure-safe defaults", "Logging, audit, reproducibility, support workflow, postmortem, and rollback evidence"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Recovery surface preserved", description: "Error explanation, retry, request-ID posture, status context, support guidance, escalation, copy details, reset, and gates remain interactive.", icon: AlertTriangle, status: "Local recovery" }, { title: "No incident theater", description: "HTTP status, block decision, retry-after, identity, incident, outage, security event, and production behavior are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before escalation", description: "Real error handling needs server context, retry safety, privacy, monitoring, support, and incident correlation.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
