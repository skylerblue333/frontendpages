import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Activity, AlertTriangle, Bell, Check, Clock3, Eye, FileClock, LockKeyhole, RefreshCw, ShieldAlert, ShieldCheck, Target, TrendingUp, Wallet, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

type Mode = "Manual review" | "Scheduled review" | "Alert planning" | "Audit trail";
const assets = ["SKY444 concept", "BTC concept", "Cash concept"];
const evidence = ["Account/wallet scope and asset identity", "Timestamped price and balance source", "Cadence, stale-data, outage, and retry behavior", "Alert consent, thresholds, delivery, and privacy", "Audit, retention, deletion, and human review"];
export default function PortfolioTracking() {
  const [mode, setMode] = useState<Mode>("Manual review");
  const [asset, setAsset] = useState(assets[0]);
  const [cadence, setCadence] = useState("24h");
  const [checks, setChecks] = useState<string[]>([]);
  const [saved, setSaved] = useState(false);
  const [refreshed, setRefreshed] = useState(false);
  const toggle = (item: string) => setChecks((items) => items.includes(item) ? items.filter((value) => value !== item) : [...items, item]);
  const reset = () => { setMode("Manual review"); setAsset(assets[0]); setCadence("24h"); setChecks([]); setSaved(false); setRefreshed(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Activity} eyebrow="PortfolioTracking · Monitoring preview" title="Design tracking controls without pretending a tracker is connected." description="Explore manual, scheduled, alert, and audit modes with asset selection, cadence, refresh simulation, and evidence requirements. No account, wallet, market, price, balance, transaction, alert delivery, scheduler, or automation source is connected." badge="Tracking planning preview"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} disabled={checks.length === 0} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Plan saved locally" : "Save local plan"}</Button><Button onClick={() => setRefreshed(true)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />{refreshed ? "Preview refreshed" : "Refresh preview"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">Reset tracking</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Mode", value: mode, hint: "Local workflow", icon: Activity, tone: "cyan" }, { label: "Asset", value: asset.split(" ")[0], hint: "Local concept", icon: Wallet, tone: "violet" }, { label: "Cadence", value: cadence, hint: "Local selection", icon: Clock3, tone: "amber" }, { label: "Automation", value: "Off", hint: "No scheduler", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="Tracking planning boundary"><strong>This is not a live tracker or alert system.</strong> Mode, asset, cadence, refresh, save, and checklist states are local planning data. They do not prove holdings, prices, movement, P&L, account activity, alert delivery, user consent, scheduler execution, or automation. No financial service is connected.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.82fr_1fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Tracking modes</p><h2 className="mt-2 text-2xl font-black">Choose a planning mode</h2><div className="mt-5 grid gap-2 sm:grid-cols-2">{(["Manual review", "Scheduled review", "Alert planning", "Audit trail"] as Mode[]).map((item) => <button key={item} onClick={() => setMode(item)} className={`flex items-center gap-3 rounded-xl border p-3 text-left ${mode === item ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10 text-slate-400"}`}><div className="flex size-9 items-center justify-center rounded-xl bg-white/10">{item === "Alert planning" ? <Bell className="size-4" /> : item === "Audit trail" ? <FileClock className="size-4" /> : item === "Scheduled review" ? <Clock3 className="size-4" /> : <Eye className="size-4" />}</div><span className="font-semibold">{item}</span><Badge variant="outline" className="ml-auto border-white/10 text-amber-200">Local</Badge></button>)}</div><p className="mt-6 text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Review cadence</p><div className="mt-3 flex gap-2">{["1h", "24h", "7d", "30d"].map((item) => <button key={item} onClick={() => setCadence(item)} className={`rounded-lg border px-3 py-2 text-xs ${cadence === item ? "border-amber-300/40 bg-amber-300/10 text-amber-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div><div className="mt-6 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex gap-3"><ShieldAlert className="mt-0.5 size-5 shrink-0 text-amber-200" /><p className="text-sm leading-6 text-slate-300">A cadence selection is not a schedule. No job, notification, price fetch, or alert delivery is created by this worksheet.</p></div></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Local tracking scope</p><h2 className="mt-2 text-2xl font-black">Select an asset concept</h2></div><Badge variant="outline" className="border-white/10 text-amber-200">Source off</Badge></div><div className="mt-5 space-y-2">{assets.map((item) => <button key={item} onClick={() => setAsset(item)} className={`flex w-full items-center gap-3 rounded-xl border p-4 text-left ${asset === item ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex size-10 items-center justify-center rounded-xl bg-cyan-300/10"><Target className="size-5 text-cyan-200" /></div><div className="flex-1"><p className="font-semibold">{item}</p><p className="mt-1 text-sm text-slate-500">Current value unavailable · movement unmeasured</p></div><span className="text-xs text-amber-200">Unverified</span></button>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2">{[{ label: "Selected asset", value: asset }, { label: "Price", value: "Unavailable" }, { label: "Balance", value: "Unavailable" }, { label: "Movement", value: "Unmeasured" }, { label: "Alerts", value: "Off" }, { label: "Audit", value: "Unavailable" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Evidence gate</p><h2 className="mt-2 text-2xl font-black">Before enabling tracking</h2><div className="mt-5 space-y-2">{evidence.map((item) => <button key={item} onClick={() => toggle(item)} className={`flex w-full items-center gap-3 rounded-xl border p-3 text-left ${checks.includes(item) ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><span className="flex size-5 items-center justify-center rounded border border-white/20">{checks.includes(item) ? "✓" : ""}</span><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></button>)}</div><Button disabled={checks.length === 0} className="mt-5 bg-cyan-300 text-slate-950 hover:bg-cyan-200"><ShieldCheck className="mr-2 size-4" />{checks.length}/{evidence.length} local checks selected</Button></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><LockKeyhole className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake tracking</h2><p className="mt-3 text-sm leading-6 text-slate-400">Changing mode, asset, cadence, checks, refresh, or save changes browser state only. It does not create a job, read prices, calculate performance, deliver an alert, or automate a decision.</p></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Production checklist</p><h2 className="mt-2 text-2xl font-black">Tracking must prove</h2><div className="mt-5 space-y-3">{["Authenticated scope, asset identity, chain/network, consent, privacy, and least-privilege access", "Timestamped source, cadence, stale-data and outage handling, retries, reconciliation, and retention", "Alert threshold semantics, delivery, duplicate suppression, rate limits, audit, and deletion", "Performance math, period boundaries, fees, benchmarks, uncertainty, and no-guarantee messaging", "Tests for scheduler failure, provider outage, empty/error/loading/offline states, and unsafe automation"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Tracking surface preserved", description: "Modes, assets, cadence, alerts, audit, refresh, evidence, and local save remain interactive.", icon: Activity, status: "Local planner" }, { title: "Automation is off", description: "No scheduler, market source, balance source, alert delivery, or external action is connected.", icon: ShieldAlert, status: "Guardrail" }, { title: "No financial result", description: "No movement, P&L, success rate, response time, or action prompt is fabricated.", icon: WifiOff, status: "Unavailable" }]} /></section></main></div>;
}
