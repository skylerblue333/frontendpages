import FeatureUnavailable from "@/components/FeatureUnavailable";

const MetaversePortal = () => (
  <FeatureUnavailable
    title="Metaverse portal"
    description="Metaverse spaces, identity, and connected experiences are intentionally held at a truthful release boundary until verified identity, asset ownership, content moderation, persistence, and monitoring providers are accepted."
    capability="Immersive metaverse spaces and connected assets"
    nextStep="Return to the launch hub"
  />
);

export default MetaversePortal;
