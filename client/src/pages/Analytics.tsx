import { useState } from "react";
import { BarChart3, Eye, Heart, LockKeyhole, ShieldAlert, TrendingUp, Users } from "lucide-react";
import { PageHeader } from "@/components/PageHeader";
import { Button } from "@/components/ui/button";

const metrics = [
  { id: "audience", label: "Audience", icon: Users, description: "Requires authenticated identity, event provenance, consent, deduplication, and a defined active-user window." },
  { id: "finance", label: "Finance", icon: TrendingUp, description: "Requires settled transactions, currency, fees, refunds, chargebacks, tax, period close, and reconciliation." },
  { id: "content", label: "Content", icon: BarChart3, description: "Requires persisted content events, creator identity, moderation, time windows, and privacy controls." },
] as const;
type MetricId = (typeof metrics)[number]["id"];

export default function Analytics() {
  const [metric, setMetric] = useState<MetricId>("audience");
  const active = metrics.find(item => item.id === metric) ?? metrics[0];
  const Icon = active.icon;
  return <div data-ui-polish="batch-181" className="container max-w-6xl animate-page-in py-8"><PageHeader backHref="/dashboard" icon={BarChart3} title="Analytics workspace" subtitle="Define trusted metrics before displaying a number" /><div className="mb-8 rounded-3xl border border-white/10 bg-gradient-to-br from-cyan-400/[0.12] via-purple-400/[0.08] to-transparent p-6"><div className="flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200"><ShieldAlert className="size-4" />Evidence-bounded analytics</div><h2 className="mt-4 max-w-3xl text-3xl font-black tracking-tight">Measure what the source can prove.</h2><p className="mt-3 max-w-3xl text-sm leading-6 text-slate-300">No live DAU, MAU, revenue, page views, content ranking, engagement, conversion, or growth result is shown until its authoritative source, definition, privacy policy, and reconciliation path are connected.</p></div><div className="mb-8 grid grid-cols-2 gap-4 md:grid-cols-4">{["DAU", "MAU", "MRR", "Page views"].map(label => <div key={label} className="card p-4"><p className="text-xs uppercase tracking-widest text-muted-foreground">{label}</p><div className="mt-2 text-2xl font-bold text-amber-200">Unavailable</div><p className="mt-1 text-xs text-muted-foreground">No authoritative source</p></div>)}</div><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><div className="card p-5"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Metric definitions</p><div className="mt-5 space-y-2">{metrics.map(item => { const MetricIcon = item.icon; return <button key={item.id} type="button" onClick={() => setMetric(item.id)} className={`w-full rounded-xl border p-4 text-left transition ${metric === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-border/50 hover:border-border"}`}><div className="flex items-center gap-3"><MetricIcon className="size-4 text-cyan-200" /><span className="font-semibold">{item.label}</span><span className="ml-auto text-xs text-amber-200">Needs evidence</span></div><p className="mt-2 text-sm leading-6 text-muted-foreground">{item.description}</p></button>; })}</div></div><div className="card p-5"><div className="flex items-start gap-3"><Icon className="mt-1 size-5 text-cyan-200" /><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected metric</p><h3 className="mt-2 text-2xl font-black">{active.label}</h3></div></div><div className="mt-6 flex min-h-64 flex-col items-center justify-center rounded-2xl border border-dashed border-border/60 bg-black/10 p-8 text-center"><BarChart3 className="size-10 text-muted-foreground/50" /><p className="mt-4 font-semibold">No analytics evidence loaded</p><p className="mt-2 max-w-md text-sm leading-6 text-muted-foreground">Charts remain empty until a verified telemetry or financial source provides documented data with a reproducible period and access policy.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Refresh unavailable</Button><Button disabled variant="outline">Export unavailable</Button><Button disabled variant="outline">Share unavailable</Button></div></div></section><section className="mt-8 card overflow-hidden"><div className="border-b border-border/50 p-5"><h3 className="font-semibold">Top content</h3><p className="mt-1 text-sm text-muted-foreground">Ranking is unavailable until persisted content and engagement events are verified.</p></div><div className="flex min-h-40 flex-col items-center justify-center p-8 text-center"><Eye className="size-8 text-muted-foreground/50" /><p className="mt-3 font-semibold">No content ranking loaded</p><p className="mt-2 text-sm text-muted-foreground">Views, likes, shares, creator identity, and performance are not fabricated.</p></div></section><div className="mt-8 rounded-2xl border border-dashed border-amber-300/20 bg-amber-300/[0.05] p-5 text-sm leading-6 text-muted-foreground"><strong className="text-amber-100">No analytics or financial claim is made.</strong> This is a local metric-governance workspace, not a live dashboard or business-performance report.</div><div className="mt-5 flex items-center gap-3 text-sm text-muted-foreground"><LockKeyhole className="size-4 text-amber-200" />Source ownership, privacy, retention, audit, and reconciliation are required before showing a number.</div></div>;
}
