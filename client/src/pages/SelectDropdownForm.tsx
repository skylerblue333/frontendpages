import { useState } from "react";
import { Check, ClipboardList, LockKeyhole, RefreshCw, ShieldAlert, SlidersHorizontal, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const choices = { module: ["Choose a module", "School", "Governance", "Marketplace", "Community"], mode: ["Choose a workflow", "Preview", "Review", "Configure", "Request approval"], audience: ["Choose an audience", "Private workspace", "Team review", "Public preview", "Consent-required"] };
export default function SelectDropdownForm() { const [module, setModule] = useState(choices.module[0]); const [mode, setMode] = useState(choices.mode[0]); const [audience, setAudience] = useState(choices.audience[0]); const [saved, setSaved] = useState(false); const [submitted, setSubmitted] = useState(false); const [showGates, setShowGates] = useState(false); const valid = module !== choices.module[0] && mode !== choices.mode[0] && audience !== choices.audience[0]; const reset = () => { setModule(choices.module[0]); setMode(choices.mode[0]); setAudience(choices.audience[0]); setSaved(false); setSubmitted(false); setShowGates(false); }; return <div data-ui-polish="batch-203" className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={ClipboardList} eyebrow="Select form · Interaction preview" title="Make the selection clear before sending the form." description="Explore an accessible local dropdown-form workspace with module, workflow, and audience selection, validation, saved state, reset, error handling, and blocked submission. No form record, account, workflow, approval, notification, or backend persistence is connected." badge="Form-governance workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200" disabled={!valid}><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save selection locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review form gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset form</Button></div></ScreenHero><main className="mx-auto max-w-5xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Fields", value: "3 local", hint: "No schema source", icon: ClipboardList, tone: "cyan" }, { label: "Validation", value: valid ? "Ready" : "Required", hint: "Client preview only", icon: SlidersHorizontal, tone: "violet" }, { label: "Persistence", value: "Blocked", hint: "No backend source", icon: LockKeyhole, tone: "amber" }, { label: "Submit", value: submitted ? "Blocked" : "Unavailable", hint: "No workflow source", icon: ShieldAlert, tone: "slate" }]} /><ScreenPreviewBanner title="Form evidence boundary"><strong>This is a local form-interaction preview, not evidence that a selection was saved, submitted, approved, notified, or persisted.</strong> Dropdowns, validation, saved state, reset, error handling, and disabled submission are browser concepts. No account, record, workflow, approval, notification, API response, audit event, or business outcome is asserted.</ScreenPreviewBanner><Card className="border-white/10 bg-white/[0.04]"><CardContent className="space-y-6 p-6"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Local form state</p><h2 className="mt-2 text-2xl font-black">Choose the review context</h2><p className="mt-2 text-sm leading-6 text-slate-400">These values are local browser state and intentionally do not imply a live workflow.</p></div><div className="grid gap-5 md:grid-cols-3">{[{ label: "Module", value: module, set: setModule, options: choices.module }, { label: "Workflow", value: mode, set: setMode, options: choices.mode }, { label: "Audience", value: audience, set: setAudience, options: choices.audience }].map((field) => <label key={field.label} className="text-sm font-semibold text-slate-300">{field.label}<select value={field.value} onChange={(event) => { field.set(event.target.value); setSubmitted(false); }} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white outline-none focus:border-cyan-300/50" aria-label={field.label}>{field.options.map((option) => <option key={option}>{option}</option>)}</select></label>)}</div>{!valid && <div className="rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4 text-sm text-amber-100">Select a module, workflow, and audience to enable local save. This validation does not call a backend.</div>}{submitted && <div className="rounded-xl border border-rose-300/20 bg-rose-300/[0.06] p-4 text-sm text-rose-100">Submission remains blocked because no governed destination, authorization, persistence, or workflow contract is connected.</div>}<div className="flex flex-wrap gap-2"><Button disabled={!valid} onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200">Save locally</Button><Button disabled onClick={() => setSubmitted(true)} variant="outline" className="border-white/10 text-slate-500">Submit unavailable</Button><Badge variant="outline" className={valid ? "border-emerald-300/20 text-emerald-200" : "border-amber-300/20 text-amber-200"}>{valid ? "Validation ready" : "Selection required"}</Badge></div>{showGates && <div className="rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No persistence or workflow claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A valid local selection does not prove an account, record, API, approval, notification, audit event, or completed workflow.</p></div>}</CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Form-governance gates</p><h2 className="mt-2 text-2xl font-black">What a real form must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Accessible labels, keyboard navigation, focus state, error semantics, required fields, dependent options, and safe defaults", "Authenticated user, tenant, form schema, version, locale, timestamp, destination, permission, and submission provenance", "Server-side validation, idempotency, CSRF protection, rate limits, authorization, persistence, error recovery, and audit", "Payment, financial, crypto, education, healthcare, legal, security, privacy, and user-impact claims require domain review", "Save, submit, approve, notify, export, retry, accessibility, and accountable approval require governed form operations", "A form preview must not be presented as a saved record, approved request, transaction, or completed workflow without evidence"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Form surface preserved", description: "Module, workflow, audience, validation, save, reset, error, submit, accessibility, and gates remain interactive.", icon: ClipboardList, status: "Local form" }, { title: "No submission theater", description: "Records, accounts, workflows, approvals, notifications, API responses, and outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Contract before submit", description: "Real forms require schema, auth, validation, persistence, error recovery, rate limits, CSRF protection, and audit.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>; }
