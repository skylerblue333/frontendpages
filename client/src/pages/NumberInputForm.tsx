import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Calculator, Check, FileCheck2, Hash, LockKeyhole, RefreshCw, ShieldCheck, SlidersHorizontal, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

export default function NumberInputForm() {
  const [first, setFirst] = useState("125");
  const [second, setSecond] = useState("32.5");
  const [precision, setPrecision] = useState("2");
  const [saved, setSaved] = useState(false);
  const result = useMemo(() => { const a = Number(first); const b = Number(second); return Number.isFinite(a) && Number.isFinite(b) ? (a + b).toFixed(Number(precision)) : "—"; }, [first, second, precision]);
  const valid = first.trim() !== "" && second.trim() !== "" && Number.isFinite(Number(first)) && Number.isFinite(Number(second));
  const reset = () => { setFirst("125"); setSecond("32.5"); setPrecision("2"); setSaved(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Calculator} eyebrow="NumberInputForm · Validation worksheet" title="Make the number explicit before submitting it." description="Test labeled numeric inputs, validation, precision, and a transparent local sum. No form submission, account record, financial meaning, server calculation, authorization, or backend result is claimed." badge="Form preview"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} disabled={!valid} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Draft saved locally" : "Save draft locally"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset form</Button></div></ScreenHero><main className="mx-auto max-w-6xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Inputs", value: "2", hint: valid ? "Valid local values" : "Review values", icon: Hash }, { label: "Result", value: result, hint: "Local sum", icon: Calculator, tone: "cyan" }, { label: "Precision", value: `${precision} dp`, hint: "Local format", icon: SlidersHorizontal, tone: "violet" }, { label: "Submit", value: "Off", hint: "No backend", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="NumberInputForm evidence boundary"><strong>Inputs, validation messages, precision, and the sum are local form fixtures—not a payment, quantity, balance, score, financial calculation, account update, server validation, authorization, or accepted submission.</strong> Production numeric forms require field semantics, units/currency, min/max and precision rules, locale handling, accessibility, server-side validation, authorization, idempotency, error recovery, persistence, and auditability.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Numeric inputs</p><h2 className="mt-2 text-3xl font-black">Local validation</h2><div className="mt-5 space-y-5"><label className="block"><span className="text-sm font-semibold text-slate-300">Base value</span><input type="number" value={first} onChange={(event) => { setFirst(event.target.value); setSaved(false); }} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-white" /><span className="mt-2 block text-xs text-slate-500">Required · unit not configured</span></label><label className="block"><span className="text-sm font-semibold text-slate-300">Adjustment</span><input type="number" value={second} onChange={(event) => { setSecond(event.target.value); setSaved(false); }} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-white" /><span className="mt-2 block text-xs text-slate-500">Required · sign and unit policy not configured</span></label><label className="block"><span className="text-sm font-semibold text-slate-300">Display precision</span><select value={precision} onChange={(event) => { setPrecision(event.target.value); setSaved(false); }} className="mt-2 w-full rounded-xl border border-white/10 bg-slate-950 px-3 py-3 text-white"><option value="0">0 decimal places</option><option value="2">2 decimal places</option><option value="4">4 decimal places</option></select></label>{!valid && <p className="rounded-xl border border-amber-300/20 bg-amber-300/[0.05] p-3 text-sm text-amber-200">Enter finite numeric values in both fields before saving the local draft.</p>}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Derived preview</p><h2 className="mt-2 text-3xl font-black">Local arithmetic</h2><div className="mt-5 rounded-2xl border border-violet-300/20 bg-violet-300/[0.05] p-6"><p className="text-xs uppercase tracking-[0.2em] text-violet-200">Base + adjustment</p><p className="mt-3 text-5xl font-black text-violet-100">{result}</p><p className="mt-3 text-sm text-slate-400">{first || "—"} + {second || "—"} · {precision} decimal places</p></div><div className="mt-5 grid gap-3 sm:grid-cols-2">{[{ label: "Validation", value: valid ? "Pass locally" : "Needs input" }, { label: "Unit", value: "Unset" }, { label: "Currency", value: "Unset" }, { label: "Server result", value: "Unavailable" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 flex items-start gap-3 text-xs leading-5 text-slate-500"><LockKeyhole className="mt-0.5 size-4 shrink-0 text-cyan-300" />The calculation is browser-local and has no financial, inventory, scoring, or account meaning.</div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Form readiness</p><h2 className="mt-2 text-2xl font-black">Required before submission claims</h2><div className="mt-5 space-y-3">{["Field labels, units, min/max, precision, locale, and accessibility", "Server validation, authorization, idempotency, and error handling", "Persistence, auditability, retry semantics, and duplicate prevention", "Domain meaning, currency/quantity policy, rounding, and reconciliation", "Privacy, support, loading, success, failure, and recovery states"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><FileCheck2 className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake submission</h2><p className="mt-3 text-sm leading-6 text-slate-400">This worksheet changes local state only. It does not submit, persist, authorize, calculate server-side, or claim acceptance.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "A number needs meaning", description: "Labels, units, ranges, precision, and domain semantics prevent misleading numeric UX.", icon: Calculator, status: "Guardrail" }, { title: "Client validation is not trust", description: "Server validation, authorization, idempotency, persistence, and audit need evidence.", icon: ShieldCheck, status: "Required" }, { title: "No fake submit", description: "The preview makes no API request and creates no account, transaction, inventory, or financial record.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
