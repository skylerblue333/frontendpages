import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Accessibility, Check, Circle, Flag, Gauge, LockKeyhole, RefreshCw, ShieldAlert, Target, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const milestones = [{ id: "foundation", label: "Foundation", detail: "Define the scope and evidence", status: "Not started" }, { id: "review", label: "Review", detail: "Validate the work with sources", status: "Not started" }, { id: "ready", label: "Ready", detail: "Pass governed release gates", status: "Blocked" }];
export default function ProgressBar() {
  const [selected, setSelected] = useState(milestones[0].id);
  const [done, setDone] = useState<string[]>([]);
  const [pace, setPace] = useState("No estimate");
  const [saved, setSaved] = useState(false);
  const [showBoundary, setShowBoundary] = useState(false);
  const current = milestones.find((item) => item.id === selected) ?? milestones[0];
  const completion = useMemo(() => Math.round((done.length / milestones.length) * 100), [done.length]);
  const toggle = (id: string) => setDone((items) => items.includes(id) ? items.filter((item) => item !== id) : [...items, id]);
  const reset = () => { setSelected(milestones[0].id); setDone([]); setPace("No estimate"); setSaved(false); setShowBoundary(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Gauge} eyebrow="ProgressBar · Visualization preview" title="Show movement without pretending progress is proven." description="Explore a local progress visualization with milestone selection, checklist intent, pace labels, accessibility, and blocked readiness. Completion is browser state only; no user, task, time, achievement, or account event is measured." badge="Progress workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Progress saved locally" : "Save progress locally"}</Button><Button onClick={() => setShowBoundary((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showBoundary ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showBoundary ? "Close boundary" : "Review boundary"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset progress</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Progress", value: `${completion}%`, hint: "Local visualization", icon: Gauge, tone: "cyan" }, { label: "Milestones", value: `${done.length}/${milestones.length}`, hint: "Browser state", icon: Flag, tone: "violet" }, { label: "Pace", value: pace, hint: "No time source", icon: Target, tone: "amber" }, { label: "Readiness", value: "Blocked", hint: "Evidence required", icon: LockKeyhole, tone: "slate" }]} /><ScreenPreviewBanner title="Progress evidence boundary"><strong>This is a local progress visualization, not a completion record.</strong> The percentage, checklist, milestones, pace intent, selected detail, and saved state are browser concepts. No user, task, course, project, time estimate, achievement, certification, completion event, or readiness claim is persisted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.82fr_1.18fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Milestone track</p><h2 className="mt-2 text-2xl font-black">Choose a step</h2></div><Badge variant="outline" className="border-white/10 text-amber-200">{completion}% local</Badge></div><div className="mt-6 h-3 overflow-hidden rounded-full bg-white/10"><div className="h-full rounded-full bg-gradient-to-r from-cyan-300 to-violet-300 transition-all" style={{ width: `${completion}%` }} /></div><div className="mt-6 space-y-3">{milestones.map((item, index) => <button key={item.id} onClick={() => setSelected(item.id)} className={`flex w-full items-center gap-3 rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><button aria-label={`toggle ${item.label}`} onClick={(event) => { event.stopPropagation(); toggle(item.id); }} className={`flex size-6 items-center justify-center rounded-full border ${done.includes(item.id) ? "border-cyan-300 bg-cyan-300 text-slate-950" : "border-white/20 text-slate-600"}`}>{done.includes(item.id) ? <Check className="size-3" /> : <span className="text-xs">{index + 1}</span>}</button><div className="flex-1"><p className="font-semibold">{item.label}</p><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><Badge variant="outline" className={done.includes(item.id) ? "border-cyan-300/30 text-cyan-200" : "border-white/10 text-slate-500"}>{done.includes(item.id) ? "Local done" : item.status}</Badge></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected detail</p><h2 className="mt-2 text-2xl font-black">{current.label}</h2><p className="mt-2 text-sm text-slate-500">{current.detail}</p><div className="mt-6 rounded-xl border border-white/10 p-5"><p className="text-sm leading-7 text-slate-300">Use this milestone to document a local intent, then connect it to a real task or learning record only after ownership, evidence, status transitions, and audit behavior are defined.</p></div><div className="mt-6"><p className="text-sm text-slate-400">Pace label</p><div className="mt-2 flex flex-wrap gap-2">{["No estimate", "Exploratory", "Time-boxed later"].map((item) => <button key={item} onClick={() => setPace(item)} className={`rounded-lg border px-3 py-2 text-xs ${pace === item ? "border-violet-300/40 bg-violet-300/10 text-violet-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div></div>{showBoundary && <div className="mt-6 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No completion claim</p><p className="mt-2 text-sm leading-6 text-slate-400">Local checkmarks do not verify work, time, assessment, certification, reward eligibility, or readiness.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Progress gates</p><h2 className="mt-2 text-2xl font-black">What a real progress record must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Authenticated owner and scoped task or course record", "Defined state machine, timestamps, dependencies, and audit trail", "Source evidence for completion with reviewer or assessment rules", "Accessibility, retry, recovery, reset, and offline reconciliation", "Privacy, retention, export, deletion, and notification behavior", "No reward, certification, promotion, or readiness without governed criteria"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><Circle className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Progress surface preserved", description: "Milestones, local percentage, checklist toggles, selected detail, pace intent, accessibility, save/reset, and blocked readiness remain interactive.", icon: Gauge, status: "Local visualization" }, { title: "No completion theater", description: "Users, tasks, time, achievements, certifications, rewards, and readiness are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before certainty", description: "Real progress needs ownership, state transitions, proof, audit, privacy, recovery, and governed criteria.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
