import { useState } from "react";
import { Activity, BarChart3, Check, EyeOff, Filter, LockKeyhole, RefreshCw, ShieldAlert, Star, TrendingUp, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";
const indicators = [{ name: "Relative strength index", code: "RSI", detail: "Momentum concept requiring a verified price series, interval, adjustment policy, and calculation definition.", status: "Unconfigured" }, { name: "Moving-average convergence", code: "MACD", detail: "Trend concept requiring authoritative candles, lookback settings, timezone, and reproducible calculation.", status: "Unavailable" }, { name: "Volatility bands", code: "Bands", detail: "Range concept requiring market source, sampling interval, missing-data policy, and reviewed interpretation.", status: "Preview" }];
export default function TechnicalIndicators() { const [selected, setSelected] = useState(0); const [range, setRange] = useState("1D"); const [saved, setSaved] = useState(false); const [refreshing, setRefreshing] = useState(false); const [status, setStatus] = useState("Market-data provider unavailable. Showing indicator concepts only."); const retry = () => { setRefreshing(true); setStatus("Retrying market-data provider…"); window.setTimeout(() => { setRefreshing(false); setStatus("Market-data provider unavailable. No verified price series was connected."); }, 500); }; const reset = () => { setSelected(0); setRange("1D"); setSaved(false); setStatus("Market-data provider unavailable. Showing indicator concepts only."); }; const indicator = indicators[selected] ?? indicators[0]; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Activity} eyebrow="Technical indicators · Market-analysis preview" title="Read indicator concepts without inventing prices, signals, or trading advice." description="Explore a local technical-analysis workspace with indicator selection, interval intent, chart-reading concepts, watchlist state, provenance, calculation reproducibility, uncertainty, retry, save/reset feedback, and explicit evidence boundaries. No live price, signal, recommendation, order, or financial outcome is connected." badge="Evidence-bounded market data"><div className="flex flex-wrap gap-2"><Button onClick={() => { setSaved(true); setStatus("Indicator view saved locally. No market order or account state was changed."); }} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save indicator view"}</Button><Button onClick={retry} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className={`mr-2 size-4 ${refreshing ? "animate-spin" : ""}`} />{refreshing ? "Retrying source" : "Retry market source"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><X className="mr-2 size-4" />Reset analysis</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Price series", value: "Unavailable", hint: "No provider source", icon: BarChart3, tone: "cyan" }, { label: "Indicators", value: "Concepts", hint: "No calculation engine", icon: Activity, tone: "violet" }, { label: "Signals", value: "Withheld", hint: "No advice source", icon: TrendingUp, tone: "amber" }, { label: "Watchlist", value: "Local", hint: "No account persistence", icon: Star, tone: "slate" }]} /><ScreenPreviewBanner title="Market-data evidence boundary"><strong>This is a local technical-analysis preview, not evidence of a live asset price, volume, indicator value, signal, trend, forecast, recommendation, trade, portfolio result, or financial outcome.</strong> Indicator cards, interval buttons, local saved state, retry feedback, and disabled alert/order actions are browser concepts. No investment advice or trading conclusion is asserted.</ScreenPreviewBanner><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex flex-wrap items-center justify-between gap-4"><div className="flex flex-wrap gap-2">{["1H", "1D", "1W", "1M"].map((item) => <Button key={item} onClick={() => setRange(item)} size="sm" variant={range === item ? "default" : "outline"} className={range === item ? "bg-cyan-300 text-slate-950 hover:bg-cyan-200" : "border-white/10 bg-white/5 text-white hover:bg-white/10"}>{item}</Button>)}</div><div className="flex items-center gap-2 text-xs text-slate-400"><Filter className="size-4" />Interval intent: {range}</div></div></CardContent></Card><section className="grid gap-4 md:grid-cols-3">{indicators.map((item, index) => <button key={item.code} onClick={() => setSelected(index)} className={`rounded-2xl border p-5 text-left transition ${selected === index ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10 bg-white/[0.04]"}`}><div className="flex items-start justify-between"><div className="flex size-11 items-center justify-center rounded-2xl bg-cyan-300/10 text-cyan-200"><Activity className="size-5" /></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.status}</Badge></div><h2 className="mt-4 font-bold">{item.name}</h2><p className="mt-1 text-xs text-cyan-200">{item.code} · {range} intent</p><p className="mt-3 text-sm leading-6 text-slate-400">{item.detail}</p></button>)}</section><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200">Selected indicator concept</p><h2 className="mt-2 text-2xl font-black">{indicator.name}</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{indicator.status}</Badge></div><p className="mt-3 text-sm leading-6 text-slate-400">{indicator.detail}</p><div className="mt-6 rounded-2xl border border-dashed border-white/15 bg-black/20 p-6"><div className="flex items-center justify-between"><div><p className="text-xs uppercase tracking-[0.2em] text-slate-500">Chart surface</p><p className="mt-2 text-lg font-bold">No verified series loaded</p></div><EyeOff className="size-6 text-amber-300" /></div><div className="mt-6 flex h-24 items-end gap-2 opacity-40">{[35, 58, 42, 76, 52, 64, 46, 70, 38, 56, 48, 68].map((height, index) => <div key={index} className="flex-1 rounded-t bg-gradient-to-t from-cyan-400 to-violet-400" style={{ height: `${height}%` }} />)}</div><p className="mt-4 text-xs text-amber-200">Illustrative UI only · bars are not prices, returns, or a prediction.</p></div><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Asset / provider", value: "Unavailable" }, { label: "Interval / timezone", value: `${range} · Unconfigured` }, { label: "Formula / lookback", value: "Unconfigured" }, { label: "Signal confidence", value: "Withheld" }, { label: "Data freshness", value: "Unverified" }, { label: "Advice / order", value: "Blocked" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-4"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-1 text-sm text-amber-200">{item.value}</p></div>)}</div><div className="mt-6 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Calculate unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Create alert unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Trade unavailable</Button></div></CardContent></Card><ScreenStatePanel type="unavailable" title="Market-data provider unavailable" description="Connect authoritative price data, symbol mapping, timestamps, calculation definitions, reproducibility, user authorization, risk disclosures, and order controls before enabling analysis or trading actions." /></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><LockKeyhole className="size-5 text-cyan-300" /><div><p className="font-bold">Indicator evidence contract</p><p className="mt-1 text-sm text-slate-400">Trustworthy analysis requires source provenance, asset identity, timestamps, market adjustments, formula versions, missing-data policy, uncertainty, validation, user suitability, and clear separation from advice.</p></div></div><div className="mt-5 grid gap-3 md:grid-cols-2">{["Asset identity, market, provider, timestamp, currency, and source provenance", "OHLCV completeness, adjustments, interval, timezone, freshness, and correction", "Formula definition, lookback, parameter version, reproducibility, and validation", "Uncertainty, lag, regime limits, false signals, and no-guarantee disclosure", "User account, watchlist, alert, suitability, privacy, and notification controls", "Order authority, confirmation, risk controls, execution, receipt, and recovery"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3 text-sm text-slate-300"><LockKeyhole className="size-4 text-amber-300" />{item}<span className="ml-auto text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Analysis surface preserved", description: "Indicator concepts, intervals, chart surface, watchlist intent, provenance, retry, save/reset, and action gates remain visible.", icon: BarChart3, status: "Local preview" }, { title: "No signal theater", description: "Prices, indicator values, trends, forecasts, confidence, recommendations, trades, and financial outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before action", description: "Real analysis requires authoritative data, reproducible formulas, uncertainty, disclosures, authorization, and execution controls.", icon: LockKeyhole, status: "Blocked" }]} /><p aria-live="polite" className="rounded-xl border border-white/10 bg-white/[0.03] p-4 text-sm text-slate-400">{status}</p></main></div>; }
