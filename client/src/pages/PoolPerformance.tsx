import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Activity, BarChart3, Check, Clock3, Cpu, Database, Gauge, LockKeyhole, Pickaxe, RefreshCw, Search, ShieldCheck, TriangleAlert, Wallet, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

type Tab = "Pool" | "Workers" | "Earnings" | "Controls";
type Worker = { id: string; name: string; state: "Fixture" | "Not connected"; note: string };
const workers: Worker[] = [
  { id: "worker-001", name: "Worker concept A", state: "Fixture", note: "No miner identity, share stream, or device telemetry." },
  { id: "worker-002", name: "Worker concept B", state: "Fixture", note: "No hashrate, accepted shares, or heartbeat is measured." },
  { id: "worker-003", name: "Worker concept C", state: "Not connected", note: "No pool account, wallet, or worker credential exists." },
];
const requirements = ["Pool endpoint and network identity", "Worker heartbeat and share accounting", "Hashrate, difficulty, rejects, and stale-share telemetry", "Wallet ownership, payout threshold, fee, and transaction proof", "Power cost, hardware cost, emissions, and profitability model"];
export default function PoolPerformance() {
  const [tab, setTab] = useState<Tab>("Pool");
  const [window, setWindow] = useState("24h");
  const [query, setQuery] = useState("");
  const [selected, setSelected] = useState(workers[0]);
  const [ack, setAck] = useState<string[]>([]);
  const [refreshed, setRefreshed] = useState(false);
  const visible = useMemo(() => workers.filter((item) => !query || `${item.name} ${item.state} ${item.note}`.toLowerCase().includes(query.toLowerCase())), [query]);
  const toggle = (item: string) => setAck((items) => items.includes(item) ? items.filter((value) => value !== item) : [...items, item]);
  const reset = () => { setTab("Pool"); setWindow("24h"); setQuery(""); setSelected(workers[0]); setAck([]); setRefreshed(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Pickaxe} eyebrow="PoolPerformance · Mining preview" title="Study pool performance without inventing mining results." description="Explore pool, worker, earnings, and control concepts with local windows and evidence requirements. No mining pool, worker, chain, wallet, share stream, hashrate, payout, uptime, energy, or profitability source is connected." badge="Mining performance preview"><div className="flex flex-wrap gap-2"><Button onClick={() => setRefreshed(true)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />{refreshed ? "Preview refreshed" : "Refresh preview"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">Reset worksheet</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Workers", value: String(workers.length), hint: "Local concepts", icon: Cpu, tone: "cyan" }, { label: "Window", value: window, hint: "Local selection", icon: Clock3, tone: "violet" }, { label: "Hashrate", value: "Unset", hint: "No telemetry", icon: Gauge, tone: "amber" }, { label: "Payout", value: "Off", hint: "No wallet source", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="Mining evidence boundary"><strong>This is not a mining dashboard or earnings statement.</strong> Worker rows, window choices, performance labels, share states, payout states, and controls are local planning data. They do not prove mining activity, hashrate, accepted shares, network difficulty, uptime, wallet ownership, payouts, energy cost, emissions, or profitability.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.82fr_1fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Performance surfaces</p><h2 className="mt-2 text-2xl font-black">Choose a worksheet</h2><div className="mt-5 grid gap-2">{(["Pool", "Workers", "Earnings", "Controls"] as Tab[]).map((item) => <button key={item} onClick={() => setTab(item)} className={`flex items-center gap-3 rounded-xl border p-3 text-left ${tab === item ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10 text-slate-400"}`}><div className="flex size-9 items-center justify-center rounded-xl bg-white/10">{item === "Pool" ? <Database className="size-4" /> : item === "Workers" ? <Cpu className="size-4" /> : item === "Earnings" ? <Wallet className="size-4" /> : <ShieldCheck className="size-4" />}</div><span className="font-semibold">{item}</span><span className="ml-auto text-xs text-slate-500">Local</span></button>)}</div><p className="mt-6 text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Time window</p><div className="mt-3 flex gap-2">{["1h", "24h", "7d", "30d"].map((item) => <button key={item} onClick={() => setWindow(item)} className={`rounded-lg border px-3 py-2 text-xs ${window === item ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div><div className="relative mt-6"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search worker concepts…" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-9 pr-3 text-sm text-white outline-none focus:border-cyan-300/50" /></div><div className="mt-6 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex gap-3"><TriangleAlert className="mt-0.5 size-5 shrink-0 text-amber-200" /><p className="text-sm leading-6 text-slate-300">A selected window is not telemetry. Real mining metrics require authenticated pool and chain sources with timestamps, scope, reconciliation, and failure states.</p></div></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">{tab} worksheet</p><h2 className="mt-2 text-2xl font-black">Mining concepts</h2></div><Badge variant="outline" className="border-white/10 text-amber-200">Source off</Badge></div>{tab === "Workers" ? <div className="mt-5 space-y-3">{visible.map((item) => <button key={item.id} onClick={() => setSelected(item)} className={`w-full rounded-xl border p-4 text-left ${selected.id === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-center justify-between gap-3"><p className="font-semibold">{item.name}</p><Badge variant="outline" className="border-white/10 text-amber-200">{item.state}</Badge></div><p className="mt-2 text-sm leading-6 text-slate-500">{item.note}</p></button>)}</div> : <div className="mt-5 grid gap-3 sm:grid-cols-2">{[{ label: "Pool endpoint", value: "Unset" }, { label: "Network", value: "Unset" }, { label: "Hashrate", value: "Not measured" }, { label: "Shares", value: "No source" }, { label: "Payouts", value: "Off" }, { label: "Profitability", value: "Not modeled" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-4"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-lg font-black text-amber-200">{item.value}</p></div>)}</div>}<div className="mt-5 rounded-xl border border-white/10 p-4"><p className="text-xs text-slate-500">Selected concept</p><p className="mt-2 text-lg font-black">{selected.name}</p><p className="mt-2 text-sm leading-6 text-slate-400">{selected.note}</p><div className="mt-4 grid gap-3 sm:grid-cols-3">{[{ label: "Window", value: window }, { label: "Telemetry", value: "Unavailable" }, { label: "Wallet", value: "Unverified" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div></div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Evidence checklist</p><h2 className="mt-2 text-2xl font-black">Before trusting performance</h2><div className="mt-5 space-y-2">{requirements.map((item) => <button key={item} onClick={() => toggle(item)} className={`flex w-full items-center gap-3 rounded-xl border p-3 text-left ${ack.includes(item) ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><span className="flex size-5 items-center justify-center rounded border border-white/20">{ack.includes(item) ? "✓" : ""}</span><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></button>)}</div><Button disabled={ack.length === 0} className="mt-5 bg-cyan-300 text-slate-950 hover:bg-cyan-200"><BarChart3 className="mr-2 size-4" />{ack.length}/{requirements.length} local checks selected</Button></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><LockKeyhole className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake mining result</h2><p className="mt-3 text-sm leading-6 text-slate-400">Changing tabs, windows, selecting a worker, refreshing, or checking evidence changes browser state only. It does not mine, query a pool, measure hashrate, generate a payout, or calculate profitability.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Pool surface preserved", description: "Pool, worker, earnings, control, window, search, selection, and evidence concepts remain interactive.", icon: Pickaxe, status: "Local worksheet" }, { title: "Financial claims are off", description: "Payouts, wallet ownership, energy cost, emissions, and profitability remain unavailable.", icon: Wallet, status: "Guardrail" }, { title: "Telemetry is off", description: "No hashrate, shares, difficulty, uptime, rejects, transactions, or success rate is fabricated.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
