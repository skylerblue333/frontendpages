import { useState } from "react";
import { BarChart3, Download, Filter, LockKeyhole, RefreshCw, ShieldAlert, SlidersHorizontal } from "lucide-react";
import { Button } from "@/components/ui/button";

const metrics = [
  { name: "Revenue", requirement: "Settled transaction provenance, currency, fees, refunds, reversals, and reconciliation." },
  { name: "Engagement", requirement: "Authenticated event definitions, consent, identity stitching, time zone, and retention policy." },
  { name: "Conversion", requirement: "A defined funnel, source events, deduplication, cohort rules, and reviewable denominator." },
  { name: "Platform usage", requirement: "Authoritative route events, privacy controls, sampling policy, and reproducible aggregation." },
];

export default function AdvancedAnalytics() {
  const [range, setRange] = useState("Period not configured");
  const [metric, setMetric] = useState("Revenue");
  const [saved, setSaved] = useState(false);
  const selected = metrics.find(item => item.name === metric) ?? metrics[0];

  return (
    <main className="min-h-screen bg-[#070a16] text-white">
      <header className="border-b border-white/10 bg-[#070a16]/90 px-4 py-4 backdrop-blur-xl"><div data-ui-polish="batch-180" className="mx-auto flex max-w-6xl items-center gap-3"><div className="flex size-10 items-center justify-center rounded-2xl bg-cyan-300/10 text-cyan-200"><BarChart3 className="size-5" /></div><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200/70">Analytics governance</p><h1 className="text-xl font-black">Advanced analytics</h1></div><span className="ml-auto rounded-full border border-amber-300/20 bg-amber-300/[0.06] px-3 py-1 text-xs text-amber-100">No live data loaded</span></div></header>
      <section className="mx-auto max-w-6xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><div className="rounded-3xl border border-white/10 bg-gradient-to-br from-violet-400/[0.14] via-cyan-400/[0.06] to-transparent p-6 sm:p-10"><div className="flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200"><ShieldAlert className="size-4" />Evidence-bounded reporting</div><h2 className="mt-4 max-w-3xl text-4xl font-black tracking-tight sm:text-5xl">Measure only what the source can prove.</h2><p className="mt-5 max-w-3xl text-base leading-7 text-slate-300">This screen preserves reporting controls and metric-design workflow without fabricating revenue, active users, engagement, conversion, platform distribution, forecasts, or performance outcomes. No live telemetry or financial source is connected.</p><div className="mt-7 flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200">{saved ? "View saved locally" : "Save view locally"}</Button><Button onClick={() => setSaved(false)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset view</Button></div></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{["Revenue", "Active users", "Conversion", "Session time"].map(label => <div key={label} className="rounded-2xl border border-white/10 bg-white/[0.04] p-5"><p className="text-xs uppercase tracking-widest text-slate-500">{label}</p><p className="mt-2 text-2xl font-black text-amber-200">Unavailable</p><p className="mt-1 text-sm text-slate-500">No authoritative source</p></div>)}</div><div className="grid gap-3 md:grid-cols-[1fr_1fr_auto]"><label className="text-sm text-slate-400">Reporting period<select value={range} onChange={event => setRange(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Period not configured</option><option>Last 7 days intent</option><option>Last 30 days intent</option><option>Last 90 days intent</option><option>Last year intent</option></select></label><label className="text-sm text-slate-400">Metric definition<select value={metric} onChange={event => setMetric(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none">{metrics.map(item => <option key={item.name}>{item.name}</option>)}</select></label><div className="flex items-end"><Button disabled className="w-full bg-slate-700 text-slate-400"><Filter className="mr-2 size-4" />Apply unavailable</Button></div></div><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><div className="rounded-2xl border border-white/10 bg-white/[0.04] p-6"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected definition</p><h3 className="mt-2 text-2xl font-black">{selected.name}</h3></div><SlidersHorizontal className="size-5 text-slate-500" /></div><p className="mt-3 text-sm leading-6 text-slate-400">{selected.requirement}</p><div className="mt-6 flex min-h-64 flex-col items-center justify-center rounded-2xl border border-dashed border-white/15 bg-black/20 p-8 text-center"><BarChart3 className="size-10 text-slate-600" /><p className="mt-4 font-semibold">No analytics evidence loaded</p><p className="mt-2 max-w-md text-sm leading-6 text-slate-500">Charts remain empty until the application receives an approved telemetry or financial data source with documented metric definitions, privacy controls, provenance, and reconciliation.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400"><Download className="mr-2 size-4" />Export unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Refresh unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Schedule unavailable</Button></div></div><div className="rounded-2xl border border-white/10 bg-white/[0.04] p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Evidence checklist</p><h3 className="mt-2 text-2xl font-black">Before showing a number</h3><div className="mt-6 space-y-3">{["Source ownership and access", "Metric definition and denominator", "Time zone and period close", "Privacy, consent, and retention", "Reconciliation and backfill policy", "Role-based access and audit"].map(item => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-4 text-sm text-slate-300"><LockKeyhole className="size-4 text-amber-200" />{item}<span className="ml-auto text-xs text-amber-200">Required</span></div>)}</div></div></section><div className="rounded-2xl border border-dashed border-white/15 bg-amber-300/[0.04] p-6 text-sm leading-6 text-slate-400"><strong className="text-amber-100">No analytics or financial claim is made.</strong> This is a local reporting-governance workspace. It does not establish revenue, users, conversion, engagement, profit, payment, business performance, or real-time operational status.</div></section>
    </main>
  );
}
