import FeatureUnavailable from "@/components/FeatureUnavailable";

const UnifiedFeed = () => (
  <FeatureUnavailable
    title="Unified feed"
    description="Feed ranking, engagement, reactions, moderation, and activity outcomes are intentionally held at a truthful release boundary until verified content persistence, identity, moderation, ranking, and delivery services are connected."
    capability="Cross-module feed and activity delivery"
    nextStep="Return to the launch hub"
  />
);

export default UnifiedFeed;
