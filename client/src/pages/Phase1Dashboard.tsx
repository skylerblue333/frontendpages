import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { BarChart3, Check, ClipboardList, FileText, Flag, Gauge, Lightbulb, LockKeyhole, MessageSquare, RefreshCw, Search, ShieldCheck, TriangleAlert, Users, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

type Track = "Roadmap" | "Priorities" | "Debate";
type Initiative = { id: string; name: string; stage: "Discovery" | "Design" | "Build"; priority: number; rationale: string; evidence: string };
const initiatives: Initiative[] = [
  { id: "mobile", name: "Mobile experience", stage: "Discovery", priority: 9, rationale: "Explore responsive flows and offline expectations.", evidence: "Needs user research and device profiling" },
  { id: "ai", name: "AI personalization", stage: "Design", priority: 8, rationale: "Define useful personalization with consent and controls.", evidence: "Needs evaluation set and privacy review" },
  { id: "community", name: "Community features", stage: "Discovery", priority: 7, rationale: "Map safe participation and moderation needs.", evidence: "Needs representative research and abuse cases" },
  { id: "api", name: "Scalable API", stage: "Design", priority: 6, rationale: "Define contracts, limits, observability, and recovery.", evidence: "Needs workload baseline and architecture review" },
];
const perspectives = [
  { role: "Product", view: "Clarify the user problem before expanding scope.", confidence: "Hypothesis" },
  { role: "Design", view: "Use progressive disclosure and accessible states.", confidence: "Design review" },
  { role: "Engineering", view: "Prove contracts, telemetry, and rollback before launch.", confidence: "Architecture review" },
  { role: "Risk", view: "Treat privacy, authorization, and failure recovery as launch gates.", confidence: "Control review" },
];
export default function Phase1Dashboard() {
  const [track, setTrack] = useState<Track>("Roadmap");
  const [query, setQuery] = useState("");
  const [selected, setSelected] = useState(initiatives[0]);
  const [checked, setChecked] = useState<string[]>([]);
  const [note, setNote] = useState("");
  const [saved, setSaved] = useState(false);
  const visible = useMemo(() => initiatives.filter((item) => !query || `${item.name} ${item.stage} ${item.rationale}`.toLowerCase().includes(query.toLowerCase())), [query]);
  const reset = () => { setTrack("Roadmap"); setQuery(""); setSelected(initiatives[0]); setChecked([]); setNote(""); setSaved(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Flag} eyebrow="Phase1Dashboard · Roadmap preview" title="Plan the first phase without inventing progress." description="Explore local roadmap tracks, initiative priority assumptions, debate perspectives, and launch evidence. No users, revenue, consensus, timeline, uptime, transaction volume, success rate, or delivery outcome is verified." badge="Phase planning preview"><div className="flex flex-wrap gap-2"><Button disabled={checked.length === 0} onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Plan saved locally" : "Save local plan"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset dashboard</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Initiatives", value: String(initiatives.length), hint: "Local roadmap", icon: Flag, tone: "cyan" }, { label: "Selected", value: String(checked.length), hint: "Evidence tasks", icon: ClipboardList, tone: "violet" }, { label: "Phase", value: "1", hint: "Planning only", icon: Gauge, tone: "amber" }, { label: "Progress", value: "Unmeasured", hint: "No live telemetry", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="Phase-plan evidence boundary"><strong>This is a roadmap and decision worksheet, not a delivery dashboard.</strong> Priority scores, stages, perspectives, confidence labels, and selected tasks are local planning data. They do not prove user count, revenue, market share, team consensus, completion, schedule, system performance, or launch readiness.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.82fr_1fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Phase tracks</p><h2 className="mt-2 text-2xl font-black">Choose a planning lens</h2><div className="mt-5 grid gap-2">{(["Roadmap", "Priorities", "Debate"] as Track[]).map((item) => <button key={item} onClick={() => setTrack(item)} className={`flex items-center gap-3 rounded-xl border p-3 text-left ${track === item ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10 text-slate-400"}`}><div className="flex size-9 items-center justify-center rounded-xl bg-white/10">{item === "Roadmap" ? <Flag className="size-4" /> : item === "Priorities" ? <BarChart3 className="size-4" /> : <MessageSquare className="size-4" />}</div><span className="font-semibold">{item}</span><span className="ml-auto text-xs text-slate-500">Local</span></button>)}</div><div className="relative mt-6"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search initiatives…" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-9 pr-3 text-sm text-white outline-none focus:border-cyan-300/50" /></div><div className="mt-6 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex gap-3"><TriangleAlert className="mt-0.5 size-5 shrink-0 text-amber-200" /><p className="text-sm leading-6 text-slate-300">A priority score is a decision aid, not evidence of demand, revenue, urgency, or expected return. Record the rationale and validate it.</p></div></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">{track} view</p><h2 className="mt-2 text-2xl font-black">Phase-one initiatives</h2></div><Badge variant="outline" className="border-white/10 text-amber-200">Evidence pending</Badge></div><div className="mt-5 space-y-3">{visible.map((item) => <button key={item.id} onClick={() => setSelected(item)} className={`w-full rounded-xl border p-4 text-left ${selected.id === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-center justify-between gap-3"><p className="font-semibold">{item.name}</p><div className="flex gap-2"><Badge variant="outline" className="border-white/10 text-cyan-200">{item.stage}</Badge><Badge variant="outline" className="border-white/10 text-amber-200">P{item.priority}</Badge></div></div><p className="mt-2 text-sm text-slate-400">{item.rationale}</p><p className="mt-2 text-xs text-slate-500">Evidence: {item.evidence}</p></button>)}</div><div className="mt-5 rounded-xl border border-white/10 p-4"><p className="text-xs text-slate-500">Selected initiative</p><p className="mt-2 text-lg font-black">{selected.name}</p><p className="mt-2 text-sm leading-6 text-slate-400">{selected.rationale}</p><button onClick={() => setChecked((items) => items.includes(selected.id) ? items.filter((id) => id !== selected.id) : [...items, selected.id])} className={`mt-4 flex items-center gap-3 rounded-xl border px-3 py-2 text-sm ${checked.includes(selected.id) ? "border-cyan-300/40 bg-cyan-300/[0.06] text-cyan-200" : "border-white/10 text-slate-400"}`}><span className="flex size-5 items-center justify-center rounded border border-white/20">{checked.includes(selected.id) ? "✓" : ""}</span>{checked.includes(selected.id) ? "Evidence task selected" : "Add evidence task"}</button><label className="mt-4 block text-sm text-slate-400">Decision note<textarea value={note} onChange={(e) => setNote(e.target.value)} placeholder="Record a local rationale or open question…" rows={3} className="mt-2 w-full resize-none rounded-xl border border-white/10 bg-black/20 p-3 text-white outline-none focus:border-cyan-300/50" /></label></div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Decision perspectives</p><h2 className="mt-2 text-2xl font-black">Review before commitment</h2><div className="mt-5 space-y-3">{perspectives.map((item) => <div key={item.role} className="rounded-xl border border-white/10 p-4"><div className="flex items-center justify-between"><p className="font-semibold">{item.role}</p><Badge variant="outline" className="border-white/10 text-slate-400">{item.confidence}</Badge></div><p className="mt-2 text-sm leading-6 text-slate-400">{item.view}</p></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><LockKeyhole className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake progress</h2><p className="mt-3 text-sm leading-6 text-slate-400">Selecting initiatives, changing tracks, and saving a local plan changes browser state only. It does not ship work, measure completion, prove consensus, create revenue, or report a launch.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Phase-planning surface preserved", description: "Roadmap, priorities, debate, progress-state, and evidence-task concepts remain interactive.", icon: Flag, status: "Local plan" }, { title: "Decisions name evidence", description: "Every initiative carries rationale and the evidence needed before commitment.", icon: FileText, status: "Guardrail" }, { title: "Live claims are off", description: "No users, transactions, revenue, success rate, timeline, uptime, or consensus is fabricated.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
