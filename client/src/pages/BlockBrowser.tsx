import { useMemo, useState } from "react";
import { Blocks, CheckCircle2, Clock3, Code2, Copy, Database, ExternalLink, FileSearch, Hash, Layers3, LockKeyhole, Network, Search, Shield, WalletCards } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

type QueryTemplate = { id: string; name: string; kind: string; network: string; status: "Synthetic template" | "Node required" | "Indexer required"; description: string; fields: string[] };
const QUERIES: QueryTemplate[] = [
  { id: "block", name: "Block lookup", kind: "Block", network: "Choose network", status: "Node required", description: "Inspect height, hash, timestamp, parent, and transaction references only after a verified node response.", fields: ["Network and chain ID", "Block height or hash", "Canonical response", "Confirmation policy"] },
  { id: "transaction", name: "Transaction lookup", kind: "Transaction", network: "Choose network", status: "Indexer required", description: "Inspect sender, recipient, value, fee, status, and confirmations only from a verified transaction source.", fields: ["Network and tx hash", "Signature or receipt", "Finality policy", "Failure state"] },
  { id: "address", name: "Address activity", kind: "Address", network: "Choose network", status: "Indexer required", description: "Review public address activity without claiming ownership, balance, identity, or custody.", fields: ["Address validation", "Network scope", "Pagination", "Privacy boundary"] },
  { id: "token", name: "Token metadata", kind: "Token", network: "Choose network", status: "Node required", description: "Review contract metadata, decimals, supply, and provenance only after contract verification.", fields: ["Contract address", "Chain and standard", "Metadata source", "Trust and risk review"] },
];

export default function BlockBrowser() { const [query, setQuery] = useState(""); const [selected, setSelected] = useState<QueryTemplate | null>(null); const [copied, setCopied] = useState(false); const visible = useMemo(() => QUERIES.filter((item) => `${item.name} ${item.kind} ${item.network} ${item.status} ${item.description}`.toLowerCase().includes(query.toLowerCase())), [query]); return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Blocks} eyebrow="Crypto · Explorer" title="Verify the chain before you trust the screen." description="Explore block, transaction, address, and token query templates with network, provenance, finality, and privacy requirements. This page does not claim live blocks, balances, ownership, transactions, confirmations, or chain status." badge="Preview explorer workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setCopied(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><FileSearch className="mr-2 size-4" />{copied ? "Query template copied" : "Copy query template"}</Button><Button variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><Network className="mr-2 size-4" />Network requirements</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Query templates", value: String(QUERIES.length), hint: "Block and address previews", icon: Blocks }, { label: "Live chain data", value: "Unavailable", hint: "No RPC or indexer connected", icon: Network, tone: "amber" }, { label: "Ownership", value: "Not inferred", hint: "Address is not identity", icon: WalletCards, tone: "violet" }, { label: "Finality", value: "Unverified", hint: "No confirmation source", icon: Shield, tone: "slate" }]} /><ScreenPreviewBanner title="Explorer evidence boundary">Query templates, search, selected detail, synthetic state, network requirements, and unavailable live chain data are available for UX review. No block, transaction, address balance, token supply, owner, confirmation, fee, or network status is fabricated.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.85fr_1.15fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="relative"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search query templates" aria-label="Search query templates" className="border-white/10 bg-black/20 pl-9 text-white placeholder:text-slate-500" /></div><div className="mt-4 space-y-2">{visible.map((item) => <button key={item.id} onClick={() => { setSelected(item); setCopied(false); }} className={`w-full rounded-xl border p-4 text-left transition ${selected?.id === item.id ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-black/15 hover:bg-white/[0.05]"}`}><div className="flex items-center gap-2"><Hash className="size-4 text-cyan-300" /><span className="font-semibold">{item.name}</span><Badge variant="outline" className="ml-auto border-amber-300/20 text-amber-200">{item.status}</Badge></div><p className="mt-2 text-xs uppercase tracking-wider text-slate-500">{item.kind} · {item.network}</p><p className="mt-2 text-sm leading-6 text-slate-400">{item.description}</p></button>)}{visible.length === 0 && <ScreenStatePanel type="empty" title="No query templates match" description="Try another entity type, status, or keyword." />}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6">{selected ? <><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Selected query</p><h2 className="mt-1 text-2xl font-bold">{selected.name}</h2><p className="mt-2 text-sm text-slate-400">{selected.kind} · {selected.network}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{selected.status}</Badge></div><p className="mt-5 text-sm leading-6 text-slate-300">{selected.description}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{selected.fields.map((field) => <div key={field} className="rounded-lg border border-white/10 bg-black/15 p-4"><CheckCircle2 className="size-4 text-emerald-300" /><p className="mt-3 text-sm text-slate-300">{field}</p><p className="mt-1 text-xs text-slate-500">Evidence required</p></div>)}</div><div className="mt-6 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex items-center gap-2 font-medium text-amber-200"><Shield className="size-4" />No live result available</div><p className="mt-2 text-sm leading-6 text-slate-300">Connect an approved RPC or indexer, validate the network, and retain the raw response before displaying chain facts.</p></div></> : <ScreenStatePanel type="empty" title="Select a query template" description="Inspect network, provenance, finality, privacy, and response-validation requirements." />}</CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Chain before claim", description: "Validate network, chain ID, canonical source, and response freshness before showing a block or transaction fact.", icon: Network, status: "Required" }, { title: "Address is not identity", description: "A public address does not prove a person, owner, custody relationship, or authorization.", icon: LockKeyhole, status: "Required" }, { title: "No fake chain data", description: "A query template is not a block, receipt, balance, token supply, confirmation, or transaction success.", icon: Database, status: "Guardrail" }]} /></main></div>; }
