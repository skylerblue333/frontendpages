import { useMemo, useState } from "react";
import { BadgeCheck, Check, ChevronRight, CreditCard, FileCheck2, LockKeyhole, MapPin, PackageCheck, RefreshCw, ShieldAlert, ShoppingCart, Truck, WifiOff } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const STEPS = [{ id: "cart", label: "Cart", icon: ShoppingCart }, { id: "delivery", label: "Delivery", icon: Truck }, { id: "payment", label: "Payment", icon: CreditCard }, { id: "confirm", label: "Confirm", icon: BadgeCheck }];
const ITEMS = [{ name: "Synthetic starter kit", price: 36 }, { name: "Synthetic support pass", price: 18 }];
export default function CheckoutFlow() { const [step, setStep] = useState("cart"); const [email, setEmail] = useState(""); const [address, setAddress] = useState(""); const [saved, setSaved] = useState(false); const total = useMemo(() => ITEMS.reduce((sum, item) => sum + item.price, 0), []); const index = STEPS.findIndex((item) => item.id === step); const next = () => { if (index < STEPS.length - 1) { setStep(STEPS[index + 1].id); setSaved(false); } }; const reset = () => { setStep("cart"); setEmail(""); setAddress(""); setSaved(false); }; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={PackageCheck} eyebrow="Commerce · Flow Orchestration" title="Make every checkout step earn its success state." description="Walk through a synthetic checkout flow with local field validation and explicit gates. This page does not authorize a payment, reserve inventory, create an order, send a receipt, or promise fulfillment." badge="Preview flow laboratory"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><FileCheck2 className="mr-2 size-4" />Save local flow draft</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset flow</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Step", value: `${index + 1}/${STEPS.length}`, hint: STEPS[index].label, icon: STEPS[index].icon }, { label: "Total", value: `$${total.toFixed(2)}`, hint: "Illustrative only", icon: ShoppingCart, tone: "violet" }, { label: "Payment", value: "Blocked", hint: "No processor", icon: CreditCard, tone: "amber" }, { label: "Order", value: "Not created", hint: "No confirmation", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="Flow evidence boundary">A progress indicator is not proof that a step completed. A production flow needs server-side state, idempotent transitions, authenticated ownership, inventory and price revalidation, payment authorization, fraud controls, receipt identity, fulfillment handoff, retries, cancellation, refunds, and safe back navigation.</ScreenPreviewBanner><section className="grid gap-3 md:grid-cols-4">{STEPS.map((item, itemIndex) => <button key={item.id} onClick={() => setStep(item.id)} className={`rounded-xl border p-4 text-left ${step === item.id ? "border-cyan-300/40 bg-cyan-300/[0.08]" : itemIndex < index ? "border-violet-300/20 bg-violet-300/[0.05]" : "border-white/10 bg-white/[0.03]"}`}><div className="flex items-center gap-3"><item.icon className="size-5 text-cyan-300" /><span className="font-semibold">{item.label}</span></div><p className="mt-2 text-xs text-slate-500">{itemIndex < index ? "Local preview passed" : itemIndex === index ? "Current review" : "Not reviewed"}</p></button>)}</section><section className="grid gap-6 lg:grid-cols-[1.05fr_0.95fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Current step</p><h2 className="mt-2 text-2xl font-black">{STEPS[index].label}</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Preview only</Badge></div>{step === "cart" && <div className="mt-6 space-y-3">{ITEMS.map((item) => <div key={item.name} className="flex items-center gap-3 rounded-xl border border-white/10 bg-black/15 p-4"><ShoppingCart className="size-5 text-cyan-300" /><span className="flex-1 text-sm text-slate-300">{item.name}</span><span className="font-mono text-sm text-slate-300">${item.price.toFixed(2)}</span></div>)}<p className="pt-2 text-sm text-slate-400">Two synthetic items · subtotal ${total.toFixed(2)} · taxes and shipping unavailable</p></div>}{step === "delivery" && <div className="mt-6 space-y-4"><div><label htmlFor="flow-email" className="mb-2 block text-sm text-slate-300">Contact label</label><Input id="flow-email" value={email} onChange={(event) => { setEmail(event.target.value); setSaved(false); }} placeholder="Local contact label" className="border-white/10 bg-black/20 text-white placeholder:text-slate-500" /></div><div><label htmlFor="flow-address" className="mb-2 block text-sm text-slate-300">Destination label</label><Input id="flow-address" value={address} onChange={(event) => { setAddress(event.target.value); setSaved(false); }} placeholder="Local destination label" className="border-white/10 bg-black/20 text-white placeholder:text-slate-500" /></div><div className="rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4 text-sm text-amber-200">Inventory, shipping rate, address verification, and delivery estimate are unavailable.</div></div>}{step === "payment" && <div className="mt-6 space-y-3"><div className="rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex items-center gap-2 font-semibold text-amber-200"><LockKeyhole className="size-4" />Payment step blocked</div><p className="mt-2 text-sm text-slate-400">No payment fields or processor are connected. No charge can be attempted.</p></div>{["Currency and final amount", "Payment tokenization", "Fraud review", "Authorization result", "Receipt identity"].map((item) => <div key={item} className="flex items-center gap-3 rounded-lg border border-white/10 bg-black/15 p-3"><ShieldAlert className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div>}{step === "confirm" && <div className="mt-6 space-y-3"><div className="rounded-xl border border-cyan-300/20 bg-cyan-300/[0.06] p-4"><div className="flex items-center gap-2 font-semibold text-cyan-200"><BadgeCheck className="size-4" />Confirmation preview</div><p className="mt-2 text-sm text-slate-400">No order, payment, reservation, receipt, shipment, or fulfillment record exists.</p></div>{["Server-side order identity", "Payment authorization", "Inventory hold", "Fulfillment handoff", "Cancellation and refund policy"].map((item) => <div key={item} className="flex items-center gap-3 rounded-lg border border-white/10 bg-black/15 p-3"><ShieldAlert className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Unavailable</span></div>)}</div>}{step !== "confirm" && <Button onClick={next} className="mt-6 bg-cyan-300 text-slate-950 hover:bg-cyan-200">Review next step <ChevronRight className="ml-2 size-4" /></Button>}{saved && <div className="mt-4 flex items-center gap-2 text-sm text-cyan-200"><Check className="size-4" />Local flow draft saved; no commerce state changed.</div>}</CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Readiness gates</p><h2 className="mt-2 text-2xl font-black">Before success</h2><div className="mt-6 space-y-2">{["Cart and price revalidated", "Delivery destination verified", "Payment authorized", "Order persisted idempotently", "Receipt and support path ready", "Fulfillment and refund states ready"].map((item) => <div key={item} className="flex items-center gap-3 rounded-lg border border-white/10 bg-black/15 p-3"><ShieldAlert className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Pending</span></div>)}</div><div className="mt-6 rounded-xl border border-white/10 bg-black/15 p-4"><div className="flex items-center gap-2 text-sm font-semibold text-slate-200"><MapPin className="size-4 text-cyan-300" />Local context</div><p className="mt-2 text-sm text-slate-400">{email || "No contact label"} · {address || "No destination label"}</p></div></CardContent></Card></section><section className="grid gap-4 md:grid-cols-3"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><PackageCheck className="size-5 text-cyan-300" /><h3 className="mt-3 font-semibold">Progress is not proof</h3><p className="mt-2 text-sm leading-6 text-slate-400">A stepper cannot claim server-side completion, payment, or fulfillment.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><LockKeyhole className="size-5 text-violet-300" /><h3 className="mt-3 font-semibold">Transitions need safety</h3><p className="mt-2 text-sm leading-6 text-slate-400">Revalidation, authorization, idempotency, retries, and rollback must be explicit.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><WifiOff className="size-5 text-amber-300" /><h3 className="mt-3 font-semibold">No fake confirmation</h3><p className="mt-2 text-sm leading-6 text-slate-400">Local progress is not an order number, receipt, shipment, or refund.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Every step has a contract", description: "Keep local navigation, validation, server persistence, and completion states separate.", icon: PackageCheck, status: "Pattern" }, { title: "Payment gates success", description: "Never show confirmation without authorization, order identity, and receipt evidence.", icon: CreditCard, status: "Required" }, { title: "No fake flow", description: "A local stepper is not checkout, payment, fulfillment, or refund execution.", icon: WifiOff, status: "Guardrail" }]} /></main></div>; }
