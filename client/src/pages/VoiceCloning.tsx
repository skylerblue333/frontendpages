import FeatureUnavailable from "@/components/FeatureUnavailable";

const VoiceCloning = () => (
  <FeatureUnavailable
    title="Voice Cloning"
    description="Voice Cloning is intentionally held at a truthful release boundary until its real provider, authorization, persistence, monitoring, and acceptance evidence are complete."
    capability="Voice Cloning on /voice-cloning"
    nextStep="Return to the launch hub"
  />
);

export default VoiceCloning;
