import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Calculator, Check, FileCheck2, LockKeyhole, RefreshCw, ShieldCheck, TriangleAlert, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

export default function OrderSummary() {
  const [quantity, setQuantity] = useState("10");
  const [unitPrice, setUnitPrice] = useState("99.80");
  const [acknowledged, setAcknowledged] = useState(false);
  const qty = Number(quantity);
  const price = Number(unitPrice);
  const subtotal = useMemo(() => Number.isFinite(qty * price) && qty > 0 && price > 0 ? (qty * price).toFixed(2) : "—", [qty, price]);
  const valid = qty > 0 && price > 0;
  const reset = () => { setQuantity("10"); setUnitPrice("99.80"); setAcknowledged(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Calculator} eyebrow="OrderSummary · Cost preview" title="See the arithmetic, then separate it from the claims." description="Review a synthetic order line, local subtotal calculation, and unavailable fee/tax/settlement states. No quote, invoice, charge, payment, or completed order is represented." badge="Summary preview"><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset summary</Button></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Line items", value: "1", hint: "Synthetic row", icon: FileCheck2, tone: "cyan" }, { label: "Subtotal", value: subtotal, hint: "Local arithmetic", icon: Calculator, tone: "violet" }, { label: "Fees + tax", value: "Unknown", hint: "No source", icon: TriangleAlert, tone: "amber" }, { label: "Total", value: "Unavailable", hint: "No settlement", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="Summary evidence boundary"><strong>This summary is not an invoice or payment confirmation.</strong> Product, quantity, unit price, subtotal, fee, tax, total, currency, account, merchant, venue, payment method, order ID, charge, refund, and settlement are not sourced from a production system. The subtotal is transparent local arithmetic only.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1fr_0.85fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Synthetic line item</p><h2 className="mt-2 text-3xl font-black">SKY / USD concept</h2></div><Badge variant="outline" className="border-white/10 text-slate-400">Currency source unset</Badge></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="text-sm text-slate-400">Quantity<input value={quantity} onChange={(e) => setQuantity(e.target.value)} inputMode="decimal" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-white outline-none focus:border-cyan-300/50" /></label><label className="text-sm text-slate-400">Unit price<input value={unitPrice} onChange={(e) => setUnitPrice(e.target.value)} inputMode="decimal" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-white outline-none focus:border-cyan-300/50" /></label></div><div className="mt-5 overflow-hidden rounded-xl border border-white/10"><div className="grid grid-cols-[1fr_auto] gap-4 border-b border-white/10 p-4"><span className="text-slate-300">SKY / USD concept</span><span className="font-semibold text-cyan-200">{subtotal}</span></div><div className="space-y-2 p-4 text-sm"><div className="flex justify-between text-slate-400"><span>Subtotal</span><span>{subtotal} local</span></div><div className="flex justify-between text-slate-400"><span>Fees</span><span className="text-amber-200">Unavailable</span></div><div className="flex justify-between text-slate-400"><span>Taxes</span><span className="text-amber-200">Unavailable</span></div><div className="flex justify-between border-t border-white/10 pt-3 font-bold text-white"><span>Total</span><span className="text-amber-200">Not computable</span></div></div></div>{!valid && <p className="mt-4 flex items-center gap-2 text-sm text-amber-200"><TriangleAlert className="size-4" />Use positive numeric quantity and unit price to calculate the local subtotal.</p>}<div className="mt-5 flex flex-wrap gap-2"><Button disabled={!valid || !acknowledged} className="bg-cyan-300/20 text-cyan-100/40">Continue unavailable</Button><Button disabled variant="outline" className="border-white/10 text-white/40">Pay unavailable</Button></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Review state</p><h2 className="mt-2 text-2xl font-black">Do not hide unknowns</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{[{ label: "Account", value: "Not connected" }, { label: "Merchant / venue", value: "Unset" }, { label: "Payment method", value: "Not selected" }, { label: "Order ID", value: "Not created" }, { label: "Receipt", value: "Unavailable" }, { label: "Settlement", value: "Blocked" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 flex gap-3"><button aria-label="Acknowledge summary boundary" onClick={() => setAcknowledged(!acknowledged)} className={`flex size-5 shrink-0 items-center justify-center rounded border ${acknowledged ? "border-cyan-300 bg-cyan-300 text-slate-950" : "border-white/20"}`}>{acknowledged ? "✓" : ""}</button><p className="text-sm leading-6 text-slate-400">I understand this is local arithmetic and cannot authorize payment, create an order, or confirm settlement.</p></div><div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="text-sm leading-6 text-slate-300">A subtotal is not a quote. Without verified fees, tax rules, currency, inventory, authorization, and a payment receipt, total cost remains unavailable.</p></div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Production checklist</p><h2 className="mt-2 text-2xl font-black">A real summary must prove</h2><div className="mt-5 space-y-3">{["Product, price, currency, inventory, merchant, and account contract", "Fee, tax, discount, rounding, exchange-rate, and total calculation source", "Authorization, balance, payment method, idempotency, and charge receipt", "Order ID, timestamps, fulfillment, cancellation, refund, and settlement state", "Privacy, audit trail, support, disputes, failure handling, and incident response"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><LockKeyhole className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake total</h2><p className="mt-3 text-sm leading-6 text-slate-400">The disabled continue and pay controls make the unavailable payment boundary visible rather than implying a successful checkout.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Arithmetic is inspectable", description: "Quantity and unit price update a transparent local subtotal.", icon: Calculator, status: "Verified locally" }, { title: "Fees stay unknown", description: "Fees, taxes, currency, and total are unavailable without authoritative services.", icon: TriangleAlert, status: "Guardrail" }, { title: "Payment stays blocked", description: "No invoice, charge, payment, order, or settlement is fabricated.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
