import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Check, Clock3, Eye, EyeOff, FileClock, Filter, History, LockKeyhole, RefreshCw, Search, ShieldAlert, Trash2, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const historyItems = [{ id: 1, title: "Learning preview", category: "Education", detail: "A local viewing-history concept awaiting consent, media source, and timestamp provenance.", state: "Preview only" }, { id: 2, title: "Community clip", category: "Community", detail: "A history concept requiring privacy controls, retention policy, and verified playback evidence.", state: "Unavailable" }, { id: 3, title: "Game showcase", category: "Gaming", detail: "A local media-history concept that must not imply a viewer, watch event, or engagement.", state: "Unmeasured" }, { id: 4, title: "Builder tutorial", category: "Technology", detail: "A tutorial-history concept awaiting identity, device, file, and deletion controls.", state: "Blocked" }];
export default function RecentlyViewed() {
  const [query, setQuery] = useState(""); const [category, setCategory] = useState("All"); const [selected, setSelected] = useState(1); const [window, setWindow] = useState("Window not set"); const [saved, setSaved] = useState(false); const [showGates, setShowGates] = useState(false);
  const categories = ["All", ...Array.from(new Set(historyItems.map((item) => item.category)))]; const filtered = useMemo(() => historyItems.filter((item) => (category === "All" || item.category === category) && `${item.title} ${item.category} ${item.detail}`.toLowerCase().includes(query.toLowerCase())), [category, query]); const item = historyItems.find((entry) => entry.id === selected) ?? historyItems[0];
  const reset = () => { setQuery(""); setCategory("All"); setSelected(1); setWindow("Window not set"); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={History} eyebrow="RecentlyViewed · History preview" title="Prove the view before writing the history." description="Explore local viewing-history concepts with search, categories, time-window intent, media types, timestamps, privacy, playback, restore, clear-history, export, audit, save, reset, and evidence gates. No live users, views, dates, files, or activity is connected." badge="History workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "View saved locally" : "Save view locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review history gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset history view</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Concepts", value: `${historyItems.length} local`, hint: "No history loaded", icon: History, tone: "cyan" }, { label: "Views", value: "Unavailable", hint: "No event source", icon: Eye, tone: "violet" }, { label: "Timestamps", value: "Unavailable", hint: "No clock source", icon: Clock3, tone: "amber" }, { label: "Privacy", value: "Review needed", hint: "No identity source", icon: LockKeyhole, tone: "slate" }]} /><ScreenPreviewBanner title="Viewing-history evidence boundary"><strong>This is a local history and privacy preview, not evidence of a user viewing media.</strong> History cards, categories, window intent, timestamp posture, privacy state, playback and restore intent, clear-history posture, saved state, and audit gates are browser concepts. No user, view, date, device, file, playback event, retention result, or engagement is asserted.</ScreenPreviewBanner><section className="flex flex-wrap items-end gap-3"><label className="text-sm text-slate-400">History window<select value={window} onChange={(event) => setWindow(event.target.value)} className="mt-2 rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Window not set</option><option>Today intent</option><option>Last 7 days intent</option><option>Last 30 days intent</option></select></label><Badge variant="outline" className="border-amber-300/20 text-amber-200">History source disconnected</Badge></section><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Search className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local history concepts" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-4 flex flex-wrap gap-2">{categories.map((entry) => <Button key={entry} onClick={() => setCategory(entry)} size="sm" variant="outline" className={category === entry ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-100" : "border-white/10 text-slate-400"}><Filter className="mr-1 size-3" />{entry}</Button>)}</div><div className="mt-6 space-y-3">{filtered.map((entry) => <button key={entry.id} onClick={() => setSelected(entry.id)} className={`w-full rounded-xl border p-4 text-left ${selected === entry.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{entry.title}</p><p className="mt-1 text-sm text-slate-500">{entry.detail}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{entry.state}</Badge></div><div className="mt-4"><Badge variant="outline" className="border-white/10 text-slate-500">{entry.category}</Badge></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected history concept</p><h2 className="mt-2 text-2xl font-black">{item.title}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{item.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Category", value: item.category }, { label: "Window", value: window }, { label: "View event", value: "Unavailable" }, { label: "Timestamp", value: "Unavailable" }, { label: "Device", value: "Unavailable" }, { label: "Privacy", value: "Review needed" }].map((entry) => <div key={entry.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{entry.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{entry.value}</p></div>)}</div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-8 text-center"><EyeOff className="mx-auto size-8 text-slate-600" /><p className="mt-3 font-semibold">No viewing history loaded</p><p className="mt-2 text-sm text-slate-500">Connect a governed history source before showing a person, item, playback event, timestamp, or retention result.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400"><Eye className="mr-2 size-4" />Open unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500"><Trash2 className="mr-2 size-4" />Clear unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500"><FileClock className="mr-2 size-4" />Export unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No viewing or retention claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local history concept does not prove that a user viewed content, that a timestamp is accurate, or that deletion occurred.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">History gates</p><h2 className="mt-2 text-2xl font-black">What a real viewing-history surface must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Identity, consent, privacy, device, tenant isolation, opt-out, deletion, retention, and legal holds", "Playback event schema, media identity, timestamp source, clock, time zone, ordering, and provenance", "History data access, pagination, deduplication, filtering, search, sorting, export, and reproducibility", "Clear-history semantics, deletion jobs, caches, backups, replicas, audit, and user-visible status", "Sensitive categories, minors, financial, education, health, location, and behavioral inference boundaries", "Incident response, support, recovery, access review, localization, accessibility, and rate limits"].map((entry) => <div key={entry} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{entry}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "History surface preserved", description: "History concepts, search, categories, windows, media types, timestamps, privacy, playback, restore, clear-history, export, audit, save/reset, and gates remain interactive.", icon: History, status: "Local history" }, { title: "No viewing theater", description: "Users, views, dates, devices, files, playback events, retention results, and engagement are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Privacy before history", description: "Real history needs consent, identity, timestamps, deletion, retention, access controls, audit, and recovery.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
