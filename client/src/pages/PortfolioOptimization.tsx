import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Check, Gauge, LockKeyhole, RefreshCw, Scale, ShieldAlert, ShieldCheck, SlidersHorizontal, Target, TriangleAlert, TrendingUp, Wallet, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const objectives = ["Preserve capital", "Balance growth and stability", "Explore long-term growth", "Learn allocation tradeoffs"];
const constraints = ["Maximum concentration", "Liquidity needs", "Loss tolerance", "Time horizon", "Tax context", "Fees and turnover"];
const scenarios = ["Baseline", "Stress", "Recovery"];
const evidence = ["Authenticated holdings and current quantities", "Timestamped prices, currency, and fees", "Investor goals, horizon, liquidity, and risk capacity", "Tax/legal context and suitability review", "Reproducible model, assumptions, uncertainty, and human approval"];
export default function PortfolioOptimization() {
  const [objective, setObjective] = useState(objectives[0]);
  const [scenario, setScenario] = useState(scenarios[0]);
  const [constraintsSelected, setConstraintsSelected] = useState<string[]>([]);
  const [evidenceSelected, setEvidenceSelected] = useState<string[]>([]);
  const [saved, setSaved] = useState(false);
  const toggle = (value: string, setter: React.Dispatch<React.SetStateAction<string[]>>) => setter((items) => items.includes(value) ? items.filter((item) => item !== value) : [...items, value]);
  const reset = () => { setObjective(objectives[0]); setScenario(scenarios[0]); setConstraintsSelected([]); setEvidenceSelected([]); setSaved(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Scale} eyebrow="PortfolioOptimization · Financial preview" title="Explore allocation tradeoffs without generating advice." description="Set a learning objective, constraints, and scenario labels while reviewing the evidence required for any real optimization. No holdings, prices, returns, risk model, tax context, investor profile, or recommendation engine is connected." badge="Optimization preview"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} disabled={evidenceSelected.length === 0} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Plan saved locally" : "Save local plan"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset planner</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Objective", value: "Selected", hint: objective, icon: Target, tone: "cyan" }, { label: "Constraints", value: String(constraintsSelected.length), hint: "Local assumptions", icon: SlidersHorizontal, tone: "violet" }, { label: "Scenario", value: scenario, hint: "Label only", icon: Gauge, tone: "amber" }, { label: "Output", value: "Unavailable", hint: "No model source", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="Optimization evidence boundary"><strong>This is not a portfolio optimizer or investment recommendation.</strong> Objective, constraint, scenario, checklist, and saved-plan states are local learning concepts. They do not calculate expected return, volatility, drawdown, allocation, tax impact, suitability, probability, or an action to take. No financial service is connected.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.82fr_1fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Learning objective</p><h2 className="mt-2 text-2xl font-black">What would you explore?</h2><div className="mt-5 space-y-2">{objectives.map((item) => <button key={item} onClick={() => setObjective(item)} className={`w-full rounded-xl border p-3 text-left ${objective === item ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10 text-slate-400"}`}><span className="text-sm font-semibold">{item}</span></button>)}</div><p className="mt-6 text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Scenario label</p><div className="mt-3 flex gap-2">{scenarios.map((item) => <button key={item} onClick={() => setScenario(item)} className={`rounded-lg border px-3 py-2 text-xs ${scenario === item ? "border-amber-300/40 bg-amber-300/10 text-amber-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div><div className="mt-6 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex gap-3"><TriangleAlert className="mt-0.5 size-5 shrink-0 text-amber-200" /><p className="text-sm leading-6 text-slate-300">A scenario label is not a forecast. It has no distributions, correlations, probabilities, prices, or investor assumptions behind it.</p></div></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Constraint worksheet</p><h2 className="mt-2 text-2xl font-black">Name tradeoffs before modeling</h2><div className="mt-5 grid gap-2 sm:grid-cols-2">{constraints.map((item) => <button key={item} onClick={() => toggle(item, setConstraintsSelected)} className={`flex items-center gap-3 rounded-xl border p-3 text-left ${constraintsSelected.includes(item) ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><span className="flex size-5 items-center justify-center rounded border border-white/20">{constraintsSelected.includes(item) ? "✓" : ""}</span><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Local</span></button>)}</div><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Current allocation", value: "Unavailable" }, { label: "Proposed allocation", value: "Unavailable" }, { label: "Expected return", value: "Not modeled" }, { label: "Risk estimate", value: "Not modeled" }, { label: "Tax impact", value: "Unavailable" }, { label: "Recommendation", value: "Blocked" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-4"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-lg font-black text-amber-200">{item.value}</p></div>)}</div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Evidence gate</p><h2 className="mt-2 text-2xl font-black">Before any real output</h2><div className="mt-5 space-y-2">{evidence.map((item) => <button key={item} onClick={() => toggle(item, setEvidenceSelected)} className={`flex w-full items-center gap-3 rounded-xl border p-3 text-left ${evidenceSelected.includes(item) ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><span className="flex size-5 items-center justify-center rounded border border-white/20">{evidenceSelected.includes(item) ? "✓" : ""}</span><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></button>)}</div><Button disabled={evidenceSelected.length === 0} className="mt-5 bg-cyan-300 text-slate-950 hover:bg-cyan-200"><ShieldCheck className="mr-2 size-4" />{evidenceSelected.length}/{evidence.length} local checks selected</Button></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><LockKeyhole className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake optimization</h2><p className="mt-3 text-sm leading-6 text-slate-400">Changing objectives, constraints, scenarios, or checklists changes browser state only. It does not calculate an allocation, assess suitability, predict returns, estimate tax, or recommend a financial action.</p></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Production checklist</p><h2 className="mt-2 text-2xl font-black">Optimization must prove</h2><div className="mt-5 space-y-3">{["Reliable holdings, prices, fees, currency, tax lots, transactions, and reconciliation", "Documented objectives, risk capacity, horizon, liquidity, constraints, and suitability process", "Reproducible objective function, assumptions, optimization method, uncertainty, sensitivity, and validation", "Human review, disclosures, no-guarantee messaging, audit, access control, and rollback", "Tests for stale data, missing data, extreme inputs, model failure, conflicting accounts, and harmful outputs"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Optimization surface preserved", description: "Objectives, constraints, scenarios, output states, evidence gate, and local save remain interactive.", icon: Scale, status: "Local planner" }, { title: "Financial model is off", description: "Holdings, prices, returns, risk, taxes, suitability, and allocation output remain unavailable.", icon: ShieldAlert, status: "Guardrail" }, { title: "No recommendation", description: "No forecast, winner, guarantee, or financial action is fabricated.", icon: WifiOff, status: "Unavailable" }]} /></section></main></div>;
}
