import { useState } from "react";
import { Activity, Check, Cloud, Database, GitBranch, Info, Layers3, LockKeyhole, Network, RefreshCw, ServerCog, ShieldAlert, Workflow, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

const nodes = [
  { name: "Web application", kind: "Frontend", state: "Preview", detail: "Route-level browser render only; deployed artifact, CDN, cache, and runtime evidence are not connected." },
  { name: "API boundary", kind: "Service", state: "Unverified", detail: "Requires authenticated route inventory, contracts, dependencies, health probes, and environment scope." },
  { name: "Data platform", kind: "Persistence", state: "Unavailable", detail: "Requires schema, migrations, connection, backups, access policy, and recovery evidence." },
  { name: "Workers and integrations", kind: "Async", state: "Unknown", detail: "Requires scheduler, queues, provider contracts, retries, secrets, and observability evidence." },
];

export default function SystemArchitecture() {
  const [selected, setSelected] = useState(0);
  const [environment, setEnvironment] = useState("Environment not verified");
  const [saved, setSaved] = useState(false);
  const [showBoundary, setShowBoundary] = useState(false);
  const [refreshing, setRefreshing] = useState(false);
  const refresh = () => { setRefreshing(true); window.setTimeout(() => setRefreshing(false), 500); };
  const reset = () => { setSelected(0); setEnvironment("Environment not verified"); setSaved(false); setShowBoundary(false); setRefreshing(false); };
  const node = nodes[selected] ?? nodes[0];
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Network} eyebrow="System architecture · Topology preview" title="Map platform boundaries without claiming deployed infrastructure." description="Explore a local architecture workspace with services, dependencies, environments, data flows, storage, queues, integrations, secrets, observability, deployment, recovery, retry, save/reset, and evidence boundaries. No production topology, health, capacity, security, or uptime is connected." badge="Evidence-bounded architecture"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save topology view"}</Button><Button onClick={() => setShowBoundary((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showBoundary ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showBoundary ? "Close evidence boundary" : "Review architecture gates"}</Button><Button onClick={refresh} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className={`mr-2 size-4 ${refreshing ? "animate-spin" : ""}`} />{refreshing ? "Retrying local state" : "Retry topology source"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><X className="mr-2 size-4" />Reset topology</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Services", value: "Unmapped", hint: "No deployed inventory", icon: Layers3, tone: "cyan" }, { label: "Dependencies", value: "Unverified", hint: "No graph source", icon: Network, tone: "violet" }, { label: "Data flows", value: "Unknown", hint: "No runtime traces", icon: Workflow, tone: "amber" }, { label: "Recovery", value: "Required", hint: "No runbook source", icon: ShieldAlert, tone: "slate" }]} /><ScreenPreviewBanner title="Architecture evidence boundary"><strong>This is a local topology-design preview, not evidence that services, dependencies, data stores, queues, integrations, environments, security controls, deployments, backups, or recovery paths exist in production.</strong> Node cards, relationship lines, environment selectors, local saved state, retry feedback, and disabled inspect/export actions are browser concepts. No topology, health, capacity, latency, availability, security, compliance, or production-readiness claim is asserted.</ScreenPreviewBanner><section className="grid gap-4 md:grid-cols-2">{nodes.map((item, index) => <button key={item.name} onClick={() => setSelected(index)} className={`rounded-2xl border p-5 text-left transition ${selected === index ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10 bg-white/[0.04]"}`}><div className="flex items-start justify-between"><div className="flex size-11 items-center justify-center rounded-2xl bg-cyan-300/10 text-cyan-200"><ServerCog className="size-5" /></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.state}</Badge></div><h2 className="mt-4 font-bold">{item.name}</h2><p className="mt-1 text-xs text-cyan-200">{item.kind}</p><p className="mt-3 text-sm leading-6 text-slate-400">{item.detail}</p></button>)}</section><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200">Selected topology concept</p><h2 className="mt-2 text-2xl font-black">{node.name}</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{node.state}</Badge></div><p className="mt-3 text-sm leading-6 text-slate-400">{node.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Identity / ownership", value: "Unverified", icon: ServerCog }, { label: "Environment / region", value: "Unconfigured", icon: Cloud }, { label: "Dependencies", value: "Unmapped", icon: Network }, { label: "Data store / schema", value: "Unavailable", icon: Database }, { label: "Telemetry / health", value: "Unmeasured", icon: Activity }, { label: "Security / access", value: "Required", icon: LockKeyhole }].map(({ label, value, icon: Icon }) => <div key={label} className="flex items-center gap-3 rounded-xl border border-white/10 p-4"><Icon className="size-4 text-cyan-300" /><span className="flex-1 text-sm text-slate-300">{label}</span><span className="text-xs text-amber-200">{value}</span></div>)}</div><label className="mt-6 block text-sm font-semibold text-slate-300">Environment intent<select value={environment} onChange={(event) => setEnvironment(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white"><option>Environment not verified</option><option>Local preview</option><option>Staging intent</option><option>Production intent</option></select></label><div className="mt-6 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Inspect deployment unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Trace dependency unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Export diagram unavailable</Button></div></CardContent></Card><ScreenStatePanel type="unavailable" title="Deployed topology unavailable" description="Connect authoritative infrastructure inventory, runtime configuration, dependency graph, health telemetry, security controls, deployment metadata, and recovery evidence before reporting architecture." /></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><Info className="size-5 text-cyan-300" /><div><p className="font-bold">Architecture evidence contract</p><p className="mt-1 text-sm text-slate-400">A production topology needs versioned service ownership, network boundaries, data classification, dependency contracts, secret custody, change history, observability, backup, disaster recovery, and accountable review.</p></div></div><div className="mt-5 grid gap-3 md:grid-cols-2">{["Service inventory, ownership, versions, environments, and regions", "Dependency contracts, network boundaries, queues, and integrations", "Schema, data classification, retention, backups, and restore tests", "Secrets, identity, authorization, encryption, and audit controls", "Deployment, migration, rollback, health, capacity, and alert evidence", "Incident, disaster recovery, RTO/RPO, runbooks, and change approval"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3 text-sm text-slate-300"><LockKeyhole className="size-4 text-amber-300" />{item}<span className="ml-auto text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card>{showBoundary && <Card className="border-amber-300/20 bg-amber-300/[0.06]"><CardContent className="p-6"><p className="font-semibold text-amber-100">No deployed-architecture claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local topology concept does not prove services, dependencies, data flows, environments, security, capacity, deployment, backup, recovery, uptime, or production readiness.</p></CardContent></Card>}<ScreenFeatureGrid features={[{ title: "Architecture surface preserved", description: "Services, dependencies, environments, data flows, storage, queues, integrations, security, observability, deployment, recovery, retry, save/reset, and evidence gates remain visible.", icon: Network, status: "Local topology" }, { title: "No infrastructure theater", description: "Production services, health, capacity, security, deployment, backup, recovery, uptime, and compliance are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before topology", description: "Real architecture requires inventory, ownership, contracts, runtime telemetry, access controls, change history, recovery tests, and audit.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
