import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Check, Eye, FileImage, LockKeyhole, RefreshCw, ShieldAlert, Sparkles, UserRound, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const interests = ["AI", "Education", "Community", "Crypto research", "Gaming"];
export default function ProfileCreation() {
  const [name, setName] = useState("");
  const [handle, setHandle] = useState("");
  const [bio, setBio] = useState("");
  const [visibility, setVisibility] = useState("Private draft");
  const [selectedInterests, setSelectedInterests] = useState<string[]>([]);
  const [saved, setSaved] = useState(false);
  const [preview, setPreview] = useState(false);
  const completeness = useMemo(() => [name.trim(), handle.trim(), bio.trim(), selectedInterests.length > 0].filter(Boolean).length, [bio, handle, name, selectedInterests]);
  const toggleInterest = (interest: string) => setSelectedInterests((items) => items.includes(interest) ? items.filter((item) => item !== interest) : [...items, interest]);
  const reset = () => { setName(""); setHandle(""); setBio(""); setVisibility("Private draft"); setSelectedInterests([]); setSaved(false); setPreview(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={UserRound} eyebrow="ProfileCreation · Draft builder preview" title="Create an identity draft before creating an account record." description="Compose a local profile with identity fields, interests, visibility intent, and preview. No account is created, handle uniqueness is checked, image is uploaded, identity is verified, or profile is published." badge="Profile builder"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Draft saved locally" : "Save draft locally"}</Button><Button onClick={() => setPreview((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{preview ? <X className="mr-2 size-4" /> : <Eye className="mr-2 size-4" />}{preview ? "Close preview" : "Preview profile"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset draft</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Draft fields", value: `${completeness}/4`, hint: "Local completeness", icon: Check, tone: "cyan" }, { label: "Account", value: "Not created", hint: "No persistence", icon: LockKeyhole, tone: "violet" }, { label: "Handle", value: handle ? "Unverified" : "Empty", hint: "No uniqueness check", icon: UserRound, tone: "amber" }, { label: "Image", value: "Not uploaded", hint: "No storage source", icon: FileImage, tone: "slate" }]} /><ScreenPreviewBanner title="Profile creation evidence boundary"><strong>This is a local profile-draft builder, not account creation.</strong> Fields, interests, visibility, completeness, and preview are browser-state concepts. No account, username reservation, identity verification, image upload, consent, persistence, or public profile is created or claimed.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Profile fields</p><h2 className="mt-2 text-2xl font-black">Draft your identity</h2></div><Badge variant="outline" className="border-white/10 text-amber-200">{completeness}/4 ready</Badge></div><div className="mt-5 space-y-4"><label className="block text-sm text-slate-400">Display name<input value={name} onChange={(event) => setName(event.target.value)} placeholder="e.g. Skycoin explorer" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-white outline-none focus:border-cyan-300/50" /></label><label className="block text-sm text-slate-400">Handle<input value={handle} onChange={(event) => setHandle(event.target.value.replace(/\s/g, "").slice(0, 24))} placeholder="@your-handle" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-white outline-none focus:border-cyan-300/50" /><span className="mt-2 block text-xs text-amber-200">Availability and uniqueness are unverified.</span></label><label className="block text-sm text-slate-400">Bio<textarea value={bio} onChange={(event) => setBio(event.target.value.slice(0, 240))} placeholder="What should people know about this profile?" className="mt-2 min-h-28 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-white outline-none focus:border-cyan-300/50" /><span className="mt-2 block text-right text-xs text-slate-500">{bio.length}/240</span></label><div><p className="text-sm text-slate-400">Interests</p><div className="mt-2 flex flex-wrap gap-2">{interests.map((interest) => <button key={interest} onClick={() => toggleInterest(interest)} className={`rounded-lg border px-3 py-2 text-xs ${selectedInterests.includes(interest) ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-200" : "border-white/10 text-slate-500"}`}>{interest}</button>)}</div></div><div><p className="text-sm text-slate-400">Visibility intent</p><div className="mt-2 flex flex-wrap gap-2">{["Private draft", "Authenticated community", "Public later"].map((item) => <button key={item} onClick={() => setVisibility(item)} className={`rounded-lg border px-3 py-2 text-xs ${visibility === item ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div></div></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Profile preview</p><h2 className="mt-2 text-2xl font-black">{name || "Your display name"}</h2><p className="mt-2 text-sm text-slate-500">{handle || "@handle-unverified"} · {visibility}</p><div className="mt-6 flex size-20 items-center justify-center rounded-3xl bg-cyan-300/10"><UserRound className="size-9 text-cyan-200" /></div><p className="mt-5 text-sm leading-7 text-slate-300">{bio || "Your local profile preview will appear here."}</p><div className="mt-5 flex flex-wrap gap-2">{selectedInterests.length ? selectedInterests.map((item) => <Badge key={item} variant="outline" className="border-white/10 text-slate-400">{item}</Badge>) : <span className="text-xs text-slate-600">No interests selected</span>}</div><div className="mt-6 rounded-2xl border border-amber-300/25 bg-amber-300/[0.06] p-5"><div className="flex items-start gap-3"><ShieldAlert className="mt-0.5 size-5 shrink-0 text-amber-200" /><div><p className="font-semibold text-amber-100">Create account is unavailable</p><p className="mt-2 text-sm leading-6 text-slate-400">No authenticated signup, handle service, avatar storage, identity verification, privacy consent, profile API, or public visibility path is connected.</p><Button disabled className="mt-4 bg-slate-700 text-slate-400">Create profile unavailable</Button></div></div></div></CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Creation gates</p><h2 className="mt-2 text-2xl font-black">What a real profile flow must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Authenticated account, session, and recoverability", "Server-side validation, handle uniqueness, and abuse limits", "Consent, privacy, visibility, export, and deletion behavior", "Image upload, content moderation, asset rights, and accessibility", "Verification, notification, support, audit, and rollback behavior", "Clear error, retry, and partial-save states"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><FileImage className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Creation surface preserved", description: "Identity fields, handle, bio, interests, visibility intent, preview, local save/reset, completeness, and creation gates remain interactive.", icon: UserRound, status: "Local builder" }, { title: "No account theater", description: "Account creation, handle uniqueness, verification, image upload, consent, persistence, and public visibility are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Identity service required", description: "Real profiles need authenticated storage, validation, privacy, moderation, asset handling, support, and deletion paths.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
