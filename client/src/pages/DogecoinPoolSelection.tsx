import FeatureUnavailable from "@/components/FeatureUnavailable";

const DogecoinPoolSelection = () => (
  <FeatureUnavailable
    title="Dogecoin Pool Selection"
    description="Dogecoin Pool Selection is intentionally held at a truthful release boundary until its real provider, authorization, persistence, monitoring, and acceptance evidence are complete."
    capability="Dogecoin Pool Selection on /dogecoin-pool-selection"
    nextStep="Return to the launch hub"
  />
);

export default DogecoinPoolSelection;
