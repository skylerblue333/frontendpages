import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { BadgeCheck, Blocks, Bookmark, Check, CircleDollarSign, FileWarning, Filter, Image as ImageIcon, LockKeyhole, RefreshCw, Search, ShieldCheck, Sparkles, WalletCards, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const ITEMS = [{ title: "Azure Relay #01", collection: "Signal Forms", chain: "Unspecified", trait: "Atmospheric", description: "Synthetic artwork card for testing gallery browsing, metadata, and custody boundaries." }, { title: "Night Garden #07", collection: "Quiet Worlds", chain: "Unspecified", trait: "Botanical", description: "A local collectible concept with intentionally unverified provenance and ownership." }, { title: "Orbit Fragment #12", collection: "Signal Forms", chain: "Unspecified", trait: "Geometric", description: "Synthetic gallery content for exploring filters, favorites, and token detail states." }, { title: "Paper Horizon #03", collection: "Paper Skies", chain: "Unspecified", trait: "Minimal", description: "A preview token card with no contract, token ID, creator, or market data attached." }, { title: "Blue Hour Index #21", collection: "Quiet Worlds", chain: "Unspecified", trait: "Cinematic", description: "A local art concept for reviewing display and provenance-readiness language." }, { title: "Glass Current #05", collection: "Paper Skies", chain: "Unspecified", trait: "Reflective", description: "Synthetic content only; no transfer, listing, wallet, or ownership action is available." }];
const COLLECTIONS = ["All", "Signal Forms", "Quiet Worlds", "Paper Skies"];

export default function NFTGallery() {
  const [query, setQuery] = useState("");
  const [collection, setCollection] = useState("All");
  const [selected, setSelected] = useState(ITEMS[0].title);
  const [favorites, setFavorites] = useState<string[]>([]);
  const [saved, setSaved] = useState(false);
  const item = ITEMS.find((entry) => entry.title === selected) ?? ITEMS[0];
  const visible = useMemo(() => ITEMS.filter((entry) => (collection === "All" || entry.collection === collection) && `${entry.title} ${entry.collection} ${entry.trait} ${entry.description}`.toLowerCase().includes(query.toLowerCase())), [collection, query]);
  const toggle = (title: string) => { setFavorites((current) => current.includes(title) ? current.filter((value) => value !== title) : [...current, title]); setSaved(false); };
  const reset = () => { setQuery(""); setCollection("All"); setSelected(ITEMS[0].title); setFavorites([]); setSaved(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={ImageIcon} eyebrow="NFTGallery · Collectible discovery" title="Explore the artwork before claiming the token." description="Browse synthetic collectible cards, collection filters, local favorites, selected-token detail, and provenance-readiness gates. No ownership, creator identity, token ID, contract, chain state, floor price, listing, transfer, or wallet custody is asserted." badge="Gallery preview"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Gallery plan saved locally" : "Save gallery plan"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset gallery</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Artwork cards", value: String(ITEMS.length), hint: "Synthetic gallery", icon: ImageIcon }, { label: "Collections", value: String(COLLECTIONS.length - 1), hint: "Local filters", icon: Filter, tone: "violet" }, { label: "Favorites", value: String(favorites.length), hint: "Local only", icon: Bookmark, tone: "amber" }, { label: "Ownership", value: "Off", hint: "No chain source", icon: WalletCards, tone: "slate" }]} /><ScreenPreviewBanner title="NFTGallery evidence boundary"><strong>Artwork cards, collection names, traits, favorites, and token-detail fields are synthetic gallery fixtures—not creator or ownership records, token IDs, contract addresses, blockchain state, provenance, floor prices, listings, transfers, royalties, or wallet custody.</strong> Production NFT functionality requires content provenance, creator rights, immutable metadata, chain and contract validation, wallet security, marketplace rules, indexer freshness, and transfer verification.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1.05fr_0.95fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search synthetic collectibles" className="w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 pl-9 text-white placeholder:text-slate-500" /></div><div className="mt-4 flex gap-2 overflow-x-auto">{COLLECTIONS.map((entry) => <button key={entry} onClick={() => setCollection(entry)} className={`rounded-xl border px-3 py-2 text-xs whitespace-nowrap ${collection === entry ? "border-cyan-300/40 bg-cyan-300/[0.1] text-cyan-200" : "border-white/10 text-slate-500"}`}>{entry}</button>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2">{visible.map((entry) => <div key={entry.title} className={`rounded-2xl border p-4 ${selected === entry.title ? "border-cyan-300/40 bg-cyan-300/[0.07]" : "border-white/10 bg-black/10"}`}><button onClick={() => { setSelected(entry.title); setSaved(false); }} className="w-full text-left"><div className="aspect-[4/3] rounded-xl bg-gradient-to-br from-cyan-300/15 via-violet-400/15 to-rose-300/15 p-3"><div className="flex h-full items-end"><div><p className="text-[10px] uppercase tracking-[0.2em] text-cyan-200">{entry.trait}</p><p className="mt-1 font-black">{entry.title}</p></div></div></div><div className="mt-3 flex items-start justify-between gap-2"><div><h2 className="font-black">{entry.title}</h2><p className="mt-1 text-xs text-cyan-200">{entry.collection}</p></div><Badge variant="outline" className="border-white/10 text-[10px] text-slate-400">Preview</Badge></div><p className="mt-2 text-sm text-slate-400">{entry.description}</p></button><div className="mt-4 flex items-center justify-between border-t border-white/10 pt-3"><span className="text-xs text-slate-500">Chain unspecified</span><button onClick={() => toggle(entry.title)} className={`rounded-lg p-2 ${favorites.includes(entry.title) ? "bg-cyan-300/15 text-cyan-200" : "text-slate-500 hover:bg-white/10"}`} aria-label={`Toggle ${entry.title} favorite`}><Bookmark className={`size-4 ${favorites.includes(entry.title) ? "fill-current" : ""}`} /></button></div></div>)}{visible.length === 0 && <div className="col-span-full p-8 text-center text-slate-500">No synthetic collectibles match this view.</div>}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-300">Selected collectible</p><h2 className="mt-2 text-3xl font-black">{item.title}</h2></div><Badge variant="outline" className="border-white/10 text-slate-400">{item.collection}</Badge></div><p className="mt-4 text-slate-400">{item.description}</p><div className="mt-5 aspect-video rounded-2xl bg-gradient-to-br from-cyan-300/15 via-violet-400/15 to-rose-300/15 p-5"><div className="flex h-full flex-col justify-between"><div className="flex items-center justify-between"><Badge variant="outline" className="border-white/10 bg-black/20 text-slate-300">Artwork placeholder</Badge><span className="text-xs text-slate-500">{item.trait}</span></div><div><p className="text-xs uppercase tracking-[0.2em] text-cyan-200">Synthetic collectible</p><p className="mt-2 text-2xl font-black">{item.title}</p></div></div></div><div className="mt-5 grid gap-3 sm:grid-cols-2">{[{ label: "Creator", value: "Not verified" }, { label: "Token ID", value: "Not assigned" }, { label: "Contract", value: "Not sourced" }, { label: "Owner", value: "Unavailable" }, { label: "Floor price", value: "Not sourced" }, { label: "Chain", value: item.chain }].map((entry) => <div key={entry.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{entry.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{entry.value}</p></div>)}</div><div className="mt-5 flex gap-2"><Button onClick={() => toggle(item.title)} className="bg-violet-500 text-white hover:bg-violet-400"><Bookmark className="mr-2 size-4" />{favorites.includes(item.title) ? "Remove favorite" : "Save favorite"}</Button><Button disabled variant="outline" className="border-white/10 text-white/40"><WalletCards className="mr-2 size-4" />Open wallet unavailable</Button></div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Collectible readiness</p><h2 className="mt-2 text-2xl font-black">Required before ownership claims</h2><div className="mt-5 space-y-3">{["Creator identity, rights, and provenance", "Immutable metadata, media integrity, and storage", "Chain, contract, token ID, and indexer validation", "Wallet ownership, transfer, royalty, and marketplace rules", "Pricing, listings, support, dispute, and recovery handling"].map((entry) => <div key={entry} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{entry}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><FileWarning className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake ownership</h2><p className="mt-3 text-sm leading-6 text-slate-400">The gallery does not claim that a collectible exists on-chain, is owned by anyone, has a market value, or can be transferred.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Artwork is not ownership", description: "Synthetic cards preserve gallery UX without token, contract, provenance, or owner claims.", icon: ImageIcon, status: "Guardrail" }, { title: "Metadata needs integrity", description: "Creator rights, immutable metadata, media storage, and chain/indexer sources need verification.", icon: BadgeCheck, status: "Required" }, { title: "No fake transfer", description: "Favorites remain local while wallet connection, listing, purchase, and transfer stay unavailable.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
