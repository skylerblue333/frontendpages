import { useState } from "react";
import { Check, ClipboardCheck, Crown, Filter, KeyRound, LockKeyhole, MailPlus, RefreshCw, ScrollText, ShieldAlert, UserRound, Users, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";
const members = [{ name: "Workspace member concept", role: "Role unconfigured", detail: "Requires authenticated identity, organization scope, verified membership, role assignment, and least-privilege authorization." }, { name: "Reviewer concept", role: "Review access", detail: "Requires approval policy, resource scope, audit trail, consent, and accountable reviewer identity." }, { name: "Pending invite concept", role: "Invitation unavailable", detail: "Requires verified email, expiration, acceptance, revocation, notification, and organization policy." }];
export default function TeamManagement() { const [filter, setFilter] = useState("All members"); const [selected, setSelected] = useState(0); const [saved, setSaved] = useState(false); const [refreshing, setRefreshing] = useState(false); const [status, setStatus] = useState("Team service unavailable. Showing local organization structure only."); const retry = () => { setRefreshing(true); setStatus("Retrying organization source…"); window.setTimeout(() => { setRefreshing(false); setStatus("Team service unavailable. No authenticated organization source was connected."); }, 500); }; const reset = () => { setFilter("All members"); setSelected(0); setSaved(false); setStatus("Team service unavailable. Showing local organization structure only."); }; const member = members[selected] ?? members[0]; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Users} eyebrow="Team management · Organization preview" title="Review team structure without inventing members, roles, access, or invitations." description="Explore a local organization-management workspace with member concepts, invitations, role scope, permission gates, audit controls, offboarding, retry, save/reset feedback, and explicit evidence boundaries. No identity, organization, membership, invitation, permission, or mutation is connected." badge="Evidence-bounded workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => { setSaved(true); setStatus("Team view saved locally. No organization or member data was changed."); }} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save team view"}</Button><Button onClick={retry} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className={`mr-2 size-4 ${refreshing ? "animate-spin" : ""}`} />{refreshing ? "Retrying source" : "Retry organization source"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><X className="mr-2 size-4" />Reset workspace</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Members", value: "Unavailable", hint: "No identity source", icon: Users, tone: "cyan" }, { label: "Roles", value: "Unconfigured", hint: "No policy source", icon: Crown, tone: "violet" }, { label: "Invitations", value: "Blocked", hint: "No mail source", icon: MailPlus, tone: "amber" }, { label: "Audit", value: "Required", hint: "No event source", icon: ScrollText, tone: "slate" }]} /><ScreenPreviewBanner title="Team evidence boundary"><strong>This is a local organization-management preview, not evidence that an organization, member, invitation, role, permission, identity, access decision, audit event, or offboarding action exists.</strong> Filters, cards, local saved state, retry feedback, and disabled invite/change/remove actions are browser concepts. No authorization, employment, security, or account conclusion is asserted.</ScreenPreviewBanner><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex flex-wrap items-center justify-between gap-4"><div className="flex flex-wrap gap-2">{["All members", "Review access", "Invitation unavailable"].map((item) => <Button key={item} onClick={() => setFilter(item)} size="sm" variant={filter === item ? "default" : "outline"} className={filter === item ? "bg-cyan-300 text-slate-950 hover:bg-cyan-200" : "border-white/10 bg-white/5 text-white hover:bg-white/10"}><Filter className="mr-2 size-4" />{item}</Button>)}</div><Button disabled variant="outline" className="border-white/10 text-slate-500"><MailPlus className="mr-2 size-4" />Invite unavailable</Button></div></CardContent></Card><section className="grid gap-4 md:grid-cols-3">{members.map((item, index) => <button key={item.name} onClick={() => setSelected(index)} className={`rounded-2xl border p-5 text-left transition ${selected === index ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10 bg-white/[0.04]"}`}><div className="flex items-start justify-between"><div className="flex size-11 items-center justify-center rounded-2xl bg-cyan-300/10 text-cyan-200"><UserRound className="size-5" /></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Unavailable</Badge></div><h2 className="mt-4 font-bold">{item.name}</h2><p className="mt-1 text-xs text-cyan-200">{item.role}</p><p className="mt-3 text-sm leading-6 text-slate-400">{item.detail}</p></button>)}</section><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200">Selected organization concept</p><h2 className="mt-2 text-2xl font-black">{member.name}</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{member.role}</Badge></div><p className="mt-3 text-sm leading-6 text-slate-400">{member.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Organization identity", value: "Unavailable", icon: Users }, { label: "Member identity", value: "Unavailable", icon: UserRound }, { label: "Role / permission", value: "Unconfigured", icon: KeyRound }, { label: "Approval / consent", value: "Required", icon: ClipboardCheck }, { label: "Audit / access log", value: "Unavailable", icon: ScrollText }, { label: "Offboarding / recovery", value: "Blocked", icon: ShieldAlert }].map(({ label, value, icon: Icon }) => <div key={label} className="flex items-center gap-3 rounded-xl border border-white/10 p-4"><Icon className="size-4 text-cyan-300" /><span className="flex-1 text-sm text-slate-300">{label}</span><span className="text-xs text-amber-200">{value}</span></div>)}</div><div className="mt-6 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Change role unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Grant access unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Remove member unavailable</Button></div></CardContent></Card><ScreenStatePanel type="unavailable" title="Organization source unavailable" description="Connect authenticated organization identity, membership persistence, role policy, authorization checks, invitations, audit logging, offboarding, and recovery before enabling team changes." /></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><LockKeyhole className="size-5 text-cyan-300" /><div><p className="font-bold">Team evidence contract</p><p className="mt-1 text-sm text-slate-400">Production team management requires tenant isolation, verified identity, least privilege, approval, invitation lifecycle, auditability, privacy, retention, offboarding, and recovery controls.</p></div></div><div className="mt-5 grid gap-3 md:grid-cols-2">{["Authenticated organization, tenant scope, member identity, and consent", "Role definitions, resource scope, least privilege, separation of duties, and approvals", "Invitation delivery, expiration, acceptance, revocation, and notification evidence", "Authorization checks, session controls, access logs, and sensitive-action confirmation", "Audit history, corrections, export, retention, privacy, and deletion controls", "Offboarding, token/session revocation, recovery, incident response, and support"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3 text-sm text-slate-300"><LockKeyhole className="size-4 text-amber-300" />{item}<span className="ml-auto text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Organization surface preserved", description: "Members, invitations, roles, permissions, approvals, audit, offboarding, retry, save/reset, and mutation gates remain visible.", icon: Users, status: "Local preview" }, { title: "No identity theater", description: "People, memberships, access grants, roles, invitations, and audit events are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before mutation", description: "Real changes require authenticated organization scope, least privilege, approval, persistence, audit, and recovery controls.", icon: LockKeyhole, status: "Blocked" }]} /><p aria-live="polite" className="rounded-xl border border-white/10 bg-white/[0.03] p-4 text-sm text-slate-400">{status}</p></main></div>; }
