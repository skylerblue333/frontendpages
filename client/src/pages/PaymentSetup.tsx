import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Check, ChevronRight, ClipboardCheck, CreditCard, FileKey2, LockKeyhole, RefreshCw, ShieldCheck, TriangleAlert, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

type Stage = "business" | "provider" | "security" | "launch";
const stages: { id: Stage; label: string; description: string; icon: typeof CreditCard }[] = [
  { id: "business", label: "Business profile", description: "Define the merchant context", icon: ClipboardCheck },
  { id: "provider", label: "Provider connection", description: "Select a server-side rail", icon: CreditCard },
  { id: "security", label: "Security controls", description: "Prove secrets and events are safe", icon: LockKeyhole },
  { id: "launch", label: "Launch gate", description: "Review evidence before activation", icon: ShieldCheck },
];
export default function PaymentSetup() {
  const [stage, setStage] = useState<Stage>("business");
  const [name, setName] = useState("SKYCOIN4444 checkout");
  const [region, setRegion] = useState("");
  const [provider, setProvider] = useState("Not selected");
  const [acknowledged, setAcknowledged] = useState(false);
  const [saved, setSaved] = useState(false);
  const completed = useMemo(() => ({ business: name.trim().length >= 3 && region.trim().length >= 2, provider: provider !== "Not selected", security: acknowledged, launch: false }), [acknowledged, name, provider, region]);
  const completionCount = Object.values(completed).filter(Boolean).length;
  const reset = () => { setStage("business"); setName("SKYCOIN4444 checkout"); setRegion(""); setProvider("Not selected"); setAcknowledged(false); setSaved(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={ClipboardCheck} eyebrow="PaymentSetup · Readiness wizard" title="Build the evidence path before enabling payments." description="Walk through business context, provider intent, security controls, and a launch gate. This wizard stores only local UI state; no provider account, credential, webhook, payment method, charge, or transaction is configured." badge="Setup preview"><div className="flex flex-wrap gap-2"><Button disabled={completionCount < 3} onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Plan saved locally" : "Save readiness plan"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset setup</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Readiness", value: `${completionCount} / 4`, hint: "Local checks", icon: ClipboardCheck, tone: "cyan" }, { label: "Provider", value: provider === "Not selected" ? "Unset" : provider, hint: "No connection", icon: CreditCard, tone: "violet" }, { label: "Secrets", value: "Off", hint: "Never collected", icon: LockKeyhole, tone: "amber" }, { label: "Launch", value: "Blocked", hint: "Evidence missing", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="Payment setup evidence boundary"><strong>Completing these steps does not enable payments.</strong> The wizard does not create an account, verify a business, connect a provider, accept credentials, configure webhooks, validate compliance, create a payment method, or process a charge. It only demonstrates the UX required before a real integration could be considered.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.82fr_1fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Setup stages</p><h2 className="mt-2 text-2xl font-black">Evidence before activation</h2><div className="mt-5 space-y-2">{stages.map((item, index) => { const Icon = item.icon; return <button key={item.id} onClick={() => setStage(item.id)} className={`flex w-full items-center gap-3 rounded-xl border p-3 text-left ${stage === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className={`flex size-9 items-center justify-center rounded-xl ${completed[item.id] ? "bg-cyan-300 text-slate-950" : "bg-white/10 text-slate-400"}`}>{completed[item.id] ? <Check className="size-4" /> : <Icon className="size-4" />}</div><div className="flex-1"><p className="font-semibold">{index + 1}. {item.label}</p><p className="mt-1 text-xs text-slate-500">{item.description}</p></div><ChevronRight className="size-4 text-slate-600" /></button>; })}</div><div className="mt-6 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex gap-3"><TriangleAlert className="mt-0.5 size-5 shrink-0 text-amber-200" /><p className="text-sm leading-6 text-slate-300">Never enter API keys, provider secrets, private keys, seed phrases, customer payment data, or compliance documents into this local wizard.</p></div></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6">{stage === "business" && <div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Business profile</p><h2 className="mt-2 text-3xl font-black">Name the checkout context</h2><label className="mt-6 block text-sm text-slate-400">Workspace label<input value={name} onChange={(e) => { setName(e.target.value); setSaved(false); }} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-white outline-none focus:border-cyan-300/50" /></label><label className="mt-4 block text-sm text-slate-400">Operating region concept<input value={region} onChange={(e) => { setRegion(e.target.value); setSaved(false); }} placeholder="Example: US / EU / Global" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-white outline-none focus:border-cyan-300/50" /></label><p className="mt-4 text-sm leading-6 text-slate-500">These fields are local labels. They do not verify a legal entity, tax status, jurisdiction, merchant account, or regulatory scope.</p></div>}{stage === "provider" && <div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Provider connection</p><h2 className="mt-2 text-3xl font-black">Choose a rail concept</h2><div className="mt-6 grid gap-3 sm:grid-cols-2">{["PayPal", "Card processor", "Wallet rail", "Not decided"].map((item) => <button key={item} onClick={() => { setProvider(item); setSaved(false); }} className={`rounded-xl border p-4 text-left ${provider === item ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><p className="font-semibold">{item}</p><p className="mt-2 text-xs text-slate-500">{item === "Not decided" ? "Keep integration unselected" : "Concept only · no connection"}</p></button>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2">{[{ label: "Client credentials", value: "Never collected" }, { label: "Webhooks", value: "Not configured" }, { label: "Environment", value: "Unset" }, { label: "Merchant account", value: "Not verified" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div></div>}{stage === "security" && <div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Security controls</p><h2 className="mt-2 text-3xl font-black">Keep the trust boundary visible</h2><div className="mt-6 space-y-3">{["Server-side secret storage and rotation", "Webhook signature and replay protection", "Idempotency, authorization, and amount validation", "Audit-safe logs with no secrets or payment data", "Refund, dispute, failure, reconciliation, and incident response"].map((item) => <div key={item} className="flex items-start gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="mt-0.5 size-4 text-cyan-300" /><span className="text-sm leading-5 text-slate-300">{item}</span><Badge variant="outline" className="border-white/10 text-amber-200">Required</Badge></div>)}</div><label className="mt-6 flex gap-3 text-sm text-slate-400"><input type="checkbox" checked={acknowledged} onChange={(e) => { setAcknowledged(e.target.checked); setSaved(false); }} className="mt-1" />I understand these controls are requirements, not proof that they are implemented.</label></div>}{stage === "launch" && <div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Launch gate</p><h2 className="mt-2 text-3xl font-black">Do not launch on a green UI</h2><div className="mt-6 space-y-3">{["Provider contract tested in sandbox", "Secrets and environment isolation reviewed", "Webhook, idempotency, reconciliation, refund, and dispute tests passing", "Monitoring, support, privacy, audit, and incident response ready", "End-to-end charge and failure evidence attached"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><span className="flex size-5 items-center justify-center rounded-full border border-white/20 text-slate-600">?</span><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-rose-200">Unverified</span></div>)}<div className="mt-5 rounded-xl border border-rose-300/20 bg-rose-300/[0.06] p-4"><p className="text-sm leading-6 text-slate-300">Launch remains blocked because no live integration evidence is available. This is intentional.</p></div></div></div>}</CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Production checklist</p><h2 className="mt-2 text-2xl font-black">Setup must prove</h2><div className="mt-5 space-y-3">{["Provider account, environment, redirect URLs, credentials, scopes, and rotation", "Authenticated order/amount/currency validation and duplicate protection", "Server-side webhook verification, reconciliation, receipt, refund, and dispute paths", "Privacy/compliance scope, secure logs, monitoring, support, and incident response", "End-to-end tests with real provider evidence before enabling any payment action"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><FileKey2 className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake readiness</h2><p className="mt-3 text-sm leading-6 text-slate-400">Completing local steps changes only the preview. It does not connect a provider, verify compliance, enable checkout, or process money.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Four stages preserved", description: "Business, provider, security, and launch-gate concepts are interactive local states.", icon: ClipboardCheck, status: "Verified locally" }, { title: "Launch stays blocked", description: "Unverified production evidence is shown as unverified instead of a green success state.", icon: ShieldCheck, status: "Guardrail" }, { title: "Secrets stay out", description: "No provider credential, webhook secret, payment method, or transaction is collected.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
