import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { BookOpen, Check, FileCheck2, FlaskConical, History, Lightbulb, LockKeyhole, RefreshCw, Search, ShieldAlert, Sparkles, X, Zap } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const playbooks = [{ id: "pb-001", name: "Trust-first product delivery", area: "Governance", version: "Draft v0", status: "Local concept", sections: ["Evidence boundaries", "Release gates", "Recovery states"], lessons: ["Make unsupported claims visible", "Prefer reversible actions", "Measure before optimizing"] }, { id: "pb-002", name: "Gaming hub experience", area: "UX", version: "Draft v0", status: "Local concept", sections: ["Visual hierarchy", "Useful state", "Progression intent"], lessons: ["Give every surface a job", "Reward exploration without fake rewards", "Keep empty states actionable"] }, { id: "pb-003", name: "Financial product safeguards", area: "Risk", version: "Draft v0", status: "High-risk review", sections: ["Custody boundaries", "Market evidence", "Transaction safety"], lessons: ["Never fabricate balances", "Show transaction state", "Require independent verification"] }];
export default function ProductBrain() {
  const [query, setQuery] = useState("");
  const [selectedId, setSelectedId] = useState(playbooks[0].id);
  const [saved, setSaved] = useState(false);
  const [showLessons, setShowLessons] = useState(false);
  const visible = useMemo(() => playbooks.filter((item) => `${item.name} ${item.area} ${item.status}`.toLowerCase().includes(query.toLowerCase())), [query]);
  const selected = playbooks.find((item) => item.id === selectedId) ?? playbooks[0];
  const reset = () => { setQuery(""); setSelectedId(playbooks[0].id); setSaved(false); setShowLessons(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Lightbulb} eyebrow="ProductBrain · Knowledge workspace preview" title="Turn product learning into a reviewable artifact, not an invented intelligence score." description="Explore local playbook concepts, sections, lessons, version intent, and evidence requirements. No AI recommendation, usage count, effectiveness rate, adoption metric, institutional memory, or strategic decision is generated or asserted." badge="Product knowledge"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Notes saved locally" : "Save notes locally"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset workspace</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Playbooks", value: String(playbooks.length), hint: "Local concepts", icon: BookOpen, tone: "cyan" }, { label: "Usage", value: "Unmeasured", hint: "No event source", icon: Zap, tone: "violet" }, { label: "Effectiveness", value: "Unmeasured", hint: "No evaluation set", icon: FlaskConical, tone: "amber" }, { label: "Recommendations", value: "Off", hint: "No AI service", icon: LockKeyhole, tone: "slate" }]} /><ScreenPreviewBanner title="ProductBrain evidence boundary"><strong>This is a local product-knowledge library, not an autonomous product brain.</strong> Playbook names, sections, lessons, and statuses are authored concepts. No usage, adoption, effectiveness, growth, revenue, users, strategic recommendation, or institutional learning claim is measured or inferred. Any future AI insight must cite its source, confidence, scope, and human reviewer.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.8fr_1.2fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Playbook library</p><h2 className="mt-2 text-2xl font-black">Find a learning artifact</h2></div><Badge variant="outline" className="border-white/10 text-amber-200">Local</Badge></div><div className="relative mt-5"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search playbooks…" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-9 pr-3 text-sm text-white outline-none focus:border-cyan-300/50" /></div><div className="mt-4 space-y-3">{visible.map((item) => <button key={item.id} onClick={() => setSelectedId(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected.id === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start gap-3"><BookOpen className="mt-1 size-5 text-cyan-200" /><div className="flex-1"><div className="flex flex-wrap items-center gap-2"><p className="font-semibold">{item.name}</p><Badge variant="outline" className="border-white/10 text-slate-400">{item.status}</Badge></div><p className="mt-2 text-sm text-slate-500">{item.area} · {item.version}</p></div></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Playbook detail</p><h2 className="mt-2 text-2xl font-black">{selected.name}</h2><p className="mt-2 text-sm text-slate-500">{selected.area} · {selected.version}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">No metrics</Badge></div><div className="mt-6 grid gap-3 sm:grid-cols-3">{[{ label: "Version", value: selected.version }, { label: "Sections", value: String(selected.sections.length) }, { label: "Lessons", value: String(selected.lessons.length) }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-6 rounded-xl border border-white/10 p-5"><p className="text-sm font-semibold text-cyan-100">Playbook sections</p><div className="mt-3 space-y-2">{selected.sections.map((section) => <div key={section} className="flex items-center gap-3 text-sm text-slate-300"><BookOpen className="size-4 text-slate-500" />{section}</div>)}</div></div><Button onClick={() => setShowLessons((value) => !value)} variant="outline" className="mt-5 border-white/10 text-slate-300">{showLessons ? <X className="mr-2 size-4" /> : <History className="mr-2 size-4" />}{showLessons ? "Hide lessons" : "Review lessons"}</Button>{showLessons && <div className="mt-4 rounded-xl border border-violet-300/20 bg-violet-300/[0.06] p-4"><p className="text-sm font-semibold text-violet-100">Authored lesson concepts</p><div className="mt-3 space-y-2">{selected.lessons.map((lesson) => <p key={lesson} className="text-sm leading-6 text-slate-400">{lesson}</p>)}</div><p className="mt-3 text-xs text-amber-200">No usage or outcome evidence attached.</p></div>}</CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Insight guardrails</p><h2 className="mt-2 text-2xl font-black">What a real product brain must show</h2><div className="mt-5 space-y-3">{["Source documents, authorship, timestamps, and version history", "Evidence-backed extraction with citations and confidence", "Human review, disagreement, correction, and deletion workflow", "Access controls for sensitive strategy, user, financial, and security data", "Evaluation set for recommendations and safe failure when context is missing"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><FileCheck2 className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Knowledge surface preserved", description: "Playbook search, selection, sections, lessons, version intent, local save/reset, and insight guardrails remain interactive.", icon: BookOpen, status: "Local library" }, { title: "No intelligence theater", description: "Usage, adoption, effectiveness, growth, revenue, users, strategic recommendations, and institutional-memory claims are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Citations before insight", description: "AI-generated product guidance remains unavailable until sources, confidence, access controls, evaluation, and human review exist.", icon: LockKeyhole, status: "Blocked" }]} /></section></main></div>;
}
