import { useMemo, useState } from "react";
import { BookOpen, Check, FileSearch, Info, LockKeyhole, RefreshCw, Search, ShieldAlert, SlidersHorizontal, Star, TrendingUp, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatePanel, ScreenStatGrid } from "@/components/ScreenExperience";
const ideas = [{ symbol: "Equity research concept", category: "Equity", source: "No issuer or exchange source", status: "Unverified" }, { symbol: "Crypto research concept", category: "Crypto", source: "No market-data source", status: "Unavailable" }, { symbol: "Index research concept", category: "Index", source: "No methodology source", status: "Preview" }, { symbol: "Test symbol concept", category: "Test", source: "Local only", status: "Test-only" }];
export default function StockSearch() { const [query, setQuery] = useState(""); const [category, setCategory] = useState("All categories"); const [saved, setSaved] = useState(false); const [showBoundary, setShowBoundary] = useState(false); const [refreshing, setRefreshing] = useState(false); const refresh = () => { setRefreshing(true); window.setTimeout(() => setRefreshing(false), 500); }; const reset = () => { setQuery(""); setCategory("All categories"); setSaved(false); setShowBoundary(false); setRefreshing(false); }; const filtered = useMemo(() => ideas.filter((item) => (category === "All categories" || item.category === category) && `${item.symbol} ${item.category} ${item.source}`.toLowerCase().includes(query.toLowerCase())), [query, category]); return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Search} eyebrow="Stock search · Research preview" title="Search research concepts without inventing market facts." description="Explore a local research workspace with symbol search, categories, source provenance, notes, watchlist intent, refresh, save/reset, and blocked quote/trade/export actions. No live quote, issuer, market, portfolio, order, or financial state is connected." badge="Evidence-bounded market research"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save research view"}</Button><Button onClick={() => setShowBoundary((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showBoundary ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showBoundary ? "Close boundary" : "Review research gates"}</Button><Button onClick={refresh} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className={`mr-2 size-4 ${refreshing ? "animate-spin" : ""}`} />{refreshing ? "Retrying local state" : "Retry research source"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><X className="mr-2 size-4" />Reset search</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Results", value: `${filtered.length} local`, hint: "No live search source", icon: Search, tone: "cyan" }, { label: "Quotes", value: "Unavailable", hint: "No market source", icon: TrendingUp, tone: "violet" }, { label: "Sources", value: "Unverified", hint: "No provenance source", icon: FileSearch, tone: "amber" }, { label: "Portfolio", value: "Unconnected", hint: "No account source", icon: ShieldAlert, tone: "slate" }]} /><ScreenPreviewBanner title="Market-research evidence boundary"><strong>This is a local search-design preview, not evidence that any symbol, issuer, quote, market, source, valuation, portfolio, order, return, or financial position exists.</strong> Result cards, category filters, local saved state, source labels, notes, watchlist concepts, refresh feedback, and disabled quote/trade/export actions are browser concepts. No price, market cap, financial statement, recommendation, ownership, transaction, performance, or financial result is asserted.</ScreenPreviewBanner><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><label className="text-sm font-semibold text-slate-300"><span className="flex items-center gap-2"><Search className="size-4 text-slate-500" />Search research concepts</span><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search equity, crypto, index..." className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white outline-none" /></label><div className="mt-5 flex flex-wrap gap-2">{["All categories", "Equity", "Crypto", "Index", "Test"].map((item) => <Button key={item} onClick={() => setCategory(item)} size="sm" variant="outline" className={category === item ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-100" : "border-white/10 text-slate-400"}>{item}</Button>)}</div></CardContent></Card><section className="grid gap-4 md:grid-cols-2">{filtered.map((item) => <Card key={item.symbol} className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="flex items-start justify-between gap-3"><div className="flex size-11 items-center justify-center rounded-2xl bg-cyan-300/10 text-cyan-200"><FileSearch className="size-5" /></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.status}</Badge></div><h2 className="mt-4 font-bold">{item.symbol}</h2><Badge variant="outline" className="mt-3 border-white/10 text-slate-500">{item.category}</Badge><p className="mt-4 text-sm text-slate-400">{item.source}</p><div className="mt-4 flex items-center gap-3 text-xs text-slate-500"><Star className="size-3" />Watchlist unavailable <BookOpen className="size-3" />Research notes local-only</div></CardContent></Card>)}</section><ScreenStatePanel type="unavailable" title="Live research source unavailable" description="Connect a documented market-data and issuer-information source with timestamps, symbol mapping, source licensing, adjustment policy, freshness, and risk disclosures before reporting a quote or research conclusion." /><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="grid gap-3 md:grid-cols-3">{[{ title: "Source provenance", text: "No issuer, exchange, market-data, filing, methodology, or timestamp source is connected.", icon: Info }, { title: "Research notes", text: "No recommendation, valuation, suitability, risk, or financial conclusion is generated.", icon: BookOpen }, { title: "Portfolio context", text: "No account, ownership, position, order, performance, or tax source is connected.", icon: SlidersHorizontal }].map(({ title, text, icon: Icon }) => <div key={title} className="rounded-xl border border-white/10 p-4"><Icon className="size-5 text-cyan-300" /><p className="mt-3 font-semibold">{title}</p><p className="mt-2 text-sm leading-6 text-slate-500">{text}</p></div>)}</div></CardContent></Card><div className="flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Load live results unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Open chart unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Trade unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Export research unavailable</Button></div>{showBoundary && <Card className="border-amber-300/20 bg-amber-300/[0.06]"><CardContent className="p-6"><p className="font-semibold text-amber-100">No recommendation or financial claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local search concept does not prove a symbol, quote, market, issuer, recommendation, valuation, portfolio, order, return, or financial position. Verify source and suitability before acting.</p></CardContent></Card>}<ScreenFeatureGrid features={[{ title: "Research surface preserved", description: "Search, categories, results, provenance, notes, watchlist, chart intent, retry, save/reset, export, trade, and evidence gates remain visible.", icon: Search, status: "Local research" }, { title: "No recommendation theater", description: "Quotes, issuer facts, valuations, recommendations, orders, returns, and financial outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before action", description: "Real research requires documented sources, timestamps, definitions, licensing, risk disclosure, and accountable review.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>; }
