import { useMemo, useState } from "react";
import { BadgeDollarSign, Blocks, CheckCircle2, Clock3, Coins, FileCheck2, Gift, Hash, LockKeyhole, Search, Shield, WalletCards, Zap } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

type Program = { id: string; name: string; mechanism: string; status: "Design preview" | "Contract required" | "Eligibility unavailable"; description: string; checks: string[] };
const PROGRAMS: Program[] = [
  { id: "validator", name: "Validator reward", mechanism: "Consensus participation", status: "Contract required", description: "A reward design requiring chain rules, validator eligibility, slashing, distribution, and an auditable contract.", checks: ["Network and chain ID", "Validator proof", "Emission schedule", "Claim transaction"] },
  { id: "liquidity", name: "Liquidity contribution", mechanism: "Pool participation", status: "Eligibility unavailable", description: "A high-risk program requiring pool accounting, lock terms, price risk disclosure, and verified reward calculation.", checks: ["Pool address", "Position proof", "Reward formula", "Exit and loss policy"] },
  { id: "community", name: "Community contribution", mechanism: "Reviewed contribution", status: "Design preview", description: "A reviewed contribution program requiring evidence, human approval, anti-abuse controls, and a distribution policy.", checks: ["Contribution evidence", "Reviewer decision", "Anti-abuse policy", "Payout authorization"] },
  { id: "education", name: "Learning reward", mechanism: "Education milestone", status: "Eligibility unavailable", description: "An education incentive requiring completion verification, certificate policy, jurisdiction review, and reward custody.", checks: ["Course identity", "Completion proof", "Eligibility rule", "Reward settlement"] },
];

export default function BlockRewards() { const [query, setQuery] = useState(""); const [selected, setSelected] = useState<Program | null>(null); const [prepared, setPrepared] = useState(false); const visible = useMemo(() => PROGRAMS.filter((program) => `${program.name} ${program.mechanism} ${program.status} ${program.description}`.toLowerCase().includes(query.toLowerCase())), [query]); return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Coins} eyebrow="Crypto · Rewards" title="Design reward systems that can survive scrutiny." description="Explore reward-mechanism templates, eligibility, emission, claims, custody, anti-abuse, and failure requirements. This page does not claim balances, token rewards, staking yield, mining output, eligibility, or successful wallet transactions." badge="Preview reward workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setPrepared(true)} disabled={!selected} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><FileCheck2 className="mr-2 size-4" />Prepare local review</Button><Button variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><Shield className="mr-2 size-4" />Reward safety checklist</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Reward programs", value: String(PROGRAMS.length), hint: "Mechanism templates", icon: Coins }, { label: "Live eligibility", value: "Unavailable", hint: "No wallet or contract source", icon: WalletCards, tone: "amber" }, { label: "Emission", value: "Unverified", hint: "No supply or schedule claim", icon: Zap, tone: "violet" }, { label: "Token transfer", value: "Blocked", hint: "No wallet action enabled", icon: LockKeyhole, tone: "slate" }]} /><ScreenPreviewBanner title="Reward evidence boundary">Reward templates, search, selected detail, eligibility and emission requirements, local review preparation, and unavailable live state are available for UX review. No balance, APR/APY, reward amount, mining result, staking result, wallet owner, token supply, or transaction success is fabricated.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.85fr_1.15fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="relative"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search reward programs" aria-label="Search reward programs" className="border-white/10 bg-black/20 pl-9 text-white placeholder:text-slate-500" /></div><div className="mt-4 space-y-2">{visible.map((program) => <button key={program.id} onClick={() => { setSelected(program); setPrepared(false); }} className={`w-full rounded-xl border p-4 text-left transition ${selected?.id === program.id ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-black/15 hover:bg-white/[0.05]"}`}><div className="flex items-center gap-2"><Coins className="size-4 text-cyan-300" /><span className="font-semibold">{program.name}</span><Badge variant="outline" className="ml-auto border-amber-300/20 text-amber-200">{program.status}</Badge></div><p className="mt-2 text-xs uppercase tracking-wider text-slate-500">{program.mechanism}</p><p className="mt-2 text-sm leading-6 text-slate-400">{program.description}</p></button>)}{visible.length === 0 && <ScreenStatePanel type="empty" title="No reward programs match" description="Try another mechanism, status, or keyword." />}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6">{selected ? <><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Selected reward program</p><h2 className="mt-1 text-2xl font-bold">{selected.name}</h2><p className="mt-2 text-sm text-slate-400">{selected.mechanism}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{selected.status}</Badge></div><p className="mt-5 text-sm leading-6 text-slate-300">{selected.description}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{selected.checks.map((check) => <div key={check} className="rounded-lg border border-white/10 bg-black/15 p-4"><CheckCircle2 className="size-4 text-emerald-300" /><p className="mt-3 text-sm text-slate-300">{check}</p><p className="mt-1 text-xs text-slate-500">Evidence required</p></div>)}</div><div className="mt-6 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex items-center gap-2 font-medium text-amber-200"><Shield className="size-4" />No live claim available</div><p className="mt-2 text-sm leading-6 text-slate-300">A real reward program requires audited contracts, validated eligibility, explicit risk disclosures, transaction simulation, and failure reconciliation.</p></div>{prepared && <div className="mt-5 rounded-xl border border-cyan-300/20 bg-cyan-300/[0.06] p-4"><div className="flex items-center gap-2 font-medium text-cyan-200"><FileCheck2 className="size-4" />Local reward review prepared</div><p className="mt-2 text-sm leading-6 text-slate-300">No wallet was connected, no balance changed, and no claim or transaction was submitted.</p></div>}</> : <ScreenStatePanel type="empty" title="Select a reward program" description="Inspect eligibility, emission, custody, anti-abuse, claim, and failure requirements." />}</CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Contract before reward", description: "Validate network, contract, emission schedule, eligibility, and failure handling before showing a reward amount.", icon: Blocks, status: "Required" }, { title: "Risk before incentive", description: "Explain price, custody, lock, slashing, liquidity, tax, and loss risks without presenting yield as guaranteed.", icon: Shield, status: "Required" }, { title: "No fake tokens", description: "A reward template is not a balance, token supply, mining result, staking return, wallet claim, or transaction success.", icon: LockKeyhole, status: "Guardrail" }]} /></main></div>; }
