import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { BarChart3, Calculator, Check, FileText, LockKeyhole, RefreshCw, ShieldAlert, SlidersHorizontal, Target, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const streams = [{ name: "AI tools", model: "Usage concept", status: "Unverified" }, { name: "Digital goods", model: "Catalog concept", status: "Unverified" }, { name: "Education", model: "Course concept", status: "Unverified" }, { name: "Creator services", model: "Marketplace concept", status: "Unverified" }, { name: "Subscriptions", model: "Plan concept", status: "Unverified" }, { name: "Infrastructure", model: "Service concept", status: "Unverified" }];
export default function Profitability() {
  const [selected, setSelected] = useState(streams[0].name);
  const [horizon, setHorizon] = useState("12 months");
  const [adoption, setAdoption] = useState("Illustrative only");
  const [saved, setSaved] = useState(false);
  const [showGates, setShowGates] = useState(false);
  const [revenueAssumption, setRevenueAssumption] = useState("Not entered");
  const margin = useMemo(() => revenueAssumption === "Not entered" ? "Uncomputed" : "Scenario only", [revenueAssumption]);
  const reset = () => { setSelected(streams[0].name); setHorizon("12 months"); setAdoption("Illustrative only"); setSaved(false); setShowGates(false); setRevenueAssumption("Not entered"); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Calculator} eyebrow="Profitability · Scenario preview" title="Model the questions before claiming the outcome." description="Review local revenue-stream concepts, assumptions, cost gates, and sensitivity controls. No live revenue, users, prices, costs, returns, forecast, market data, or investment recommendation is produced." badge="Profitability worksheet"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Scenario saved locally" : "Save scenario locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review evidence gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset scenario</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Streams", value: `${streams.length} concepts`, hint: "Not validated", icon: Target, tone: "cyan" }, { label: "Revenue", value: "Unmeasured", hint: "No ledger source", icon: BarChart3, tone: "violet" }, { label: "Margin", value: margin, hint: "No cost inputs", icon: Calculator, tone: "amber" }, { label: "Decision", value: "Not provided", hint: "No recommendation", icon: LockKeyhole, tone: "slate" }]} /><ScreenPreviewBanner title="Profitability evidence boundary"><strong>This is an illustrative scenario worksheet, not a financial forecast.</strong> Stream labels, assumptions, controls, and uncomputed outputs are browser state. No revenue, users, prices, costs, margins, returns, yield, break-even, market data, business performance, or investment recommendation is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.88fr_1.12fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Revenue concepts</p><h2 className="mt-2 text-2xl font-black">Choose a stream to inspect</h2></div><Badge variant="outline" className="border-white/10 text-amber-200">All unverified</Badge></div><div className="mt-5 space-y-3">{streams.map((stream) => <button key={stream.name} onClick={() => setSelected(stream.name)} className={`flex w-full items-center gap-3 rounded-xl border p-4 text-left ${selected === stream.name ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><Target className="size-5 text-cyan-200" /><div className="flex-1"><p className="font-semibold">{stream.name}</p><p className="mt-1 text-sm text-slate-500">{stream.model}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{stream.status}</Badge></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="space-y-6 p-6"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">{selected} scenario</p><h2 className="mt-2 text-2xl font-black">Assumptions, not outcomes</h2></div><div><p className="text-sm text-slate-400">Planning horizon</p><div className="mt-2 flex flex-wrap gap-2">{["3 months", "12 months", "24 months"].map((item) => <button key={item} onClick={() => setHorizon(item)} className={`rounded-lg border px-3 py-2 text-xs ${horizon === item ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div></div><div><p className="text-sm text-slate-400">Adoption assumption</p><div className="mt-2 flex flex-wrap gap-2">{["Illustrative only", "Low case", "Base case", "High case"].map((item) => <button key={item} onClick={() => setAdoption(item)} className={`rounded-lg border px-3 py-2 text-xs ${adoption === item ? "border-violet-300/40 bg-violet-300/10 text-violet-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div></div><label className="block text-sm text-slate-400">Revenue assumption<input value={revenueAssumption} onChange={(event) => setRevenueAssumption(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-white outline-none" /></label><div className="grid gap-3 sm:grid-cols-3">{[{ label: "Revenue", value: "Not computed" }, { label: "Costs", value: "Not entered" }, { label: "Break-even", value: "Blocked" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="rounded-2xl border border-amber-300/25 bg-amber-300/[0.06] p-5"><div className="flex items-start gap-3"><ShieldAlert className="mt-0.5 size-5 shrink-0 text-amber-200" /><div><p className="font-semibold text-amber-100">Forecast blocked</p><p className="mt-2 text-sm leading-6 text-slate-400">No validated inputs, users, prices, costs, conversion, retention, revenue ledger, tax treatment, market data, or model evidence are connected.</p><Button disabled className="mt-4 bg-slate-700 text-slate-400">Generate forecast unavailable</Button></div></div></div></CardContent></Card></section>{showGates && <Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Evidence gates</p><h2 className="mt-2 text-2xl font-black">What a real profitability model must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Source-controlled users, prices, volume, conversion, churn, and retention", "Recognized revenue, refunds, taxes, fees, costs, and cash reconciliation", "Versioned assumptions with scenario lineage and sensitivity analysis", "Forecast methodology, uncertainty, calibration, backtesting, and review", "Authorization, privacy, audit, data freshness, and rollback", "No investment, financing, or guaranteed-return language without governed review"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><FileText className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card>}<ScreenFeatureGrid features={[{ title: "Profitability surface preserved", description: "Revenue-stream concepts, scenario selection, assumptions, sensitivity intent, cost gates, blocked forecast, save/reset, and evidence requirements remain interactive.", icon: Calculator, status: "Local model" }, { title: "No financial theater", description: "Revenue, users, prices, costs, margins, returns, market data, forecasts, and recommendations are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before decisions", description: "Real profitability requires reconciled data, versioned assumptions, uncertainty, audit, privacy, and governed financial review.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
