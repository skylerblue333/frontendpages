import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Activity, AlertTriangle, Check, CircleDot, Clock3, FileCheck2, Gauge, LockKeyhole, RefreshCw, Server, ShieldCheck, WifiOff, XCircle } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const SERVICES = [{ name: "Web interface", status: "Fixture", detail: "Browser surface only", icon: Activity }, { name: "API contract", status: "Unavailable", detail: "No live probe", icon: Server }, { name: "Data layer", status: "Unverified", detail: "No database probe", icon: CircleDot }, { name: "External providers", status: "Blocked", detail: "No integration check", icon: WifiOff }];
const DRILLS = ["Dependency timeout", "Authentication failure", "Database saturation", "Unknown incident"];

export default function NetworkHealth() {
  const [filter, setFilter] = useState("All");
  const [drill, setDrill] = useState(DRILLS[0]);
  const [refreshed, setRefreshed] = useState(false);
  const [note, setNote] = useState("");
  const [saved, setSaved] = useState(false);
  const visible = SERVICES.filter((item) => filter === "All" || item.status === filter);
  const reset = () => { setFilter("All"); setDrill(DRILLS[0]); setRefreshed(false); setNote(""); setSaved(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Activity} eyebrow="NetworkHealth · Observability readiness" title="Instrument the system before declaring it healthy." description="Review synthetic service checks, incident-drill concepts, freshness labels, and operational readiness gates. No uptime, latency, throughput, node status, security posture, availability, incident response, or live monitoring claim is made." badge="Health preview"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Runbook saved locally" : "Save runbook locally"}</Button><Button onClick={() => setRefreshed(true)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className={`mr-2 size-4 ${refreshed ? "text-cyan-200" : ""}`} />{refreshed ? "Fixture refreshed" : "Refresh fixture"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">Reset</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Checks", value: String(SERVICES.length), hint: "Synthetic fixtures", icon: Gauge }, { label: "Live source", value: "Off", hint: "No probe", icon: WifiOff, tone: "slate" }, { label: "Freshness", value: "None", hint: "No telemetry", icon: Clock3, tone: "amber" }, { label: "Incident", value: "Drill", hint: drill, icon: AlertTriangle, tone: "violet" }]} /><ScreenPreviewBanner title="NetworkHealth evidence boundary"><strong>Service labels, status badges, refresh state, incident drills, and readiness gates are observability fixtures—not live uptime, latency, throughput, node health, monitoring coverage, security posture, incident count, SLO, or availability evidence.</strong> Production health requires instrumented services, authenticated probes, timestamps, alert ownership, SLO definitions, dependency visibility, incident response, status communication, and privacy-safe structured logs.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Synthetic checks</p><h2 className="mt-2 text-3xl font-black">Service readiness</h2></div><div className="flex gap-2">{["All", "Fixture", "Unavailable", "Unverified", "Blocked"].map((item) => <button key={item} onClick={() => setFilter(item)} className={`rounded-lg border px-2 py-1 text-[10px] ${filter === item ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div></div><div className="mt-5 space-y-3">{visible.map((item) => { const Icon = item.icon; return <div key={item.name} className="flex items-center gap-3 rounded-2xl border border-white/10 p-4"><div className="flex size-10 items-center justify-center rounded-xl bg-cyan-300/10 text-cyan-200"><Icon className="size-5" /></div><div className="flex-1"><p className="font-semibold">{item.name}</p><p className="text-xs text-slate-500">{item.detail}</p></div><Badge variant="outline" className="border-white/10 text-amber-200">{item.status}</Badge></div>; })}{visible.length === 0 && <p className="py-8 text-center text-slate-500">No checks match this filter.</p>}</div><div className="mt-5 flex items-start gap-3 text-xs leading-5 text-slate-500"><LockKeyhole className="mt-0.5 size-4 shrink-0 text-cyan-300" />Refresh only changes local fixture state. It does not probe a server, database, provider, node, account, or network.</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Incident drill</p><h2 className="mt-2 text-3xl font-black">Practice the boundary</h2><div className="mt-4 space-y-2">{DRILLS.map((item) => <button key={item} onClick={() => setDrill(item)} className={`w-full rounded-xl border p-3 text-left text-sm ${drill === item ? "border-violet-300/40 bg-violet-300/[0.08]" : "border-white/10"}`}>{item}</button>)}</div><div className="mt-5 rounded-2xl border border-violet-300/20 bg-violet-300/[0.05] p-5"><p className="text-xs uppercase tracking-[0.2em] text-violet-200">Selected drill</p><p className="mt-2 text-lg font-black">{drill}</p><p className="mt-2 text-sm leading-6 text-slate-400">No incident is active. Define ownership, evidence, communication, mitigation, recovery, and follow-up before operating a real service.</p></div><div className="mt-4 grid gap-3 sm:grid-cols-2">{[{ label: "Alert", value: "Not sourced" }, { label: "Owner", value: "Unassigned" }, { label: "Impact", value: "Unknown" }, { label: "Status page", value: "Unavailable" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Observability readiness</p><h2 className="mt-2 text-2xl font-black">Required before health claims</h2><div className="mt-5 space-y-3">{["Authenticated probes, timestamps, service ownership, and dependency map", "SLOs, latency/throughput definitions, error budgets, and alert thresholds", "Structured logs, traces, dashboards, privacy controls, and retention", "Incident response, escalation, status communication, and postmortems", "Synthetic checks, failure handling, recovery verification, and support"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><FileCheck2 className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake health</h2><p className="mt-3 text-sm leading-6 text-slate-400">The preview does not report uptime, latency, throughput, security, availability, incidents, nodes, or current platform status.</p></CardContent></Card></section><textarea value={note} onChange={(event) => { setNote(event.target.value); setSaved(false); }} placeholder="Write a local incident or observability note…" className="min-h-24 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-sm text-white placeholder:text-slate-500" /><ScreenFeatureGrid features={[{ title: "A badge is not telemetry", description: "Synthetic statuses preserve operations UX without live health or availability claims.", icon: Gauge, status: "Guardrail" }, { title: "Incidents need ownership", description: "Alerts, SLOs, escalation, communication, and recovery evidence need operations systems.", icon: ShieldCheck, status: "Required" }, { title: "No fake probe", description: "Refresh changes only local fixture state and makes no server, provider, node, or database request.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
