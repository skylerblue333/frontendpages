import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Accessibility, Bell, Check, ChevronRight, Eye, Globe2, LockKeyhole, Moon, RefreshCw, Save, Settings2, ShieldAlert, Sparkles, Sun, ToggleLeft, Volume2, X, Zap } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

type Preference = { id: string; label: string; description: string; icon: typeof Moon; value: boolean };
const initialPreferences: Preference[] = [
  { id: "motion", label: "Reduced motion", description: "Prefer calmer transitions and avoid non-essential movement.", icon: Accessibility, value: false },
  { id: "notifications", label: "Product notifications", description: "Show notification preferences without enabling email, push, or SMS delivery.", icon: Bell, value: true },
  { id: "focus", label: "Focus mode", description: "Prioritize essential controls and reduce secondary visual noise.", icon: Eye, value: false },
  { id: "sound", label: "Interface sounds", description: "Keep sound intent visible; browser audio permission is not requested here.", icon: Volume2, value: false },
];
const languages = ["English", "Spanish", "French", "German"];
export default function PreferencesSetup() {
  const [preferences, setPreferences] = useState(initialPreferences);
  const [theme, setTheme] = useState("System");
  const [language, setLanguage] = useState(languages[0]);
  const [density, setDensity] = useState("Comfortable");
  const [saved, setSaved] = useState(false);
  const [showPrivacy, setShowPrivacy] = useState(false);
  const toggle = (id: string) => { setSaved(false); setPreferences((items) => items.map((item) => item.id === id ? { ...item, value: !item.value } : item)); };
  const reset = () => { setPreferences(initialPreferences); setTheme("System"); setLanguage(languages[0]); setDensity("Comfortable"); setSaved(false); setShowPrivacy(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Settings2} eyebrow="PreferencesSetup · Experience setup" title="Make the experience yours without hiding what is saved." description="Configure accessibility, appearance, language, density, focus, notification intent, and privacy choices. Changes are interactive in this preview; account persistence and external delivery remain unavailable until an authenticated settings service is connected." badge="Preference center"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Save className="mr-2 size-4" />{saved ? "Preview saved" : "Save preview"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset preferences</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Preferences", value: String(preferences.length), hint: "Local controls", icon: Settings2, tone: "cyan" }, { label: "Theme", value: theme, hint: "Preview only", icon: theme === "Light" ? Sun : Moon, tone: "violet" }, { label: "Language", value: language, hint: "Copy not translated", icon: Globe2, tone: "amber" }, { label: "Persistence", value: "Off", hint: "No account service", icon: LockKeyhole, tone: "slate" }]} /><ScreenPreviewBanner title="Preference evidence boundary"><strong>These controls do not change a remote account.</strong> Toggles and selections are held in browser state for this preview. No email, push, SMS, browser audio, analytics, profile, consent, privacy, or authentication preference is persisted or transmitted. The page never claims a saved account state.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Experience controls</p><h2 className="mt-2 text-2xl font-black">Accessibility and behavior</h2></div><Badge variant="outline" className="border-white/10 text-amber-200">Browser state</Badge></div><div className="mt-5 space-y-3">{preferences.map((item) => { const Icon = item.icon; return <div key={item.id} className="flex items-center gap-3 rounded-xl border border-white/10 p-4"><div className="flex size-10 shrink-0 items-center justify-center rounded-xl bg-cyan-300/10"><Icon className="size-5 text-cyan-200" /></div><div className="flex-1"><p className="font-semibold">{item.label}</p><p className="mt-1 text-sm leading-5 text-slate-500">{item.description}</p></div><button aria-pressed={item.value} onClick={() => toggle(item.id)} className={`relative h-7 w-12 rounded-full border transition ${item.value ? "border-cyan-300/50 bg-cyan-300/30" : "border-white/15 bg-white/5"}`}><span className={`absolute top-1 size-5 rounded-full bg-white transition ${item.value ? "left-6" : "left-1"}`} /></button></div>; })}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Display setup</p><h2 className="mt-2 text-2xl font-black">Choose your baseline</h2><div className="mt-5 space-y-4"><label className="block text-sm text-slate-400">Theme<select value={theme} onChange={(event) => { setTheme(event.target.value); setSaved(false); }} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-white outline-none"><option>System</option><option>Dark</option><option>Light</option></select></label><label className="block text-sm text-slate-400">Language<select value={language} onChange={(event) => { setLanguage(event.target.value); setSaved(false); }} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-white outline-none">{languages.map((item) => <option key={item}>{item}</option>)}</select></label><label className="block text-sm text-slate-400">Density<select value={density} onChange={(event) => { setDensity(event.target.value); setSaved(false); }} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-white outline-none"><option>Comfortable</option><option>Compact</option><option>Spacious</option></select></label></div><div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="text-sm font-semibold text-amber-100">Translation and theme boundary</p><p className="mt-2 text-sm leading-6 text-slate-400">Selections demonstrate intent. Most product copy remains English and the preview does not change the full application theme or load translation bundles.</p></div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[0.9fr_1.1fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Privacy center</p><h2 className="mt-2 text-2xl font-black">Review before saving</h2></div><Button onClick={() => setShowPrivacy((value) => !value)} variant="outline" className="border-white/10 text-slate-300">{showPrivacy ? <X className="size-4" /> : <ChevronRight className="size-4" />}</Button></div><div className="mt-5 space-y-3">{[{ label: "Account persistence", value: "Unavailable", icon: LockKeyhole }, { label: "Notification delivery", value: "Not configured", icon: Bell }, { label: "Analytics consent", value: "Not collected", icon: ShieldAlert }, { label: "Browser permissions", value: "Not requested", icon: Eye }].map(({ label, value, icon: Icon }) => <div key={label} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><Icon className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{label}</span><span className="text-xs text-amber-200">{value}</span></div>)}</div>{showPrivacy && <div className="mt-5 rounded-xl border border-cyan-300/20 bg-cyan-300/[0.05] p-4"><p className="text-sm leading-6 text-slate-300">The current preview intentionally has no network write path. Saving only acknowledges the local choices above; it does not create consent, profile, account, notification, or privacy records.</p></div>}</CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Setup readiness</p><h2 className="mt-2 text-2xl font-black">What production setup must prove</h2><div className="mt-5 space-y-3">{["Authenticated account ownership, per-user persistence, versioning, and conflict handling", "Accessible keyboard, screen-reader, contrast, focus, reduced-motion, and localization behavior", "Notification channels, consent, unsubscribe, delivery failure, quiet hours, and audit", "Privacy controls, export/delete semantics, retention, data minimization, and regional policy", "Safe fallback when storage, translation, preference sync, or permission APIs are unavailable"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><Check className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Preference surfaces preserved", description: "Accessibility toggles, notifications, focus, sounds, theme, language, density, privacy review, save, and reset are interactive.", icon: Accessibility, status: "Local preview" }, { title: "No silent persistence", description: "The UI clearly separates browser state from account state and does not claim remote save, consent, or notification delivery.", icon: ShieldAlert, status: "Guardrail" }, { title: "Ready for governed sync", description: "The production checklist names identity, accessibility, privacy, notification, localization, and offline requirements before wiring a backend.", icon: Sparkles, status: "Requirements" }]} /></main></div>;
}
