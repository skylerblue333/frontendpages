import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Check, ClipboardCheck, FileClock, History, LockKeyhole, RefreshCw, Search, ShieldAlert, SlidersHorizontal, Tag, Users, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

type CatalogItem = { id: string; name: string; tier: string; price: string; status: string; version: string; owner: string };
const initialItems: CatalogItem[] = [
  { id: "catalog-001", name: "Citizen access", tier: "Citizen", price: "Unpublished", status: "Draft", version: "Local v0", owner: "Unassigned" },
  { id: "catalog-002", name: "Builder access", tier: "Builder", price: "Unpublished", status: "Review required", version: "Local v0", owner: "Unassigned" },
  { id: "catalog-003", name: "Scalable access", tier: "Scalable", price: "Unavailable", status: "Blocked", version: "Unknown", owner: "Unassigned" },
];
const gates = ["Authenticated administrator and scoped role", "Currency, region, tax, price source, and effective dates", "Approval workflow, separation of duties, and version history", "Entitlement mapping, billing reconciliation, and rollback", "Audit log, preview, publishing guard, and outage behavior"];
export default function PricingManagement() {
  const [items, setItems] = useState(initialItems);
  const [query, setQuery] = useState("");
  const [selectedId, setSelectedId] = useState(initialItems[1].id);
  const [saved, setSaved] = useState(false);
  const [showHistory, setShowHistory] = useState(false);
  const visible = useMemo(() => items.filter((item) => `${item.name} ${item.tier} ${item.status}`.toLowerCase().includes(query.toLowerCase())), [items, query]);
  const selected = items.find((item) => item.id === selectedId) ?? items[0];
  const reset = () => { setItems(initialItems); setQuery(""); setSelectedId(initialItems[1].id); setSaved(false); setShowHistory(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Tag} eyebrow="PricingManagement · Catalog administration preview" title="Manage the price book only after governance is ready." description="Review catalog entries, tier status, version intent, ownership, approval gates, and audit requirements. No live price is edited or published, no role is changed, and no entitlement or billing record is written." badge="Catalog admin"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Draft saved locally" : "Save draft locally"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset catalog</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Catalog entries", value: String(items.length), hint: "Local drafts", icon: Tag, tone: "cyan" }, { label: "Published", value: "0", hint: "Publishing blocked", icon: LockKeyhole, tone: "violet" }, { label: "Approvals", value: "0", hint: "No workflow source", icon: ClipboardCheck, tone: "amber" }, { label: "Audit", value: "Off", hint: "No admin service", icon: ShieldAlert, tone: "slate" }]} /><ScreenPreviewBanner title="Catalog administration evidence boundary"><strong>This is a local catalog preview, not a billing administration console.</strong> Entries, statuses, versions, and owners are browser-state concepts. No live price, tax, currency, customer, entitlement, invoice, payment, approval, role, audit event, or publish action is claimed or executed.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.85fr_1.15fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Price book</p><h2 className="mt-2 text-2xl font-black">Inspect catalog drafts</h2></div><Badge variant="outline" className="border-white/10 text-amber-200">{visible.length} listed</Badge></div><div className="relative mt-5"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search catalog entries…" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-9 pr-3 text-sm text-white outline-none focus:border-cyan-300/50" /></div><div className="mt-4 space-y-3">{visible.map((item) => <button key={item.id} onClick={() => setSelectedId(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected.id === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start gap-3"><Tag className="mt-1 size-5 text-cyan-200" /><div className="flex-1"><div className="flex flex-wrap items-center gap-2"><p className="font-semibold">{item.name}</p><Badge variant="outline" className="border-white/10 text-slate-400">{item.status}</Badge></div><p className="mt-2 text-sm text-slate-500">{item.tier} · {item.price} · {item.version}</p><p className="mt-2 text-xs text-amber-200">Owner: {item.owner}</p></div></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Catalog detail</p><h2 className="mt-2 text-2xl font-black">{selected.name}</h2><p className="mt-2 text-sm text-slate-500">Selected local catalog record.</p></div><Button onClick={() => setShowHistory((value) => !value)} variant="outline" className="border-white/10 text-slate-300">{showHistory ? <X className="size-4" /> : <History className="mr-2 size-4" />} {showHistory ? "Close" : "History"}</Button></div><div className="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{[{ label: "Tier", value: selected.tier }, { label: "Price", value: selected.price }, { label: "Status", value: selected.status }, { label: "Version", value: selected.version }, { label: "Owner", value: selected.owner }, { label: "Publish", value: "Blocked" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div>{showHistory && <div className="mt-5 rounded-xl border border-violet-300/20 bg-violet-300/[0.06] p-4"><div className="flex items-center gap-3"><FileClock className="size-5 text-violet-200" /><div><p className="font-semibold">History is unavailable</p><p className="mt-1 text-sm leading-6 text-slate-400">No catalog version store or admin audit service is connected. The local version label does not prove an immutable history.</p></div></div></div>}<div className="mt-6 rounded-2xl border border-amber-300/25 bg-amber-300/[0.06] p-5"><div className="flex items-start gap-3"><LockKeyhole className="mt-0.5 size-5 shrink-0 text-amber-200" /><div><p className="font-semibold text-amber-100">Publishing is blocked</p><p className="mt-2 text-sm leading-6 text-slate-400">A real publish action requires scoped admin authorization, review, price/tax validation, effective-date handling, entitlement reconciliation, rollback, and an audit event.</p><Button disabled className="mt-4 bg-slate-700 text-slate-400">Publish unavailable</Button></div></div></div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Administration gates</p><h2 className="mt-2 text-2xl font-black">Required before production</h2><div className="mt-5 space-y-3">{gates.map((gate) => <div key={gate} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ClipboardCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{gate}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Catalog surface preserved", description: "Price-book search, catalog selection, status/version/owner detail, local history view, save/reset, and publish state remain inspectable.", icon: SlidersHorizontal, status: "Local admin" }, { title: "No live pricing claims", description: "Prices, taxes, currencies, customers, invoices, payments, entitlements, and role changes remain unavailable.", icon: ShieldAlert, status: "Guardrail" }, { title: "Publish requires governance", description: "Production changes need scoped authorization, approval, validation, reconciliation, rollback, and audit evidence.", icon: LockKeyhole, status: "Blocked" }]} /></section></main></div>;
}
