import { useState } from "react";
import { Award, BookOpen, Check, ClipboardCheck, Clock3, LockKeyhole, RefreshCw, ShieldAlert, Star, UserRound, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatePanel, ScreenStatGrid } from "@/components/ScreenExperience";
const modules = [{ name: "Foundations module concept", type: "Course", state: "Not enrolled", detail: "Requires learner identity, enrollment, curriculum, lesson evidence, and instructor or content-owner authorization." }, { name: "Practice assessment concept", type: "Quiz", state: "Unscored", detail: "Requires question bank, attempt policy, grading rules, accessibility, integrity controls, and persisted results." }, { name: "Certification pathway concept", type: "Credential", state: "Blocked", detail: "Requires completion criteria, assessment evidence, issuer policy, identity verification, and revocation support." }];
export default function StudentProgress() { const [selected, setSelected] = useState(0); const [saved, setSaved] = useState(false); const [showBoundary, setShowBoundary] = useState(false); const [retrying, setRetrying] = useState(false); const retry = () => { setRetrying(true); window.setTimeout(() => setRetrying(false), 500); }; const reset = () => { setSelected(0); setSaved(false); setShowBoundary(false); setRetrying(false); }; const module = modules[selected]; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={BookOpen} eyebrow="Student progress · Learning preview" title="Track learning concepts without inventing grades or certificates." description="Explore a local learner-progress workspace with identity, enrollment, modules, lessons, quizzes, streaks, certificates, rewards, privacy, accessibility, retry, save/reset, and evidence boundaries. No student, course enrollment, score, attendance, streak, certificate, reward, or educational outcome exists." badge="Evidence-bounded learning"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save learning view"}</Button><Button onClick={() => setShowBoundary((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showBoundary ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showBoundary ? "Close evidence gates" : "Review progress gates"}</Button><Button onClick={retry} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className={`mr-2 size-4 ${retrying ? "animate-spin" : ""}`} />{retrying ? "Retrying local state" : "Retry learner source"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><X className="mr-2 size-4" />Reset progress</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Enrollment", value: "Unverified", hint: "No learner source", icon: UserRound, tone: "cyan" }, { label: "Lessons", value: "Unmeasured", hint: "No completion source", icon: BookOpen, tone: "violet" }, { label: "Assessment", value: "Unscored", hint: "No grading source", icon: ClipboardCheck, tone: "amber" }, { label: "Certificate", value: "Blocked", hint: "No issuer source", icon: Award, tone: "slate" }]} /><ScreenPreviewBanner title="Learning evidence boundary"><strong>This is a local learning-design preview, not evidence that a student, enrollment, lesson completion, quiz score, streak, certificate, reward, ranking, attendance, or educational outcome exists.</strong> Module cards, local selection, progress labels, streak and reward concepts, saved state, retry feedback, privacy gates, and disabled submit/complete/claim actions are browser concepts. No grade, credential, achievement, learner identity, or education result is asserted.</ScreenPreviewBanner><section className="grid gap-4 lg:grid-cols-3">{modules.map((item, index) => <button key={item.name} onClick={() => setSelected(index)} className={`rounded-2xl border p-5 text-left ${selected === index ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10 bg-white/[0.04]"}`}><div className="flex items-start justify-between"><div className="flex size-11 items-center justify-center rounded-2xl bg-cyan-300/10 text-cyan-200"><BookOpen className="size-5" /></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.state}</Badge></div><h2 className="mt-4 font-bold">{item.name}</h2><Badge variant="outline" className="mt-3 border-white/10 text-slate-500">{item.type}</Badge><p className="mt-4 text-sm leading-6 text-slate-400">{item.detail}</p></button>)}</section><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200">Selected learning concept</p><h2 className="mt-2 text-2xl font-black">{module.name}</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{module.state}</Badge></div><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Learner identity", value: "Unverified", icon: UserRound }, { label: "Enrollment", value: "Not connected", icon: BookOpen }, { label: "Lesson evidence", value: "Unmeasured", icon: Clock3 }, { label: "Quiz / grade", value: "Unscored", icon: ClipboardCheck }, { label: "Streak / reward", value: "Unavailable", icon: Star }, { label: "Certificate / issuer", value: "Blocked", icon: Award }].map(({ label, value, icon: Icon }) => <div key={label} className="flex items-center gap-3 rounded-xl border border-white/10 p-4"><Icon className="size-4 text-cyan-300" /><span className="flex-1 text-sm text-slate-300">{label}</span><span className="text-xs text-amber-200">{value}</span></div>)}</div><div className="mt-6 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Start lesson unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Submit quiz unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Claim certificate unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Claim reward unavailable</Button></div></CardContent></Card><ScreenStatePanel type="unavailable" title="Learner record source unavailable" description="Connect authorized enrollment, curriculum, lesson, assessment, attendance, certificate, reward, privacy, accessibility, and audit data before reporting progress or outcomes." /></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="grid gap-3 md:grid-cols-3">{[{ title: "Assessment integrity", text: "No question attempt, score, grade, completion, ranking, or assessment result is generated.", icon: ClipboardCheck }, { title: "Credential integrity", text: "No certificate, issuer, credential, revocation, reward, or achievement state is connected.", icon: Award }, { title: "Learner privacy", text: "No identity, accommodation, attendance, consent, retention, or education record is displayed.", icon: LockKeyhole }].map(({ title, text, icon: Icon }) => <div key={title} className="rounded-xl border border-white/10 p-4"><Icon className="size-5 text-cyan-300" /><p className="mt-3 font-semibold">{title}</p><p className="mt-2 text-sm leading-6 text-slate-500">{text}</p></div>)}</div></CardContent></Card>{showBoundary && <Card className="border-amber-300/20 bg-amber-300/[0.06]"><CardContent className="p-6"><p className="font-semibold text-amber-100">No grade or achievement claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local learning concept does not prove a student, enrollment, lesson completion, score, streak, certificate, reward, ranking, attendance, or educational outcome.</p></CardContent></Card>}<ScreenFeatureGrid features={[{ title: "Learning surface preserved", description: "Identity, enrollment, modules, lessons, quizzes, streaks, certificates, rewards, privacy, accessibility, retry, save/reset, and action gates remain visible.", icon: BookOpen, status: "Local learning" }, { title: "No grade theater", description: "Students, scores, completions, streaks, certificates, rankings, rewards, and educational outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before credentialing", description: "Real progress requires authorization, curriculum, assessment, privacy, accessibility, issuer policy, persistence, and audit.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>; }
