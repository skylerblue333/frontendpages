import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Check, Eye, FileImage, LockKeyhole, Palette, RefreshCw, ShieldAlert, UserRound, Wallet, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const tabs = ["Basics", "Wallet intent", "Theme", "Privacy"];
const themes = [{ id: "midnight", name: "Midnight", colors: ["#07111f", "#22d3ee", "#8b5cf6"] }, { id: "aurora", name: "Aurora", colors: ["#061b1b", "#2dd4bf", "#f59e0b"] }, { id: "paper", name: "Paper", colors: ["#f8fafc", "#334155", "#7c3aed"] }];
export default function ProfileEdit() {
  const [tab, setTab] = useState(tabs[0]);
  const [name, setName] = useState("Skycoin explorer");
  const [bio, setBio] = useState("A local profile draft for exploring the SKYCOIN4444 ecosystem.");
  const [wallet, setWallet] = useState("");
  const [theme, setTheme] = useState(themes[0].id);
  const [visibility, setVisibility] = useState("Private draft");
  const [saved, setSaved] = useState(false);
  const [showBoundary, setShowBoundary] = useState(false);
  const walletShape = useMemo(() => wallet ? "Entered locally · unverified" : "Empty", [wallet]);
  const reset = () => { setTab(tabs[0]); setName("Skycoin explorer"); setBio("A local profile draft for exploring the SKYCOIN4444 ecosystem."); setWallet(""); setTheme(themes[0].id); setVisibility("Private draft"); setSaved(false); setShowBoundary(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={UserRound} eyebrow="ProfileEdit · Edit workspace preview" title="Edit the profile surface without making custody or identity promises." description="Explore local basics, wallet-address intent, theme, and privacy controls. No account update, wallet connection, transaction, staking, reward, upload, verification, or public publication is connected." badge="Edit workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Changes saved locally" : "Save changes locally"}</Button><Button onClick={() => setShowBoundary((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showBoundary ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showBoundary ? "Close boundary" : "Review boundary"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset edits</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Fields", value: "Local", hint: "Not account synced", icon: UserRound, tone: "cyan" }, { label: "Wallet", value: walletShape, hint: "No custody path", icon: Wallet, tone: "violet" }, { label: "Theme", value: themes.find((item) => item.id === theme)?.name ?? "Local", hint: "Browser preview", icon: Palette, tone: "amber" }, { label: "Visibility", value: visibility, hint: "Unpublished", icon: Eye, tone: "slate" }]} /><ScreenPreviewBanner title="Profile-edit evidence boundary"><strong>This is a local profile-edit worksheet, not an account update.</strong> Name, bio, wallet-address text, theme, visibility, and saved state are browser concepts. No account persistence, wallet verification, private-key custody, transaction, staking, reward, image upload, consent, or public profile update is claimed.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.82fr_1.18fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex flex-wrap gap-2">{tabs.map((item) => <button key={item} onClick={() => setTab(item)} className={`rounded-lg border px-4 py-2 text-sm ${tab === item ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div><div className="mt-6 space-y-5">{tab === "Basics" && <><label className="block text-sm text-slate-400">Display name<input value={name} onChange={(event) => setName(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-white outline-none" /></label><label className="block text-sm text-slate-400">Bio<textarea value={bio} onChange={(event) => setBio(event.target.value.slice(0, 240))} className="mt-2 min-h-28 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-white outline-none" /><span className="mt-2 block text-right text-xs text-slate-500">{bio.length}/240</span></label><div className="rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4 text-sm text-slate-400">Image upload is unavailable; the avatar remains a placeholder.</div></>}{tab === "Wallet intent" && <><label className="block text-sm text-slate-400">Public wallet address<input value={wallet} onChange={(event) => setWallet(event.target.value.trim().slice(0, 120))} placeholder="0x… or another network address" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 font-mono text-sm text-white outline-none" /></label><div className="rounded-2xl border border-amber-300/25 bg-amber-300/[0.06] p-5"><div className="flex items-start gap-3"><Wallet className="mt-0.5 size-5 shrink-0 text-amber-200" /><div><p className="font-semibold text-amber-100">Wallet connection is blocked</p><p className="mt-2 text-sm leading-6 text-slate-400">This field is a local text intent only. No address validation, network detection, signature, private-key access, custody, transaction, staking, token reward, or payment action occurs here.</p><Button disabled className="mt-4 bg-slate-700 text-slate-400">Connect wallet unavailable</Button></div></div></div></>}{tab === "Theme" && <><p className="text-sm text-slate-400">Theme preset</p><div className="grid gap-3 sm:grid-cols-3">{themes.map((item) => <button key={item.id} onClick={() => setTheme(item.id)} className={`rounded-xl border p-4 text-left ${theme === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="mb-3 flex gap-2">{item.colors.map((color) => <span key={color} className="size-5 rounded-full" style={{ backgroundColor: color }} />)}</div><p className="font-semibold">{item.name}</p><p className="mt-1 text-xs text-slate-500">Local preview</p></button>)}</div></>}{tab === "Privacy" && <><p className="text-sm text-slate-400">Visibility intent</p><div className="flex flex-wrap gap-2">{["Private draft", "Authenticated community", "Public later"].map((item) => <button key={item} onClick={() => setVisibility(item)} className={`rounded-lg border px-3 py-2 text-xs ${visibility === item ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div><div className="grid gap-3 sm:grid-cols-2">{["Analytics consent", "Personalization", "Data sharing", "Retention"].map((item) => <div key={item} className="flex items-center justify-between rounded-xl border border-white/10 p-4"><span className="text-sm text-slate-300">{item}</span><Badge variant="outline" className="border-amber-300/20 text-amber-200">Unconfigured</Badge></div>)}</div></>}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Edit preview</p><h2 className="mt-2 text-2xl font-black">{name || "Your profile"}</h2><p className="mt-2 text-sm text-slate-500">{visibility} · {themes.find((item) => item.id === theme)?.name}</p><div className="mt-6 flex size-20 items-center justify-center rounded-3xl bg-cyan-300/10"><UserRound className="size-9 text-cyan-200" /></div><p className="mt-5 text-sm leading-7 text-slate-300">{bio || "No bio drafted."}</p><div className="mt-6 rounded-2xl border border-amber-300/25 bg-amber-300/[0.06] p-5"><div className="flex items-start gap-3"><LockKeyhole className="mt-0.5 size-5 shrink-0 text-amber-200" /><div><p className="font-semibold text-amber-100">Save is local only</p><p className="mt-2 text-sm leading-6 text-slate-400">No profile API, account identity, upload storage, wallet provider, privacy consent service, or public visibility path is connected.</p></div></div></div></CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Edit gates</p><h2 className="mt-2 text-2xl font-black">What a real edit flow must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Authenticated ownership and field-level authorization", "Server validation, optimistic concurrency, and rollback", "Wallet address validation without private-key custody", "Image rights, moderation, storage, resize, and deletion", "Privacy consent, visibility, export, and deletion behavior", "Accessible validation, errors, retry, and support"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><FileImage className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Edit surface preserved", description: "Basics, wallet intent, theme, privacy, local preview, save/reset, validation boundaries, and edit gates remain interactive.", icon: UserRound, status: "Local edit" }, { title: "No custody theater", description: "Account updates, wallet verification, transactions, staking, rewards, uploads, consent, and public publication are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Proof before persistence", description: "Real edits require authenticated authorization, validation, privacy, asset, wallet, accessibility, rollback, and support paths.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
