import { useState } from "react";
import { Link } from "wouter";
import { Activity, ArrowLeftRight, BarChart3, Clock3, DollarSign, Info, PieChart, Plus, ShieldAlert, TrendingUp, Wallet } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { EmptyState } from "@/components/EmptyState";
import { PageHeader } from "@/components/PageHeader";
import { useAuth } from "@/_core/hooks/useAuth";
import { getLoginUrl } from "@/const";

export default function Portfolio() {
  const { isAuthenticated, loading } = useAuth();
  const [view, setView] = useState<"holdings" | "history">("holdings");

  if (loading) {
    return <div className="min-h-screen bg-background p-6" aria-busy="true" aria-live="polite"><div className="mx-auto max-w-5xl animate-pulse space-y-6"><div className="h-10 w-64 rounded-xl bg-muted" /><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><div className="h-24 rounded-2xl bg-muted" /><div className="h-24 rounded-2xl bg-muted" /><div className="h-24 rounded-2xl bg-muted" /><div className="h-24 rounded-2xl bg-muted" /></div><div className="h-64 rounded-2xl bg-muted" /></div></div>;
  }

  if (!isAuthenticated) {
    const loginUrl = getLoginUrl();
    return <div className="min-h-screen bg-background px-4 py-10 sm:px-6"><div className="mx-auto flex min-h-[70vh] max-w-xl items-center justify-center"><Card className="w-full border-border/70 bg-card/90 text-center shadow-lg"><CardContent className="space-y-5 p-8 sm:p-10"><div className="mx-auto flex size-14 items-center justify-center rounded-2xl bg-primary/10 text-primary" aria-hidden="true"><PieChart className="size-7" /></div><div><h1 className="text-2xl font-semibold tracking-tight">Portfolio access requires authentication</h1><p className="mt-2 text-sm leading-6 text-muted-foreground">Holdings, allocation, performance, and history are private account data. Sign in when authentication is configured for this environment.</p></div>{loginUrl ? <Button asChild><a href={loginUrl}>Sign in to continue</a></Button> : <div className="rounded-xl border border-amber-500/30 bg-amber-500/10 p-4 text-sm text-amber-200" role="status">Authentication is not configured in this environment.</div>}</CardContent></Card></div></div>;
  }

  return <div className="min-h-screen bg-background text-foreground"><div className="border-b border-border/70 bg-card/60"><div className="mx-auto max-w-5xl px-4 py-6 sm:px-6 lg:px-8"><PageHeader backHref="/wallet" icon={PieChart} title="Portfolio" subtitle="A structured view of holdings, allocation, performance, and history when verified account data is available." badge="Live data unavailable" badgeVariant="outline" actions={<div className="flex flex-wrap gap-2"><Button variant="outline" size="sm" asChild><Link href="/crypto-hub"><ArrowLeftRight className="mr-2 size-4" /> Crypto Hub</Link></Button><Button variant="outline" size="sm" disabled><Plus className="mr-2 size-4" /> Add asset</Button></div>} /></div></div><main className="mx-auto max-w-5xl space-y-6 px-4 py-6 sm:px-6 lg:px-8">
    <section className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4" aria-label="Portfolio status">{[
      { label: "Total value", value: "Unavailable", hint: "Verified holdings and price data required", icon: <DollarSign className="size-4" /> },
      { label: "Performance", value: "Unavailable", hint: "Historical valuation service required", icon: <TrendingUp className="size-4" /> },
      { label: "Assets", value: "Unavailable", hint: "Authenticated holdings service required", icon: <BarChart3 className="size-4" /> },
      { label: "Allocation", value: "Unavailable", hint: "Verified holdings data required", icon: <PieChart className="size-4" /> },
    ].map((item) => <Card key={item.label} className="border-border/70 bg-card/80"><CardContent className="space-y-3 p-5"><div className="flex items-center justify-between text-muted-foreground"><span className="text-sm">{item.label}</span><span aria-hidden="true">{item.icon}</span></div><p className="text-xl font-semibold">{item.value}</p><p className="text-xs leading-5 text-muted-foreground">{item.hint}</p></CardContent></Card>)}</section>

    <Card className="border-border/70 bg-card/80"><CardContent className="flex flex-col gap-4 p-5 sm:flex-row sm:items-start"><div className="flex size-10 shrink-0 items-center justify-center rounded-xl bg-amber-500/10 text-amber-300" aria-hidden="true"><ShieldAlert className="size-5" /></div><div><h2 className="font-semibold">Portfolio data boundary</h2><p className="mt-1 text-sm leading-6 text-muted-foreground">No balances, prices, P&amp;L, allocations, or performance figures are displayed until the underlying account and market-data services are verified. This prevents simulated financial information from being mistaken for a real portfolio.</p></div></CardContent></Card>

    <Card className="border-border/70 bg-card/80"><CardHeader><div className="flex flex-wrap items-center justify-between gap-3"><CardTitle className="text-base">Portfolio view</CardTitle><div className="flex rounded-xl border border-border bg-muted/30 p-1" role="tablist" aria-label="Portfolio view"><button type="button" role="tab" aria-selected={view === "holdings"} onClick={() => setView("holdings")} className={`rounded-lg px-3 py-2 text-sm transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring ${view === "holdings" ? "bg-background font-medium text-foreground shadow-sm" : "text-muted-foreground hover:text-foreground"}`}>Holdings</button><button type="button" role="tab" aria-selected={view === "history"} onClick={() => setView("history")} className={`rounded-lg px-3 py-2 text-sm transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring ${view === "history" ? "bg-background font-medium text-foreground shadow-sm" : "text-muted-foreground hover:text-foreground"}`}>History</button></div></div></CardHeader><CardContent>{view === "holdings" ? <EmptyState title="Holdings are unavailable" description="Connect the authenticated portfolio service to display assets, quantities, valuations, allocation, and links to verified detail views." hint="Backend required before holdings can be shown." icon={<BarChart3 className="size-5" />} /> : <EmptyState title="Portfolio history is unavailable" description="Connect a verified historical valuation and transaction service to display performance, P&amp;L, alerts, and exportable history." hint="Live data unavailable." icon={<Clock3 className="size-5" />} />}</CardContent></Card>

    <section className="grid gap-5 lg:grid-cols-2"><Card className="border-border/70 bg-card/80"><CardHeader><CardTitle className="text-base">Prepared capabilities</CardTitle></CardHeader><CardContent className="space-y-3 text-sm text-muted-foreground"><div className="flex items-start gap-3"><PieChart className="mt-0.5 size-4 shrink-0 text-primary" aria-hidden="true" /><span>Allocation and performance views can be enabled once holdings and market data are available.</span></div><div className="flex items-start gap-3"><Activity className="mt-0.5 size-4 shrink-0 text-primary" aria-hidden="true" /><span>Alerts, exports, sharing, and analytics remain extension points rather than fake actions.</span></div></CardContent></Card><Card className="border-border/70 bg-card/80"><CardHeader><CardTitle className="text-base">Live tip</CardTitle></CardHeader><CardContent><div className="rounded-xl bg-primary/5 p-4 text-sm leading-6 text-muted-foreground"><Info className="mb-2 size-4 text-primary" aria-hidden="true" />Compare the current period with the previous period before drawing conclusions from portfolio performance.</div></CardContent></Card></section>
  </main></div>;
}
