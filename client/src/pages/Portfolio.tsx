import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { AlertTriangle, BarChart3, Bell, Check, Clock3, DollarSign, Download, FilePlus2, Info, LockKeyhole, PieChart, RefreshCw, Search, ShieldAlert, ShieldCheck, TrendingUp, Wallet, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

type View = "Holdings" | "History";
type Asset = { id: string; symbol: string; name: string; className: string; source: string; state: string; note: string };
const assets: Asset[] = [
  { id: "asset-001", symbol: "SKY444", name: "Sky concept asset", className: "Token concept", source: "No wallet source", state: "Unverified", note: "Address, chain, custody, and balance evidence required." },
  { id: "asset-002", symbol: "BTC", name: "Bitcoin concept", className: "Digital asset", source: "No market source", state: "Unavailable", note: "Price, quantity, timestamp, and network evidence required." },
  { id: "asset-003", symbol: "USD", name: "Cash concept", className: "Fiat", source: "No account source", state: "Unverified", note: "Account, currency, institution, and balance evidence required." },
];
const checks = ["Authenticated holdings and wallet/account ownership", "Timestamped market prices and currency conversion", "Transaction history, cost basis, fees, and reconciliation", "Allocation, P&L, performance period, and benchmark methodology", "Alerts, exports, sharing, privacy, and financial-risk disclosures"];
export default function Portfolio() {
  const [view, setView] = useState<View>("Holdings");
  const [period, setPeriod] = useState("1M");
  const [query, setQuery] = useState("");
  const [selected, setSelected] = useState(assets[0]);
  const [checksSelected, setChecksSelected] = useState<string[]>([]);
  const [saved, setSaved] = useState(false);
  const visible = useMemo(() => assets.filter((item) => !query || `${item.symbol} ${item.name} ${item.className} ${item.note}`.toLowerCase().includes(query.toLowerCase())), [query]);
  const toggle = (item: string) => setChecksSelected((items) => items.includes(item) ? items.filter((value) => value !== item) : [...items, item]);
  const reset = () => { setView("Holdings"); setPeriod("1M"); setQuery(""); setSelected(assets[0]); setChecksSelected([]); setSaved(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Wallet} eyebrow="Portfolio · Financial preview" title="Inspect portfolio requirements without inventing wealth data." description="Explore holdings and history views, asset concepts, period selection, allocation and performance boundaries, alerts, exports, and evidence requirements. No wallet, account, market, transaction, price, balance, P&L, or tax source is connected." badge="Portfolio preview"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} disabled={visible.length === 0} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "View saved locally" : "Save view locally"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset portfolio</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Total value", value: "Unavailable", hint: "No verified holdings", icon: DollarSign, tone: "cyan" }, { label: "Performance", value: "Unavailable", hint: "No price history", icon: TrendingUp, tone: "violet" }, { label: "Assets", value: String(assets.length), hint: "Local concepts", icon: BarChart3, tone: "amber" }, { label: "Allocation", value: "Unset", hint: "No balance source", icon: PieChart, tone: "slate" }]} /><ScreenPreviewBanner title="Portfolio data boundary"><strong>This is not a financial statement or investment recommendation.</strong> Asset labels, unavailable values, period controls, selected detail, and evidence checks are local UI concepts. They do not represent balances, ownership, prices, returns, allocation, performance, tax position, risk, solvency, or a recommendation to act. No financial service is connected.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.82fr_1fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local assets…" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-9 pr-3 text-sm text-white outline-none focus:border-cyan-300/50" /></div><div className="mt-6 flex rounded-xl border border-white/10 bg-black/10 p-1" role="tablist" aria-label="Portfolio view"><button role="tab" aria-selected={view === "Holdings"} onClick={() => setView("Holdings")} className={`flex-1 rounded-lg px-3 py-2 text-sm ${view === "Holdings" ? "bg-white/10 font-semibold text-white" : "text-slate-500"}`}>Holdings</button><button role="tab" aria-selected={view === "History"} onClick={() => setView("History")} className={`flex-1 rounded-lg px-3 py-2 text-sm ${view === "History" ? "bg-white/10 font-semibold text-white" : "text-slate-500"}`}>History</button></div><p className="mt-6 text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Review period</p><div className="mt-3 flex gap-2">{["1W", "1M", "3M", "1Y"].map((item) => <button key={item} onClick={() => setPeriod(item)} className={`rounded-lg border px-3 py-2 text-xs ${period === item ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div><div className="mt-6 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex gap-3"><ShieldAlert className="mt-0.5 size-5 shrink-0 text-amber-200" /><p className="text-sm leading-6 text-slate-300">No balances, prices, P&L, allocations, or historical figures appear until the underlying account and market-data services are verified.</p></div></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">{view} view</p><h2 className="mt-2 text-2xl font-black">Portfolio concepts</h2></div><Badge variant="outline" className="border-white/10 text-amber-200">{visible.length} visible</Badge></div>{view === "Holdings" ? <div className="mt-5 space-y-3">{visible.map((item) => <button key={item.id} onClick={() => setSelected(item)} className={`w-full rounded-xl border p-4 text-left ${selected.id === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-center gap-3"><div className="flex size-10 items-center justify-center rounded-xl bg-cyan-300/10"><Wallet className="size-5 text-cyan-200" /></div><div className="flex-1"><div className="flex flex-wrap items-center gap-2"><p className="font-semibold">{item.symbol} · {item.name}</p><Badge variant="outline" className="border-white/10 text-slate-400">{item.className}</Badge></div><p className="mt-2 text-sm leading-5 text-slate-500">{item.note}</p></div><span className="text-xs text-amber-200">{item.state}</span></div></button>)}</div> : <div className="mt-5 rounded-xl border border-dashed border-white/15 p-8 text-center"><Clock3 className="mx-auto size-7 text-slate-500" /><p className="mt-3 font-semibold">Portfolio history unavailable</p><p className="mt-2 text-sm leading-6 text-slate-500">A verified transaction and historical valuation service is required before performance, P&L, alerts, or exportable history can be shown.</p><Button disabled className="mt-5"><Download className="mr-2 size-4" />Export unavailable</Button></div>}<div className="mt-5 rounded-xl border border-white/10 p-4"><p className="text-xs text-slate-500">Selected asset concept</p><p className="mt-2 text-lg font-black">{selected.symbol} · {selected.name}</p><p className="mt-2 text-sm leading-6 text-slate-400">{selected.note}</p><div className="mt-4 grid gap-3 sm:grid-cols-3">{[{ label: "Balance", value: "Unavailable" }, { label: "Price", value: "Unavailable" }, { label: "Source", value: selected.source }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div></div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Evidence checklist</p><h2 className="mt-2 text-2xl font-black">Before trusting a portfolio</h2><div className="mt-5 space-y-2">{checks.map((item) => <button key={item} onClick={() => toggle(item)} className={`flex w-full items-center gap-3 rounded-xl border p-3 text-left ${checksSelected.includes(item) ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><span className="flex size-5 items-center justify-center rounded border border-white/20">{checksSelected.includes(item) ? "✓" : ""}</span><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></button>)}</div><Button disabled={checksSelected.length === 0} className="mt-5 bg-cyan-300 text-slate-950 hover:bg-cyan-200"><ShieldCheck className="mr-2 size-4" />{checksSelected.length}/{checks.length} local checks selected</Button></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><LockKeyhole className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake portfolio</h2><p className="mt-3 text-sm leading-6 text-slate-400">Changing tabs, periods, search, selection, or evidence checks changes browser state only. It does not connect a wallet, import balances, fetch prices, calculate P&L, send alerts, or recommend a financial action.</p></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Production checklist</p><h2 className="mt-2 text-2xl font-black">Portfolio must prove</h2><div className="mt-5 space-y-3">{["Authenticated account/wallet ownership, chain/network validation, custody boundaries, and address privacy", "Timestamped prices, currency conversion, market source, stale-data handling, and valuation reconciliation", "Transactions, cost basis, fees, transfers, duplicates, corrections, and export integrity", "Performance methodology, period boundaries, benchmarks, allocation math, and uncertainty disclosures", "Alerts, sharing, deletion, rate limits, audit, security, accessibility, and financial-risk messaging"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Portfolio surface preserved", description: "Holdings/history tabs, search, periods, asset selection, allocation/performance boundaries, alerts, and exports remain visible.", icon: Wallet, status: "Local worksheet" }, { title: "Financial data is off", description: "Balances, prices, P&L, ownership, transactions, taxes, and benchmarks remain unavailable.", icon: ShieldAlert, status: "Guardrail" }, { title: "No recommendation", description: "No investment advice, risk assessment, solvency claim, or action prompt is fabricated.", icon: WifiOff, status: "Unavailable" }]} /></section></main></div>;
}
