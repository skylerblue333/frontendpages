import { useMemo, useState } from "react";
import { BookOpen, Check, Code2, Copy, ExternalLink, KeyRound, LockKeyhole, Search, Shield, Webhook, Zap } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

type Endpoint = { id: string; method: "GET" | "POST"; path: string; description: string; example: string; status: "Contract draft" | "Route verification required" };
const ENDPOINTS: Endpoint[] = [
  { id: "feedback", method: "GET", path: "/api/trpc/feedback.list", description: "List feedback records after authentication, authorization, pagination, and output validation are verified.", example: "fetch('/api/trpc/feedback.list', { headers: { Authorization: 'Bearer <server-issued-token>' } });", status: "Route verification required" },
  { id: "roadmap", method: "GET", path: "/api/trpc/roadmap.query", description: "Query roadmap initiatives with documented visibility and ownership rules.", example: "fetch('/api/trpc/roadmap.query', { headers: { Authorization: 'Bearer <server-issued-token>' } });", status: "Contract draft" },
  { id: "debate", method: "POST", path: "/api/trpc/agents.debate", description: "Prepare a multi-agent debate request; execution and model availability require a verified backend contract.", example: "fetch('/api/trpc/agents.debate', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ topic: '<approved-topic>', agents: ['strategist', 'engineer'] }) });", status: "Contract draft" },
  { id: "orders", method: "POST", path: "/api/trpc/orders.create", description: "Create an order only after authentication, inventory, price, payment, idempotency, and fulfillment contracts are verified.", example: "fetch('/api/trpc/orders.create', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ items: [{ productId: '<verified-id>', quantity: 1 }] }) });", status: "Route verification required" },
];
const AUTH = [{ name: "Bearer token", description: "Server-issued access token with scoped permissions.", sample: "Authorization: Bearer <access-token>" }, { name: "API key", description: "Use only for a verified server-to-server contract; never expose it in browser code.", sample: "X-API-Key: <server-side-key>" }, { name: "OAuth 2.0", description: "Use an approved OAuth flow with redirect, scope, expiry, and revocation handling.", sample: "Authorization: Bearer <oauth-access-token>" }];

export default function APIDocs() { const [query, setQuery] = useState(""); const [selected, setSelected] = useState(ENDPOINTS[0]); const [copied, setCopied] = useState(false); const visible = useMemo(() => ENDPOINTS.filter((endpoint) => `${endpoint.path} ${endpoint.description} ${endpoint.method}`.toLowerCase().includes(query.toLowerCase())), [query]); const copy = () => { void navigator.clipboard?.writeText(selected.example); setCopied(true); window.setTimeout(() => setCopied(false), 1500); }; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={BookOpen} eyebrow="Developer Platform · API Docs" title="Build against contracts, not assumptions." description="Explore the current API documentation surface with endpoint examples, authentication guidance, security notes, and explicit contract-verification states. This page does not claim that every example is deployed or that quotas, SDKs, or webhooks are live." badge="Contract-first reference"><div className="flex flex-wrap gap-2"><Button onClick={() => document.getElementById("api-reference")?.scrollIntoView({ behavior: "smooth" })} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><BookOpen className="mr-2 size-4" />Open reference</Button><Button variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><Shield className="mr-2 size-4" />Security checklist</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Endpoint examples", value: String(ENDPOINTS.length), hint: "Contract-reviewed drafts", icon: Code2 }, { label: "Auth patterns", value: String(AUTH.length), hint: "Server-side guidance", icon: KeyRound, tone: "violet" }, { label: "Deployment", value: "Unverified", hint: "No live API claim", icon: LockKeyhole, tone: "amber" }, { label: "Secrets", value: "Server only", hint: "Never ship keys to browser", icon: Shield, tone: "slate" }]} /><ScreenPreviewBanner title="Documentation evidence boundary">Endpoint cards, copyable examples, authentication patterns, security guidance, and draft rate-limit states are available for UX review. The screen does not invent base URLs, deployed routes, API keys, quotas, SDK releases, uptime, or webhook delivery.</ScreenPreviewBanner><section id="api-reference" className="grid gap-6 lg:grid-cols-[0.75fr_1.25fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="relative"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search endpoints" aria-label="Search API endpoints" className="border-white/10 bg-black/20 pl-9 text-white placeholder:text-slate-500" /></div><div className="mt-4 space-y-2">{visible.map((endpoint) => <button key={endpoint.id} onClick={() => setSelected(endpoint)} className={`w-full rounded-xl border p-4 text-left transition ${selected.id === endpoint.id ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-black/15 hover:bg-white/[0.05]"}`}><div className="flex items-center gap-2"><Badge variant="outline" className={endpoint.method === "GET" ? "border-cyan-300/20 text-cyan-200" : "border-violet-300/20 text-violet-200"}>{endpoint.method}</Badge><span className="truncate font-mono text-xs text-slate-300">{endpoint.path}</span></div><p className="mt-2 text-sm leading-6 text-slate-400">{endpoint.description}</p><Badge variant="outline" className="mt-3 border-amber-300/20 text-amber-200">{endpoint.status}</Badge></button>)}{visible.length === 0 && <ScreenStatePanel type="empty" title="No endpoints match" description="Try another path, method, or keyword." />}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex flex-wrap items-center gap-2"><Badge variant="outline" className={selected.method === "GET" ? "border-cyan-300/20 text-cyan-200" : "border-violet-300/20 text-violet-200"}>{selected.method}</Badge><code className="font-mono text-sm text-slate-200">{selected.path}</code><Badge variant="outline" className="ml-auto border-amber-300/20 text-amber-200">{selected.status}</Badge></div><h2 className="mt-5 text-2xl font-bold">{selected.description}</h2><div className="mt-5 rounded-xl border border-white/10 bg-black/30 p-4"><div className="flex items-center justify-between"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Request example</p><Button size="sm" variant="outline" onClick={copy} className="border-white/15 text-slate-300">{copied ? <Check className="mr-1 size-3.5" /> : <Copy className="mr-1 size-3.5" />}{copied ? "Copied" : "Copy"}</Button></div><pre className="mt-4 overflow-x-auto whitespace-pre-wrap text-xs leading-6 text-slate-300">{selected.example}</pre></div><div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex items-center gap-2 font-medium text-amber-200"><LockKeyhole className="size-4" />Before shipping</div><p className="mt-2 text-sm leading-6 text-slate-300">Verify the route, schema, authentication, authorization, status codes, rate limits, idempotency, logging, and error envelope against the backend implementation.</p></div></CardContent></Card></section><section><Tabs defaultValue="auth" className="space-y-5"><TabsList className="grid h-auto w-full grid-cols-3 gap-2 border border-white/10 bg-white/[0.04] p-2"><TabsTrigger value="auth" className="gap-2 text-slate-300 data-[state=active]:bg-cyan-300 data-[state=active]:text-slate-950"><KeyRound className="size-4" />Authentication</TabsTrigger><TabsTrigger value="limits" className="gap-2 text-slate-300 data-[state=active]:bg-violet-300 data-[state=active]:text-slate-950"><Zap className="size-4" />Rate limits</TabsTrigger><TabsTrigger value="resources" className="gap-2 text-slate-300 data-[state=active]:bg-amber-300 data-[state=active]:text-slate-950"><ExternalLink className="size-4" />Resources</TabsTrigger></TabsList><TabsContent value="auth"><div className="grid gap-4 md:grid-cols-3">{AUTH.map((method) => <Card key={method.name} className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><KeyRound className="size-5 text-cyan-300" /><h3 className="mt-4 font-semibold">{method.name}</h3><p className="mt-2 text-sm leading-6 text-slate-400">{method.description}</p><code className="mt-4 block rounded-lg bg-black/20 p-3 text-xs text-slate-300">{method.sample}</code></CardContent></Card>)}</div></TabsContent><TabsContent value="limits"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><ScreenStatePanel type="unavailable" title="Rate limits are not verified" description="Do not publish numeric quotas until the gateway configuration, plan rules, headers, retry behavior, and billing contract are confirmed." /></CardContent></Card></TabsContent><TabsContent value="resources"><div className="grid gap-4 md:grid-cols-3">{[["API reference", BookOpen], ["Webhook guide", Webhook], ["SDK and examples", Code2]].map(([label, Icon]) => <Card key={label as string} className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><Icon className="size-5 text-amber-300" /><h3 className="mt-4 font-semibold">{label as string}</h3><p className="mt-2 text-sm leading-6 text-slate-400">Resource link pending verification.</p><Badge variant="outline" className="mt-4 border-amber-300/20 text-amber-200">Not connected</Badge></CardContent></Card>)}</div></TabsContent></Tabs></section><ScreenFeatureGrid features={[{ title: "Server-side secrets", description: "Keep API keys, tokens, database credentials, and signing material out of browser bundles.", icon: Shield, status: "Required" }, { title: "Contract verification", description: "Match examples to real routes, schemas, auth, status codes, and error envelopes before publication.", icon: Code2, status: "Required" }, { title: "No fake quotas", description: "Show rate limits, SDKs, webhooks, and uptime only when configuration and evidence exist.", icon: LockKeyhole, status: "Guardrail" }]} /></main></div>; }
