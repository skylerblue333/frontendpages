import { useState } from "react";
import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Activity, BarChart3, Check, Cpu, DollarSign, FileCheck2, Gauge, LockKeyhole, Pause, Play, RefreshCw, ShieldAlert, Sparkles, Wallet, WifiOff, Zap } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const HARDWARE = [{ name: "ASIC reference", algorithm: "SHA-256", note: "Hardware research subject", color: "from-amber-400 to-orange-500" }, { name: "GPU reference", algorithm: "GPU workload", note: "Scenario planning subject", color: "from-blue-400 to-indigo-500" }, { name: "Validator reference", algorithm: "Proof of stake", note: "Not mining", color: "from-emerald-400 to-cyan-500" }];
const SESSIONS = [{ id: "Scenario A", status: "Plan only", window: "Short run", output: "Unavailable" }, { id: "Scenario B", status: "Needs source", window: "Long run", output: "Unavailable" }, { id: "Scenario C", status: "Blocked", window: "Payout review", output: "Unavailable" }];
const CHART = [{ period: "P1", work: 42, energy: 31 }, { period: "P2", work: 56, energy: 38 }, { period: "P3", work: 48, energy: 35 }, { period: "P4", work: 63, energy: 44 }, { period: "P5", work: 52, energy: 39 }];

export default function MiningDashboard() {
  const [running, setRunning] = useState(false);
  const [selectedHardware, setSelectedHardware] = useState(HARDWARE[0].name);
  const [saved, setSaved] = useState(false);
  const hardware = HARDWARE.find((item) => item.name === selectedHardware) ?? HARDWARE[0];
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Cpu} eyebrow="Mining · Operations" title="Model the operation before claiming the output." description="Review hardware subjects, local run state, deterministic scenario charts, session previews, and evidence gates. No hashrate, mined coins, earnings, payout, wallet balance, pool metric, or blockchain mining result is presented as real." badge="Mining preview"><div className="flex flex-wrap gap-2"><Button onClick={() => { setRunning((value) => !value); setSaved(false); }} className={running ? "bg-amber-300 text-slate-950 hover:bg-amber-200" : "bg-cyan-300 text-slate-950 hover:bg-cyan-200"}>{running ? <><Pause className="mr-2 size-4" />Pause local scenario</> : <><Play className="mr-2 size-4" />Start local scenario</>}</Button><Button onClick={() => { setRunning(false); setSelectedHardware(HARDWARE[0].name); setSaved(false); }} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset plan</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Scenario", value: running ? "Local run" : "Ready", hint: "No miner connected", icon: Activity, tone: running ? "cyan" : "slate" }, { label: "Hashrate", value: "Off", hint: "No telemetry source", icon: Gauge, tone: "amber" }, { label: "Earnings", value: "Off", hint: "No price or reward", icon: DollarSign, tone: "slate" }, { label: "Payout", value: "Blocked", hint: "No wallet or pool", icon: Wallet, tone: "violet" }]} /><ScreenPreviewBanner title="Mining evidence boundary"><strong>Hardware names, algorithm labels, scenario points, session rows, and readiness gates are planning fixtures—not live hashrate, mined output, rewards, earnings, uptime, pool activity, payout, wallet balance, or blockchain state.</strong> Production mining requires hardware telemetry, network inputs, pool contracts, energy costs, price sources, reward settlement, wallet custody, monitoring, and operational controls.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Hardware subjects</p><h2 className="mt-2 text-2xl font-black">Choose a planning lens</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Research only</Badge></div><div className="mt-5 grid gap-3 sm:grid-cols-3">{HARDWARE.map((item) => <button key={item.name} onClick={() => { setSelectedHardware(item.name); setSaved(false); }} className={`rounded-xl border p-4 text-left ${selectedHardware === item.name ? "border-cyan-300/40 bg-cyan-300/[0.07]" : "border-white/10 bg-black/10"}`}><div className={`flex size-10 items-center justify-center rounded-xl bg-gradient-to-br ${item.color} text-slate-950`}><Cpu className="size-5" /></div><h3 className="mt-4 font-black">{item.name}</h3><p className="mt-1 text-xs text-slate-500">{item.algorithm}</p><p className="mt-3 text-xs text-amber-200">{item.note}</p></button>)}</div><div className="mt-6 rounded-xl border border-white/10 p-4"><div className="flex items-center gap-3"><div className="flex size-10 items-center justify-center rounded-xl bg-violet-300/10 text-violet-200"><Cpu className="size-5" /></div><div><p className="font-semibold">Selected: {hardware.name}</p><p className="text-sm text-slate-500">{hardware.algorithm} · local scenario lens</p></div></div><div className="mt-5 grid gap-3 sm:grid-cols-3">{[{ label: "Hashrate", value: "Unavailable" }, { label: "Energy", value: "Needs input" }, { label: "Network", value: "Not configured" }].map((item) => <div key={item.label} className="rounded-xl bg-black/15 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><Zap className="size-5 text-amber-300" /><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-300">Run state</p><h2 className="mt-1 text-2xl font-black">{running ? "Local scenario active" : "Scenario ready"}</h2></div></div><div className="mt-6 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-200">No mining process is connected</p><p className="mt-2 text-sm leading-6 text-slate-400">The control changes local UI state only. It cannot start hardware, contact a pool, mint coins, calculate earnings, or send a payout.</p></div><div className="mt-5 space-y-3">{["Hardware telemetry", "Pool connection", "Energy and network inputs", "Reward settlement", "Wallet and payout confirmation"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Scenario chart</p><h2 className="mt-2 text-2xl font-black">Work and energy indices</h2></div><Badge variant="outline" className="border-white/10 text-slate-400">Synthetic</Badge></div><div className="mt-5 h-64"><ResponsiveContainer width="100%" height="100%"><BarChart data={CHART}><CartesianGrid strokeDasharray="3 3" stroke="rgba(255,255,255,0.08)" /><XAxis dataKey="period" stroke="#94a3b8" fontSize={12} /><YAxis stroke="#94a3b8" fontSize={12} /><Tooltip contentStyle={{ background: "#10152e", border: "1px solid rgba(255,255,255,0.12)", color: "#fff" }} /><Bar dataKey="work" fill="#22d3ee" name="Work index" /><Bar dataKey="energy" fill="#fbbf24" name="Energy index" /></BarChart></ResponsiveContainer></div><p className="mt-4 text-xs text-slate-500">Synthetic indices · not hashrate, wattage, cost, profit, or mined output</p></CardContent></Card><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-300">Session preview</p><h2 className="mt-2 text-2xl font-black">Operational review rows</h2></div><Button onClick={() => setSaved(true)} variant="outline" className="border-white/15 text-white">{saved ? <><Check className="mr-2 size-4" />Saved locally</> : "Save rows"}</Button></div><div className="mt-5 space-y-3">{SESSIONS.map((session) => <div key={session.id} className="flex flex-wrap items-center gap-3 rounded-xl border border-white/10 p-4"><div className="flex size-9 items-center justify-center rounded-lg bg-white/10"><Activity className="size-4 text-cyan-300" /></div><div className="flex-1"><p className="font-semibold">{session.id}</p><p className="text-xs text-slate-500">{session.window} · {session.status}</p></div><p className="text-sm text-amber-200">Output {session.output}</p></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><ShieldAlert className="size-5 text-red-300" /><h2 className="mt-4 text-xl font-black">No payout record</h2><p className="mt-3 text-sm leading-6 text-slate-400">There is no mined amount, reward transaction, wallet destination, tax record, or payout confirmation attached to this preview.</p><div className="mt-5 rounded-xl border border-red-300/20 bg-red-300/[0.05] p-4 text-sm text-red-200">Never treat a local run state as proof of blockchain activity.</div></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "A dashboard is not a miner", description: "Controls and charts preserve the planning UX without starting hardware or asserting network output.", icon: WifiOff, status: "Guardrail" }, { title: "Earnings need reconciliation", description: "Rewards require current network data, price source, pool settlement, wallet custody, and audit history.", icon: DollarSign, status: "Required" }, { title: "No fake payout", description: "No wallet, pool, reward, or blockchain action is exposed until its integration is verified.", icon: Wallet, status: "Unavailable" }]} /></main></div>;
}
