import { useMemo, useState } from "react";
import { Link } from "wouter";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Award, Check, Crown, Eye, Heart, LockKeyhole, Play, Plus, Search, Sparkles, Star, TrendingUp, UserRound, Users, Video, WalletCards, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const CREATORS = [{ id: "skyler", name: "SkylerDev", handle: "@skylerdev", avatar: "S", tier: "Featured", categories: ["Tech", "AI", "Web3"], bio: "Building future-facing experiments and explaining the process.", live: true, gradient: "from-violet-500 to-fuchsia-500", preview: ["AI systems field notes", "Web3 tutorial series"] }, { id: "crypto", name: "CryptoQueen", handle: "@cryptoqueen", avatar: "C", tier: "Featured", categories: ["DeFi", "NFTs", "Research"], bio: "Making complex market concepts easier to explore.", live: false, gradient: "from-blue-500 to-cyan-500", preview: ["DeFi research notes", "NFT curation guide"] }, { id: "neon", name: "NeonStreamer", handle: "@neonstreamer", avatar: "N", tier: "Rising", categories: ["Gaming", "Streaming", "Commentary"], bio: "Gaming sessions and creator experiments in one place.", live: true, gradient: "from-emerald-500 to-teal-500", preview: ["Shadow Arena session", "Creator setup tour"] }, { id: "artist", name: "AIArtist", handle: "@aiartist", avatar: "A", tier: "Rising", categories: ["Art", "AI", "Collectibles"], bio: "Generative art studies and creative technology process.", live: false, gradient: "from-orange-500 to-red-500", preview: ["Generative art study", "Model workflow notes"] }, { id: "phil", name: "TechPhilosopher", handle: "@techphil", avatar: "T", tier: "Featured", categories: ["Tech", "Writing", "Society"], bio: "Long-form essays about technology and human connection.", live: false, gradient: "from-indigo-500 to-purple-500", preview: ["Attention economy essay", "AI consciousness notes"] }, { id: "fit", name: "FitnessCrypto", handle: "@fitcrypto", avatar: "F", tier: "Rising", categories: ["Fitness", "Lifestyle", "Crypto"], bio: "Movement, habit design, and responsible creator experiments.", live: false, gradient: "from-yellow-500 to-orange-500", preview: ["Thirty-day movement plan", "Morning routine draft"] }];
const FILTERS = [{ id: "all", label: "All" }, { id: "live", label: "Live preview" }, { id: "featured", label: "Featured" }, { id: "rising", label: "Rising" }];

function CreatorCard({ creator }: { creator: typeof CREATORS[number] }) {
  const [followed, setFollowed] = useState(false);
  const [subscribed, setSubscribed] = useState(false);
  return <Card className="overflow-hidden border-white/10 bg-white/[0.04]"><div className={`relative h-24 bg-gradient-to-br ${creator.gradient}`}>{creator.live && <Badge className="absolute right-3 top-3 border-red-300/30 bg-red-400/20 text-red-100">Live preview</Badge>}</div><CardContent className="p-5"><div className="-mt-12 flex items-end justify-between"><div className={`flex size-16 items-center justify-center rounded-2xl border-4 border-[#070a16] bg-gradient-to-br ${creator.gradient} text-2xl font-black`}>{creator.avatar}</div><Badge variant="outline" className="border-violet-300/20 text-violet-200">{creator.tier}</Badge></div><div className="mt-4 flex items-center gap-2"><h2 className="font-black">{creator.name}</h2><span className="text-xs text-slate-500">{creator.handle}</span></div><p className="mt-2 min-h-10 text-sm leading-5 text-slate-400">{creator.bio}</p><div className="mt-4 flex flex-wrap gap-1.5">{creator.categories.map((category) => <Badge key={category} variant="outline" className="border-white/10 text-[10px] text-slate-400">{category}</Badge>)}</div><div className="mt-4 rounded-xl border border-white/10 bg-black/10 p-3"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-cyan-300">Preview content</p>{creator.preview.map((item) => <p key={item} className="mt-2 truncate text-xs text-slate-400">• {item}</p>)}</div><div className="mt-4 flex gap-2"><Button onClick={() => setFollowed((current) => !current)} variant="outline" className="flex-1 border-white/15 text-xs text-white">{followed ? <><Check className="mr-1 size-3" />Following locally</> : <><Plus className="mr-1 size-3" />Follow locally</>}</Button><Button onClick={() => setSubscribed((current) => !current)} className={`flex-1 text-xs ${subscribed ? "bg-white/10 text-white" : "bg-violet-500 text-white hover:bg-violet-400"}`}>{subscribed ? "Selected locally" : <><Star className="mr-1 size-3" />Preview tier</>}</Button></div></CardContent></Card>;
}

export default function CreatorSpotlight() {
  const [query, setQuery] = useState("");
  const [filter, setFilter] = useState("all");
  const filtered = useMemo(() => CREATORS.filter((creator) => { const matchesQuery = `${creator.name} ${creator.handle} ${creator.categories.join(" ")}`.toLowerCase().includes(query.toLowerCase()); const matchesFilter = filter === "all" || (filter === "live" && creator.live) || creator.tier.toLowerCase() === filter; return matchesQuery && matchesFilter; }), [filter, query]);
  const live = CREATORS.filter((creator) => creator.live);
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Crown} eyebrow="Creator discovery · Spotlight" title="Discover the work, not a fabricated leaderboard." description="Browse deterministic featured-creator cards, live-session previews, categories, and local follow/tier interactions. This screen does not claim platform ranking, creator earnings, audience reach, verification, or promotional placement." badge="Editorial preview"><Link href="/creator-onboarding"><Button className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Sparkles className="mr-2 size-4" />Prepare a creator profile</Button></Link></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Featured cards", value: String(CREATORS.length), hint: "Synthetic discovery set", icon: Users }, { label: "Live previews", value: String(live.length), hint: "No live service connected", icon: Video, tone: "amber" }, { label: "Ranking", value: "Off", hint: "No score or placement proof", icon: WifiOff, tone: "slate" }, { label: "Earnings", value: "Off", hint: "No financial data", icon: WalletCards, tone: "violet" }]} /><ScreenPreviewBanner title="Discovery evidence boundary"><strong>Featured labels, live badges, follower counts, subscriber counts, verification marks, earnings, views, rankings, and category placement are synthetic presentation data—not proof of a real creator, audience, platform metric, or promotional commitment.</strong> Production discovery requires editorial criteria, source provenance, moderation, privacy, opt-in rules, freshness, ranking policy, and creator controls.</ScreenPreviewBanner>{live.length > 0 && <section className="rounded-2xl border border-red-300/20 bg-red-300/[0.05] p-5"><div className="flex items-center gap-2"><div className="size-2 rounded-full bg-red-300" /><h2 className="font-black text-red-100">Live-session preview</h2><Badge variant="outline" className="border-red-300/20 text-red-200">{live.length} local cards</Badge></div><div className="mt-4 flex gap-3 overflow-x-auto pb-1">{live.map((creator) => <div key={creator.id} className="flex min-w-56 items-center gap-3 rounded-xl border border-white/10 bg-black/15 p-3"><div className={`flex size-10 items-center justify-center rounded-xl bg-gradient-to-br ${creator.gradient} font-black`}>{creator.avatar}</div><div className="min-w-0 flex-1"><p className="truncate font-semibold">{creator.name}</p><p className="text-xs text-red-200">Session preview · no viewers</p></div><Play className="size-4 text-red-200" /></div>)}</div></section>}<section className="space-y-5"><div className="flex flex-col gap-3 lg:flex-row"><div className="relative flex-1"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search creator, handle, or category" className="border-white/10 bg-white/[0.04] pl-9 text-white placeholder:text-slate-500" /></div><div className="flex gap-2 overflow-x-auto">{FILTERS.map((item) => <button key={item.id} onClick={() => setFilter(item.id)} className={`rounded-xl border px-3 py-2 text-sm whitespace-nowrap ${filter === item.id ? "border-cyan-300/40 bg-cyan-300/[0.1] text-cyan-200" : "border-white/10 text-slate-400"}`}>{item.label}</button>)}</div></div><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{filtered.map((creator) => <CreatorCard key={creator.id} creator={creator} />)}</div>{filtered.length === 0 && <div className="rounded-2xl border border-white/10 bg-white/[0.04] p-12 text-center"><UserRound className="mx-auto size-10 text-slate-600" /><p className="mt-4 font-semibold">No creator cards match</p><p className="mt-2 text-sm text-slate-500">Try another search or reset the local filter.</p></div>}</section><ScreenFeatureGrid features={[{ title: "Editorial criteria first", description: "Discovery placement needs documented criteria, source data, moderation, opt-in, and an appeals path.", icon: Award, status: "Required" }, { title: "Live means connected", description: "A live badge cannot prove a stream, viewers, availability, or content delivery without a real service.", icon: Play, status: "Guardrail" }, { title: "No fake social proof", description: "Followers, earnings, views, rankings, and verification remain unavailable without authoritative evidence.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
