import FeatureUnavailable from "@/components/FeatureUnavailable";

const FrameworkTemplates = () => (
  <FeatureUnavailable
    title="Framework Templates"
    description="Framework Templates is intentionally held at a truthful release boundary until its real provider, authorization, persistence, monitoring, and acceptance evidence are complete."
    capability="Framework Templates on /framework-templates"
    nextStep="Return to the launch hub"
  />
);

export default FrameworkTemplates;
