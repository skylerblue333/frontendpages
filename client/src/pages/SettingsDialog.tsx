import { useState } from "react";
import { Bell, Check, KeyRound, LockKeyhole, SlidersHorizontal, WalletCards, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
export default function SettingsDialog() { const [email, setEmail] = useState(false); const [messages, setMessages] = useState(false); const [mfa, setMfa] = useState(false); const [saved, setSaved] = useState(false); const [closed, setClosed] = useState(false); const reset = () => { setEmail(false); setMessages(false); setMfa(false); setSaved(false); setClosed(false); }; if (closed) return <div className="min-h-screen bg-[#070a16] p-6 text-white"><Card className="mx-auto mt-20 max-w-xl border-white/10 bg-white/[0.04]"><CardContent className="p-8 text-center"><SlidersHorizontal className="mx-auto size-9 text-slate-600" /><h1 className="mt-4 text-2xl font-black">Settings panel closed locally</h1><p className="mt-2 text-sm leading-6 text-slate-500">Closing this preview changes only browser state. No account preference was persisted.</p><Button onClick={reset} className="mt-6 bg-cyan-300 text-slate-950 hover:bg-cyan-200">Reopen local panel</Button></CardContent></Card></div>; return <div className="min-h-screen bg-[#070a16] p-4 text-white sm:p-8"><Card className="mx-auto max-w-2xl border-white/10 bg-white/[0.04] shadow-2xl shadow-cyan-950/20"><CardHeader className="border-b border-white/10"><div className="flex items-start justify-between gap-4"><div><div className="flex items-center gap-2"><SlidersHorizontal className="size-5 text-cyan-300" /><Badge variant="outline" className="border-amber-300/20 text-amber-200">Local preview</Badge></div><CardTitle className="mt-3 text-3xl font-black">Quick settings</CardTitle><p className="mt-2 text-sm leading-6 text-slate-400">Review preferences without implying account persistence, identity, security enrollment, or delivery.</p></div><Button aria-label="Close settings dialog" onClick={() => setClosed(true)} variant="outline" size="icon" className="border-white/10 text-slate-300"><X className="size-4" /></Button></div></CardHeader><CardContent className="space-y-5 p-6"><div className="rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4 text-sm leading-6 text-amber-100"><strong>Evidence boundary:</strong> toggles are local browser intent. No email, message, MFA, wallet, session, or account state is connected.</div>{[{ label: "Email updates", detail: "No email provider, recipient, delivery, opt-out, or audit source is connected.", value: email, set: setEmail, icon: Bell }, { label: "Community messages", detail: "No authenticated inbox, sender, recipient, moderation, or delivery contract is connected.", value: messages, set: setMessages, icon: LockKeyhole }, { label: "Two-factor enrollment intent", detail: "This does not enroll an authenticator, issue a code, change a session, or prove account protection.", value: mfa, set: setMfa, icon: KeyRound }].map(({ label, detail, value, set, icon: Icon }) => <div key={label} className="flex items-center justify-between gap-4 rounded-xl border border-white/10 p-4"><div className="flex items-start gap-3"><Icon className="mt-0.5 size-5 text-cyan-300" /><div><p className="font-semibold">{label}</p><p className="mt-2 text-sm leading-6 text-slate-500">{detail}</p></div></div><button aria-pressed={value} onClick={() => set(!value)} className={`h-6 w-11 shrink-0 rounded-full p-1 transition ${value ? "bg-cyan-300" : "bg-white/10"}`}><span className={`block size-4 rounded-full bg-white transition ${value ? "translate-x-5" : "translate-x-0"}`} /></button></div>)}<div className="rounded-xl border border-white/10 p-4"><div className="flex items-start gap-3"><WalletCards className="size-5 text-slate-500" /><div><p className="font-semibold">Wallet</p><p className="mt-2 text-sm leading-6 text-slate-500">Not connected. No address, ownership, balance, token, NFT, transaction, private key, seed phrase, custody, or financial result is available.</p></div></div></div><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save locally"}</Button><Button onClick={reset} variant="outline" className="border-white/10 text-slate-300">Reset</Button><Button onClick={() => setClosed(true)} variant="outline" className="border-white/10 text-slate-300">Cancel</Button></div>{saved && <p className="text-sm text-cyan-200">Preferences were saved only in this browser view; no server mutation occurred.</p>}</CardContent></Card></div>; }
