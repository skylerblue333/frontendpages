import { useState } from "react";
import { Bell, CalendarClock, Check, CircleHelp, Clock3, Globe2, LockKeyhole, MapPin, MessageSquare, RefreshCw, ShieldAlert, UserCheck, UserRound, UsersRound, WifiOff, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Textarea } from "@/components/ui/textarea";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const CHECKS = ["Timezone and locale confirmed", "Organizer identity verified", "Invite delivery connected", "Attendee availability checked", "Reminder preference recorded", "Cancellation path defined"];
export default function CalendarView() { const [rsvp, setRsvp] = useState("Not selected"); const [question, setQuestion] = useState(""); const [saved, setSaved] = useState(false); return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={CalendarClock} eyebrow="Scheduling · Event Detail" title="Review the meeting before anyone is notified." description="Inspect a focused event detail, attendance intent, timezone checklist, and local discussion note. This page does not send invitations, confirm attendance, schedule reminders, or synchronize a calendar." badge="Preview event detail"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />Save local response</Button><Button onClick={() => { setRsvp("Not selected"); setQuestion(""); setSaved(false); }} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset view</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Event state", value: "Preview", hint: "Not a confirmed meeting", icon: CalendarClock }, { label: "RSVP", value: rsvp, hint: "Local selection", icon: UserCheck, tone: "violet" }, { label: "Timezone", value: "Unverified", hint: "Needs source context", icon: Globe2, tone: "amber" }, { label: "Notifications", value: "Unavailable", hint: "No invite service", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="Event-detail evidence boundary">This detail page is not an invitation or calendar record. A real event requires a verified organizer, timezone-aware timestamp, participant consent, invitation state, reminder preferences, update and cancellation semantics, privacy controls, and a provider-backed audit trail.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Selected event</p><h2 className="mt-2 text-3xl font-black">Product review workshop</h2><p className="mt-3 text-slate-400">April 6 · 10:00 local preview time</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Synthetic</Badge></div><div className="mt-6 grid gap-3 sm:grid-cols-3"><Detail label="Location" value="Remote · preview" icon={MapPin} /><Detail label="Organizer" value="Unverified" icon={UserRound} /><Detail label="Guests" value="Unconfirmed" icon={UsersRound} /></div><div className="mt-6 rounded-xl border border-white/10 bg-black/15 p-4"><div className="flex items-center gap-2 text-sm font-medium"><MessageSquare className="size-4 text-cyan-300" />Agenda context</div><p className="mt-3 text-sm leading-6 text-slate-300">Review launch readiness, open questions, and next-step ownership. This is preview copy; no meeting or attendee record exists.</p></div><div className="mt-6"><p className="text-sm font-semibold">Attendance intent</p><div className="mt-3 grid gap-2 sm:grid-cols-3">{["Going", "Maybe", "Can't attend"].map((option) => <button key={option} onClick={() => { setRsvp(option); setSaved(false); }} className={`rounded-xl border p-3 text-sm ${rsvp === option ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-200" : "border-white/10 bg-black/15 text-slate-400"}`}>{option}</button>)}</div><p className="mt-2 text-xs text-slate-500">This selection is local and does not notify an organizer.</p></div></CardContent></Card><div className="space-y-6"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><Globe2 className="size-5 text-cyan-300" /><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Readiness checklist</p><h2 className="mt-1 text-xl font-bold">What is not proven</h2></div></div><div className="mt-5 space-y-2">{CHECKS.map((item) => <div key={item} className="flex items-center gap-3 rounded-lg border border-white/10 bg-black/15 p-3"><CircleHelp className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Unavailable</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><MessageSquare className="size-5 text-violet-300" /><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Local question</p><h2 className="mt-1 text-xl font-bold">Add context</h2></div></div><Textarea value={question} onChange={(event) => setQuestion(event.target.value)} placeholder="What should be clarified before this event?" className="mt-5 min-h-24 border-white/10 bg-black/20 text-white placeholder:text-slate-500" />{saved && <p className="mt-3 flex items-center gap-2 text-sm text-cyan-200"><Check className="size-4" />Local response saved; no notification was sent.</p>}</CardContent></Card></div></section><section className="grid gap-4 md:grid-cols-3"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><Clock3 className="size-5 text-cyan-300" /><h3 className="mt-3 font-semibold">Time needs a source</h3><p className="mt-2 text-sm leading-6 text-slate-400">A displayed local time is not a timezone-normalized event timestamp.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><Bell className="size-5 text-violet-300" /><h3 className="mt-3 font-semibold">No reminder claim</h3><p className="mt-2 text-sm leading-6 text-slate-400">A preview RSVP does not schedule a reminder or create a notification.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><LockKeyhole className="size-5 text-amber-300" /><h3 className="mt-3 font-semibold">Privacy is part of attendance</h3><p className="mt-2 text-sm leading-6 text-slate-400">Guest lists, notes, locations, and attendance choices need scoped access and retention controls.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Detail supports decisions", description: "A focused event view should surface context, state, and unresolved gates instead of a fake confirmation.", icon: CalendarClock, status: "Pattern" }, { title: "Intent is not attendance", description: "A local Going/Maybe choice is not a sent RSVP, organizer response, or accepted invitation.", icon: UserCheck, status: "Guardrail" }, { title: "No fake sync", description: "Without a provider, calendar writes, notifications, and availability remain unavailable.", icon: WifiOff, status: "Required" }]} /></main></div>; }
function Detail({ label, value, icon: Icon }: { label: string; value: string; icon: typeof CalendarClock }) { return <div className="rounded-lg border border-white/10 bg-black/15 p-4"><Icon className="size-4 text-slate-400" /><p className="mt-3 text-xs uppercase tracking-wider text-slate-500">{label}</p><p className="mt-1 text-sm text-slate-200">{value}</p></div>; }
