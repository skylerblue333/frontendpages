import { useMemo, useState } from "react";
import { Activity, AlertTriangle, Bot, CheckCircle2, ChevronRight, Code2, Copy, Cpu, Eye, FileCode2, GitBranch, LockKeyhole, Play, Search, Shield, Sparkles, Terminal, TestTube2, WandSparkles, Zap } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { Textarea } from "@/components/ui/textarea";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

const BOTS = ["NOVA", "CIPHER", "ATLAS", "PRISM", "FORGE", "VECTOR", "NEXUS", "PULSE", "SHIELD", "ORACLE", "ECHO", "TITAN"];
const LANGUAGES = ["TypeScript", "JavaScript", "Python", "Rust", "Go", "Solidity", "SQL", "YAML"];
const MODES = ["Generate", "Review", "Refactor", "Test", "Document", "Audit"];
const QUICK_TASKS = ["React component", "tRPC route", "API test plan", "Database schema", "CI workflow", "Smart-contract review"];
const SPRINT_STEPS = ["Define acceptance criteria", "Generate a reviewable change", "Run tests and security checks", "Request human approval", "Prepare a branch or patch"];

function CopyButton({ text }: { text: string }) { const [copied, setCopied] = useState(false); return <Button variant="ghost" size="icon" onClick={() => { void navigator.clipboard?.writeText(text); setCopied(true); setTimeout(() => setCopied(false), 1400); }} aria-label="Copy output" className="text-slate-400 hover:text-white">{copied ? <CheckCircle2 className="size-4 text-emerald-300" /> : <Copy className="size-4" />}</Button>; }

function GeneratorPanel() {
  const [bot, setBot] = useState("NOVA"); const [language, setLanguage] = useState("TypeScript"); const [mode, setMode] = useState("Generate"); const [prompt, setPrompt] = useState(""); const [target, setTarget] = useState(""); const [context, setContext] = useState(""); const [prepared, setPrepared] = useState(false);
  return <div className="grid gap-5 xl:grid-cols-[0.7fr_1.3fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="space-y-5 p-5"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Configure</p><h2 className="mt-1 text-xl font-bold">Choose a workflow</h2></div><div><p className="mb-2 text-xs uppercase tracking-wider text-slate-500">Agent archetype</p><div className="grid grid-cols-3 gap-2">{BOTS.map((name) => <button key={name} onClick={() => setBot(name)} className={`rounded-lg border p-2 text-xs font-semibold transition ${bot === name ? "border-cyan-300/50 bg-cyan-300/10 text-cyan-100" : "border-white/10 bg-black/15 text-slate-400 hover:bg-white/[0.05]"}`}>{name}</button>)}</div></div><div className="grid grid-cols-2 gap-3"><label className="text-xs text-slate-400">Language<select value={language} onChange={(event) => setLanguage(event.target.value)} className="mt-2 w-full rounded-md border border-white/10 bg-black/20 px-2 py-2 text-xs text-white">{LANGUAGES.map((item) => <option key={item}>{item}</option>)}</select></label><label className="text-xs text-slate-400">Mode<select value={mode} onChange={(event) => setMode(event.target.value)} className="mt-2 w-full rounded-md border border-white/10 bg-black/20 px-2 py-2 text-xs text-white">{MODES.map((item) => <option key={item}>{item}</option>)}</select></label></div><label className="block text-xs text-slate-400">Target file <Input value={target} onChange={(event) => setTarget(event.target.value)} placeholder="e.g. client/src/App.tsx" className="mt-2 border-white/10 bg-black/20 text-white placeholder:text-slate-600" /></label><div><p className="mb-2 text-xs uppercase tracking-wider text-slate-500">Quick tasks</p><div className="flex flex-wrap gap-2">{QUICK_TASKS.map((task) => <Button key={task} size="sm" variant="outline" onClick={() => setPrompt(`Prepare a ${task} with typed interfaces, tests, and a review checklist.`)} className="border-white/10 bg-white/5 text-slate-300 hover:bg-white/10">{task}</Button>)}</div></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-300">Build brief</p><h2 className="mt-1 text-xl font-bold">What should {bot} prepare?</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Human review</Badge></div><Textarea value={prompt} onChange={(event) => { setPrompt(event.target.value); setPrepared(false); }} placeholder="Describe the code change, review, or test plan you need..." aria-label="Code studio prompt" className="mt-5 min-h-40 border-white/10 bg-black/20 font-mono text-sm text-white placeholder:text-slate-500" /><details className="mt-4 rounded-lg border border-white/10 bg-black/10 p-3"><summary className="cursor-pointer text-xs text-slate-400"><ChevronRight className="mr-1 inline size-3" />Add context code</summary><Textarea value={context} onChange={(event) => setContext(event.target.value)} placeholder="Paste existing code or constraints for context..." className="mt-3 min-h-24 border-white/10 bg-black/20 font-mono text-xs text-white" /></details><Button onClick={() => setPrepared(Boolean(prompt.trim()))} disabled={!prompt.trim()} className="mt-5 w-full bg-violet-300 text-slate-950 hover:bg-violet-200"><WandSparkles className="mr-2 size-4" />Prepare {mode.toLowerCase()} preview</Button>{prepared ? <div className="mt-5 rounded-xl border border-emerald-300/20 bg-emerald-300/[0.06] p-4"><div className="flex items-center gap-2 font-medium text-emerald-200"><CheckCircle2 className="size-4" />Request prepared</div><p className="mt-2 text-sm leading-6 text-slate-300">{bot} is ready to prepare a {mode.toLowerCase()} workflow for {language}{target ? ` in ${target}` : ""}. No code, test result, security finding, or branch is fabricated before a verified service responds.</p></div> : <div className="mt-5 rounded-xl border border-white/10 bg-black/15 p-4 text-sm leading-6 text-slate-400"><Terminal className="mr-2 inline size-4 text-violet-300" />Generated output will appear here after a connected code service is verified.</div>}</CardContent></Card></div>;
}

function AutonomousPanel() { const [selected, setSelected] = useState<string[]>([]); const toggle = (step: string) => setSelected((current) => current.includes(step) ? current.filter((item) => item !== step) : [...current, step]); return <div className="grid gap-5 lg:grid-cols-[0.75fr_1.25fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Orchestrator</p><h2 className="mt-1 text-xl font-bold">Autonomous sprint</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Disabled</Badge></div><div className="mt-5 grid grid-cols-2 gap-3">{[{ label: "Active bots", value: "Not connected" }, { label: "Tasks", value: "Preview" }, { label: "Pushes", value: "0 claimed" }, { label: "Execution", value: "Review first" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 bg-black/15 p-3"><p className="text-lg font-bold text-cyan-200">{item.value}</p><p className="mt-1 text-xs text-slate-400">{item.label}</p></div>)}</div><Button disabled className="mt-5 w-full"><Play className="mr-2 size-4" />Run autonomous cycle</Button><p className="mt-3 text-xs leading-5 text-slate-500">Execution, commits, pushes, and production changes remain disabled until identity, permissions, review, and audit contracts are verified.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="flex items-center gap-2"><GitBranch className="size-5 text-violet-300" /><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-300">Review plan</p><h2 className="mt-1 text-xl font-bold">Safe change pipeline</h2></div></div><div className="mt-5 space-y-3">{SPRINT_STEPS.map((step, index) => <button key={step} onClick={() => toggle(step)} className={`flex w-full items-center gap-3 rounded-xl border p-4 text-left transition ${selected.includes(step) ? "border-emerald-300/30 bg-emerald-300/[0.06]" : "border-white/10 bg-black/15 hover:bg-white/[0.05]"}`}><span className={`flex size-8 items-center justify-center rounded-lg text-sm font-bold ${selected.includes(step) ? "bg-emerald-300/20 text-emerald-200" : "bg-white/10 text-slate-300"}`}>{selected.includes(step) ? <CheckCircle2 className="size-4" /> : index + 1}</span><span className="flex-1 text-sm text-slate-200">{step}</span><Badge variant="outline" className="border-white/10 text-slate-500">Preview</Badge></button>)}</div></CardContent></Card></div>; }

function AnalyzePanel() { const [code, setCode] = useState(""); const [type, setType] = useState("Full analysis"); const [prepared, setPrepared] = useState(false); const [query, setQuery] = useState(""); return <div className="grid gap-5 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-emerald-300">Static review</p><h2 className="mt-1 text-xl font-bold">Code analyzer</h2></div><select value={type} onChange={(event) => setType(event.target.value)} className="rounded-md border border-white/10 bg-black/20 px-3 py-2 text-xs text-white"><option>Full analysis</option><option>Security audit</option><option>Performance</option><option>Code quality</option></select></div><Textarea value={code} onChange={(event) => { setCode(event.target.value); setPrepared(false); }} placeholder="Paste code here for a review plan..." aria-label="Code analyzer input" className="mt-5 min-h-64 border-white/10 bg-black/20 font-mono text-xs text-white placeholder:text-slate-500" /><Button onClick={() => setPrepared(Boolean(code.trim()))} disabled={!code.trim()} className="mt-5 w-full bg-emerald-300 text-slate-950 hover:bg-emerald-200"><Activity className="mr-2 size-4" />Prepare analysis preview</Button></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-emerald-300">Findings</p><h2 className="mt-1 text-xl font-bold">Evidence before scores</h2></div><CopyButton text="Analysis output requires a verified code service." /></div>{prepared ? <div className="mt-5 space-y-4"><Badge className="border-emerald-300/20 bg-emerald-300/10 text-emerald-200">Review request prepared</Badge><div className="rounded-xl border border-amber-300/20 bg-amber-300/[0.05] p-4"><div className="flex items-center gap-2 font-medium text-amber-100"><AlertTriangle className="size-4" />No findings invented</div><p className="mt-2 text-sm leading-6 text-slate-300">A {type.toLowerCase()} report will appear only after a verified analyzer responds. This screen does not claim a score, issue count, or security certification.</p></div></div> : <ScreenStatePanel type="empty" title="No code analyzed" description="Paste code to prepare a bounded analysis request. Results remain unavailable until the service contract is verified." />}</CardContent></Card></div>; }

export default function AICodeStudio() { return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Code2} eyebrow="AI & Automation · Code Studio" title="Build with evidence, not blind automation." description="Explore code generation, review, refactor, testing, documentation, auditing, and autonomous sprint planning in one workspace. Actual code output, commits, pushes, and security results appear only after verified services respond." badge="Preview studio"><div className="flex flex-wrap gap-2"><Button onClick={() => document.getElementById("code-studio")?.scrollIntoView({ behavior: "smooth" })} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Code2 className="mr-2 size-4" />Open code studio</Button><Button variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><LockKeyhole className="mr-2 size-4" />Review permissions</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Studio modes", value: "3", hint: "Generate, sprint, analyze", icon: Code2 }, { label: "Agent archetypes", value: String(BOTS.length), hint: "Selectable workflow roles", icon: Bot, tone: "violet" }, { label: "Live output", value: "Not connected", hint: "No code is fabricated", icon: LockKeyhole, tone: "amber" }, { label: "Push access", value: "Disabled", hint: "Human approval is required", icon: GitBranch, tone: "slate" }]} /><ScreenPreviewBanner title="Production boundary">The studio’s selectors, quick tasks, prompt drafting, review pipeline, analysis configuration, and explicit disabled states are available for UX review. Generated code, autonomous tasks, line counts, commits, pushes, security findings, test results, and performance scores are not represented as live facts in this preview.</ScreenPreviewBanner><section id="code-studio"><Tabs defaultValue="generate" className="space-y-4"><TabsList className="grid h-auto w-full grid-cols-3 gap-2 border border-white/10 bg-white/[0.04] p-2"><TabsTrigger value="generate" className="gap-2 text-slate-300 data-[state=active]:bg-cyan-300 data-[state=active]:text-slate-950"><Zap className="size-4" />Generator</TabsTrigger><TabsTrigger value="autonomous" className="gap-2 text-slate-300 data-[state=active]:bg-violet-300 data-[state=active]:text-slate-950"><Cpu className="size-4" />Autonomous sprint</TabsTrigger><TabsTrigger value="analyze" className="gap-2 text-slate-300 data-[state=active]:bg-emerald-300 data-[state=active]:text-slate-950"><Activity className="size-4" />Analyzer</TabsTrigger></TabsList><TabsContent value="generate"><GeneratorPanel /></TabsContent><TabsContent value="autonomous"><AutonomousPanel /></TabsContent><TabsContent value="analyze"><AnalyzePanel /></TabsContent></Tabs></section><ScreenFeatureGrid features={[{ title: "Typed workflow inputs", description: "Choose language, mode, agent archetype, target file, and context before requesting output.", icon: FileCode2, status: "Available" }, { title: "Review before side effects", description: "Autonomous execution, commits, pushes, and production changes remain disabled until authorization is proven.", icon: Shield, status: "Required" }, { title: "Evidence-based analysis", description: "Scores and findings appear only when a verified analyzer returns them; the preview never invents them.", icon: TestTube2, status: "Contract required" }]} /></main></div>; }
