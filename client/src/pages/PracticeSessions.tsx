import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { BookOpen, CalendarClock, Check, Clock3, GraduationCap, LockKeyhole, Plus, RefreshCw, Search, ShieldAlert, ShieldCheck, Sparkles, Users, WifiOff, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

type Difficulty = "Beginner" | "Intermediate" | "Advanced";
type Topic = { id: string; name: string; description: string; difficulty: Difficulty; duration: number; resources: string[]; state: string };
const topics: Topic[] = [
  { id: "topic-001", name: "Market-data literacy", description: "Learn how timestamps, sources, freshness, and uncertainty shape a responsible data review.", difficulty: "Beginner", duration: 20, resources: ["Glossary", "Worksheet"], state: "Local concept" },
  { id: "topic-002", name: "Wallet safety review", description: "Practice identifying custody boundaries, address validation, and secrets that must stay private.", difficulty: "Intermediate", duration: 30, resources: ["Checklist", "Scenario"], state: "Local concept" },
  { id: "topic-003", name: "Evidence-led product audit", description: "Map a product claim to the tests, telemetry, and source contracts needed to support it.", difficulty: "Advanced", duration: 45, resources: ["Rubric", "Case study"], state: "Local concept" },
];
const sessions = [
  { id: "session-001", topic: "Market-data literacy", status: "Planned", date: "Not scheduled", partner: "No partner source", note: "A local practice plan, not a booked appointment." },
  { id: "session-002", topic: "Wallet safety review", status: "Draft", date: "Not scheduled", partner: "No partner source", note: "A local checklist state, not a verified learning event." },
];
const filters = ["All", "Beginner", "Intermediate", "Advanced"];
export default function PracticeSessions() {
  const [tab, setTab] = useState<"Available" | "My sessions">("Available");
  const [query, setQuery] = useState("");
  const [difficulty, setDifficulty] = useState("All");
  const [selected, setSelected] = useState(topics[0]);
  const [scheduled, setScheduled] = useState<string[]>([]);
  const [saved, setSaved] = useState(false);
  const [showDetail, setShowDetail] = useState(false);
  const visible = useMemo(() => topics.filter((item) => (!query || `${item.name} ${item.description} ${item.resources.join(" ")}`.toLowerCase().includes(query.toLowerCase())) && (difficulty === "All" || item.difficulty === difficulty)), [query, difficulty]);
  const schedule = (topic: Topic) => { setSelected(topic); setScheduled((items) => items.includes(topic.id) ? items : [...items, topic.id]); setShowDetail(true); };
  const reset = () => { setTab("Available"); setQuery(""); setDifficulty("All"); setSelected(topics[0]); setScheduled([]); setSaved(false); setShowDetail(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={GraduationCap} eyebrow="PracticeSessions · Learning preview" title="Plan practice without inventing a partner or completion record." description="Explore topic discovery, difficulty, duration, resources, local schedule intent, session filters, and selected detail. No learner account, partner, booking, attendance, XP, certificate, video room, or completion service is connected." badge="Practice planner"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} disabled={scheduled.length === 0} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Plan saved locally" : "Save local plan"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset sessions</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Topics", value: String(topics.length), hint: "Local concepts", icon: BookOpen, tone: "cyan" }, { label: "Planned", value: String(scheduled.length), hint: "Local intent", icon: CalendarClock, tone: "violet" }, { label: "Attendance", value: "Off", hint: "No event source", icon: Users, tone: "amber" }, { label: "XP", value: "Unverified", hint: "No progress source", icon: Sparkles, tone: "slate" }]} /><ScreenPreviewBanner title="Practice-session evidence boundary"><strong>This is not a booked lesson or verified learning record.</strong> Topic cards, labels, filters, schedule intent, selected detail, and local save state are browser concepts. They do not prove a user, partner, booking, attendance, completion, XP, assessment, certificate, or live room. No education or collaboration service is queried.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.82fr_1fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-2"><div className="relative flex-1"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search practice topics…" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-9 pr-3 text-sm text-white outline-none focus:border-cyan-300/50" /></div><Badge variant="outline" className="border-white/10 text-amber-200">{visible.length} visible</Badge></div><div className="mt-5 grid grid-cols-2 gap-2"><button onClick={() => setTab("Available")} className={`rounded-xl border p-3 text-sm ${tab === "Available" ? "border-cyan-300/40 bg-cyan-300/[0.06] text-cyan-200" : "border-white/10 text-slate-500"}`}>Available topics</button><button onClick={() => setTab("My sessions")} className={`rounded-xl border p-3 text-sm ${tab === "My sessions" ? "border-cyan-300/40 bg-cyan-300/[0.06] text-cyan-200" : "border-white/10 text-slate-500"}`}>My sessions</button></div><p className="mt-6 text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Difficulty filter</p><div className="mt-3 flex flex-wrap gap-2">{filters.map((item) => <button key={item} onClick={() => setDifficulty(item)} className={`rounded-lg border px-3 py-2 text-xs ${difficulty === item ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div><div className="mt-6 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex gap-3"><ShieldAlert className="mt-0.5 size-5 shrink-0 text-amber-200" /><p className="text-sm leading-6 text-slate-300">Filters and tabs change browser state only. They do not query a learner account, partner directory, calendar, class roster, or progress service.</p></div></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6">{tab === "Available" ? <><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Available concepts</p><h2 className="mt-2 text-2xl font-black">Choose a practice topic</h2></div><Badge variant="outline" className="border-white/10 text-amber-200">Source off</Badge></div><div className="mt-5 space-y-3">{visible.map((topic) => <button key={topic.id} onClick={() => { setSelected(topic); setShowDetail(true); }} className={`w-full rounded-xl border p-4 text-left ${selected.id === topic.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start gap-3"><div className="flex size-10 items-center justify-center rounded-xl bg-cyan-300/10"><BookOpen className="size-5 text-cyan-200" /></div><div className="flex-1"><div className="flex flex-wrap items-center gap-2"><p className="font-semibold">{topic.name}</p><Badge variant="outline" className="border-white/10 text-slate-400">{topic.difficulty}</Badge></div><p className="mt-2 text-sm leading-5 text-slate-500">{topic.description}</p><div className="mt-3 flex flex-wrap gap-2 text-xs text-slate-400"><span className="flex items-center gap-1"><Clock3 className="size-3" />{topic.duration} min</span>{topic.resources.map((resource) => <span key={resource} className="rounded border border-white/10 px-2 py-1">{resource}</span>)}</div></div><span className="text-xs text-amber-200">{scheduled.includes(topic.id) ? "Planned locally" : topic.state}</span></div><Button onClick={(event) => { event.stopPropagation(); schedule(topic); }} size="sm" className="mt-4 bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Plus className="mr-1 size-4" />{scheduled.includes(topic.id) ? "Planned" : "Plan locally"}</Button></button>)}</div></> : <><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Local session list</p><h2 className="mt-2 text-2xl font-black">My sessions</h2></div><Badge variant="outline" className="border-white/10 text-amber-200">Account off</Badge></div><div className="mt-5 space-y-3">{sessions.map((session) => <button key={session.id} onClick={() => { const topic = topics.find((item) => item.name === session.topic); if (topic) { setSelected(topic); setShowDetail(true); } }} className="w-full rounded-xl border border-white/10 p-4 text-left"><div className="flex items-start gap-3"><CalendarClock className="mt-1 size-5 text-cyan-200" /><div className="flex-1"><p className="font-semibold">{session.topic}</p><p className="mt-1 text-sm text-slate-500">{session.status} · {session.date} · {session.partner}</p><p className="mt-2 text-xs text-slate-400">{session.note}</p></div><Badge variant="outline" className="border-white/10 text-amber-200">Local</Badge></div></button>)}</div></>}</CardContent></Card></section>{showDetail && <section><Card className="border-cyan-300/20 bg-cyan-300/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Selected detail</p><h2 className="mt-2 text-2xl font-black">{selected.name}</h2><p className="mt-3 max-w-3xl text-sm leading-6 text-slate-400">{selected.description}</p></div><Button aria-label="Close selected practice detail" onClick={() => setShowDetail(false)} variant="outline" className="border-white/10 text-slate-400"><X className="size-4" /></Button></div><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{[{ label: "Difficulty", value: selected.difficulty }, { label: "Duration", value: `${selected.duration} min` }, { label: "Resources", value: selected.resources.join(" · ") }, { label: "Completion", value: "Unmeasured" }, { label: "Attendance", value: "Unavailable" }, { label: "Partner", value: "Unavailable" }, { label: "XP", value: "Unverified" }, { label: "Certificate", value: "Unavailable" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div></CardContent></Card></section>}<section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Practice must prove</p><h2 className="mt-2 text-2xl font-black">Production checklist</h2><div className="mt-5 space-y-3">{["Authenticated learner, privacy, account ownership, and safe collaboration permissions", "Topic content, difficulty, duration, resources, versioning, accessibility, and source attribution", "Scheduling, time zones, partner identity, reminders, cancellation, attendance, and retry behavior", "Progress, assessment, XP/certification rules, integrity, audit, and completion reconciliation", "Tests for empty/loading/error/offline states, provider outage, duplicate booking, and unsafe content"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Practice surface preserved", description: "Topics, difficulty, duration, resources, search, tabs, schedule intent, filters, and detail remain interactive.", icon: GraduationCap, status: "Local planner" }, { title: "Accounts and live sessions are off", description: "Learners, partners, calendars, rooms, attendance, XP, progress, and certificates remain unavailable.", icon: ShieldAlert, status: "Guardrail" }, { title: "No fake completion", description: "No booking, attendance, learning outcome, score, streak, XP, or success rate is fabricated.", icon: WifiOff, status: "Unavailable" }]} /></section></main></div>;
}
