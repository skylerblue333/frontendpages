import { useState } from "react";
import { Accessibility, Check, Eye, Layers3, LockKeyhole, MonitorCog, RefreshCw, ShieldAlert, SlidersHorizontal, Sparkles, SunMoon, Type, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatePanel, ScreenStatGrid } from "@/components/ScreenExperience";
const themes = [{ name: "Aurora", detail: "Cyan / violet preview", tone: "from-cyan-400/30 to-violet-500/30" }, { name: "Midnight", detail: "Deep neutral preview", tone: "from-slate-600/40 to-slate-950/50" }, { name: "Solar", detail: "Warm contrast preview", tone: "from-amber-400/30 to-orange-500/30" }];
export default function StyleSelector() { const [theme, setTheme] = useState(0); const [density, setDensity] = useState("Comfortable"); const [contrast, setContrast] = useState("Standard"); const [motion, setMotion] = useState("Reduced"); const [saved, setSaved] = useState(false); const [showBoundary, setShowBoundary] = useState(false); const [retrying, setRetrying] = useState(false); const retry = () => { setRetrying(true); window.setTimeout(() => setRetrying(false), 500); }; const reset = () => { setTheme(0); setDensity("Comfortable"); setContrast("Standard"); setMotion("Reduced"); setSaved(false); setShowBoundary(false); setRetrying(false); }; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Sparkles} eyebrow="Style selector · Personalization preview" title="Tune the interface without claiming preferences were persisted." description="Explore a local style workspace with themes, density, contrast, motion, typography, preview, accessibility checks, local save/reset, retry, and honest client-only persistence boundaries. No account preference, cross-device sync, accessibility certification, or visual-outcome claim is connected." badge="Evidence-bounded personalization"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save local style"}</Button><Button onClick={() => setShowBoundary((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showBoundary ? <X className="mr-2 size-4" /> : <LockKeyhole className="mr-2 size-4" />}{showBoundary ? "Close boundary" : "Review persistence gates"}</Button><Button onClick={retry} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className={`mr-2 size-4 ${retrying ? "animate-spin" : ""}`} />{retrying ? "Retrying local state" : "Retry preference source"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><X className="mr-2 size-4" />Reset style</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Theme", value: themes[theme].name, hint: "Local preview", icon: Sparkles, tone: "cyan" }, { label: "Density", value: density, hint: "Local control", icon: Layers3, tone: "violet" }, { label: "Contrast", value: contrast, hint: "Not certified", icon: SunMoon, tone: "amber" }, { label: "Motion", value: motion, hint: "Local control", icon: Accessibility, tone: "slate" }]} /><ScreenPreviewBanner title="Personalization evidence boundary"><strong>This is a local style-design preview, not evidence that preferences were persisted, synchronized, accessible to every user, or certified against an accessibility standard.</strong> Theme selection, density, contrast, motion, typography controls, local saved state, retry feedback, and visual preview are browser concepts. No account setting, cross-device sync, accessibility conformance, readability outcome, or conversion result is asserted.</ScreenPreviewBanner><section className="grid gap-4 lg:grid-cols-3">{themes.map((item, index) => <button key={item.name} onClick={() => setTheme(index)} className={`rounded-2xl border bg-gradient-to-br p-5 text-left ${item.tone} ${theme === index ? "border-cyan-300/50" : "border-white/10"}`}><div className="flex items-start justify-between"><div className="flex size-11 items-center justify-center rounded-2xl bg-black/20 text-cyan-100"><Sparkles className="size-5" /></div><Badge variant="outline" className="border-white/20 text-white">{theme === index ? "Selected locally" : "Preview"}</Badge></div><h2 className="mt-4 font-bold">{item.name}</h2><p className="mt-2 text-sm text-slate-300">{item.detail}</p></button>)}</section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="grid gap-5 md:grid-cols-3"><label className="text-sm font-semibold text-slate-300"><span className="flex items-center gap-2"><Layers3 className="size-4 text-slate-500" />Density</span><select value={density} onChange={(event) => setDensity(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white"><option>Compact</option><option>Comfortable</option><option>Spacious</option></select></label><label className="text-sm font-semibold text-slate-300"><span className="flex items-center gap-2"><SunMoon className="size-4 text-slate-500" />Contrast intent</span><select value={contrast} onChange={(event) => setContrast(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white"><option>Standard</option><option>High contrast intent</option><option>Dimmed intent</option></select></label><label className="text-sm font-semibold text-slate-300"><span className="flex items-center gap-2"><Accessibility className="size-4 text-slate-500" />Motion intent</span><select value={motion} onChange={(event) => setMotion(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white"><option>Reduced</option><option>Standard</option><option>Expanded intent</option></select></label></div></CardContent></Card><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200">Preview surface</p><h2 className="mt-2 text-2xl font-black">{themes[theme].name} · {density}</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Local only</Badge></div><div className="mt-6 rounded-2xl border border-white/10 bg-gradient-to-br from-cyan-300/15 to-violet-500/15 p-6"><div className="flex items-center gap-3"><MonitorCog className="size-6 text-cyan-200" /><div><p className="font-bold">Interface preview</p><p className="mt-1 text-sm text-slate-400">Contrast: {contrast} · Motion: {motion} · Typography: not configured</p></div></div><div className="mt-5 grid gap-3 sm:grid-cols-3">{["Navigation", "Cards", "Feedback"].map((label) => <div key={label} className="rounded-xl border border-white/10 bg-black/15 p-4"><Type className="size-4 text-cyan-300" /><p className="mt-3 text-sm font-semibold">{label}</p><p className="mt-1 text-xs text-slate-500">Preview only</p></div>)}</div></div></CardContent></Card><ScreenStatePanel type="empty" title="Account preference sync unavailable" description="Connect an authenticated preference profile, privacy policy, conflict resolution, cross-device sync, reset semantics, and accessibility validation before claiming a persisted style experience." /></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="grid gap-3 md:grid-cols-3">{[{ title: "Accessibility checks", text: "Contrast, focus, keyboard, screen reader, motion, zoom, semantics, and content reflow still require testing.", icon: Accessibility }, { title: "Preference privacy", text: "No account identifier, behavioral profile, device sync, or personalization analytics is stored.", icon: LockKeyhole }, { title: "Visual quality", text: "A polished preview does not prove readability, comprehension, retention, or conversion outcomes.", icon: Eye }].map(({ title, text, icon: Icon }) => <div key={title} className="rounded-xl border border-white/10 p-4"><Icon className="size-5 text-cyan-300" /><p className="mt-3 font-semibold">{title}</p><p className="mt-2 text-sm leading-6 text-slate-500">{text}</p></div>)}</div></CardContent></Card>{showBoundary && <Card className="border-amber-300/20 bg-amber-300/[0.06]"><CardContent className="p-6"><p className="font-semibold text-amber-100">No persistence or accessibility certification claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local style preview does not prove account persistence, cross-device synchronization, WCAG conformance, readability, comprehension, retention, or conversion outcomes.</p></CardContent></Card>}<ScreenFeatureGrid features={[{ title: "Style surface preserved", description: "Themes, density, contrast, motion, typography, preview, accessibility, privacy, retry, save/reset, and sync gates remain visible.", icon: Sparkles, status: "Local personalization" }, { title: "No preference theater", description: "Cross-device preferences, accessibility certification, readability, retention, and conversion outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before claiming", description: "Real personalization requires account storage, privacy, conflict resolution, accessibility testing, and audit.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>; }
