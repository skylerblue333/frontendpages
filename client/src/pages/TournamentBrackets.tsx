import FeatureUnavailable from "@/components/FeatureUnavailable";

const TournamentBrackets = () => (
  <FeatureUnavailable
    title="Tournament Brackets"
    description="Tournament Brackets is intentionally held at a truthful release boundary until its real provider, authorization, persistence, monitoring, and acceptance evidence are complete."
    capability="Tournament Brackets on /tournament-brackets"
    nextStep="Return to the launch hub"
  />
);

export default TournamentBrackets;
