import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Check, ClipboardList, Filter, FolderOpen, LockKeyhole, RefreshCw, Search, ShieldAlert, SlidersHorizontal, Store, Users, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const listings = [{ id: "ml-001", name: "HopeAI workspace", group: "AI", status: "Draft", evidence: "Required", owner: "Unassigned" }, { id: "ml-002", name: "SkySchool learning", group: "Education", status: "Review required", evidence: "Required", owner: "Unassigned" }, { id: "ml-003", name: "Crypto Hub", group: "Financial", status: "Blocked", evidence: "High-risk review", owner: "Unassigned" }, { id: "ml-004", name: "Community spaces", group: "Community", status: "Draft", evidence: "Required", owner: "Unassigned" }];
const statuses = ["All", "Draft", "Review required", "Blocked"];
export default function ProductListings() {
  const [query, setQuery] = useState("");
  const [status, setStatus] = useState("All");
  const [selectedId, setSelectedId] = useState(listings[0].id);
  const [selected, setSelected] = useState<string[]>([]);
  const [saved, setSaved] = useState(false);
  const [showGates, setShowGates] = useState(false);
  const visible = useMemo(() => listings.filter((item) => (status === "All" || item.status === status) && `${item.name} ${item.group} ${item.status}`.toLowerCase().includes(query.toLowerCase())), [query, status]);
  const current = listings.find((item) => item.id === selectedId) ?? listings[0];
  const toggle = (id: string) => setSelected((items) => items.includes(id) ? items.filter((item) => item !== id) : [...items, id]);
  const reset = () => { setQuery(""); setStatus("All"); setSelectedId(listings[0].id); setSelected([]); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Store} eyebrow="ProductListings · Listing management preview" title="Manage listing concepts before the catalog becomes public." description="Search and filter local listing concepts, inspect statuses, owners, evidence, grouping, and bulk-action intent. No live listing count, seller identity, moderation decision, inventory, publication, ranking, commerce, or destructive action is connected." badge="Listing management"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "View saved locally" : "Save view locally"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset view</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Listing concepts", value: String(listings.length), hint: "Local registry", icon: ClipboardList, tone: "cyan" }, { label: "Published", value: "0", hint: "No publication source", icon: LockKeyhole, tone: "violet" }, { label: "Owners", value: "0", hint: "No identity source", icon: Users, tone: "amber" }, { label: "Bulk actions", value: "Blocked", hint: "No admin service", icon: ShieldAlert, tone: "slate" }]} /><ScreenPreviewBanner title="Listing-management evidence boundary"><strong>This is a local listing registry preview, not an admin console.</strong> Listing rows, status, evidence, owner, groups, and selection are browser-state concepts. No live count, customer, seller, inventory, moderation decision, publication, ranking, analytics, commerce, or deletion is verified or executed.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.85fr_1.15fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Listing registry</p><h2 className="mt-2 text-2xl font-black">Find a listing concept</h2></div><Badge variant="outline" className="border-white/10 text-amber-200">{visible.length} visible</Badge></div><div className="relative mt-5"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search listings…" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-9 pr-3 text-sm text-white outline-none focus:border-cyan-300/50" /></div><div className="mt-4 flex flex-wrap gap-2">{statuses.map((item) => <button key={item} onClick={() => setStatus(item)} className={`rounded-lg border px-3 py-2 text-xs ${status === item ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div><div className="mt-4 space-y-2">{visible.map((item) => <button key={item.id} onClick={() => setSelectedId(item.id)} className={`flex w-full items-center gap-3 rounded-xl border p-4 text-left ${current.id === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><button aria-label={`select ${item.name}`} onClick={(event) => { event.stopPropagation(); toggle(item.id); }} className={`flex size-5 items-center justify-center rounded border ${selected.includes(item.id) ? "border-cyan-300 bg-cyan-300 text-slate-950" : "border-white/20"}`}>{selected.includes(item.id) && <Check className="size-3" />}</button><Store className="size-5 text-cyan-200" /><div className="flex-1"><div className="flex flex-wrap items-center gap-2"><p className="font-semibold">{item.name}</p><Badge variant="outline" className="border-white/10 text-slate-400">{item.status}</Badge></div><p className="mt-2 text-sm text-slate-500">{item.group} · Evidence {item.evidence} · Owner {item.owner}</p></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Listing detail</p><h2 className="mt-2 text-2xl font-black">{current.name}</h2><p className="mt-2 text-sm text-slate-500">{current.group} · local registry row</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{current.status}</Badge></div><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Evidence", value: current.evidence }, { label: "Owner", value: current.owner }, { label: "Publication", value: "Blocked" }, { label: "Inventory", value: "Unknown" }, { label: "Ranking", value: "Off" }, { label: "Commerce", value: "Unavailable" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-6 rounded-2xl border border-amber-300/25 bg-amber-300/[0.06] p-5"><div className="flex items-start gap-3"><LockKeyhole className="mt-0.5 size-5 shrink-0 text-amber-200" /><div><p className="font-semibold text-amber-100">Administrative actions are blocked</p><p className="mt-2 text-sm leading-6 text-slate-400">A real management service needs scoped identity, moderation, asset rights, versioning, audit, rollback, catalog sync, support, and safe bulk-operation semantics.</p></div></div></div></CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Bulk selection intent</p><h2 className="mt-2 text-2xl font-black">{selected.length} local rows selected</h2></div><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/10 text-slate-300">{showGates ? <X className="size-4" /> : <SlidersHorizontal className="mr-2 size-4" />} {showGates ? "Close" : "Review gates"}</Button></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled variant="outline" className="border-white/10 text-slate-500">Publish unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Archive unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Delete unavailable</Button></div>{showGates && <div className="mt-5 grid gap-3 sm:grid-cols-2">{["Authenticated role and per-row authorization", "Preview, confirmation, idempotency, and audit", "Moderation, asset rights, and policy review", "Rollback, error recovery, and partial-failure handling"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldAlert className="size-4 text-slate-500" /><span className="text-sm text-slate-300">{item}</span></div>)}</div>}</CardContent></Card><ScreenFeatureGrid features={[{ title: "Management surface preserved", description: "Listing search, status filters, local selection, selected detail, owner/evidence labels, bulk-action intent, save/reset, and gates remain interactive.", icon: ClipboardList, status: "Local registry" }, { title: "No admin theater", description: "Live counts, ownership, moderation, inventory, publication, ranking, commerce, and destructive operations are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Bulk actions stay safe", description: "Publishing, archiving, deleting, and catalog changes remain unavailable until authorization, preview, audit, rollback, and failure handling exist.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
