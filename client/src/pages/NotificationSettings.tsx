import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Bell, Check, Clock3, FileCheck2, LockKeyhole, Mail, RefreshCw, Settings2, ShieldCheck, Smartphone, ToggleRight, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const SETTINGS = [{ title: "Product reminders", description: "Review windows, saved drafts, and workspace prompts.", icon: Bell, defaultOn: true }, { title: "Security prompts", description: "Access, consent, and safety-check concepts.", icon: ShieldCheck, defaultOn: true }, { title: "Digest mode", description: "Group lower-priority notification concepts.", icon: Mail, defaultOn: false }, { title: "Device push", description: "Requires a real device token and provider.", icon: Smartphone, defaultOn: false }];

export default function NotificationSettings() {
  const [values, setValues] = useState<Record<string, boolean>>(Object.fromEntries(SETTINGS.map((item) => [item.title, item.defaultOn])));
  const [quiet, setQuiet] = useState(true);
  const [saved, setSaved] = useState(false);
  const [note, setNote] = useState("");
  const toggle = (title: string) => { setValues((current) => ({ ...current, [title]: !current[title] })); setSaved(false); };
  const reset = () => { setValues(Object.fromEntries(SETTINGS.map((item) => [item.title, item.defaultOn]))); setQuiet(true); setSaved(false); setNote(""); };
  const active = Object.values(values).filter(Boolean).length;
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Settings2} eyebrow="NotificationSettings · Control surface" title="Make the setting explicit before making it durable." description="Review grouped notification controls, quiet-hours planning, channel requirements, and local diagnostics. No account setting, provider authorization, delivery change, unsubscribe action, or backend record is claimed." badge="Settings preview"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Settings saved locally" : "Save settings locally"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset settings</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Controls", value: `${active}/${SETTINGS.length}`, hint: "Local concepts", icon: Settings2 }, { label: "Quiet hours", value: quiet ? "On" : "Off", hint: "Timezone unset", icon: Clock3, tone: "violet" }, { label: "Device", value: "Off", hint: "No token", icon: Smartphone, tone: "slate" }, { label: "Account sync", value: "Off", hint: "No persistence", icon: WifiOff, tone: "cyan" }]} /><ScreenPreviewBanner title="NotificationSettings evidence boundary"><strong>Grouped controls, active counts, quiet-hours state, and diagnostics are local configuration fixtures—not account preferences, provider authorization, email/push delivery changes, unsubscribe state, legal consent, or a guaranteed notification behavior.</strong> Production settings require authentication, durable storage, channel ownership, timezone semantics, providers, retries, quiet-hour exceptions, privacy, auditability, deletion, and support.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1fr_0.85fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Settings groups</p><h2 className="mt-2 text-3xl font-black">Local control surface</h2><div className="mt-5 space-y-3">{SETTINGS.map((item) => { const Icon = item.icon; const activeSetting = values[item.title]; return <button key={item.title} onClick={() => toggle(item.title)} className={`flex w-full items-center gap-3 rounded-2xl border p-4 text-left ${activeSetting ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-black/10"}`}><div className="flex size-10 items-center justify-center rounded-xl bg-cyan-300/10 text-cyan-200"><Icon className="size-4" /></div><div className="flex-1"><p className="font-black">{item.title}</p><p className="mt-1 text-sm text-slate-500">{item.description}</p></div><span className={`text-xs ${activeSetting ? "text-cyan-200" : "text-slate-500"}`}>{activeSetting ? "Local on" : "Off"}</span></button>; })}</div><div className="mt-5 flex items-start gap-3 text-xs leading-5 text-slate-500"><LockKeyhole className="mt-0.5 size-4 shrink-0 text-cyan-300" />These switches change local component state only. No account, provider, device, or notification is contacted.</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Control diagnostics</p><h2 className="mt-2 text-3xl font-black">{saved ? "Local snapshot marked" : "No remote snapshot"}</h2><div className="mt-5 space-y-3">{[{ label: "Storage", value: "Component state" }, { label: "Channel", value: "Provider unset" }, { label: "Timezone", value: "Not configured" }, { label: "Unsubscribe", value: "Unavailable" }, { label: "Audit", value: "Not recorded" }].map((item) => <div key={item.label} className="flex items-center justify-between rounded-xl border border-white/10 p-3"><span className="text-sm text-slate-400">{item.label}</span><span className="text-xs text-amber-200">{item.value}</span></div>)}</div><div className="mt-5 rounded-2xl border border-violet-300/20 bg-violet-300/[0.05] p-5"><div className="flex items-center gap-3"><ToggleRight className="size-5 text-violet-200" /><div><p className="font-black">Quiet hours</p><p className="text-xs text-slate-500">22:00–07:00 · local planning only</p></div><button onClick={() => { setQuiet(!quiet); setSaved(false); }} className="ml-auto rounded-xl border border-white/10 px-3 py-2 text-xs text-violet-200">{quiet ? "On" : "Off"}</button></div><p className="mt-3 text-sm leading-6 text-slate-400">Timezone, channel exceptions, retries, and support behavior are not configured.</p></div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Settings note</p><h2 className="mt-2 text-2xl font-black">Capture the missing evidence</h2><textarea value={note} onChange={(event) => { setNote(event.target.value); setSaved(false); }} placeholder="Write a local settings, consent, or support note…" className="mt-5 min-h-28 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-sm text-white placeholder:text-slate-500" /><div className="mt-5 space-y-3">{["Account identity and durable preference storage", "Provider authorization, tokens, retries, and delivery receipts", "Timezone, quiet hours, exceptions, and digest semantics", "Privacy, accessibility, sensitive topics, unsubscribe, and deletion", "Auditability, support, failures, and incident response"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><FileCheck2 className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake control</h2><p className="mt-3 text-sm leading-6 text-slate-400">The surface changes local state only. It does not save to an account, register a device, authorize a channel, send or suppress a message, unsubscribe, or guarantee behavior.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "A setting is not persistence", description: "Local controls preserve UX without account, channel, consent, or backend claims.", icon: Settings2, status: "Guardrail" }, { title: "Control needs ownership", description: "Providers, tokens, timezones, exceptions, audit, privacy, and support need evidence.", icon: ShieldCheck, status: "Required" }, { title: "No fake action", description: "The preview makes no account, provider, push, email, unsubscribe, or backend request.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
