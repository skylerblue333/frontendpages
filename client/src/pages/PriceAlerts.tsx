import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Bell, Check, ChevronRight, Clock3, Database, LockKeyhole, Mail, Plus, RefreshCw, Search, Send, ShieldAlert, Smartphone, TrendingDown, TrendingUp, WifiOff, X, Zap } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

type Alert = { id: string; asset: string; condition: string; threshold: string; channel: string; status: string; source: string };
const initialAlerts: Alert[] = [
  { id: "alert-001", asset: "SKY", condition: "Above", threshold: "Set locally", channel: "In-app intent", status: "Draft", source: "No market feed" },
  { id: "alert-002", asset: "BTC", condition: "Below", threshold: "Set locally", channel: "Email intent", status: "Blocked", source: "No price feed" },
];
const assets = ["SKY", "BTC", "ETH", "SOL"];
export default function PriceAlerts() {
  const [alerts, setAlerts] = useState(initialAlerts);
  const [asset, setAsset] = useState(assets[0]);
  const [condition, setCondition] = useState("Above");
  const [threshold, setThreshold] = useState("");
  const [channel, setChannel] = useState("In-app intent");
  const [query, setQuery] = useState("");
  const [selectedId, setSelectedId] = useState(initialAlerts[0].id);
  const [saved, setSaved] = useState(false);
  const visible = useMemo(() => alerts.filter((item) => `${item.asset} ${item.condition} ${item.channel} ${item.status}`.toLowerCase().includes(query.toLowerCase())), [alerts, query]);
  const selected = alerts.find((item) => item.id === selectedId) ?? alerts[0];
  const addAlert = () => { const id = `alert-${Date.now()}`; const next: Alert = { id, asset, condition, threshold: threshold || "Set locally", channel, status: "Draft", source: "No market feed" }; setAlerts((items) => [next, ...items]); setSelectedId(id); setThreshold(""); setSaved(false); };
  const reset = () => { setAlerts(initialAlerts); setAsset(assets[0]); setCondition("Above"); setThreshold(""); setChannel("In-app intent"); setQuery(""); setSelectedId(initialAlerts[0].id); setSaved(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Bell} eyebrow="PriceAlerts · Market-monitoring preview" title="Design an alert without pretending a price was observed." description="Create and inspect local alert intents with asset, condition, threshold, and channel controls. No market feed, wallet, portfolio, notification provider, account, delivered alert, or transaction is connected." badge="Alert builder"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} disabled={alerts.length === 0} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Alerts saved locally" : "Save local alerts"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset alerts</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Alert intents", value: String(alerts.length), hint: "Local drafts", icon: Bell, tone: "cyan" }, { label: "Market feed", value: "Off", hint: "No live prices", icon: Database, tone: "violet" }, { label: "Delivery", value: "Blocked", hint: "No provider", icon: Send, tone: "amber" }, { label: "Triggered", value: "0", hint: "No evaluator", icon: LockKeyhole, tone: "slate" }]} /><ScreenPreviewBanner title="Price alert evidence boundary"><strong>No alert is active or delivered.</strong> Alert rows are local configuration intent. No current price, percentage movement, portfolio value, balance, trigger event, notification, user, transaction, or market statistic is claimed. A real alert requires an authenticated owner, permissioned price source, freshness/market rules, evaluator, retry/idempotency, and notification delivery.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.85fr_1.15fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Create local intent</p><h2 className="mt-2 text-2xl font-black">Configure a condition</h2></div><Badge variant="outline" className="border-white/10 text-amber-200">No feed</Badge></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-sm text-slate-400">Asset<select value={asset} onChange={(event) => setAsset(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-white outline-none">{assets.map((item) => <option key={item}>{item}</option>)}</select></label><label className="text-sm text-slate-400">Condition<select value={condition} onChange={(event) => setCondition(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-white outline-none"><option>Above</option><option>Below</option><option>Change by</option></select></label><label className="text-sm text-slate-400">Threshold<input value={threshold} onChange={(event) => setThreshold(event.target.value)} placeholder="Demo value only" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-white outline-none" /></label><label className="text-sm text-slate-400">Delivery intent<select value={channel} onChange={(event) => setChannel(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-white outline-none"><option>In-app intent</option><option>Email intent</option><option>Push intent</option><option>Webhook intent</option></select></label></div><Button onClick={addAlert} className="mt-5 w-full bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Plus className="mr-2 size-4" />Add local alert intent</Button><div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex gap-3"><ShieldAlert className="mt-0.5 size-5 shrink-0 text-amber-200" /><p className="text-sm leading-6 text-slate-400">The threshold is a local label. It is not checked against a current price, does not arm a trigger, and will not send a notification.</p></div></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Alert list</p><h2 className="mt-2 text-2xl font-black">Local configurations</h2></div><Badge variant="outline" className="border-white/10 text-amber-200">{visible.length} listed</Badge></div><div className="relative mt-5"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local alerts…" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-9 pr-3 text-sm text-white outline-none focus:border-cyan-300/50" /></div><div className="mt-4 space-y-3">{visible.map((item) => <button key={item.id} onClick={() => setSelectedId(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected?.id === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start gap-3"><Bell className="mt-1 size-5 text-cyan-200" /><div className="flex-1"><div className="flex flex-wrap items-center gap-2"><p className="font-semibold">{item.asset} · {item.condition}</p><Badge variant="outline" className="border-white/10 text-slate-400">{item.status}</Badge></div><p className="mt-2 text-sm text-slate-500">Threshold: {item.threshold} · {item.channel}</p><div className="mt-3 flex items-center gap-2 text-xs text-slate-400"><WifiOff className="size-3" />{item.source}</div></div><ChevronRight className="size-4 text-slate-600" /></div></button>)}</div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[0.8fr_1.2fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Selected detail</p><h2 className="mt-2 text-2xl font-black">{selected ? `${selected.asset} · ${selected.condition}` : "No alert selected"}</h2>{selected && <div className="mt-5 grid gap-3 sm:grid-cols-2">{[{ label: "Threshold", value: selected.threshold }, { label: "Source", value: selected.source }, { label: "Delivery", value: selected.channel }, { label: "Trigger", value: "Not evaluated" }, { label: "Current price", value: "Unavailable" }, { label: "Last checked", value: "Never" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div>}</CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Production gates</p><h2 className="mt-2 text-2xl font-black">What a real alert must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Authenticated owner and permission", "Trusted price source and freshness", "Market, currency, timezone, and threshold rules", "Trigger evaluator with idempotency and retries", "Email/push/in-app delivery and consent", "Audit log, pause/delete, and outage handling"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><Check className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Alert surface preserved", description: "Asset, condition, threshold, channel, search, list, selected detail, local add/save/reset interactions remain available.", icon: Bell, status: "Local builder" }, { title: "No fake market data", description: "Current price, movement, trigger, portfolio, balance, transaction, and market statistics remain unavailable.", icon: TrendingUp, status: "Source off" }, { title: "Delivery is protected", description: "No notification provider, webhook, email, push, account, or evaluator is contacted from this preview.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
