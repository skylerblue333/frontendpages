import { useState } from "react";
import { Activity, BarChart3, Check, Download, Filter, Info, LockKeyhole, RefreshCw, ShieldAlert, SlidersHorizontal, TrendingUp, Users, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";
const metrics = [{ label: "Activity", value: "Unmeasured", note: "No event source" }, { label: "Users", value: "Unknown", note: "No identity source" }, { label: "Retention", value: "Unavailable", note: "No cohort source" }, { label: "Outcome", value: "Unverified", note: "No attribution source" }];
export default function StatisticsPanel() { const [range, setRange] = useState("Range not configured"); const [segment, setSegment] = useState("All segments"); const [saved, setSaved] = useState(false); const [showBoundary, setShowBoundary] = useState(false); const [refreshing, setRefreshing] = useState(false); const refresh = () => { setRefreshing(true); window.setTimeout(() => setRefreshing(false), 500); }; const reset = () => { setRange("Range not configured"); setSegment("All segments"); setSaved(false); setShowBoundary(false); }; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={BarChart3} eyebrow="Statistics panel · Analytics preview" title="Inspect metric definitions before trusting a number." description="Review a local analytics workspace for activity, users, retention, segmentation, trends, comparisons, export intent, refresh, save/reset, and evidence boundaries. No live telemetry, identity, market, wallet, AI, health, or business-outcome data is connected." badge="Evidence-bounded statistics"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save statistics view"}</Button><Button onClick={() => setShowBoundary((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showBoundary ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showBoundary ? "Close evidence gates" : "Review evidence gates"}</Button><Button onClick={refresh} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className={`mr-2 size-4 ${refreshing ? "animate-spin" : ""}`} />{refreshing ? "Retrying local state" : "Retry statistics"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><X className="mr-2 size-4" />Reset panel</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={metrics.map(({ label, value, note }, index) => ({ label, value, hint: note, icon: [Activity, Users, TrendingUp, ShieldAlert][index], tone: index === 1 ? "violet" : index === 2 ? "amber" : index === 3 ? "slate" : "cyan" }))} /><ScreenPreviewBanner title="Statistics evidence boundary"><strong>This is a local metric-design preview, not evidence that users, events, activity, retention, revenue, market state, wallet state, AI outcomes, health outcomes, or business outcomes exist.</strong> Metric cards, filter intent, local range and segment controls, visual scaffolding, saved state, retry feedback, and disabled live-query/export actions are browser concepts. No telemetry, identity, population, cohort, transaction, price, attribution, diagnosis, or business result is asserted.</ScreenPreviewBanner><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="grid gap-4 md:grid-cols-2"><label className="text-sm font-semibold text-slate-300"><span className="flex items-center gap-2"><Filter className="size-4 text-slate-500" />Time range</span><select value={range} onChange={(event) => setRange(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white outline-none"><option>Range not configured</option><option>Last 7 days intent</option><option>Last 30 days intent</option><option>Quarter-to-date intent</option></select></label><label className="text-sm font-semibold text-slate-300"><span className="flex items-center gap-2"><SlidersHorizontal className="size-4 text-slate-500" />Segment</span><select value={segment} onChange={(event) => setSegment(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white outline-none"><option>All segments</option><option>New users intent</option><option>Returning users intent</option><option>Test cohort intent</option></select></label></div></CardContent></Card><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200">Trend scaffold</p><h2 className="mt-2 text-2xl font-black">No live series loaded</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Unmeasured</Badge></div><div className="mt-7 flex h-40 items-end gap-3">{[34, 52, 41, 68, 46, 58, 30, 44, 37, 62, 49, 55].map((height, index) => <div key={index} className="flex-1 rounded-t-lg bg-gradient-to-t from-cyan-400/30 to-violet-400/70" style={{ height: `${height}%` }} />)}</div><p className="mt-5 text-sm leading-6 text-slate-500">Bars are visual scaffolding, not a time series, count, rate, trend, forecast, baseline, comparison, or business result.</p></CardContent></Card><ScreenStatePanel type="empty" title="No statistics source connected" description="Connect an authenticated, privacy-reviewed event or data source before rendering counts, cohorts, rates, trends, market data, wallet data, AI metrics, health metrics, or business outcomes." /></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><Info className="size-5 text-cyan-300" /><div><p className="font-bold">Metric contract checklist</p><p className="mt-1 text-sm text-slate-400">Each metric needs a source, definition, population, time window, sampling method, privacy basis, freshness, error handling, access policy, and reproducible query.</p></div></div><div className="mt-5 grid gap-3 md:grid-cols-2">{["Event and identity provenance", "Metric definition and denominator", "Cohort, segment, and time-window rules", "Privacy, consent, retention, and access controls", "Freshness, quality, anomaly, and reconciliation checks", "Export, audit, deletion, and accountable review"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3 text-sm text-slate-300"><Check className="size-4 text-amber-300" />{item}<span className="ml-auto text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><div className="flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Load live statistics unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500"><Download className="mr-2 size-4" />Export unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Compare unavailable</Button></div>{showBoundary && <Card className="border-amber-300/20 bg-amber-300/[0.06]"><CardContent className="p-6"><p className="font-semibold text-amber-100">No user, market, AI, health, or business claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local statistics panel does not prove a population, count, trend, retention rate, market state, wallet state, AI outcome, diagnosis, conversion, revenue, or business result.</p></CardContent></Card>}<ScreenFeatureGrid features={[{ title: "Statistics surface preserved", description: "Activity, users, retention, outcomes, ranges, segments, trends, comparisons, export intent, refresh, save/reset, and metric contracts remain visible.", icon: BarChart3, status: "Local analytics" }, { title: "No number theater", description: "Counts, rates, trends, forecasts, market values, user outcomes, and business results are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before reporting", description: "Real statistics require provenance, definitions, privacy, freshness, quality, reconciliation, access, and audit.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>; }
