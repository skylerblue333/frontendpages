import { useMemo, useState } from "react";
import { Archive, CheckCircle2, Clock3, Database, FileText, FolderArchive, LockKeyhole, RotateCcw, Search, Shield, Trash2, Undo2 } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

type Item = { id: string; name: string; kind: string; status: "Archive preview" | "Restore preview" | "Retention review"; description: string };
const ITEMS: Item[] = [
  { id: "report", name: "Usage report draft", kind: "Document", status: "Archive preview", description: "A local report example that should retain source, period, owner, and sensitivity metadata." },
  { id: "course", name: "Completed course export", kind: "Education", status: "Retention review", description: "A record example requiring learner consent, certification provenance, and deletion rules." },
  { id: "media", name: "Creator media package", kind: "Media", status: "Restore preview", description: "A media archive example requiring object versioning, access policy, and integrity checks." },
  { id: "audit", name: "Audit event bundle", kind: "Security", status: "Retention review", description: "A security record example that must not be deleted outside an approved retention policy." },
];
const REQUIREMENTS = ["Owner and classification", "Retention start and end", "Restore permissions", "Integrity and version", "Deletion approval", "Audit event"];

export default function ArchiveManagement() { const [query, setQuery] = useState(""); const [items, setItems] = useState(ITEMS); const [selected, setSelected] = useState<Item | null>(null); const [preview, setPreview] = useState<"archive" | "restore" | null>(null); const visible = useMemo(() => items.filter((item) => `${item.name} ${item.kind} ${item.status} ${item.description}`.toLowerCase().includes(query.toLowerCase())), [items, query]); const addDraft = () => { const draft = { id: `draft-${items.length + 1}`, name: `Archive draft ${items.length + 1}`, kind: "Draft", status: "Archive preview" as const, description: "Local archive placeholder; no file or record has been stored." }; setItems((current) => [draft, ...current]); setSelected(draft); setPreview("archive"); }; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Archive} eyebrow="Data Lifecycle · Archives" title="Keep retention, restore, and deletion explainable." description="Explore archive item templates, retention requirements, local archive/restore previews, and access controls. This page does not claim stored records, file counts, storage capacity, successful restores, or completed deletion." badge="Preview archive workspace"><div className="flex flex-wrap gap-2"><Button onClick={addDraft} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><FolderArchive className="mr-2 size-4" />Add local archive draft</Button><Button variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><Shield className="mr-2 size-4" />Retention policy</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Archive examples", value: String(items.length), hint: "Local lifecycle records", icon: Archive }, { label: "Stored data", value: "Unavailable", hint: "No archive store connected", icon: Database, tone: "amber" }, { label: "Retention", value: "Required", hint: "Owner and policy needed", icon: Clock3, tone: "violet" }, { label: "Deletion", value: "Gated", hint: "Approval and audit required", icon: LockKeyhole, tone: "slate" }]} /><ScreenPreviewBanner title="Archive evidence boundary">Archive examples, search, local archive/restore preview states, retention requirements, restore permissions, integrity checks, and deletion gates are available for UX review. No object, record, file count, storage size, restore result, deletion result, or retention period is fabricated.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.85fr_1.15fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="relative"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search archive examples" aria-label="Search archive examples" className="border-white/10 bg-black/20 pl-9 text-white placeholder:text-slate-500" /></div><div className="mt-4 space-y-2">{visible.map((item) => <button key={item.id} onClick={() => { setSelected(item); setPreview(null); }} className={`w-full rounded-xl border p-4 text-left transition ${selected?.id === item.id ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-black/15 hover:bg-white/[0.05]"}`}><div className="flex items-center gap-2"><Archive className="size-4 text-cyan-300" /><span className="font-semibold">{item.name}</span><Badge variant="outline" className="ml-auto border-amber-300/20 text-amber-200">{item.status}</Badge></div><p className="mt-2 text-xs uppercase tracking-wider text-slate-500">{item.kind}</p><p className="mt-2 text-sm leading-6 text-slate-400">{item.description}</p></button>)}{visible.length === 0 && <ScreenStatePanel type="empty" title="No archive items match" description="Try another name, kind, or status." />}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6">{selected ? <><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Selected archive item</p><h2 className="mt-1 text-2xl font-bold">{selected.name}</h2><p className="mt-2 text-sm text-slate-400">{selected.kind} · {selected.status}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Preview</Badge></div><p className="mt-5 text-sm leading-6 text-slate-300">{selected.description}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{REQUIREMENTS.map((item) => <div key={item} className="rounded-lg border border-white/10 bg-black/15 p-4"><CheckCircle2 className="size-4 text-emerald-300" /><p className="mt-3 text-sm text-slate-300">{item}</p><p className="mt-1 text-xs text-slate-500">Evidence required</p></div>)}</div><div className="mt-6 flex flex-wrap gap-2"><Button size="sm" onClick={() => setPreview("archive")} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Archive className="mr-2 size-4" />Preview archive</Button><Button size="sm" variant="outline" onClick={() => setPreview("restore")} className="border-white/15 text-slate-300"><RotateCcw className="mr-2 size-4" />Preview restore</Button><Button size="sm" variant="outline" disabled className="border-white/10 text-slate-500"><Trash2 className="mr-2 size-4" />Deletion gated</Button></div>{preview && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex items-center gap-2 font-medium text-amber-200">{preview === "archive" ? <Archive className="size-4" /> : <Undo2 className="size-4" />}{preview === "archive" ? "Archive" : "Restore"} preview prepared</div><p className="mt-2 text-sm leading-6 text-slate-300">No record or file was moved. Connect a verified storage service and authorization policy before performing this action.</p></div>}</> : <ScreenStatePanel type="empty" title="Select an archive example" description="Inspect ownership, retention, restore, integrity, deletion, and audit requirements." />}</CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Retention is explicit", description: "Every archive needs classification, owner, legal or product basis, start date, end date, and deletion behavior.", icon: Clock3, status: "Required" }, { title: "Restore is authorized", description: "Verify identity, permissions, integrity, version, and audit trail before making data available again.", icon: Shield, status: "Required" }, { title: "No fake storage", description: "A preview item is not a stored record, successful restore, object count, or deletion result.", icon: AlertTriangleIcon, status: "Guardrail" }]} /></main></div>; }
function AlertTriangleIcon() { return <span aria-hidden="true">!</span>; }
