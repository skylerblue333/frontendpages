import { useMemo, useState } from "react";
import { Check, FileKey2, Filter, KeyRound, LockKeyhole, RefreshCw, Shield, ShieldAlert, Siren, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const controls = [{ id: 1, name: "Authentication posture", category: "Identity", detail: "A local control concept covering sessions, MFA, recovery, device trust, rate limits, authorization, and sensitive-action reauthentication.", state: "Evidence needed" }, { id: 2, name: "Data protection", category: "Privacy", detail: "A data-protection concept covering encryption boundaries, key management, minimization, retention, deletion, access, and incident response.", state: "Unconfigured" }, { id: 3, name: "API and edge controls", category: "Infrastructure", detail: "An edge-control concept covering scoped API keys, secrets, WAF, ingress, egress, rate limits, SSRF, uploads, and dependency posture.", state: "Preview" }, { id: 4, name: "Vulnerability response", category: "Operations", detail: "A disclosure concept covering reporting channel, triage, severity, remediation, ownership, communication, and any reward funding.", state: "Blocked" }];
export default function Security() { const [query, setQuery] = useState(""); const [category, setCategory] = useState("All"); const [selected, setSelected] = useState(1); const [evidence, setEvidence] = useState("Evidence state not configured"); const [review, setReview] = useState("Review cadence not configured"); const [saved, setSaved] = useState(false); const [showGates, setShowGates] = useState(false); const categories = ["All", ...Array.from(new Set(controls.map((item) => item.category)))]; const filtered = useMemo(() => controls.filter((item) => (category === "All" || item.category === category) && `${item.name} ${item.category} ${item.detail}`.toLowerCase().includes(query.toLowerCase())), [category, query]); const control = controls.find((item) => item.id === selected) ?? controls[0]; const reset = () => { setQuery(""); setCategory("All"); setSelected(1); setEvidence("Evidence state not configured"); setReview("Review cadence not configured"); setSaved(false); setShowGates(false); }; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Shield} eyebrow="Security center · Control preview" title="Show the control evidence before making the security claim." description="Explore local identity, privacy, infrastructure, and vulnerability-response control concepts with search, category filters, evidence and review intent, save/reset, audit gates, and blocked security actions. No encryption, MFA, WAF, API key, audit log, threat count, uptime, bug bounty, certification, compliance, or incident outcome is connected." badge="Evidence-bounded security workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save control locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review security gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset controls</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Control concepts", value: `${controls.length} local`, hint: "No posture source", icon: Shield, tone: "cyan" }, { label: "Identity", value: "Unconfigured", hint: "No auth evidence", icon: KeyRound, tone: "violet" }, { label: "Data protection", value: "Evidence needed", hint: "No key source", icon: LockKeyhole, tone: "amber" }, { label: "Incidents", value: "Unavailable", hint: "No operations source", icon: Siren, tone: "slate" }]} /><ScreenPreviewBanner title="Security evidence boundary"><strong>This is a local control-design preview, not evidence that the platform is encrypted, MFA-protected, WAF-protected, monitored, certified, compliant, available, or incident-free.</strong> Control cards, filters, evidence/review intent, saved state, audit gates, and disabled protection/reporting actions are browser concepts. No security control, threat count, uptime, incident status, vulnerability reward, certification, compliance posture, or user safety outcome is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Filter className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local security controls" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-4 flex flex-wrap gap-2">{categories.map((entry) => <Button key={entry} onClick={() => setCategory(entry)} size="sm" variant="outline" className={category === entry ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-100" : "border-white/10 text-slate-400"}>{entry}</Button>)}</div><div className="mt-6 space-y-3">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{item.name}</p><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.state}</Badge></div><div className="mt-4"><Badge variant="outline" className="border-white/10 text-slate-500">{item.category}</Badge></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected control concept</p><h2 className="mt-2 text-2xl font-black">{control.name}</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{control.state}</Badge></div><p className="mt-2 text-sm leading-6 text-slate-400">{control.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Category", value: control.category }, { label: "Evidence", value: evidence }, { label: "Review", value: review }, { label: "Owner", value: "Unavailable" }, { label: "Severity", value: "Not assessed" }, { label: "Certification", value: "Not claimed" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2"><label className="text-sm text-slate-400">Evidence state<select value={evidence} onChange={(event) => setEvidence(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Evidence state not configured</option><option>Design evidence intent</option><option>Implementation evidence intent</option><option>Test evidence intent</option><option>Independent review intent</option></select></label><label className="text-sm text-slate-400">Review cadence<select value={review} onChange={(event) => setReview(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Review cadence not configured</option><option>Per-release intent</option><option>Monthly intent</option><option>Quarterly intent</option><option>Incident-triggered intent</option></select></label></div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-8 text-center"><Shield className="mx-auto size-8 text-slate-600" /><p className="mt-3 font-semibold">No security evidence loaded</p><p className="mt-2 text-sm text-slate-500">Connect verified control implementation, provider configuration, secrets handling, logs, monitoring, tests, incident response, disclosure ownership, and independent acceptance before making posture claims.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Run audit unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500"><FileKey2 className="mr-2 size-4" />Rotate key unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Report vulnerability unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Claim certification unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No security or compliance claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A control concept does not prove encryption, MFA, WAF, audit logging, uptime, threat counts, vulnerability handling, certification, compliance, or user safety.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Security-governance gates</p><h2 className="mt-2 text-2xl font-black">What a real security center must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Authenticated control, owner, environment, asset, scope, version, timestamp, evidence source, and review provenance", "Sessions, MFA, recovery, authorization, IDOR prevention, rate limits, CSRF, XSS, SSRF, uploads, secrets, and dependency controls", "Encryption, key management, API keys, WAF, ingress/egress, logging, monitoring, alerting, backups, recovery, and incident response", "Vulnerability reporting, triage, severity, remediation, communication, responsible disclosure, reward funding, and legal ownership", "SOC 2, ISO, PCI, HIPAA, GDPR, security, privacy, uptime, threat, and user-impact claims require independent domain review", "Audit, rotate, report, remediate, notify, export, certify, accessibility, and accountable approval require governed security operations"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Security surface preserved", description: "Identity, privacy, infrastructure, vulnerability response, evidence, review, audit, key rotation, disclosure, certification, save/reset, and gates remain interactive.", icon: Shield, status: "Local controls" }, { title: "No security theater", description: "Encryption, MFA, WAF, logs, uptime, threats, vulnerabilities, certification, compliance, rewards, and safety outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before trust", description: "Real security posture requires implemented controls, provider configuration, tests, monitoring, incident response, disclosure ownership, and independent acceptance.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>; }
