import { useMemo, useState } from "react";
import { CheckCircle2, Heart, LockKeyhole, MessageCircle, RefreshCw, Search, Shield, Sparkles, Star, Users } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

type Profile = { id: string; name: string; label: string; interests: string[]; note: string };
const PROFILES: Profile[] = [
  { id: "atlas", name: "Example profile A", label: "Shared interests", interests: ["Technology", "Travel", "Creative work"], note: "A sample profile used to demonstrate explainable matching states." },
  { id: "nova", name: "Example profile B", label: "Conversation style", interests: ["Learning", "Design", "Community"], note: "A sample profile used to demonstrate respectful conversation prompts." },
  { id: "orbit", name: "Example profile C", label: "Values prompts", interests: ["Growth", "Web3", "Wellbeing"], note: "A sample profile used to demonstrate consent-first discovery." },
];
const DIMENSIONS = ["Shared interests", "Conversation preferences", "Values prompts", "Availability", "Connection goals", "Privacy preferences"];
const SUGGESTIONS = ["Ask an open question about a shared interest.", "Share your preferred conversation pace before moving off-platform.", "Respect a no or a pause without trying to persuade.", "Keep sensitive personal details out of an early conversation."];

export default function AIMatchmaker() { const [selected, setSelected] = useState(PROFILES[0]); const [query, setQuery] = useState(""); const [prepared, setPrepared] = useState(false); const [refreshing, setRefreshing] = useState(false); const visible = useMemo(() => PROFILES.filter((profile) => `${profile.name} ${profile.interests.join(" ")} ${profile.label}`.toLowerCase().includes(query.toLowerCase())), [query]); const recheck = () => { setRefreshing(true); setPrepared(false); window.setTimeout(() => setRefreshing(false), 500); }; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Heart} eyebrow="AI & Automation · Matchmaker" title="Find connection without pretending to predict people." description="Explore an explainable compatibility workflow using example profiles and consent-first prompts. The preview does not infer personality, predict behavior, expose private data, or claim a relationship outcome." badge="Preview matchmaker"><div className="flex flex-wrap gap-2"><Button onClick={() => document.getElementById("matchmaker")?.scrollIntoView({ behavior: "smooth" })} className="bg-pink-300 text-slate-950 hover:bg-pink-200"><Heart className="mr-2 size-4" />Explore matches</Button><Button onClick={recheck} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className={`mr-2 size-4 ${refreshing ? "animate-spin" : ""}`} />{refreshing ? "Reviewing" : "Recheck boundary"}</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Example profiles", value: String(PROFILES.length), hint: "Synthetic preview records", icon: Users }, { label: "Match dimensions", value: String(DIMENSIONS.length), hint: "Explainable, user-controlled inputs", icon: Sparkles, tone: "violet" }, { label: "Predictions", value: "Disabled", hint: "No behavior or outcome claims", icon: LockKeyhole, tone: "amber" }, { label: "Consent", value: "Required", hint: "Respect privacy and boundaries", icon: Shield, tone: "slate" }]} /><ScreenPreviewBanner title="Human and privacy boundary">The match selector, search, example dimensions, conversation suggestions, recheck state, and consent guardrails are available for UX review. The screen does not fabricate compatibility percentages, confidence scores, ages, identities, personality profiles, private attributes, or relationship predictions.</ScreenPreviewBanner><section id="matchmaker" className="grid gap-6 lg:grid-cols-[0.75fr_1.25fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-pink-300">Discover</p><h2 className="mt-1 text-xl font-bold">Example matches</h2></div><Badge variant="outline" className="border-pink-300/20 text-pink-200">Synthetic preview</Badge></div><div className="relative mt-4"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search interests or profile labels" aria-label="Search match examples" className="border-white/10 bg-black/20 pl-9 text-white placeholder:text-slate-500" /></div><div className="mt-4 space-y-2">{visible.map((profile) => <button key={profile.id} onClick={() => { setSelected(profile); setPrepared(false); }} className={`w-full rounded-xl border p-4 text-left transition ${selected.id === profile.id ? "border-pink-300/40 bg-pink-300/[0.08]" : "border-white/10 bg-black/15 hover:bg-white/[0.05]"}`}><div className="flex items-start gap-3"><div className="flex size-10 items-center justify-center rounded-full bg-gradient-to-br from-pink-300/30 to-violet-300/30 text-pink-100"><Users className="size-4" /></div><div className="flex-1"><h3 className="font-medium">{profile.name}</h3><p className="mt-1 text-xs text-slate-400">{profile.label}</p><div className="mt-2 flex flex-wrap gap-1.5">{profile.interests.map((interest) => <Badge key={interest} variant="outline" className="border-white/10 text-slate-400">{interest}</Badge>)}</div></div></div></button>)}{visible.length === 0 && <ScreenStatePanel type="empty" title="No examples match" description="Try a different interest or label." />}</div></CardContent></Card><div className="space-y-6"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-pink-300">Explainable view</p><h2 className="mt-1 text-2xl font-bold">{selected.name}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{selected.note}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">No score</Badge></div><div className="mt-5 grid gap-3 sm:grid-cols-2">{DIMENSIONS.map((dimension) => <div key={dimension} className="rounded-xl border border-white/10 bg-black/15 p-4"><div className="flex items-center gap-2 text-sm font-medium"><CheckCircle2 className="size-4 text-pink-300" />{dimension}</div><p className="mt-2 text-xs leading-5 text-slate-500">User-provided context can be reviewed here; no inferred compatibility value is shown.</p></div>)}</div><div className="mt-5 flex flex-wrap gap-3"><Button onClick={() => setPrepared(true)} className="bg-pink-300 text-slate-950 hover:bg-pink-200"><Sparkles className="mr-2 size-4" />Prepare conversation prompt</Button><Button variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><MessageCircle className="mr-2 size-4" />Message boundary</Button></div>{prepared && <div className="mt-5 rounded-xl border border-emerald-300/20 bg-emerald-300/[0.06] p-4" aria-live="polite"><div className="flex items-center gap-2 font-medium text-emerald-200"><CheckCircle2 className="size-4" />Prompt prepared for review</div><p className="mt-2 text-sm leading-6 text-slate-300">“What is something you’ve enjoyed learning recently?” is a safe example opener. Sending messages, revealing identity, and matching real users require authenticated, consent-aware services.</p></div>}</CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-300">Conversation guide</p><h2 className="mt-1 text-xl font-bold">Respectful suggestions</h2><div className="mt-4 space-y-3">{SUGGESTIONS.map((suggestion) => <div key={suggestion} className="flex items-start gap-3 text-sm text-slate-300"><Star className="mt-0.5 size-4 shrink-0 text-violet-300" />{suggestion}</div>)}</div></CardContent></Card></div></section><ScreenFeatureGrid features={[{ title: "Explainable inputs", description: "Make matching dimensions visible and user-controlled instead of opaque or inferred.", icon: Sparkles, status: "Available" }, { title: "Consent first", description: "Require permission before messaging, sharing data, or moving a conversation off-platform.", icon: Shield, status: "Required" }, { title: "No behavior prediction", description: "Do not claim to predict conflict, retention, attraction, or relationship outcomes.", icon: LockKeyhole, status: "Guardrail" }]} /></main></div>; }
