import { useMemo, useState } from "react";
import { BadgeCheck, Check, CreditCard, FileCheck2, LockKeyhole, MapPin, Package, RefreshCw, ShieldAlert, ShoppingBag, Truck, WalletCards, WifiOff } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

type Item = { id: string; name: string; description: string; price: number; qty: number };
const INITIAL: Item[] = [{ id: "item-1", name: "Preview creator toolkit", description: "Synthetic catalog item for checkout-state review.", price: 48, qty: 1 }, { id: "item-2", name: "Preview learning pass", description: "Synthetic entitlement; access and fulfillment are unavailable.", price: 24, qty: 2 }];
const STEPS = ["Review", "Delivery", "Payment", "Confirmation"];
export default function Checkout() { const [items, setItems] = useState(INITIAL); const [step, setStep] = useState("Review"); const [destination, setDestination] = useState(""); const [saved, setSaved] = useState(false); const subtotal = useMemo(() => items.reduce((sum, item) => sum + item.price * item.qty, 0), [items]); const updateQty = (id: string, delta: number) => { setItems((current) => current.map((item) => item.id === id ? { ...item, qty: Math.max(0, item.qty + delta) } : item).filter((item) => item.qty > 0)); setSaved(false); }; const reset = () => { setItems(INITIAL); setStep("Review"); setDestination(""); setSaved(false); }; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={ShoppingBag} eyebrow="Commerce · Checkout Review" title="Review the order before anyone charges or ships it." description="Explore a synthetic cart, local quantity arithmetic, delivery details, and payment gates. This page does not reserve inventory, calculate tax, authorize payment, create an order, or promise fulfillment." badge="Preview checkout workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><FileCheck2 className="mr-2 size-4" />Save local order plan</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset checkout</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Items", value: String(items.length), hint: "Synthetic catalog", icon: ShoppingBag }, { label: "Subtotal", value: `$${subtotal.toFixed(2)}`, hint: "Local arithmetic", icon: WalletCards, tone: "violet" }, { label: "Payment", value: "Blocked", hint: "No processor", icon: CreditCard, tone: "amber" }, { label: "Fulfillment", value: "Unavailable", hint: "No inventory source", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="Checkout evidence boundary">A subtotal is not a charge, tax total, order, reservation, shipment, or refund. A production checkout needs verified catalog and inventory, price and currency rules, tax jurisdiction, shipping terms, authenticated buyer, payment tokenization, fraud controls, idempotency, order state, receipts, fulfillment, cancellation, and refund handling.</ScreenPreviewBanner><section className="flex flex-wrap gap-2">{STEPS.map((item) => <button key={item} onClick={() => setStep(item)} className={`rounded-xl border px-4 py-2 text-sm ${step === item ? "border-cyan-300/40 bg-cyan-300/[0.1] text-cyan-200" : "border-white/10 text-slate-400"}`}>{item}</button>)}</section><section className="grid gap-6 lg:grid-cols-[1.05fr_0.95fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="flex items-center justify-between"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">{step} workspace</p><Badge variant="outline" className="border-amber-300/20 text-amber-200">Preview only</Badge></div>{step === "Review" && <div className="mt-5 space-y-3">{items.map((item) => <div key={item.id} className="rounded-xl border border-white/10 bg-black/15 p-4"><div className="flex items-start gap-3"><ShoppingBag className="mt-1 size-5 text-cyan-300" /><div className="flex-1"><p className="font-semibold">{item.name}</p><p className="mt-1 text-xs text-slate-500">{item.description}</p></div><span className="font-mono text-sm text-slate-300">${(item.price * item.qty).toFixed(2)}</span></div><div className="mt-4 flex items-center justify-between text-sm text-slate-400"><span>${item.price.toFixed(2)} each · quantity {item.qty}</span><div className="flex gap-2"><Button onClick={() => updateQty(item.id, -1)} size="sm" variant="outline" className="border-white/15 text-white">−</Button><Button onClick={() => updateQty(item.id, 1)} size="sm" variant="outline" className="border-white/15 text-white">+</Button></div></div></div>)}{items.length === 0 && <p className="py-8 text-center text-sm text-slate-500">Cart is empty in this local preview.</p>}</div>}{step === "Delivery" && <div className="mt-6 space-y-4"><div><label htmlFor="destination" className="mb-2 block text-sm text-slate-300">Destination label</label><Input id="destination" value={destination} onChange={(event) => { setDestination(event.target.value); setSaved(false); }} placeholder="Local delivery destination" className="border-white/10 bg-black/20 text-white placeholder:text-slate-500" /></div>{["Inventory reservation", "Shipping method and rate", "Delivery estimate", "Address validation", "Fulfillment partner"].map((item) => <div key={item} className="flex items-center gap-3 rounded-lg border border-white/10 bg-black/15 p-3"><Truck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Unavailable</span></div>)}</div>}{step === "Payment" && <div className="mt-6 space-y-3"><div className="rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex items-center gap-2 font-semibold text-amber-200"><LockKeyhole className="size-4" />No payment processor connected</div><p className="mt-2 text-sm text-slate-400">No card, wallet, bank, billing address, fraud check, authorization, or charge attempt is available in this preview.</p></div>{["Currency and final price", "Tax jurisdiction", "Payment tokenization", "Fraud and risk checks", "Idempotency key", "Authorization and receipt"].map((item) => <div key={item} className="flex items-center gap-3 rounded-lg border border-white/10 bg-black/15 p-3"><CreditCard className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div>}{step === "Confirmation" && <div className="mt-6 space-y-3"><div className="rounded-xl border border-cyan-300/20 bg-cyan-300/[0.06] p-4"><div className="flex items-center gap-2 font-semibold text-cyan-200"><BadgeCheck className="size-4" />Confirmation preview</div><p className="mt-2 text-sm text-slate-400">This is a local review checkpoint only. No order number, payment authorization, inventory hold, shipment, or receipt exists.</p></div>{["Order state machine", "Customer receipt", "Fulfillment handoff", "Cancellation and refund", "Support contact"].map((item) => <div key={item} className="flex items-center gap-3 rounded-lg border border-white/10 bg-black/15 p-3"><ShieldAlert className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Unavailable</span></div>)}</div>}{saved && <div className="mt-6 flex items-center gap-2 rounded-xl border border-cyan-300/20 bg-cyan-300/[0.06] p-4 text-sm text-cyan-200"><Check className="size-4" />Local order plan saved; no charge or order was created.</div>}</CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Order summary</p><h2 className="mt-2 text-3xl font-black">${subtotal.toFixed(2)}</h2><p className="mt-1 text-sm text-slate-500">Illustrative subtotal · tax and shipping unavailable</p><div className="mt-6 space-y-2">{["Catalog and stock verified", "Price and currency verified", "Destination and tax verified", "Payment authorized", "Order and fulfillment created"].map((item) => <div key={item} className="flex items-center gap-3 rounded-lg border border-white/10 bg-black/15 p-3"><ShieldAlert className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Pending</span></div>)}<div className="mt-5 rounded-xl border border-white/10 p-4"><div className="flex items-center gap-2 text-sm font-semibold text-slate-200"><MapPin className="size-4 text-cyan-300" />Destination</div><p className="mt-2 text-sm text-slate-400">{destination || "Not provided"}</p></div></div></CardContent></Card></section><section className="grid gap-4 md:grid-cols-3"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><ShoppingBag className="size-5 text-cyan-300" /><h3 className="mt-3 font-semibold">Review is not order</h3><p className="mt-2 text-sm leading-6 text-slate-400">A cart and subtotal do not reserve stock, charge a customer, or create fulfillment.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><LockKeyhole className="size-5 text-violet-300" /><h3 className="mt-3 font-semibold">Payment is security</h3><p className="mt-2 text-sm leading-6 text-slate-400">Tokenization, fraud, authorization, idempotency, receipts, and refunds require provider evidence.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><WifiOff className="size-5 text-amber-300" /><h3 className="mt-3 font-semibold">No fake commerce</h3><p className="mt-2 text-sm leading-6 text-slate-400">Synthetic prices and quantities are not inventory, payment, shipment, or delivery claims.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Steps have contracts", description: "Keep review, delivery, payment, and confirmation states separate and verifiable.", icon: Package, status: "Pattern" }, { title: "Payment before success", description: "Never show a success state without provider authorization, order identity, and receipt evidence.", icon: CreditCard, status: "Required" }, { title: "No fake order", description: "A local plan is not a charge, reservation, shipment, fulfillment, or refund.", icon: WifiOff, status: "Guardrail" }]} /></main></div>; }
