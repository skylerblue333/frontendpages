import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Activity, Check, Film, LockKeyhole, Play, Radio, RefreshCw, Search, ShieldAlert, Users, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const streams = [{ id: 1, name: "Community showcase", category: "Community", status: "Draft", detail: "A local live-stream concept awaiting encoder, audience, moderation, and delivery contracts." }, { id: 2, name: "SkySchool live class", category: "Education", status: "Needs review", detail: "A teaching-stream concept awaiting access policy, captions, recording, and learner safeguards." }, { id: 3, name: "Game event broadcast", category: "Gaming", status: "Blocked", detail: "A broadcast concept that must not imply live viewers, match state, sponsorship, or engagement." }, { id: 4, name: "Product briefing", category: "Operations", status: "Unconnected", detail: "An internal-stream concept awaiting audience identity, retention, and incident response." }];
export default function RealTimeStreaming() {
  const [query, setQuery] = useState("");
  const [category, setCategory] = useState("All");
  const [selected, setSelected] = useState(1);
  const [transport, setTransport] = useState("Transport not configured");
  const [access, setAccess] = useState("Audience not configured");
  const [saved, setSaved] = useState(false);
  const [showGates, setShowGates] = useState(false);
  const categories = ["All", ...Array.from(new Set(streams.map((item) => item.category)))];
  const filtered = useMemo(() => streams.filter((item) => (category === "All" || item.category === category) && `${item.name} ${item.category} ${item.detail}`.toLowerCase().includes(query.toLowerCase())), [category, query]);
  const stream = streams.find((item) => item.id === selected) ?? streams[0];
  const reset = () => { setQuery(""); setCategory("All"); setSelected(1); setTransport("Transport not configured"); setAccess("Audience not configured"); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Radio} eyebrow="RealTimeStreaming · Broadcast preview" title="Design the stream before calling it live." description="Explore local stream concepts with search, categories, transport, audience access, buffering, playback, captions, moderation, delivery, analytics, recovery, save, reset, and evidence gates. No live stream, viewers, bandwidth, latency, playback, or engagement is connected." badge="Streaming workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "View saved locally" : "Save design locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review streaming gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset design</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Streams", value: `${streams.length} local`, hint: "Not broadcasting", icon: Radio, tone: "cyan" }, { label: "Viewers", value: "Unavailable", hint: "No audience source", icon: Users, tone: "violet" }, { label: "Playback", value: "Unmeasured", hint: "No player source", icon: Play, tone: "amber" }, { label: "Delivery", value: "Unconnected", hint: "No CDN/transport", icon: Activity, tone: "slate" }]} /><ScreenPreviewBanner title="Streaming evidence boundary"><strong>This is a local broadcast-architecture preview, not a live streaming service.</strong> Stream labels, transport and audience intent, buffering posture, playback state, captions, moderation, delivery, saved state, and evidence gates are browser concepts. No live stream, viewer, bandwidth, latency, playback, recording, reach, engagement, or monetization outcome is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Search className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local stream drafts" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-4 flex flex-wrap gap-2">{categories.map((entry) => <Button key={entry} onClick={() => setCategory(entry)} size="sm" variant="outline" className={category === entry ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-100" : "border-white/10 text-slate-400"}>{entry}</Button>)}</div><div className="mt-6 space-y-3">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{item.name}</p><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.status}</Badge></div><div className="mt-4"><Badge variant="outline" className="border-white/10 text-slate-500">{item.category}</Badge></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected stream</p><h2 className="mt-2 text-2xl font-black">{stream.name}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{stream.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Status", value: stream.status }, { label: "Transport", value: transport }, { label: "Audience", value: access }, { label: "Viewers", value: "Unavailable" }, { label: "Latency", value: "Unmeasured" }, { label: "Recording", value: "Not configured" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2"><label className="text-sm text-slate-400">Transport<select value={transport} onChange={(event) => setTransport(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Transport not configured</option><option>WebRTC intent</option><option>Low-latency HLS intent</option><option>WebSocket intent</option></select></label><label className="text-sm text-slate-400">Audience access<select value={access} onChange={(event) => setAccess(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Audience not configured</option><option>Public intent</option><option>Authenticated intent</option><option>Invite-only intent</option></select></label></div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-8 text-center"><Film className="mx-auto size-7 text-slate-600" /><p className="mt-3 font-semibold">No stream session loaded</p><p className="mt-2 text-sm text-slate-500">Connect a governed encoder, transport, player, CDN, and audience service before showing live playback or reach.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Start stream unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Open player unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Moderate unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No live broadcast claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A stream draft does not encode, publish, deliver, play, record, reach viewers, or generate engagement.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Streaming gates</p><h2 className="mt-2 text-2xl font-black">What a real broadcast must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Encoder, ingest, codec, bitrate, keyframes, transport, origin, CDN, player, and playback compatibility", "Audience identity, access control, geography, privacy, consent, chat, moderation, and abuse response", "Latency, buffering, adaptive bitrate, reconnect, ordering, loss, synchronization, and device coverage", "Captions, transcripts, recording, accessibility, localization, content warnings, and retention", "Viewers, reach, watch time, reactions, engagement, ads, sponsorship, monetization, and attribution", "Operations, incident response, takedown, audit, export, support, scaling, cost, rollback, and disaster recovery"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Streaming surface preserved", description: "Stream drafts, search, categories, transport, audience, buffering, playback, captions, moderation, delivery, analytics, recovery, save/reset, and gates remain interactive.", icon: Radio, status: "Local streams" }, { title: "No broadcast theater", description: "Live streams, viewers, bandwidth, latency, playback, recording, reach, engagement, and monetization are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Delivery before live", description: "Real streaming needs encoding, transport, audience controls, player reliability, moderation, observability, and recovery.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
