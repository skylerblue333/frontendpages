import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Check, Clipboard, Copy, Filter, KeyRound, LockKeyhole, QrCode, RefreshCw, Search, ShieldAlert, Wallet, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const assets = [{ id: 1, name: "Skycoin", network: "Network not selected", state: "Address unavailable", detail: "A local receiving concept awaiting a connected wallet and verified network context." }, { id: 2, name: "Bitcoin", network: "Bitcoin intent", state: "Address unavailable", detail: "A receiving-flow concept requiring address validation, chain selection, and custody boundaries." }, { id: 3, name: "Ethereum", network: "Ethereum intent", state: "Address unavailable", detail: "A receiving-flow concept awaiting wallet ownership, token compatibility, and confirmation policy." }, { id: 4, name: "Stablecoin", network: "Network not selected", state: "Blocked", detail: "A high-risk asset concept where wrong-network deposits and unsupported tokens must be prevented." }];
export default function ReceiveCrypto() {
  const [query, setQuery] = useState(""); const [network, setNetwork] = useState("Network not selected"); const [selected, setSelected] = useState(1); const [copied, setCopied] = useState(false); const [saved, setSaved] = useState(false); const [showGates, setShowGates] = useState(false);
  const filtered = useMemo(() => assets.filter((item) => `${item.name} ${item.network} ${item.detail}`.toLowerCase().includes(query.toLowerCase())), [query]); const asset = assets.find((item) => item.id === selected) ?? assets[0];
  const reset = () => { setQuery(""); setNetwork("Network not selected"); setSelected(1); setCopied(false); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Wallet} eyebrow="ReceiveCrypto · Wallet preview" title="Verify the network before accepting an asset." description="Explore a local receiving workspace with asset search, network selection, address and QR intent, copy state, confirmations, transaction status, security, support, save, reset, and evidence gates. No wallet, address, balance, deposit, confirmation, or blockchain transaction is connected." badge="Receiving workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "View saved locally" : "Save view locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review receiving gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset view</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Assets", value: `${assets.length} local`, hint: "No wallet loaded", icon: Wallet, tone: "cyan" }, { label: "Address", value: "Unavailable", hint: "No custody source", icon: KeyRound, tone: "violet" }, { label: "Balance", value: "Unavailable", hint: "No chain source", icon: Clipboard, tone: "amber" }, { label: "Confirmations", value: "Unknown", hint: "No transaction source", icon: Check, tone: "slate" }]} /><ScreenPreviewBanner title="Receiving evidence boundary"><strong>This is a local wallet-receiving preview, not a deposit service.</strong> Asset labels, network intent, address and QR posture, copy state, confirmation state, saved state, security gates, and empty transaction state are browser concepts. No wallet, address, balance, deposit, confirmation, transaction hash, ownership, or blockchain finality is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Search className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local assets" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-6 space-y-3">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{item.name}</p><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.state}</Badge></div><p className="mt-3 text-xs text-slate-500">{item.network}</p></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected receiving asset</p><h2 className="mt-2 text-2xl font-black">{asset.name}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{asset.detail}</p><label className="mt-6 block text-sm text-slate-400">Network<select value={network} onChange={(event) => setNetwork(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Network not selected</option><option>Bitcoin mainnet intent</option><option>Ethereum mainnet intent</option><option>Skycoin network intent</option><option>Testnet intent</option></select></label><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Address", value: "Unavailable" }, { label: "Network", value: network }, { label: "Balance", value: "Unavailable" }, { label: "Last deposit", value: "Unknown" }, { label: "Confirmations", value: "Unknown" }, { label: "Transaction hash", value: "Unavailable" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-8 text-center"><QrCode className="mx-auto size-8 text-slate-600" /><p className="mt-3 font-semibold">No receiving address loaded</p><p className="mt-2 text-sm text-slate-500">Connect a governed wallet and verify the network before displaying or copying an address or QR payload.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400"><Copy className="mr-2 size-4" />Copy unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Show QR unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Refresh status unavailable</Button></div>{copied && <p className="mt-3 text-sm text-cyan-200">Copy state is local only; no address was copied.</p>}{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No deposit or custody claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A network selector does not create a wallet, reserve an address, accept funds, or prove confirmations.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Receiving gates</p><h2 className="mt-2 text-2xl font-black">What a real crypto receiving flow must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Wallet ownership, custody model, address derivation, seed protection, signing policy, and recovery", "Network and asset validation, token contract, decimals, memo/tag, wrong-network prevention, and testnet separation", "Address display, QR encoding, copy protection, malware substitution defenses, privacy, and support", "Chain listener, transaction hash, mempool state, block inclusion, confirmations, reorgs, and finality", "Balance source, indexing, token metadata, dust, fees, spam assets, and reconciliation", "Deposit status, notifications, audit, incident response, user messaging, and failed-transfer recovery"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Receiving surface preserved", description: "Assets, search, network selection, address and QR intent, copy state, confirmations, transaction status, security, support, save/reset, and gates remain interactive.", icon: Wallet, status: "Local wallet" }, { title: "No crypto theater", description: "Wallets, addresses, balances, deposits, confirmations, hashes, ownership, and blockchain finality are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Verification before funds", description: "Real receiving needs secure custody, network validation, address integrity, chain indexing, confirmations, reconciliation, and recovery.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
