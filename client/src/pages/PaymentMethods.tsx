import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Banknote, Check, CreditCard, LockKeyhole, Plus, RefreshCw, ShieldCheck, TriangleAlert, WalletCards, WifiOff, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

type MethodType = "card" | "wallet" | "bank";
type Method = { id: string; type: MethodType; name: string; detail: string; state: string; icon: typeof CreditCard };
const METHODS: Method[] = [
  { id: "card-preview", type: "card", name: "Card concept", detail: "•••• •••• •••• unavailable", state: "Not stored", icon: CreditCard },
  { id: "wallet-preview", type: "wallet", name: "Wallet concept", detail: "Address unavailable", state: "Not connected", icon: WalletCards },
  { id: "bank-preview", type: "bank", name: "Bank account concept", detail: "Account details unavailable", state: "Not linked", icon: Banknote },
];
export default function PaymentMethods() {
  const [selected, setSelected] = useState(METHODS[0]);
  const [added, setAdded] = useState<string[]>([]);
  const [acknowledged, setAcknowledged] = useState(false);
  const [saved, setSaved] = useState(false);
  const reset = () => { setSelected(METHODS[0]); setAdded([]); setAcknowledged(false); setSaved(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={CreditCard} eyebrow="PaymentMethods · Funding preview" title="Design funding choices without storing funding data." description="Explore card, wallet, and bank-account concepts with local add/remove intent. No PAN, token, account number, address, balance, provider token, payment method, or charge is stored or connected." badge="Payment method preview"><div className="flex flex-wrap gap-2"><Button disabled={!acknowledged} onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Plan saved locally" : "Save local plan"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset methods</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Methods", value: String(METHODS.length), hint: "Concepts only", icon: CreditCard, tone: "cyan" }, { label: "Local intents", value: String(added.length), hint: "Not saved remotely", icon: Plus, tone: "violet" }, { label: "Sensitive data", value: "Off", hint: "No collection", icon: LockKeyhole, tone: "amber" }, { label: "Charges", value: "Blocked", hint: "No provider", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="Payment-method evidence boundary"><strong>These are funding-method UX fixtures only.</strong> The preview does not collect, tokenize, vault, display, verify, delete, or charge a card, wallet, or bank account. It does not identify a payer, balance, provider, billing address, mandate, payment method, or transaction.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1fr_0.85fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Available concepts</p><h2 className="mt-2 text-2xl font-black">Choose a funding pattern</h2><div className="mt-5 space-y-3">{METHODS.map((method) => { const Icon = method.icon; return <button key={method.id} onClick={() => setSelected(method)} className={`w-full rounded-2xl border p-4 text-left ${selected.id === method.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10 hover:border-white/20"}`}><div className="flex items-center gap-3"><div className="flex size-10 items-center justify-center rounded-xl bg-cyan-300/10 text-cyan-200"><Icon className="size-5" /></div><div className="flex-1"><p className="font-bold">{method.name}</p><p className="mt-1 text-sm text-slate-500">{method.detail}</p></div><Badge variant="outline" className="border-white/10 text-amber-200">{method.state}</Badge></div></button>; })}</div><div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex gap-3"><TriangleAlert className="mt-0.5 size-5 shrink-0 text-amber-200" /><p className="text-sm leading-6 text-slate-300">Never paste a card number, security code, bank account, seed phrase, private key, or provider token into this page.</p></div></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected method</p><h2 className="mt-2 text-3xl font-black">{selected.name}</h2><p className="mt-3 text-slate-400">{selected.detail}</p><div className="mt-5 grid gap-3 sm:grid-cols-2">{[{ label: "Type", value: selected.type }, { label: "Status", value: selected.state }, { label: "Provider", value: "Not connected" }, { label: "Identifier", value: "Unavailable" }, { label: "Default", value: "Not selected" }, { label: "Charge path", value: "Blocked" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold capitalize text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 flex flex-wrap gap-2"><Button onClick={() => setAdded((items) => items.includes(selected.id) ? items.filter((id) => id !== selected.id) : [...items, selected.id])} variant="outline" className="border-cyan-300/30 bg-cyan-300/[0.06] text-cyan-100 hover:bg-cyan-300/10">{added.includes(selected.id) ? <><X className="mr-2 size-4" />Remove local intent</> : <><Plus className="mr-2 size-4" />Add local intent</>}</Button><Button disabled className="bg-cyan-300/20 text-cyan-100/40">Use for payment unavailable</Button></div><div className="mt-5 flex gap-3"><button aria-label="Acknowledge payment method boundary" onClick={() => setAcknowledged(!acknowledged)} className={`flex size-5 shrink-0 items-center justify-center rounded border ${acknowledged ? "border-cyan-300 bg-cyan-300 text-slate-950" : "border-white/20"}`}>{acknowledged ? "✓" : ""}</button><p className="text-sm leading-6 text-slate-400">I understand local method intents do not store or authorize a payment method.</p></div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Production checklist</p><h2 className="mt-2 text-2xl font-black">Funding methods must prove</h2><div className="mt-5 space-y-3">{["Hosted fields/tokenization or secure wallet handoff; never expose raw payment data to the app", "Provider/customer/mandate scope, consent, default selection, detach, and lifecycle semantics", "PCI/privacy scope, vault/key management, fraud controls, rate limits, and redaction", "Authorization, capture, refunds, disputes, failures, receipts, and reconciliation", "Audit events, support, retention, monitoring, and incident response"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><LockKeyhole className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake funding source</h2><p className="mt-3 text-sm leading-6 text-slate-400">Adding a local intent changes browser state only. It does not tokenize, vault, verify, debit, connect a wallet, or create a payment method.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Three patterns preserved", description: "Card, wallet, and bank-account concepts remain interactive without collecting secrets.", icon: CreditCard, status: "Local concepts" }, { title: "Sensitive data is off", description: "Identifiers and provider states remain unavailable instead of using fake masked data.", icon: LockKeyhole, status: "Security guardrail" }, { title: "Charges are blocked", description: "No method, token, account, payment, charge, or transaction success is fabricated.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
