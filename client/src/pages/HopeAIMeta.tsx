import HopeAIAdvanced from "@/pages/HopeAIAdvanced";

/**
 * Meta intentionally shares the validated AI-governance boundary until each
 * multimodal provider, upload, privacy, safety, and operations contract exists.
 * No AI provider is connected; this is not an AI conversation.
 */
export default function HopeAIMeta() {
  return <HopeAIAdvanced />;
}
