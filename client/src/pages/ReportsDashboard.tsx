import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { AlertTriangle, BarChart3, Check, Clock3, Download, Filter, LockKeyhole, RefreshCw, Search, ShieldAlert, UserRound, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const reports = [{ id: 1, name: "Safety-intake queue", category: "Safety", detail: "A local queue concept awaiting report provenance, policy taxonomy, severity, moderation ownership, and safe triage.", state: "Preview only" }, { id: 2, name: "Content-review queue", category: "Content", detail: "A content queue concept requiring evidence, rights context, reviewer assignment, appeals, and retention policy.", state: "Needs evidence" }, { id: 3, name: "Security-signal queue", category: "Security", detail: "A security operations concept that must not imply incidents, affected users, severity, or remediation.", state: "Unmeasured" }, { id: 4, name: "Abuse-control queue", category: "Abuse", detail: "An abuse-control concept requiring false-positive review, rate controls, escalation, and auditable outcomes.", state: "Blocked" }];
export default function ReportsDashboard() {
  const [query, setQuery] = useState(""); const [category, setCategory] = useState("All"); const [selected, setSelected] = useState(1); const [window, setWindow] = useState("Window not configured"); const [assignment, setAssignment] = useState("Owner not assigned"); const [saved, setSaved] = useState(false); const [showGates, setShowGates] = useState(false);
  const categories = ["All", ...Array.from(new Set(reports.map((item) => item.category)))]; const filtered = useMemo(() => reports.filter((item) => (category === "All" || item.category === category) && `${item.name} ${item.category} ${item.detail}`.toLowerCase().includes(query.toLowerCase())), [category, query]); const report = reports.find((item) => item.id === selected) ?? reports[0];
  const reset = () => { setQuery(""); setCategory("All"); setSelected(1); setWindow("Window not configured"); setAssignment("Owner not assigned"); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={BarChart3} eyebrow="ReportsDashboard · Operations preview" title="Prove the queue before reporting an outcome." description="Explore local report-operation concepts with search, categories, statuses, severity, assignment, timelines, evidence, analytics, exports, moderation, escalation, audit, save, reset, and governance gates. No live users, incidents, counts, timestamps, moderation decisions, or operational outcome is connected." badge="Reports workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "View saved locally" : "Save dashboard locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review dashboard gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset dashboard</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Queues", value: `${reports.length} local`, hint: "No report source", icon: BarChart3, tone: "cyan" }, { label: "Reports", value: "Unavailable", hint: "No event source", icon: AlertTriangle, tone: "violet" }, { label: "Reviewers", value: "Unassigned", hint: "No identity source", icon: UserRound, tone: "amber" }, { label: "Timeline", value: "Unconnected", hint: "No clock source", icon: Clock3, tone: "slate" }]} /><ScreenPreviewBanner title="Dashboard evidence boundary"><strong>This is a local report-operations preview, not evidence that incidents exist, queues are current, or moderation actions occurred.</strong> Queue cards, categories, statuses, severity, assignments, timelines, evidence, analytics, exports, escalation, saved state, and audit gates are browser concepts. No user, report, incident, count, timestamp, reviewer, moderation decision, or operational outcome is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Search className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local report queues" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-4 flex flex-wrap gap-2">{categories.map((entry) => <Button key={entry} onClick={() => setCategory(entry)} size="sm" variant="outline" className={category === entry ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-100" : "border-white/10 text-slate-400"}><Filter className="mr-1 size-3" />{entry}</Button>)}</div><div className="mt-6 space-y-3">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{item.name}</p><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.state}</Badge></div><div className="mt-4"><Badge variant="outline" className="border-white/10 text-slate-500">{item.category}</Badge></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected queue concept</p><h2 className="mt-2 text-2xl font-black">{report.name}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{report.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Category", value: report.category }, { label: "State", value: report.state }, { label: "Reports", value: "Unavailable" }, { label: "Severity", value: "Unassessed" }, { label: "Reviewer", value: assignment }, { label: "Timeline", value: window }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2"><label className="text-sm text-slate-400">Reporting window<select value={window} onChange={(event) => setWindow(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Window not configured</option><option>Current-window intent</option><option>Daily intent</option><option>Weekly intent</option></select></label><label className="text-sm text-slate-400">Reviewer assignment<select value={assignment} onChange={(event) => setAssignment(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Owner not assigned</option><option>Trust-and-safety intent</option><option>Content-review intent</option><option>Security-review intent</option></select></label></div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-8 text-center"><BarChart3 className="mx-auto size-8 text-slate-600" /><p className="mt-3 font-semibold">No report telemetry loaded</p><p className="mt-2 text-sm text-slate-500">Connect governed report events, reviewer identity, timestamps, policy taxonomy, evidence storage, and audit sources before showing operational counts.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Refresh unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Export unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500"><Download className="mr-2 size-4" />Download unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Escalate unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No incident or operations claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local queue concept does not prove current volume, urgency, reviewer action, incident severity, response time, or operational health.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Dashboard gates</p><h2 className="mt-2 text-2xl font-black">What a real reports dashboard must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Event provenance, report identity, subject privacy, consent, timestamps, locale, and tenant boundaries", "Policy taxonomy, severity, triage, reviewer identity, conflicts, assignment, escalation, and queue ownership", "Evidence storage, file safety, redaction, retention, chain of custody, appeals, and deletion", "Operational counts, filters, freshness, deduplication, pagination, exports, rate limits, and audit consistency", "Moderation, security, abuse, minors, threats, emergency escalation, support, and user-visible communication", "Incident response, SLA, uptime, alerting, rollback, access control, privacy leakage, and recovery"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Dashboard surface preserved", description: "Queues, search, categories, statuses, severity, assignment, timelines, evidence, analytics, exports, moderation, escalation, audit, save/reset, and gates remain interactive.", icon: BarChart3, status: "Local concepts" }, { title: "No operations theater", description: "Users, reports, incidents, counts, timestamps, reviewers, moderation decisions, and operational outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before metrics", description: "Real dashboards need governed events, identity, policy taxonomy, reviewer ownership, timestamp provenance, privacy, and audit.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
