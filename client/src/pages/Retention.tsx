import { useMemo, useState } from "react";
import { Archive, Check, Clock3, Filter, FolderClock, Gavel, LockKeyhole, RefreshCw, Search, ShieldAlert, Trash2, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const policies = [{ id: 1, name: "Account activity policy", category: "Product", detail: "A local retention concept awaiting record inventory, purpose, jurisdiction, schedule authority, privacy review, and deletion evidence.", state: "Draft" }, { id: 2, name: "Learning evidence policy", category: "Education", detail: "An education-record concept requiring learner privacy, safeguarding, institutional ownership, and approved retention authority.", state: "Needs evidence" }, { id: 3, name: "Wallet event policy", category: "Financial", detail: "A sensitive-record concept that must not imply custody, transaction history, balances, or regulatory compliance.", state: "Unmeasured" }, { id: 4, name: "Support case policy", category: "Operations", detail: "A support-record concept requiring access controls, legal-hold handling, redaction, escalation, and audit ownership.", state: "Blocked" }];
export default function Retention() {
  const [query, setQuery] = useState(""); const [category, setCategory] = useState("All"); const [selected, setSelected] = useState(1); const [window, setWindow] = useState("Retention window not configured"); const [hold, setHold] = useState("Legal hold review needed"); const [saved, setSaved] = useState(false); const [showGates, setShowGates] = useState(false);
  const categories = ["All", ...Array.from(new Set(policies.map((item) => item.category)))]; const filtered = useMemo(() => policies.filter((item) => (category === "All" || item.category === category) && `${item.name} ${item.category} ${item.detail}`.toLowerCase().includes(query.toLowerCase())), [category, query]); const policy = policies.find((item) => item.id === selected) ?? policies[0];
  const reset = () => { setQuery(""); setCategory("All"); setSelected(1); setWindow("Retention window not configured"); setHold("Legal hold review needed"); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={FolderClock} eyebrow="Retention · Records preview" title="Prove the retention rule before deleting a record." description="Explore local retention-policy concepts with search, categories, schedules, retention windows, legal holds, archival, deletion, privacy, approvals, audit, recovery, save, reset, and evidence gates. No live record, date, storage, compliance, or legal outcome is connected." badge="Records workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "View saved locally" : "Save policy locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review retention gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset policy</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Policies", value: `${policies.length} local`, hint: "No record source", icon: FolderClock, tone: "cyan" }, { label: "Records", value: "Unavailable", hint: "No inventory source", icon: Archive, tone: "violet" }, { label: "Schedule", value: "Unconfigured", hint: "No authority source", icon: Clock3, tone: "amber" }, { label: "Legal holds", value: "Review needed", hint: "No legal source", icon: Gavel, tone: "slate" }]} /><ScreenPreviewBanner title="Retention evidence boundary"><strong>This is a local records-policy preview, not evidence that a record exists, is subject to a date, or may be deleted.</strong> Policy cards, schedules, retention windows, legal holds, archival, deletion, privacy, approvals, saved state, and audit gates are browser concepts. No record, user, timestamp, storage object, jurisdiction, compliance status, or legal conclusion is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Search className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local retention policies" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-4 flex flex-wrap gap-2">{categories.map((entry) => <Button key={entry} onClick={() => setCategory(entry)} size="sm" variant="outline" className={category === entry ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-100" : "border-white/10 text-slate-400"}><Filter className="mr-1 size-3" />{entry}</Button>)}</div><div className="mt-6 space-y-3">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{item.name}</p><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.state}</Badge></div><div className="mt-4"><Badge variant="outline" className="border-white/10 text-slate-500">{item.category}</Badge></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected retention concept</p><h2 className="mt-2 text-2xl font-black">{policy.name}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{policy.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Category", value: policy.category }, { label: "State", value: policy.state }, { label: "Records", value: "Unavailable" }, { label: "Schedule", value: window }, { label: "Legal hold", value: hold }, { label: "Deletion", value: "Blocked" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2"><label className="text-sm text-slate-400">Retention window<select value={window} onChange={(event) => setWindow(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Retention window not configured</option><option>Short-window intent</option><option>Standard-window intent</option><option>Long-window intent</option></select></label><label className="text-sm text-slate-400">Legal hold posture<select value={hold} onChange={(event) => setHold(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Legal hold review needed</option><option>Hold-check intent</option><option>Hold-active intent</option><option>Hold-clearance intent</option></select></label></div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-8 text-center"><Archive className="mx-auto size-8 text-slate-600" /><p className="mt-3 font-semibold">No retention evidence loaded</p><p className="mt-2 text-sm text-slate-500">Connect governed records, storage, timestamps, purpose, jurisdiction, privacy controls, legal holds, deletion authority, and audit before archiving or deleting.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Evaluate unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Archive unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500"><Trash2 className="mr-2 size-4" />Delete unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Export unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No compliance or deletion claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local policy does not prove a retention period, legal obligation, storage location, hold status, compliance outcome, or safe deletion.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Retention gates</p><h2 className="mt-2 text-2xl font-black">What a real records-retention workflow must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Record inventory, owner, purpose, classification, source, timestamp, storage, backup, tenant, and data residency boundaries", "Retention authority, jurisdiction, policy version, schedule semantics, event trigger, exceptions, and legal review", "Privacy, consent, sensitive data, minors, wallet/financial records, education, support, redaction, and access controls", "Legal holds, discovery, preservation, suspension, release, appeals, documentation, and accountable decision owners", "Archive, deletion, deletion verification, recovery, rollback, audit, notification, user-visible status, and support", "Availability, export, accessibility, localization, incident response, operational monitoring, and policy change history"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Retention surface preserved", description: "Policies, search, categories, schedules, windows, legal holds, archival, deletion, privacy, approvals, audit, recovery, save/reset, and gates remain interactive.", icon: FolderClock, status: "Local concepts" }, { title: "No compliance theater", description: "Records, dates, storage, jurisdictions, holds, legal status, compliance, and deletion outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before deletion", description: "Real retention decisions need authoritative records, policy version, legal review, privacy controls, hold handling, and audit.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
