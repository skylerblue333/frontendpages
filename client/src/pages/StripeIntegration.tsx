import { useState } from "react";
import { AlertTriangle, Check, CreditCard, FileCheck2, KeyRound, LockKeyhole, RefreshCw, RotateCcw, Settings2, ShieldAlert, Webhook, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatePanel, ScreenStatGrid } from "@/components/ScreenExperience";
const checks = [{ label: "Provider account", value: "Unconnected", icon: CreditCard }, { label: "Secret handling", value: "Server-only", icon: KeyRound }, { label: "Webhook endpoint", value: "Unverified", icon: Webhook }, { label: "Catalog sync", value: "Unavailable", icon: Settings2 }, { label: "Ledger reconciliation", value: "Required", icon: RotateCcw }, { label: "Payout / disputes", value: "Blocked", icon: ShieldAlert }];
export default function StripeIntegration() { const [saved, setSaved] = useState(false); const [showBoundary, setShowBoundary] = useState(false); const [retrying, setRetrying] = useState(false); const retry = () => { setRetrying(true); window.setTimeout(() => setRetrying(false), 500); }; const reset = () => { setSaved(false); setShowBoundary(false); setRetrying(false); }; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={CreditCard} eyebrow="Stripe integration · Provider preview" title="Design payment integration without exposing secrets or claiming a connection." description="Explore a local provider-configuration workspace with account mode, secret handling, webhook, product catalog, customers, payments, payouts, reconciliation, logging, retry, save/reset, and evidence boundaries. No provider account, API key, webhook, product, customer, charge, payout, or entitlement exists." badge="Evidence-bounded integration"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save integration policy"}</Button><Button onClick={() => setShowBoundary((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showBoundary ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showBoundary ? "Close evidence gates" : "Review integration gates"}</Button><Button onClick={retry} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className={`mr-2 size-4 ${retrying ? "animate-spin" : ""}`} />{retrying ? "Retrying local state" : "Retry provider checks"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><X className="mr-2 size-4" />Reset integration</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Provider", value: "Unconnected", hint: "No account source", icon: CreditCard, tone: "cyan" }, { label: "Secrets", value: "Protected", hint: "Never rendered", icon: KeyRound, tone: "violet" }, { label: "Webhooks", value: "Unverified", hint: "No endpoint source", icon: Webhook, tone: "amber" }, { label: "Ledger", value: "Required", hint: "No reconciliation source", icon: ShieldAlert, tone: "slate" }]} /><ScreenPreviewBanner title="Integration evidence boundary"><strong>This is a local provider-configuration preview, not evidence that Stripe or any payment provider is connected, authenticated, configured, receiving events, reconciling money, or granting entitlements.</strong> Readiness cards, server-only secret policy, webhook concepts, catalog sync, logs, local saved state, retry feedback, and disabled connect/test actions are browser concepts. No secret, account, customer, payment, charge, payout, invoice, refund, or entitlement is asserted.</ScreenPreviewBanner><section className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{checks.map(({ label, value, icon: Icon }) => <Card key={label} className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="flex items-start justify-between"><div className="flex size-11 items-center justify-center rounded-2xl bg-cyan-300/10 text-cyan-200"><Icon className="size-5" /></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{value}</Badge></div><h2 className="mt-4 font-bold">{label}</h2><p className="mt-2 text-sm text-slate-500">Evidence required before activation.</p></CardContent></Card>)}</section><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start gap-3"><KeyRound className="mt-1 size-5 text-cyan-300" /><div><p className="font-bold">Secret-handling contract</p><p className="mt-2 text-sm leading-6 text-slate-400">API keys, webhook secrets, customer data, payment methods, and provider tokens must remain server-side, redacted from logs, rotated, scoped, and never placed in client bundles or screenshots.</p></div></div><div className="mt-6 grid gap-3 sm:grid-cols-2">{["Server-side environment storage", "Least-privilege provider scopes", "Rotation and revocation runbook", "Redacted structured logging", "Webhook signature verification", "Idempotency and replay protection"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3 text-sm text-slate-300"><FileCheck2 className="size-4 text-amber-300" />{item}<span className="ml-auto text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenStatePanel type="unavailable" title="Provider connection unavailable" description="Connect a server-side provider account through approved configuration, then verify signatures, catalog, customers, payment events, refunds, disputes, payouts, reconciliation, entitlements, and audit logs before enabling operations." /></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><AlertTriangle className="size-5 text-amber-300" /><div><p className="font-bold">Activation checklist</p><p className="mt-1 text-sm text-slate-400">A connection is not complete until account identity, mode, catalog, webhooks, payment flows, refunds, disputes, payouts, reconciliation, entitlements, observability, and failure handling are proven.</p></div></div></CardContent></Card><div className="flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Connect provider unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Test webhook unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Sync catalog unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Open provider logs unavailable</Button></div>{showBoundary && <Card className="border-amber-300/20 bg-amber-300/[0.06]"><CardContent className="p-6"><p className="font-semibold text-amber-100">No provider or payment claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local integration policy does not prove an account, key, webhook, catalog, customer, charge, payout, invoice, refund, reconciliation, or entitlement.</p></CardContent></Card>}<ScreenFeatureGrid features={[{ title: "Integration surface preserved", description: "Provider, secrets, webhooks, catalog, payments, payouts, reconciliation, logs, retry, save/reset, and activation gates remain visible.", icon: CreditCard, status: "Local provider" }, { title: "No secret theater", description: "API keys, webhook secrets, customer data, payment methods, charges, payouts, and entitlements are not fabricated or exposed.", icon: LockKeyhole, status: "Guardrail" }, { title: "Evidence before activation", description: "Real integration requires server-side secrets, scopes, signatures, idempotency, reconciliation, failure handling, and audit.", icon: ShieldAlert, status: "Blocked" }]} /></main></div>; }
