import { useMemo, useState } from "react";
import { AlertTriangle, Ban, CheckCircle2, Clock3, FileText, LockKeyhole, MessageSquare, Search, Shield, UserRound, UserX, XCircle } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

type Scenario = { id: string; subject: string; scope: string; state: "Review required" | "Appeal open" | "Evidence missing"; description: string };
const SCENARIOS: Scenario[] = [
  { id: "s-1", subject: "Synthetic account A", scope: "Community visibility", state: "Review required", description: "Example block scenario requiring a specific policy, evidence, proportionality, and notice." },
  { id: "s-2", subject: "Synthetic account B", scope: "Direct interactions", state: "Appeal open", description: "Example temporary restriction requiring expiry, user notice, appeal review, and audit." },
  { id: "s-3", subject: "Synthetic account C", scope: "Marketplace contact", state: "Evidence missing", description: "Example safety scenario requiring context and a human decision before any access change." },
  { id: "s-4", subject: "Synthetic account D", scope: "High-impact access", state: "Review required", description: "Example sensitive case requiring authority, identity verification, and a documented reason." },
];
const REQUIREMENTS = ["Verified authority and subject scope", "Policy and evidence reference", "Specific scope and duration", "Human approval and reason", "Notice and appeal", "Expiry and audit record"];

export default function BlockUser() { const [query, setQuery] = useState(""); const [selected, setSelected] = useState<Scenario | null>(null); const [preview, setPreview] = useState<"block" | "unblock" | null>(null); const visible = useMemo(() => SCENARIOS.filter((scenario) => `${scenario.subject} ${scenario.scope} ${scenario.state} ${scenario.description}`.toLowerCase().includes(query.toLowerCase())), [query]); return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={UserX} eyebrow="Trust & Safety · User Blocking" title="Make blocking scoped, reviewable, and reversible." description="Explore synthetic user-block scenarios, scope controls, notice, appeals, expiry, and audit requirements. This workspace does not identify users, change account access, send notices, or claim enforcement results." badge="Preview blocking workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setPreview("block")} disabled={!selected} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Ban className="mr-2 size-4" />Preview block</Button><Button variant="outline" onClick={() => setPreview("unblock")} disabled={!selected} className="border-white/15 bg-white/5 text-white hover:bg-white/10"><XCircle className="mr-2 size-4" />Preview unblock</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Synthetic scenarios", value: String(SCENARIOS.length), hint: "Privacy-safe examples", icon: UserX }, { label: "Live block state", value: "Unavailable", hint: "No identity or enforcement connector", icon: Ban, tone: "amber" }, { label: "Scope control", value: "Required", hint: "Avoid global action by default", icon: Shield, tone: "violet" }, { label: "Appeal path", value: "Required", hint: "Notice and reversal evidence", icon: MessageSquare, tone: "slate" }]} /><ScreenPreviewBanner title="Blocking evidence boundary">Synthetic scenarios, search, selected detail, local block/unblock previews, scope and duration requirements, notice, appeals, and unavailable enforcement state are available for UX review. No user was identified, blocked, unblocked, notified, or placed in a real account state.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.85fr_1.15fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="relative"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search synthetic scenarios" aria-label="Search synthetic scenarios" className="border-white/10 bg-black/20 pl-9 text-white placeholder:text-slate-500" /></div><div className="mt-4 space-y-2">{visible.map((scenario) => <button key={scenario.id} onClick={() => { setSelected(scenario); setPreview(null); }} className={`w-full rounded-xl border p-4 text-left transition ${selected?.id === scenario.id ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-black/15 hover:bg-white/[0.05]"}`}><div className="flex items-center gap-2"><UserRound className="size-4 text-cyan-300" /><span className="font-semibold">{scenario.subject}</span><Badge variant="outline" className="ml-auto border-amber-300/20 text-amber-200">{scenario.state}</Badge></div><p className="mt-2 text-xs uppercase tracking-wider text-slate-500">{scenario.scope}</p><p className="mt-2 text-sm leading-6 text-slate-400">{scenario.description}</p></button>)}{visible.length === 0 && <ScreenStatePanel type="empty" title="No scenarios match" description="Try another scope, review state, or keyword." />}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6">{selected ? <><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Selected block scenario</p><h2 className="mt-1 text-2xl font-bold">{selected.subject}</h2><p className="mt-2 text-sm text-slate-400">{selected.scope}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{selected.state}</Badge></div><p className="mt-5 text-sm leading-6 text-slate-300">{selected.description}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{REQUIREMENTS.map((item) => <div key={item} className="rounded-lg border border-white/10 bg-black/15 p-4"><CheckCircle2 className="size-4 text-emerald-300" /><p className="mt-3 text-sm text-slate-300">{item}</p><p className="mt-1 text-xs text-slate-500">Evidence required</p></div>)}</div>{preview && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex items-center gap-2 font-medium text-amber-200"><AlertTriangle className="size-4" />{preview === "block" ? "Block" : "Unblock"} preview prepared</div><p className="mt-2 text-sm leading-6 text-slate-300">No account access changed. A real action requires verified authority, exact scope, human review, notice, appeal, expiry, and an audit record.</p></div>}</> : <ScreenStatePanel type="empty" title="Select a block scenario" description="Inspect scope, duration, policy evidence, notice, appeal, expiry, and audit requirements." />}</CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Scope before action", description: "A block should identify the exact surface, duration, and rationale rather than silently becoming a global account action.", icon: Shield, status: "Required" }, { title: "Reversal is designed", description: "Provide expiry, unblock authority, notice, appeal, and clear recovery evidence for every restriction.", icon: Clock3, status: "Required" }, { title: "No fake block", description: "A local preview never changes identity, permissions, visibility, messages, transactions, or account state.", icon: LockKeyhole, status: "Guardrail" }]} /></main></div>; }
