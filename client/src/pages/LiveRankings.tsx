import { Award, ShieldCheck, Trophy, Users } from "lucide-react";
import { PageHeader } from "@/components/PageHeader";

export default function LiveRankings() {
  return <div className="container max-w-6xl animate-page-in py-8"><PageHeader icon={Trophy} title="Live Rankings" subtitle="Cross-platform reputation and competition surface" /><div className="mb-8 rounded-3xl border border-amber-300/20 bg-amber-300/[0.05] p-6"><div className="flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.2em] text-amber-200"><ShieldCheck className="size-4" />Verified ranking boundary</div><h2 className="mt-3 text-3xl font-black">One ranking surface, many products, one evidence model.</h2><p className="mt-3 max-w-3xl text-sm leading-6 text-muted-foreground">Designed to consolidate unique ranking functionality from the frontend source repositories while preventing fabricated scores, users, wins, or reputation values.</p></div><div className="grid gap-4 md:grid-cols-3">{[[Users,"Players"],[Trophy,"Competitions"],[Award,"Reputation"]].map(([Icon,label]) => <div key={label as string} className="card p-5"><Icon className="size-6 text-amber-200" /><p className="mt-4 font-bold">{label as string}</p><p className="mt-2 text-sm text-muted-foreground">Authoritative ranking events required.</p><div className="mt-4 text-xs text-amber-200">Unavailable</div></div>)}</div><div className="mt-8 card p-6"><h3 className="font-bold">Live leaderboard</h3><div className="mt-4 flex min-h-56 items-center justify-center rounded-2xl border border-dashed border-border/60 text-center"><div><Trophy className="mx-auto size-9 text-muted-foreground/50" /><p className="mt-3 font-semibold">No verified ranking stream</p><p className="mt-2 text-sm text-muted-foreground">Connect event sourcing, identity, and scoring rules to populate this surface.</p></div></div></div></div>;
}
