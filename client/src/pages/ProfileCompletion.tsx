import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Accessibility, Check, ChevronRight, Eye, FileText, LockKeyhole, RefreshCw, ShieldAlert, UserRound, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const initial = [{ id: "identity", label: "Identity basics", detail: "Display name and short bio", done: false, icon: UserRound }, { id: "privacy", label: "Privacy choices", detail: "Visibility and sharing intent", done: false, icon: Eye }, { id: "accessibility", label: "Accessibility preferences", detail: "Readable, navigable experience", done: false, icon: Accessibility }, { id: "evidence", label: "Evidence review", detail: "Understand unsupported claims", done: false, icon: FileText }];
export default function ProfileCompletion() {
  const [items, setItems] = useState(initial);
  const [selectedId, setSelectedId] = useState(initial[0].id);
  const [saved, setSaved] = useState(false);
  const [showBoundary, setShowBoundary] = useState(false);
  const complete = useMemo(() => items.filter((item) => item.done).length, [items]);
  const selected = items.find((item) => item.id === selectedId) ?? items[0];
  const toggle = (id: string) => setItems((current) => current.map((item) => item.id === id ? { ...item, done: !item.done } : item));
  const reset = () => { setItems(initial); setSelectedId(initial[0].id); setSaved(false); setShowBoundary(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={UserRound} eyebrow="ProfileCompletion · Onboarding preview" title="Build an account checklist users can understand." description="Review local completion steps for identity, privacy, accessibility, and evidence literacy. Progress is browser state only; no account record, verification, consent, readiness, or onboarding completion is persisted." badge="Completion workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Checklist saved locally" : "Save checklist locally"}</Button><Button onClick={() => setShowBoundary((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showBoundary ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showBoundary ? "Close boundary" : "View boundary"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset checklist</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Checklist", value: `${complete}/${items.length}`, hint: "Local progress", icon: Check, tone: "cyan" }, { label: "Account", value: "Unknown", hint: "No persistence", icon: LockKeyhole, tone: "violet" }, { label: "Verification", value: "Off", hint: "No identity source", icon: UserRound, tone: "amber" }, { label: "Accessibility", value: "Review", hint: "Preferences unpersisted", icon: Accessibility, tone: "slate" }]} /><ScreenPreviewBanner title="Completion evidence boundary"><strong>This is a local onboarding checklist, not a completion certificate.</strong> Step labels, progress, selected detail, and saved state are browser concepts. No account record, identity verification, legal consent, profile readiness, personalization, accessibility preference, or onboarding event is persisted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.82fr_1.18fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Local checklist</p><h2 className="mt-2 text-2xl font-black">Choose a step</h2></div><Badge variant="outline" className="border-white/10 text-amber-200">{complete}/{items.length}</Badge></div><div className="mt-5 space-y-3">{items.map((item) => { const Icon = item.icon; return <button key={item.id} onClick={() => setSelectedId(item.id)} className={`flex w-full items-center gap-3 rounded-xl border p-4 text-left ${selected.id === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><button aria-label={`toggle ${item.label}`} onClick={(event) => { event.stopPropagation(); toggle(item.id); }} className={`flex size-5 items-center justify-center rounded border ${item.done ? "border-cyan-300 bg-cyan-300 text-slate-950" : "border-white/20"}`}>{item.done && <Check className="size-3" />}</button><Icon className="size-5 text-cyan-200" /><div className="flex-1"><p className="font-semibold">{item.label}</p><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><ChevronRight className="size-4 text-slate-600" /></button>; })}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Step detail</p><h2 className="mt-2 text-2xl font-black">{selected.label}</h2><p className="mt-2 text-sm text-slate-500">{selected.detail}</p></div><Badge variant="outline" className={selected.done ? "border-cyan-300/30 text-cyan-200" : "border-amber-300/20 text-amber-200"}>{selected.done ? "Local done" : "Needs review"}</Badge></div><div className="mt-6 rounded-xl border border-white/10 p-5"><p className="text-sm leading-7 text-slate-300">Review this step locally, decide what evidence or preference should be collected, then connect it to an authenticated account workflow only after the data contract and privacy behavior are defined.</p></div><Button onClick={() => toggle(selected.id)} className="mt-5 bg-cyan-300 text-slate-950 hover:bg-cyan-200">{selected.done ? "Mark for review" : "Mark locally complete"}</Button>{showBoundary && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No readiness claim</p><p className="mt-2 text-sm leading-6 text-slate-400">Local completion cannot verify identity, permissions, consent, accessibility support, account sync, or readiness to use financial, AI, community, or educational features.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Onboarding gates</p><h2 className="mt-2 text-2xl font-black">What a real completion flow must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Authenticated account and recoverable session", "Clear purpose, optionality, consent, and privacy explanation", "Server-side validation and persistence with audit-safe events", "Accessible controls, keyboard flow, localization, and error recovery", "Role, entitlement, safety, and feature availability checks", "Reset, export, deletion, and support behavior"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><FileText className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Onboarding surface preserved", description: "Checklist items, local progress, selected detail, step completion, evidence boundary, save/reset, and account gates remain interactive.", icon: UserRound, status: "Local flow" }, { title: "No completion theater", description: "Account persistence, verification, consent, readiness, personalization, accessibility preference, and onboarding events are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Persistence before certainty", description: "Real completion needs authenticated storage, privacy contracts, accessible error recovery, permissions, entitlements, and support.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
