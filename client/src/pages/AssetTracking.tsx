import { useMemo, useState } from "react";
import { Archive, CheckCircle2, ClipboardCheck, Clock3, FileImage, History, MapPin, Package, Search, Shield, Tag, Truck, UserRound } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

type Record = { id: string; name: string; kind: string; state: "Location unavailable" | "Check-in preview" | "Chain-of-custody review"; lastKnown: string; description: string };
const RECORDS: Record[] = [
  { id: "kit", name: "Creator equipment kit", kind: "Physical", state: "Location unavailable", lastKnown: "No tracking source", description: "A custody record example requiring handoff, owner, timestamp, and condition evidence." },
  { id: "media", name: "Campaign media package", kind: "Digital", state: "Check-in preview", lastKnown: "Workspace draft", description: "A digital provenance example requiring version, checksum, access scope, and export audit." },
  { id: "collectible", name: "Collectible provenance", kind: "On-chain", state: "Chain-of-custody review", lastKnown: "Network unverified", description: "An ownership-history example requiring network, transaction hash, signer, and confirmation evidence." },
  { id: "lesson", name: "Lesson publication bundle", kind: "Content", state: "Location unavailable", lastKnown: "No catalog source", description: "A content lifecycle example requiring contributor, approval, version, and publication history." },
];
const REQUIREMENTS = ["Record identifier and source", "Owner or custodian", "Timestamp and timezone", "Location or network context", "Condition and integrity", "Handoff audit event"];

export default function AssetTracking() { const [query, setQuery] = useState(""); const [records, setRecords] = useState(RECORDS); const [selected, setSelected] = useState<Record | null>(null); const [action, setAction] = useState<"check-in" | "check-out" | null>(null); const visible = useMemo(() => records.filter((record) => `${record.name} ${record.kind} ${record.state} ${record.lastKnown} ${record.description}`.toLowerCase().includes(query.toLowerCase())), [query, records]); const addRecord = () => { const draft = { id: `draft-${records.length + 1}`, name: `Tracking record ${records.length + 1}`, kind: "Draft", state: "Check-in preview" as const, lastKnown: "Local workspace", description: "Local tracking placeholder; no location, owner, custody, or movement has been recorded." }; setRecords((current) => [draft, ...current]); setSelected(draft); setAction("check-in"); }; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={MapPin} eyebrow="Operations · Asset Tracking" title="Trace an asset without pretending to know where it is." description="Explore provenance records, custody requirements, status filters, and local check-in/check-out previews. This page does not claim GPS, blockchain ownership, live movement, possession, or location history." badge="Preview tracking workspace"><div className="flex flex-wrap gap-2"><Button onClick={addRecord} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><ClipboardCheck className="mr-2 size-4" />Add local tracking record</Button><Button variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><Shield className="mr-2 size-4" />Custody checklist</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Tracking examples", value: String(records.length), hint: "Local provenance records", icon: MapPin }, { label: "Live location", value: "Unavailable", hint: "No GPS or chain feed", icon: Truck, tone: "amber" }, { label: "Custody", value: "Required", hint: "Handoff audit needed", icon: UserRound, tone: "violet" }, { label: "Movement history", value: "Unverified", hint: "No event source connected", icon: History, tone: "slate" }]} /><ScreenPreviewBanner title="Tracking evidence boundary">Tracking examples, search, selected-record detail, custody requirements, local check-in/check-out previews, and unavailable location state are available for UX review. No GPS location, network ownership, movement, possession, timestamp, transaction, or custody event is fabricated.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.85fr_1.15fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="relative"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search tracking records" aria-label="Search tracking records" className="border-white/10 bg-black/20 pl-9 text-white placeholder:text-slate-500" /></div><div className="mt-4 space-y-2">{visible.map((record) => <button key={record.id} onClick={() => { setSelected(record); setAction(null); }} className={`w-full rounded-xl border p-4 text-left transition ${selected?.id === record.id ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-black/15 hover:bg-white/[0.05]"}`}><div className="flex items-center gap-2"><MapPin className="size-4 text-cyan-300" /><span className="font-semibold">{record.name}</span><Badge variant="outline" className="ml-auto border-amber-300/20 text-amber-200">{record.state}</Badge></div><p className="mt-2 text-xs uppercase tracking-wider text-slate-500">{record.kind} · {record.lastKnown}</p><p className="mt-2 text-sm leading-6 text-slate-400">{record.description}</p></button>)}{visible.length === 0 && <ScreenStatePanel type="empty" title="No tracking records match" description="Try another asset name, kind, or state." />}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6">{selected ? <><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Selected tracking record</p><h2 className="mt-1 text-2xl font-bold">{selected.name}</h2><p className="mt-2 text-sm text-slate-400">{selected.kind} · {selected.lastKnown}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{selected.state}</Badge></div><p className="mt-5 text-sm leading-6 text-slate-300">{selected.description}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{REQUIREMENTS.map((item) => <div key={item} className="rounded-lg border border-white/10 bg-black/15 p-4"><CheckCircle2 className="size-4 text-emerald-300" /><p className="mt-3 text-sm text-slate-300">{item}</p><p className="mt-1 text-xs text-slate-500">Evidence required</p></div>)}</div><div className="mt-6 flex flex-wrap gap-2"><Button size="sm" onClick={() => setAction("check-in")} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><ClipboardCheck className="mr-2 size-4" />Preview check-in</Button><Button size="sm" variant="outline" onClick={() => setAction("check-out")} className="border-white/15 text-slate-300"><Truck className="mr-2 size-4" />Preview check-out</Button></div>{action && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex items-center gap-2 font-medium text-amber-200"><Clock3 className="size-4" />{action === "check-in" ? "Check-in" : "Check-out"} preview prepared</div><p className="mt-2 text-sm leading-6 text-slate-300">No custody, location, owner, or movement record was written. Connect an authorized tracking source and audit service before performing this action.</p></div>}</> : <ScreenStatePanel type="empty" title="Select a tracking record" description="Inspect source, custodian, timestamp, location, integrity, and handoff requirements." />}</CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Provenance is sourced", description: "Every location or ownership statement needs an authoritative source, timestamp, identifier, and freshness state.", icon: DatabaseIcon, status: "Required" }, { title: "Custody is accountable", description: "Record who handled an asset, when, under what authority, and with what condition or integrity evidence.", icon: UserRound, status: "Required" }, { title: "No fake movement", description: "A local check-in preview is not GPS, chain ownership, possession, or a completed handoff.", icon: AlertTriangleIcon, status: "Guardrail" }]} /></main></div>; }
function DatabaseIcon() { return <span aria-hidden="true">◫</span>; }
function AlertTriangleIcon() { return <span aria-hidden="true">!</span>; }
