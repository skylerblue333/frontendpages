import FeatureUnavailable from "@/components/FeatureUnavailable";

const AdaptiveRoadmap = () => (
  <FeatureUnavailable
    title="Adaptive roadmap"
    description="Roadmap progress, delivery velocity, impact, owners, and release outcomes are intentionally held at a truthful release boundary until verified work items, source changes, deployment records, acceptance evidence, and rollback plans are connected."
    capability="Roadmap planning and delivery evidence"
    nextStep="Return to the launch hub"
  />
);

export default AdaptiveRoadmap;
