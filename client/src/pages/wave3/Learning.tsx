import FeatureUnavailable from "@/components/FeatureUnavailable";

const Learning = () => (
  <FeatureUnavailable
    title="Learning wave preview"
    description="Enrollment, course completion, certificates, learner identity, progress, and educational outcomes are intentionally held at a truthful release boundary until verified course, assessment, credential, safeguarding, and persistence services are connected."
    capability="Education enrollment and credential evidence"
    nextStep="Return to the launch hub"
  />
);

export default Learning;
