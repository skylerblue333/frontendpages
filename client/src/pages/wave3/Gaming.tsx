import FeatureUnavailable from "@/components/FeatureUnavailable";

const Gaming = () => (
  <FeatureUnavailable
    title="Gaming wave preview"
    description="Game recording, rewards, rankings, player identity, and completion outcomes are intentionally held at a truthful release boundary until verified game state, account, moderation, reward, and persistence services are connected."
    capability="Gaming state and reward evidence"
    nextStep="Return to the launch hub"
  />
);

export default Gaming;
