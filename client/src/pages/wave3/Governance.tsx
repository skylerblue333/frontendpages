import FeatureUnavailable from "@/components/FeatureUnavailable";

const Governance = () => (
  <FeatureUnavailable
    title="Governance wave preview"
    description="Vote recording, eligibility, quorum, delegation, proposal execution, and governance outcomes are intentionally held at a truthful release boundary until verified identity, voting power, policy, execution, and audit services are connected."
    capability="Governance voting and execution evidence"
    nextStep="Return to the launch hub"
  />
);

export default Governance;
