import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { BookOpen, Check, ClipboardCheck, FileText, Gauge, KeyRound, LockKeyhole, RefreshCw, ShieldCheck, Sparkles, Target, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const AUDIENCES = [{ name: "Investors", objective: "Explain the evidence and open questions", draft: "SKYCOIN4444 is exploring an integrated ecosystem concept. This brief separates current interface work from future opportunities and identifies the evidence still needed before financial or market claims." }, { name: "Users", objective: "Clarify what is useful today", draft: "Explore a connected product surface with local previews, clear boundaries, and an evolving set of tools. Capabilities should be verified at the moment they are offered." }, { name: "Enterprise", objective: "Describe readiness without overclaiming", draft: "SKYCOIN4444 is organizing product experiences around explicit integration boundaries, verification gates, and operational proof rather than unsupported certification or scale claims." }, { name: "Creators", objective: "Set expectations for the workflow", draft: "Use the workspace to shape ideas, review evidence, and plan future publishing. Monetization, audience, rights, and delivery require connected services and explicit terms." }];
const CLAIMS = ["Every factual statement has a source or is labeled as a proposal", "Financial, market, scale, and performance claims have owner and date", "Compliance, security, and capability language has engineering evidence", "Audience promise is separated from product capability and roadmap", "Draft has review owner, version, and publication gate"];

export default function NarrativeEngine() {
  const [audience, setAudience] = useState(AUDIENCES[0].name);
  const [objective, setObjective] = useState(AUDIENCES[0].objective);
  const [draft, setDraft] = useState(AUDIENCES[0].draft);
  const [note, setNote] = useState("");
  const [saved, setSaved] = useState(false);
  const selected = AUDIENCES.find((item) => item.name === audience) ?? AUDIENCES[0];
  const choose = (item: (typeof AUDIENCES)[number]) => { setAudience(item.name); setObjective(item.objective); setDraft(item.draft); setSaved(false); };
  const reset = () => choose(AUDIENCES[0]);
  return <div className="min-h-screen bg-[#080711] text-white"><ScreenHero icon={BookOpen} eyebrow="NarrativeEngine · Evidence-led messaging" title="Shape the story before claiming the outcome." description="Build a local narrative brief by audience, objective, draft, and proof checklist. No model is invoked, no persuasiveness or engagement is measured, and no market, revenue, valuation, compliance, performance, or growth outcome is predicted." badge="Narrative preview"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-amber-300 text-slate-950 hover:bg-amber-200"><Check className="mr-2 size-4" />{saved ? "Brief saved locally" : "Save narrative brief"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset brief</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Audience", value: audience, hint: "Local selection", icon: Target }, { label: "Objective", value: objective, hint: "Brief direction", icon: BookOpen, tone: "violet" }, { label: "Proof gates", value: `${CLAIMS.length}`, hint: "Review checklist", icon: ClipboardCheck, tone: "amber" }, { label: "Metrics", value: "Off", hint: "No outcome source", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="NarrativeEngine evidence boundary"><strong>Audience labels, objectives, draft copy, and proof gates are local writing fixtures—not generated copy, persuasion scores, engagement, CTR, conversion, sentiment, TAM, revenue, valuation, user scale, compliance, security certification, or competitive advantage.</strong> A production narrative system requires source provenance, review ownership, claims governance, versioning, audience research, accessibility, rights, privacy, and controlled publication.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.9fr_1.1fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Audience brief</p><h2 className="mt-2 text-3xl font-black">Choose the lens</h2><div className="mt-5 space-y-3">{AUDIENCES.map((item) => <button key={item.name} onClick={() => choose(item)} className={`w-full rounded-2xl border p-4 text-left ${audience === item.name ? "border-amber-300/40 bg-amber-300/[0.08]" : "border-white/10 bg-black/10"}`}><div className="flex items-center justify-between"><span className="font-black">{item.name}</span>{audience === item.name && <Check className="size-4 text-amber-200" />}</div><p className="mt-2 text-xs text-slate-500">{item.objective}</p></button>)}</div><label className="mt-5 block"><span className="text-sm font-semibold text-slate-300">Objective</span><input value={objective} onChange={(event) => { setObjective(event.target.value); setSaved(false); }} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-white" /></label><div className="mt-5 flex items-start gap-3 text-xs leading-5 text-slate-500"><LockKeyhole className="mt-0.5 size-4 shrink-0 text-amber-200" />Do not use this preview to make unsupported financial, health, legal, security, compliance, or investment promises.</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Draft workspace</p><h2 className="mt-2 text-3xl font-black">{selected.name} narrative</h2></div><Badge variant="outline" className="border-white/10 text-slate-400">Local draft</Badge></div><p className="mt-3 text-slate-400">{selected.objective}</p><textarea value={draft} onChange={(event) => { setDraft(event.target.value); setSaved(false); }} className="mt-5 min-h-48 w-full rounded-xl border border-white/10 bg-black/20 p-4 text-sm leading-7 text-white" /><div className="mt-5 rounded-2xl border border-amber-300/20 bg-amber-300/[0.05] p-5"><div className="flex items-center gap-3"><div className="flex size-10 items-center justify-center rounded-xl bg-amber-300/10 text-amber-200"><Sparkles className="size-5" /></div><div><p className="font-black">No optimization result</p><p className="text-xs text-slate-500">Copy quality, persuasiveness, and audience response are unmeasured</p></div></div><div className="mt-4 grid gap-3 sm:grid-cols-3">{[{ label: "Source", value: "Not attached" }, { label: "Version", value: "Local draft" }, { label: "Publication", value: "Blocked" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div></div><textarea value={note} onChange={(event) => { setNote(event.target.value); setSaved(false); }} placeholder="Write a local review note or source request…" className="mt-5 min-h-24 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-sm text-white placeholder:text-slate-500" /><div className="mt-4 flex gap-2"><Button disabled className="flex-1 bg-white/10 text-white/40"><FileText className="mr-2 size-4" />Generate unavailable</Button><Button disabled variant="outline" className="flex-1 border-white/10 text-white/40">Publish unavailable</Button></div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Claims review</p><h2 className="mt-2 text-2xl font-black">Required before publication</h2><div className="mt-5 space-y-3">{CLAIMS.map((claim) => <div key={claim} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{claim}</span><span className="text-xs text-amber-200">Review</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><Gauge className="size-5 text-amber-200" /><h2 className="mt-4 text-xl font-black">No fake performance</h2><p className="mt-3 text-sm leading-6 text-slate-400">This workspace does not calculate engagement, CTR, conversion, sentiment, persuasion, forecast, ROI, market size, valuation, or growth.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "A draft is not evidence", description: "Audience copy preserves writing UX without outcome, market, revenue, or capability claims.", icon: FileText, status: "Guardrail" }, { title: "Claims need owners", description: "Sources, dates, review ownership, versioning, and publication gates are required.", icon: ShieldCheck, status: "Required" }, { title: "No fake generation", description: "The preview invokes no model and produces no performance result, publication, or audience response.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
