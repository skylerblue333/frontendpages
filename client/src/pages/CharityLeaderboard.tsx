import { useMemo, useState } from "react";
import { BarChart3, Check, CircleDollarSign, EyeOff, FileCheck2, Heart, Info, Medal, RefreshCw, Search, ShieldAlert, Target, Trophy, UsersRound, WifiOff } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

type Donor = { id: string; name: string; total: string; campaigns: string; state: string; note: string };
const DONORS: Donor[] = [{ id: "donor-1", name: "Synthetic donor A", total: "Unavailable", campaigns: "Unverified", state: "Preview", note: "No identity, transfer, receipt, or beneficiary record is connected." }, { id: "donor-2", name: "Anonymous preview", total: "Unavailable", campaigns: "Unverified", state: "Privacy-safe", note: "A privacy-preserving label cannot prove amount, rank, or donor consent." }, { id: "donor-3", name: "Synthetic donor C", total: "Unavailable", campaigns: "Unverified", state: "Review", note: "No wallet, campaign, payment, or contribution history is available." }];
const CAMPAIGNS = ["All causes", "Environment", "Education", "Humanitarian"];
export default function CharityLeaderboard() { const [query, setQuery] = useState(""); const [cause, setCause] = useState("All causes"); const [selectedId, setSelectedId] = useState(DONORS[0].id); const [saved, setSaved] = useState(false); const visible = useMemo(() => DONORS.filter((donor) => donor.name.toLowerCase().includes(query.toLowerCase())), [query]); const selected = visible.find((donor) => donor.id === selectedId) ?? visible[0]; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Trophy} eyebrow="Impact · Recognition Review" title="Explain the ranking before you celebrate it." description="Review synthetic donor records, privacy-safe labels, cause filters, and ranking evidence requirements. This page does not publish donor amounts, identify people, transfer funds, award badges, or claim impact." badge="Preview recognition workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><FileCheck2 className="mr-2 size-4" />Save local review</Button><Button onClick={() => { setQuery(""); setCause("All causes"); setSaved(false); }} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset review</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Visible donors", value: String(visible.length), hint: "Synthetic records", icon: UsersRound }, { label: "Amounts", value: "Unavailable", hint: "No transaction source", icon: CircleDollarSign, tone: "violet" }, { label: "Ranking", value: "Undefined", hint: "Method not configured", icon: Trophy, tone: "amber" }, { label: "Impact", value: "Unavailable", hint: "No outcome evidence", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="Recognition evidence boundary">A leaderboard is not proof of contribution, identity, consent, ranking fairness, campaign progress, or impact. A responsible recognition system needs a defined period and metric, deduplicated transaction source, privacy choice, anti-gaming rules, eligibility, dispute path, and evidence that beneficiaries were actually served.</ScreenPreviewBanner><section className="flex flex-col gap-4 sm:flex-row"><div className="relative flex-1"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search synthetic donor labels" className="border-white/10 bg-black/20 pl-9 text-white placeholder:text-slate-500" /></div><div className="flex flex-wrap gap-2">{CAMPAIGNS.map((item) => <button key={item} onClick={() => setCause(item)} className={`rounded-xl border px-3 py-2 text-sm ${cause === item ? "border-cyan-300/40 bg-cyan-300/[0.1] text-cyan-200" : "border-white/10 text-slate-400"}`}>{item}</button>)}</div></section><section className="grid gap-6 lg:grid-cols-[1fr_1fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="flex items-center justify-between"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Recognition queue</p><Badge variant="outline" className="border-amber-300/20 text-amber-200">{cause}</Badge></div><div className="mt-5 space-y-3">{visible.map((donor, index) => <button key={donor.id} onClick={() => setSelectedId(donor.id)} className={`w-full rounded-xl border p-4 text-left ${selected?.id === donor.id ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-black/15"}`}><div className="flex items-center gap-3"><span className="flex size-8 items-center justify-center rounded-full bg-white/5 text-sm text-slate-400">{index + 1}</span><div className="flex-1"><p className="font-semibold">{donor.name}</p><p className="mt-1 text-xs text-slate-500">{donor.campaigns} campaign associations</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{donor.state}</Badge></div><div className="mt-3 flex items-center justify-between text-xs"><span className="text-slate-500">Contribution total</span><span className="text-violet-200">{donor.total}</span></div></button>)}{visible.length === 0 && <p className="py-8 text-center text-sm text-slate-500">No synthetic donor labels match this search.</p>}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6">{selected ? <><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Selected record</p><h2 className="mt-2 text-2xl font-black">{selected.name}</h2><p className="mt-2 text-sm text-slate-400">{selected.state} · privacy review</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Synthetic</Badge></div><p className="mt-6 text-sm leading-6 text-slate-300">{selected.note}</p><div className="mt-6 space-y-2">{["Donor identity and consent", "Transaction hash and source", "Campaign attribution", "Privacy and anonymity choice", "Anti-gaming and duplicate policy", "Recognition dispute path", "Impact and beneficiary evidence"].map((item) => <div key={item} className="flex items-center gap-3 rounded-lg border border-white/10 bg-black/15 p-3"><Info className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Unavailable</span></div>)}</div>{saved && <div className="mt-5 flex items-center gap-2 rounded-xl border border-cyan-300/20 bg-cyan-300/[0.06] p-4 text-sm text-cyan-200"><Check className="size-4" />Local recognition review saved; no identity or ranking changed.</div>}</> : <p className="text-sm text-slate-500">Select a synthetic record.</p>}</CardContent></Card></section><section className="grid gap-4 md:grid-cols-3"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><EyeOff className="size-5 text-cyan-300" /><h3 className="mt-3 font-semibold">Privacy is a choice</h3><p className="mt-2 text-sm leading-6 text-slate-400">Anonymous recognition must not be reversed through wallet, campaign, or amount inference.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><ShieldAlert className="size-5 text-violet-300" /><h3 className="mt-3 font-semibold">Rankings need method</h3><p className="mt-2 text-sm leading-6 text-slate-400">Period, metric, eligibility, deduplication, and anti-gaming rules define what a rank means.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><BarChart3 className="size-5 text-amber-300" /><h3 className="mt-3 font-semibold">No fake impact</h3><p className="mt-2 text-sm leading-6 text-slate-400">Donor recognition is not proof that a beneficiary outcome occurred.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Recognition needs consent", description: "Separate public display, anonymous labels, and private contribution records.", icon: EyeOff, status: "Pattern" }, { title: "Rankings need provenance", description: "A score or position needs a defined period, source, and dispute path.", icon: Trophy, status: "Required" }, { title: "No fake donor ledger", description: "Synthetic labels are not donors, amounts, transactions, badges, or impact.", icon: WifiOff, status: "Guardrail" }]} /></main></div>; }
