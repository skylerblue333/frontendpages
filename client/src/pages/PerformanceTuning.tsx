import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Activity, BarChart3, Check, Database, Gauge, LockKeyhole, RefreshCw, Search, ServerCog, ShieldCheck, TriangleAlert, WifiOff, Zap } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

type Area = "frontend" | "api" | "database" | "delivery";
type Opportunity = { id: string; title: string; area: Area; impact: "High" | "Medium"; confidence: "Needs measurement" | "Design review"; detail: string; proof: string };
const opportunities: Opportunity[] = [
  { id: "bundle", title: "Measure route-level bundle cost", area: "frontend", impact: "High", confidence: "Needs measurement", detail: "Profile real route bundles before adding code-splitting or removing dependencies.", proof: "Bundle analyzer, route load trace, device/network matrix" },
  { id: "renders", title: "Audit unnecessary renders", area: "frontend", impact: "Medium", confidence: "Needs measurement", detail: "Use a profiler to identify render cascades before memoization or state changes.", proof: "React profiler capture and interaction trace" },
  { id: "nplus1", title: "Check query fan-out", area: "api", impact: "High", confidence: "Needs measurement", detail: "Inspect request traces and database spans for N+1 behavior before caching.", proof: "Trace IDs, query count, p95/p99 windows, representative workload" },
  { id: "validation", title: "Bound expensive validation", area: "api", impact: "Medium", confidence: "Design review", detail: "Define input limits, timeouts, pagination, and retry semantics without hiding errors.", proof: "Contract tests, timeout budget, error taxonomy" },
  { id: "indexes", title: "Review slow query indexes", area: "database", impact: "High", confidence: "Needs measurement", detail: "Use query plans and representative data before changing indexes or constraints.", proof: "EXPLAIN plans, production-like dataset, migration rollback" },
  { id: "pool", title: "Verify connection-pool behavior", area: "database", impact: "Medium", confidence: "Needs measurement", detail: "Measure saturation, queueing, and recovery before changing pool limits.", proof: "Pool telemetry, load test, failure/recovery trace" },
  { id: "images", title: "Audit image and asset weight", area: "delivery", impact: "Medium", confidence: "Needs measurement", detail: "Compare formats, dimensions, cache headers, and perceived loading on target devices.", proof: "Web performance trace and asset inventory" },
  { id: "errors", title: "Improve failure observability", area: "delivery", impact: "High", confidence: "Design review", detail: "Make errors actionable with structured context while excluding secrets and personal data.", proof: "Redaction test, event schema, alert/runbook review" },
];
export default function PerformanceTuning() {
  const [area, setArea] = useState<Area>("frontend");
  const [impact, setImpact] = useState("All");
  const [query, setQuery] = useState("");
  const [selected, setSelected] = useState(opportunities[0]);
  const [checked, setChecked] = useState<string[]>([]);
  const [saved, setSaved] = useState(false);
  const filtered = useMemo(() => opportunities.filter((item) => item.area === area && (impact === "All" || item.impact === impact) && (!query || `${item.title} ${item.detail} ${item.proof}`.toLowerCase().includes(query.toLowerCase()))), [area, impact, query]);
  const reset = () => { setArea("frontend"); setImpact("All"); setQuery(""); setSelected(opportunities[0]); setChecked([]); setSaved(false); };
  const toggleCheck = (id: string) => setChecked((items) => items.includes(id) ? items.filter((item) => item !== id) : [...items, id]);
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Zap} eyebrow="PerformanceTuning · Optimization planner" title="Tune from evidence, not impressive-looking numbers." description="Prioritize local optimization opportunities across frontend, API, database, and delivery. No live metrics, active-user count, transaction volume, success rate, response time, automation result, or SLO claim is fabricated." badge="Tuning preview"><div className="flex flex-wrap gap-2"><Button disabled={checked.length === 0} onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Plan saved locally" : "Save tuning plan"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset planner</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Opportunities", value: String(opportunities.length), hint: "Typed local ideas", icon: Zap, tone: "cyan" }, { label: "Selected checks", value: String(checked.length), hint: "Local checklist", icon: Check, tone: "violet" }, { label: "Live metrics", value: "Off", hint: "No telemetry", icon: WifiOff, tone: "amber" }, { label: "Automation", value: "Off", hint: "No mutation", icon: LockKeyhole, tone: "slate" }]} /><ScreenPreviewBanner title="Performance-tuning evidence boundary"><strong>This is a prioritization worksheet, not a tuning result.</strong> Impact, confidence, opportunity count, and selected actions are local planning labels. They do not prove faster response time, lower cost, higher success rate, uptime, active users, throughput, or SLO compliance. Measure before and after every production change.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.82fr_1fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Optimization areas</p><h2 className="mt-2 text-2xl font-black">Choose a workstream</h2><div className="mt-5 space-y-2">{([{ id: "frontend", label: "Frontend experience", icon: Activity }, { id: "api", label: "API and services", icon: ServerCog }, { id: "database", label: "Database behavior", icon: Database }, { id: "delivery", label: "Delivery and reliability", icon: Gauge }] as const).map((item) => <button key={item.id} onClick={() => { setArea(item.id); setSelected(opportunities.find((op) => op.area === item.id) ?? opportunities[0]); }} className={`flex w-full items-center gap-3 rounded-xl border p-3 text-left ${area === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10 text-slate-400"}`}><item.icon className="size-4" /><span className="font-semibold">{item.label}</span><span className="ml-auto text-xs text-slate-500">{opportunities.filter((op) => op.area === item.id).length} ideas</span></button>)}</div><p className="mt-6 text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Filters</p><div className="mt-2 flex flex-wrap gap-2">{["All", "High", "Medium"].map((item) => <button key={item} onClick={() => setImpact(item)} className={`rounded-lg border px-3 py-2 text-xs ${impact === item ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-200" : "border-white/10 text-slate-500"}`}>{item} impact</button>)}</div><div className="relative mt-4"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search opportunities…" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-9 pr-3 text-sm text-white outline-none focus:border-cyan-300/50" /></div><div className="mt-6 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex gap-3"><TriangleAlert className="mt-0.5 size-5 shrink-0 text-amber-200" /><p className="text-sm leading-6 text-slate-300">Do not optimize from invented metrics. Capture a baseline, define a hypothesis, change one variable, compare safely, and keep rollback evidence.</p></div></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">{area} opportunities</p><h2 className="mt-2 text-2xl font-black">Plan the next measurement</h2><div className="mt-5 space-y-3">{filtered.length === 0 ? <div className="rounded-xl border border-dashed border-white/15 p-8 text-center"><p className="font-semibold">No opportunities match</p><p className="mt-2 text-sm text-slate-500">Change the local filters.</p></div> : filtered.map((item) => <button key={item.id} onClick={() => setSelected(item)} className={`w-full rounded-xl border p-4 text-left ${selected.id === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-center justify-between gap-3"><p className="font-semibold">{item.title}</p><div className="flex gap-2"><Badge variant="outline" className="border-white/10 text-amber-200">{item.impact}</Badge><Badge variant="outline" className="border-white/10 text-slate-400">{item.confidence}</Badge></div></div><p className="mt-2 text-sm leading-5 text-slate-400">{item.detail}</p></button>)}</div><div className="mt-5 rounded-xl border border-white/10 p-4"><p className="text-xs text-slate-500">Selected opportunity</p><p className="mt-2 text-lg font-black">{selected.title}</p><p className="mt-2 text-sm leading-6 text-slate-400">Measurement needed: {selected.proof}</p><button onClick={() => toggleCheck(selected.id)} className={`mt-4 flex items-center gap-3 rounded-xl border px-3 py-2 text-sm ${checked.includes(selected.id) ? "border-cyan-300/40 bg-cyan-300/[0.06] text-cyan-200" : "border-white/10 text-slate-400"}`}><span className="flex size-5 items-center justify-center rounded border border-white/20">{checked.includes(selected.id) ? "✓" : ""}</span>{checked.includes(selected.id) ? "Added to local plan" : "Add measurement to local plan"}</button></div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Production checklist</p><h2 className="mt-2 text-2xl font-black">Optimization must prove</h2><div className="mt-5 space-y-3">{["Baseline and after metrics with defined workload, device/network, window, and statistical method", "Correctness, security, accessibility, cost, and reliability regression checks", "Safe rollout, feature flag, canary, rollback, migration, and incident plan", "Trace/log/query evidence without secrets, tokens, personal data, or misleading aggregation", "Review by owners with documented decision, tradeoff, support/runbook, and audit trail"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><LockKeyhole className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake tuning result</h2><p className="mt-3 text-sm leading-6 text-slate-400">Selecting or saving a local opportunity changes planner state only. It does not change code, deploy a fix, run automation, measure performance, or prove an SLO improvement.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Four workstreams preserved", description: "Frontend, API, database, and delivery tuning concepts are interactive and measurable.", icon: Zap, status: "Planning" }, { title: "Measurement precedes action", description: "Every opportunity names the evidence needed before optimization or automation.", icon: BarChart3, status: "Guardrail" }, { title: "No live result is claimed", description: "No active users, transactions, success rate, response time, uptime, or SLO improvement is fabricated.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
