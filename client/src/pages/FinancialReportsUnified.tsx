import { FileBarChart, LockKeyhole, ShieldCheck } from "lucide-react";
import { PageHeader } from "@/components/PageHeader";

export default function FinancialReportsUnified() {
  return <div className="container max-w-6xl animate-page-in py-8"><PageHeader icon={FileBarChart} title="Financial Reports" subtitle="Unified reporting surface for commerce and financial infrastructure" /><div className="mb-8 rounded-3xl border border-emerald-300/20 bg-emerald-300/[0.05] p-6"><div className="flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.2em] text-emerald-200"><ShieldCheck className="size-4" />Reconciliation-first reporting</div><h2 className="mt-3 text-3xl font-black">Reports are only as trustworthy as their source ledger.</h2><p className="mt-3 max-w-3xl text-sm leading-6 text-muted-foreground">This closes a financial-reporting frontend gap without creating fake revenue, volume, balances, margins, burns, or payouts.</p></div><div className="grid gap-4 md:grid-cols-4">{["Revenue", "Fees", "Refunds", "Net settlement"].map(label => <div key={label} className="card p-5"><p className="text-xs uppercase tracking-widest text-muted-foreground">{label}</p><p className="mt-3 text-2xl font-black text-amber-200">Unavailable</p><p className="mt-2 text-xs text-muted-foreground">Ledger source required</p></div>)}</div><div className="mt-8 card p-6"><div className="flex min-h-56 items-center justify-center rounded-2xl border border-dashed border-border/60 text-center"><div><FileBarChart className="mx-auto size-9 text-muted-foreground/50" /><p className="mt-3 font-semibold">No reconciled reporting period</p><p className="mt-2 max-w-md text-sm text-muted-foreground">Connect the event-sourced financial ledger, currency rules, refunds, chargebacks, and period-close process before publishing a report.</p></div></div></div><div className="mt-6 flex items-center gap-2 text-sm text-muted-foreground"><LockKeyhole className="size-4 text-amber-200" />Financial claims remain locked until independently reconcilable evidence is available.</div></div>;
}
