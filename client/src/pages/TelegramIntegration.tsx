import { useState } from "react";
import { Bot, Check, Globe2, KeyRound, LockKeyhole, MessageCircle, RefreshCw, ShieldAlert, Webhook, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";
const modes = [{ name: "Bot connection", detail: "Requires approved bot ownership, server-side token storage, rotation, scope review, and authorization evidence.", state: "Unconfigured" }, { name: "Webhook delivery", detail: "Requires verified endpoint ownership, secret validation, event retries, redaction, rate limits, and delivery monitoring.", state: "Unavailable" }, { name: "Chat notifications", detail: "Requires chat consent, recipient identity, notification preferences, privacy, and auditable delivery receipts.", state: "Blocked" }];
export default function TelegramIntegration() { const [selected, setSelected] = useState(0); const [saved, setSaved] = useState(false); const [refreshing, setRefreshing] = useState(false); const [status, setStatus] = useState("Telegram service unavailable. Showing integration structure only."); const retry = () => { setRefreshing(true); setStatus("Retrying Telegram provider…"); window.setTimeout(() => { setRefreshing(false); setStatus("Telegram service unavailable. No verified bot or webhook source was connected."); }, 500); }; const reset = () => { setSelected(0); setSaved(false); setStatus("Telegram service unavailable. Showing integration structure only."); }; const mode = modes[selected] ?? modes[0]; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Bot} eyebrow="Telegram integration · Connectivity preview" title="Design reliable messaging without inventing bots, chats, users, or delivery." description="Review a local Telegram integration workspace with connection modes, bot authorization, webhook integrity, chat consent, notification preferences, retries, redaction, audit, save/reset feedback, and explicit evidence boundaries. No bot, chat, message, notification, or synchronization is connected." badge="Evidence-bounded integration"><div className="flex flex-wrap gap-2"><Button onClick={() => { setSaved(true); setStatus("Integration view saved locally. No connection or message was created."); }} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save integration view"}</Button><Button onClick={retry} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className={`mr-2 size-4 ${refreshing ? "animate-spin" : ""}`} />{refreshing ? "Retrying source" : "Retry Telegram source"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><X className="mr-2 size-4" />Reset integration</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Bot", value: "Unconfigured", hint: "No token source", icon: Bot, tone: "cyan" }, { label: "Webhook", value: "Unavailable", hint: "No endpoint source", icon: Webhook, tone: "violet" }, { label: "Messages", value: "Blocked", hint: "No chat consent", icon: MessageCircle, tone: "amber" }, { label: "Privacy", value: "Required", hint: "No policy source", icon: LockKeyhole, tone: "slate" }]} /><ScreenPreviewBanner title="Telegram evidence boundary"><strong>This is a local integration preview, not evidence that a bot, token, chat, user, message, notification, webhook, delivery receipt, or synchronization exists.</strong> Mode cards, local saved state, retry feedback, and disabled connect/send/subscribe actions are browser concepts. No account, privacy, security, or delivery conclusion is asserted.</ScreenPreviewBanner><section className="grid gap-4 md:grid-cols-3">{modes.map((item, index) => <button key={item.name} onClick={() => setSelected(index)} className={`rounded-2xl border p-5 text-left transition ${selected === index ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10 bg-white/[0.04]"}`}><div className="flex items-start justify-between"><div className="flex size-11 items-center justify-center rounded-2xl bg-cyan-300/10 text-cyan-200"><Bot className="size-5" /></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.state}</Badge></div><h2 className="mt-4 font-bold">{item.name}</h2><p className="mt-3 text-sm leading-6 text-slate-400">{item.detail}</p></button>)}</section><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200">Selected connection concept</p><div className="mt-2 flex items-start justify-between"><h2 className="text-2xl font-black">{mode.name}</h2><Badge variant="outline" className="border-amber-300/20 text-amber-200">{mode.state}</Badge></div><p className="mt-3 text-sm leading-6 text-slate-400">{mode.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Bot ownership / token", value: "Required", icon: KeyRound }, { label: "Chat / user scope", value: "Unavailable", icon: MessageCircle }, { label: "Webhook / delivery", value: "Unverified", icon: Webhook }, { label: "Privacy / redaction", value: "Required", icon: LockKeyhole }, { label: "Retries / rate limits", value: "Unconfigured", icon: RefreshCw }, { label: "Audit / receipts", value: "Blocked", icon: ShieldAlert }].map(({ label, value, icon: Icon }) => <div key={label} className="flex items-center gap-3 rounded-xl border border-white/10 p-4"><Icon className="size-4 text-cyan-300" /><span className="flex-1 text-sm text-slate-300">{label}</span><span className="text-xs text-amber-200">{value}</span></div>)}</div><div className="mt-6 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Connect unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Send unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Subscribe unavailable</Button></div></CardContent></Card><ScreenStatePanel type="unavailable" title="Telegram provider unavailable" description="Connect an approved bot, server-side secret handling, webhook verification, chat authorization, privacy, retries, redaction, notification, and delivery monitoring before enabling the integration." /></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><Globe2 className="size-5 text-cyan-300" /><div><p className="font-bold">Integration evidence contract</p><p className="mt-1 text-sm text-slate-400">Production messaging requires bot ownership, secret rotation, least privilege, user consent, endpoint verification, idempotency, rate limits, redaction, audit, delivery receipts, and recovery.</p></div></div><div className="mt-5 grid gap-3 md:grid-cols-2">{["Approved bot ownership, server-side token storage, rotation, and access scope", "Webhook or polling integrity, endpoint verification, retries, and idempotency", "Chat and user identity, consent, notification preferences, and opt-out", "Message validation, redaction, rate limits, abuse controls, and privacy", "Delivery state, receipts, monitoring, audit history, correction, and recovery", "Account isolation, incident response, secret revocation, and support"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3 text-sm text-slate-300"><LockKeyhole className="size-4 text-amber-300" />{item}<span className="ml-auto text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Integration surface preserved", description: "Bot, webhook, chat, notifications, privacy, retries, redaction, audit, and connection gates remain visible.", icon: Bot, status: "Local preview" }, { title: "No delivery theater", description: "Tokens, bots, chats, users, messages, notifications, receipts, and synchronization are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before connection", description: "Real integrations require ownership, authorization, privacy, delivery integrity, monitoring, and recovery controls.", icon: LockKeyhole, status: "Blocked" }]} /><p aria-live="polite" className="rounded-xl border border-white/10 bg-white/[0.03] p-4 text-sm text-slate-400">{status}</p></main></div>; }
