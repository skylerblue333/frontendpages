import { useMemo, useState } from "react";
import { AlertTriangle, Check, KeyRound, LockKeyhole, Mail, RefreshCw, ShieldAlert, ShieldCheck, UserRound, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
export default function Signin() { const [email, setEmail] = useState(""); const [loading, setLoading] = useState(false); const [saved, setSaved] = useState(false); const [status, setStatus] = useState("No authentication attempt"); const [showGates, setShowGates] = useState(false); const [showDemo, setShowDemo] = useState(false); const emailIntent = useMemo(() => email.length > 3 ? "Local format intent" : "Not entered", [email]); const simulateReview = () => { setLoading(true); setStatus("Local review only; no credentials submitted"); window.setTimeout(() => setLoading(false), 450); }; const reset = () => { setEmail(""); setLoading(false); setSaved(false); setStatus("No authentication attempt"); setShowGates(false); setShowDemo(false); }; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={ShieldCheck} eyebrow="Sign in · Authentication readiness preview" title="Review access states without fabricating a session." description="Explore a local sign-in readiness workspace with email intent, password-policy guidance, MFA, SSO, recovery, demo, loading, and failure states. No credentials are stored, no authentication is attempted, no session is created, and no redirect or account access is claimed." badge="Evidence-bounded authentication workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save view locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review auth gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset view</Button></div></ScreenHero><main className="mx-auto max-w-6xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Identity", value: email ? "Local intent" : "Not entered", hint: "No account source", icon: UserRound, tone: "cyan" }, { label: "Credentials", value: "Not collected", hint: "No password field", icon: KeyRound, tone: "violet" }, { label: "MFA", value: "Unverified", hint: "No enrollment", icon: ShieldCheck, tone: "amber" }, { label: "Session", value: "Not created", hint: "No auth attempt", icon: LockKeyhole, tone: "slate" }]} /><ScreenPreviewBanner title="Authentication evidence boundary"><strong>This is a local sign-in design preview, not evidence that an identity, credential, account, MFA factor, SSO provider, session, redirect, or protected resource access exists.</strong> Email input, password guidance, local loading state, saved state, recovery and SSO concepts, and disabled authentication actions are browser concepts. No password or token is stored, no request is sent, no session is created, and no sign-in success or failure from a real provider is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1.05fr_0.95fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="space-y-5 p-6"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Local sign-in review</p><h2 className="mt-2 text-2xl font-black">Access intent</h2><p className="mt-2 text-sm leading-6 text-slate-400">Do not enter a real password, recovery code, seed phrase, or sensitive personal information into this preview.</p></div><label className="text-sm font-semibold text-slate-300">Email intent<input value={email} onChange={(event) => setEmail(event.target.value)} placeholder="No real email required" type="email" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 font-normal text-white outline-none" /><span className="mt-2 block text-xs text-amber-200">{emailIntent}; no address validation, account lookup, or identity proof is connected.</span></label><div className="rounded-xl border border-white/10 p-4"><div className="flex items-start gap-3"><KeyRound className="mt-0.5 size-5 text-cyan-300" /><div><p className="font-semibold">Password-policy intent</p><p className="mt-2 text-sm leading-6 text-slate-500">No password field is collected. A real flow requires server-side hashing, breach checks, rate limits, bot defense, recovery, session rotation, and secret redaction.</p></div></div></div><div className="flex flex-wrap gap-2"><Button onClick={simulateReview} disabled={loading} className="bg-slate-700 text-slate-300">{loading ? "Reviewing locally…" : "Review sign-in locally"}</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Authenticate unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Continue with SSO unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Recover account unavailable</Button></div><div className="rounded-xl border border-white/10 p-4"><p className="font-semibold">Local state</p><p className="mt-2 text-sm leading-6 text-slate-500">{status}</p></div>{showGates && <div className="rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No session or access claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local sign-in review does not prove credentials, identity, MFA, SSO, session creation, token issuance, redirect, or protected-resource access.</p></div>}</CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="space-y-5 p-6"><div className="flex items-start gap-3"><AlertTriangle className="mt-0.5 size-5 text-amber-200" /><div><p className="font-semibold">Safe failure boundary</p><p className="mt-2 text-sm leading-6 text-slate-500">No fake success, demo token, localStorage auth token, account email, dashboard redirect, or provider response is used.</p></div></div><div className="rounded-xl border border-white/10 p-5"><Mail className="size-6 text-violet-300" /><p className="mt-3 font-semibold">Recovery and verification</p><p className="mt-2 text-sm leading-6 text-slate-500">No reset email, code, recovery token, device challenge, email ownership, or account lockout state is connected.</p><Button disabled className="mt-4 bg-slate-700 text-slate-400">Send recovery unavailable</Button></div><div className="rounded-xl border border-white/10 p-5"><ShieldCheck className="size-6 text-cyan-300" /><p className="mt-3 font-semibold">MFA and SSO concepts</p><p className="mt-2 text-sm leading-6 text-slate-500">No authenticator enrollment, OAuth provider, scope, callback, device trust, session cookie, or account protection outcome is available.</p><Button disabled className="mt-4 bg-slate-700 text-slate-400">Enroll MFA unavailable</Button></div><Button onClick={() => setShowDemo((value) => !value)} variant="outline" className="w-full border-white/10 text-slate-300">{showDemo ? "Hide demo boundary" : "Review demo boundary"}</Button>{showDemo && <div className="rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No demo credentials</p><p className="mt-2 text-sm leading-6 text-slate-400">No demo email, password, token, identity, entitlement, or login result is provided. Any real credentials must remain server-side and must never be hardcoded into a preview.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Authentication-governance gates</p><h2 className="mt-2 text-2xl font-black">What a real sign-in flow must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Server-side credential verification, hashing, breach checks, rate limits, bot defense, CSRF, tenant isolation, safe errors, and token handling", "Authenticated identity, MFA, SSO, callback validation, session rotation, secure cookies, device trust, logout, revocation, and recovery", "Email, wallet, crypto, NFT, financial, education, AI, marketplace, community, and security access claims require separate domain evidence", "No secrets, passwords, tokens, private keys, seed phrases, or personal data may be logged, persisted in browser storage, or exposed", "Authenticate, recover, enroll, verify, logout, accessibility, confirmation, retry, and accountable approval require governed operations", "A sign-in preview must not be presented as successful login, account access, identity verification, protected resource access, or security outcome without evidence"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Sign-in surface preserved", description: "Email intent, password policy, MFA, SSO, recovery, demo, loading, failure, save/reset, and blocked authentication actions remain visible.", icon: ShieldCheck, status: "Local auth UX" }, { title: "No fake access", description: "Credentials, tokens, sessions, redirects, account identities, provider responses, and protected-resource access are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before authentication", description: "Real sign-in requires secure server-side auth, session management, recovery, authorization, audit, and safe failure handling.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>; }
