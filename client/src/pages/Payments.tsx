import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ArrowRight, Banknote, Check, Coins, CreditCard, History, LockKeyhole, Package, RefreshCw, Search, ShieldCheck, TriangleAlert, WalletCards, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

type Tab = "plans" | "methods" | "history";
const plans = [{ name: "Basic", price: "10", detail: "Concept tier · credits unavailable", tone: "cyan" }, { name: "Execution", price: "25", detail: "Concept tier · execution unavailable", tone: "violet" }, { name: "Power", price: "50", detail: "Concept tier · priority unavailable", tone: "amber" }];
const methods = [{ label: "Card concept", detail: "Hosted fields required", icon: CreditCard }, { label: "Wallet concept", detail: "Address/network unavailable", icon: WalletCards }, { label: "Bank concept", detail: "Account details unavailable", icon: Banknote }];
const fixtureOrders = [{ id: "fixture-order-001", label: "Basic plan concept", amount: "$10.00 fixture", status: "Not charged" }, { id: "fixture-order-002", label: "Execution plan concept", amount: "$25.00 fixture", status: "Not charged" }];
export default function Payments() {
  const [tab, setTab] = useState<Tab>("plans");
  const [selectedPlan, setSelectedPlan] = useState(plans[0].name);
  const [selectedMethod, setSelectedMethod] = useState("");
  const [tokenIntent, setTokenIntent] = useState(false);
  const [query, setQuery] = useState("");
  const [acknowledged, setAcknowledged] = useState(false);
  const [saved, setSaved] = useState(false);
  const filteredOrders = useMemo(() => fixtureOrders.filter((item) => !query || `${item.id} ${item.label}`.toLowerCase().includes(query.toLowerCase())), [query]);
  const reset = () => { setTab("plans"); setSelectedPlan(plans[0].name); setSelectedMethod(""); setTokenIntent(false); setQuery(""); setAcknowledged(false); setSaved(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={CreditCard} eyebrow="Payments · Commerce preview" title="Explore the commerce surface without pretending money moved." description="Compare subscription concepts, funding-method patterns, local order fixtures, and SKY444 token-payment intent. No provider, token balance, order, charge, discount, burn, refund, or entitlement is verified." badge="Commerce preview"><div className="flex flex-wrap gap-2"><Button disabled={!acknowledged} onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Plan saved locally" : "Save local intent"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset payments</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Plans", value: "3", hint: "Concept tiers", icon: CreditCard, tone: "cyan" }, { label: "Method", value: selectedMethod ? "Selected" : "Unset", hint: "Local intent", icon: WalletCards, tone: "violet" }, { label: "Token balance", value: "Off", hint: "No SKY444 source", icon: Coins, tone: "amber" }, { label: "Orders", value: "0 verified", hint: "Fixtures only", icon: History, tone: "slate" }]} /><ScreenPreviewBanner title="Commerce evidence boundary"><strong>This is not a payment or subscription system.</strong> Prices, credits, discounts, token balances, token burns, orders, charges, refunds, payment methods, entitlements, and history are local concepts or fixtures. No checkout provider, wallet, ledger, account, database, or transaction source is connected.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.82fr_1fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Commerce surfaces</p><h2 className="mt-2 text-2xl font-black">Explore a local flow</h2><div className="mt-5 space-y-2">{([{ id: "plans", label: "Subscription concepts", icon: CreditCard }, { id: "methods", label: "Payment-method concepts", icon: WalletCards }, { id: "history", label: "Order-history fixtures", icon: History }] as const).map((item) => <button key={item.id} onClick={() => setTab(item.id)} className={`flex w-full items-center gap-3 rounded-xl border p-3 text-left ${tab === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10 text-slate-400"}`}><item.icon className="size-4" /><span className="font-semibold">{item.label}</span><span className="ml-auto text-xs text-slate-500">Preview</span></button>)}</div><div className="mt-6 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex gap-3"><TriangleAlert className="mt-0.5 size-5 shrink-0 text-amber-200" /><p className="text-sm leading-6 text-slate-300">Never enter card numbers, security codes, seed phrases, private keys, provider tokens, or real customer data into this preview.</p></div></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6">{tab === "plans" && <div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Subscription concepts</p><h2 className="mt-2 text-3xl font-black">Choose a plan fixture</h2><div className="mt-5 grid gap-3 md:grid-cols-3">{plans.map((plan) => <button key={plan.name} onClick={() => setSelectedPlan(plan.name)} className={`rounded-2xl border p-4 text-left ${selectedPlan === plan.name ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><p className="font-bold">{plan.name}</p><p className="mt-3 text-3xl font-black">${plan.price}<span className="text-sm font-normal text-slate-500"> / concept</span></p><p className="mt-3 text-xs leading-5 text-slate-500">{plan.detail}</p><Badge variant="outline" className="mt-4 border-white/10 text-amber-200">Not purchasable</Badge></button>)}</div><div className="mt-6 rounded-xl border border-cyan-300/20 bg-cyan-300/[0.05] p-4"><p className="font-semibold">Selected: {selectedPlan}</p><p className="mt-2 text-sm text-slate-400">Plan selection changes local state only. It does not subscribe, grant credits, or create an invoice.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-cyan-300/20 text-cyan-100/40">Subscribe unavailable</Button><Button onClick={() => setTokenIntent(!tokenIntent)} variant="outline" className="border-white/10 text-white hover:bg-white/10"><Coins className="mr-2 size-4" />{tokenIntent ? "Remove SKY444 intent" : "Add SKY444 token intent"}</Button></div></div>}{tab === "methods" && <div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Payment-method concepts</p><h2 className="mt-2 text-3xl font-black">Choose a funding pattern</h2><div className="mt-5 space-y-3">{methods.map((method) => <button key={method.label} onClick={() => setSelectedMethod(method.label)} className={`flex w-full items-center gap-3 rounded-xl border p-4 text-left ${selectedMethod === method.label ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><method.icon className="size-5 text-cyan-300" /><span className="flex-1"><span className="block font-semibold">{method.label}</span><span className="mt-1 block text-sm text-slate-500">{method.detail}</span></span><ArrowRight className="size-4 text-slate-600" /></button>)}</div><p className="mt-5 text-sm leading-6 text-slate-500">Selection is local intent only. Hosted fields, tokenization, vaulting, provider scope, lifecycle, and charge paths are unavailable.</p></div>}{tab === "history" && <div><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Order-history fixtures</p><h2 className="mt-2 text-3xl font-black">No verified orders</h2></div><Package className="size-8 text-slate-600" /></div><div className="relative mt-5"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search fixture orders…" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-9 pr-3 text-sm text-white outline-none focus:border-cyan-300/50" /></div><div className="mt-5 space-y-3">{filteredOrders.map((order) => <div key={order.id} className="flex items-center gap-3 rounded-xl border border-white/10 p-4"><Package className="size-5 text-slate-500" /><div className="flex-1"><p className="font-semibold">{order.label}</p><p className="mt-1 text-xs text-slate-500">{order.id} · {order.amount}</p></div><Badge variant="outline" className="border-white/10 text-amber-200">{order.status}</Badge></div>)}</div><p className="mt-5 text-sm leading-6 text-slate-500">These are synthetic fixtures, not account history, invoices, receipts, provider orders, or completed purchases.</p></div>}</CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Production checklist</p><h2 className="mt-2 text-2xl font-black">Commerce must prove</h2><div className="mt-5 space-y-3">{["Authenticated product, price, currency, tax, entitlement, and availability contracts", "Provider order/charge/refund/dispute/webhook/idempotency/reconciliation evidence", "Wallet balance, chain/network/address/decimal/fee/signature/receipt validation before token payment", "Privacy, PCI/compliance scope, fraud controls, audit, support, monitoring, and incident response", "End-to-end tests for success, decline, retry, duplicate, refund, dispute, and settlement states"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><LockKeyhole className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake economy</h2><p className="mt-3 text-sm leading-6 text-slate-400">Selecting a plan, method, order fixture, or SKY444 intent changes browser state only. It does not charge, discount, burn tokens, grant credits, create an order, or produce an entitlement.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Three commerce surfaces", description: "Plan concepts, method concepts, and order-history fixtures remain interactive.", icon: CreditCard, status: "Local preview" }, { title: "Token claim is blocked", description: "SKY444 token balance, discount, burn, signing, and receipt are unavailable rather than fabricated.", icon: Coins, status: "Guardrail" }, { title: "Order history is synthetic", description: "Fixture rows are clearly separated from authenticated account history and provider records.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
