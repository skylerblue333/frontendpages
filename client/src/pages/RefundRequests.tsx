import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Check, ClipboardList, DollarSign, Filter, LockKeyhole, RefreshCw, Search, ShieldAlert, Timer, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const cases = [{ id: 1, name: "Service-quality review", category: "Subscription", detail: "A local refund-case concept awaiting order identity, entitlement, policy, and payment evidence.", state: "Draft" }, { id: 2, name: "Duplicate-charge review", category: "Payments", detail: "A payment-review concept that must not imply a duplicate charge, amount, or refund eligibility.", state: "Evidence needed" }, { id: 3, name: "Digital-item dispute", category: "Digital", detail: "A digital-goods concept requiring delivery, access, fraud, and jurisdiction review.", state: "Unmeasured" }, { id: 4, name: "Education cancellation", category: "Education", detail: "An education case concept awaiting enrollment, cancellation terms, learner identity, and support ownership.", state: "Blocked" }];
export default function RefundRequests() {
  const [query, setQuery] = useState(""); const [category, setCategory] = useState("All"); const [selected, setSelected] = useState(1); const [reason, setReason] = useState("Reason not configured"); const [saved, setSaved] = useState(false); const [showGates, setShowGates] = useState(false);
  const categories = ["All", ...Array.from(new Set(cases.map((item) => item.category)))]; const filtered = useMemo(() => cases.filter((item) => (category === "All" || item.category === category) && `${item.name} ${item.category} ${item.detail}`.toLowerCase().includes(query.toLowerCase())), [category, query]); const request = cases.find((item) => item.id === selected) ?? cases[0];
  const reset = () => { setQuery(""); setCategory("All"); setSelected(1); setReason("Reason not configured"); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={ClipboardList} eyebrow="RefundRequests · Financial operations preview" title="Verify the entitlement before promising a refund." description="Explore local refund-case concepts with search, categories, request reasons, order and payment posture, eligibility, amounts, statuses, timelines, approvals, evidence, support, audit, save, reset, and gates. No live user, order, payment, amount, refund, or financial outcome is connected." badge="Refund workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "View saved locally" : "Save request locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review refund gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset request</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Cases", value: `${cases.length} local`, hint: "No requests loaded", icon: ClipboardList, tone: "cyan" }, { label: "Orders", value: "Unavailable", hint: "No order source", icon: DollarSign, tone: "violet" }, { label: "Eligibility", value: "Unassessed", hint: "No policy source", icon: Timer, tone: "amber" }, { label: "Refunds", value: "Blocked", hint: "No payment source", icon: ShieldAlert, tone: "slate" }]} /><ScreenPreviewBanner title="Refund evidence boundary"><strong>This is a local refund-case preview, not evidence that a refund is eligible, approved, issued, or owed.</strong> Case cards, reasons, order and payment posture, eligibility intent, amount state, status, timeline, saved state, approvals, support, and audit gates are browser concepts. No user, order, charge, currency amount, entitlement, refund, payout, or financial conclusion is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Search className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local refund cases" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-4 flex flex-wrap gap-2">{categories.map((entry) => <Button key={entry} onClick={() => setCategory(entry)} size="sm" variant="outline" className={category === entry ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-100" : "border-white/10 text-slate-400"}><Filter className="mr-1 size-3" />{entry}</Button>)}</div><div className="mt-6 space-y-3">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{item.name}</p><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.state}</Badge></div><div className="mt-4"><Badge variant="outline" className="border-white/10 text-slate-500">{item.category}</Badge></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected refund case</p><h2 className="mt-2 text-2xl font-black">{request.name}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{request.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Category", value: request.category }, { label: "State", value: request.state }, { label: "Order", value: "Unavailable" }, { label: "Amount", value: "Unavailable" }, { label: "Eligibility", value: "Unassessed" }, { label: "Timeline", value: "Unavailable" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><label className="mt-5 block text-sm text-slate-400">Request reason<select value={reason} onChange={(event) => setReason(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Reason not configured</option><option>Service issue intent</option><option>Duplicate charge intent</option><option>Cancellation intent</option><option>Unauthorized payment intent</option></select></label><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-8 text-center"><ClipboardList className="mx-auto size-8 text-slate-600" /><p className="mt-3 font-semibold">No refund evidence loaded</p><p className="mt-2 text-sm text-slate-500">Connect governed order, payment, entitlement, policy, and identity sources before submitting or deciding a request.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Submit unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Check eligibility unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Contact support unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No refund or amount claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local case does not prove a charge, entitlement, eligibility, approval, refund, or payment outcome.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Refund gates</p><h2 className="mt-2 text-2xl font-black">What a real refund workflow must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Authenticated user, order, payment, entitlement, merchant, jurisdiction, consent, and support ownership", "Amount, currency, tax, fees, discounts, settlement, payment method, ledger, and reconciliation", "Refund policy, terms, eligibility, deadlines, exceptions, evidence, fraud, chargeback, and abuse controls", "Approvals, permissions, idempotency, refund provider, status, retries, partial refunds, and failure handling", "Notifications, audit, privacy, deletion, legal requests, incident response, and user-visible timeline", "Payout, wallet or bank destination, tax reporting, support escalation, appeals, and recovery"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Refund surface preserved", description: "Cases, search, categories, reasons, orders, payments, eligibility, amounts, statuses, timelines, approvals, evidence, support, audit, save/reset, and gates remain interactive.", icon: ClipboardList, status: "Local cases" }, { title: "No financial theater", description: "Users, orders, charges, currency amounts, entitlements, eligibility, refunds, payouts, and financial outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before money movement", description: "Real refunds need authenticated records, policy review, approvals, provider status, reconciliation, audit, and support.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
