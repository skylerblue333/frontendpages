import { useState } from "react";
import { Activity, AlertTriangle, Check, Clock3, Cloud, Info, LockKeyhole, RefreshCw, ServerCog, ShieldAlert, Users, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

const services = [
  { name: "Core API concept", state: "Unmeasured", detail: "Requires authenticated health and readiness probes, freshness, environment, and release identity." },
  { name: "Web application concept", state: "Preview", detail: "Browser route verified only; CDN, deployment, runtime, and user-impact telemetry are not connected." },
  { name: "Data service concept", state: "Unavailable", detail: "Requires dependency health, query telemetry, migrations, capacity, backups, and recovery evidence." },
  { name: "Worker fleet concept", state: "Unknown", detail: "Requires scheduler, queue depth, retries, dead-letter handling, ownership, and alert evidence." },
];

export default function SystemStatus() {
  const [selected, setSelected] = useState(0);
  const [query, setQuery] = useState("");
  const [windowName, setWindowName] = useState("Current window not configured");
  const [saved, setSaved] = useState(false);
  const [showBoundary, setShowBoundary] = useState(false);
  const [refreshing, setRefreshing] = useState(false);
  const refresh = () => { setRefreshing(true); window.setTimeout(() => setRefreshing(false), 500); };
  const reset = () => { setSelected(0); setQuery(""); setWindowName("Current window not configured"); setSaved(false); setShowBoundary(false); setRefreshing(false); };
  const visible = services.filter((service) => `${service.name} ${service.state} ${service.detail}`.toLowerCase().includes(query.toLowerCase()));
  const service = services[selected] ?? services[0];
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Activity} eyebrow="System status · Health preview" title="Review service health without inventing uptime." description="Explore a local health-and-incident workspace with service inventory, probes, uptime, latency, capacity, deployments, incidents, alerts, ownership, maintenance, retry, save/reset, and explicit evidence boundaries. No live telemetry or production status is connected." badge="Evidence-bounded operations"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save status view"}</Button><Button onClick={() => setShowBoundary((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showBoundary ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showBoundary ? "Close evidence boundary" : "Review status gates"}</Button><Button onClick={refresh} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className={`mr-2 size-4 ${refreshing ? "animate-spin" : ""}`} />{refreshing ? "Retrying local state" : "Retry health source"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><X className="mr-2 size-4" />Reset status</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Services", value: "Unmapped", hint: "No inventory source", icon: ServerCog, tone: "cyan" }, { label: "Availability", value: "Unmeasured", hint: "No probe source", icon: Activity, tone: "violet" }, { label: "Incidents", value: "Unknown", hint: "No incident source", icon: AlertTriangle, tone: "amber" }, { label: "SLA", value: "Unverified", hint: "No contract source", icon: ShieldAlert, tone: "slate" }]} /><ScreenPreviewBanner title="System-status evidence boundary"><strong>This is a local operational-status preview, not evidence that any service is online, healthy, available, secure, deployed, incident-free, within SLA, or monitored.</strong> Service cards, filters, time-window intent, local saved state, retry feedback, and disabled live-query actions are browser concepts. No uptime, latency, capacity, deployment, incident, alert, security, SLA, or production outcome is asserted.</ScreenPreviewBanner><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="grid gap-4 md:grid-cols-[1.1fr_0.9fr]"><label className="text-sm font-semibold text-slate-300">Search services<input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search API, web, data, workers..." className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white outline-none" /></label><label className="text-sm font-semibold text-slate-300">Time window<select value={windowName} onChange={(event) => setWindowName(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white"><option>Current window not configured</option><option>Last hour intent</option><option>Last 24 hours intent</option><option>Seven-day intent</option></select></label></div></CardContent></Card><section className="grid gap-4 md:grid-cols-2">{visible.map((item) => <button key={item.name} onClick={() => setSelected(services.indexOf(item))} className={`rounded-2xl border p-5 text-left transition ${selected === services.indexOf(item) ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10 bg-white/[0.04]"}`}><div className="flex items-start justify-between"><div className="flex size-11 items-center justify-center rounded-2xl bg-cyan-300/10 text-cyan-200"><ServerCog className="size-5" /></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.state}</Badge></div><h2 className="mt-4 font-bold">{item.name}</h2><p className="mt-3 text-sm leading-6 text-slate-400">{item.detail}</p><div className="mt-4 flex items-center gap-2 text-xs text-slate-500"><Users className="size-3" />Owner unverified <Cloud className="ml-2 size-3" />Environment unknown</div></button>)}</section><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200">Selected service concept</p><h2 className="mt-2 text-2xl font-black">{service.name}</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{service.state}</Badge></div><p className="mt-3 text-sm leading-6 text-slate-400">{service.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Health / readiness", value: "Unmeasured", icon: Activity }, { label: "Uptime / latency", value: "Unavailable", icon: Clock3 }, { label: "Deployment / release", value: "Unverified", icon: Cloud }, { label: "Incident / alert", value: "Unknown", icon: AlertTriangle }, { label: "Capacity / errors", value: "No source", icon: ServerCog }, { label: "Ownership / escalation", value: "Required", icon: Users }].map(({ label, value, icon: Icon }) => <div key={label} className="flex items-center gap-3 rounded-xl border border-white/10 p-4"><Icon className="size-4 text-cyan-300" /><span className="flex-1 text-sm text-slate-300">{label}</span><span className="text-xs text-amber-200">{value}</span></div>)}</div><div className="mt-6 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Probe service unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Acknowledge incident unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Open runbook unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Export status unavailable</Button></div></CardContent></Card><ScreenStatePanel type="unavailable" title="Live status source unavailable" description="Connect authenticated health probes, deployment metadata, incident management, alerting, ownership, service contracts, and audit data before reporting operational status." /></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><Info className="size-5 text-cyan-300" /><div><p className="font-bold">Status evidence contract</p><p className="mt-1 text-sm text-slate-400">A trustworthy status view needs reproducible probes, timestamps, environment identity, release version, alert state, incident history, ownership, access controls, and audit logs.</p></div></div><div className="mt-5 grid gap-3 md:grid-cols-2">{["Authenticated health and readiness probes with freshness", "Latency, errors, capacity, availability, and dependency telemetry", "Deployment, release, rollback, migration, and maintenance evidence", "Incident, alert, ownership, escalation, and user-impact records", "Security controls, access policy, environment scope, and audit logs", "SLA, service windows, status definitions, and recovery runbooks"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3 text-sm text-slate-300"><LockKeyhole className="size-4 text-amber-300" />{item}<span className="ml-auto text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card>{showBoundary && <Card className="border-amber-300/20 bg-amber-300/[0.06]"><CardContent className="p-6"><p className="font-semibold text-amber-100">No uptime or incident-free claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local status surface does not prove service health, uptime, latency, capacity, deployment, security, incident state, SLA compliance, or production readiness.</p></CardContent></Card>}<ScreenFeatureGrid features={[{ title: "System status surface preserved", description: "Services, probes, uptime, latency, capacity, deployments, incidents, alerts, ownership, time windows, retry, save/reset, and evidence gates remain visible.", icon: Activity, status: "Local operations" }, { title: "No uptime theater", description: "Health, availability, incidents, latency, capacity, security, SLA, and production outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before reporting", description: "Real status requires probes, freshness, release identity, incident data, access controls, runbooks, and audit logs.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
