import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ArrowRight, BookOpen, Check, CheckCircle2, FileCheck2, HelpCircle, LockKeyhole, RefreshCw, ShieldCheck, Sparkles, Users, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const LESSONS = [{ id: "discover", title: "Discover the surface", detail: "Learn to distinguish a route, a preview, and a verified integration.", icon: Sparkles, tag: "Orientation" }, { id: "claims", title: "Read the boundary", detail: "Look for source, freshness, unavailable states, and the evidence required for a claim.", icon: ShieldCheck, tag: "Trust" }, { id: "actions", title: "Test an action", detail: "A button should expose loading, success, failure, retry, and safe unavailable states.", icon: CheckCircle2, tag: "UX" }, { id: "privacy", title: "Protect context", detail: "Do not enter secrets, health details, financial credentials, or personal identity into a preview.", icon: LockKeyhole, tag: "Privacy" }, { id: "navigation", title: "Recover deliberately", detail: "Use search, back, reset, and safe exit paths when a feature or integration is unavailable.", icon: ArrowRight, tag: "Recovery" }, { id: "recap", title: "Keep exploring", detail: "Finish with a local recap; completion is not enrollment, certification, or service activation.", icon: BookOpen, tag: "Recap" }];

export default function OnboardingTutorial() {
  const [current, setCurrent] = useState(0);
  const [completed, setCompleted] = useState<string[]>([]);
  const [focus, setFocus] = useState("Trust boundaries");
  const [saved, setSaved] = useState(false);
  const lesson = LESSONS[current];
  const Icon = lesson.icon;
  const mark = () => setCompleted((items) => items.includes(lesson.id) ? items : [...items, lesson.id]);
  const next = () => { mark(); if (current < LESSONS.length - 1) setCurrent(current + 1); };
  const reset = () => { setCurrent(0); setCompleted([]); setFocus("Trust boundaries"); setSaved(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={BookOpen} eyebrow="OnboardingTutorial · Guided preview" title="Learn how to inspect the feature before using it." description="Work through six local lessons on evidence, safe actions, privacy, recovery, and honest completion. No account, service, reward, certificate, personalization, or backend progress is claimed." badge="Tutorial preview"><div className="flex flex-wrap gap-2"><Button onClick={() => { mark(); setSaved(true); }} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Tutorial saved locally" : "Save tutorial locally"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset tutorial</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Lessons", value: `${current + 1}/${LESSONS.length}`, hint: "Local tutorial", icon: BookOpen }, { label: "Progress", value: `${Math.round(((current + 1) / LESSONS.length) * 100)}%`, hint: `${completed.length} completed locally`, icon: CheckCircle2, tone: "cyan" }, { label: "Focus", value: focus === "Trust boundaries" ? "Trust" : focus, hint: "Local preference", icon: HelpCircle, tone: "violet" }, { label: "Activation", value: "Off", hint: "No account/service", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="OnboardingTutorial evidence boundary"><strong>Lesson progress, focus selection, and local recap are tutorial fixtures—not account enrollment, personalization, service activation, rewards, certification, verified learning outcomes, or durable backend progress.</strong> Production tutorials require identity, persistence, versioned content, accessibility, analytics consent, recoverable checkpoints, localization, support, and clear completion semantics.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.8fr_1.2fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Lesson map</p><h2 className="mt-2 text-2xl font-black">Inspect the path</h2><div className="mt-5 space-y-2">{LESSONS.map((item, index) => { const ItemIcon = item.icon; const done = completed.includes(item.id); return <button key={item.id} onClick={() => setCurrent(index)} className={`flex w-full items-center gap-3 rounded-xl border p-3 text-left ${index === current ? "border-cyan-300/40 bg-cyan-300/[0.08]" : done ? "border-violet-300/20 bg-violet-300/[0.05]" : "border-white/10 bg-black/10"}`}><div className="flex size-8 items-center justify-center rounded-lg bg-white/5 text-cyan-200">{done ? <Check className="size-4" /> : <ItemIcon className="size-4" />}</div><div className="flex-1"><p className="text-sm font-black">{item.title}</p><p className="mt-1 text-xs text-slate-500">{item.tag}</p></div>{index === current && <Badge variant="outline" className="border-cyan-300/30 text-cyan-200">Now</Badge>}</button>; })}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-7"><div className="flex items-start gap-5"><div className="flex size-14 shrink-0 items-center justify-center rounded-2xl bg-gradient-to-br from-cyan-400/30 to-violet-400/30 text-cyan-100"><Icon className="size-7" /></div><div><div className="flex flex-wrap items-center gap-2"><Badge variant="outline" className="border-white/10 text-slate-400">Lesson {current + 1}</Badge><Badge variant="outline" className="border-violet-300/30 text-violet-200">{lesson.tag}</Badge></div><h2 className="mt-3 text-3xl font-black">{lesson.title}</h2><p className="mt-3 text-lg text-slate-300">{lesson.detail}</p></div></div><div className="mt-7 rounded-2xl border border-cyan-300/20 bg-cyan-300/[0.05] p-5"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200">Choose a local learning lens</p><div className="mt-4 flex flex-wrap gap-2">{["Trust boundaries", "Build habits", "Protect privacy", "Recover safely"].map((item) => <button key={item} onClick={() => setFocus(item)} className={`rounded-xl border px-3 py-2 text-xs ${focus === item ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-100" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div></div><div className="mt-7 flex flex-wrap gap-3"><Button onClick={() => setCurrent(Math.max(0, current - 1))} disabled={current === 0} variant="outline" className="border-white/10 text-white">Previous</Button><Button onClick={next} className="bg-gradient-to-r from-cyan-400 to-violet-500 text-slate-950 hover:from-cyan-300 hover:to-violet-400">{current === LESSONS.length - 1 ? "Complete locally" : "Mark and continue"}<ArrowRight className="ml-2 size-4" /></Button></div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Recap checklist</p><h2 className="mt-2 text-2xl font-black">Before claiming completion</h2><div className="mt-5 space-y-3">{["I can tell a preview from a verified integration", "I know where source, freshness, and unavailable states appear", "I will not enter secrets or sensitive personal data", "I can recover with reset, back, search, or safe exit", "I understand local completion is not certification or activation"].map((item, index) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><CheckCircle2 className={`size-4 ${completed.length > index ? "text-cyan-300" : "text-slate-500"}`} /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Review</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><FileCheck2 className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake completion</h2><p className="mt-3 text-sm leading-6 text-slate-400">This tutorial changes local state only. It does not enroll, certify, personalize, activate a service, issue rewards, collect analytics, or persist progress to an account.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "A lesson is not entitlement", description: "Local progress preserves guided UX without account, reward, certificate, or activation claims.", icon: BookOpen, status: "Guardrail" }, { title: "Trust is the outcome", description: "Evidence, privacy, accessibility, recovery, content versioning, and support need explicit contracts.", icon: ShieldCheck, status: "Required" }, { title: "No fake activation", description: "The preview makes no account, provider, reward, analytics, or backend request.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
