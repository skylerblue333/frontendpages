import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { AlertTriangle, Check, Clipboard, Copy, FileWarning, LockKeyhole, RefreshCw, ShieldAlert, Wallet, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

export default function ReceiveCryptoError() {
  const [copied, setCopied] = useState(false); const [retry, setRetry] = useState("Retry not attempted"); const [saved, setSaved] = useState(false); const [showGates, setShowGates] = useState(false);
  const reset = () => { setCopied(false); setRetry("Retry not attempted"); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={AlertTriangle} eyebrow="ReceiveCryptoError · Wallet recovery preview" title="Explain the failure before asking for a retry." description="Explore a local receiving-error recovery workspace with error context, network posture, retry intent, request identifiers, copy details, security guidance, support escalation, and recovery gates. No wallet, address, deposit, transaction, incident, or production outcome is connected." badge="Recovery workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setRetry(retry === "Retry not attempted" ? "Local retry intent" : "Retry not attempted")} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><RefreshCw className="mr-2 size-4" />Toggle local retry</Button><Button onClick={() => setSaved(true)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{saved ? "View saved locally" : "Save view locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review recovery gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset view</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Status", value: "Preview only", hint: "No error source", icon: AlertTriangle, tone: "cyan" }, { label: "Network", value: "Unknown", hint: "No chain context", icon: Wallet, tone: "violet" }, { label: "Request ID", value: "Unavailable", hint: "No service response", icon: Clipboard, tone: "amber" }, { label: "Incident", value: "Unknown", hint: "No incident source", icon: FileWarning, tone: "slate" }]} /><ScreenPreviewBanner title="Wallet-error evidence boundary"><strong>This is a local error-recovery preview, not evidence of a failed deposit or production incident.</strong> Error copy, network posture, retry intent, request-ID state, copied state, support guidance, saved state, and evidence gates are browser concepts. No wallet, address, balance, deposit, transaction, block, incident, outage, security event, or user impact is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1.05fr_0.95fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start gap-4"><div className="rounded-2xl bg-amber-300/10 p-3"><AlertTriangle className="size-7 text-amber-200" /></div><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Local recovery state</p><h2 className="mt-2 text-2xl font-black">Receiving action unavailable</h2><p className="mt-3 text-sm leading-6 text-slate-400">A governed wallet service would provide a safe, specific explanation here. No address or transaction context is loaded in this preview.</p></div></div><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Error detail", value: "Unavailable" }, { label: "Network", value: "Unknown" }, { label: "Asset", value: "Unknown" }, { label: "Request ID", value: "Unavailable" }, { label: "Retry", value: retry }, { label: "Support", value: "Not configured" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-7 text-center"><Wallet className="mx-auto size-7 text-slate-600" /><p className="mt-3 font-semibold">No transaction context loaded</p><p className="mt-2 text-sm text-slate-500">Do not retry, resend, or claim a deposit until wallet, network, address, and transaction state are verified.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Retry unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Open wallet unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Contact support unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No transaction or incident claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local error message does not prove a failed transfer, lost funds, outage, compromise, or recoverable transaction.</p></div>}</CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Safe recovery checklist</p><h2 className="mt-2 text-2xl font-black">Verify before trying again</h2><div className="mt-6 space-y-3">{["Confirm the connected wallet, asset, network, address, memo/tag, and testnet/mainnet context", "Check request identifier, service status, transaction hash, mempool, block, confirmations, and reorg state", "Protect against duplicate submissions, wrong-network deposits, address substitution, and unsafe retries", "Preserve user-safe wording, privacy, support ownership, escalation, and audit details", "Only mark recovery complete after reconciliation, chain evidence, and clear user-visible status"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div><div className="mt-6 rounded-xl border border-white/10 bg-black/10 p-4"><p className="font-semibold">Copy diagnostic details</p><p className="mt-2 text-sm text-slate-500">No request identifier or sensitive wallet detail is available to copy.</p><Button onClick={() => setCopied(true)} variant="outline" className="mt-4 border-white/10 text-white"><Copy className="mr-2 size-4" />{copied ? "Copied local state" : "Copy details"}</Button></div></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Recovery surface preserved", description: "Error copy, network and asset context, retry intent, request identifiers, copy details, security guidance, support, save/reset, and gates remain interactive.", icon: AlertTriangle, status: "Local recovery" }, { title: "No wallet-error theater", description: "Wallets, addresses, deposits, transactions, blocks, incidents, outages, security events, and user impact are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before retry", description: "Real recovery needs wallet and network context, transaction provenance, duplicate prevention, reconciliation, support, and audit.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
