import { useState } from "react";
import { Activity, BarChart3, Check, Download, Eye, Heart, Info, LockKeyhole, MessageCircle, RefreshCw, Search, ShieldAlert, Users, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatePanel, ScreenStatGrid } from "@/components/ScreenExperience";
const streams = [{ name: "Creator stream concept", status: "Unconnected", audience: "Unmeasured", engagement: "Unavailable" }, { name: "Community stream concept", status: "Preview", audience: "Unknown", engagement: "No source" }, { name: "Test stream concept", status: "Test-only", audience: "Local", engagement: "Not measured" }];
export default function StreamAnalytics() { const [query, setQuery] = useState(""); const [selected, setSelected] = useState(0); const [saved, setSaved] = useState(false); const [showBoundary, setShowBoundary] = useState(false); const [refreshing, setRefreshing] = useState(false); const refresh = () => { setRefreshing(true); window.setTimeout(() => setRefreshing(false), 500); }; const reset = () => { setQuery(""); setSelected(0); setSaved(false); setShowBoundary(false); setRefreshing(false); }; const filtered = streams.filter((item) => item.name.toLowerCase().includes(query.toLowerCase())); const stream = streams[selected] ?? streams[0]; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={BarChart3} eyebrow="Stream analytics · Audience preview" title="Measure stream concepts without inventing viewers or engagement." description="Explore a local stream-analytics workspace with stream search, viewers, engagement, retention, moderation, revenue, provenance, refresh, save/reset, and blocked live-query/export actions. No stream, viewer, audience, reaction, revenue, or business outcome is connected." badge="Evidence-bounded stream analytics"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save analytics view"}</Button><Button onClick={() => setShowBoundary((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showBoundary ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showBoundary ? "Close boundary" : "Review stream gates"}</Button><Button onClick={refresh} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className={`mr-2 size-4 ${refreshing ? "animate-spin" : ""}`} />{refreshing ? "Retrying local state" : "Retry analytics source"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><X className="mr-2 size-4" />Reset analytics</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Viewers", value: "Unmeasured", hint: "No audience source", icon: Eye, tone: "cyan" }, { label: "Engagement", value: "Unavailable", hint: "No reaction source", icon: Heart, tone: "violet" }, { label: "Retention", value: "Unknown", hint: "No cohort source", icon: Activity, tone: "amber" }, { label: "Revenue", value: "Unverified", hint: "No settlement source", icon: ShieldAlert, tone: "slate" }]} /><ScreenPreviewBanner title="Stream analytics evidence boundary"><strong>This is a local analytics preview, not evidence that a stream, viewer, audience, engagement, retention, moderation, subscription, donation, revenue, or business outcome exists.</strong> Stream cards, query filtering, local selection, visual trend scaffolding, saved state, retry feedback, and disabled live-query/export actions are browser concepts. No view, impression, chat, reaction, follower, subscriber, payment, revenue, or creator result is asserted.</ScreenPreviewBanner><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><label className="text-sm font-semibold text-slate-300"><span className="flex items-center gap-2"><Search className="size-4 text-slate-500" />Search stream concepts</span><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search creator, community, test..." className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white outline-none" /></label></CardContent></Card><section className="grid gap-4 lg:grid-cols-3">{filtered.map((item) => <button key={item.name} onClick={() => setSelected(streams.indexOf(item))} className={`rounded-2xl border p-5 text-left ${stream === item ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10 bg-white/[0.04]"}`}><div className="flex items-start justify-between"><div className="flex size-11 items-center justify-center rounded-2xl bg-cyan-300/10 text-cyan-200"><Activity className="size-5" /></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.status}</Badge></div><h2 className="mt-4 font-bold">{item.name}</h2><p className="mt-3 text-sm text-slate-400">Audience: {item.audience}</p><p className="mt-1 text-sm text-slate-500">Engagement: {item.engagement}</p></button>)}</section><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200">Selected stream</p><h2 className="mt-2 text-2xl font-black">{stream.name}</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{stream.status}</Badge></div><div className="mt-6 flex h-36 items-end gap-2">{[32, 48, 42, 61, 38, 54, 46, 67, 45, 58, 40, 52].map((height, index) => <div key={index} className="flex-1 rounded-t-md bg-gradient-to-t from-cyan-400/25 to-violet-400/70" style={{ height: `${height}%` }} />)}</div><p className="mt-4 text-sm text-slate-500">Bars are visual scaffolding, not viewers, watch time, retention, engagement, or revenue.</p></CardContent></Card><ScreenStatePanel type="unavailable" title="Live stream source unavailable" description="Connect authenticated stream telemetry, viewer identity policy, moderation events, engagement events, payment records, and privacy-reviewed retention data before reporting metrics." /></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="grid gap-3 md:grid-cols-3">{[{ title: "Audience", text: "No viewer, unique-user, geography, age, reach, impression, or watch-time source is connected.", icon: Users }, { title: "Engagement", text: "No chat, reaction, follow, subscriber, replay, moderation, or retention source is connected.", icon: MessageCircle }, { title: "Revenue", text: "No donation, gift, subscription, payout, fee, tax, settlement, or creator-account source is connected.", icon: ShieldAlert }].map(({ title, text, icon: Icon }) => <div key={title} className="rounded-xl border border-white/10 p-4"><Icon className="size-5 text-cyan-300" /><p className="mt-3 font-semibold">{title}</p><p className="mt-2 text-sm leading-6 text-slate-500">{text}</p></div>)}</div></CardContent></Card><div className="flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Load live analytics unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500"><Download className="mr-2 size-4" />Export unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Open moderation unavailable</Button></div>{showBoundary && <Card className="border-amber-300/20 bg-amber-300/[0.06]"><CardContent className="p-6"><p className="font-semibold text-amber-100">No audience or revenue claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local stream concept does not prove viewers, engagement, retention, moderation outcomes, subscriptions, donations, revenue, or creator business performance.</p></CardContent></Card>}<ScreenFeatureGrid features={[{ title: "Stream analytics surface preserved", description: "Streams, viewers, engagement, retention, moderation, revenue, provenance, search, retry, save/reset, and export gates remain visible.", icon: BarChart3, status: "Local analytics" }, { title: "No engagement theater", description: "Viewers, views, watch time, reactions, followers, subscriptions, payments, revenue, and outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before reporting", description: "Real stream analytics require telemetry, identity policy, privacy, moderation, payment reconciliation, and audit.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>; }

