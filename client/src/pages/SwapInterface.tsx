import { useState } from "react";
import { ArrowDownUp, Check, CircleDollarSign, Clock3, Info, KeyRound, LockKeyhole, RefreshCw, ShieldAlert, Wallet, X, Zap } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

const assets = [
  { name: "Skycoin concept", symbol: "SKY", state: "Unverified", detail: "Requires token registry, network, contract address, decimals, liquidity, and price provenance." },
  { name: "USD stablecoin concept", symbol: "USD", state: "Unconfigured", detail: "Requires supported network, issuer, reserves, contract verification, and current quote source." },
  { name: "Creator token concept", symbol: "CRT", state: "Unavailable", detail: "Requires issuer authorization, token metadata, liquidity policy, and market-data evidence." },
];

export default function SwapInterface() {
  const [from, setFrom] = useState(assets[0].symbol);
  const [to, setTo] = useState(assets[1].symbol);
  const [slippage, setSlippage] = useState("Not configured");
  const [saved, setSaved] = useState(false);
  const [showBoundary, setShowBoundary] = useState(false);
  const [refreshing, setRefreshing] = useState(false);
  const refresh = () => { setRefreshing(true); window.setTimeout(() => setRefreshing(false), 500); };
  const reset = () => { setFrom(assets[0].symbol); setTo(assets[1].symbol); setSlippage("Not configured"); setSaved(false); setShowBoundary(false); setRefreshing(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={ArrowDownUp} eyebrow="Swap interface · Transaction preview" title="Plan a token swap without inventing a quote or settlement." description="Explore a local swap-planning workspace with assets, wallet and network, amount intent, quote provenance, slippage, approvals, liquidity, route, fees, transaction review, retry, save/reset, and explicit execution boundaries. No wallet, balance, quote, rate, transaction, or financial outcome is connected." badge="Evidence-bounded swap planning"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save swap plan"}</Button><Button onClick={() => setShowBoundary((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showBoundary ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showBoundary ? "Close evidence boundary" : "Review swap gates"}</Button><Button onClick={refresh} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className={`mr-2 size-4 ${refreshing ? "animate-spin" : ""}`} />{refreshing ? "Retrying local state" : "Retry quote source"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><X className="mr-2 size-4" />Reset plan</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Wallet", value: "Unconnected", hint: "No address source", icon: Wallet, tone: "cyan" }, { label: "Quote", value: "Unavailable", hint: "No market source", icon: CircleDollarSign, tone: "violet" }, { label: "Approval", value: "Unverified", hint: "No contract source", icon: KeyRound, tone: "amber" }, { label: "Settlement", value: "Blocked", hint: "No transaction source", icon: ShieldAlert, tone: "slate" }]} /><ScreenPreviewBanner title="Swap evidence boundary"><strong>This is a local swap-design preview, not evidence that a wallet, balance, token, quote, rate, liquidity route, approval, gas estimate, transaction, settlement, or financial result exists.</strong> Asset selectors, amount intent, slippage controls, local saved state, retry feedback, and disabled connect/quote/approve/swap actions are browser concepts. No address, price, balance, token ownership, transaction hash, or gain/loss is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><ArrowDownUp className="size-5 text-cyan-300" /><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200">Swap intent</p><h2 className="mt-2 text-2xl font-black">Choose assets locally</h2></div></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold text-slate-300">From<select value={from} onChange={(event) => setFrom(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white"><option>SKY</option><option>USD</option><option>CRT</option></select></label><label className="text-sm font-semibold text-slate-300">To<select value={to} onChange={(event) => setTo(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white"><option>USD</option><option>SKY</option><option>CRT</option></select></label></div><div className="mt-5 rounded-2xl border border-white/10 bg-black/20 p-5"><div className="flex items-center justify-between"><span className="text-sm text-slate-400">Amount intent</span><Badge variant="outline" className="border-amber-300/20 text-amber-200">Not entered</Badge></div><p className="mt-3 text-3xl font-black">{from} <span className="text-slate-600">→</span> {to}</p><p className="mt-2 text-sm text-slate-500">No balance, amount, decimal, minimum received, quote, or route is available.</p></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold text-slate-300">Slippage intent<select value={slippage} onChange={(event) => setSlippage(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white"><option>Not configured</option><option>0.50% intent</option><option>1.00% intent</option><option>Custom policy required</option></select></label><div className="rounded-xl border border-white/10 p-4"><p className="text-sm text-slate-400">Route intent</p><p className="mt-2 font-bold">Unavailable</p><p className="mt-1 text-xs text-slate-500">No liquidity or venue source</p></div></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200">Transaction review</p><h2 className="mt-2 text-2xl font-black">No quote loaded</h2><p className="mt-3 text-sm leading-6 text-slate-400">A real swap requires a connected wallet, supported network, validated token contracts, fresh quote, route, slippage policy, fee and gas disclosure, allowance review, simulation, signing, confirmation, reconciliation, and failure recovery.</p><div className="mt-6 space-y-3">{[{ label: "Network / wallet", value: "Unverified", icon: Wallet }, { label: "Quote / rate", value: "Unavailable", icon: CircleDollarSign }, { label: "Allowance / approval", value: "Unverified", icon: KeyRound }, { label: "Gas / fees", value: "Unavailable", icon: Zap }, { label: "Transaction status", value: "Blocked", icon: Clock3 }].map(({ label, value, icon: Icon }) => <div key={label} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><Icon className="size-4 text-cyan-300" /><span className="flex-1 text-sm text-slate-300">{label}</span><span className="text-xs text-amber-200">{value}</span></div>)}</div><div className="mt-6 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Connect wallet unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Get quote unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Approve unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Swap unavailable</Button></div></CardContent></Card></section><section className="grid gap-4 md:grid-cols-3">{assets.map((asset) => <Card key={asset.symbol} className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="flex items-start justify-between"><div className="flex size-11 items-center justify-center rounded-2xl bg-cyan-300/10 text-cyan-200"><CircleDollarSign className="size-5" /></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{asset.state}</Badge></div><h2 className="mt-4 font-bold">{asset.name}</h2><p className="mt-1 text-xs text-cyan-200">{asset.symbol}</p><p className="mt-3 text-sm leading-6 text-slate-400">{asset.detail}</p></CardContent></Card>)}</section><ScreenStatePanel type="unavailable" title="Swap provider unavailable" description="Connect authorized wallet, network, token registry, quote provider, liquidity route, contract simulation, transaction signer, and reconciliation data before enabling execution." /><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><Info className="size-5 text-cyan-300" /><div><p className="font-bold">Swap safety contract</p><p className="mt-1 text-sm text-slate-400">Execution requires fresh quotes, validated contracts, allowance limits, slippage protection, fee disclosure, signing consent, idempotency, confirmation, and failed-transaction recovery.</p></div></div><div className="mt-5 grid gap-3 md:grid-cols-2">{["Network, wallet, token, and contract identity", "Balance, decimals, quote freshness, and route provenance", "Allowance, approval, simulation, slippage, gas, and fee policy", "Signature, nonce, replay, duplicate, and transaction-state controls", "Confirmation, reconciliation, failure, timeout, and retry handling", "Privacy, key custody, audit logging, and user-readable receipts"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3 text-sm text-slate-300"><LockKeyhole className="size-4 text-amber-300" />{item}<span className="ml-auto text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card>{showBoundary && <Card className="border-amber-300/20 bg-amber-300/[0.06]"><CardContent className="p-6"><p className="font-semibold text-amber-100">No quote or transaction claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local swap plan does not prove token ownership, wallet control, balance, price, quote, approval, transaction, settlement, or financial outcome.</p></CardContent></Card>}<ScreenFeatureGrid features={[{ title: "Swap surface preserved", description: "Assets, wallet, network, amount, quote, slippage, liquidity, approval, gas, fees, review, retry, save/reset, and execution gates remain visible.", icon: ArrowDownUp, status: "Local swap" }, { title: "No transaction theater", description: "Balances, prices, quotes, approvals, signatures, hashes, settlements, and financial outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before execution", description: "Real swaps require contract identity, quote provenance, wallet authorization, simulation, signing, confirmation, reconciliation, and custody controls.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
