import { useState } from "react";
import { CheckCircle2, Code2, Copy, FileCode2, KeyRound, LockKeyhole, ShieldAlert, WalletCards } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";

const surfaces = [
  { name: "Embed shell", detail: "Review the host, iframe, CSP, sandbox, origin, and accessibility contract." },
  { name: "Wallet surface", detail: "Requires verified custody, balances, signing, transaction, fee, and compliance infrastructure." },
  { name: "AI surface", detail: "Requires approved model access, prompt policy, data handling, quota, cost, and output controls." },
  { name: "Data surface", detail: "Requires source provenance, freshness, privacy, caching, rate limits, and correction paths." },
];
const gates = ["Published package and version provenance", "Server-side key issuance, rotation, scope, and revocation", "Authentication, origin, CSP, sandbox, and permission controls", "Billing, quotas, support, incident response, and rollback", "Security, privacy, legal, accessibility, and integration testing"];
const snippet = `// Illustrative integration shape only\n// No package, key, wallet, AI, payment, or data provider is active.\n\nconst integration = {\n  provider: "NOT_CONFIGURED",\n  apiKey: "SERVER_SIDE_REFERENCE_ONLY",\n  capabilities: [],\n  status: "UNAVAILABLE"\n};`;

export default function EmbedSDK() {
  const [selected, setSelected] = useState(0);
  const [copied, setCopied] = useState(false);
  const surface = surfaces[selected];
  const copy = async () => { await navigator.clipboard?.writeText(snippet); setCopied(true); window.setTimeout(() => setCopied(false), 1500); };
  return <main className="min-h-screen bg-gradient-to-br from-slate-950 via-blue-950/50 to-slate-900 p-6 text-white sm:p-10"><div data-ui-polish="batch-187" className="mx-auto max-w-6xl"><div className="flex items-center gap-4"><div className="flex size-12 items-center justify-center rounded-2xl bg-blue-300/10 text-blue-200"><Code2 className="size-6" /></div><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-blue-200/70">Integration governance</p><h1 className="text-3xl font-black sm:text-4xl">Embed workspace</h1></div><Badge variant="outline" className="ml-auto border-amber-300/20 bg-amber-300/[0.06] text-amber-100">SDK not connected</Badge></div><div className="mt-8 rounded-3xl border border-white/10 bg-gradient-to-br from-blue-400/[0.14] via-cyan-400/[0.08] to-transparent p-6 sm:p-10"><div className="flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.2em] text-blue-200"><ShieldAlert className="size-4" />Truthful integration boundary</div><h2 className="mt-4 max-w-3xl text-4xl font-black tracking-tight">Document the contract before embedding a capability.</h2><p className="mt-4 max-w-3xl text-base leading-7 text-slate-300">This workspace preserves integration planning, code review, and security gates while making no claim that an SDK package, API key, wallet, AI chat, price feed, social feed, payment, webhook, or commercial plan is available.</p><div className="mt-7 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400"><KeyRound className="mr-2 size-4" />API key unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Docs unavailable</Button></div></div><section className="mt-8 grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><div className="rounded-2xl border border-white/10 bg-white/[0.04] p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-blue-200">Embed surfaces</p><div className="mt-5 space-y-3">{surfaces.map((item, index) => <button key={item.name} type="button" onClick={() => setSelected(index)} className={`w-full rounded-xl border p-4 text-left transition ${selected === index ? "border-blue-300/40 bg-blue-300/[0.08]" : "border-white/10 hover:border-white/20"}`}><div className="flex items-center gap-3"><span className={`flex size-8 items-center justify-center rounded-full text-sm font-bold ${selected === index ? "bg-blue-200 text-slate-950" : "bg-white/10 text-slate-400"}`}>{index + 1}</span><span className="font-semibold">{item.name}</span><span className="ml-auto text-xs text-amber-200">Needs evidence</span></div><p className="mt-2 text-sm leading-6 text-slate-500">{item.detail}</p></button>)}</div></div><div className="rounded-2xl border border-white/10 bg-white/[0.04] p-6"><div className="flex items-start gap-4"><div className="flex size-12 shrink-0 items-center justify-center rounded-2xl bg-blue-300/10 text-blue-200"><WalletCards className="size-6" /></div><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-blue-200">Selected surface</p><h2 className="mt-2 text-3xl font-black">{surface.name}</h2><p className="mt-3 text-sm leading-6 text-slate-400">{surface.detail}</p></div></div><div className="mt-7 rounded-2xl border border-dashed border-white/15 bg-black/20 p-8 text-center"><FileCode2 className="mx-auto size-9 text-slate-600" /><p className="mt-3 font-semibold">No integration package loaded</p><p className="mt-2 text-sm leading-6 text-slate-500">Connect approved package provenance, provider contracts, test credentials, sandbox, observability, rate limits, and rollback before embedding this surface.</p></div><div className="mt-6 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Install unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Configure unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Publish unavailable</Button></div></div></section><section className="mt-8 rounded-2xl border border-white/10 bg-white/[0.04] p-6"><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200">Illustrative code contract</p><h2 className="mt-2 text-2xl font-black">Review shape, not an active SDK</h2></div><Button onClick={copy} variant="outline" className="border-white/10 text-white"><Copy className="mr-2 size-4" />{copied ? "Copied" : "Copy"}</Button></div><pre className="mt-5 overflow-x-auto rounded-2xl border border-white/10 bg-black/30 p-5 text-xs leading-6 text-cyan-100">{snippet}</pre><p className="mt-3 text-sm leading-6 text-slate-500">The example contains no real endpoint, credential, package, wallet operation, payment event, balance, or generated response.</p></section><section className="mt-8 rounded-2xl border border-white/10 bg-white/[0.04] p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Integration acceptance gates</p><div className="mt-5 grid gap-3 sm:grid-cols-2">{gates.map(item => <div key={item} className="flex items-start gap-3 rounded-xl border border-white/10 p-4 text-sm leading-6 text-slate-300"><LockKeyhole className="mt-1 size-4 shrink-0 text-amber-200" />{item}</div>)}</div></section><div className="mt-8 flex items-center gap-3 text-sm leading-6 text-slate-500"><CheckCircle2 className="size-4 shrink-0 text-emerald-200" /><strong className="text-amber-100">No SDK, key, wallet, AI, data, payment, webhook, pricing, or SLA claim is made.</strong> Local integration-governance state only.</div></div></main>;
}
