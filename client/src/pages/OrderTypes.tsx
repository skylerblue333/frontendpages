import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { BookOpen, Check, ChevronRight, LockKeyhole, RefreshCw, ShieldCheck, TriangleAlert, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

type OrderKind = "market" | "limit" | "stop" | "stop-limit" | "bracket";
const TYPES: Array<{ id: OrderKind; title: string; summary: string; trigger: string; risk: string; needs: string[] }> = [
  { id: "market", title: "Market", summary: "Conceptually requests execution near an available market price.", trigger: "No local trigger", risk: "Price and fill are unknown", needs: ["Authenticated quote", "Venue routing", "Slippage policy"] },
  { id: "limit", title: "Limit", summary: "Conceptually sets a maximum buy or minimum sell price.", trigger: "Price threshold", risk: "May not fill", needs: ["Precision rules", "Tick size", "Expiry policy"] },
  { id: "stop", title: "Stop", summary: "Conceptually activates after a trigger condition is reached.", trigger: "Trigger price", risk: "Gap and execution risk", needs: ["Trigger source", "Market data", "Failure handling"] },
  { id: "stop-limit", title: "Stop-limit", summary: "Conceptually activates a limit instruction after a trigger.", trigger: "Trigger + limit", risk: "May activate without fill", needs: ["Two-price validation", "Venue semantics", "State reconciliation"] },
  { id: "bracket", title: "Bracket", summary: "Conceptually groups an entry with exit protections.", trigger: "Linked legs", risk: "Leg and cancellation risk", needs: ["Atomicity", "OCO semantics", "Idempotency"] },
];
export default function OrderTypes() {
  const [selected, setSelected] = useState<OrderKind>("limit");
  const [acknowledged, setAcknowledged] = useState(false);
  const [question, setQuestion] = useState<"price" | "speed" | "protection">("price");
  const current = useMemo(() => TYPES.find((item) => item.id === selected) ?? TYPES[1], [selected]);
  const reset = () => { setSelected("limit"); setAcknowledged(false); setQuestion("price"); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={BookOpen} eyebrow="OrderTypes · Structure guide" title="Choose a structure by its tradeoffs, not its label." description="Compare common order concepts, select a local learning path, and inspect the evidence a real venue would need. No order type is connected to a broker, exchange, quote, account, or execution." badge="Type guide"><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset guide</Button></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Concepts", value: "5", hint: "Compared locally", icon: BookOpen, tone: "cyan" }, { label: "Selected", value: current.title, hint: "Learning path", icon: Check, tone: "violet" }, { label: "Execution", value: "Off", hint: "No venue", icon: WifiOff, tone: "amber" }, { label: "Advice", value: "Off", hint: "Not suitability", icon: ShieldCheck, tone: "slate" }]} /><ScreenPreviewBanner title="Order-type evidence boundary"><strong>This page explains concepts; it does not recommend a strategy or enable a trade.</strong> Trigger, fill, price, time-in-force, venue, account, balance, fee, slippage, risk, linkage, cancellation, and execution semantics vary by instrument and venue. All text and states here are educational/local fixtures, not financial advice or trading outcomes.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1fr_0.85fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{TYPES.map((item) => <button key={item.id} onClick={() => { setSelected(item.id); setAcknowledged(false); }} className={`rounded-2xl border p-4 text-left transition ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10 hover:border-white/20"}`}><div className="flex items-center justify-between gap-2"><p className="font-bold">{item.title}</p><ChevronRight className="size-4 text-slate-500" /></div><p className="mt-2 text-sm leading-5 text-slate-400">{item.summary}</p><Badge variant="outline" className="mt-4 border-white/10 text-amber-200">{item.trigger}</Badge></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected concept</p><h2 className="mt-2 text-3xl font-black">{current.title}</h2><p className="mt-3 leading-6 text-slate-400">{current.summary}</p><div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="text-xs uppercase tracking-[0.16em] text-amber-200">Primary risk</p><p className="mt-2 text-sm text-slate-300">{current.risk}</p></div><div className="mt-5 space-y-3">{current.needs.map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Needed</span></div>)}</div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[0.9fr_1fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200">Learning prompt</p><h2 className="mt-2 text-2xl font-black">What matters most to you?</h2><div className="mt-5 grid gap-2">{(["price", "speed", "protection"] as const).map((item) => <button key={item} onClick={() => setQuestion(item)} className={`rounded-xl border p-3 text-left text-sm capitalize ${question === item ? "border-cyan-300/40 bg-cyan-300/[0.06] text-cyan-100" : "border-white/10 text-slate-400"}`}>{item === "price" ? "I want more price control" : item === "speed" ? "I want to understand execution speed" : "I want to understand protection and linked risk"}</button>)}</div><div className="mt-5 flex gap-3"><button aria-label="Acknowledge educational boundary" onClick={() => setAcknowledged(!acknowledged)} className={`flex size-5 shrink-0 items-center justify-center rounded border ${acknowledged ? "border-cyan-300 bg-cyan-300 text-slate-950" : "border-white/20"}`}>{acknowledged ? "✓" : ""}</button><p className="text-sm leading-6 text-slate-400">I understand this comparison is educational and not a personalized recommendation.</p></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Implementation readiness</p><h2 className="mt-2 text-2xl font-black">Never enable from a label alone</h2><div className="mt-5 space-y-3">{["Instrument, venue, account permissions, precision, lot size, and time-in-force contract", "Trigger, quote, fill, slippage, fee, balance, and stale-data semantics", "Atomicity, idempotency, cancellation, partial fills, and reconciliation", "Risk controls, monitoring, audit trail, privacy, support, and incident response"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><TriangleAlert className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}<Button disabled={!acknowledged} className="mt-2 w-full bg-cyan-300/20 text-cyan-100/40">Use {current.title} unavailable</Button></div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected boundary</p><h2 className="mt-2 text-xl font-black">No venue semantics are assumed</h2><p className="mt-3 text-sm leading-6 text-slate-400">The selected {current.title.toLowerCase()} concept changes only local state. It does not create a payload, submit an order, reserve funds, or imply that a broker or exchange supports this type.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><LockKeyhole className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake suitability</h2><p className="mt-3 text-sm leading-6 text-slate-400">The learning prompt is not a risk profile, approval, or financial recommendation. Personalized guidance requires verified user context and appropriate controls.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Five structures compared", description: "Market, limit, stop, stop-limit, and bracket concepts each expose their tradeoffs.", icon: BookOpen, status: "Educational" }, { title: "Risks are visible", description: "Fill, gap, linkage, price, and execution uncertainty remain explicit.", icon: TriangleAlert, status: "Guardrail" }, { title: "No execution", description: "The guide cannot create, submit, fill, cancel, or settle an order.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
