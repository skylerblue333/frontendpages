import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Check, Clock3, Eye, EyeOff, FileKey2, LockKeyhole, MessageSquare, RefreshCw, ShieldCheck, TriangleAlert, UserRoundCheck, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

type Step = "phone" | "code" | "complete";
export default function PhoneVerification() {
  const [step, setStep] = useState<Step>("phone");
  const [phone, setPhone] = useState("");
  const [code, setCode] = useState("");
  const [showCode, setShowCode] = useState(false);
  const [resent, setResent] = useState(false);
  const [acknowledged, setAcknowledged] = useState(false);
  const digits = code.replace(/\D/g, "").slice(0, 6);
  const normalizedPhone = phone.replace(/\D/g, "");
  const phoneValid = normalizedPhone.length >= 7;
  const codeValid = digits.length === 6;
  const status = useMemo(() => step === "complete" ? "Local complete" : step === "code" ? "Awaiting code" : "Phone not submitted", [step]);
  const reset = () => { setStep("phone"); setPhone(""); setCode(""); setShowCode(false); setResent(false); setAcknowledged(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={UserRoundCheck} eyebrow="PhoneVerification · Recovery preview" title="Design a verification flow without sending a message." description="Explore phone formatting, local code entry, resend and expiry states, and recovery guidance. No SMS, phone number, OTP, carrier, account, device, identity, or verification record is connected." badge="Verification preview"><div className="flex flex-wrap gap-2"><Button disabled={!acknowledged || (step === "phone" ? !phoneValid : !codeValid)} onClick={() => setStep(step === "phone" ? "code" : "complete")} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{step === "phone" ? "Continue locally" : step === "code" ? "Verify locally" : "Local completion"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset flow</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Step", value: step === "phone" ? "1 / 3" : step === "code" ? "2 / 3" : "3 / 3", hint: status, icon: FileKey2, tone: "cyan" }, { label: "Phone", value: phoneValid ? "Formatted" : "Unset", hint: "Local field", icon: MessageSquare, tone: "violet" }, { label: "Code", value: codeValid ? "6 digits" : "Unset", hint: "No token source", icon: LockKeyhole, tone: "amber" }, { label: "SMS", value: "Off", hint: "No message sent", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="Verification evidence boundary"><strong>This is not phone verification.</strong> Phone formatting, code entry, local step progress, resend intent, expiry labels, and completion state do not prove phone ownership, identity, account access, device possession, carrier delivery, OTP issuance, or server verification. No message or account mutation occurs.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1fr_0.85fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Local flow</p><h2 className="mt-2 text-3xl font-black">{step === "phone" ? "Enter a phone concept" : step === "code" ? "Review the code step" : "Completion preview"}</h2></div><Badge variant="outline" className="border-white/10 text-amber-200">{status}</Badge></div>{step === "phone" && <div className="mt-6"><label className="text-sm text-slate-400">Phone number concept<input value={phone} onChange={(e) => setPhone(e.target.value)} placeholder="e.g. +1 555 010 0000" inputMode="tel" autoComplete="off" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-white outline-none focus:border-cyan-300/50" /></label><p className="mt-3 text-sm text-slate-500">Only local formatting is applied. The number is not stored, sent, looked up, or associated with an account.</p></div>}{step === "code" && <div className="mt-6"><label className="text-sm text-slate-400">One-time code concept<input value={showCode ? digits : digits.replace(/\d/g, "•")} onChange={(e) => setCode(e.target.value.replace(/\D/g, "").slice(0, 6))} inputMode="numeric" autoComplete="one-time-code" placeholder="6 digits" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 p-3 tracking-[0.4em] text-white outline-none focus:border-cyan-300/50" /></label><div className="mt-3 flex flex-wrap gap-2"><Button onClick={() => setShowCode(!showCode)} variant="outline" className="border-white/10 text-slate-300 hover:bg-white/10">{showCode ? <EyeOff className="mr-2 size-4" /> : <Eye className="mr-2 size-4" />}{showCode ? "Hide code" : "Show code"}</Button><Button onClick={() => setResent(true)} variant="outline" className="border-white/10 text-slate-300 hover:bg-white/10"><MessageSquare className="mr-2 size-4" />{resent ? "Resend intent recorded" : "Resend intent"}</Button></div><p className="mt-3 text-sm text-slate-500">No OTP exists to compare. Expiry, attempt limits, delivery, and rate limiting remain unavailable.</p></div>}{step === "complete" && <div className="mt-6 rounded-xl border border-cyan-300/20 bg-cyan-300/[0.06] p-5"><Check className="size-6 text-cyan-200" /><p className="mt-3 text-lg font-black">Local completion only</p><p className="mt-2 text-sm leading-6 text-slate-400">The browser flow reached its final preview step. No phone ownership, identity, account, session, or security factor was verified.</p></div>}<div className="mt-6 flex gap-3"><button aria-label="Acknowledge verification boundary" onClick={() => setAcknowledged(!acknowledged)} className={`flex size-5 shrink-0 items-center justify-center rounded border ${acknowledged ? "border-cyan-300 bg-cyan-300 text-slate-950" : "border-white/20"}`}>{acknowledged ? "✓" : ""}</button><p className="text-sm leading-6 text-slate-400">I understand this local flow cannot verify a phone or change an account.</p></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Safety states</p><h2 className="mt-2 text-2xl font-black">What remains unavailable</h2><div className="mt-5 space-y-3">{[{ label: "SMS delivery", detail: "No carrier, message, delivery receipt, or sender exists." }, { label: "OTP token", detail: "No server-issued secret or expiry is available." }, { label: "Attempt controls", detail: "No rate limit, lockout, replay, or abuse protection is connected." }, { label: "Account binding", detail: "No identity, session, phone ownership, or account mutation occurs." }, { label: "Recovery", detail: "Use the account's verified recovery path outside this preview." }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><div className="flex items-center justify-between gap-3"><p className="font-semibold">{item.label}</p><span className="text-xs text-amber-200">Unavailable</span></div><p className="mt-1 text-sm leading-5 text-slate-500">{item.detail}</p></div>)}</div><div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex gap-3"><TriangleAlert className="mt-0.5 size-5 shrink-0 text-amber-200" /><p className="text-sm leading-6 text-slate-300">Never use a client-only completion state as proof of identity or account ownership.</p></div></div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Production checklist</p><h2 className="mt-2 text-2xl font-black">Phone verification must prove</h2><div className="mt-5 space-y-3">{["Server-issued, short-lived, single-use OTP with secure hashing and replay protection", "SMS provider delivery, sender integrity, carrier failures, cost limits, and abuse/rate controls", "Authenticated account binding, ownership challenge, session/step-up policy, and recovery semantics", "Privacy-safe phone storage, normalization, access, retention, deletion, and user consent", "Tests for brute force, enumeration, race/replay, resend abuse, delivery delay, lockout, and support recovery"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><LockKeyhole className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake verification</h2><p className="mt-3 text-sm leading-6 text-slate-400">Entering a phone or code, recording resend intent, or reaching local completion changes browser state only. It does not send SMS, validate a token, prove ownership, or change an account.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Three-step flow preserved", description: "Phone, code, and completion states remain visible with accessible local interactions.", icon: FileKey2, status: "Local flow" }, { title: "Recovery boundaries are explicit", description: "Delivery, token, expiry, attempts, ownership, and account binding stay unavailable.", icon: ShieldCheck, status: "Guardrail" }, { title: "No SMS or account mutation", description: "No message, verification record, identity claim, or session change is fabricated.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
