import { useMemo, useState } from "react";
import { BarChart3, Check, Filter, LockKeyhole, RefreshCw, Search, ShieldAlert, TrendingUp, UsersRound, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const analytics = [{ id: 1, name: "Query demand", category: "Volume", detail: "A local query-measurement concept requiring event provenance, privacy-safe aggregation, bot handling, time window, source, and denominator semantics.", state: "Unconfigured" }, { id: 2, name: "Result quality", category: "Quality", detail: "A search-quality concept requiring click and satisfaction definitions, ranking context, sampling, moderation, and human review.", state: "Needs evidence" }, { id: 3, name: "Audience cohorts", category: "Cohorts", detail: "A cohort concept requiring identity, consent, retention window, minimization, segmentation, and privacy review.", state: "Blocked" }, { id: 4, name: "Trend exploration", category: "Trends", detail: "A trend concept requiring time-series source, seasonality, aggregation, anomaly semantics, and cautious interpretation.", state: "Preview" }];
export default function SearchAnalytics() { const [query, setQuery] = useState(""); const [category, setCategory] = useState("All"); const [selected, setSelected] = useState(1); const [period, setPeriod] = useState("Period not configured"); const [aggregation, setAggregation] = useState("Aggregation not configured"); const [saved, setSaved] = useState(false); const [showGates, setShowGates] = useState(false); const categories = ["All", ...Array.from(new Set(analytics.map((item) => item.category)))]; const filtered = useMemo(() => analytics.filter((item) => (category === "All" || item.category === category) && `${item.name} ${item.category} ${item.detail}`.toLowerCase().includes(query.toLowerCase())), [category, query]); const metric = analytics.find((item) => item.id === selected) ?? analytics[0]; const bars = [42, 67, 51, 76, 58, 72, 46]; const reset = () => { setQuery(""); setCategory("All"); setSelected(1); setPeriod("Period not configured"); setAggregation("Aggregation not configured"); setSaved(false); setShowGates(false); }; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={BarChart3} eyebrow="Search analytics · Measurement preview" title="Define the denominator before interpreting the trend." description="Explore local query-volume, result-quality, cohort, and trend concepts with search, category and period filters, illustrative-only bars, aggregation intent, privacy/ranking gates, save/reset, and blocked live analytics/export actions. No query volume, user, click, ranking, revenue, conversion, trend, or recommendation is connected." badge="Search-measurement workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save metric locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review analytics gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset metric</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Metric concepts", value: `${analytics.length} local`, hint: "No event source", icon: BarChart3, tone: "cyan" }, { label: "Query volume", value: "Unavailable", hint: "No search log source", icon: Search, tone: "violet" }, { label: "Cohorts", value: "Unavailable", hint: "No identity source", icon: UsersRound, tone: "amber" }, { label: "Trend", value: "Illustrative", hint: "Not measured", icon: TrendingUp, tone: "slate" }]} /><ScreenPreviewBanner title="Search-measurement evidence boundary"><strong>This is a local analytics preview, not evidence that searches, users, clicks, rankings, trends, or business outcomes exist.</strong> Metric cards, filters, illustrative bars, aggregation intent, saved state, privacy/ranking gates, and disabled live analytics/export actions are browser concepts. No query volume, user count, click rate, ranking, trend, recommendation, revenue, conversion, or market outcome is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Filter className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local analytics concepts" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-4 flex flex-wrap gap-2">{categories.map((entry) => <Button key={entry} onClick={() => setCategory(entry)} size="sm" variant="outline" className={category === entry ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-100" : "border-white/10 text-slate-400"}>{entry}</Button>)}</div><div className="mt-6 space-y-3">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{item.name}</p><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.state}</Badge></div><div className="mt-4"><Badge variant="outline" className="border-white/10 text-slate-500">{item.category}</Badge></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected metric concept</p><h2 className="mt-2 text-2xl font-black">{metric.name}</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{metric.state}</Badge></div><p className="mt-2 text-sm leading-6 text-slate-400">{metric.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Category", value: metric.category }, { label: "Period", value: period }, { label: "Aggregation", value: aggregation }, { label: "Users", value: "Unavailable" }, { label: "Ranking", value: "Unverified" }, { label: "Outcome", value: "Not claimed" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2"><label className="text-sm text-slate-400">Period intent<select value={period} onChange={(event) => setPeriod(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Period not configured</option><option>Daily intent</option><option>Weekly intent</option><option>Monthly intent</option><option>Rolling-window intent</option></select></label><label className="text-sm text-slate-400">Aggregation intent<select value={aggregation} onChange={(event) => setAggregation(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Aggregation not configured</option><option>Privacy-safe counts</option><option>Unique-query intent</option><option>Cohort aggregation</option><option>Human-review aggregation</option></select></label></div><div className="mt-6 rounded-2xl border border-white/10 bg-black/20 p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200">Illustrative-only curve</p><p className="mt-2 text-sm text-slate-400">Bars are design scaffolding, not measured search activity.</p></div><TrendingUp className="size-6 text-cyan-300" /></div><div className="mt-6 flex h-36 items-end gap-2">{bars.map((height, index) => <div key={index} className="flex-1 rounded-t-md bg-gradient-to-t from-cyan-400/40 to-violet-300/70" style={{ height: `${height}%` }} />)}</div></div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-8 text-center"><BarChart3 className="mx-auto size-8 text-slate-600" /><p className="mt-3 font-semibold">No search analytics evidence loaded</p><p className="mt-2 text-sm text-slate-500">Connect governed search events, privacy-safe aggregation, cohort rules, ranking context, freshness, source provenance, retention, and audit before measuring.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Load analytics unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Calculate unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Export unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Alert unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No volume, user, or trend claim</p><p className="mt-2 text-sm leading-6 text-slate-400">An illustrative chart does not prove query volume, users, clicks, rankings, trends, recommendations, revenue, conversions, or business outcomes.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Search-analytics gates</p><h2 className="mt-2 text-2xl font-black">What a real search analytics system must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Authenticated event, query, category, user/tenant, timestamp, locale, device, source, index version, and privacy boundary", "Query aggregation, unique semantics, bot filtering, deduplication, sampling, cohort rules, denominator, missingness, and retention", "Ranking, click, conversion, satisfaction, recommendation, trend, anomaly, quality, and source freshness semantics", "Privacy, consent, sensitive data, redaction, differential or minimum-count safeguards, deletion, export, access, and audit", "Revenue, marketplace, property, education, social, AI, crypto, financial, and user-impact claims require domain review", "Load, calculate, refresh, alert, share, export, report, accessibility, and accountable approval require governed analytics controls"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Search analytics surface preserved", description: "Query volume, result quality, cohorts, trends, filters, periods, aggregation, charts, alerts, export, save/reset, and gates remain interactive.", icon: BarChart3, status: "Local metrics" }, { title: "No analytics theater", description: "Queries, users, clicks, rankings, trends, recommendations, revenue, conversions, and outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Privacy before measurement", description: "Real search analytics requires governed events, aggregation, privacy safeguards, denominator semantics, ranking context, freshness, and audit.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>; }
