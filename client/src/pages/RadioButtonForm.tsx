import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Check, ClipboardCheck, Eye, FileInput, LockKeyhole, RefreshCw, ShieldAlert, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
export default function RadioButtonForm() {
  const [purpose, setPurpose] = useState("explore");
  const [access, setAccess] = useState("review");
  const [format, setFormat] = useState("summary");
  const [saved, setSaved] = useState(false);
  const [submitted, setSubmitted] = useState(false);
  const [showGates, setShowGates] = useState(false);
  const reset = () => { setPurpose("explore"); setAccess("review"); setFormat("summary"); setSaved(false); setSubmitted(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={FileInput} eyebrow="RadioButtonForm · Form preview" title="Choose the intent before submitting the form." description="Explore a local radio-group form with required fields, defaults, accessibility, conditional review, reset, save, and submission gates. No account update, preference persistence, backend submission, or user record is connected." badge="Form workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save draft locally"}</Button><Button onClick={() => setSubmitted(true)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><ClipboardCheck className="mr-2 size-4" />{submitted ? "Submission preview shown" : "Review submission"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review form gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset form</Button></div></ScreenHero><main className="mx-auto max-w-6xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Fields", value: "3 local", hint: "Radio groups", icon: FileInput, tone: "cyan" }, { label: "Validation", value: "Intent", hint: "Not server-checked", icon: ClipboardCheck, tone: "violet" }, { label: "Draft", value: saved ? "Saved locally" : "Unsaved", hint: "Browser state only", icon: Check, tone: "amber" }, { label: "Submission", value: "Preview", hint: "No backend", icon: LockKeyhole, tone: "slate" }]} /><ScreenPreviewBanner title="Form evidence boundary"><strong>This is a local form-interaction preview, not a submitted record.</strong> Radio selections, required-field posture, conditional review, saved state, and submission preview are browser concepts. No account setting, preference, consent, profile, user record, database write, notification, or external request is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1.15fr_0.85fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="space-y-8"><fieldset><legend className="text-lg font-bold">What is this form for?</legend><p className="mt-1 text-sm text-slate-500">Required · local selection</p><div className="mt-4 grid gap-3 sm:grid-cols-3">{[{ id: "explore", label: "Explore", note: "View a preview" }, { id: "configure", label: "Configure", note: "Prepare a setting" }, { id: "request", label: "Request", note: "Draft a need" }].map((item) => <label key={item.id} className={`cursor-pointer rounded-xl border p-4 ${purpose === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><input type="radio" name="purpose" value={item.id} checked={purpose === item.id} onChange={(event) => setPurpose(event.target.value)} className="sr-only" /><p className="font-semibold">{item.label}</p><p className="mt-1 text-xs text-slate-500">{item.note}</p></label>)}</div></fieldset><fieldset><legend className="text-lg font-bold">Who should review the outcome?</legend><p className="mt-1 text-sm text-slate-500">Required · local selection</p><div className="mt-4 grid gap-3 sm:grid-cols-3">{[{ id: "review", label: "Self review", note: "No other person" }, { id: "team", label: "Team review", note: "Needs permissions" }, { id: "admin", label: "Admin review", note: "Needs authorization" }].map((item) => <label key={item.id} className={`cursor-pointer rounded-xl border p-4 ${access === item.id ? "border-violet-300/40 bg-violet-300/[0.06]" : "border-white/10"}`}><input type="radio" name="access" value={item.id} checked={access === item.id} onChange={(event) => setAccess(event.target.value)} className="sr-only" /><p className="font-semibold">{item.label}</p><p className="mt-1 text-xs text-slate-500">{item.note}</p></label>)}</div></fieldset><fieldset><legend className="text-lg font-bold">Preferred response format</legend><p className="mt-1 text-sm text-slate-500">Required · local selection</p><div className="mt-4 grid gap-3 sm:grid-cols-3">{[{ id: "summary", label: "Summary", note: "Compact result" }, { id: "detail", label: "Detailed", note: "Expanded result" }, { id: "steps", label: "Next steps", note: "Action outline" }].map((item) => <label key={item.id} className={`cursor-pointer rounded-xl border p-4 ${format === item.id ? "border-amber-300/40 bg-amber-300/[0.06]" : "border-white/10"}`}><input type="radio" name="format" value={item.id} checked={format === item.id} onChange={(event) => setFormat(event.target.value)} className="sr-only" /><p className="font-semibold">{item.label}</p><p className="mt-1 text-xs text-slate-500">{item.note}</p></label>)}</div></fieldset></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Review panel</p><h2 className="mt-2 text-2xl font-black">Your local selection</h2><div className="mt-6 space-y-3">{[{ label: "Purpose", value: purpose }, { label: "Reviewer", value: access }, { label: "Format", value: format }, { label: "Required fields", value: "Complete locally" }, { label: "Accessibility", value: "Keyboard labels present" }, { label: "Persistence", value: "Not connected" }].map((item) => <div key={item.label} className="flex items-center justify-between gap-3 rounded-xl border border-white/10 p-3"><span className="text-sm text-slate-400">{item.label}</span><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.value}</Badge></div>)}</div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-6 text-center"><Eye className="mx-auto size-7 text-slate-600" /><p className="mt-3 font-semibold">Conditional sections are preview-only</p><p className="mt-2 text-sm text-slate-500">A real form must define authorization, validation, persistence, error recovery, and data handling.</p></div>{submitted && <div className="mt-5 rounded-xl border border-cyan-300/20 bg-cyan-300/[0.06] p-4"><p className="font-semibold text-cyan-100">Submission preview ready</p><p className="mt-2 text-sm leading-6 text-slate-400">The selected values are prepared locally. No form was submitted and no record was created.</p></div>}{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No persistence claim</p><p className="mt-2 text-sm leading-6 text-slate-400">Browser state does not prove server validation, authorization, consent capture, database writes, notifications, or external side effects.</p></div>}</CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Form surface preserved", description: "Radio groups, required posture, defaults, accessible labels, conditional review, save, submission preview, reset, and gates remain interactive.", icon: FileInput, status: "Local form" }, { title: "No submission theater", description: "Account changes, preferences, consent, user records, database writes, notifications, and external requests are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Validation before persistence", description: "Real forms need server validation, authorization, privacy, audit, retries, and clear failure handling.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
