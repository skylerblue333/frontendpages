import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Activity, Bell, Check, FileText, Gauge, LockKeyhole, RefreshCw, ShieldAlert, Sparkles, Trophy, UserRound, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const panels = ["Overview", "Activity", "Achievements", "Notifications", "Settings"];
export default function ProfileDashboard() {
  const [panel, setPanel] = useState(panels[0]);
  const [saved, setSaved] = useState(false);
  const [showBoundaries, setShowBoundaries] = useState(false);
  const reset = () => { setPanel(panels[0]); setSaved(false); setShowBoundaries(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Gauge} eyebrow="ProfileDashboard · Account dashboard preview" title="Give the profile a useful home without inventing a user history." description="Explore local account-dashboard panels for overview, activity, achievements, notifications, and settings. No account sync, social activity, progress, rewards, unread count, balance, connection, or analytics claim is asserted." badge="Dashboard workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "View saved locally" : "Save view locally"}</Button><Button onClick={() => setShowBoundaries((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showBoundaries ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showBoundaries ? "Close boundary" : "Review boundaries"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset dashboard</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Account", value: "Local", hint: "Not synced", icon: UserRound, tone: "cyan" }, { label: "Activity", value: "Unmeasured", hint: "No event source", icon: Activity, tone: "violet" }, { label: "Achievements", value: "Unverified", hint: "No reward ledger", icon: Trophy, tone: "amber" }, { label: "Notifications", value: "Unknown", hint: "No delivery source", icon: Bell, tone: "slate" }]} /><ScreenPreviewBanner title="Dashboard evidence boundary"><strong>This is a local account-dashboard preview, not a user record.</strong> Panel names, summary values, activity states, achievements, notifications, and settings are browser-state concepts. No identity, balance, progress, reward, unread count, social connection, analytics, or account event is measured or persisted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.8fr_1.2fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><div className="flex size-16 items-center justify-center rounded-2xl bg-cyan-300/10"><UserRound className="size-8 text-cyan-200" /></div><div><h2 className="text-2xl font-black">Skycoin explorer</h2><p className="mt-1 text-sm text-slate-500">Local dashboard draft · account not connected</p></div></div><div className="mt-6 grid grid-cols-2 gap-3">{[{ label: "Profile", value: "Draft" }, { label: "Privacy", value: "Review" }, { label: "Preferences", value: "Local" }, { label: "Integrations", value: "Off" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Dashboard posture</p><h2 className="mt-2 text-2xl font-black">Account state unknown</h2></div><LockKeyhole className="size-5 text-amber-200" /></div><div className="mt-6 rounded-2xl border border-amber-300/25 bg-amber-300/[0.06] p-5"><div className="flex items-start gap-3"><ShieldAlert className="mt-0.5 size-5 shrink-0 text-amber-200" /><div><p className="font-semibold text-amber-100">Dashboard data is unavailable</p><p className="mt-2 text-sm leading-6 text-slate-400">No authenticated account, event stream, achievement ledger, notification service, portfolio source, social graph, or settings persistence is connected.</p></div></div></div>{showBoundaries && <div className="mt-4 rounded-xl border border-cyan-300/20 bg-cyan-300/[0.05] p-4 text-sm leading-6 text-slate-400">Local labels do not prove account ownership, progress, rewards, balances, engagement, delivery, or feature entitlement.</div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex flex-wrap gap-2">{panels.map((item) => <button key={item} onClick={() => setPanel(item)} className={`flex items-center gap-2 rounded-lg border px-4 py-2 text-sm ${panel === item ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-200" : "border-white/10 text-slate-500"}`}>{item === "Overview" && <Gauge className="size-4" />}{item === "Activity" && <Activity className="size-4" />}{item === "Achievements" && <Trophy className="size-4" />}{item === "Notifications" && <Bell className="size-4" />}{item === "Settings" && <FileText className="size-4" />}{item}</button>)}</div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-12 text-center">{panel === "Overview" && <Gauge className="mx-auto size-10 text-cyan-300/40" />}{panel === "Activity" && <Activity className="mx-auto size-10 text-violet-300/40" />}{panel === "Achievements" && <Trophy className="mx-auto size-10 text-amber-300/40" />}{panel === "Notifications" && <Bell className="mx-auto size-10 text-pink-300/40" />}{panel === "Settings" && <FileText className="mx-auto size-10 text-slate-300/40" />}<h3 className="mt-4 text-xl font-black">{panel} unavailable in preview</h3><p className="mx-auto mt-2 max-w-xl text-sm leading-6 text-slate-500">This panel is preserved as a useful state, but its live data source is not connected. Nothing is fabricated to make the dashboard appear active.</p><Button disabled className="mt-5 bg-slate-700 text-slate-400">Open {panel.toLowerCase()} unavailable</Button></div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Dashboard surface preserved", description: "Profile summary, overview, activity, achievements, notifications, settings, local view state, save/reset, and boundaries remain interactive.", icon: Gauge, status: "Local dashboard" }, { title: "No account theater", description: "Identity, activity, progress, rewards, unread counts, balances, social connections, analytics, and entitlements are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Data service required", description: "Real dashboards need authenticated persistence, event contracts, privacy controls, delivery semantics, audit, and recovery behavior.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
