import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Check, Clock3, EyeOff, Filter, Layers3, LockKeyhole, RefreshCw, Search, ShieldAlert, Sparkles, ThumbsUp, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const cards = [{ id: 1, title: "Learning spotlight", category: "Education", reason: "Feed concept awaiting consented learner context, content metadata, and freshness policy.", freshness: "Freshness unavailable" }, { id: 2, title: "Community signal", category: "Community", reason: "Discovery concept awaiting moderation, exposure controls, and audience privacy review.", freshness: "Timestamp unavailable" }, { id: 3, title: "Game discovery", category: "Gaming", reason: "Feed concept awaiting preference evidence and safe-ranking policy.", freshness: "Rank unmeasured" }, { id: 4, title: "Financial education", category: "Finance", reason: "Education-only concept that must not become personalized financial advice.", freshness: "Suitability blocked" }];
export default function RecommendationsFeed() {
  const [query, setQuery] = useState(""); const [category, setCategory] = useState("All"); const [selected, setSelected] = useState(1); const [sort, setSort] = useState("Sort not configured"); const [feedback, setFeedback] = useState("Feedback not connected"); const [saved, setSaved] = useState(false); const [showGates, setShowGates] = useState(false);
  const categories = ["All", ...Array.from(new Set(cards.map((item) => item.category)))]; const filtered = useMemo(() => cards.filter((item) => (category === "All" || item.category === category) && `${item.title} ${item.category} ${item.reason}`.toLowerCase().includes(query.toLowerCase())), [category, query]); const card = cards.find((item) => item.id === selected) ?? cards[0];
  const reset = () => { setQuery(""); setCategory("All"); setSelected(1); setSort("Sort not configured"); setFeedback("Feedback not connected"); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Layers3} eyebrow="RecommendationsFeed · Discovery preview" title="Curate the feed only after proving the signal." description="Explore local discovery-feed concepts with search, categories, reason codes, freshness and ranking intent, personalization, feedback, pagination, refresh, moderation, save, reset, and evidence gates. No live users, scores, predictions, timestamps, engagement, or outcome is connected." badge="Feed workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "View saved locally" : "Save feed locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review feed gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset feed</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Cards", value: `${cards.length} local`, hint: "No feed loaded", icon: Layers3, tone: "cyan" }, { label: "Ranking", value: "Unmeasured", hint: "No model source", icon: Sparkles, tone: "violet" }, { label: "Freshness", value: "Unavailable", hint: "No timestamps", icon: Clock3, tone: "amber" }, { label: "Engagement", value: "Unknown", hint: "No feedback source", icon: ThumbsUp, tone: "slate" }]} /><ScreenPreviewBanner title="Feed evidence boundary"><strong>This is a local discovery-feed preview, not evidence of personalization or engagement.</strong> Cards, categories, reason codes, ranking and freshness intent, feedback state, saved state, pagination, moderation posture, and evidence gates are browser concepts. No user, score, prediction, rank, timestamp, view, click, like, share, or outcome is asserted.</ScreenPreviewBanner><section className="flex flex-wrap items-end gap-3"><label className="text-sm text-slate-400">Feed ordering<select value={sort} onChange={(event) => setSort(event.target.value)} className="mt-2 rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Sort not configured</option><option>Freshness intent</option><option>Editorial intent</option><option>Relevance intent</option></select></label><Badge variant="outline" className="border-amber-300/20 text-amber-200">Feed source disconnected</Badge></section><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Search className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local feed cards" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-4 flex flex-wrap gap-2">{categories.map((entry) => <Button key={entry} onClick={() => setCategory(entry)} size="sm" variant="outline" className={category === entry ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-100" : "border-white/10 text-slate-400"}><Filter className="mr-1 size-3" />{entry}</Button>)}</div><div className="mt-6 space-y-3">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{item.title}</p><p className="mt-1 text-sm text-slate-500">{item.reason}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.freshness}</Badge></div><div className="mt-4"><Badge variant="outline" className="border-white/10 text-slate-500">{item.category}</Badge></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected feed card</p><h2 className="mt-2 text-2xl font-black">{card.title}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{card.reason}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Category", value: card.category }, { label: "Ordering", value: sort }, { label: "Freshness", value: card.freshness }, { label: "Rank", value: "Unmeasured" }, { label: "Feedback", value: feedback }, { label: "Moderation", value: "Not configured" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><label className="mt-5 block text-sm text-slate-400">Feedback posture<select value={feedback} onChange={(event) => setFeedback(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Feedback not connected</option><option>Helpful intent</option><option>Hide intent</option><option>Report intent</option></select></label><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-8 text-center"><EyeOff className="mx-auto size-7 text-slate-600" /><p className="mt-3 font-semibold">No live feed cards loaded</p><p className="mt-2 text-sm text-slate-500">Connect governed content, user context, ranking policy, timestamps, and moderation before displaying a feed.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Refresh unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Load more unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Open card unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No feed or engagement claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local card does not prove relevance, freshness, audience exposure, engagement, quality, or outcome.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Feed gates</p><h2 className="mt-2 text-2xl font-black">What a real recommendation feed must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["User identity, consent, opt-out, data minimization, sensitive traits, deletion, and tenant isolation", "Content eligibility, freshness, availability, metadata, safety, rights, policy, and explanation provenance", "Ranking model, features, confidence, calibration, exploration, diversity, suppression, fairness, and drift", "Feed pagination, deduplication, replay, caching, personalization, cold start, and reproducible ordering", "Moderation, reports, appeals, safety interventions, age boundaries, and human oversight", "Views, clicks, dwell, likes, shares, conversions, experiments, audit, rollback, and incident response"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Feed surface preserved", description: "Cards, search, categories, reason codes, ranking, freshness, personalization, feedback, pagination, refresh, moderation, save/reset, and gates remain interactive.", icon: Layers3, status: "Local feed" }, { title: "No engagement theater", description: "Users, scores, predictions, timestamps, exposure, views, clicks, likes, shares, and outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Governance before feed", description: "Real feeds need consent, content policy, ranking evidence, moderation, privacy, measurement, and rollback.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
