import { useMemo, useState } from "react";
import { Activity, BarChart3, BookOpen, Bot, Brain, Check, CheckCircle2, Code2, Copy, FileCode2, Lightbulb, LockKeyhole, MessageSquare, Network, RefreshCw, Send, Shield, Sparkles, Target, TrendingUp, WandSparkles } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { Textarea } from "@/components/ui/textarea";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

type Module = { name: string; description: string; icon: typeof TrendingUp; status: "Preview" | "Contract required" };
const MODULES: Module[] = [
  { name: "Feed ranking", description: "Explore how ranking signals could be documented and reviewed.", icon: TrendingUp, status: "Preview" },
  { name: "Content moderation", description: "Review a moderation workflow with explicit human escalation.", icon: Shield, status: "Contract required" },
  { name: "Fraud detection", description: "Map risk signals without claiming production fraud coverage.", icon: Activity, status: "Contract required" },
  { name: "Recommendation engine", description: "Inspect a recommendation brief and its explainability needs.", icon: Sparkles, status: "Preview" },
  { name: "Sentiment analysis", description: "Plan a sentiment report with data-source transparency.", icon: BarChart3, status: "Preview" },
  { name: "Creator growth", description: "Draft a creator support plan without promising growth.", icon: Target, status: "Preview" },
  { name: "Social graph", description: "Document relationship insights only after privacy review.", icon: Network, status: "Contract required" },
  { name: "Learning coach", description: "Turn a topic into a reviewable lesson outline.", icon: BookOpen, status: "Preview" },
];
const QUICK_PROMPTS = ["Explain DeFi yield farming", "Create a React hook outline", "What is a DAO?", "Plan a wallet UX review"];
const LESSON_TOPICS = ["Smart contract security", "Web3 wallet integration", "DAO governance", "Layer 2 scaling"];

function CopyButton({ text }: { text: string }) {
  const [copied, setCopied] = useState(false);
  return <Button variant="ghost" size="icon" onClick={() => { void navigator.clipboard?.writeText(text); setCopied(true); setTimeout(() => setCopied(false), 1400); }} aria-label="Copy preview text" className="text-slate-400 hover:text-white">{copied ? <Check className="size-4 text-emerald-300" /> : <Copy className="size-4" />}</Button>;
}

function ChatPreview() {
  const [input, setInput] = useState("");
  const [prompt, setPrompt] = useState("");
  const [prepared, setPrepared] = useState(false);
  const shownPrompt = prompt || "Explain how a DAO proposal moves from idea to review.";
  return <div className="grid gap-5 lg:grid-cols-[1.3fr_0.7fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Conversation preview</p><h2 className="mt-1 text-xl font-bold">Ask the Brain</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Not connected</Badge></div><div className="mt-5 space-y-4"><div className="flex gap-3"><div className="flex size-9 shrink-0 items-center justify-center rounded-xl bg-cyan-300/10 text-cyan-200"><Brain className="size-4" /></div><div className="rounded-xl border border-white/10 bg-black/15 p-4 text-sm leading-6 text-slate-300">This is a local conversation preview. Choose a prompt to inspect the interaction pattern; no live model response or saved history is claimed.</div></div>{prepared && <div className="flex flex-row-reverse gap-3"><div className="rounded-xl border border-cyan-300/20 bg-cyan-300/[0.08] p-4 text-sm leading-6 text-slate-200">{shownPrompt}</div></div>}<div className="flex gap-2"><Textarea value={input} onChange={(event) => { setInput(event.target.value); setPrepared(false); }} onKeyDown={(event) => { if (event.key === "Enter" && !event.shiftKey) { event.preventDefault(); if (input.trim()) { setPrompt(input); setPrepared(true); setInput(""); } } }} placeholder="Ask a question for the preview..." aria-label="AI Brain prompt" className="min-h-20 border-white/10 bg-black/20 text-white placeholder:text-slate-500" /><Button onClick={() => { if (input.trim()) { setPrompt(input); setPrepared(true); setInput(""); } }} disabled={!input.trim()} aria-label="Prepare chat preview" className="self-end bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Send className="size-4" /></Button></div></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-300">Quick prompts</p><h2 className="mt-1 text-xl font-bold">Start with a direction</h2><div className="mt-4 space-y-2">{QUICK_PROMPTS.map((item) => <button key={item} onClick={() => { setInput(item); setPrepared(false); }} className="w-full rounded-lg border border-white/10 bg-black/15 p-3 text-left text-sm text-slate-300 transition hover:border-cyan-300/30 hover:bg-white/[0.05]">{item}</button>)}</div></CardContent></Card></div>;
}

function CodePreview() {
  const [mode, setMode] = useState("Generate");
  const [language, setLanguage] = useState("TypeScript");
  const [code, setCode] = useState("");
  const [prepared, setPrepared] = useState(false);
  const modes = ["Generate", "Debug", "Review", "Optimize"];
  return <Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="flex flex-wrap gap-2">{modes.map((item) => <Button key={item} size="sm" variant={mode === item ? "default" : "outline"} onClick={() => { setMode(item); setPrepared(false); }} className={mode === item ? "bg-violet-300 text-slate-950 hover:bg-violet-200" : "border-white/15 bg-white/5 text-slate-300 hover:bg-white/10"}>{item}</Button>)}</div><div className="mt-5 grid gap-4 md:grid-cols-[0.7fr_1.3fr]"><div className="space-y-4"><div><label className="text-sm font-medium text-slate-300" htmlFor="brain-language">Language</label><select id="brain-language" value={language} onChange={(event) => setLanguage(event.target.value)} className="mt-2 w-full rounded-md border border-white/10 bg-black/20 px-3 py-2 text-sm text-white"><option>TypeScript</option><option>JavaScript</option><option>Python</option><option>Rust</option><option>Solidity</option></select></div><Textarea value={code} onChange={(event) => { setCode(event.target.value); setPrepared(false); }} placeholder={mode === "Generate" ? "Describe the code you want to preview..." : "Paste code or an error for review..."} aria-label="Code tool input" className="min-h-36 border-white/10 bg-black/20 font-mono text-xs text-white placeholder:text-slate-500" /><Button onClick={() => setPrepared(Boolean(code.trim()))} disabled={!code.trim()} className="w-full bg-violet-300 text-slate-950 hover:bg-violet-200"><WandSparkles className="mr-2 size-4" />Prepare {mode.toLowerCase()} preview</Button></div><div className="rounded-xl border border-white/10 bg-black/20 p-4"><div className="flex items-center justify-between"><div className="flex items-center gap-2 text-sm font-medium"><FileCode2 className="size-4 text-violet-300" />Output boundary</div><CopyButton text="Preview output is not generated until a verified AI service is connected." /></div>{prepared ? <div className="mt-4 space-y-3"><Badge className="border-emerald-300/20 bg-emerald-300/10 text-emerald-200">Request prepared</Badge><p className="text-sm leading-6 text-slate-300">A {mode.toLowerCase()} workflow for {language} is ready for a connected model. The platform has not invented code, review scores, or security findings.</p></div> : <p className="mt-4 text-sm leading-6 text-slate-400">Enter a request to see the local workflow state. Generated code and analysis require a verified service contract.</p>}</div></div></CardContent></Card>;
}

function LearnPreview() {
  const [topic, setTopic] = useState("");
  const [prepared, setPrepared] = useState(false);
  return <Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="flex items-start gap-3"><div className="flex size-11 items-center justify-center rounded-2xl bg-amber-300/10 text-amber-200"><BookOpen className="size-5" /></div><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-300">Learning preview</p><h2 className="mt-1 text-xl font-bold">Build a lesson outline</h2><p className="mt-1 text-sm text-slate-400">Select a topic to inspect the lesson workflow without claiming a generated course.</p></div></div><Textarea value={topic} onChange={(event) => { setTopic(event.target.value); setPrepared(false); }} placeholder="What do you want to learn?" aria-label="Learning topic" className="mt-5 min-h-24 border-white/10 bg-black/20 text-white placeholder:text-slate-500" /><div className="mt-4 flex flex-wrap gap-2">{LESSON_TOPICS.map((item) => <Button key={item} size="sm" variant="outline" onClick={() => { setTopic(item); setPrepared(false); }} className="border-white/15 bg-white/5 text-slate-300 hover:bg-white/10">{item}</Button>)}</div><Button onClick={() => setPrepared(Boolean(topic.trim()))} disabled={!topic.trim()} className="mt-5 bg-amber-300 text-slate-950 hover:bg-amber-200"><Lightbulb className="mr-2 size-4" />Prepare lesson preview</Button>{prepared && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex items-center gap-2 font-medium text-amber-100"><CheckCircle2 className="size-4" />Lesson request prepared</div><p className="mt-2 text-sm leading-6 text-slate-300">A reviewable outline for “{topic}” can be generated after a verified learning service is connected.</p></div>}</CardContent></Card>;
}

function ModulesPreview() {
  const [query, setQuery] = useState("");
  const filtered = useMemo(() => MODULES.filter((module) => `${module.name} ${module.description}`.toLowerCase().includes(query.toLowerCase())), [query]);
  return <div className="space-y-4"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-emerald-300">Control map</p><h2 className="mt-1 text-xl font-bold">AI modules</h2></div><div className="relative w-full sm:max-w-xs"><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search modules" aria-label="Search AI modules" className="border-white/10 bg-white/5 text-white placeholder:text-slate-500" /></div></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{filtered.map((module) => { const Icon = module.icon; return <Card key={module.name} className="border-white/10 bg-white/[0.04]"><CardContent className="p-4"><div className="flex items-center justify-between"><div className="flex size-10 items-center justify-center rounded-xl bg-emerald-300/10 text-emerald-200"><Icon className="size-5" /></div><Badge variant="outline" className={module.status === "Preview" ? "border-emerald-300/20 text-emerald-200" : "border-amber-300/20 text-amber-200"}>{module.status}</Badge></div><h3 className="mt-4 font-semibold">{module.name}</h3><p className="mt-2 text-sm leading-6 text-slate-400">{module.description}</p></CardContent></Card>; })}</div>{filtered.length === 0 && <ScreenStatePanel type="empty" title="No modules match" description="Try a different module search." />}</div>;
}

export default function AIBrain() {
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Brain} eyebrow="AI & Automation · Control Center" title="A transparent cockpit for AI work." description="Explore conversation, code, learning, and platform-intelligence workflows in one reviewable workspace. Live model claims, accuracy scores, production metrics, and autonomous actions stay disabled until their contracts are proven." badge="Preview control center"><div className="flex flex-wrap gap-2"><Button onClick={() => document.getElementById("brain-tabs")?.scrollIntoView({ behavior: "smooth" })} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Sparkles className="mr-2 size-4" />Explore AI workspace</Button><Button variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><LockKeyhole className="mr-2 size-4" />Review boundaries</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Workspace modes", value: "4", hint: "Chat, code, learn, modules", icon: Brain }, { label: "Module map", value: String(MODULES.length), hint: "Reviewable capability concepts", icon: Network, tone: "violet" }, { label: "Live models", value: "Not connected", hint: "No provider claim in preview", icon: LockKeyhole, tone: "amber" }, { label: "Execution", value: "Review first", hint: "No external actions are triggered", icon: Shield, tone: "slate" }]} /><ScreenPreviewBanner title="Claim-to-proof boundary">The tabs, local prompt states, code-tool workflow states, learning outline states, module search, and response guardrails are available for frontend review. No model output, accuracy score, platform statistic, user count, moderation total, or production response-time claim is invented here.</ScreenPreviewBanner><section id="brain-tabs"><Tabs defaultValue="chat" className="space-y-4"><TabsList className="grid h-auto w-full grid-cols-2 gap-2 border border-white/10 bg-white/[0.04] p-2 sm:grid-cols-4"><TabsTrigger value="chat" className="gap-2 text-slate-300 data-[state=active]:bg-cyan-300 data-[state=active]:text-slate-950"><MessageSquare className="size-4" />Chat</TabsTrigger><TabsTrigger value="code" className="gap-2 text-slate-300 data-[state=active]:bg-violet-300 data-[state=active]:text-slate-950"><Code2 className="size-4" />Code tools</TabsTrigger><TabsTrigger value="learn" className="gap-2 text-slate-300 data-[state=active]:bg-amber-300 data-[state=active]:text-slate-950"><BookOpen className="size-4" />Learn</TabsTrigger><TabsTrigger value="modules" className="gap-2 text-slate-300 data-[state=active]:bg-emerald-300 data-[state=active]:text-slate-950"><Bot className="size-4" />Modules</TabsTrigger></TabsList><TabsContent value="chat"><ChatPreview /></TabsContent><TabsContent value="code"><CodePreview /></TabsContent><TabsContent value="learn"><LearnPreview /></TabsContent><TabsContent value="modules"><ModulesPreview /></TabsContent></Tabs></section><ScreenFeatureGrid features={[{ title: "Human review", description: "Inspect prompts and outputs before any publishing, financial, or external action.", icon: Shield, status: "Required" }, { title: "Provider transparency", description: "Show connected model and data-source details only when the integration responds.", icon: Bot, status: "Contract required" }, { title: "Explainable modules", description: "Document what each capability does and what evidence is needed before activation.", icon: Network, status: "Available" }]} /></main></div>;
}
