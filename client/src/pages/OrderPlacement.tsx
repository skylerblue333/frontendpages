import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Ban, Check, FileInput, LockKeyhole, RefreshCw, ShieldCheck, TriangleAlert, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

type Side = "buy" | "sell";
type Mode = "market" | "limit";
export default function OrderPlacement() {
  const [side, setSide] = useState<Side>("buy");
  const [mode, setMode] = useState<Mode>("limit");
  const [quantity, setQuantity] = useState("10");
  const [price, setPrice] = useState("99.80");
  const [acknowledged, setAcknowledged] = useState(false);
  const [saved, setSaved] = useState(false);
  const qty = Number(quantity);
  const limit = Number(price);
  const notional = useMemo(() => mode === "market" ? "Not computable" : Number.isFinite(qty * limit) && qty > 0 && limit > 0 ? (qty * limit).toFixed(2) : "—", [mode, qty, limit]);
  const valid = qty > 0 && (mode === "market" || limit > 0);
  const reset = () => { setSide("buy"); setMode("limit"); setQuantity("10"); setPrice("99.80"); setAcknowledged(false); setSaved(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={FileInput} eyebrow="OrderPlacement · Intent builder" title="Build the intent without pretending it was submitted." description="Configure a synthetic buy or sell concept, inspect local notional arithmetic, and acknowledge the missing execution boundary. No account, balance, venue, quote, signature, order, or fill is connected." badge="Placement preview"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Intent saved locally" : "Save intent locally"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset builder</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Intent", value: saved ? "Saved" : "Draft", hint: "Local state", icon: FileInput, tone: "cyan" }, { label: "Side", value: side === "buy" ? "Buy" : "Sell", hint: "Concept only", icon: side === "buy" ? Check : Ban, tone: "violet" }, { label: "Notional", value: notional, hint: mode === "limit" ? "Local arithmetic" : "No market quote", icon: TriangleAlert, tone: "amber" }, { label: "Submission", value: "Blocked", hint: "No venue", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="Placement evidence boundary"><strong>This is an order-intent worksheet, not an order-entry system.</strong> It does not query a market, reserve funds, validate balances, contact a venue, sign a payload, create an order ID, submit a request, or claim a fill. Market mode intentionally shows no quote because no authenticated data source exists.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1.05fr_0.95fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Order concept</p><h2 className="mt-2 text-3xl font-black">SKY / USD</h2></div><Badge variant="outline" className="border-white/10 text-slate-400">Venue unset</Badge></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><div><p className="text-sm text-slate-400">Side</p><div className="mt-2 flex rounded-xl border border-white/10 bg-black/20 p-1">{(["buy", "sell"] as Side[]).map((item) => <button key={item} onClick={() => { setSide(item); setSaved(false); }} className={`flex-1 rounded-lg py-2 text-sm font-bold capitalize ${side === item ? item === "buy" ? "bg-cyan-300 text-slate-950" : "bg-rose-300 text-slate-950" : "text-slate-400 hover:text-white"}`}>{item}</button>)}</div></div><div><p className="text-sm text-slate-400">Order type</p><div className="mt-2 flex rounded-xl border border-white/10 bg-black/20 p-1">{(["limit", "market"] as Mode[]).map((item) => <button key={item} onClick={() => { setMode(item); setSaved(false); }} className={`flex-1 rounded-lg py-2 text-sm font-bold capitalize ${mode === item ? "bg-violet-300 text-slate-950" : "text-slate-400 hover:text-white"}`}>{item}</button>)}</div></div></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-sm text-slate-400">Quantity<input value={quantity} onChange={(e) => { setQuantity(e.target.value); setSaved(false); }} inputMode="decimal" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-white outline-none focus:border-cyan-300/50" /></label><label className={`text-sm text-slate-400 ${mode === "market" ? "opacity-60" : ""}`}>Limit price{mode === "market" ? <span className="ml-2 text-xs text-amber-200">Not used in market mode</span> : null}<input disabled={mode === "market"} value={price} onChange={(e) => { setPrice(e.target.value); setSaved(false); }} inputMode="decimal" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-white outline-none disabled:cursor-not-allowed disabled:opacity-50 focus:border-cyan-300/50" /></label></div><div className="mt-5 grid gap-3 sm:grid-cols-3">{[{ label: "Time in force", value: "Not selected" }, { label: "Account", value: "Not connected" }, { label: "Buying power", value: "Not verified" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div>{!valid && <p className="mt-4 flex items-center gap-2 text-sm text-amber-200"><TriangleAlert className="size-4" />Enter a positive quantity and, for limit mode, a positive limit price.</p>}<div className="mt-5 flex flex-wrap gap-2"><Button disabled={!valid || !acknowledged} className={side === "buy" ? "bg-cyan-300/20 text-cyan-100/40" : "bg-rose-300/20 text-rose-100/40"}>{side === "buy" ? "Submit buy unavailable" : "Submit sell unavailable"}</Button><Button disabled variant="outline" className="border-white/10 text-white/40">Preview payload unavailable</Button></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Risk and consent</p><h2 className="mt-2 text-2xl font-black">Before any real submission</h2><div className="mt-5 space-y-3">{[{ label: "Notional", value: `${notional} ${mode === "limit" ? "USD local" : "without quote"}` }, { label: "Fees", value: "Unavailable" }, { label: "Slippage", value: "Unavailable" }, { label: "Balance", value: "Not verified" }, { label: "Settlement", value: "Blocked" }].map((item) => <div key={item.label} className="flex items-center justify-between rounded-xl border border-white/10 p-3"><span className="text-sm text-slate-400">{item.label}</span><span className="text-sm font-semibold text-amber-200">{item.value}</span></div>)}</div><div className="mt-5 flex gap-3"><button aria-label="Acknowledge placement boundary" onClick={() => { setAcknowledged(!acknowledged); setSaved(false); }} className={`flex size-5 shrink-0 items-center justify-center rounded border ${acknowledged ? "border-cyan-300 bg-cyan-300 text-slate-950" : "border-white/20"}`}>{acknowledged ? "✓" : ""}</button><p className="text-sm leading-6 text-slate-400">I understand this builder is local-only and cannot place, sign, charge, or execute a real order.</p></div><div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="text-sm leading-6 text-slate-300">This interface is not financial advice. A production implementation needs verified suitability, permissions, balances, pricing, fees, execution, and audit controls.</p></div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Execution readiness</p><h2 className="mt-2 text-2xl font-black">Evidence required before enablement</h2><div className="mt-5 space-y-3">{["Authenticated account and role permissions for this instrument and venue", "Live quote or validated limit semantics, precision, lot size, and stale-data policy", "Balance, buying-power, fee, slippage, risk, and duplicate-submission checks", "Idempotent authorization, signing, server submission, status polling, and receipts", "Failure, cancellation, reconciliation, privacy, support, audit, and incident response"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><LockKeyhole className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake execution</h2><p className="mt-3 text-sm leading-6 text-slate-400">Saving the intent changes local UI state only. The disabled submit controls are an intentional safety boundary, not a missing success state.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Buy and sell preserved", description: "Side switching remains interactive while the venue and account boundary stays explicit.", icon: FileInput, status: "Local intent" }, { title: "Market mode is honest", description: "No quote is invented when a live market source is unavailable.", icon: WifiOff, status: "No quote" }, { title: "Submission is gated", description: "Validation and acknowledgment are required before the intentionally disabled control.", icon: ShieldCheck, status: "Guardrail" }]} /></main></div>;
}
