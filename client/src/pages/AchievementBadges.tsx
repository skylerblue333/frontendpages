import { useState } from "react";
import { Award, CheckCircle2, LockKeyhole, ShieldAlert, Sparkles } from "lucide-react";
import { Card } from "@/components/ui/card";
import { Button } from "@/components/ui/button";

const badgeConcepts = [
  { name: "Getting started", detail: "A local onboarding milestone concept requiring a persisted account event and completion rule." },
  { name: "Builder mindset", detail: "A local contribution concept requiring verified activity, ownership, moderation, and review." },
  { name: "Community care", detail: "A local community-safety concept requiring governed participation and accountable moderation." },
  { name: "Learning momentum", detail: "A local education concept requiring enrolled course events, assessment, accessibility, and privacy." },
];

export default function AchievementBadges() {
  const [selected, setSelected] = useState(0);
  const [saved, setSaved] = useState(false);
  const badge = badgeConcepts[selected];
  return <main className="min-h-screen bg-gradient-to-br from-slate-950 via-purple-950/60 to-slate-900 p-6 text-white sm:p-10"><div data-ui-polish="batch-180" className="mx-auto max-w-6xl"><div className="flex items-center gap-4"><div className="flex size-12 items-center justify-center rounded-2xl bg-purple-300/10 text-purple-200"><Award className="size-6" /></div><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-purple-200/70">Recognition governance</p><h1 className="text-3xl font-black sm:text-4xl">Achievement workspace</h1></div><span className="ml-auto rounded-full border border-amber-300/20 bg-amber-300/[0.06] px-3 py-1 text-xs text-amber-100">No awards issued</span></div><div className="mt-8 rounded-3xl border border-white/10 bg-gradient-to-br from-purple-400/[0.14] via-indigo-400/[0.08] to-transparent p-6 sm:p-10"><div className="flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.2em] text-purple-200"><ShieldAlert className="size-4" />Truthful badge boundary</div><h2 className="mt-4 max-w-3xl text-4xl font-black tracking-tight">Celebrate progress only when the record can prove it.</h2><p className="mt-4 max-w-3xl text-base leading-7 text-slate-300">This screen preserves badge and recognition planning without claiming a user earned an award, completed a milestone, reached a rank, received XP, or obtained a reward. Concepts remain local until identity, event, policy, and persistence systems are verified.</p><div className="mt-7 flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-purple-200 text-slate-950 hover:bg-purple-100">{saved ? "Plan saved locally" : "Save badge plan locally"}</Button><Button onClick={() => setSaved(false)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">Reset</Button></div></div><section className="mt-8 grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><div className="rounded-2xl border border-white/10 bg-white/[0.04] p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Badge concepts</p><div className="mt-5 space-y-3">{badgeConcepts.map((item, index) => <button key={item.name} type="button" onClick={() => setSelected(index)} className={`w-full rounded-xl border p-4 text-left transition ${selected === index ? "border-purple-300/40 bg-purple-300/[0.08]" : "border-white/10 hover:border-white/20"}`}><div className="flex items-center gap-3"><Sparkles className="size-4 text-purple-200" /><span className="font-semibold">{item.name}</span><span className="ml-auto text-xs text-amber-200">Concept</span></div><p className="mt-2 text-sm leading-6 text-slate-500">{item.detail}</p></button>)}</div></div><div className="rounded-2xl border border-white/10 bg-white/[0.04] p-6"><div className="flex items-start gap-4"><div className="flex size-12 shrink-0 items-center justify-center rounded-2xl bg-purple-300/10 text-purple-200"><Award className="size-6" /></div><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected concept</p><h2 className="mt-2 text-3xl font-black">{badge.name}</h2><p className="mt-3 text-sm leading-6 text-slate-400">{badge.detail}</p></div></div><div className="mt-7 grid gap-3 sm:grid-cols-3">{["Status", "Progress", "Award"].map(label => <div key={label} className="rounded-xl border border-white/10 p-4"><p className="text-xs text-slate-500">{label}</p><p className="mt-2 font-bold text-amber-200">Unavailable</p></div>)}</div><div className="mt-6 rounded-2xl border border-dashed border-white/15 bg-black/20 p-8 text-center"><Award className="mx-auto size-9 text-slate-600" /><p className="mt-3 font-semibold">No recognition evidence loaded</p><p className="mt-2 text-sm leading-6 text-slate-500">Connect authenticated event history, eligibility policy, anti-abuse controls, persistence, privacy, accessibility, appeals, and audit before displaying an earned badge.</p></div><div className="mt-6 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Claim unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Share unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Redeem unavailable</Button></div></div></section><section className="mt-8 rounded-2xl border border-white/10 bg-white/[0.04] p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Recognition gates</p><h2 className="mt-2 text-2xl font-black">Before issuing a badge</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Authenticated identity and event provenance", "Eligibility, completion, and anti-abuse policy", "Persistence, revocation, appeals, and audit", "Privacy, accessibility, safeguarding, and support", "Clear separation from XP, tokens, money, rank, or financial outcome", "Owner approval, correction path, and user communication"].map(item => <div key={item} className="flex items-start gap-3 rounded-xl border border-white/10 p-4 text-sm leading-6 text-slate-300"><LockKeyhole className="mt-1 size-4 shrink-0 text-amber-200" />{item}</div>)}</div></section><div className="mt-8 flex items-center gap-3 text-sm text-slate-500"><CheckCircle2 className="size-4 text-emerald-200" /><strong className="text-amber-100">No achievement, reward, rank, or completion claim is made.</strong> Local planning state only.</div></div></main>;
}
