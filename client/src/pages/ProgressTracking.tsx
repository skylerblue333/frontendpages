import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Activity, Award, BookOpen, Check, Clock3, Flame, Gauge, LockKeyhole, RefreshCw, ShieldAlert, Target, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const tabs = ["Overview", "Milestones", "Daily activity", "Skills"];
const milestones = [{ label: "Define baseline", detail: "Choose what evidence matters", done: false }, { label: "Review evidence", detail: "Validate progress with a source", done: false }, { label: "Governed outcome", detail: "Pass a real completion gate", done: false }];
const skills = ["Foundations", "Practice", "Reflection", "Transfer"];
export default function ProgressTracking() {
  const [tab, setTab] = useState(tabs[0]);
  const [selected, setSelected] = useState(milestones[0].label);
  const [done, setDone] = useState<string[]>([]);
  const [goal, setGoal] = useState("Unscoped local goal");
  const [saved, setSaved] = useState(false);
  const [showGates, setShowGates] = useState(false);
  const completion = useMemo(() => Math.round((done.length / milestones.length) * 100), [done.length]);
  const toggle = (label: string) => setDone((items) => items.includes(label) ? items.filter((item) => item !== label) : [...items, label]);
  const reset = () => { setTab(tabs[0]); setSelected(milestones[0].label); setDone([]); setGoal("Unscoped local goal"); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Activity} eyebrow="ProgressTracking · Longitudinal preview" title="Track the questions, not imaginary history." description="Explore a local progress workspace with goals, milestones, daily activity, skills, streak intent, and review gates. No user history, learning events, XP, completion, time, streak, reward, or achievement is measured." badge="Tracking workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Tracking saved locally" : "Save tracking locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review evidence gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset tracking</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Local progress", value: `${completion}%`, hint: "Browser visualization", icon: Gauge, tone: "cyan" }, { label: "Goal", value: goal === "Unscoped local goal" ? "Unscoped" : "Drafted", hint: "No owner source", icon: Target, tone: "violet" }, { label: "Streak", value: "Unmeasured", hint: "No date ledger", icon: Flame, tone: "amber" }, { label: "Rewards", value: "Unavailable", hint: "No achievement source", icon: Award, tone: "slate" }]} /><ScreenPreviewBanner title="Tracking evidence boundary"><strong>This is a local longitudinal-progress preview, not a learning or account record.</strong> Goal text, milestone checkmarks, tabs, percentage, daily activity labels, skills, streak intent, and saved state are browser concepts. No user, event, time, completion, XP, reward, certification, streak, or achievement is persisted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.82fr_1.18fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Local goal</p><h2 className="mt-2 text-2xl font-black">Give the tracker a scope</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Unverified</Badge></div><input value={goal} onChange={(event) => setGoal(event.target.value)} className="mt-6 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-white outline-none" /><div className="mt-6 h-3 overflow-hidden rounded-full bg-white/10"><div className="h-full rounded-full bg-gradient-to-r from-cyan-300 to-violet-300" style={{ width: `${completion}%` }} /></div><p className="mt-2 text-xs text-slate-500">{completion}% local visualization; not a completion claim.</p><div className="mt-6 space-y-3">{milestones.map((item) => <button key={item.label} onClick={() => setSelected(item.label)} className={`flex w-full items-center gap-3 rounded-xl border p-4 text-left ${selected === item.label ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><button aria-label={`toggle ${item.label}`} onClick={(event) => { event.stopPropagation(); toggle(item.label); }} className={`flex size-6 items-center justify-center rounded-full border ${done.includes(item.label) ? "border-cyan-300 bg-cyan-300 text-slate-950" : "border-white/20 text-slate-600"}`}>{done.includes(item.label) && <Check className="size-3" />}</button><div className="flex-1"><p className="font-semibold">{item.label}</p><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><Badge variant="outline" className={done.includes(item.label) ? "border-cyan-300/30 text-cyan-200" : "border-white/10 text-slate-500"}>{done.includes(item.label) ? "Local done" : "Needs review"}</Badge></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected milestone</p><h2 className="mt-2 text-2xl font-black">{selected}</h2><div className="mt-6 rounded-xl border border-white/10 p-5"><p className="text-sm leading-7 text-slate-300">Use this local milestone to decide what evidence, owner, date, and review rule a real tracking service would need before it can report progress.</p></div><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Dates", value: "Unknown", icon: Clock3 }, { label: "Events", value: "Not loaded", icon: Activity }, { label: "Assessment", value: "Unconfigured", icon: BookOpen }, { label: "Reward", value: "Unavailable", icon: Award }].map((item) => { const Icon = item.icon; return <div key={item.label} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><Icon className="size-4 text-slate-500" /><div><p className="text-xs text-slate-500">{item.label}</p><p className="mt-1 text-sm font-semibold text-amber-200">{item.value}</p></div></div>; })}</div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No longitudinal claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local checkmark cannot prove learning, practice, time, streak, mastery, completion, reward eligibility, or certification.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex flex-wrap gap-2">{tabs.map((item) => <button key={item} onClick={() => setTab(item)} className={`flex items-center gap-2 rounded-lg border px-4 py-2 text-sm ${tab === item ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-200" : "border-white/10 text-slate-500"}`}>{item === "Overview" && <Gauge className="size-4" />}{item === "Milestones" && <Target className="size-4" />}{item === "Daily activity" && <Activity className="size-4" />}{item === "Skills" && <BookOpen className="size-4" />}{item}</button>)}</div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-10">{tab === "Skills" ? <div className="grid gap-3 sm:grid-cols-2">{skills.map((skill) => <div key={skill} className="rounded-xl border border-white/10 p-4"><p className="font-semibold">{skill}</p><p className="mt-2 text-sm text-slate-500">Not assessed · evidence unavailable</p></div>)}</div> : <div className="text-center"><Activity className="mx-auto size-10 text-violet-300/40" /><h3 className="mt-4 text-xl font-black">{tab} unavailable in preview</h3><p className="mx-auto mt-2 max-w-xl text-sm leading-6 text-slate-500">No {tab.toLowerCase()} ledger is connected. Nothing is fabricated to imply history, consistency, completion, or mastery.</p><Button disabled className="mt-5 bg-slate-700 text-slate-400">Load {tab.toLowerCase()} unavailable</Button></div>}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Tracking gates</p><h2 className="mt-2 text-2xl font-black">What a real tracker must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Authenticated owner and scoped goal or course", "Event schema, timestamps, source, and offline reconciliation", "Evidence for completion, review, mastery, and state transitions", "Streak rules, timezone, missed-day handling, and reset semantics", "Privacy, retention, export, deletion, notifications, and audit", "Reward, certification, promotion, and achievement governance"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Tracking surface preserved", description: "Goals, milestones, local percentage, daily activity, skills, streak/reward states, tabs, save/reset, and evidence gates remain interactive.", icon: Activity, status: "Local tracker" }, { title: "No history theater", description: "Users, events, time, completion, XP, mastery, streaks, rewards, certifications, and achievements are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before progress", description: "Real tracking needs ownership, event contracts, review rules, privacy, recovery, and governed reward criteria.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
