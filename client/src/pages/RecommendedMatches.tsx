import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Check, Filter, Handshake, LockKeyhole, MessageCircle, RefreshCw, Search, ShieldAlert, SlidersHorizontal, UsersRound, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const matches = [{ id: 1, name: "Learning collaborator", category: "Education", reason: "A compatibility concept awaiting consented goals, profile fields, and safety review.", state: "Preview only" }, { id: 2, name: "Community interest match", category: "Community", reason: "A discovery concept requiring moderation, privacy, anti-harassment, and exposure controls.", state: "Needs evidence" }, { id: 3, name: "Game teammate concept", category: "Gaming", reason: "A team-fit concept awaiting verified preferences, availability, and fair matching policy.", state: "Unmeasured" }, { id: 4, name: "Professional peer concept", category: "Networking", reason: "A connection concept that must not infer identity, status, trust, or professional suitability.", state: "Blocked" }];
export default function RecommendedMatches() {
  const [query, setQuery] = useState(""); const [category, setCategory] = useState("All"); const [selected, setSelected] = useState(1); const [criteria, setCriteria] = useState("Criteria not configured"); const [feedback, setFeedback] = useState("Feedback not connected"); const [saved, setSaved] = useState(false); const [showGates, setShowGates] = useState(false);
  const categories = ["All", ...Array.from(new Set(matches.map((item) => item.category)))]; const filtered = useMemo(() => matches.filter((item) => (category === "All" || item.category === category) && `${item.name} ${item.category} ${item.reason}`.toLowerCase().includes(query.toLowerCase())), [category, query]); const match = matches.find((item) => item.id === selected) ?? matches[0];
  const reset = () => { setQuery(""); setCategory("All"); setSelected(1); setCriteria("Criteria not configured"); setFeedback("Feedback not connected"); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={UsersRound} eyebrow="RecommendedMatches · Compatibility preview" title="Prove compatibility before calling it a match." description="Explore local compatibility concepts with search, categories, criteria, reason codes, ranking intent, profile posture, messaging, feedback, refresh, moderation, save, reset, and evidence gates. No live users, profiles, scores, predictions, relationship, trust, or safety claim is connected." badge="Match workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "View saved locally" : "Save view locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review match gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset view</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Concepts", value: `${matches.length} local`, hint: "No match output", icon: UsersRound, tone: "cyan" }, { label: "Profiles", value: "Unavailable", hint: "No identity source", icon: Handshake, tone: "violet" }, { label: "Compatibility", value: "Unmeasured", hint: "No criteria source", icon: SlidersHorizontal, tone: "amber" }, { label: "Safety", value: "Review needed", hint: "No moderation source", icon: ShieldAlert, tone: "slate" }]} /><ScreenPreviewBanner title="Match evidence boundary"><strong>This is a local compatibility and discovery preview, not a social or relationship matching service.</strong> Match cards, categories, criteria, reason codes, ranking intent, profile posture, feedback, saved state, messaging and moderation gates are browser concepts. No user, profile, score, compatibility, identity, trust, safety, relationship, or outcome is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Search className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local compatibility concepts" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-4 flex flex-wrap gap-2">{categories.map((entry) => <Button key={entry} onClick={() => setCategory(entry)} size="sm" variant="outline" className={category === entry ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-100" : "border-white/10 text-slate-400"}><Filter className="mr-1 size-3" />{entry}</Button>)}</div><div className="mt-6 space-y-3">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{item.name}</p><p className="mt-1 text-sm text-slate-500">{item.reason}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.state}</Badge></div><div className="mt-4"><Badge variant="outline" className="border-white/10 text-slate-500">{item.category}</Badge></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected compatibility concept</p><h2 className="mt-2 text-2xl font-black">{match.name}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{match.reason}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Category", value: match.category }, { label: "State", value: match.state }, { label: "Criteria", value: criteria }, { label: "Profiles", value: "Unavailable" }, { label: "Compatibility", value: "Unmeasured" }, { label: "Moderation", value: "Review needed" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2"><label className="text-sm text-slate-400">Matching criteria<select value={criteria} onChange={(event) => setCriteria(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Criteria not configured</option><option>Shared goals intent</option><option>Shared interests intent</option><option>Availability intent</option></select></label><label className="text-sm text-slate-400">Feedback posture<select value={feedback} onChange={(event) => setFeedback(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Feedback not connected</option><option>Helpful intent</option><option>Not relevant intent</option><option>Report intent</option></select></label></div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-8 text-center"><Handshake className="mx-auto size-7 text-slate-600" /><p className="mt-3 font-semibold">No match output loaded</p><p className="mt-2 text-sm text-slate-500">Connect consented profiles, matching policy, safety review, and moderation before displaying a person or initiating contact.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400"><RefreshCw className="mr-2 size-4" />Refresh unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500"><MessageCircle className="mr-2 size-4" />Message unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Report unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No compatibility or safety claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local concept does not prove that people are compatible, trustworthy, safe, available, or likely to connect.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Match gates</p><h2 className="mt-2 text-2xl font-black">What a real matching system must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Identity, consent, age boundaries, privacy, sensitive traits, opt-out, deletion, and tenant isolation", "Profile authenticity, freshness, availability, moderation, abuse prevention, and content provenance", "Criteria definitions, weights, ranking, fairness, explainability, cold start, drift, and reproducibility", "Safety review, harassment controls, block/report, contact permissions, rate limits, and human oversight", "High-impact contexts, employment, education, finance, dating, health, and relationship implications require review", "Feedback, appeals, incidents, audits, experiments, rollback, support, and outcome measurement"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Match surface preserved", description: "Concepts, search, categories, criteria, reasons, ranking, profile posture, messaging, feedback, refresh, moderation, save/reset, and gates remain interactive.", icon: UsersRound, status: "Local concepts" }, { title: "No relationship theater", description: "Users, profiles, scores, compatibility, identity, trust, safety, relationships, and outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Safety before contact", description: "Real matching needs consent, profile authenticity, fairness, moderation, abuse prevention, contact controls, and support.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
