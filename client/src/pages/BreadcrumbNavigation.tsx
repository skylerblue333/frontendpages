import { useMemo, useState } from "react";
import { Accessibility, ArrowRight, Check, ChevronRight, Clipboard, Code2, Eye, Home, Info, Keyboard, Layers3, Menu, MoreHorizontal, Route, Settings2, Smartphone, Sparkles, Tablet, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const DEFAULT_CRUMBS = ["Home", "Ecosystem", "Creator Tools", "Breadcrumb Navigation"];
const DEPTHS = [2, 3, 4, 5];

export default function BreadcrumbNavigation() { const [depth, setDepth] = useState(4); const [compact, setCompact] = useState(false); const [copied, setCopied] = useState(false); const [label, setLabel] = useState("Breadcrumb Navigation"); const crumbs = useMemo(() => [...DEFAULT_CRUMBS.slice(0, depth - 1), label || "Current page"], [depth, label]); const copyMarkup = () => { setCopied(true); window.setTimeout(() => setCopied(false), 1200); }; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Route} eyebrow="Navigation · Interaction Patterns" title="Make location obvious without stealing the page." description="Explore breadcrumb depth, compact overflow, responsive behavior, keyboard guidance, and semantic structure. This page is a component reference preview, not a claim that every route currently uses the same navigation pattern." badge="Preview navigation lab"><div className="flex flex-wrap gap-2"><Button onClick={copyMarkup} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200">{copied ? <><Check className="mr-2 size-4" />Copied preview</> : <><Clipboard className="mr-2 size-4" />Copy markup preview</>}</Button><Button onClick={() => setCompact(!compact)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{compact ? <><Layers3 className="mr-2 size-4" />Expanded trail</> : <><MoreHorizontal className="mr-2 size-4" />Compact trail</>}</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Trail depth", value: String(depth), hint: "Interactive hierarchy", icon: Route }, { label: "Semantic nav", value: "Required", hint: "Landmark and current page", icon: Accessibility, tone: "violet" }, { label: "Overflow mode", value: compact ? "Compact" : "Expanded", hint: "Responsive preview", icon: MoreHorizontal, tone: "amber" }, { label: "Keyboard path", value: "Defined", hint: "Focus order guidance", icon: Keyboard, tone: "slate" }]} /><ScreenPreviewBanner title="Navigation evidence boundary">This lab demonstrates a component pattern. A production navigation system needs route-aware labels, localized names, stable links, current-page semantics, mobile overflow behavior, focus management, back-navigation rules, and analytics that do not infer user intent from a render alone.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.8fr_1.2fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><Settings2 className="size-5 text-cyan-300" /><h2 className="text-xl font-bold">Pattern controls</h2></div><div className="mt-6 space-y-5"><div><label className="mb-2 block text-sm font-medium text-slate-300" htmlFor="current-label">Current page label</label><Input id="current-label" value={label} onChange={(event) => setLabel(event.target.value)} className="border-white/10 bg-black/20 text-white" /></div><div><p className="mb-2 text-sm font-medium text-slate-300">Trail depth</p><div className="grid grid-cols-4 gap-2">{DEPTHS.map((item) => <button key={item} onClick={() => setDepth(item)} className={`rounded-xl border p-3 text-sm transition ${depth === item ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-200" : "border-white/10 bg-black/15 text-slate-500 hover:text-white"}`}>{item} levels</button>)}</div></div><div className="rounded-xl border border-white/10 bg-black/15 p-4"><div className="flex items-center gap-2 text-sm font-medium"><Keyboard className="size-4 text-violet-300" />Keyboard contract</div><p className="mt-2 text-sm leading-6 text-slate-400">Links receive focus in reading order. The current page is announced but is not an interactive link. Compact overflow must remain reachable without trapping focus.</p></div></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Live pattern preview</p><h2 className="mt-1 text-2xl font-black">Route context</h2></div><Badge variant="outline" className="border-cyan-300/25 text-cyan-200">{compact ? "Compact" : "Expanded"}</Badge></div><div className="mt-8 rounded-2xl border border-cyan-300/20 bg-gradient-to-br from-cyan-300/[0.08] to-violet-300/[0.08] p-6"><nav aria-label="Breadcrumb preview"><ol className="flex flex-wrap items-center gap-2 text-sm">{compact && crumbs.length > 3 ? <><li><Button variant="ghost" size="icon" className="size-8 text-slate-300"><MoreHorizontal className="size-4" /></Button></li><ChevronRight className="size-4 text-slate-600" /><li className="text-slate-400">{crumbs[crumbs.length - 2]}</li><ChevronRight className="size-4 text-slate-600" /><li aria-current="page" className="font-semibold text-cyan-200">{crumbs[crumbs.length - 1]}</li></> : crumbs.map((crumb, index) => <li key={`${crumb}-${index}`} className="flex items-center gap-2">{index > 0 && <ChevronRight className="size-4 text-slate-600" />}{index === crumbs.length - 1 ? <span aria-current="page" className="font-semibold text-cyan-200">{crumb}</span> : <button className="text-slate-400 hover:text-white">{index === 0 && <Home className="mr-1 inline size-3" />}{crumb}</button>}</li>)}</ol></nav><div className="mt-10 flex items-center gap-4 border-t border-white/10 pt-6"><div className="flex size-12 items-center justify-center rounded-2xl bg-cyan-300/10"><Eye className="size-5 text-cyan-300" /></div><div><p className="text-lg font-semibold">Current destination stays visible</p><p className="text-sm text-slate-400">{crumbs.length} labels · current page announced via <code className="text-cyan-200">aria-current</code></p></div></div></div></CardContent></Card></section><section className="grid gap-6 md:grid-cols-3"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><Smartphone className="size-5 text-cyan-300" /><h3 className="mt-3 font-semibold">Mobile compression</h3><p className="mt-2 text-sm leading-6 text-slate-400">Collapse middle levels only when the destination and surrounding context remain understandable.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><Tablet className="size-5 text-violet-300" /><h3 className="mt-3 font-semibold">Responsive handoff</h3><p className="mt-2 text-sm leading-6 text-slate-400">Use the same route truth across desktop, tablet, and mobile rather than separate invented hierarchies.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><Code2 className="size-5 text-amber-300" /><h3 className="mt-3 font-semibold">Semantic structure</h3><p className="mt-2 text-sm leading-6 text-slate-400">Use a navigation landmark, ordered list, link semantics, and a current-page marker.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Location before decoration", description: "Breadcrumbs explain where the user is and how to move upward; they should not compete with the page title.", icon: Route, status: "Pattern" }, { title: "Overflow with intent", description: "Compact trails can hide middle levels, but never hide the destination or trap keyboard focus.", icon: MoreHorizontal, status: "Responsive" }, { title: "Current page is state", description: "The final crumb is announced as the current page rather than presented as a misleading link.", icon: Accessibility, status: "Guardrail" }]} /></main></div>; }
