import { useMemo, useState } from "react";
import { Blocks, Check, ClipboardCheck, Database, Fingerprint, Hash, Info, RefreshCw, Search, ShieldAlert, Wallet, WifiOff } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

type Block = { height: string; hash: string; txs: number; state: string; note: string };
const BLOCKS: Block[] = [{ height: "Preview block A", hash: "Hash unavailable", txs: 0, state: "Synthetic", note: "A chain-shaped record for reviewing block evidence fields." }, { height: "Preview block B", hash: "Hash unavailable", txs: 0, state: "Synthetic", note: "No timestamp, proposer, finality, or transaction list is claimed." }, { height: "Preview block C", hash: "Hash unavailable", txs: 0, state: "Unavailable", note: "Live chain access is not configured for this workspace." }];
const CHAINS = ["Chain not selected", "EVM preview", "UTXO preview", "Custom network preview"];
export default function ChainExplorer() { const [chain, setChain] = useState(CHAINS[0]); const [query, setQuery] = useState(""); const [mode, setMode] = useState("Blocks"); const [selectedHeight, setSelectedHeight] = useState(BLOCKS[0].height); const [saved, setSaved] = useState(false); const visible = useMemo(() => BLOCKS.filter((block) => `${block.height} ${block.hash} ${block.state}`.toLowerCase().includes(query.toLowerCase())), [query]); const selected = visible.find((block) => block.height === selectedHeight) ?? visible[0]; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Blocks} eyebrow="Blockchain · Explorer" title="Inspect the evidence before calling it on-chain." description="Review synthetic block records, query modes, chain selection, and evidence requirements. This page does not connect to an RPC, indexer, wallet, or explorer provider, and it does not claim live chain state." badge="Preview chain explorer"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><ClipboardCheck className="mr-2 size-4" />Save local query</Button><Button onClick={() => { setChain(CHAINS[0]); setQuery(""); setMode("Blocks"); setSaved(false); }} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset explorer</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Chain", value: chain === CHAINS[0] ? "Unselected" : "Preview", hint: "No network binding", icon: Database }, { label: "Blocks", value: String(visible.length), hint: "Synthetic records", icon: Blocks, tone: "violet" }, { label: "Latest height", value: "Unavailable", hint: "No RPC source", icon: Hash, tone: "amber" }, { label: "Wallet state", value: "Unavailable", hint: "No address lookup", icon: Wallet, tone: "slate" }]} /><ScreenPreviewBanner title="Explorer evidence boundary">A block-shaped card is not proof of a chain, height, hash, transaction, wallet, balance, timestamp, proposer, finality, or confirmation. Real explorer data requires a network identifier, trusted RPC or indexer source, consistency checks, reorg handling, rate limits, query provenance, and clear error states.</ScreenPreviewBanner><section className="flex flex-col gap-4 sm:flex-row"><select value={chain} onChange={(event) => { setChain(event.target.value); setSaved(false); }} className="rounded-md border border-white/10 bg-black/20 px-3 py-2 text-sm text-white"><option>{CHAINS[0]}</option><option>{CHAINS[1]}</option><option>{CHAINS[2]}</option><option>{CHAINS[3]}</option></select><div className="flex flex-1 gap-2">{["Blocks", "Transaction", "Wallet"].map((item) => <button key={item} onClick={() => setMode(item)} className={`rounded-xl border px-4 py-2 text-sm ${mode === item ? "border-cyan-300/40 bg-cyan-300/[0.1] text-cyan-200" : "border-white/10 text-slate-400"}`}>{item}</button>)}</div><div className="relative flex-1"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder={`${mode} query preview`} className="border-white/10 bg-black/20 pl-9 text-white placeholder:text-slate-500" /></div></section><section className="grid gap-6 lg:grid-cols-[1.05fr_0.95fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">{mode} evidence queue</p>{mode !== "Blocks" && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex items-center gap-2 font-semibold text-amber-200"><WifiOff className="size-4" />Lookup unavailable</div><p className="mt-2 text-sm leading-6 text-slate-300">No live RPC, indexer, wallet provider, or transaction service is connected for this query.</p></div>}<div className="mt-5 space-y-3">{visible.map((block) => <button key={block.height} onClick={() => setSelectedHeight(block.height)} className={`w-full rounded-xl border p-4 text-left ${selected?.height === block.height ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-black/15"}`}><div className="flex items-center gap-3"><Blocks className="size-5 text-cyan-300" /><div className="flex-1"><p className="font-semibold">{block.height}</p><p className="mt-1 text-xs text-slate-500">{block.hash} · {block.txs} tx preview</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{block.state}</Badge></div><p className="mt-3 text-xs text-slate-500">{block.note}</p></button>)}{visible.length === 0 && <p className="py-8 text-center text-sm text-slate-500">No synthetic records match this query.</p>}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6">{selected ? <><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Selected evidence</p><h2 className="mt-2 text-2xl font-black">{selected.height}</h2><p className="mt-2 text-sm text-slate-400">{chain} · {selected.state}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Not live</Badge></div><p className="mt-6 text-sm leading-6 text-slate-300">{selected.note}</p><div className="mt-6 space-y-2">{["Network identity and chain ID", "Block hash and parent hash", "Timestamp and proposer", "Transaction list and receipts", "Finality and reorg state", "Wallet ownership and balance"].map((item) => <div key={item} className="flex items-center gap-3 rounded-lg border border-white/10 bg-black/15 p-3"><Info className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Unavailable</span></div>)}</div>{saved && <div className="mt-5 rounded-xl border border-cyan-300/20 bg-cyan-300/[0.06] p-4"><div className="flex items-center gap-2 font-semibold text-cyan-200"><Check className="size-4" />Local query saved</div><p className="mt-1 text-sm text-slate-400">No network request, wallet lookup, transaction lookup, or chain record was created.</p></div>}</> : <p className="text-sm text-slate-500">Select a synthetic record.</p>}</CardContent></Card></section><section className="grid gap-4 md:grid-cols-3"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><Fingerprint className="size-5 text-cyan-300" /><h3 className="mt-3 font-semibold">Network identity first</h3><p className="mt-2 text-sm leading-6 text-slate-400">A chain name without network ID, RPC source, and consistency checks is not a verified network.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><ShieldAlert className="size-5 text-violet-300" /><h3 className="mt-3 font-semibold">Reorgs change evidence</h3><p className="mt-2 text-sm leading-6 text-slate-400">Blocks, receipts, and confirmations need finality and reorganization handling.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><WifiOff className="size-5 text-amber-300" /><h3 className="mt-3 font-semibold">No fake explorer</h3><p className="mt-2 text-sm leading-6 text-slate-400">A preview record is not a live chain, transaction, wallet balance, or confirmation.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Query before claim", description: "Keep chain, block, transaction, and wallet query modes visibly separate.", icon: Search, status: "Pattern" }, { title: "Evidence has provenance", description: "Network and block fields need a source, freshness, consistency, and error policy.", icon: Database, status: "Required" }, { title: "No fake chain state", description: "Synthetic records are not blocks, hashes, receipts, balances, or live telemetry.", icon: WifiOff, status: "Guardrail" }]} /></main></div>; }
