import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Check, Clock3, FileCheck2, Filter, Gauge, LockKeyhole, RefreshCw, Search, ShieldAlert, Sparkles, Wallet, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const POOLS = [{ id: "pool-a", name: "Northstar reference", algorithm: "SHA-256", region: "Region to verify", focus: "Custody and payout review", state: "Needs source" }, { id: "pool-b", name: "Atlas reference", algorithm: "Scrypt", region: "Region to verify", focus: "Fee and settlement review", state: "Research" }, { id: "pool-c", name: "Orbit reference", algorithm: "GPU workload", region: "Region to verify", focus: "Uptime and support review", state: "Unverified" }, { id: "pool-d", name: "Validator reference", algorithm: "Proof of stake", region: "Network to verify", focus: "Validator terms review", state: "Not a mining pool" }];
const FILTERS = ["all", "SHA-256", "Scrypt", "GPU workload", "Proof of stake"];
const GATES = ["Pool identity and operator", "Fee and payout policy", "Current hashrate and uptime", "Network and reward rules", "Wallet, custody, and support"];

export default function MiningPoolSelector() {
  const [filter, setFilter] = useState("all");
  const [query, setQuery] = useState("");
  const [selected, setSelected] = useState(POOLS[0].id);
  const [saved, setSaved] = useState(false);
  const pool = POOLS.find((item) => item.id === selected) ?? POOLS[0];
  const visible = useMemo(() => POOLS.filter((item) => (filter === "all" || item.algorithm === filter) && `${item.name} ${item.algorithm} ${item.focus}`.toLowerCase().includes(query.toLowerCase())), [filter, query]);
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Filter} eyebrow="Mining · Pool selector" title="Compare the diligence, not a fabricated payout." description="Review synthetic pool references with search, algorithm filters, local selection, and a diligence checklist. No pool share, hashrate, fee, uptime, profitability, reward, or payout is asserted." badge="Pool comparison preview"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Comparison saved locally" : "Save comparison"}</Button><Button onClick={() => { setFilter("all"); setQuery(""); setSelected(POOLS[0].id); setSaved(false); }} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset selector</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Pool references", value: String(POOLS.length), hint: "Synthetic cards", icon: Filter }, { label: "Hashrate", value: "Off", hint: "No network feed", icon: Gauge, tone: "amber" }, { label: "Payout", value: "Blocked", hint: "No wallet route", icon: Wallet, tone: "violet" }, { label: "Profitability", value: "Off", hint: "No price or reward", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="Pool evidence boundary"><strong>Pool names, algorithms, regions, focus areas, and statuses are comparison fixtures—not current pool identity, hashrate, market share, fees, uptime, earnings, payout terms, or mining recommendations.</strong> Production pool selection requires verified operator identity, current network data, contract and payout terms, security review, monitoring, support, and user disclosures.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex flex-col gap-3 sm:flex-row"><div className="relative flex-1"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search pool references" className="w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 pl-9 text-white placeholder:text-slate-500" /></div><div className="flex gap-2 overflow-x-auto">{FILTERS.map((item) => <button key={item} onClick={() => setFilter(item)} className={`rounded-xl border px-3 py-2 text-xs whitespace-nowrap ${filter === item ? "border-cyan-300/40 bg-cyan-300/[0.1] text-cyan-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div></div><div className="mt-5 space-y-3">{visible.map((item) => <button key={item.id} onClick={() => { setSelected(item.id); setSaved(false); }} className={`w-full rounded-2xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.07]" : "border-white/10 bg-black/10"}`}><div className="flex items-start justify-between gap-3"><div><h2 className="font-black">{item.name}</h2><p className="mt-1 text-sm text-slate-400">{item.algorithm} · {item.region}</p></div><Badge variant="outline" className="border-amber-300/20 text-[10px] text-amber-200">{item.state}</Badge></div><p className="mt-3 text-xs text-slate-500">Focus: {item.focus}</p></button>)}{visible.length === 0 && <div className="p-8 text-center text-slate-500">No pool references match this view.</div>}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Selected reference</p><h2 className="mt-2 text-3xl font-black">{pool.name}</h2><p className="mt-3 text-slate-400">{pool.algorithm} · {pool.focus}</p><div className="mt-6 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex items-center gap-2 font-semibold text-amber-200"><ShieldAlert className="size-4" />Diligence required</div><p className="mt-2 text-sm leading-6 text-slate-400">No operator, pool endpoint, current fee, hashrate, payout minimum, reward statement, or wallet destination is attached to this card.</p></div><div className="mt-5 space-y-3">{GATES.map((gate) => <div key={gate} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{gate}</span><span className="text-xs text-amber-200">Verify</span></div>)}</div></CardContent></Card></section><section className="grid gap-4 md:grid-cols-3">{[{ title: "Hashrate needs freshness", description: "Pool capacity and share data require a current authoritative source and timestamp.", icon: Gauge }, { title: "Payout needs settlement", description: "Fees, minimums, rewards, and wallet transfers need contracts, reconciliation, and recovery.", icon: Wallet }, { title: "Selector is not advice", description: "A comparison UI cannot establish profitability, reliability, security, or suitability.", icon: FileCheck2 }].map((item) => <Card key={item.title} className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><item.icon className="size-5 text-cyan-300" /><h2 className="mt-4 font-black">{item.title}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{item.description}</p></CardContent></Card>)}</section><ScreenFeatureGrid features={[{ title: "Pool cards are not operators", description: "Synthetic references preserve comparison UX without asserting an active pool or network connection.", icon: WifiOff, status: "Guardrail" }, { title: "Fees require terms", description: "Fee and payout claims need sourced policies, current network context, and a reconciliation trail.", icon: FileCheck2, status: "Required" }, { title: "No fake mining result", description: "The selector does not expose hashrate, reward, payout, wallet, or profitability actions.", icon: LockKeyhole, status: "Unavailable" }]} /></main></div>;
}
