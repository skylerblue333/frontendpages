import { useState } from "react";
import { Bell, Check, Clock3, Filter, GitBranch, Info, ListChecks, LockKeyhole, RefreshCw, ShieldAlert, UserRound, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

const tasks = [
  { name: "Core platform readiness review", category: "Core", state: "In review", detail: "Local task concept pending authenticated ownership, acceptance criteria, dependencies, and auditable completion review." },
  { name: "Curriculum accessibility check", category: "Education", state: "Blocked", detail: "Local task concept pending learner scope, content provenance, reviewer assignment, and privacy-safe progress evidence." },
  { name: "Financial control review", category: "Financial", state: "Draft", detail: "Local task concept pending authorization, non-custodial boundaries, approval evidence, and transaction-safe audit history." },
  { name: "Community safety audit", category: "Community", state: "Unverified", detail: "Local task concept pending scope, moderation ownership, evidence links, and accountable escalation policy." },
];

export default function TaskList() {
  const [selected, setSelected] = useState(0);
  const [filter, setFilter] = useState("All work");
  const [saved, setSaved] = useState(false);
  const [showBoundary, setShowBoundary] = useState(false);
  const [refreshing, setRefreshing] = useState(false);
  const refresh = () => { setRefreshing(true); window.setTimeout(() => setRefreshing(false), 500); };
  const reset = () => { setSelected(0); setFilter("All work"); setSaved(false); setShowBoundary(false); setRefreshing(false); };
  const visible = tasks.filter((task) => filter === "All work" || task.category === filter);
  const task = tasks[selected] ?? tasks[0];
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={ListChecks} eyebrow="Task list · Planning preview" title="Plan work without inventing ownership or completion." description="Review a local task-list workspace with typed task concepts, filters, ownership, status, priority, due dates, dependencies, comments, notifications, permissions, retry, save/reset, and explicit evidence boundaries. No task record, assignee, progress, reminder, or completion is connected." badge="Evidence-bounded task planning"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save task view"}</Button><Button onClick={() => setShowBoundary((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showBoundary ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showBoundary ? "Close evidence boundary" : "Review task gates"}</Button><Button onClick={refresh} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className={`mr-2 size-4 ${refreshing ? "animate-spin" : ""}`} />{refreshing ? "Retrying local state" : "Retry task source"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><X className="mr-2 size-4" />Reset task view</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Tasks", value: "Local", hint: "No task source", icon: ListChecks, tone: "cyan" }, { label: "Ownership", value: "Unverified", hint: "No identity source", icon: UserRound, tone: "violet" }, { label: "Progress", value: "Unmeasured", hint: "No completion source", icon: Clock3, tone: "amber" }, { label: "Notifications", value: "Blocked", hint: "No delivery source", icon: Bell, tone: "slate" }]} /><ScreenPreviewBanner title="Task-list evidence boundary"><strong>This is a local task-planning preview, not evidence that a task exists, is assigned, started, progressing, overdue, notified, approved, executed, or completed.</strong> Typed task cards, filters, local saved state, retry feedback, and disabled create/assign/complete actions are browser concepts. No owner, assignee, due date, comment, artifact, reminder, or business outcome is asserted.</ScreenPreviewBanner><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-2 text-sm font-semibold text-slate-300"><Filter className="size-4 text-cyan-300" />Workstream filter</div><div className="mt-4 flex flex-wrap gap-2">{["All work", "Core", "Education", "Financial", "Community"].map((item) => <Button key={item} onClick={() => setFilter(item)} variant={filter === item ? "default" : "outline"} className={filter === item ? "bg-cyan-300 text-slate-950 hover:bg-cyan-200" : "border-white/10 bg-white/5 text-white hover:bg-white/10"}>{item}</Button>)}</div></CardContent></Card><section className="grid gap-4 lg:grid-cols-2">{visible.map((item) => <button key={item.name} onClick={() => setSelected(tasks.indexOf(item))} className={`rounded-2xl border p-5 text-left transition ${selected === tasks.indexOf(item) ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10 bg-white/[0.04]"}`}><div className="flex items-start justify-between"><div className="flex size-11 items-center justify-center rounded-2xl bg-cyan-300/10 text-cyan-200"><ListChecks className="size-5" /></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.state}</Badge></div><h2 className="mt-4 font-bold">{item.name}</h2><p className="mt-1 text-xs text-cyan-200">{item.category}</p><p className="mt-3 text-sm leading-6 text-slate-400">{item.detail}</p></button>)}</section><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200">Selected task concept</p><h2 className="mt-2 text-2xl font-black">{task.name}</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{task.state}</Badge></div><p className="mt-3 text-sm leading-6 text-slate-400">{task.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Owner / assignee", value: "Unavailable", icon: UserRound }, { label: "Priority / status", value: task.state, icon: ListChecks }, { label: "Due date / schedule", value: "Unavailable", icon: Clock3 }, { label: "Dependencies", value: "Unmapped", icon: GitBranch }, { label: "Comments / artifacts", value: "Unavailable", icon: Info }, { label: "Reminder / notification", value: "Blocked", icon: Bell }].map(({ label, value, icon: Icon }) => <div key={label} className="flex items-center gap-3 rounded-xl border border-white/10 p-4"><Icon className="size-4 text-cyan-300" /><span className="flex-1 text-sm text-slate-300">{label}</span><span className="text-xs text-amber-200">{value}</span></div>)}</div><div className="mt-6 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Create unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Assign unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Comment unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Complete unavailable</Button></div></CardContent></Card><ScreenStatePanel type="unavailable" title="Task service unavailable" description="Connect authenticated task storage, tenant authorization, validation, collaboration, notifications, audit history, conflict handling, and completion evidence before enabling task mutations." /></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><Info className="size-5 text-cyan-300" /><div><p className="font-bold">Task-management evidence contract</p><p className="mt-1 text-sm text-slate-400">Real task management requires immutable identity, owner and assignee controls, validated transitions, collaboration moderation, artifact provenance, notification consent, audit history, and accountable completion review.</p></div></div><div className="mt-5 grid gap-3 md:grid-cols-2">{["Authenticated account, tenant, owner, assignee, and permission scope", "Typed task schema, status transitions, priority, due dates, and dependencies", "Comments, artifacts, links, redaction, moderation, and provenance", "Notifications, reminders, time zones, consent, delivery, and retry", "Concurrency, idempotency, conflict handling, deletion, and audit", "Completion criteria, acceptance evidence, reopen, appeal, and recovery"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3 text-sm text-slate-300"><LockKeyhole className="size-4 text-amber-300" />{item}<span className="ml-auto text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card>{showBoundary && <Card className="border-amber-300/20 bg-amber-300/[0.06]"><CardContent className="p-6"><p className="font-semibold text-amber-100">No ownership or completion claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local task concept does not prove an owner, assignee, due date, progress, comment, artifact, notification, approval, execution, or completion.</p></CardContent></Card>}<ScreenFeatureGrid features={[{ title: "Task-list surface preserved", description: "Task concepts, filters, ownership, status, priority, due dates, dependencies, collaboration, notifications, retry, save/reset, and mutation gates remain visible.", icon: ListChecks, status: "Local planning" }, { title: "No completion theater", description: "Tasks, owners, progress, reminders, comments, artifacts, approvals, execution, and completed outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before mutation", description: "Real task management requires identity, authorization, validation, collaboration, notifications, conflict handling, audit, and acceptance evidence.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
