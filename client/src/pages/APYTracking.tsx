import { useMemo, useState } from "react";
import { AlertTriangle, BarChart3, CalendarDays, CheckCircle2, Database, Gauge, LockKeyhole, Search, Shield, Sparkles, TrendingUp, Wallet } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

type Pool = { id: string; name: string; network: string; category: string; status: "Data required" | "Risk review" | "Contract required"; description: string };
const POOLS: Pool[] = [
  { id: "liquidity", name: "Liquidity strategy", network: "Network unverified", category: "Liquidity", status: "Data required", description: "Review source, lockup, impermanent-loss, withdrawal, and counterparty assumptions." },
  { id: "staking", name: "Staking strategy", network: "Network unverified", category: "Staking", status: "Risk review", description: "Document validator, slashing, unbonding, custody, and reward-source requirements." },
  { id: "lending", name: "Lending strategy", network: "Network unverified", category: "Lending", status: "Risk review", description: "Review collateral, liquidation, oracle, borrower, and protocol risk before any analysis." },
  { id: "vault", name: "Managed vault", network: "Provider unverified", category: "Vault", status: "Contract required", description: "Require a verified provider, fee schedule, custody model, and historical methodology." },
];
const PERIODS = ["7 days", "30 days", "90 days", "Since launch"];

export default function APYTracking() { const [query, setQuery] = useState(""); const [period, setPeriod] = useState(PERIODS[1]); const [selected, setSelected] = useState(POOLS[0]); const [prepared, setPrepared] = useState(false); const visible = useMemo(() => POOLS.filter((pool) => `${pool.name} ${pool.network} ${pool.category} ${pool.description}`.toLowerCase().includes(query.toLowerCase())), [query]); return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={TrendingUp} eyebrow="Crypto Hub · Yield Research" title="Track yield claims with the risk attached." description="Explore strategy templates, time periods, source requirements, and risk review. This page does not display APY, rewards, wallet balances, returns, earnings, or yield predictions." badge="Preview yield workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setPrepared(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Sparkles className="mr-2 size-4" />Prepare review</Button><Button variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><LockKeyhole className="mr-2 size-4" />No wallet action</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Strategies", value: String(POOLS.length), hint: "Research templates", icon: TrendingUp }, { label: "APY feed", value: "Unavailable", hint: "No live yield source", icon: Database, tone: "amber" }, { label: "Wallet balance", value: "Hidden", hint: "No custody connection", icon: Wallet, tone: "violet" }, { label: "Risk review", value: "Required", hint: "Before any financial action", icon: Shield, tone: "slate" }]} /><ScreenPreviewBanner title="Yield-data safety boundary">Strategy cards, search, period filters, selected detail, source and risk requirements, and review preparation are available for UX review. No APY, APR, reward amount, return, earnings, balance, liquidity, protocol safety, or investment recommendation is fabricated.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.8fr_1.2fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="relative"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search yield strategies" aria-label="Search yield strategies" className="border-white/10 bg-black/20 pl-9 text-white placeholder:text-slate-500" /></div><div className="mt-4 flex gap-2 overflow-x-auto pb-1">{PERIODS.map((item) => <Button key={item} size="sm" variant={period === item ? "default" : "outline"} onClick={() => { setPeriod(item); setPrepared(false); }} className={period === item ? "bg-cyan-300 text-slate-950 hover:bg-cyan-200" : "border-white/15 bg-white/5 text-slate-300 hover:bg-white/10"}><CalendarDays className="mr-1 size-3.5" />{item}</Button>)}</div><div className="mt-4 space-y-2">{visible.map((pool) => <button key={pool.id} onClick={() => { setSelected(pool); setPrepared(false); }} className={`w-full rounded-xl border p-4 text-left transition ${selected.id === pool.id ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-black/15 hover:bg-white/[0.05]"}`}><div className="flex items-center gap-2"><TrendingUp className="size-4 text-cyan-300" /><span className="font-semibold">{pool.name}</span><Badge variant="outline" className="ml-auto border-amber-300/20 text-amber-200">{pool.status}</Badge></div><p className="mt-2 text-xs uppercase tracking-wider text-slate-500">{pool.category} · {pool.network}</p><p className="mt-2 text-sm leading-6 text-slate-400">{pool.description}</p></button>)}{visible.length === 0 && <ScreenStatePanel type="empty" title="No strategies match" description="Try another search term." />}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Selected strategy</p><h2 className="mt-1 text-2xl font-bold">{selected.name}</h2><p className="mt-2 text-sm text-slate-400">{selected.category} · {selected.network} · {period}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{selected.status}</Badge></div><p className="mt-5 text-sm leading-6 text-slate-300">{selected.description}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{["Source and timestamp", "Reward methodology", "Lockup and withdrawal", "Counterparty and custody", "Loss and liquidation scenarios", "Fees and net calculation"].map((item) => <div key={item} className="rounded-xl border border-white/10 bg-black/15 p-4"><CheckCircle2 className="size-4 text-emerald-300" /><p className="mt-3 text-sm text-slate-300">{item}</p><p className="mt-1 text-xs text-slate-500">Evidence required</p></div>)}</div>{prepared && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex items-center gap-2 font-medium text-amber-200"><Database className="size-4" />Review prepared for {period}</div><p className="mt-2 text-sm leading-6 text-slate-300">No yield number or financial recommendation is produced. Connect a verified source and complete risk review before displaying results.</p></div>}</CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Source before APY", description: "Show source, timestamp, methodology, fees, and stale-data state before any yield number.", icon: Database, status: "Required" }, { title: "Risk is not decoration", description: "Surface lockup, custody, slashing, liquidation, counterparty, and loss scenarios.", icon: Shield, status: "Required" }, { title: "No fabricated earnings", description: "Never invent reward amounts, balances, returns, or protocol safety to fill a dashboard.", icon: AlertTriangle, status: "Guardrail" }]} /></main></div>; }
