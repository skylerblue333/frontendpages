import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { BriefcaseBusiness, Check, Filter, FileText, LockKeyhole, RefreshCw, Search, ShieldAlert, Star, Users, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const projects = [{ id: 1, name: "HopeAI research lab", category: "AI", summary: "A local project concept for evidence-backed AI experiments.", status: "Concept", owner: "Unknown", budget: "Unpublished" }, { id: 2, name: "SkySchool curriculum", category: "Education", summary: "A local project concept for structured learning pathways.", status: "Draft", owner: "Unknown", budget: "Unpublished" }, { id: 3, name: "Community marketplace", category: "Community", summary: "A local project concept for creator and ecosystem discovery.", status: "Review", owner: "Unknown", budget: "Unpublished" }];
export default function ProjectListing() {
  const [query, setQuery] = useState("");
  const [category, setCategory] = useState("All");
  const [selected, setSelected] = useState(projects[0].id);
  const [interest, setInterest] = useState(false);
  const [saved, setSaved] = useState(false);
  const [showBoundary, setShowBoundary] = useState(false);
  const categories = ["All", "AI", "Education", "Community"];
  const filtered = useMemo(() => projects.filter((project) => `${project.name} ${project.summary}`.toLowerCase().includes(query.toLowerCase()) && (category === "All" || project.category === category)), [category, query]);
  const selectedProject = projects.find((project) => project.id === selected) ?? projects[0];
  const reset = () => { setQuery(""); setCategory("All"); setSelected(projects[0].id); setInterest(false); setSaved(false); setShowBoundary(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={BriefcaseBusiness} eyebrow="ProjectListing · Discovery preview" title="Discover projects without inventing their traction." description="Explore local project concepts with search, category filters, status, owner, budget, evidence, interest, and application intent. No projects, users, funding, dates, ratings, applications, or outcomes are verified." badge="Project catalog"><div className="flex flex-wrap gap-2"><Button onClick={() => setInterest((value) => !value)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200">{interest ? "Interest saved locally" : "Save interest locally"}</Button><Button onClick={() => setSaved(true)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><Check className="mr-2 size-4" />{saved ? "View saved locally" : "Save view locally"}</Button><Button onClick={() => setShowBoundary((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showBoundary ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showBoundary ? "Close boundary" : "Review boundary"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset catalog</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Projects", value: `${projects.length} concepts`, hint: "Not verified", icon: BriefcaseBusiness, tone: "cyan" }, { label: "Owners", value: "Unknown", hint: "No directory source", icon: Users, tone: "violet" }, { label: "Budget", value: "Unpublished", hint: "No finance source", icon: FileText, tone: "amber" }, { label: "Ratings", value: "Unmeasured", hint: "No trust signal", icon: Star, tone: "slate" }]} /><ScreenPreviewBanner title="Project listing evidence boundary"><strong>This is a local project-discovery catalog, not a verified marketplace.</strong> Project cards, categories, status, owner, budget, interest, and selected detail are browser concepts. No users, funding, dates, ratings, reviews, applications, eligibility, team, delivery, or outcome is measured or persisted.</ScreenPreviewBanner><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex flex-wrap items-center gap-3"><div className="relative min-w-64 flex-1"><Search className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search project concepts" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><Filter className="size-4 text-slate-500" />{categories.map((item) => <button key={item} onClick={() => setCategory(item)} className={`rounded-lg border px-3 py-2 text-xs ${category === item ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div><div className="mt-6 grid gap-4 md:grid-cols-3">{filtered.map((project) => <button key={project.id} onClick={() => setSelected(project.id)} className={`rounded-2xl border p-5 text-left ${selected === project.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><h2 className="font-black">{project.name}</h2><Badge variant="outline" className="border-amber-300/20 text-amber-200">{project.status}</Badge></div><p className="mt-3 text-sm leading-6 text-slate-400">{project.summary}</p><div className="mt-5 flex flex-wrap gap-2"><Badge variant="outline" className="border-white/10 text-slate-500">{project.category}</Badge><Badge variant="outline" className="border-white/10 text-slate-500">Owner {project.owner}</Badge></div></button>)}{filtered.length === 0 && <div className="col-span-full rounded-2xl border border-dashed border-white/10 p-12 text-center text-slate-500">No local project concepts match this search.</div>}</div></CardContent></Card><section className="grid gap-6 lg:grid-cols-[0.9fr_1.1fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected project</p><h2 className="mt-2 text-2xl font-black">{selectedProject.name}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{selectedProject.summary}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Category", value: selectedProject.category }, { label: "Status", value: selectedProject.status }, { label: "Owner", value: selectedProject.owner }, { label: "Budget", value: selectedProject.budget }, { label: "Rating", value: "Unmeasured" }, { label: "Applications", value: "Unavailable" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 flex flex-wrap gap-2"><Button onClick={() => setInterest((value) => !value)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200">{interest ? "Interest saved locally" : "I’m interested locally"}</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Apply unavailable</Button></div>{showBoundary && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No project-trust claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local card does not prove an owner, team, budget, funding, delivery status, rating, eligibility, or opportunity to apply.</p></div>}</CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Project gates</p><h2 className="mt-2 text-2xl font-black">What a real project listing must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Authenticated owner, team, role, and project authorization", "Source-backed scope, status, milestones, dates, and dependencies", "Budget, funding, payment, tax, and financial disclosure", "Moderated description, links, media, ratings, reviews, and reports", "Application eligibility, routing, notification, and privacy behavior", "Audit, versioning, rollback, support, and deletion"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Project discovery preserved", description: "Search, categories, cards, selected detail, status, owner, budget, ratings, interest, application state, and gates remain interactive.", icon: BriefcaseBusiness, status: "Local catalog" }, { title: "No opportunity theater", description: "Projects, users, owners, funding, dates, ratings, reviews, applications, eligibility, and outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before trust", description: "Real listings need authenticated ownership, financial disclosure, moderation, privacy, application routing, and audit.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
