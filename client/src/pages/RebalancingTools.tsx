import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ArrowLeftRight, Check, Filter, LockKeyhole, RefreshCw, Search, ShieldAlert, SlidersHorizontal, Target, Wallet, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const plans = [{ id: 1, name: "Core allocation", category: "Portfolio", status: "Draft", detail: "A local target mix awaiting verified holdings, prices, risk policy, and approval." }, { id: 2, name: "Treasury bands", category: "Operations", status: "Unverified", detail: "A treasury allocation concept that must not imply custody, balances, solvency, or liquidity." }, { id: 3, name: "Education sandbox", category: "Learning", status: "Simulation only", detail: "A teaching scenario for drift and constraints without investment advice or execution." }, { id: 4, name: "Digital assets", category: "Crypto", status: "Blocked", detail: "A high-risk allocation concept requiring network, custody, pricing, and transaction controls." }];
export default function RebalancingTools() {
  const [query, setQuery] = useState(""); const [category, setCategory] = useState("All"); const [selected, setSelected] = useState(1); const [target, setTarget] = useState("Targets not configured"); const [risk, setRisk] = useState("Constraints not configured"); const [saved, setSaved] = useState(false); const [showGates, setShowGates] = useState(false);
  const categories = ["All", ...Array.from(new Set(plans.map((item) => item.category)))]; const filtered = useMemo(() => plans.filter((item) => (category === "All" || item.category === category) && `${item.name} ${item.category} ${item.detail}`.toLowerCase().includes(query.toLowerCase())), [category, query]); const plan = plans.find((item) => item.id === selected) ?? plans[0];
  const reset = () => { setQuery(""); setCategory("All"); setSelected(1); setTarget("Targets not configured"); setRisk("Constraints not configured"); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={ArrowLeftRight} eyebrow="RebalancingTools · Allocation preview" title="Model the drift before moving an asset." description="Explore local allocation plans with target bands, drift, constraints, scenarios, approvals, simulation, reports, and evidence gates. No holdings, prices, balances, allocations, orders, transactions, or financial recommendation is connected." badge="Allocation workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "View saved locally" : "Save design locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review allocation gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset design</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Plans", value: `${plans.length} local`, hint: "No portfolio loaded", icon: ArrowLeftRight, tone: "cyan" }, { label: "Holdings", value: "Unavailable", hint: "No custody source", icon: Wallet, tone: "violet" }, { label: "Drift", value: "Unmeasured", hint: "No prices or weights", icon: SlidersHorizontal, tone: "amber" }, { label: "Orders", value: "Blocked", hint: "No execution path", icon: Target, tone: "slate" }]} /><ScreenPreviewBanner title="Allocation evidence boundary"><strong>This is a local allocation and education preview, not an investment or execution service.</strong> Plans, targets, bands, constraints, scenarios, saved state, simulation posture, approvals, and evidence gates are browser concepts. No holding, price, balance, weight, return, risk, order, transaction, or recommendation is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Search className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local allocation plans" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-4 flex flex-wrap gap-2">{categories.map((entry) => <Button key={entry} onClick={() => setCategory(entry)} size="sm" variant="outline" className={category === entry ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-100" : "border-white/10 text-slate-400"}><Filter className="mr-1 size-3" />{entry}</Button>)}</div><div className="mt-6 space-y-3">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{item.name}</p><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.status}</Badge></div><div className="mt-4"><Badge variant="outline" className="border-white/10 text-slate-500">{item.category}</Badge></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected allocation plan</p><h2 className="mt-2 text-2xl font-black">{plan.name}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{plan.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Status", value: plan.status }, { label: "Targets", value: target }, { label: "Holdings", value: "Unavailable" }, { label: "Prices", value: "Unavailable" }, { label: "Drift", value: "Unmeasured" }, { label: "Approval", value: "Not configured" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2"><label className="text-sm text-slate-400">Target bands<select value={target} onChange={(event) => setTarget(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Targets not configured</option><option>Equal-weight intent</option><option>Strategic bands intent</option><option>Custom weights intent</option></select></label><label className="text-sm text-slate-400">Risk constraints<select value={risk} onChange={(event) => setRisk(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Constraints not configured</option><option>Concentration cap intent</option><option>Volatility band intent</option><option>Liquidity floor intent</option></select></label></div><div className="mt-5 grid gap-3 sm:grid-cols-2"><div className="rounded-xl border border-white/10 p-4"><p className="font-semibold">Simulation</p><p className="mt-2 text-sm text-slate-500">No portfolio loaded</p><Button disabled className="mt-4 w-full bg-slate-700 text-slate-400">Run unavailable</Button></div><div className="rounded-xl border border-white/10 p-4"><p className="font-semibold">Execution</p><p className="mt-2 text-sm text-slate-500">Orders blocked</p><Button disabled className="mt-4 w-full bg-slate-700 text-slate-400">Submit unavailable</Button></div></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No financial or transaction claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A target mix does not calculate a personalized recommendation, establish suitability, or authorize an order.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Allocation gates</p><h2 className="mt-2 text-2xl font-black">What a real rebalance must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Authenticated holdings, prices, quantities, cost basis, currency, custody, and valuation timestamp", "Target policy, drift formula, bands, rounding, minimums, tax, fees, slippage, and liquidity", "Risk profile, suitability, jurisdiction, disclosures, concentration, volatility, and loss scenarios", "Approval, permissions, idempotency, order validation, signing, confirmations, and transaction status", "Simulation, backtesting, stress tests, benchmarks, data provenance, and reproducible reports", "Failed orders, partial fills, rollback, reconciliation, audit, support, incident response, and recovery"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Allocation surface preserved", description: "Plans, search, categories, targets, bands, constraints, scenarios, approvals, simulation, reports, execution gates, save/reset, and evidence remain interactive.", icon: ArrowLeftRight, status: "Local plans" }, { title: "No financial theater", description: "Holdings, prices, balances, weights, returns, risk, orders, transactions, and recommendations are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before execution", description: "Real rebalancing needs authenticated data, suitability, approval, signing, transaction status, reconciliation, and recovery.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
