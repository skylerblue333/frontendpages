import FeatureUnavailable from "@/components/FeatureUnavailable";

const RebalancingTools = () => (
  <FeatureUnavailable
    title="Rebalancing Tools"
    description="Rebalancing Tools is intentionally held at a truthful release boundary until its real provider, authorization, persistence, monitoring, and acceptance evidence are complete."
    capability="Rebalancing Tools on /rebalancing-tools"
    nextStep="Return to the launch hub"
  />
);

export default RebalancingTools;
