import { useMemo, useState } from "react";
import { Check, Eye, Grid2X2, Heart, Info, Layers3, RefreshCw, Search, ShieldAlert, Sparkles, Tag, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

type Item = { id: string; name: string; category: string; status: string; description: string; accent: string };
const ITEMS: Item[] = [{ id: "item-01", name: "Aurora interface", category: "Design", status: "Preview", description: "A synthetic visual system card for comparing hierarchy and contrast.", accent: "from-cyan-300/30 to-blue-500/10" }, { id: "item-02", name: "Orbit workspace", category: "Product", status: "Unverified", description: "A synthetic workspace concept with no production availability claim.", accent: "from-violet-300/30 to-fuchsia-500/10" }, { id: "item-03", name: "Signal dashboard", category: "Data", status: "Preview", description: "A synthetic analytics composition with no live metrics attached.", accent: "from-amber-300/30 to-orange-500/10" }, { id: "item-04", name: "Atlas collection", category: "Community", status: "Unverified", description: "A synthetic community artifact with no ownership or endorsement claim.", accent: "from-emerald-300/30 to-teal-500/10" }];
const CATEGORIES = ["All", "Design", "Product", "Data", "Community"];
export default function CardGridView() { const [query, setQuery] = useState(""); const [category, setCategory] = useState("All"); const [selectedId, setSelectedId] = useState(ITEMS[0].id); const [compare, setCompare] = useState<string[]>([]); const [saved, setSaved] = useState(false); const visible = useMemo(() => ITEMS.filter((item) => `${item.name} ${item.category} ${item.description}`.toLowerCase().includes(query.toLowerCase()) && (category === "All" || item.category === category)), [query, category]); const selected = visible.find((item) => item.id === selectedId) ?? visible[0]; const toggleCompare = (id: string) => setCompare((current) => current.includes(id) ? current.filter((item) => item !== id) : current.length < 3 ? [...current, id] : current); return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Grid2X2} eyebrow="Collections · Visual Grid" title="Browse the collection before you call it a recommendation." description="Explore synthetic visual cards, category filters, search, selected detail, and a local compare set. This page does not assert ownership, availability, ranking, endorsement, inventory, or recommendation quality." badge="Preview card-grid workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Heart className="mr-2 size-4" />Save local view</Button><Button onClick={() => { setQuery(""); setCategory("All"); setCompare([]); setSaved(false); }} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset grid</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Visible cards", value: String(visible.length), hint: "Synthetic collection", icon: Layers3 }, { label: "Compare set", value: String(compare.length), hint: "Local selection", icon: Check, tone: "violet" }, { label: "Ranking", value: "Unavailable", hint: "No recommendation model", icon: Sparkles, tone: "amber" }, { label: "Ownership", value: "Unverified", hint: "No inventory source", icon: ShieldAlert, tone: "slate" }]} /><ScreenPreviewBanner title="Collection evidence boundary">A card grid is a presentation pattern, not proof of inventory, ownership, popularity, endorsement, freshness, availability, or recommendation quality. Real collection data needs provenance, moderation, permissions, timestamps, accessibility metadata, and a clear source of truth.</ScreenPreviewBanner><section className="flex flex-col gap-4 sm:flex-row"><div className="relative flex-1"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search synthetic cards" className="border-white/10 bg-black/20 pl-9 text-white placeholder:text-slate-500" /></div><div className="flex flex-wrap gap-2">{CATEGORIES.map((item) => <button key={item} onClick={() => setCategory(item)} className={`rounded-xl border px-3 py-2 text-sm ${category === item ? "border-cyan-300/40 bg-cyan-300/[0.1] text-cyan-200" : "border-white/10 text-slate-400"}`}>{item}</button>)}</div></section><section className="grid gap-6 lg:grid-cols-[1.2fr_0.8fr]"><div className="grid gap-4 sm:grid-cols-2">{visible.map((item) => <Card key={item.id} className={`overflow-hidden border-white/10 bg-white/[0.04] transition ${selected?.id === item.id ? "ring-1 ring-cyan-300/40" : ""}`}><div className={`h-28 bg-gradient-to-br ${item.accent}`}><div className="flex items-start justify-between p-4"><Badge variant="outline" className="border-white/15 bg-black/20 text-white">{item.category}</Badge><span className="rounded-full border border-white/15 bg-black/20 p-2"><Eye className="size-4 text-white" /></span></div></div><CardContent className="p-5"><div className="flex items-start justify-between gap-3"><div><h2 className="font-bold">{item.name}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{item.description}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.status}</Badge></div><div className="mt-5 flex gap-2"><Button onClick={() => setSelectedId(item.id)} variant="outline" className="flex-1 border-white/15 text-white">Inspect</Button><Button onClick={() => toggleCompare(item.id)} variant="outline" className={`border-white/15 ${compare.includes(item.id) ? "text-cyan-200" : "text-slate-400"}`}>{compare.includes(item.id) ? <Check className="size-4" /> : <Layers3 className="size-4" />}</Button></div></CardContent></Card>)}{visible.length === 0 && <p className="col-span-full py-12 text-center text-sm text-slate-500">No synthetic cards match this view.</p>}</div><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6">{selected ? <><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Selected card</p><h2 className="mt-2 text-2xl font-black">{selected.name}</h2><p className="mt-2 text-sm text-slate-400">{selected.category} · {selected.status}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Synthetic</Badge></div><div className={`mt-6 h-36 rounded-2xl bg-gradient-to-br ${selected.accent}`} /><p className="mt-5 text-sm leading-6 text-slate-300">{selected.description}</p><div className="mt-5 space-y-2">{["Source provenance", "Ownership or license", "Freshness timestamp", "Accessibility metadata", "Moderation state"].map((item) => <div key={item} className="flex items-center gap-3 rounded-lg border border-white/10 bg-black/15 p-3"><Info className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Unverified</span></div>)}</div>{saved && <div className="mt-5 rounded-xl border border-cyan-300/20 bg-cyan-300/[0.06] p-4"><p className="font-semibold text-cyan-200">Local grid view saved</p><p className="mt-1 text-sm text-slate-400">No card ownership, ranking, inventory, or server record changed.</p></div>}</> : <p className="text-sm text-slate-500">Select a card to inspect it.</p>}</CardContent></Card></section>{compare.length > 0 && <Card className="border-violet-300/20 bg-violet-300/[0.05]"><CardContent className="p-5"><div className="flex items-center gap-3"><Layers3 className="size-5 text-violet-300" /><div><p className="font-semibold text-violet-200">Local compare set</p><p className="mt-1 text-sm text-slate-400">{compare.map((id) => ITEMS.find((item) => item.id === id)?.name).join(" · ")} · comparison only, not a recommendation.</p></div></div></CardContent></Card>}<section className="grid gap-4 md:grid-cols-3"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><Tag className="size-5 text-cyan-300" /><h3 className="mt-3 font-semibold">Grid is a lens</h3><p className="mt-2 text-sm leading-6 text-slate-400">Filters and cards organize a collection; they do not prove completeness or popularity.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><Sparkles className="size-5 text-violet-300" /><h3 className="mt-3 font-semibold">Compare, don’t imply</h3><p className="mt-2 text-sm leading-6 text-slate-400">A local compare set helps inspection without pretending an algorithm ranked the items.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><ShieldAlert className="size-5 text-amber-300" /><h3 className="mt-3 font-semibold">Metadata needs evidence</h3><p className="mt-2 text-sm leading-6 text-slate-400">Ownership, license, freshness, moderation, and provenance need real source records.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Visual scan", description: "Use card hierarchy, category, and selected detail to make browsing useful without invented ranking.", icon: Grid2X2, status: "Pattern" }, { title: "Compare locally", description: "Select a small set for side-by-side reasoning instead of claiming recommendation quality.", icon: Layers3, status: "Ready" }, { title: "No fake catalog", description: "Synthetic cards are not owned assets, inventory, endorsements, or marketplace listings.", icon: ShieldAlert, status: "Guardrail" }]} /></main></div>; }
