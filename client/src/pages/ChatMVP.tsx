import { useMemo, useState } from "react";
import { Bot, Briefcase, Check, DollarSign, FileText, Heart, MessageCircle, Plus, RefreshCw, Search, Send, ShieldAlert, ShoppingBag, Sparkles, UserRound, WifiOff, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

type Contact = { id: number; name: string; avatar: string; topic: string; kind: "AI" | "Person" | "Group"; state: string };
type Turn = { from: "assistant" | "local" | "system"; text: string };
const CONTACTS: Contact[] = [{ id: 1, name: "NOVA preview", avatar: "✦", topic: "Action-planning sandbox", kind: "AI", state: "Synthetic" }, { id: 2, name: "Synthetic collaborator", avatar: "A", topic: "Project conversation", kind: "Person", state: "Preview" }, { id: 3, name: "Creator Hub preview", avatar: "C", topic: "Group workflow", kind: "Group", state: "Preview" }];
const ACTIONS = [{ label: "Tip plan", icon: DollarSign }, { label: "Service request", icon: Briefcase }, { label: "Listing draft", icon: ShoppingBag }, { label: "AI task", icon: Bot }];
export default function ChatMVP() { const [selectedId, setSelectedId] = useState(1); const [query, setQuery] = useState(""); const [message, setMessage] = useState(""); const [intent, setIntent] = useState(""); const [turns, setTurns] = useState<Turn[]>([{ from: "assistant", text: "Synthetic response preview: I can help structure an action plan, but this workspace cannot send payments, contact people, create listings, or call an AI model." }]); const [saved, setSaved] = useState(false); const selected = CONTACTS.find((contact) => contact.id === selectedId) ?? CONTACTS[0]; const filtered = useMemo(() => CONTACTS.filter((contact) => `${contact.name} ${contact.topic}`.toLowerCase().includes(query.toLowerCase())), [query]); const addTurn = () => { const text = message.trim() || intent || "Local action-plan draft"; setTurns((items) => [...items, { from: "local", text }, { from: "system", text: "Preview only: no message, tip, listing, service request, match, or AI task was executed." }]); setMessage(""); setSaved(true); }; const reset = () => { setTurns([]); setMessage(""); setIntent(""); setSaved(false); }; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={MessageCircle} eyebrow="Social · Action Chat Sandbox" title="Let the conversation clarify the action before anything executes." description="Explore synthetic contacts, local message drafts, and action-intent review. This page does not send messages, transfer funds, call a model, create listings, match people, or invoke tools." badge="Preview chat MVP"><div className="flex flex-wrap gap-2"><Button onClick={addTurn} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Send className="mr-2 size-4" />Add local turn</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset chat</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Contact", value: selected.kind, hint: selected.state, icon: UserRound }, { label: "Turns", value: String(turns.length), hint: "Local preview", icon: MessageCircle, tone: "violet" }, { label: "Actions", value: "Blocked", hint: "No execution", icon: ShieldAlert, tone: "amber" }, { label: "Wallet", value: "Unavailable", hint: "No payment rail", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="Chat-action evidence boundary">A chat bubble is not proof of identity, delivery, model inference, availability, pricing, payment, matching, listing creation, or service fulfillment. Real action chat needs authenticated participants, consent, intent confirmation, authorization, idempotency, payment and rollback policy, tool audit logs, moderation, and a clear result state.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.8fr_1.2fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-4"><div className="relative"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search synthetic contacts" className="border-white/10 bg-black/20 pl-9 text-white placeholder:text-slate-500" /></div><div className="mt-5 space-y-2">{filtered.map((contact) => <button key={contact.id} onClick={() => { setSelectedId(contact.id); setSaved(false); }} className={`w-full rounded-xl border p-4 text-left ${selected.id === contact.id ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-black/15"}`}><div className="flex items-center gap-3"><div className="flex size-10 items-center justify-center rounded-full bg-cyan-300/10 font-bold text-cyan-200">{contact.avatar}</div><div className="flex-1"><p className="font-semibold">{contact.name}</p><p className="mt-1 text-xs text-slate-500">{contact.topic}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{contact.kind}</Badge></div></button>)}{filtered.length === 0 && <p className="py-8 text-center text-sm text-slate-500">No synthetic contacts match this search.</p>}<Button onClick={() => setSaved(true)} variant="outline" className="mt-3 w-full border-white/15 text-white"><Plus className="mr-2 size-4" />New local conversation</Button></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">{selected.name}</p><h2 className="mt-2 text-2xl font-black">{selected.topic}</h2></div><Badge variant="outline" className="border-cyan-300/20 text-cyan-200">{selected.state}</Badge></div><div className="mt-5 max-h-64 space-y-3 overflow-auto rounded-xl border border-white/10 bg-black/15 p-4">{turns.length === 0 && <p className="py-8 text-center text-sm text-slate-500">No local turns yet.</p>}{turns.map((turn, index) => <div key={`${turn.from}-${index}`} className={`rounded-xl p-3 ${turn.from === "assistant" ? "bg-cyan-300/[0.08]" : turn.from === "system" ? "bg-amber-300/[0.08]" : "bg-white/[0.05]"}`}><p className="text-[10px] uppercase tracking-[0.18em] text-slate-500">{turn.from === "assistant" ? "Synthetic assistant" : turn.from === "system" ? "Safety boundary" : "Local draft"}</p><p className="mt-2 text-sm leading-6 text-slate-300">{turn.text}</p></div>)}</div><div className="mt-4 flex gap-2"><Input value={message} onChange={(event) => { setMessage(event.target.value); setSaved(false); }} placeholder="Write a local message draft" className="border-white/10 bg-black/20 text-white placeholder:text-slate-500" /><Button onClick={addTurn} variant="outline" className="border-white/15 text-white">Add</Button></div><div className="mt-5 grid grid-cols-2 gap-2">{ACTIONS.map(({ label, icon: Icon }) => <button key={label} onClick={() => { setIntent(label); setSaved(false); }} className={`flex items-center gap-2 rounded-lg border p-3 text-left text-sm ${intent === label ? "border-violet-300/40 bg-violet-300/[0.08] text-violet-200" : "border-white/10 text-slate-400"}`}><Icon className="size-4" />{label}</button>)}</div>{intent && <div className="mt-4 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex items-center gap-2 font-semibold text-amber-200"><ShieldAlert className="size-4" />Action intent selected: {intent}</div><p className="mt-1 text-sm text-slate-400">Review target, amount, authorization, consent, and rollback before a real execution path exists.</p></div>}{saved && <div className="mt-4 flex items-center gap-2 text-sm text-cyan-200"><Check className="size-4" />Local state saved; nothing was sent or executed.</div>}</CardContent></Card></section><section className="grid gap-4 md:grid-cols-3"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><Bot className="size-5 text-cyan-300" /><h3 className="mt-3 font-semibold">Assistant identity is scoped</h3><p className="mt-2 text-sm leading-6 text-slate-400">A name, avatar, or online dot does not prove a model, source, availability, or accuracy.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><ShieldAlert className="size-5 text-violet-300" /><h3 className="mt-3 font-semibold">Actions need confirmation</h3><p className="mt-2 text-sm leading-6 text-slate-400">Tips, listings, service requests, matches, and AI tasks require explicit intent and audit.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><WifiOff className="size-5 text-amber-300" /><h3 className="mt-3 font-semibold">No fake value flow</h3><p className="mt-2 text-sm leading-6 text-slate-400">Local chat is not delivery, payment, earnings, marketplace inventory, or fulfillment.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Conversation before execution", description: "Separate drafting, intent review, confirmation, tool call, and result states.", icon: MessageCircle, status: "Pattern" }, { title: "Action boundaries are visible", description: "Money, people, listings, and AI tools need authorization, idempotency, and rollback.", icon: ShieldAlert, status: "Required" }, { title: "No fake ShadowChat", description: "Synthetic contacts and local turns are not messages, payments, listings, matches, or model calls.", icon: WifiOff, status: "Guardrail" }]} /></main></div>; }
