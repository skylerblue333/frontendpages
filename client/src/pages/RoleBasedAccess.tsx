import { useMemo, useState } from "react";
import { Check, Filter, KeyRound, LockKeyhole, RefreshCw, ShieldAlert, UsersRound, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const policies = [{ id: 1, name: "Member access policy", category: "Core", detail: "A local role concept requiring authenticated identity, tenant scope, least privilege, session policy, and review.", state: "Unconfigured" }, { id: 2, name: "Admin access policy", category: "Privileged", detail: "A privileged-access concept requiring separation of duties, MFA, approval, logging, break-glass, and recovery evidence.", state: "Needs review" }, { id: 3, name: "Wallet access policy", category: "Crypto", detail: "A high-risk wallet policy concept requiring custody boundary, address controls, signing authorization, transaction limits, and audit.", state: "Blocked" }, { id: 4, name: "Data access policy", category: "Privacy", detail: "A data-access concept requiring classification, purpose, consent, redaction, retention, deletion, and lawful-access handling.", state: "Unmeasured" }];
export default function RoleBasedAccess() {
  const [query, setQuery] = useState(""); const [category, setCategory] = useState("All"); const [selected, setSelected] = useState(1); const [scope, setScope] = useState("Scope not configured"); const [approval, setApproval] = useState("Approval not configured"); const [saved, setSaved] = useState(false); const [showGates, setShowGates] = useState(false); const categories = ["All", ...Array.from(new Set(policies.map((item) => item.category)))]; const filtered = useMemo(() => policies.filter((item) => (category === "All" || item.category === category) && `${item.name} ${item.category} ${item.detail}`.toLowerCase().includes(query.toLowerCase())), [category, query]); const policy = policies.find((item) => item.id === selected) ?? policies[0]; const reset = () => { setQuery(""); setCategory("All"); setSelected(1); setScope("Scope not configured"); setApproval("Approval not configured"); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={KeyRound} eyebrow="Role-based access · Security preview" title="Define the permission boundary before granting access." description="Explore local core, privileged, crypto, and privacy access-policy concepts with search, category filters, scope and approval intent, save/reset, review gates, and blocked enforcement actions. No live roles, users, permissions, sessions, wallet custody, data access, or security certification is connected." badge="Authorization workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save policy locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review access gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset policy</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Policies", value: `${policies.length} local`, hint: "No auth source", icon: KeyRound, tone: "cyan" }, { label: "Roles", value: "Unavailable", hint: "No directory source", icon: UsersRound, tone: "violet" }, { label: "Permissions", value: "Unconfigured", hint: "No policy source", icon: LockKeyhole, tone: "amber" }, { label: "Enforcement", value: "Blocked", hint: "No authorization", icon: ShieldAlert, tone: "slate" }]} /><ScreenPreviewBanner title="Authorization evidence boundary"><strong>This is a local authorization-policy preview, not proof that access control, role membership, permissions, sessions, custody, or security certification exists.</strong> Policy cards, filters, scope and approval intent, saved state, and disabled enforcement actions are browser concepts. No user, role, permission, session, wallet, data access, security status, or compliance outcome is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Filter className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local access policies" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-4 flex flex-wrap gap-2">{categories.map((entry) => <Button key={entry} onClick={() => setCategory(entry)} size="sm" variant="outline" className={category === entry ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-100" : "border-white/10 text-slate-400"}>{entry}</Button>)}</div><div className="mt-6 space-y-3">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{item.name}</p><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.state}</Badge></div><div className="mt-4"><Badge variant="outline" className="border-white/10 text-slate-500">{item.category}</Badge></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected policy concept</p><h2 className="mt-2 text-2xl font-black">{policy.name}</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{policy.state}</Badge></div><p className="mt-2 text-sm leading-6 text-slate-400">{policy.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Category", value: policy.category }, { label: "Scope", value: scope }, { label: "Approval", value: approval }, { label: "Roles", value: "Unavailable" }, { label: "Permissions", value: "Unconfigured" }, { label: "Audit", value: "Required" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2"><label className="text-sm text-slate-400">Scope intent<select value={scope} onChange={(event) => setScope(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Scope not configured</option><option>Tenant scope intent</option><option>Resource scope intent</option><option>Role scope intent</option><option>Break-glass scope intent</option></select></label><label className="text-sm text-slate-400">Approval intent<select value={approval} onChange={(event) => setApproval(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Approval not configured</option><option>Owner approval intent</option><option>Two-person approval intent</option><option>Security review intent</option><option>Periodic review intent</option></select></label></div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-8 text-center"><KeyRound className="mx-auto size-8 text-slate-600" /><p className="mt-3 font-semibold">No authorization evidence loaded</p><p className="mt-2 text-sm text-slate-500">Connect governed identity, sessions, roles, permissions, resource scopes, MFA, approvals, audit, privacy, custody, and recovery before enforcing access.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Grant unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Revoke unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Test policy unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Export audit unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No live authorization or security claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A policy concept does not prove a user role, permission, session, access decision, wallet custody boundary, data authorization, or security certification.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Access-control gates</p><h2 className="mt-2 text-2xl font-black">What a real authorization system must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Authenticated identity, session, tenant, role, resource, action, scope, timestamp, and decision provenance", "Least privilege, separation of duties, MFA, privileged access, break-glass, recovery, revocation, and periodic review", "Wallet, payment, blockchain, financial, personal, educational, AI, marketplace, and admin actions require domain authorization", "Policy enforcement, deny-by-default, server-side checks, IDOR protection, CSRF, rate limits, audit, and incident response", "Privacy, consent, classification, redaction, retention, deletion, export, lawful access, and support ownership", "Security, compliance, certification, custody, privacy, and user-impact claims require independent evidence and accountable review"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Authorization surface preserved", description: "Core, privileged, crypto, privacy policies, filters, roles, permissions, scopes, approvals, grant, revoke, testing, audit, save/reset, and gates remain interactive.", icon: KeyRound, status: "Local policies" }, { title: "No security theater", description: "Users, roles, permissions, sessions, access decisions, custody, privacy, compliance, and certifications are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Server evidence before enforcement", description: "Real access control requires authenticated server-side decisions, least privilege, audit, revocation, recovery, and domain review.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
