import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Check, ClipboardCheck, FileCheck2, LockKeyhole, RefreshCw, ShieldCheck, TriangleAlert, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

type Status = "pending" | "paid" | "failed" | "refunded";
export default function PaymentConfirmation() {
  const [status, setStatus] = useState<Status>("pending");
  const [reference, setReference] = useState("preview-payment-001");
  const [amount, setAmount] = useState("998.00");
  const [acknowledged, setAcknowledged] = useState(false);
  const validAmount = Number(amount) > 0;
  const statusCopy = useMemo(() => ({ pending: "Awaiting evidence", paid: "Fixture status only", failed: "Fixture failure only", refunded: "Fixture refund only" }[status]), [status]);
  const reset = () => { setStatus("pending"); setReference("preview-payment-001"); setAmount("998.00"); setAcknowledged(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={ClipboardCheck} eyebrow="PaymentConfirmation · Receipt preview" title="Show the receipt boundary before showing success." description="Review a local payment status fixture, amount arithmetic, and evidence requirements. No charge, capture, payer, provider, receipt, refund, or settlement is verified." badge="Payment receipt preview"><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset confirmation</Button></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Status", value: status === "pending" ? "Pending" : status === "paid" ? "Paid*" : status === "failed" ? "Failed*" : "Refunded*", hint: statusCopy, icon: ClipboardCheck, tone: "cyan" }, { label: "Amount", value: amount, hint: "Local fixture", icon: FileCheck2, tone: "violet" }, { label: "Reference", value: reference ? "Set" : "Unset", hint: "Not provider-issued", icon: ShieldCheck, tone: "amber" }, { label: "Settlement", value: "Off", hint: "No provider", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="Payment confirmation evidence boundary"><strong>This is a local receipt-state fixture, not payment success.</strong> Status, amount, currency, provider reference, payer, merchant, order, authorization, capture, refund, dispute, timestamp, receipt, and settlement are not sourced from a payment provider or database. Even the “Paid” option is explicitly marked as a fixture status.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1fr_0.85fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Receipt fixture</p><h2 className="mt-2 text-3xl font-black">Payment review</h2></div><Badge variant="outline" className="border-white/10 text-amber-200">Local only</Badge></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="text-sm text-slate-400">Reference label<input value={reference} onChange={(e) => setReference(e.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-white outline-none focus:border-cyan-300/50" /></label><label className="text-sm text-slate-400">Amount label<input value={amount} onChange={(e) => setAmount(e.target.value)} inputMode="decimal" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-white outline-none focus:border-cyan-300/50" /></label></div><p className="mt-5 text-sm text-slate-500">Choose a fixture state to test downstream UI. This does not mutate a payment record.</p><div className="mt-3 flex flex-wrap gap-2">{(["pending", "paid", "failed", "refunded"] as Status[]).map((item) => <button key={item} onClick={() => setStatus(item)} className={`rounded-full border px-3 py-1.5 text-xs capitalize ${status === item ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-200" : "border-white/10 text-slate-500"}`}>{item}{item !== "pending" ? " fixture" : ""}</button>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2">{[{ label: "Provider", value: "Not sourced" }, { label: "Currency", value: "Not verified" }, { label: "Payer", value: "Not identified" }, { label: "Receipt ID", value: "Not issued" }, { label: "Timestamp", value: "Not recorded" }, { label: "Settlement", value: "Blocked" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 flex gap-3"><button aria-label="Acknowledge payment confirmation boundary" onClick={() => setAcknowledged(!acknowledged)} className={`flex size-5 shrink-0 items-center justify-center rounded border ${acknowledged ? "border-cyan-300 bg-cyan-300 text-slate-950" : "border-white/20"}`}>{acknowledged ? "✓" : ""}</button><p className="text-sm leading-6 text-slate-400">I understand this fixture cannot confirm a real charge, receipt, refund, or settlement.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled={!validAmount || !acknowledged} className="bg-cyan-300/20 text-cyan-100/40">Open receipt unavailable</Button><Button disabled variant="outline" className="border-white/10 text-white/40">Retry provider unavailable</Button></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Status semantics</p><h2 className="mt-2 text-2xl font-black">A label is not evidence</h2><div className="mt-5 space-y-3">{[{ label: "Pending", detail: "Requires provider query or webhook evidence." }, { label: "Paid", detail: "Requires verified capture/authorization and receipt." }, { label: "Failed", detail: "Requires typed provider failure and retry semantics." }, { label: "Refunded", detail: "Requires refund ID, amount, status, and reconciliation." }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="font-semibold text-white">{item.label}</p><p className="mt-1 text-sm leading-5 text-slate-500">{item.detail}</p></div>)}</div><div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex gap-3"><TriangleAlert className="mt-0.5 size-5 shrink-0 text-amber-200" /><p className="text-sm leading-6 text-slate-300">Never display payment success from a client-only callback. Verify server-side provider events and reconcile them with the order.</p></div></div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Production checklist</p><h2 className="mt-2 text-2xl font-black">A real confirmation must prove</h2><div className="mt-5 space-y-3">{["Provider order/authorization/capture IDs, amount, currency, payer, merchant, and timestamps", "Server-verified webhook signatures, idempotency, replay protection, and reconciliation", "Typed pending/paid/failed/refunded/disputed states with retries and failure handling", "Receipt generation, privacy, redaction, support, audit events, and retention", "Monitoring, fraud controls, chargeback handling, incident response, and customer notification"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><LockKeyhole className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake success</h2><p className="mt-3 text-sm leading-6 text-slate-400">Changing the fixture status changes local UI state only. It does not charge, capture, refund, notify, issue a receipt, or settle money.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Status states are testable", description: "Pending, paid, failed, and refunded fixture states exercise downstream UI without provider claims.", icon: ClipboardCheck, status: "Local fixture" }, { title: "Evidence is named", description: "Provider IDs, webhooks, timestamps, receipts, and reconciliation are listed as requirements.", icon: ShieldCheck, status: "Guardrail" }, { title: "Settlement is off", description: "No charge, capture, refund, dispute, payment, or transaction success is fabricated.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
