import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Activity, BarChart3, Check, Download, Filter, LockKeyhole, RefreshCw, Search, ShieldAlert, TrendingUp, Users, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const metrics = [{ id: 1, name: "Active users", category: "Audience", unit: "count", detail: "Requires authenticated event data and a defined reporting window." }, { id: 2, name: "Course completion", category: "Learning", unit: "%", detail: "Requires course events, denominator rules, and completion definitions." }, { id: 3, name: "Portfolio value", category: "Financial", unit: "currency", detail: "Requires verified balances, prices, timestamps, and valuation policy." }, { id: 4, name: "API latency", category: "Operations", unit: "ms", detail: "Requires request traces, sampling, environment, and percentile rules." }];
export default function QuickStats() {
  const [query, setQuery] = useState("");
  const [category, setCategory] = useState("All");
  const [selected, setSelected] = useState(1);
  const [range, setRange] = useState("Window not set");
  const [comparison, setComparison] = useState("No comparison");
  const [saved, setSaved] = useState(false);
  const [showGates, setShowGates] = useState(false);
  const categories = ["All", ...Array.from(new Set(metrics.map((metric) => metric.category)))];
  const filtered = useMemo(() => metrics.filter((metric) => (category === "All" || metric.category === category) && `${metric.name} ${metric.category} ${metric.detail}`.toLowerCase().includes(query.toLowerCase())), [category, query]);
  const metric = metrics.find((item) => item.id === selected) ?? metrics[0];
  const reset = () => { setQuery(""); setCategory("All"); setSelected(1); setRange("Window not set"); setComparison("No comparison"); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={BarChart3} eyebrow="QuickStats · Metrics preview" title="Define the metric before claiming the number." description="Explore local metric concepts with categories, time range, comparison, trend intent, activity, performance, export, and evidence gates. No users, events, values, percentages, rankings, or analytics are connected." badge="Metrics workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "View saved locally" : "Save view locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review metric gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset view</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Metrics", value: `${metrics.length} local`, hint: "Values unavailable", icon: BarChart3, tone: "cyan" }, { label: "Window", value: range, hint: "Not persisted", icon: Activity, tone: "violet" }, { label: "Comparison", value: comparison, hint: "No baseline", icon: TrendingUp, tone: "amber" }, { label: "Events", value: "Unconnected", hint: "No telemetry source", icon: Users, tone: "slate" }]} /><ScreenPreviewBanner title="Metrics evidence boundary"><strong>This is a local metric-definition and visualization preview, not an analytics report.</strong> Metric names, units, filters, ranges, comparisons, trend intent, saved state, and empty chart states are browser concepts. No user, event, count, percentage, currency value, latency, ranking, trend, benchmark, performance, or business conclusion is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.84fr_1.16fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Search className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local metric concepts" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-4 flex flex-wrap gap-2">{categories.map((entry) => <Button key={entry} onClick={() => setCategory(entry)} size="sm" variant="outline" className={category === entry ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-100" : "border-white/10 text-slate-400"}><Filter className="mr-1 size-3" />{entry}</Button>)}</div><div className="mt-6 space-y-3">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{item.name}</p><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Unmeasured</Badge></div><div className="mt-4 flex gap-2"><Badge variant="outline" className="border-white/10 text-slate-500">{item.category}</Badge><Badge variant="outline" className="border-white/10 text-slate-500">{item.unit}</Badge></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected metric</p><h2 className="mt-2 text-2xl font-black">{metric.name}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{metric.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Category", value: metric.category }, { label: "Unit", value: metric.unit }, { label: "Current value", value: "Unavailable" }, { label: "Source", value: "Not connected" }, { label: "Trend", value: "Unmeasured" }, { label: "Confidence", value: "Not assessed" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2"><label className="text-sm text-slate-400">Time range<select value={range} onChange={(event) => setRange(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Window not set</option><option>Today intent</option><option>7-day intent</option><option>30-day intent</option></select></label><label className="text-sm text-slate-400">Comparison<select value={comparison} onChange={(event) => setComparison(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>No comparison</option><option>Previous period intent</option><option>Baseline intent</option></select></label></div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-8 text-center"><BarChart3 className="mx-auto size-7 text-slate-600" /><p className="mt-3 font-semibold">No chart data loaded</p><p className="mt-2 text-sm text-slate-500">Connect a governed event source before plotting a value or trend.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400"><Download className="mr-2 size-4" />Export unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Refresh unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No analytics claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A metric label does not establish a value, trend, population, date, source, benchmark, or business decision.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Metrics gates</p><h2 className="mt-2 text-2xl font-black">What a real metric must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Metric definition, grain, denominator, population, exclusions, and ownership", "Event schema, source integrity, timestamps, deduplication, freshness, and retention", "Time zone, window, cohort, comparison, baseline, aggregation, and missing-data rules", "Privacy, consent, access control, financial-data handling, and sensitive attributes", "Validation, statistical uncertainty, confidence, benchmark, monitoring, and rollback", "Export, audit, reproducibility, incident response, and decision-use policy"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Metrics surface preserved", description: "Metric cards, search, categories, units, range, comparison, trends, empty chart, export, refresh, save/reset, and gates remain interactive.", icon: BarChart3, status: "Local metrics" }, { title: "No analytics theater", description: "Users, events, counts, percentages, currency, latency, rankings, trends, benchmarks, and business conclusions are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Definition before number", description: "Real metrics need governed events, windows, privacy, validation, uncertainty, audit, and decision policy.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
