import { useState } from "react";
import { Check, Eye, Heart, Info, LockKeyhole, MessageCircle, RefreshCw, Search, ShieldAlert, SlidersHorizontal, Users, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

const profiles = [
  { name: "Creator profile concept", type: "Creator", state: "Unverified", detail: "Requires identity, consent, age assurance, profile provenance, and moderation review." },
  { name: "Learning partner concept", type: "Education", state: "Unavailable", detail: "Requires shared-interest policy, privacy basis, safety controls, and accountable discovery rules." },
  { name: "Community profile concept", type: "Community", state: "Unmeasured", detail: "Requires profile ownership, audience controls, block/report flows, and interaction evidence." },
];

export default function SwipeInterface() {
  const [selected, setSelected] = useState(0);
  const [query, setQuery] = useState("");
  const [mode, setMode] = useState("Discovery intent");
  const [saved, setSaved] = useState(false);
  const [showBoundary, setShowBoundary] = useState(false);
  const [refreshing, setRefreshing] = useState(false);
  const refresh = () => { setRefreshing(true); window.setTimeout(() => setRefreshing(false), 500); };
  const reset = () => { setSelected(0); setQuery(""); setMode("Discovery intent"); setSaved(false); setShowBoundary(false); setRefreshing(false); };
  const visible = profiles.filter((profile) => `${profile.name} ${profile.type} ${profile.detail}`.toLowerCase().includes(query.toLowerCase()));
  const profile = profiles[selected] ?? profiles[0];
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Users} eyebrow="Swipe interface · Discovery preview" title="Explore people concepts without inventing identity or connection." description="Review a local discovery workspace with profile concepts, search, preference intent, consent, age assurance, safety, moderation, block/report controls, messaging, privacy, retry, save/reset, and evidence boundaries. No person, match, identity, message, or social outcome is connected." badge="Evidence-bounded discovery"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save discovery view"}</Button><Button onClick={() => setShowBoundary((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showBoundary ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showBoundary ? "Close evidence boundary" : "Review safety gates"}</Button><Button onClick={refresh} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className={`mr-2 size-4 ${refreshing ? "animate-spin" : ""}`} />{refreshing ? "Retrying local state" : "Retry profile source"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><X className="mr-2 size-4" />Reset discovery</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Profiles", value: "Unmeasured", hint: "No identity source", icon: Users, tone: "cyan" }, { label: "Matches", value: "Unavailable", hint: "No matching source", icon: Heart, tone: "violet" }, { label: "Safety", value: "Required", hint: "No moderation source", icon: ShieldAlert, tone: "amber" }, { label: "Messages", value: "Blocked", hint: "No contact source", icon: MessageCircle, tone: "slate" }]} /><ScreenPreviewBanner title="Discovery evidence boundary"><strong>This is a local discovery-design preview, not evidence that any person, profile, identity, age, match, preference, message, consent, relationship, or social outcome exists.</strong> Profile cards, filters, local selection, preference intent, saved state, retry feedback, and disabled like/message actions are browser concepts. No identity, recommendation, match, contact, engagement, or safety outcome is asserted.</ScreenPreviewBanner><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="grid gap-4 md:grid-cols-[1.2fr_0.8fr]"><label className="text-sm font-semibold text-slate-300"><span className="flex items-center gap-2"><Search className="size-4 text-slate-500" />Search profile concepts</span><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search creator, learning, community..." className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white outline-none" /></label><label className="text-sm font-semibold text-slate-300"><span className="flex items-center gap-2"><SlidersHorizontal className="size-4 text-slate-500" />Discovery mode</span><select value={mode} onChange={(event) => setMode(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white"><option>Discovery intent</option><option>Shared interests intent</option><option>Creator collaboration intent</option><option>Community intent</option></select></label></div></CardContent></Card><section className="grid gap-4 md:grid-cols-3">{visible.map((item, index) => <button key={item.name} onClick={() => setSelected(profiles.indexOf(item))} className={`rounded-2xl border p-5 text-left transition ${selected === profiles.indexOf(item) ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10 bg-white/[0.04]"}`}><div className="flex items-start justify-between"><div className="flex size-11 items-center justify-center rounded-2xl bg-cyan-300/10 text-cyan-200"><Users className="size-5" /></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.state}</Badge></div><h2 className="mt-4 font-bold">{item.name}</h2><p className="mt-1 text-xs text-cyan-200">{item.type}</p><p className="mt-3 text-sm leading-6 text-slate-400">{item.detail}</p></button>)}</section><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200">Selected discovery concept</p><h2 className="mt-2 text-2xl font-black">{profile.name}</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{profile.state}</Badge></div><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Identity / ownership", value: "Unverified", icon: Users }, { label: "Consent / privacy", value: "Required", icon: LockKeyhole }, { label: "Age / safety", value: "Unverified", icon: ShieldAlert }, { label: "Recommendation", value: "Unavailable", icon: Eye }, { label: "Match / mutual intent", value: "Unmeasured", icon: Heart }, { label: "Messaging / contact", value: "Blocked", icon: MessageCircle }].map(({ label, value, icon: Icon }) => <div key={label} className="flex items-center gap-3 rounded-xl border border-white/10 p-4"><Icon className="size-4 text-cyan-300" /><span className="flex-1 text-sm text-slate-300">{label}</span><span className="text-xs text-amber-200">{value}</span></div>)}</div><div className="mt-6 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Like unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Pass unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Message unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Report unavailable</Button></div></CardContent></Card><ScreenStatePanel type="unavailable" title="Discovery source unavailable" description="Connect authorized profile, consent, age-assurance, preference, recommendation, moderation, block/report, messaging, privacy, and audit data before enabling discovery." /></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><Info className="size-5 text-cyan-300" /><div><p className="font-bold">Discovery safety contract</p><p className="mt-1 text-sm text-slate-400">Real discovery requires identity provenance, consent, age and jurisdiction rules, privacy controls, explainable preferences, moderation, abuse prevention, block/report paths, and accountable contact permissions.</p></div></div><div className="mt-5 grid gap-3 md:grid-cols-2">{["Profile ownership, identity, age, locale, and consent", "Preference provenance, recommendation explainability, and fairness", "Privacy, visibility, retention, block, report, and safety controls", "Moderation, abuse detection, rate limits, and escalation", "Mutual intent, messaging authorization, delivery, and audit", "Accessible interaction, recovery, deletion, and appeal flows"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3 text-sm text-slate-300"><LockKeyhole className="size-4 text-amber-300" />{item}<span className="ml-auto text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card>{showBoundary && <Card className="border-amber-300/20 bg-amber-300/[0.06]"><CardContent className="p-6"><p className="font-semibold text-amber-100">No identity or match claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local profile concept does not prove a person, identity, age, preference, recommendation, match, message, consent, relationship, safety outcome, or engagement result.</p></CardContent></Card>}<ScreenFeatureGrid features={[{ title: "Discovery surface preserved", description: "Profiles, search, preferences, consent, age assurance, safety, moderation, block/report, messaging, retry, save/reset, and action gates remain visible.", icon: Users, status: "Local discovery" }, { title: "No match theater", description: "People, identities, recommendations, matches, messages, relationships, and social outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before contact", description: "Real discovery requires identity, consent, age assurance, privacy, moderation, abuse prevention, mutual intent, and audit.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
