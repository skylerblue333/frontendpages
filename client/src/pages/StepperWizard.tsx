import { useState } from "react";
import { Check, CircleAlert, ClipboardCheck, FileCheck2, KeyRound, LockKeyhole, RefreshCw, ShieldAlert, Sparkles, UserRound, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner } from "@/components/ScreenExperience";
const steps = [{ title: "Intent", description: "Define the workflow outcome and accountable owner.", icon: Sparkles }, { title: "Identity", description: "Verify account, permissions, privacy, and consent.", icon: UserRound }, { title: "Integrations", description: "Validate providers, contracts, credentials, and scopes.", icon: KeyRound }, { title: "Review", description: "Inspect data, risks, approvals, rollback, and audit.", icon: ClipboardCheck }, { title: "Complete", description: "Only claim completion after reproducible evidence.", icon: FileCheck2 }];
export default function StepperWizard() { const [step, setStep] = useState(0); const [saved, setSaved] = useState(false); const [showBoundary, setShowBoundary] = useState(false); const [retrying, setRetrying] = useState(false); const retry = () => { setRetrying(true); window.setTimeout(() => setRetrying(false), 500); }; const reset = () => { setStep(0); setSaved(false); setShowBoundary(false); setRetrying(false); }; const current = steps[step]; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={ClipboardCheck} eyebrow="Stepper wizard · Workflow preview" title="Move through setup without pretending a workflow completed." description="Explore a local guided workflow with intent, identity, integrations, review, completion, readiness, retry, save/reset, and evidence boundaries. No account, permission, provider, credential, provisioning, integration, or completion state is connected." badge="Evidence-bounded wizard"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save wizard state"}</Button><Button onClick={() => setShowBoundary((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showBoundary ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showBoundary ? "Close boundary" : "Review completion gates"}</Button><Button onClick={retry} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className={`mr-2 size-4 ${retrying ? "animate-spin" : ""}`} />{retrying ? "Retrying local state" : "Retry readiness"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><X className="mr-2 size-4" />Reset wizard</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenPreviewBanner title="Wizard evidence boundary"><strong>This is a local workflow-design preview, not evidence that an account, permission, integration, provisioning run, approval, delivery, or completion exists.</strong> Step selection, local saved state, readiness copy, retry feedback, and disabled submit/complete actions are browser concepts. No identity, security, API, credential, payment, deployment, automation, or business result is asserted.</ScreenPreviewBanner><section className="grid gap-3 md:grid-cols-5">{steps.map(({ title, icon: Icon }, index) => <button key={title} onClick={() => setStep(index)} className={`rounded-2xl border p-4 text-left ${step === index ? "border-cyan-300/40 bg-cyan-300/[0.07]" : "border-white/10 bg-white/[0.04]"}`}><div className="flex items-center justify-between"><span className="text-xs font-semibold text-slate-500">0{index + 1}</span><Icon className="size-4 text-cyan-300" /></div><p className="mt-4 font-semibold">{title}</p><p className="mt-1 text-xs text-slate-500">{index <= step ? "Local view" : "Not reached"}</p></button>)}</section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200">Current step</p><h2 className="mt-2 text-2xl font-black">{current.title}</h2><p className="mt-2 text-sm text-slate-400">{current.description}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{step === steps.length - 1 ? "Completion blocked" : "Preview"}</Badge></div><div className="mt-6 grid gap-3 md:grid-cols-2">{["Inputs defined", "Owner accountable", "Permissions verified", "Provider contract connected", "Errors and retry defined", "Audit and rollback defined"].map((item, index) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-4"><CircleAlert className="size-5 text-amber-300" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">{index === 0 && step === 0 ? "Local" : "Required"}</span></div>)}</div><div className="mt-6 flex flex-wrap gap-2"><Button onClick={() => setStep(Math.max(0, step - 1))} disabled={step === 0} variant="outline" className="border-white/10 text-slate-400">Previous</Button><Button onClick={() => setStep(Math.min(steps.length - 1, step + 1))} disabled={step === steps.length - 1} className="bg-cyan-300 text-slate-950">Next local step</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Submit unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Complete unavailable</Button></div></CardContent></Card>{showBoundary && <Card className="border-amber-300/20 bg-amber-300/[0.06]"><CardContent className="p-6"><p className="font-semibold text-amber-100">No workflow-completion claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local step selection does not prove account creation, identity verification, permissions, provider connection, provisioning, approval, delivery, or business completion.</p></CardContent></Card>}<ScreenFeatureGrid features={[{ title: "Wizard surface preserved", description: "Intent, identity, integrations, review, completion, readiness, step navigation, retry, save/reset, and gates remain visible.", icon: ClipboardCheck, status: "Local workflow" }, { title: "No completion theater", description: "Account, permission, integration, provisioning, delivery, approval, and business outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before completion", description: "Real workflows require contracts, authorization, validation, persistence, rollback, audit, and accountable review.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>; }
