import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Calendar, Check, ClipboardList, FileText, Film, Image, LockKeyhole, Mic, Play, Plus, RefreshCw, Send, Shield, Sparkles, Upload, Video, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const FORMATS = [{ id: "article", name: "Article", description: "Long-form text draft", icon: FileText }, { id: "video", name: "Video", description: "Short-form video plan", icon: Film }, { id: "audio", name: "Audio", description: "Podcast or voice draft", icon: Mic }, { id: "live", name: "Live stream", description: "Session run-of-show", icon: Video }];
const INITIAL_DRAFTS = [{ id: "draft-1", title: "Welcome to the studio", format: "Article", stage: "Outline", updated: "Local now" }, { id: "draft-2", title: "Creator workflow walkthrough", format: "Video", stage: "Script", updated: "Local now" }, { id: "draft-3", title: "Community questions", format: "Live stream", stage: "Run of show", updated: "Local now" }];
const STAGES = ["Idea", "Outline", "Script", "Review", "Ready"];

export default function CreatorStudio() {
  const [format, setFormat] = useState(FORMATS[0].id);
  const [drafts, setDrafts] = useState(INITIAL_DRAFTS);
  const [selected, setSelected] = useState(INITIAL_DRAFTS[0].id);
  const [saved, setSaved] = useState(false);
  const current = drafts.find((draft) => draft.id === selected) ?? drafts[0];
  const selectedFormat = FORMATS.find((item) => item.id === format) ?? FORMATS[0];
  const stageIndex = useMemo(() => STAGES.indexOf(current?.stage ?? "Idea"), [current]);
  const advance = () => { setDrafts((items) => items.map((draft) => draft.id === current.id ? { ...draft, stage: STAGES[Math.min(STAGES.length - 1, stageIndex + 1)] } : draft)); setSaved(false); };
  const createDraft = () => { const id = `draft-${drafts.length + 1}`; const item = { id, title: `New ${selectedFormat.name} draft`, format: selectedFormat.name, stage: "Idea", updated: "Local now" }; setDrafts((items) => [...items, item]); setSelected(id); setSaved(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Film} eyebrow="Creator production · Studio" title="Turn an idea into a reviewable draft." description="Plan articles, videos, audio, and live-session formats with local workflow stages. This studio does not upload media, publish content, schedule a live event, moderate comments, calculate analytics, or distribute to a platform." badge="Production preview"><div className="flex flex-wrap gap-2"><Button onClick={createDraft} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Plus className="mr-2 size-4" />New local draft</Button><Button onClick={() => setSaved(true)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save studio state"}</Button><Button onClick={() => { setDrafts(INITIAL_DRAFTS); setSelected(INITIAL_DRAFTS[0].id); setFormat(FORMATS[0].id); setSaved(false); }} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Local drafts", value: String(drafts.length), hint: "Not uploaded", icon: ClipboardList }, { label: "Workflow", value: `${stageIndex + 1}/${STAGES.length}`, hint: "Current draft stage", icon: Play, tone: "violet" }, { label: "Publishing", value: "Off", hint: "No platform connected", icon: WifiOff, tone: "amber" }, { label: "Uploads", value: "Blocked", hint: "No media storage", icon: Upload, tone: "slate" }]} /><ScreenPreviewBanner title="Creator Studio evidence boundary"><strong>Draft titles, stages, schedules, review badges, and format choices are local planning state—not proof of an uploaded asset, published post, scheduled stream, audience reach, monetization, moderation outcome, or platform distribution.</strong> Production studio workflows require authenticated ownership, media storage, transcoding, rights, moderation, scheduling, analytics, rollback, and a platform publishing contract.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.85fr_1.15fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Draft library</p><h2 className="mt-2 text-2xl font-black">Local work queue</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Offline preview</Badge></div><div className="mt-5 space-y-3">{drafts.map((draft) => <button key={draft.id} onClick={() => setSelected(draft.id)} className={`w-full rounded-xl border p-4 text-left ${selected === draft.id ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-black/10"}`}><div className="flex items-center justify-between gap-3"><span className="font-semibold">{draft.title}</span><span className="text-xs text-cyan-200">{draft.stage}</span></div><p className="mt-2 text-xs text-slate-500">{draft.format} · {draft.updated} · local only</p></button>)}</div><div className="mt-5 rounded-xl border border-red-300/20 bg-red-300/[0.05] p-4"><p className="font-semibold text-red-200">No upload or publish action</p><p className="mt-2 text-sm leading-6 text-slate-400">The draft list does not represent a server asset, content ID, scheduled job, public post, or moderation record.</p></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-300">Draft workspace</p><h2 className="mt-2 text-2xl font-black">{current?.title}</h2></div><Badge variant="outline" className="border-violet-300/20 text-violet-200">{current?.format}</Badge></div><div className="mt-6 flex items-center justify-between gap-2">{STAGES.map((stage, index) => <div key={stage} className="flex flex-1 items-center gap-2"><div className={`flex size-8 items-center justify-center rounded-full text-xs font-bold ${index <= stageIndex ? "bg-cyan-300 text-slate-950" : "bg-white/10 text-slate-500"}`}>{index < stageIndex ? <Check className="size-4" /> : index + 1}</div>{index < STAGES.length - 1 && <div className={`h-1 flex-1 rounded-full ${index < stageIndex ? "bg-cyan-300" : "bg-white/10"}`} />}</div>)}</div><div className="mt-2 flex justify-between text-[10px] text-slate-500">{STAGES.map((stage) => <span key={stage}>{stage}</span>)}</div><div className="mt-8 grid gap-3 sm:grid-cols-2">{FORMATS.map((item) => <button key={item.id} onClick={() => { setFormat(item.id); setSaved(false); }} className={`flex items-center gap-3 rounded-xl border p-4 text-left ${format === item.id ? "border-violet-300/40 bg-violet-300/[0.08]" : "border-white/10 bg-black/10"}`}><item.icon className="size-5 text-violet-200" /><div><p className="font-semibold">{item.name}</p><p className="mt-1 text-xs text-slate-500">{item.description}</p></div></button>)}</div><div className="mt-6 flex flex-wrap gap-2"><Button onClick={advance} disabled={stageIndex >= STAGES.length - 1} className="bg-violet-500 text-white hover:bg-violet-400"><ArrowRightIcon />Advance locally</Button><Button onClick={() => setSaved(true)} variant="outline" className="border-white/15 text-white"><ClipboardList className="mr-2 size-4" />Mark review note</Button></div></CardContent></Card></section><section className="grid gap-4 md:grid-cols-3">{[{ title: "Media pipeline", description: "Upload, storage, transcoding, thumbnails, and delivery are not connected.", icon: Upload }, { title: "Review gates", description: "Rights, safety, privacy, moderation, and creator ownership require authoritative workflows.", icon: Shield }, { title: "Distribution", description: "Scheduling, publishing, analytics, monetization, and rollback remain unavailable.", icon: Send }].map((item) => <Card key={item.title} className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><item.icon className="size-5 text-cyan-300" /><h2 className="mt-4 font-black">{item.title}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{item.description}</p></CardContent></Card>)}</section><ScreenFeatureGrid features={[{ title: "Draft state is not content state", description: "A local title and stage cannot establish a stored asset, content ID, license, or public post.", icon: FileText, status: "Guardrail" }, { title: "Review before publish", description: "Rights, moderation, privacy, and ownership gates must be verifiable and recoverable.", icon: Shield, status: "Required" }, { title: "No fake distribution", description: "The studio preserves production workflow utility without claiming uploads, schedules, viewers, or revenue.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
function ArrowRightIcon() { return <span className="mr-2">→</span>; }
