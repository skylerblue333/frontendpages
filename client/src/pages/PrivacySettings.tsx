import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { BarChart3, Check, Download, Eye, FileKey2, LockKeyhole, RefreshCw, ShieldAlert, Trash2, UserRoundCheck, WifiOff, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
type Setting = { id: string; title: string; description: string; value: boolean; icon: typeof Eye };
const initial: Setting[] = [{ id: "analytics", title: "Product analytics", description: "Intent for analytics controls; no telemetry is sent from this preview.", value: false, icon: BarChart3 }, { id: "personalization", title: "Personalization", description: "Intent for local recommendations; no profile or model is updated.", value: true, icon: Eye }, { id: "sharing", title: "Data sharing", description: "Intent remains disabled; no processor or partner sharing is configured.", value: false, icon: UserRoundCheck }, { id: "retention", title: "Extended retention", description: "Intent for a retention choice; no retention policy is applied here.", value: false, icon: FileKey2 }];
export default function PrivacySettings() {
  const [settings, setSettings] = useState(initial);
  const [saved, setSaved] = useState(false);
  const [showRights, setShowRights] = useState(false);
  const toggle = (id: string) => { setSaved(false); setSettings((items) => items.map((item) => item.id === id ? { ...item, value: !item.value } : item)); };
  const reset = () => { setSettings(initial); setSaved(false); setShowRights(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={LockKeyhole} eyebrow="PrivacySettings · Control preview" title="Choose privacy intent without pretending it was synchronized." description="Review analytics, personalization, sharing, retention, export, deletion, and consent-intent controls. The toggles remain local browser state; no account record, processor choice, rights request, or deletion is executed." badge="Privacy controls"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Controls saved locally" : "Save controls locally"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset controls</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Controls", value: String(settings.length), hint: "Local intent", icon: LockKeyhole, tone: "cyan" }, { label: "Enabled", value: String(settings.filter((item) => item.value).length), hint: "Browser state", icon: Check, tone: "violet" }, { label: "Account sync", value: "Off", hint: "No settings service", icon: WifiOff, tone: "amber" }, { label: "Rights requests", value: "Off", hint: "No legal workflow", icon: ShieldAlert, tone: "slate" }]} /><ScreenPreviewBanner title="Privacy settings evidence boundary"><strong>No privacy preference is being persisted or enforced.</strong> The switches are local preview state. No analytics consent, personalization profile, data-sharing permission, retention rule, account update, export, deletion, processor request, or legal acknowledgement is created. Never treat this page as proof of compliance or privacy protection.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Control center</p><h2 className="mt-2 text-2xl font-black">Choose local intent</h2></div><Badge variant="outline" className="border-white/10 text-amber-200">Not synchronized</Badge></div><div className="mt-5 space-y-3">{settings.map((item) => { const Icon = item.icon; return <div key={item.id} className="flex items-center gap-3 rounded-xl border border-white/10 p-4"><div className="flex size-10 shrink-0 items-center justify-center rounded-xl bg-cyan-300/10"><Icon className="size-5 text-cyan-200" /></div><div className="flex-1"><p className="font-semibold">{item.title}</p><p className="mt-1 text-sm leading-5 text-slate-500">{item.description}</p></div><button aria-pressed={item.value} onClick={() => toggle(item.id)} className={`relative h-7 w-12 rounded-full border transition ${item.value ? "border-cyan-300/50 bg-cyan-300/30" : "border-white/15 bg-white/5"}`}><span className={`absolute top-1 size-5 rounded-full bg-white transition ${item.value ? "left-6" : "left-1"}`} /></button></div>; })}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Current posture</p><h2 className="mt-2 text-2xl font-black">Local only</h2><div className="mt-5 space-y-3">{[{ label: "Account preference", value: "Unavailable" }, { label: "Consent ledger", value: "Unavailable" }, { label: "Processor sharing", value: "Not configured" }, { label: "Retention policy", value: "Not applied" }, { label: "Analytics delivery", value: "Off" }, { label: "Personalization profile", value: "Not updated" }].map((item) => <div key={item.label} className="flex items-center justify-between gap-3 rounded-xl border border-white/10 p-3"><span className="text-sm text-slate-400">{item.label}</span><span className="text-sm font-semibold text-amber-200">{item.value}</span></div>)}</div><div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex gap-3"><ShieldAlert className="mt-0.5 size-5 shrink-0 text-amber-200" /><p className="text-sm leading-6 text-slate-400">A local toggle is not consent. Production behavior requires identity, legal basis, versioned policy, persistence, enforcement, and audit.</p></div></div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[0.82fr_1.18fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Data rights</p><h2 className="mt-2 text-2xl font-black">Request workflow</h2></div><Button onClick={() => setShowRights((value) => !value)} variant="outline" className="border-white/10 text-slate-300">{showRights ? <X className="size-4" /> : <UserRoundCheck className="size-4" />}</Button></div><div className="mt-5 grid gap-3 sm:grid-cols-2"><Button disabled variant="outline" className="justify-start border-white/10 text-slate-500"><Download className="mr-2 size-4" />Export unavailable</Button><Button disabled variant="outline" className="justify-start border-white/10 text-slate-500"><Trash2 className="mr-2 size-4" />Delete unavailable</Button></div>{showRights && <div className="mt-5 rounded-xl border border-cyan-300/20 bg-cyan-300/[0.05] p-4 text-sm leading-6 text-slate-400">No request is submitted. A real workflow must verify identity, determine scope, handle exceptions, track status, and honor retention/legal obligations.</div>}</CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Production requirements</p><h2 className="mt-2 text-2xl font-black">What must be implemented</h2><div className="mt-5 space-y-3">{["Authenticated per-user persistence and cross-device conflict handling", "Versioned consent and policy records with withdrawal semantics", "Processor/analytics enforcement, redaction, and data minimization", "Retention, export, deletion, legal hold, and regional policy workflows", "Audit, support escalation, offline/error states, and accessibility coverage"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><Check className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Controls preserved", description: "Analytics, personalization, sharing, retention, local save/reset, posture, export/delete affordances, and rights review remain visible and interactive.", icon: LockKeyhole, status: "Local preview" }, { title: "No silent consent", description: "Toggles never imply account persistence, legal consent, enforcement, compliance, or privacy protection.", icon: ShieldAlert, status: "Guardrail" }, { title: "Rights are protected by default", description: "Export and deletion remain disabled until identity, scope, verification, retention, and audit workflows exist.", icon: FileKey2, status: "Blocked" }]} /></main></div>;
}
