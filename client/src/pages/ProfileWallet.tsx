import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Activity, Check, Coins, ExternalLink, Eye, LockKeyhole, Network, RefreshCw, ShieldAlert, Wallet, X, Zap } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const tabs = ["Overview", "Activity", "Earn"];
const networks = ["Network not selected", "Ethereum concept", "Solana concept", "Skycoin concept"];
export default function ProfileWallet() {
  const [tab, setTab] = useState(tabs[0]);
  const [network, setNetwork] = useState(networks[0]);
  const [address, setAddress] = useState("");
  const [saved, setSaved] = useState(false);
  const [showGates, setShowGates] = useState(false);
  const reset = () => { setTab(tabs[0]); setNetwork(networks[0]); setAddress(""); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Wallet} eyebrow="ProfileWallet · Wallet readiness preview" title="Make wallet boundaries impossible to misunderstand." description="Review local network intent, address text, activity, and earning surfaces. No wallet is connected, no balance or ownership is measured, and no transaction, staking, reward, payment, or custody action is available." badge="Wallet workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Wallet intent saved locally" : "Save intent locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><ShieldAlert className="mr-2 size-4" />{showGates ? "Hide gates" : "Review wallet gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset wallet</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Wallet", value: "Not connected", hint: "No custody path", icon: Wallet, tone: "cyan" }, { label: "Network", value: network === networks[0] ? "Unselected" : "Concept only", hint: "No RPC source", icon: Network, tone: "violet" }, { label: "Balance", value: "Unavailable", hint: "No ledger source", icon: Coins, tone: "amber" }, { label: "Activity", value: "Unavailable", hint: "No chain source", icon: Activity, tone: "slate" }]} /><ScreenPreviewBanner title="Wallet evidence boundary"><strong>This is a local wallet-readiness worksheet, not a wallet.</strong> Network intent, address text, tabs, and local save state are browser concepts. No wallet connection, private key, seed phrase, balance, ownership, transaction, network result, staking, reward, payment, or custody claim is made.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.82fr_1.18fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><div className="flex size-16 items-center justify-center rounded-2xl bg-cyan-300/10"><Wallet className="size-8 text-cyan-200" /></div><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Wallet posture</p><h2 className="mt-2 text-2xl font-black">No wallet connected</h2><p className="mt-1 text-sm text-slate-500">Custody and chain access are unavailable.</p></div></div><div className="mt-6 space-y-4"><label className="block text-sm text-slate-400">Public address intent<input value={address} onChange={(event) => setAddress(event.target.value.trim().slice(0, 140))} placeholder="Paste only if you are documenting an address concept" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 font-mono text-sm text-white outline-none" /><span className="mt-2 block text-xs text-amber-200">Local text only; never enter a seed phrase or private key.</span></label><div><p className="text-sm text-slate-400">Network intent</p><div className="mt-2 flex flex-wrap gap-2">{networks.map((item) => <button key={item} onClick={() => setNetwork(item)} className={`rounded-lg border px-3 py-2 text-xs ${network === item ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div></div><div className="rounded-2xl border border-amber-300/25 bg-amber-300/[0.06] p-5"><div className="flex items-start gap-3"><LockKeyhole className="mt-0.5 size-5 shrink-0 text-amber-200" /><div><p className="font-semibold text-amber-100">Connection is blocked</p><p className="mt-2 text-sm leading-6 text-slate-400">No wallet provider, RPC, address validation, signature verification, custody, transaction builder, or chain indexer is connected.</p><Button disabled className="mt-4 bg-slate-700 text-slate-400">Connect wallet unavailable</Button></div></div></div></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Ledger posture</p><h2 className="mt-2 text-2xl font-black">No financial data loaded</h2><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Address", value: address ? "Local text" : "Empty" }, { label: "Ownership", value: "Unverified" }, { label: "Balance", value: "Unavailable" }, { label: "Transactions", value: "Unavailable" }, { label: "Network", value: network === networks[0] ? "Unselected" : "Concept" }, { label: "Rewards", value: "Not measured" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div>{showGates && <div className="mt-5 rounded-xl border border-cyan-300/20 bg-cyan-300/[0.05] p-4 text-sm leading-6 text-slate-400">A real wallet surface must prove network, address format, chain status, signature, confirmations, failure state, idempotency, user consent, and never expose private keys or seed phrases.</div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex flex-wrap gap-2">{tabs.map((item) => <button key={item} onClick={() => setTab(item)} className={`flex items-center gap-2 rounded-lg border px-4 py-2 text-sm ${tab === item ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-200" : "border-white/10 text-slate-500"}`}>{item === "Overview" && <Wallet className="size-4" />}{item === "Activity" && <Activity className="size-4" />}{item === "Earn" && <Zap className="size-4" />}{item}</button>)}</div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-12 text-center">{tab === "Overview" && <Wallet className="mx-auto size-10 text-cyan-300/40" />}{tab === "Activity" && <Activity className="mx-auto size-10 text-violet-300/40" />}{tab === "Earn" && <Zap className="mx-auto size-10 text-amber-300/40" />}<h3 className="mt-4 text-xl font-black">{tab} unavailable in preview</h3><p className="mx-auto mt-2 max-w-xl text-sm leading-6 text-slate-500">This tab is preserved as an honest empty state. No {tab.toLowerCase()} data source is connected, and no balances, transactions, rewards, yield, or earnings are fabricated.</p><Button disabled className="mt-5 bg-slate-700 text-slate-400">Load {tab.toLowerCase()} unavailable</Button></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Wallet safety gates</p><h2 className="mt-2 text-2xl font-black">What real wallet functionality must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Network, address, chain ID, RPC, and explorer verification", "Wallet-provider permission, signature, session, and disconnect", "Private-key and seed-phrase exclusion from client and logs", "Transaction parameters, simulation, signature, confirmation, and failure", "Idempotency, replay protection, nonce, fee, and duplicate submission handling", "Custody, staking, rewards, payments, tax, support, and recovery contracts"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ExternalLink className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Wallet surface preserved", description: "Overview, activity, earn, network intent, address text, ledger posture, wallet gates, local save/reset, and blocked actions remain interactive.", icon: Wallet, status: "Local wallet" }, { title: "No financial theater", description: "Balances, ownership, transactions, rewards, staking, payments, earnings, custody, and yield are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Security before action", description: "Real wallet features require chain verification, safe signing, idempotency, custody boundaries, failure handling, and support.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
