import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { BarChart3, Bell, Check, Clock3, FileCheck2, Filter, Info, Lightbulb, LockKeyhole, RefreshCw, ShieldCheck, Sparkles, Tag, WifiOff, Zap } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const EVENTS = [{ id: "e1", title: "Review window opened", priority: "High", type: "Workspace", detail: "A synthetic reminder to review evidence before changing a screen." }, { id: "e2", title: "Security checklist due", priority: "Critical", type: "Security", detail: "A local guardrail concept for access and secret review." }, { id: "e3", title: "Study plan checkpoint", priority: "Medium", type: "Learning", detail: "A synthetic learning reminder without enrollment or grade state." }, { id: "e4", title: "Wallet safety note", priority: "Low", type: "Finance", detail: "A local safety concept; no account, balance, or transaction exists." }, { id: "e5", title: "Community digest concept", priority: "Ambient", type: "Community", detail: "A local digest fixture without sender, activity, or delivery data." }];
const FILTERS = ["All", "Unread", "Critical", "High", "Medium", "Low"];
const PRIORITIES = ["Critical", "High", "Medium", "Low", "Ambient"];

export default function NotificationIntelligence() {
  const [filter, setFilter] = useState("All");
  const [selectedId, setSelectedId] = useState(EVENTS[0].id);
  const [read, setRead] = useState<string[]>([]);
  const [saved, setSaved] = useState(false);
  const [note, setNote] = useState("");
  const visible = useMemo(() => EVENTS.filter((event) => (filter === "All" || filter === "Unread" ? filter === "All" || !read.includes(event.id) : event.priority === filter)), [filter, read]);
  const selected = EVENTS.find((event) => event.id === selectedId) ?? EVENTS[0];
  const reset = () => { setFilter("All"); setSelectedId(EVENTS[0].id); setRead([]); setSaved(false); setNote(""); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Sparkles} eyebrow="NotificationIntelligence · Triage design" title="Explain the priority before calling it intelligent." description="Review synthetic notification events, rule-based priority concepts, local read state, summary planning, and analytics definitions. No AI model, ranking score, personalization, anomaly detection, user profile, delivery, or performance result is claimed." badge="Intelligence preview"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Triage plan saved locally" : "Save triage plan locally"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset triage</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Events", value: String(EVENTS.length), hint: "Synthetic feed", icon: Bell }, { label: "Unread", value: String(EVENTS.filter((event) => !read.includes(event.id)).length), hint: "Local state", icon: Zap, tone: "violet" }, { label: "Rules", value: String(PRIORITIES.length), hint: "Concepts only", icon: Lightbulb, tone: "amber" }, { label: "AI", value: "Off", hint: "No invocation", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="NotificationIntelligence evidence boundary"><strong>Priority labels, rule cards, local read state, summary placeholders, and analytics definitions are triage-design fixtures—not AI output, relevance scores, personalized ranking, urgency determination, anomaly detection, user profiling, read-rate analytics, delivery performance, or a provider response.</strong> Production intelligence requires an authorized model or explicit rules, event provenance, evaluation, explainability, privacy, feedback handling, safety review, channel consent, and observability.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1.05fr_0.95fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Synthetic feed</p><h2 className="mt-2 text-3xl font-black">Triage review</h2></div><Badge variant="outline" className="border-white/10 text-slate-400">No ranking</Badge></div><div className="mt-4 flex gap-2 overflow-x-auto">{FILTERS.map((item) => <button key={item} onClick={() => setFilter(item)} className={`rounded-xl border px-3 py-2 text-xs whitespace-nowrap ${filter === item ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div><div className="mt-5 space-y-3">{visible.map((event) => <button key={event.id} onClick={() => { setSelectedId(event.id); setRead((current) => current.includes(event.id) ? current : [...current, event.id]); }} className={`w-full rounded-2xl border p-4 text-left ${selectedId === event.id ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-black/10"}`}><div className="flex items-start gap-3"><div className={`mt-1 size-2 rounded-full ${read.includes(event.id) ? "bg-slate-600" : "bg-cyan-300"}`} /><div className="flex-1"><div className="flex items-center justify-between gap-2"><p className="font-black">{event.title}</p><Badge variant="outline" className="border-white/10 text-[10px] text-amber-200">{event.priority}</Badge></div><p className="mt-2 text-sm text-slate-400">{event.detail}</p><p className="mt-2 text-xs text-slate-500">{event.type} · {read.includes(event.id) ? "Read locally" : "Unread locally"}</p></div></div></button>)}{visible.length === 0 && <p className="py-8 text-center text-slate-500">No synthetic events match this filter.</p>}</div><div className="mt-5 flex items-start gap-3 text-xs leading-5 text-slate-500"><LockKeyhole className="mt-0.5 size-4 shrink-0 text-cyan-300" />Local selection does not rank a person, send an alert, or update a notification provider.</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Explainability worksheet</p><h2 className="mt-2 text-3xl font-black">{selected.title}</h2><p className="mt-3 text-slate-400">{selected.detail}</p><div className="mt-5 grid gap-3 sm:grid-cols-2">{[{ label: "Priority", value: selected.priority }, { label: "Rule source", value: "Not configured" }, { label: "Score", value: "Not calculated" }, { label: "Model", value: "Off" }, { label: "User profile", value: "Not read" }, { label: "Delivery", value: "Unavailable" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 rounded-2xl border border-violet-300/20 bg-violet-300/[0.05] p-5"><div className="flex items-center gap-3"><Info className="size-5 text-violet-200" /><div><p className="font-black">No AI summary</p><p className="text-xs text-slate-500">Draft summary and highlights are not generated</p></div></div><p className="mt-3 text-sm leading-6 text-slate-400">A production summary needs source records, model/provider, prompt policy, evaluation, privacy, and human review.</p></div><textarea value={note} onChange={(event) => { setNote(event.target.value); setSaved(false); }} placeholder="Write a local triage or explainability note…" className="mt-5 min-h-24 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-sm text-white placeholder:text-slate-500" /><div className="mt-4 flex gap-2"><Button onClick={() => setRead((current) => current.includes(selected.id) ? current.filter((id) => id !== selected.id) : [...current, selected.id])} className="bg-violet-500 text-white hover:bg-violet-400"><Check className="mr-2 size-4" />{read.includes(selected.id) ? "Mark unread locally" : "Mark read locally"}</Button><Button disabled variant="outline" className="border-white/10 text-white/40">Send unavailable</Button></div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-2"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Rule catalog</p><h2 className="mt-2 text-2xl font-black">Explain before optimizing</h2><div className="mt-5 space-y-3">{PRIORITIES.map((priority) => <div key={priority} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><Tag className="size-4 text-amber-200" /><span className="flex-1 text-sm text-slate-300">{priority} priority concept</span><span className="text-xs text-slate-500">Rule not configured</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><BarChart3 className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">Analytics definitions only</h2><p className="mt-3 text-sm leading-6 text-slate-400">Read rate, average score, read time, batching, and activity counts are not collected. No chart or AI summary can be treated as a platform measurement.</p></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Intelligence readiness</p><h2 className="mt-2 text-2xl font-black">Required before smart-notification claims</h2><div className="mt-5 space-y-3">{["Event provenance, rule/model version, feature policy, and explainability", "Evaluation set, relevance/urgency metrics, calibration, and error review", "Privacy, consent, sensitive attributes, feedback, and user controls", "Delivery lifecycle, rate limits, quiet hours, and failure handling", "Human review, appeals, accessibility, audit logs, and incident response"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><FileCheck2 className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake intelligence</h2><p className="mt-3 text-sm leading-6 text-slate-400">This page does not invoke AI, calculate priority, profile users, detect anomalies, summarize activity, or send notifications.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "A label is not intelligence", description: "Priority concepts preserve triage UX without ranking, urgency, profile, or model claims.", icon: Sparkles, status: "Guardrail" }, { title: "Smart needs evaluation", description: "Provenance, calibration, privacy, feedback, human review, and delivery evidence are required.", icon: ShieldCheck, status: "Required" }, { title: "No fake model", description: "The preview makes no tRPC, AI, analytics, or notification-provider request.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
