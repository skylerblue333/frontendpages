import { useMemo, useState } from "react";
import { Activity, AlertTriangle, ArrowRight, Check, CheckCircle2, Cloud, Database, FileCheck2, Globe2, HardDrive, Info, LockKeyhole, Network, RefreshCw, Route, Server, Settings2, ShieldCheck, Timer, Trash2, Unplug, WifiOff, Zap } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const ROUTES = [{ path: "/", policy: "public-shell", ttl: "Preview: 300s", state: "Reference route" }, { path: "/assets/*", policy: "immutable-assets", ttl: "Preview: 1 year", state: "Reference route" }, { path: "/api/*", policy: "no-store", ttl: "Preview: 0s", state: "Safety default" }, { path: "/private/*", policy: "private-session", ttl: "Not cached", state: "Safety default" }];
const GATES = ["Origin and health-check contract", "Cache-control and vary policy", "Purge authorization and scope", "TLS, headers, and secret handling", "Logs, metrics, and alert ownership"];

export default function CDNManagement() { const [selectedPath, setSelectedPath] = useState("/"); const [origin, setOrigin] = useState("origin.preview.internal"); const [ttl, setTtl] = useState("300"); const [purgePrepared, setPurgePrepared] = useState(false); const [updated, setUpdated] = useState(false); const selected = ROUTES.find((route) => route.path === selectedPath) ?? ROUTES[0]; const cacheable = useMemo(() => selected.policy !== "no-store" && selected.policy !== "private-session", [selected.policy]); return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Globe2} eyebrow="Infrastructure · Edge Configuration" title="Make cache behavior deliberate at the edge." description="Review origin routing, cache policy, safe defaults, and purge requirements. This page does not connect to a CDN provider, report live edge health, purge a cache, measure latency, or claim availability." badge="Preview CDN control plane"><div className="flex flex-wrap gap-2"><Button onClick={() => setUpdated(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Settings2 className="mr-2 size-4" />Prepare local config</Button><Button onClick={() => setPurgePrepared(true)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><Trash2 className="mr-2 size-4" />Preview purge plan</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Routes", value: String(ROUTES.length), hint: "Reference policies", icon: Route }, { label: "Cache status", value: "Unconnected", hint: "No provider telemetry", icon: WifiOff, tone: "amber" }, { label: "Origin", value: "Preview", hint: "Not health checked", icon: Server, tone: "violet" }, { label: "Purge", value: "Unexecuted", hint: "Authorization required", icon: Trash2, tone: "slate" }]} /><ScreenPreviewBanner title="CDN evidence boundary">A configuration preview is not a deployed edge network. Live locations, cache-hit ratio, response time, uptime, bandwidth, origin health, purge completion, TLS posture, and incident state require a connected provider, authenticated telemetry, agreed metric definitions, and an auditable change path.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.9fr_1.1fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Route policies</p><h2 className="mt-1 text-xl font-bold">Select a path</h2></div><Route className="size-5 text-cyan-300" /></div><div className="mt-5 space-y-2">{ROUTES.map((route) => <button key={route.path} onClick={() => { setSelectedPath(route.path); setUpdated(false); setPurgePrepared(false); }} className={`w-full rounded-xl border p-4 text-left transition ${selectedPath === route.path ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-black/15 hover:bg-white/[0.05]"}`}><div className="flex items-center gap-3"><span className="font-mono text-sm text-cyan-200">{route.path}</span><Badge variant="outline" className="ml-auto border-white/10 text-slate-400">{route.state}</Badge></div><div className="mt-2 flex items-center gap-3 text-xs text-slate-500"><span>{route.policy}</span><ArrowRight className="size-3" /><span>{route.ttl}</span></div></button>)}</div><div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex items-center gap-2 text-sm font-medium text-amber-200"><AlertTriangle className="size-4" />Private and API paths default to no-store</div><p className="mt-2 text-xs leading-5 text-slate-400">Do not cache personalized data, tokens, private responses, or mutable API output without an explicit security review.</p></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Configuration preview</p><h2 className="mt-2 text-2xl font-black">{selected.path}</h2><p className="mt-2 text-sm text-slate-400">Policy: {selected.policy} · {cacheable ? "Cache candidate" : "No-store safety boundary"}</p></div><Badge variant="outline" className="border-cyan-300/25 text-cyan-200">Local only</Badge></div><div className="mt-6 space-y-4"><div><label className="mb-2 block text-sm text-slate-300" htmlFor="origin">Origin hostname</label><Input id="origin" value={origin} onChange={(event) => { setOrigin(event.target.value); setUpdated(false); }} className="border-white/10 bg-black/20 text-white" /></div><div><label className="mb-2 block text-sm text-slate-300" htmlFor="ttl">Preview TTL seconds</label><Input id="ttl" value={ttl} onChange={(event) => { setTtl(event.target.value); setUpdated(false); }} inputMode="numeric" className="border-white/10 bg-black/20 text-white" /></div><div className="grid gap-3 sm:grid-cols-3"><Detail label="Origin" value={origin} icon={Server} /><Detail label="Policy" value={selected.policy} icon={ShieldCheck} /><Detail label="TTL" value={`${ttl}s preview`} icon={Timer} /></div></div>{updated && <div className="mt-6 rounded-xl border border-cyan-300/20 bg-cyan-300/[0.06] p-4"><div className="flex items-center gap-2 font-medium text-cyan-200"><Check className="size-4" />Local configuration prepared</div><p className="mt-2 text-sm leading-6 text-slate-300">No CDN provider was called, no origin was changed, and no edge deployment occurred.</p></div>}{purgePrepared && <div className="mt-6 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex items-center gap-2 font-medium text-amber-200"><Trash2 className="size-4" />Purge plan requires authorization</div><p className="mt-2 text-sm leading-6 text-slate-300">No cache key was invalidated. A production purge needs scope, operator authorization, propagation evidence, and a rollback or warm-cache plan.</p></div>}</CardContent></Card></section><section className="grid gap-4 md:grid-cols-2 xl:grid-cols-5">{GATES.map((gate) => <Card key={gate} className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><FileCheck2 className="size-5 text-cyan-300" /><p className="mt-3 text-sm font-semibold">{gate}</p><p className="mt-2 text-xs text-slate-500">Required before deployment</p></CardContent></Card>)}</section><section className="grid gap-4 md:grid-cols-3"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><HardDrive className="size-5 text-cyan-300" /><h3 className="mt-3 font-semibold">Cache correctness</h3><p className="mt-2 text-sm leading-6 text-slate-400">Vary, authorization, cookies, revalidation, and invalidation determine whether a response is safe to reuse.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><Activity className="size-5 text-violet-300" /><h3 className="mt-3 font-semibold">Telemetry has definitions</h3><p className="mt-2 text-sm leading-6 text-slate-400">Hit ratio, latency, availability, and errors need consistent windows, sources, and alert thresholds.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><LockKeyhole className="size-5 text-amber-300" /><h3 className="mt-3 font-semibold">Purge is destructive</h3><p className="mt-2 text-sm leading-6 text-slate-400">Invalidate only the intended keys, with authorization, audit history, and a clear propagation state.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Policy before performance", description: "A fast unsafe cache is a correctness and privacy incident, not a performance win.", icon: ShieldCheck, status: "Guardrail" }, { title: "Origin truth matters", description: "Edge configuration cannot prove that an origin is healthy, deployable, or correctly secured.", icon: Server, status: "Required" }, { title: "No fake edge metrics", description: "Without provider telemetry, active users, cache hit, latency, bandwidth, and uptime remain unavailable.", icon: WifiOff, status: "Required" }]} /></main></div>; }
function Detail({ label, value, icon: Icon }: { label: string; value: string; icon: typeof Server }) { return <div className="rounded-lg border border-white/10 bg-black/15 p-4"><Icon className="size-4 text-slate-400" /><p className="mt-3 text-xs uppercase tracking-wider text-slate-500">{label}</p><p className="mt-1 truncate text-sm text-slate-200">{value}</p></div>; }
