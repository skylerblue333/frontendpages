import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Activity, BarChart3, Check, Download, Filter, LockKeyhole, RefreshCw, Search, ShieldAlert, Target, Users, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const reports = [
  { id: 1, name: "Active users", category: "Product", detail: "A local metric definition awaiting an authenticated event source and denominator policy." },
  { id: 2, name: "Session funnel", category: "Growth", detail: "A funnel concept awaiting event names, identity stitching, and conversion semantics." },
  { id: 3, name: "Learning completion", category: "Education", detail: "A cohort concept awaiting learner consent, course events, and completion rules." },
  { id: 4, name: "Stream engagement", category: "Media", detail: "An engagement concept that must not imply viewers, watch time, or audience behavior." },
];

export default function RealTimeAnalytics() {
  const [query, setQuery] = useState("");
  const [category, setCategory] = useState("All");
  const [selected, setSelected] = useState(1);
  const [range, setRange] = useState("Window not set");
  const [comparison, setComparison] = useState("Comparison not configured");
  const [saved, setSaved] = useState(false);
  const [showGates, setShowGates] = useState(false);
  const categories = ["All", ...Array.from(new Set(reports.map((item) => item.category)))];
  const filtered = useMemo(() => reports.filter((item) => (category === "All" || item.category === category) && `${item.name} ${item.category} ${item.detail}`.toLowerCase().includes(query.toLowerCase())), [category, query]);
  const report = reports.find((item) => item.id === selected) ?? reports[0];
  const reset = () => { setQuery(""); setCategory("All"); setSelected(1); setRange("Window not set"); setComparison("Comparison not configured"); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={BarChart3} eyebrow="RealTimeAnalytics · Metrics preview" title="Define the event before declaring the trend." description="Explore local analytics definitions with search, categories, reporting window, comparison, funnels, cohorts, segments, exports, alerts, and evidence gates. No live users, events, counts, percentages, conversion, ranking, or business conclusion is connected." badge="Analytics workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "View saved locally" : "Save view locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review analytics gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset view</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Definitions", value: `${reports.length} local`, hint: "No events loaded", icon: BarChart3, tone: "cyan" }, { label: "Users", value: "Unavailable", hint: "No identity source", icon: Users, tone: "violet" }, { label: "Events", value: "Unconnected", hint: "No telemetry source", icon: Activity, tone: "amber" }, { label: "Conversion", value: "Unmeasured", hint: "No denominator", icon: Target, tone: "slate" }]} /><ScreenPreviewBanner title="Analytics evidence boundary"><strong>This is a local analytics-definition preview, not a live reporting system.</strong> Definitions, filters, time windows, comparisons, empty chart state, cohort and funnel intent, saved state, export posture, and evidence gates are browser concepts. No user, event, count, percentage, trend, conversion, ranking, attribution, forecast, or business conclusion is asserted.</ScreenPreviewBanner><section className="flex flex-wrap items-end gap-3"><label className="text-sm text-slate-400">Reporting window<select value={range} onChange={(event) => setRange(event.target.value)} className="mt-2 rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Window not set</option><option>Last 15 minutes intent</option><option>Last hour intent</option><option>Last 24 hours intent</option></select></label><label className="text-sm text-slate-400">Comparison<select value={comparison} onChange={(event) => setComparison(event.target.value)} className="mt-2 rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Comparison not configured</option><option>Previous window intent</option><option>Year-over-year intent</option><option>Segment comparison intent</option></select></label><Badge variant="outline" className="border-amber-300/20 text-amber-200">Telemetry disconnected</Badge></section><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Search className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local metric definitions" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-4 flex flex-wrap gap-2">{categories.map((entry) => <Button key={entry} onClick={() => setCategory(entry)} size="sm" variant="outline" className={category === entry ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-100" : "border-white/10 text-slate-400"}><Filter className="mr-1 size-3" />{entry}</Button>)}</div><div className="mt-6 space-y-3">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{item.name}</p><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Unmeasured</Badge></div><div className="mt-4"><Badge variant="outline" className="border-white/10 text-slate-500">{item.category}</Badge></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected definition</p><h2 className="mt-2 text-2xl font-black">{report.name}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{report.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Category", value: report.category }, { label: "Window", value: range }, { label: "Comparison", value: comparison }, { label: "Users", value: "Unavailable" }, { label: "Events", value: "Unconnected" }, { label: "Value", value: "Unavailable" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-8 text-center"><BarChart3 className="mx-auto size-7 text-slate-600" /><p className="mt-3 font-semibold">No analytics events loaded</p><p className="mt-2 text-sm text-slate-500">Connect a governed event source before displaying counts, trends, funnels, cohorts, conversion, or recommendations.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400"><Download className="mr-2 size-4" />Export unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Refresh unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Create alert unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No analytics claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A metric definition does not establish activity, growth, conversion, retention, engagement, performance, attribution, or business value.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Analytics gates</p><h2 className="mt-2 text-2xl font-black">What a real analytics system must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Authenticated event schema, identity stitching, consent, privacy, retention, deletion, and provenance", "Metric definitions, units, denominators, time zones, late events, deduplication, and backfills", "Funnels, cohorts, segments, filters, joins, attribution, sampling, missing data, and confidence", "Aggregation, percentiles, comparisons, baselines, alerts, anomaly logic, and reproducible queries", "Exports, access controls, redaction, audit, legal requests, cost controls, and incident response", "Product decisions, experiments, recommendations, forecasts, and business conclusions require review"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Analytics surface preserved", description: "Metric definitions, search, categories, windows, comparisons, funnels, cohorts, segments, exports, alerts, save/reset, and gates remain interactive.", icon: BarChart3, status: "Local definitions" }, { title: "No analytics theater", description: "Users, events, counts, percentages, trends, conversion, attribution, rankings, forecasts, and business outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Event provenance before insight", description: "Real analytics needs governed events, identity, privacy, reproducible definitions, query review, and export controls.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
