import { useMemo, useState } from "react";
import { Check, Clock3, FileKey2, Filter, Globe2, LockKeyhole, RefreshCw, ShieldAlert, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const certificates = [{ id: 1, name: "Primary domain certificate", category: "Domain", detail: "A local certificate concept requiring domain ownership, issuer, SANs, chain, key boundary, validity, renewal, and deployment evidence.", state: "Unconfigured" }, { id: 2, name: "API endpoint certificate", category: "API", detail: "An API TLS concept requiring endpoint inventory, certificate chain, client policy, rotation, monitoring, and incident response.", state: "No evidence" }, { id: 3, name: "Webhook certificate", category: "Integration", detail: "An integration certificate concept requiring endpoint provenance, mutual TLS intent, secret rotation, signature policy, and replay protection.", state: "Blocked" }, { id: 4, name: "Internal service certificate", category: "Service", detail: "An internal-service concept requiring service identity, trust store, workload boundaries, renewal automation, access policy, and audit.", state: "Needs review" }];
export default function SSLCertificates() { const [query, setQuery] = useState(""); const [category, setCategory] = useState("All"); const [selected, setSelected] = useState(1); const [issuer, setIssuer] = useState("Issuer not configured"); const [renewal, setRenewal] = useState("Renewal not configured"); const [saved, setSaved] = useState(false); const [showGates, setShowGates] = useState(false); const categories = ["All", ...Array.from(new Set(certificates.map((item) => item.category)))]; const filtered = useMemo(() => certificates.filter((item) => (category === "All" || item.category === category) && `${item.name} ${item.category} ${item.detail}`.toLowerCase().includes(query.toLowerCase())), [category, query]); const certificate = certificates.find((item) => item.id === selected) ?? certificates[0]; const reset = () => { setQuery(""); setCategory("All"); setSelected(1); setIssuer("Issuer not configured"); setRenewal("Renewal not configured"); setSaved(false); setShowGates(false); }; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={FileKey2} eyebrow="SSL certificates · TLS posture preview" title="Inspect the trust chain before calling a connection secure." description="Explore local domain, API, integration, and internal-service certificate concepts with search, category filters, issuer and renewal intent, validity and TLS gates, save/reset, and blocked inspection/renewal actions. No certificate, domain, issuer, key, chain, endpoint, TLS posture, security certification, or uptime state is connected." badge="Transport-security workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save certificate locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review TLS gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset certificate</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Certificates", value: `${certificates.length} local`, hint: "No certificate source", icon: FileKey2, tone: "cyan" }, { label: "Domains", value: "Unconfigured", hint: "No domain source", icon: Globe2, tone: "violet" }, { label: "Renewal", value: "Unconfigured", hint: "No automation source", icon: Clock3, tone: "amber" }, { label: "TLS", value: "Unverified", hint: "No endpoint source", icon: ShieldAlert, tone: "slate" }]} /><ScreenPreviewBanner title="Certificate evidence boundary"><strong>This is a local TLS-certificate preview, not evidence that a domain, endpoint, certificate, key, issuer, trust chain, security posture, or uptime exists.</strong> Certificate cards, filters, issuer and renewal intent, saved state, TLS gates, and disabled inspection/renewal actions are browser concepts. No certificate validity, domain ownership, private key, TLS version, cipher, chain, revocation, security certification, or availability outcome is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Filter className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local certificates" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-4 flex flex-wrap gap-2">{categories.map((entry) => <Button key={entry} onClick={() => setCategory(entry)} size="sm" variant="outline" className={category === entry ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-100" : "border-white/10 text-slate-400"}>{entry}</Button>)}</div><div className="mt-6 space-y-3">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{item.name}</p><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.state}</Badge></div><div className="mt-4"><Badge variant="outline" className="border-white/10 text-slate-500">{item.category}</Badge></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected certificate concept</p><h2 className="mt-2 text-2xl font-black">{certificate.name}</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{certificate.state}</Badge></div><p className="mt-2 text-sm leading-6 text-slate-400">{certificate.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Category", value: certificate.category }, { label: "Issuer", value: issuer }, { label: "Renewal", value: renewal }, { label: "Domain", value: "Unconfigured" }, { label: "Validity", value: "Unavailable" }, { label: "TLS", value: "Unverified" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2"><label className="text-sm text-slate-400">Issuer intent<select value={issuer} onChange={(event) => setIssuer(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Issuer not configured</option><option>Public CA intent</option><option>Private CA intent</option><option>Managed certificate intent</option><option>Independent-review intent</option></select></label><label className="text-sm text-slate-400">Renewal intent<select value={renewal} onChange={(event) => setRenewal(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Renewal not configured</option><option>Manual renewal intent</option><option>Automated renewal intent</option><option>Rotation-required intent</option><option>Expiry-alert intent</option></select></label></div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-8 text-center"><FileKey2 className="mx-auto size-8 text-slate-600" /><p className="mt-3 font-semibold">No TLS evidence loaded</p><p className="mt-2 text-sm text-slate-500">Connect governed domain inventory, certificate chain, issuer, private-key boundary, TLS configuration, validity, revocation, renewal automation, endpoint tests, monitoring, and audit before making a security claim.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Inspect unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Renew unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Rotate unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Export unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No certificate or security claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A certificate concept does not prove domain ownership, validity, private-key control, TLS posture, trust chain, revocation status, security certification, or uptime.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">TLS lifecycle gates</p><h2 className="mt-2 text-2xl font-black">What a real certificate program must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Authenticated domain and endpoint inventory, ownership, SANs, issuer, chain, key algorithm, deployment, timestamp, and source", "Private-key protection, HSM/secret boundary, least privilege, rotation, renewal, revocation, compromise response, and audit", "TLS versions, ciphers, certificate validation, hostname checks, mTLS, client compatibility, browser behavior, and monitoring", "Expiry alerts, automated renewal, rollback, incident response, support, uptime, recovery, and environment separation", "Security, compliance, privacy, financial, wallet, AI, education, and user-impact claims require domain review", "Inspect, renew, rotate, disconnect, test, export, notify, and accountable approval require governed operations"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Certificate surface preserved", description: "Domain, API, integration, service certificates, filters, issuers, validity, renewal, TLS, inspection, rotation, export, save/reset, and gates remain interactive.", icon: FileKey2, status: "Local certificates" }, { title: "No TLS theater", description: "Certificates, domains, keys, issuers, chains, TLS posture, revocation, security certification, and uptime are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before secure", description: "Real certificate management requires authenticated inventory, key protection, chain validation, renewal, monitoring, revocation, and incident response.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>; }
