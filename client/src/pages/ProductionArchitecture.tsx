import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Activity, Check, Cloud, Database, GitBranch, LockKeyhole, Network, RefreshCw, Server, ShieldAlert, SlidersHorizontal, Zap } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const surfaces = [{ title: "Request path", icon: Network, items: ["Client", "Gateway", "Auth boundary", "Service", "Response"] }, { title: "Data layers", icon: Database, items: ["Primary store", "Cache intent", "Object storage", "Event log"] }, { title: "AI scaling", icon: Zap, items: ["Request", "Policy gate", "Model router", "Execution", "Response"] }, { title: "Delivery path", icon: GitBranch, items: ["Change", "Checks", "Artifact", "Deploy", "Rollback"] }];
const gates = ["Environment and secret inventory", "Authentication, authorization, and tenant isolation", "Database migrations, backups, restore drills, and recovery objectives", "Rate limits, queues, retries, idempotency, and overload behavior", "Metrics, logs, traces, alerts, dashboards, and incident ownership", "Security, privacy, dependency, and change-management evidence"];
export default function ProductionArchitecture() {
  const [selected, setSelected] = useState(surfaces[0].title);
  const [saved, setSaved] = useState(false);
  const [showGates, setShowGates] = useState(false);
  const reset = () => { setSelected(surfaces[0].title); setSaved(false); setShowGates(false); };
  const current = surfaces.find((item) => item.title === selected) ?? surfaces[0];
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Cloud} eyebrow="ProductionArchitecture · Readiness preview" title="Design the production path before promising production behavior." description="Explore local topology concepts for request flow, data layers, AI scaling, delivery, observability, and resilience. No uptime, latency, capacity, deployment, security certification, failover, cost, or availability claim is measured or verified." badge="Architecture workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Review saved locally" : "Save review locally"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset architecture</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Topology", value: String(surfaces.length), hint: "Local design paths", icon: Network, tone: "cyan" }, { label: "Uptime", value: "Unmeasured", hint: "No telemetry", icon: Activity, tone: "violet" }, { label: "Capacity", value: "Unverified", hint: "No load test", icon: Server, tone: "amber" }, { label: "Deployment", value: "Unconfirmed", hint: "No release proof", icon: LockKeyhole, tone: "slate" }]} /><ScreenPreviewBanner title="Production architecture evidence boundary"><strong>This is an architecture design worksheet, not a production attestation.</strong> Topology, service names, scaling rules, resilience patterns, pipeline stages, and monitoring concepts are local designs. No uptime, latency, capacity, security control, compliance status, deployment, incident response, failover, or cost outcome is claimed as live.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.8fr_1.2fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Architecture paths</p><h2 className="mt-2 text-2xl font-black">Choose a design surface</h2></div><Badge variant="outline" className="border-white/10 text-amber-200">Local</Badge></div><div className="mt-5 space-y-3">{surfaces.map((item) => { const Icon = item.icon; return <button key={item.title} onClick={() => setSelected(item.title)} className={`flex w-full items-center gap-3 rounded-xl border p-4 text-left ${selected === item.title ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><Icon className="size-5 text-cyan-200" /><div className="flex-1"><p className="font-semibold">{item.title}</p><p className="mt-1 text-sm text-slate-500">{item.items.join(" → ")}</p></div></button>; })}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected path</p><h2 className="mt-2 text-2xl font-black">{current.title}</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Design only</Badge></div><div className="mt-6 flex flex-wrap items-center gap-2">{current.items.map((item, index) => <div key={item} className="flex items-center gap-2"><span className="rounded-xl border border-cyan-300/20 bg-cyan-300/[0.06] px-4 py-3 text-sm text-cyan-100">{item}</span>{index < current.items.length - 1 && <span className="text-slate-600">→</span>}</div>)}</div><div className="mt-6 grid gap-3 sm:grid-cols-3">{[{ label: "Status", value: "Design concept" }, { label: "Telemetry", value: "Not connected" }, { label: "Owner", value: "Unassigned" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-6 rounded-2xl border border-amber-300/25 bg-amber-300/[0.06] p-5"><div className="flex items-start gap-3"><ShieldAlert className="mt-0.5 size-5 shrink-0 text-amber-200" /><div><p className="font-semibold text-amber-100">Operational proof is unavailable</p><p className="mt-2 text-sm leading-6 text-slate-400">The browser cannot verify live infrastructure, network topology, autoscaling, queue depth, model routing, database health, deploy state, security controls, or failover behavior from this design surface.</p></div></div></div></CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Readiness gates</p><h2 className="mt-2 text-2xl font-black">Evidence required before production claims</h2></div><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/10 text-slate-300"><SlidersHorizontal className="mr-2 size-4" />{showGates ? "Hide gates" : "Review gates"}</Button></div>{showGates && <div className="mt-5 grid gap-3 sm:grid-cols-2">{gates.map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><Check className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div>}<div className="mt-5 grid gap-3 sm:grid-cols-2">{["Autoscaling rules are design intent until load tests and control-plane telemetry exist.", "CI/CD stages are not evidence that a deployment occurred or can be rolled back.", "Observability tools are not evidence of dashboards, alerts, retention, or response ownership.", "Failover patterns are not evidence of tested recovery, RTO/RPO, or customer continuity."].map((item) => <div key={item} className="rounded-xl border border-white/10 p-4 text-sm leading-6 text-slate-400">{item}</div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Architecture surface preserved", description: "Request, data, AI scaling, delivery, observability, autoscaling, and resilience design concepts remain selectable and reviewable.", icon: Network, status: "Local design" }, { title: "No infrastructure theater", description: "Uptime, latency, capacity, security, compliance, deployments, costs, failover, and availability are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Proof before production", description: "Real claims require telemetry, load tests, release evidence, security review, backups, recovery drills, and accountable owners.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
