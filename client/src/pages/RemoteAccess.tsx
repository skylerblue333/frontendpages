import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Check, Computer, Filter, KeyRound, LockKeyhole, MonitorUp, RefreshCw, Search, ShieldAlert, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const sessions = [{ id: 1, name: "Development workstation", category: "Workstation", detail: "A local remote-session concept awaiting host identity, user consent, authentication, network policy, and audit.", state: "Preview only" }, { id: 2, name: "Support device review", category: "Support", detail: "A support-access concept requiring explicit consent, limited scope, session recording, and revocation.", state: "Needs evidence" }, { id: 3, name: "Lab environment", category: "Infrastructure", detail: "An infrastructure-access concept requiring inventory, credentials, network controls, monitoring, and recovery.", state: "Unconnected" }, { id: 4, name: "Personal device", category: "Personal", detail: "A device-access concept that must not imply ownership, location, availability, or permission.", state: "Blocked" }];
export default function RemoteAccess() {
  const [query, setQuery] = useState(""); const [category, setCategory] = useState("All"); const [selected, setSelected] = useState(1); const [auth, setAuth] = useState("Authentication not configured"); const [scope, setScope] = useState("Scope not configured"); const [saved, setSaved] = useState(false); const [showGates, setShowGates] = useState(false);
  const categories = ["All", ...Array.from(new Set(sessions.map((item) => item.category)))]; const filtered = useMemo(() => sessions.filter((item) => (category === "All" || item.category === category) && `${item.name} ${item.category} ${item.detail}`.toLowerCase().includes(query.toLowerCase())), [category, query]); const session = sessions.find((item) => item.id === selected) ?? sessions[0];
  const reset = () => { setQuery(""); setCategory("All"); setSelected(1); setAuth("Authentication not configured"); setScope("Scope not configured"); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={MonitorUp} eyebrow="RemoteAccess · Session architecture preview" title="Prove the permission before opening a session." description="Explore local remote-session concepts with search, categories, host and device posture, authentication, permissions, control, file transfer, monitoring, audit, recovery, save, reset, and evidence gates. No live session, device, credential, network path, user consent, or access outcome is connected." badge="Access workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "View saved locally" : "Save session locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review access gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset access</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Concepts", value: `${sessions.length} local`, hint: "No sessions loaded", icon: MonitorUp, tone: "cyan" }, { label: "Hosts", value: "Unavailable", hint: "No inventory source", icon: Computer, tone: "violet" }, { label: "Authentication", value: "Unconfigured", hint: "No credential source", icon: KeyRound, tone: "amber" }, { label: "Permissions", value: "Review needed", hint: "No consent source", icon: LockKeyhole, tone: "slate" }]} /><ScreenPreviewBanner title="Remote-access evidence boundary"><strong>This is a local access-architecture preview, not evidence that a remote session can be opened or that access is authorized.</strong> Session cards, host/device posture, authentication, scope, control, file transfer, monitoring, audit, recovery, saved state, and evidence gates are browser concepts. No host, device, credential, network, user, consent, access, or security outcome is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Search className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local remote-session concepts" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-4 flex flex-wrap gap-2">{categories.map((entry) => <Button key={entry} onClick={() => setCategory(entry)} size="sm" variant="outline" className={category === entry ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-100" : "border-white/10 text-slate-400"}><Filter className="mr-1 size-3" />{entry}</Button>)}</div><div className="mt-6 space-y-3">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{item.name}</p><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.state}</Badge></div><div className="mt-4"><Badge variant="outline" className="border-white/10 text-slate-500">{item.category}</Badge></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected access concept</p><h2 className="mt-2 text-2xl font-black">{session.name}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{session.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Category", value: session.category }, { label: "State", value: session.state }, { label: "Host", value: "Unavailable" }, { label: "Device", value: "Unavailable" }, { label: "Authentication", value: auth }, { label: "Permission", value: "Review needed" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2"><label className="text-sm text-slate-400">Authentication posture<select value={auth} onChange={(event) => setAuth(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Authentication not configured</option><option>SSO intent</option><option>Certificate intent</option><option>Key-based intent</option></select></label><label className="text-sm text-slate-400">Session scope<select value={scope} onChange={(event) => setScope(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Scope not configured</option><option>View-only intent</option><option>Support intent</option><option>Admin intent</option></select></label></div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-8 text-center"><MonitorUp className="mx-auto size-8 text-slate-600" /><p className="mt-3 font-semibold">No remote session loaded</p><p className="mt-2 text-sm text-slate-500">Connect an approved host inventory, identity provider, consent record, network policy, and session broker before enabling control.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Connect unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Control unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Transfer unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Disconnect unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No access or security claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local session concept does not prove a host exists, a user consented, credentials are valid, access is authorized, or a device is secure.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Access gates</p><h2 className="mt-2 text-2xl font-black">What a real remote-access workflow must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Host inventory, ownership, user identity, consent, purpose, scope, session expiry, revocation, and tenant isolation", "Authentication, authorization, least privilege, MFA, key handling, credential rotation, and secret redaction", "Network path, broker, firewall, device posture, endpoint security, encryption, recording, and protocol status", "Control, clipboard, file transfer, screen capture, process actions, destructive operations, and approvals", "Audit, monitoring, anomaly detection, incident response, support, privacy, retention, and legal boundaries", "Recovery, disconnect, rollback, session failure, lockout, offline behavior, accessibility, and user-visible status"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Access surface preserved", description: "Sessions, search, categories, hosts, devices, authentication, scope, control, transfer, monitoring, audit, recovery, save/reset, and gates remain interactive.", icon: MonitorUp, status: "Local concepts" }, { title: "No access theater", description: "Hosts, devices, credentials, network paths, users, consent, authorization, access, and security outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Consent before control", description: "Real remote access needs identity, consent, least privilege, secure transport, audit, revocation, monitoring, and recovery.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
