import { useMemo, useState } from "react";
import { Activity, Building2, Check, CircleUserRound, Clock3, FileCheck2, Filter, Mail, MessageSquare, Network, NotebookPen, Phone, Plus, Search, ShieldAlert, Tag, Target, UserRound, WifiOff } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

type Contact = { id: string; name: string; organization: string; stage: string; tags: string[]; lastTouch: string; note: string };
const CONTACTS: Contact[] = [{ id: "contact-preview-01", name: "Synthetic partner A", organization: "Preview organization", stage: "Qualified", tags: ["enterprise", "synthetic"], lastTouch: "Preview record", note: "Needs verified relationship history." }, { id: "contact-preview-02", name: "Synthetic creator B", organization: "Demo studio", stage: "Proposal", tags: ["creator", "synthetic"], lastTouch: "Preview record", note: "No real outreach or proposal exists." }, { id: "contact-preview-03", name: "Synthetic community C", organization: "Example collective", stage: "Lead", tags: ["community", "synthetic"], lastTouch: "Preview record", note: "Qualification has not been performed." }];
const STAGES = ["Lead", "Qualified", "Proposal", "Negotiation", "Closed"];

export default function CRM() { const [contacts, setContacts] = useState(CONTACTS); const [query, setQuery] = useState(""); const [stage, setStage] = useState("All"); const [selectedId, setSelectedId] = useState(CONTACTS[0].id); const [note, setNote] = useState(""); const [saved, setSaved] = useState(false); const visible = useMemo(() => contacts.filter((contact) => { const haystack = `${contact.name} ${contact.organization} ${contact.tags.join(" ")}`.toLowerCase(); return haystack.includes(query.toLowerCase()) && (stage === "All" || contact.stage === stage); }), [contacts, query, stage]); const selected = visible.find((contact) => contact.id === selectedId) ?? visible[0]; const saveNote = () => { if (!selected || !note.trim()) return; setContacts((current) => current.map((contact) => contact.id === selected.id ? { ...contact, note } : contact)); setNote(""); setSaved(true); }; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Network} eyebrow="Relationships · CRM Preview" title="Keep context close without inventing a pipeline." description="Explore synthetic relationship records, stage filters, selected-account context, and local notes. This page does not connect contacts, send outreach, score leads, forecast revenue, create deals, or synchronize a CRM provider." badge="Preview relationship workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(false)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Plus className="mr-2 size-4" />Prepare local contact</Button><Button variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><FileCheck2 className="mr-2 size-4" />Review data boundary</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Visible contacts", value: String(visible.length), hint: "Synthetic preview records", icon: UserRound }, { label: "Pipeline", value: "Unverified", hint: "No opportunity values", icon: Target, tone: "violet" }, { label: "Lead scoring", value: "Unavailable", hint: "No behavioral model", icon: ShieldAlert, tone: "amber" }, { label: "CRM sync", value: "Unconnected", hint: "No contact mutations", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="Relationship-data evidence boundary">The records below are synthetic previews. A real CRM needs consent and lawful processing, contact provenance, deduplication, access controls, retention rules, activity integrity, provider contracts, and clear separation between a relationship note, a qualified opportunity, a forecast, and revenue.</ScreenPreviewBanner><section className="flex flex-wrap gap-2">{["All", ...STAGES].map((item) => <button key={item} onClick={() => setStage(item)} className={`rounded-xl border px-4 py-2 text-sm transition ${stage === item ? "border-cyan-300/40 bg-cyan-300/[0.1] text-cyan-200" : "border-white/10 bg-black/15 text-slate-400 hover:text-white"}`}>{item}</button>)}</section><section className="grid gap-6 lg:grid-cols-[1.05fr_0.95fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="flex flex-col gap-3 sm:flex-row"><div className="relative flex-1"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search synthetic relationships" aria-label="Search synthetic relationships" className="border-white/10 bg-black/20 pl-9 text-white placeholder:text-slate-500" /></div><Button variant="outline" size="icon" className="border-white/15 text-slate-300"><Filter className="size-4" /></Button></div><div className="mt-5 space-y-2">{visible.map((contact) => <button key={contact.id} onClick={() => { setSelectedId(contact.id); setSaved(false); }} className={`w-full rounded-xl border p-4 text-left transition ${selected?.id === contact.id ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-black/15 hover:bg-white/[0.05]"}`}><div className="flex items-center gap-3"><div className="flex size-10 items-center justify-center rounded-xl bg-cyan-300/10"><CircleUserRound className="size-5 text-cyan-300" /></div><div className="flex-1"><p className="font-semibold">{contact.name}</p><p className="mt-1 text-xs text-slate-500">{contact.organization}</p></div><Badge variant="outline" className="border-white/10 text-slate-400">{contact.stage}</Badge></div><div className="mt-3 flex flex-wrap gap-2">{contact.tags.map((tag) => <span key={tag} className="rounded bg-white/5 px-2 py-1 text-[10px] text-slate-500">#{tag}</span>)}</div></button>)}{visible.length === 0 && <p className="py-10 text-center text-sm text-slate-500">No synthetic contacts match this view.</p>}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6">{selected ? <><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Selected relationship</p><h2 className="mt-2 text-2xl font-black">{selected.name}</h2><p className="mt-2 text-sm text-slate-400">{selected.organization}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Synthetic</Badge></div><div className="mt-6 grid gap-3 sm:grid-cols-3"><Detail label="Stage" value={selected.stage} icon={Target} /><Detail label="Last touch" value={selected.lastTouch} icon={Clock3} /><Detail label="Sync" value="Unavailable" icon={WifiOff} /></div><div className="mt-6 rounded-xl border border-white/10 bg-black/15 p-4"><div className="flex items-center gap-2 text-sm font-medium"><NotebookPen className="size-4 text-cyan-300" />Local relationship note</div><p className="mt-3 text-sm leading-6 text-slate-300">{selected.note}</p><Textarea value={note} onChange={(event) => setNote(event.target.value)} placeholder="Add a local note for this preview record" className="mt-4 min-h-24 border-white/10 bg-black/20 text-white placeholder:text-slate-500" /><Button onClick={saveNote} className="mt-3 bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />Save local note</Button></div>{saved && <div className="mt-5 rounded-xl border border-cyan-300/20 bg-cyan-300/[0.06] p-4"><p className="font-semibold text-cyan-200">Local note saved</p><p className="mt-1 text-sm text-slate-400">No contact, deal, outreach, score, or provider record was changed.</p></div>}</> : <p className="text-sm text-slate-500">Select a synthetic record.</p>}</CardContent></Card></section><section className="grid gap-4 md:grid-cols-3"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><Tag className="size-5 text-cyan-300" /><h3 className="mt-3 font-semibold">Stage is context</h3><p className="mt-2 text-sm leading-6 text-slate-400">A selected stage is not a qualified lead, deal value, forecast, or conversion probability.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><Mail className="size-5 text-violet-300" /><h3 className="mt-3 font-semibold">No outreach claim</h3><p className="mt-2 text-sm leading-6 text-slate-400">A contact card does not prove an email, call, meeting, proposal, or response occurred.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><ShieldAlert className="size-5 text-amber-300" /><h3 className="mt-3 font-semibold">Relationship data is sensitive</h3><p className="mt-2 text-sm leading-6 text-slate-400">Consent, provenance, retention, access, deletion, and provider boundaries need real implementation evidence.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Context over vanity metrics", description: "Show what a record means and where it came from rather than inventing pipeline totals or win rates.", icon: Activity, status: "Pattern" }, { title: "Notes need provenance", description: "A real activity timeline needs actor, timestamp, source, edit history, and privacy controls.", icon: MessageSquare, status: "Required" }, { title: "No fake CRM sync", description: "Local previews are not contact creation, deal updates, outreach delivery, or revenue recognition.", icon: WifiOff, status: "Guardrail" }]} /></main></div>; }
function Detail({ label, value, icon: Icon }: { label: string; value: string; icon: typeof Target }) { return <div className="rounded-lg border border-white/10 bg-black/15 p-4"><Icon className="size-4 text-slate-400" /><p className="mt-3 text-xs uppercase tracking-wider text-slate-500">{label}</p><p className="mt-1 text-sm text-slate-200">{value}</p></div>; }
