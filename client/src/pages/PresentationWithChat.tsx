import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Bot, Check, ChevronLeft, ChevronRight, Eye, FileText, LockKeyhole, MessageSquare, MonitorPlay, Pause, Play, RefreshCw, Send, ShieldAlert, Sparkles, Users, WifiOff, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

type Slide = { id: number; title: string; subtitle: string; note: string; boundary: string };
const slides: Slide[] = [
  { id: 1, title: "SKYCOIN4444", subtitle: "Ecosystem overview", note: "Introduce the platform vision and separate current surfaces from future plans.", boundary: "Claims require source links and implementation evidence." },
  { id: 2, title: "Core platform", subtitle: "Accounts, dashboards, and community surfaces", note: "Explain what the product intends to connect without implying every module is production-ready.", boundary: "Feature availability must be verified per route." },
  { id: 3, title: "Responsible growth", subtitle: "Evidence before scale", note: "Show how observability, security, and user trust shape the roadmap.", boundary: "No user, revenue, market, or growth figures are asserted." },
  { id: 4, title: "Roadmap", subtitle: "From preview to proof", note: "Describe integration gates, tests, and operational ownership.", boundary: "Dates and milestones remain draft until approved." },
  { id: 5, title: "Q&A", subtitle: "Open discussion", note: "Capture questions for a later verified response.", boundary: "Chat is a local draft; no audience or moderation service is connected." },
];
export default function PresentationWithChat() {
  const [current, setCurrent] = useState(0);
  const [playing, setPlaying] = useState(false);
  const [chat, setChat] = useState("");
  const [messages, setMessages] = useState<string[]>(["Local draft: add a question for the speaker."]);
  const [saved, setSaved] = useState(false);
  const [showNotes, setShowNotes] = useState(true);
  const slide = slides[current];
  const progress = useMemo(() => Math.round(((current + 1) / slides.length) * 100), [current]);
  const send = () => { if (!chat.trim()) return; setMessages((items) => [...items, `Draft question: ${chat.trim()}`]); setChat(""); };
  const reset = () => { setCurrent(0); setPlaying(false); setChat(""); setMessages(["Local draft: add a question for the speaker."]); setSaved(false); setShowNotes(true); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={MonitorPlay} eyebrow="PresentationWithChat · Studio preview" title="Build the presentation before you claim the audience is watching." description="Navigate a local five-slide presentation draft, inspect speaker notes and evidence boundaries, and capture draft questions. Viewer counts, live chat, AI answers, co-editing, streaming, publishing, and export are not connected." badge="Presentation studio"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Draft saved locally" : "Save draft locally"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset studio</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Slides", value: String(slides.length), hint: "Local draft", icon: FileText, tone: "cyan" }, { label: "Current", value: `${current + 1}/${slides.length}`, hint: `${progress}% reviewed`, icon: MonitorPlay, tone: "violet" }, { label: "Viewers", value: "Unknown", hint: "No presence source", icon: Users, tone: "amber" }, { label: "Chat", value: "Draft", hint: "No live channel", icon: MessageSquare, tone: "slate" }]} /><ScreenPreviewBanner title="Presentation and chat evidence boundary"><strong>This is a local presentation draft, not a live broadcast.</strong> Slide content, notes, navigation, and draft questions are browser state. No viewer count, participant identity, live message, AI response, collaboration event, stream, published URL, export, investment claim, market claim, security certification, or production metric is verified here.</ScreenPreviewBanner><section className="grid gap-6 xl:grid-cols-[1fr_0.38fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Slide {slide.id} of {slides.length}</p><h2 className="mt-2 text-3xl font-black">{slide.title}</h2><p className="mt-2 text-slate-400">{slide.subtitle}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Draft</Badge></div><div className="mt-6 flex min-h-[360px] items-center justify-center rounded-2xl border border-white/10 bg-gradient-to-br from-cyan-300/[0.12] via-violet-300/[0.08] to-black/20 p-10 text-center"><div><Sparkles className="mx-auto size-12 text-cyan-200" /><p className="mt-5 text-4xl font-black tracking-tight">{slide.title}</p><p className="mt-3 text-lg text-slate-300">{slide.subtitle}</p><p className="mx-auto mt-6 max-w-xl text-sm leading-6 text-slate-500">This visual is a local composition concept. It does not prove the claims or capabilities represented by the presentation.</p></div></div><div className="mt-5 flex flex-wrap items-center justify-between gap-3"><div className="flex items-center gap-2"><Button aria-label="Previous slide" onClick={() => setCurrent((value) => Math.max(0, value - 1))} disabled={current === 0} variant="outline" className="border-white/10 text-slate-300"><ChevronLeft className="size-4" /></Button><Button aria-label="Toggle presentation preview" onClick={() => setPlaying((value) => !value)} variant="outline" className="border-white/10 text-slate-300">{playing ? <Pause className="mr-2 size-4" /> : <Play className="mr-2 size-4" />}{playing ? "Pause preview" : "Play preview"}</Button><Button aria-label="Next slide" onClick={() => setCurrent((value) => Math.min(slides.length - 1, value + 1))} disabled={current === slides.length - 1} variant="outline" className="border-white/10 text-slate-300"><ChevronRight className="size-4" /></Button></div><span className="text-xs text-slate-500">Preview playback is local only</span></div><div className="mt-5 flex gap-2 overflow-x-auto pb-1">{slides.map((item, index) => <button key={item.id} onClick={() => setCurrent(index)} className={`min-w-28 rounded-xl border p-3 text-left ${index === current ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><p className="text-xs text-slate-500">Slide {item.id}</p><p className="mt-2 truncate text-sm font-semibold">{item.title}</p></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="flex h-full flex-col p-5"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Draft chat</p><h2 className="mt-2 text-2xl font-black">Questions</h2></div><Badge variant="outline" className="border-white/10 text-amber-200">Offline</Badge></div><div className="mt-5 flex-1 space-y-3">{messages.map((message, index) => <div key={`${message}-${index}`} className="rounded-xl border border-white/10 bg-black/15 p-3 text-sm leading-5 text-slate-300"><div className="mb-2 flex items-center gap-2 text-xs text-slate-500"><MessageSquare className="size-3" />Local note</div>{message}</div>)}</div><div className="mt-5"><div className="flex gap-2"><input value={chat} onChange={(event) => setChat(event.target.value)} onKeyDown={(event) => { if (event.key === "Enter") send(); }} placeholder="Draft a question…" className="min-w-0 flex-1 rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none focus:border-cyan-300/50" /><Button aria-label="Add draft question" onClick={send} size="icon" className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Send className="size-4" /></Button></div><p className="mt-3 text-xs leading-5 text-slate-500">No live audience, moderation, identity, or AI answer source is connected.</p></div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[0.9fr_1.1fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Speaker notes</p><h2 className="mt-2 text-2xl font-black">Evidence-aware narration</h2></div><Button onClick={() => setShowNotes((value) => !value)} variant="outline" className="border-white/10 text-slate-300">{showNotes ? <X className="size-4" /> : <Eye className="size-4" />}</Button></div>{showNotes && <div className="mt-5 space-y-4"><div className="rounded-xl border border-white/10 p-4"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-cyan-300">Speaker note</p><p className="mt-2 text-sm leading-6 text-slate-300">{slide.note}</p></div><div className="rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-amber-200">Boundary for this slide</p><p className="mt-2 text-sm leading-6 text-slate-400">{slide.boundary}</p></div></div>}</CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Studio readiness</p><h2 className="mt-2 text-2xl font-black">What live presentation must prove</h2><div className="mt-5 space-y-3">{["Authenticated presenter, audience identity, participant permissions, and moderation", "Versioned slide content, citations, approval, speaker notes, accessibility, and localization", "Real-time chat transport, ordering, delivery state, abuse controls, and retention", "Streaming, presence, recording, export, publishing, and failure recovery", "Claim review for market, financial, security, compliance, team, roadmap, and performance statements"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldAlert className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Presentation surface preserved", description: "Slides, thumbnails, previous/next navigation, local play/pause, speaker notes, draft chat, save, and reset remain interactive.", icon: MonitorPlay, status: "Local studio" }, { title: "No live audience theater", description: "Viewer count, participants, live messages, AI replies, co-editing, stream, and published presentation are not claimed.", icon: WifiOff, status: "Offline" }, { title: "Claims need evidence", description: "Narration now identifies that platform, market, financial, security, compliance, and roadmap claims need proof before publication.", icon: ShieldAlert, status: "Guardrail" }]} /></main></div>;
}
