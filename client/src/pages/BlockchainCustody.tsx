import { useMemo, useState } from "react";
import { Activity, AlertTriangle, ArrowDownLeft, CheckCircle2, ChevronRight, Copy, Eye, EyeOff, FileKey2, Globe, Hash, LockKeyhole, Network, Plus, RefreshCw, Search, Send, Shield, Wallet, XCircle, Zap } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

type WalletRecord = { id: string; label: string; chain: string; address: string; status: "Address preview" | "Balance unavailable" | "Contract required" };
type TransactionRecord = { id: string; type: string; chain: string; status: "Unsigned draft" | "Network unavailable" | "Confirmation unverified"; description: string };
const CHAINS = ["Ethereum", "Polygon", "BNB Smart Chain", "Base"];
const WALLETS: WalletRecord[] = [
  { id: "wallet-eth", label: "Ethereum public address", chain: "Ethereum", address: "0x1234…9abc", status: "Balance unavailable" },
  { id: "wallet-poly", label: "Polygon public address", chain: "Polygon", address: "0x5678…def0", status: "Address preview" },
  { id: "wallet-base", label: "Base public address", chain: "Base", address: "0x2468…1357", status: "Contract required" },
];
const TXS: TransactionRecord[] = [
  { id: "tx-1", type: "Native transfer", chain: "Ethereum", status: "Unsigned draft", description: "A transaction shape requiring validated addresses, amount, nonce, gas, signer, and explicit confirmation." },
  { id: "tx-2", type: "Token transfer", chain: "Polygon", status: "Network unavailable", description: "A token transaction requiring contract verification, decimals, allowance, simulation, and finality monitoring." },
  { id: "tx-3", type: "Receive record", chain: "Base", status: "Confirmation unverified", description: "A history shape requiring canonical receipt data and confirmation policy before status is shown." },
];
const SECURITY = ["No seed phrase in the client", "Hardware or managed signer", "Network and chain-ID validation", "Checksum address validation", "Simulation before signing", "Idempotent broadcast and reconciliation"];

export default function BlockchainCustody() { const [activeView, setActiveView] = useState("wallets"); const [query, setQuery] = useState(""); const [wallets, setWallets] = useState(WALLETS); const [selectedWallet, setSelectedWallet] = useState<WalletRecord | null>(null); const [selectedTx, setSelectedTx] = useState<TransactionRecord | null>(null); const [showAddress, setShowAddress] = useState(false); const [prepared, setPrepared] = useState(false); const visibleWallets = useMemo(() => wallets.filter((wallet) => `${wallet.label} ${wallet.chain} ${wallet.status}`.toLowerCase().includes(query.toLowerCase())), [wallets, query]); const registerLocal = () => { const draft = { id: `draft-${wallets.length + 1}`, label: `Local wallet plan ${wallets.length + 1}`, chain: "Choose network", address: "Not derived", status: "Contract required" as const }; setWallets((current) => [draft, ...current]); setSelectedWallet(draft); setPrepared(true); }; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Wallet} eyebrow="Crypto · Custody" title="Keep keys out of the UI, and claims out of the dark." description="Explore non-custodial wallet architecture, public-address previews, transaction lifecycle requirements, and multi-chain safeguards. No seed phrase, private key, balance, owner, signature, or broadcast is available in this preview." badge="High-safety custody preview"><div className="flex flex-wrap gap-2"><Button onClick={registerLocal} className="bg-amber-300 text-slate-950 hover:bg-amber-200"><Plus className="mr-2 size-4" />Add local wallet plan</Button><Button variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><Shield className="mr-2 size-4" />Security model</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Wallet plans", value: String(wallets.length), hint: "Public-address previews", icon: Wallet }, { label: "Live balances", value: "Unavailable", hint: "No RPC or indexer connected", icon: Activity, tone: "amber" }, { label: "Signing", value: "Blocked", hint: "No signer or private-key path", icon: LockKeyhole, tone: "violet" }, { label: "Broadcast", value: "Blocked", hint: "No transaction side effects", icon: Send, tone: "slate" }]} /><ScreenPreviewBanner title="Custody evidence boundary">Wallet architecture, chain selection, public-address examples, local registration plans, transaction lifecycle detail, and unavailable chain state are available for UX review. No seed phrase, private key, balance, owner identity, signature, transaction hash, broadcast, or confirmation is fabricated.</ScreenPreviewBanner><section><div className="mb-5 flex flex-wrap gap-2 rounded-xl border border-white/10 bg-white/[0.04] p-2">{[["wallets", "Wallets"], ["history", "Transaction history"], ["register", "Register plan"]].map(([value, label]) => <Button key={value} size="sm" variant={activeView === value ? "default" : "outline"} onClick={() => setActiveView(value)} className={activeView === value ? "bg-amber-300 text-slate-950 hover:bg-amber-200" : "border-white/15 bg-white/5 text-slate-300 hover:bg-white/10"}>{label}</Button>)}</div>{activeView === "wallets" && <div className="grid gap-6 lg:grid-cols-[0.85fr_1.15fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="relative"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search wallet plans" aria-label="Search wallet plans" className="border-white/10 bg-black/20 pl-9 text-white placeholder:text-slate-500" /></div><div className="mt-4 space-y-2">{visibleWallets.map((wallet) => <button key={wallet.id} onClick={() => { setSelectedWallet(wallet); setPrepared(false); }} className={`w-full rounded-xl border p-4 text-left transition ${selectedWallet?.id === wallet.id ? "border-amber-300/40 bg-amber-300/[0.08]" : "border-white/10 bg-black/15 hover:bg-white/[0.05]"}`}><div className="flex items-center gap-2"><Wallet className="size-4 text-amber-300" /><span className="font-semibold">{wallet.label}</span><Badge variant="outline" className="ml-auto border-amber-300/20 text-amber-200">{wallet.status}</Badge></div><p className="mt-2 text-xs uppercase tracking-wider text-slate-500">{wallet.chain}</p><p className="mt-2 font-mono text-sm text-slate-400">{wallet.address}</p></button>)}{visibleWallets.length === 0 && <ScreenStatePanel type="empty" title="No wallet plans match" description="Try another chain, status, or keyword." />}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6">{selectedWallet ? <><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-300">Selected public-address plan</p><h2 className="mt-1 text-2xl font-bold">{selectedWallet.label}</h2><p className="mt-2 text-sm text-slate-400">{selectedWallet.chain}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{selectedWallet.status}</Badge></div><div className="mt-5 flex items-center gap-3 rounded-xl border border-white/10 bg-black/15 p-4"><Hash className="size-4 text-slate-500" /><span className="flex-1 font-mono text-sm text-slate-300">{showAddress ? "Synthetic address — not derived" : "Address hidden in preview"}</span><Button size="sm" variant="ghost" onClick={() => setShowAddress((value) => !value)} className="text-slate-400">{showAddress ? <EyeOff className="size-4" /> : <Eye className="size-4" />}</Button></div><div className="mt-6 grid gap-3 sm:grid-cols-2">{["Network and chain ID", "Derivation or signer policy", "Public-address validation", "Balance source and freshness", "Owner authorization", "Recovery and revocation"].map((item) => <div key={item} className="rounded-lg border border-white/10 bg-black/15 p-4"><CheckCircle2 className="size-4 text-emerald-300" /><p className="mt-3 text-sm text-slate-300">{item}</p><p className="mt-1 text-xs text-slate-500">Evidence required</p></div>)}</div><div className="mt-6 flex flex-wrap gap-2"><Button size="sm" disabled className="bg-amber-300 text-slate-950"><Send className="mr-2 size-4" />Send unavailable</Button><Button size="sm" variant="outline" disabled className="border-white/10 text-slate-500"><ArrowDownLeft className="mr-2 size-4" />Receive unavailable</Button></div>{prepared && <div className="mt-5 rounded-xl border border-cyan-300/20 bg-cyan-300/[0.06] p-4"><div className="flex items-center gap-2 font-medium text-cyan-200"><FileKey2 className="size-4" />Local wallet plan prepared</div><p className="mt-2 text-sm leading-6 text-slate-300">No wallet was derived, no key was generated, and no chain or account record was created.</p></div>}</> : <ScreenStatePanel type="empty" title="Select a wallet plan" description="Inspect chain, signer, address, balance, authorization, recovery, and transaction requirements." />}</CardContent></Card></div>}{activeView === "history" && <Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-300">Transaction lifecycle</p><h2 className="mt-1 text-2xl font-bold">Drafts before side effects</h2></div><Button size="sm" variant="outline" onClick={() => setSelectedTx(null)} className="border-white/15 text-slate-300"><RefreshCw className="mr-2 size-4" />Reset selection</Button></div><div className="mt-5 space-y-3">{TXS.map((tx) => <button key={tx.id} onClick={() => setSelectedTx(tx)} className={`w-full rounded-xl border p-4 text-left ${selectedTx?.id === tx.id ? "border-amber-300/40 bg-amber-300/[0.08]" : "border-white/10 bg-black/15 hover:bg-white/[0.05]"}`}><div className="flex items-center gap-2"><Activity className="size-4 text-amber-300" /><span className="font-semibold">{tx.type}</span><Badge variant="outline" className="ml-auto border-amber-300/20 text-amber-200">{tx.status}</Badge></div><p className="mt-2 text-xs uppercase tracking-wider text-slate-500">{tx.chain}</p><p className="mt-2 text-sm leading-6 text-slate-400">{tx.description}</p></button>)}</div>{selectedTx && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex items-center gap-2 font-medium text-amber-200"><AlertTriangle className="size-4" />Transaction action unavailable</div><p className="mt-2 text-sm leading-6 text-slate-300">A real transaction needs validated parameters, signer confirmation, gas simulation, idempotency, broadcast, receipt polling, failure handling, and a verifiable hash.</p></div>}</CardContent></Card>}{activeView === "register" && <Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><Network className="size-5 text-amber-300" /><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-300">Register plan</p><h2 className="mt-1 text-2xl font-bold">Choose a chain without deriving a key</h2></div></div><div className="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{CHAINS.map((chain) => <Button key={chain} onClick={() => { setActiveView("wallets"); registerLocal(); }} variant="outline" className="h-auto justify-start border-white/10 bg-black/15 p-4 text-left text-slate-300 hover:bg-white/[0.05]"><Globe className="mr-3 size-4 text-amber-300" />{chain}</Button>)}</div><div className="mt-6 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex items-center gap-2 font-medium text-amber-200"><LockKeyhole className="size-4" />Non-custodial boundary</div><p className="mt-2 text-sm leading-6 text-slate-300">This preview does not generate mnemonics, derive private keys, store secrets, connect a wallet, or submit a transaction. Production custody requires reviewed key-management infrastructure and user-controlled signing.</p></div></CardContent></Card>}</section><ScreenFeatureGrid features={[{ title: "Keys stay out of the client", description: "Never place seed phrases or private keys in browser state, logs, URLs, analytics, or ordinary application storage.", icon: LockKeyhole, status: "Required" }, { title: "Transaction states are evidence", description: "Unsigned, signed, broadcast, confirmed, and failed states require distinct server and chain evidence; never skip to success.", icon: Activity, status: "Required" }, { title: "No fake custody", description: "A public-address preview is not a wallet, balance, owner, signer, transaction, or secure custody service.", icon: Shield, status: "Guardrail" }]} /></main></div>; }
