import { useMemo, useState } from "react";
import { Check, Clock3, Filter, GitBranch, LockKeyhole, PlayCircle, RefreshCw, ShieldAlert, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const jobs = [{ id: 1, name: "Data reconciliation", category: "Finance", detail: "A local recurring-job concept requiring source contracts, idempotency, transaction boundaries, retry policy, reconciliation, alerting, and audit.", state: "Unconfigured" }, { id: 2, name: "Content refresh", category: "Content", detail: "A refresh concept requiring source ownership, freshness, rate limits, moderation, error recovery, cache correctness, and approval.", state: "Needs evidence" }, { id: 3, name: "Notification dispatch", category: "Messaging", detail: "A notification-job concept requiring recipient consent, quiet hours, provider, deduplication, delivery status, retries, and privacy.", state: "Blocked" }, { id: 4, name: "Compliance evidence request", category: "Governance", detail: "An evidence-job concept requiring scope, access, retention, redaction, reviewer approval, immutable audit, and accountable ownership.", state: "Preview" }];
export default function ScheduledJobs() { const [query, setQuery] = useState(""); const [category, setCategory] = useState("All"); const [selected, setSelected] = useState(1); const [cadence, setCadence] = useState("Cadence not configured"); const [connector, setConnector] = useState("Connector not configured"); const [saved, setSaved] = useState(false); const [showGates, setShowGates] = useState(false); const categories = ["All", ...Array.from(new Set(jobs.map((item) => item.category)))]; const filtered = useMemo(() => jobs.filter((item) => (category === "All" || item.category === category) && `${item.name} ${item.category} ${item.detail}`.toLowerCase().includes(query.toLowerCase())), [category, query]); const job = jobs.find((item) => item.id === selected) ?? jobs[0]; const reset = () => { setQuery(""); setCategory("All"); setSelected(1); setCadence("Cadence not configured"); setConnector("Connector not configured"); setSaved(false); setShowGates(false); }; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Clock3} eyebrow="Scheduled jobs · Automation preview" title="Prove the job boundary before allowing execution." description="Explore local finance, content, messaging, and governance automation concepts with search, category filters, cadence and connector intent, retry and approval gates, save/reset, and blocked execute/pause actions. No schedule, worker, connector, credential, execution, output, uptime, or operational result is connected." badge="Automation governance workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save job locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review automation gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset job</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Job concepts", value: `${jobs.length} local`, hint: "No scheduler source", icon: Clock3, tone: "cyan" }, { label: "Cadence", value: "Unconfigured", hint: "No schedule source", icon: RefreshCw, tone: "violet" }, { label: "Connectors", value: "Unavailable", hint: "No worker source", icon: GitBranch, tone: "amber" }, { label: "Execution", value: "Blocked", hint: "No operations source", icon: ShieldAlert, tone: "slate" }]} /><ScreenPreviewBanner title="Automation evidence boundary"><strong>This is a local job-governance preview, not evidence that a scheduler, worker, connector, credential, execution, or operational outcome exists.</strong> Job cards, filters, cadence and connector intent, saved state, retry/approval gates, and disabled execute/pause actions are browser concepts. No schedule, trigger, worker, credential, run, output, uptime, notification, financial, messaging, or compliance result is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Filter className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local scheduled jobs" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-4 flex flex-wrap gap-2">{categories.map((entry) => <Button key={entry} onClick={() => setCategory(entry)} size="sm" variant="outline" className={category === entry ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-100" : "border-white/10 text-slate-400"}>{entry}</Button>)}</div><div className="mt-6 space-y-3">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{item.name}</p><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.state}</Badge></div><div className="mt-4"><Badge variant="outline" className="border-white/10 text-slate-500">{item.category}</Badge></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected job concept</p><h2 className="mt-2 text-2xl font-black">{job.name}</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{job.state}</Badge></div><p className="mt-2 text-sm leading-6 text-slate-400">{job.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Category", value: job.category }, { label: "Cadence", value: cadence }, { label: "Connector", value: connector }, { label: "Last run", value: "Unavailable" }, { label: "Retries", value: "Unconfigured" }, { label: "Approval", value: "Required" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2"><label className="text-sm text-slate-400">Cadence intent<select value={cadence} onChange={(event) => setCadence(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Cadence not configured</option><option>Manual intent</option><option>Hourly intent</option><option>Daily intent</option><option>Event-triggered intent</option></select></label><label className="text-sm text-slate-400">Connector intent<select value={connector} onChange={(event) => setConnector(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Connector not configured</option><option>Database connector intent</option><option>Webhook connector intent</option><option>Queue connector intent</option><option>Managed integration intent</option></select></label></div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-8 text-center"><PlayCircle className="mx-auto size-8 text-slate-600" /><p className="mt-3 font-semibold">No job evidence loaded</p><p className="mt-2 text-sm text-slate-500">Connect governed scheduler, worker, trigger, connector, server-side credentials, idempotency, retries, logs, alerts, approvals, pause/resume, recovery, and audit before executing.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Run now unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Schedule unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Pause unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">View logs unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No execution or operational claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A scheduled-job concept does not prove a trigger, worker, credential, run, output, delivery, uptime, retry, notification, financial result, or compliance operation.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Automation governance gates</p><h2 className="mt-2 text-2xl font-black">What a real scheduled-job system must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Authenticated owner, tenant, schedule, trigger, timezone, worker, connector, credential, environment, and change history", "Idempotency, duplicate prevention, transaction boundary, retries, backoff, dead letters, timeouts, cancellation, and recovery", "Logs, metrics, traces, alerts, status callbacks, output provenance, sensitive-data redaction, retention, and audit", "Finance, blockchain, wallet, AI, messaging, education, community, privacy, security, and user-impact claims require domain review", "Pause/resume, approvals, dry runs, rollback, notification, rate limits, cost controls, support, and incident response", "Run now, schedule, pause, retry, view logs, export, share, and accountable approval require governed operations"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Job surface preserved", description: "Finance, content, messaging, governance jobs, filters, cadence, connectors, retries, approvals, logs, run, schedule, pause, save/reset, and gates remain interactive.", icon: Clock3, status: "Local jobs" }, { title: "No automation theater", description: "Schedulers, workers, credentials, runs, outputs, deliveries, uptime, retries, notifications, and operations are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before execution", description: "Real jobs require governed triggers, worker boundaries, secrets, idempotency, retries, observability, approvals, recovery, and audit.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>; }
