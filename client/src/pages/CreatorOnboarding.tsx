import { useMemo, useState } from "react";
import { Link } from "wouter";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ArrowRight, Camera, Check, CheckCircle2, FileText, Globe, LockKeyhole, Mic, PenTool, Radio, RefreshCw, Shield, Sparkles, Star, TrendingUp, User, Video, WalletCards, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const INITIAL_STEPS = [{ id: "profile", label: "Profile brief", description: "Define bio, avatar, links, and creator identity fields", icon: Camera, done: false }, { id: "format", label: "Choose a content format", description: "Select a local draft path before requesting publishing access", icon: FileText, done: false }, { id: "policy", label: "Review creator policies", description: "Confirm moderation, rights, privacy, and community expectations", icon: Shield, done: false }, { id: "payout", label: "Prepare payout requirements", description: "Document currency, custody, tax, refund, and settlement needs", icon: WalletCards, done: false }];
const FORMATS = [{ name: "Article", description: "Long-form tutorials, stories, or announcements", icon: FileText, examples: "Tutorial · Opinion · Story", status: "Draft path" }, { name: "Audio", description: "Podcast, music, voice note, or interview concept", icon: Mic, examples: "Podcast · Music · Interview", status: "Draft path" }, { name: "Live stream", description: "Plan a real-time format with safety and moderation gates", icon: Radio, examples: "AMA · Tutorial · Gaming", status: "Requires live-service proof" }, { name: "Exclusive drop", description: "Plan gated access without claiming token ownership or sales", icon: LockKeyhole, examples: "Early access · Members only", status: "Requires entitlement proof" }];

export default function CreatorOnboarding() {
  const [steps, setSteps] = useState(INITIAL_STEPS);
  const [selectedFormat, setSelectedFormat] = useState<string | null>(null);
  const [saved, setSaved] = useState(false);
  const completed = steps.filter((step) => step.done).length;
  const progress = useMemo(() => Math.round((completed / steps.length) * 100), [completed, steps.length]);
  const toggle = (id: string) => { setSteps((current) => current.map((step) => step.id === id ? { ...step, done: !step.done } : step)); setSaved(false); };
  const reset = () => { setSteps(INITIAL_STEPS); setSelectedFormat(null); setSaved(false); };

  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Sparkles} eyebrow="Creator studio · Onboarding" title="Prepare the creator journey before opening the gates." description="Use this local readiness plan to shape a profile, select a content format, review policies, and document payout requirements. No identity verification, publishing approval, audience reach, payout, token, or live-service access is granted." badge="Readiness preview"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save readiness plan"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset plan</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Readiness", value: `${progress}%`, hint: "Local checklist", icon: CheckCircle2, tone: progress === 100 ? "violet" : "amber" }, { label: "Formats", value: "4", hint: "Draft paths", icon: Video }, { label: "Identity", value: "Unverified", hint: "No KYC or ownership", icon: User, tone: "slate" }, { label: "Payout", value: "Off", hint: "No custody connected", icon: WifiOff, tone: "amber" }]} /><ScreenPreviewBanner title="Creator-readiness evidence boundary"><strong>Completion here means a local planning step was checked; it does not establish identity, eligibility, approval, audience reach, publishing access, ownership, earnings, payout, token custody, or a live-classroom/live-stream capability.</strong> Production onboarding requires verified identity, content rights, age and jurisdiction rules, moderation review, payout controls, privacy consent, audit history, and recoverable state.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1.05fr_0.95fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Local readiness checklist</p><h2 className="mt-2 text-2xl font-black">{completed} of {steps.length} steps reviewed</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Preview only</Badge></div><div className="mt-6 h-2 rounded-full bg-white/10"><div className="h-full rounded-full bg-gradient-to-r from-cyan-300 to-violet-400 transition-all" style={{ width: `${progress}%` }} /></div><div className="mt-5 space-y-3">{steps.map((step) => <button key={step.id} onClick={() => toggle(step.id)} className={`flex w-full items-center gap-4 rounded-xl border p-4 text-left transition ${step.done ? "border-cyan-300/30 bg-cyan-300/[0.06]" : "border-white/10 bg-black/10 hover:border-white/20"}`}><div className={`flex size-10 items-center justify-center rounded-xl ${step.done ? "bg-cyan-300/15 text-cyan-200" : "bg-white/5 text-slate-400"}`}>{step.done ? <Check className="size-5" /> : <step.icon className="size-5" />}</div><div className="flex-1"><p className={`font-semibold ${step.done ? "text-cyan-100" : "text-white"}`}>{step.label}</p><p className="mt-1 text-sm text-slate-500">{step.description}</p></div><span className="text-xs text-slate-500">{step.done ? "Reviewed" : "Review"}</span></button>)}</div>{saved && <p className="mt-5 text-sm text-cyan-200">Local readiness plan saved; no creator account or access state changed.</p>}</CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><PenTool className="size-5 text-violet-300" /><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-300">Choose a draft path</p><h2 className="mt-1 text-2xl font-black">What do you want to prepare?</h2></div></div><div className="mt-6 space-y-3">{FORMATS.map((format) => <button key={format.name} onClick={() => { setSelectedFormat(format.name); setSaved(false); }} className={`flex w-full items-start gap-3 rounded-xl border p-4 text-left ${selectedFormat === format.name ? "border-violet-300/40 bg-violet-300/[0.08]" : "border-white/10 bg-black/10"}`}><format.icon className="mt-1 size-5 text-violet-200" /><div className="flex-1"><div className="flex items-center justify-between gap-2"><p className="font-semibold">{format.name}</p><span className="text-xs text-amber-200">{format.status}</span></div><p className="mt-1 text-sm text-slate-400">{format.description}</p><p className="mt-2 text-xs text-slate-500">{format.examples}</p></div></button>)}</div><div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-200">{selectedFormat ? `${selectedFormat} selected locally` : "No publishing action"}</p><p className="mt-2 text-sm leading-6 text-slate-400">Selection only shapes the next draft step. It does not upload media, start a stream, mint a drop, publish content, or promise monetization.</p></div></CardContent></Card></section><section className="grid gap-4 md:grid-cols-3">{[{ label: "Identity and eligibility", description: "Verification, age, jurisdiction, ownership, and consent are not connected.", icon: User }, { label: "Rights and moderation", description: "Content rights, safety review, reporting, and takedown workflows require production services.", icon: Shield }, { label: "Custody and settlement", description: "Wallets, fees, payouts, refunds, taxes, and token transfers remain unavailable.", icon: WalletCards }].map((item) => <Card key={item.label} className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><item.icon className="size-5 text-cyan-300" /><h2 className="mt-4 font-black">{item.label}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{item.description}</p></CardContent></Card>)}</section><div className="flex flex-wrap gap-3">{[{ label: "Creator Studio", href: "/creator-studio", icon: Star }, { label: "Analytics preview", href: "/creator-analytics", icon: TrendingUp }, { label: "Home", href: "/", icon: Globe }].map((item) => <Link key={item.label} href={item.href}><Button variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><item.icon className="mr-2 size-4" />{item.label}<ArrowRight className="ml-2 size-4" /></Button></Link>)}</div><ScreenFeatureGrid features={[{ title: "Readiness is not approval", description: "A checked local step cannot prove identity, eligibility, moderation approval, or publishing access.", icon: LockKeyhole, status: "Guardrail" }, { title: "Draft before publish", description: "Format selection keeps the creator journey useful without uploading or publishing unsupported content.", icon: PenTool, status: "Preview" }, { title: "Payout needs custody", description: "No wallet, payment processor, token contract, refund flow, or settlement ledger is connected.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
