import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Blocks, Check, CircleDollarSign, FileWarning, KeyRound, LockKeyhole, Network, RefreshCw, ShieldCheck, Sparkles, WalletCards, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const MODES = ["Non-custodial concept", "Connected wallet", "Managed custody"];
const NETWORKS = ["Network unset", "Ethereum-compatible", "Solana-compatible", "Testnet placeholder"];
const ASSETS = [{ name: "Azure Relay #01", state: "Ownership unverified", detail: "Synthetic gallery reference; no token or wallet link." }, { name: "Night Garden #07", state: "Metadata unavailable", detail: "Local collectible concept; no chain record." }, { name: "Orbit Fragment #12", state: "Transfer blocked", detail: "Preview asset; no signing or transaction path." }];

export default function NFTWallet() {
  const [mode, setMode] = useState(MODES[0]);
  const [network, setNetwork] = useState(NETWORKS[0]);
  const [selected, setSelected] = useState(ASSETS[0].name);
  const [note, setNote] = useState("");
  const [saved, setSaved] = useState(false);
  const asset = ASSETS.find((item) => item.name === selected) ?? ASSETS[0];
  const reset = () => { setMode(MODES[0]); setNetwork(NETWORKS[0]); setSelected(ASSETS[0].name); setNote(""); setSaved(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={WalletCards} eyebrow="NFTWallet · Custody planning" title="Review custody boundaries before connecting a wallet." description="Plan a local NFT-wallet experience with custody modes, network selection, synthetic asset slots, transaction-state concepts, and security gates. No wallet connects, balance loads, ownership verifies, private key stores, signature occurs, or transfer is submitted." badge="Wallet preview"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Wallet plan saved locally" : "Save wallet plan"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset wallet plan</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Custody", value: "Unverified", hint: "Local mode selection", icon: KeyRound }, { label: "Network", value: network === NETWORKS[0] ? "Unset" : "Planned", hint: "No RPC source", icon: Network, tone: "violet" }, { label: "Assets", value: String(ASSETS.length), hint: "Synthetic slots", icon: Sparkles, tone: "amber" }, { label: "Balance", value: "Off", hint: "No chain read", icon: CircleDollarSign, tone: "slate" }]} /><ScreenPreviewBanner title="NFTWallet evidence boundary"><strong>Custody mode, network selection, asset names, transaction states, and security checklist are wallet-planning fixtures—not connected accounts, balances, token ownership, chain state, private-key custody, signatures, transaction hashes, confirmations, or transfer results.</strong> Production wallet functionality requires secure provider integration, address validation, chain-aware indexing, key isolation, signing safeguards, transaction lifecycle, user consent, recovery, rate limits, and support.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1fr_0.95fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Wallet setup</p><h2 className="mt-2 text-3xl font-black">Choose the boundary</h2></div><Badge variant="outline" className="border-white/10 text-slate-400">Local only</Badge></div><p className="mt-3 text-slate-400">A mode selection does not connect a provider or establish custody.</p><div className="mt-5 space-y-3">{MODES.map((item) => <button key={item} onClick={() => { setMode(item); setSaved(false); }} className={`w-full rounded-2xl border p-4 text-left ${mode === item ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-black/10"}`}><div className="flex items-center justify-between"><span className="font-black">{item}</span>{mode === item && <Check className="size-4 text-cyan-200" />}</div><p className="mt-2 text-xs text-slate-500">{item === MODES[0] ? "User-controlled wallet concept; provider unverified." : item === MODES[1] ? "External provider concept; connection unavailable." : "High-risk custody concept; no secure custody service exists here."}</p></button>)}</div><label className="mt-5 block"><span className="text-sm font-semibold text-slate-300">Network planning</span><select value={network} onChange={(event) => { setNetwork(event.target.value); setSaved(false); }} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-white">{NETWORKS.map((item) => <option key={item} value={item}>{item}</option>)}</select></label><textarea value={note} onChange={(event) => { setNote(event.target.value); setSaved(false); }} placeholder="Write a local wallet-safety or recovery note…" className="mt-5 min-h-24 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-sm text-white placeholder:text-slate-500" /><div className="mt-4 flex items-start gap-3 text-xs leading-5 text-slate-500"><LockKeyhole className="mt-0.5 size-4 shrink-0 text-cyan-300" />Never enter seed phrases, private keys, passwords, access tokens, or recovery secrets into this preview.</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-300">Asset slots</p><h2 className="mt-2 text-3xl font-black">{asset.name}</h2><p className="mt-3 text-slate-400">{asset.detail}</p><div className="mt-5 space-y-3">{ASSETS.map((item) => <button key={item.name} onClick={() => { setSelected(item.name); setSaved(false); }} className={`w-full rounded-xl border p-4 text-left ${selected === item.name ? "border-violet-300/40 bg-violet-300/[0.08]" : "border-white/10 bg-black/10"}`}><div className="flex items-center justify-between gap-2"><span className="font-semibold">{item.name}</span><Badge variant="outline" className="border-amber-300/20 text-[10px] text-amber-200">{item.state}</Badge></div><p className="mt-2 text-xs text-slate-500">{item.detail}</p></button>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2">{[{ label: "Address", value: "Unavailable" }, { label: "Balance", value: "Not read" }, { label: "Token ID", value: "Not sourced" }, { label: "Transfer", value: "Blocked" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><Button disabled className="mt-5 w-full bg-white/10 text-white/40"><WalletCards className="mr-2 size-4" />Connect wallet unavailable</Button></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Wallet readiness</p><h2 className="mt-2 text-2xl font-black">Required before custody claims</h2><div className="mt-5 space-y-3">{["Provider, chain, address, token, and indexer validation", "Key isolation, signing consent, and recovery design", "Balance freshness, ownership proof, and transaction state", "Replay, duplicate, phishing, and malicious-contract protection", "Failure handling, support, export, deletion, and incident response"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><FileWarning className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake custody</h2><p className="mt-3 text-sm leading-6 text-slate-400">The planner does not connect a wallet, display a balance, prove ownership, store secrets, sign, submit, or confirm a transaction.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "A slot is not an NFT", description: "Synthetic asset references preserve wallet UX without ownership, token, address, or chain claims.", icon: Sparkles, status: "Guardrail" }, { title: "Custody needs isolation", description: "Keys, signing, recovery, provider permissions, and malicious-contract controls need security design.", icon: KeyRound, status: "Required" }, { title: "No fake transfer", description: "Local mode and selection state remain non-custodial while wallet connection and signing stay unavailable.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
