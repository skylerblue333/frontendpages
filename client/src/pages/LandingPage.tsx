import Home from "./Home";

/**
 * Public landing surface. It intentionally presents product orientation and
 * verified release posture only; provider-dependent financial data and
 * production infrastructure evidence are not represented as active.
 */
export default function LandingPage() {
  return <Home />;
}
