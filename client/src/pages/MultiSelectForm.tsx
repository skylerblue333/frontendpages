import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Check, CheckCircle2, ClipboardList, Filter, LockKeyhole, RefreshCw, Search, Send, ShieldCheck, SlidersHorizontal, Sparkles, WifiOff, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const OPTIONS = [{ label: "Analytics workspace", group: "Workspace", description: "Plan a dashboard surface for verified product events." }, { label: "Creator tools", group: "Workspace", description: "Organize creator planning, review, and publishing capabilities." }, { label: "Learning paths", group: "Education", description: "Select course and progress experiences for a learner journey." }, { label: "Wallet safety", group: "Finance", description: "Keep wallet features behind custody and network gates." }, { label: "AI assistance", group: "AI", description: "Route AI help only after provider and privacy verification." }, { label: "Community spaces", group: "Community", description: "Plan social surfaces with consent, moderation, and privacy controls." }, { label: "Notifications", group: "Operations", description: "Prepare preferences without claiming delivery or event coverage." }, { label: "Accessibility review", group: "Quality", description: "Include keyboard, contrast, motion, and assistive-technology checks." }];
const GROUPS = ["All", "Workspace", "Education", "Finance", "AI", "Community", "Operations", "Quality"];

export default function MultiSelectForm() {
  const [selected, setSelected] = useState<string[]>(["Accessibility review"]);
  const [query, setQuery] = useState("");
  const [group, setGroup] = useState("All");
  const [saved, setSaved] = useState(false);
  const visible = useMemo(() => OPTIONS.filter((item) => (group === "All" || item.group === group) && `${item.label} ${item.group} ${item.description}`.toLowerCase().includes(query.toLowerCase())), [group, query]);
  const toggle = (label: string) => { setSelected((current) => current.includes(label) ? current.filter((item) => item !== label) : [...current, label]); setSaved(false); };
  const reset = () => { setSelected(["Accessibility review"]); setQuery(""); setGroup("All"); setSaved(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={ClipboardList} eyebrow="MultiSelectForm · Selection planning" title="Assemble a scope before submitting a form." description="Select product-scope options, inspect grouped descriptions, and review a local selection summary. This preview validates interaction only: it does not submit, persist, authorize, provision, notify, or claim backend acceptance." badge="Form preview"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Selection saved locally" : "Save selection"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset form</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Options", value: String(OPTIONS.length), hint: "Local scope cards", icon: SlidersHorizontal }, { label: "Selected", value: String(selected.length), hint: "Local selection", icon: CheckCircle2, tone: "cyan" }, { label: "Groups", value: String(GROUPS.length - 1), hint: "Filter categories", icon: Filter, tone: "violet" }, { label: "Submit", value: "Off", hint: "No backend action", icon: Send, tone: "amber" }]} /><ScreenPreviewBanner title="MultiSelectForm evidence boundary"><strong>Selection state, group filters, descriptions, and local validation messaging are interaction fixtures—not a submitted form, saved preference, authorized scope, provisioned feature, notification, backend record, or acceptance result.</strong> Production forms require schema validation, authorization, CSRF protection, persistence, idempotency, status feedback, auditability, and a recoverable failure path.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1fr_0.85fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search selection options" className="w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 pl-9 text-white placeholder:text-slate-500" /></div><div className="mt-4 flex gap-2 overflow-x-auto">{GROUPS.map((item) => <button key={item} onClick={() => setGroup(item)} className={`rounded-xl border px-3 py-2 text-xs whitespace-nowrap ${group === item ? "border-cyan-300/40 bg-cyan-300/[0.1] text-cyan-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div><div className="mt-5 space-y-3">{visible.map((item) => { const isSelected = selected.includes(item.label); return <button key={item.label} onClick={() => toggle(item.label)} className={`flex w-full items-start gap-3 rounded-2xl border p-4 text-left ${isSelected ? "border-cyan-300/40 bg-cyan-300/[0.07]" : "border-white/10 bg-black/10"}`}><div className={`flex size-10 shrink-0 items-center justify-center rounded-xl ${isSelected ? "bg-cyan-300/15 text-cyan-200" : "bg-white/5 text-slate-500"}`}>{isSelected ? <Check className="size-5" /> : <Sparkles className="size-5" />}</div><div className="min-w-0 flex-1"><div className="flex flex-wrap items-center justify-between gap-2"><h2 className="font-black">{item.label}</h2><Badge variant="outline" className="border-white/10 text-[10px] text-slate-400">{item.group}</Badge></div><p className="mt-2 text-sm text-slate-400">{item.description}</p></div></button>; })}{visible.length === 0 && <div className="p-8 text-center text-slate-500">No options match this filter.</div>}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-300">Selection summary</p><h2 className="mt-2 text-3xl font-black">{selected.length} selected</h2></div><Badge variant="outline" className="border-white/10 text-slate-400">Local only</Badge></div><div className="mt-5 rounded-2xl border border-white/10 bg-black/20 p-4"><p className="text-sm text-slate-400">Current scope</p><div className="mt-4 flex flex-wrap gap-2">{selected.length > 0 ? selected.map((item) => <span key={item} className="inline-flex items-center gap-1 rounded-full border border-cyan-300/20 bg-cyan-300/[0.08] px-3 py-1.5 text-xs text-cyan-200">{item}<button onClick={() => toggle(item)} aria-label={`Remove ${item}`}><X className="size-3" /></button></span>) : <span className="text-sm text-slate-500">No scope selected.</span>}</div></div><div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-200">Validation preview</p><p className="mt-2 text-sm leading-6 text-slate-400">{selected.length > 0 ? "A local selection exists. Required permissions, dependencies, and backend validation are not checked." : "Choose at least one option before a real form could be considered complete."}</p></div><div className="mt-4 flex items-start gap-3 text-xs leading-5 text-slate-500"><LockKeyhole className="mt-0.5 size-4 shrink-0 text-cyan-300" />No selection is sent, stored remotely, or used to change an account.</div><Button disabled className="mt-5 w-full bg-white/10 text-white/40"><Send className="mr-2 size-4" />Submit unavailable</Button></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Production form gates</p><h2 className="mt-2 text-2xl font-black">Required before submission</h2><div className="mt-5 space-y-3">{["Schema validation and accessible error messages", "Authentication, authorization, and CSRF protection", "Persistence, idempotency, and audit event", "Success, failure, retry, and rollback states", "Privacy, retention, and support ownership"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><WifiOff className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake submission</h2><p className="mt-3 text-sm leading-6 text-slate-400">The form demonstrates selection and validation copy without claiming an API call, database write, account change, or acceptance response.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Selection is not submission", description: "Local scope state preserves form UX without a backend record or authorization claim.", icon: ClipboardList, status: "Guardrail" }, { title: "Errors need contracts", description: "Real forms need typed validation, field-level errors, retries, and accessible feedback.", icon: ShieldCheck, status: "Required" }, { title: "No fake provisioning", description: "The preview does not change permissions, enable features, notify users, or create records.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
