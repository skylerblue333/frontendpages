import { useState } from "react";
import { useLocation } from "wouter";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ArrowRight, BookOpen, Check, CheckCircle2, Circle, FileCheck2, Gamepad2, Heart, LockKeyhole, Mic, RefreshCw, ShoppingCart, ShieldCheck, Sparkles, TrendingUp, Users, Vote, WifiOff, Zap } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const STEPS = [{ id: "welcome", title: "Orientation", description: "Understand the evidence-first boundary before exploring modules.", detail: "SKYCOIN4444 is represented here as a set of navigation surfaces. Capabilities and integrations require verification.", icon: Zap, status: "Foundation" }, { id: "ai", title: "AI workbench", description: "Review local AI concepts and invocation gates.", detail: "Model availability, tools, privacy, context, and output quality are not asserted by this tour.", icon: Sparkles, status: "Preview" }, { id: "learning", title: "Learning paths", description: "Explore course and study-plan concepts.", detail: "Enrollment, grades, instructors, progress, and credentials need a real LMS boundary.", icon: BookOpen, status: "Preview" }, { id: "games", title: "Gaming spaces", description: "Find playable and multiplayer surfaces.", detail: "A route card does not prove live presence, matchmaking, leaderboards, rewards, or network state.", icon: Gamepad2, status: "Preview" }, { id: "governance", title: "Governance", description: "Review decision and voting concepts.", detail: "Votes, quorum, tokens, staking, proposals, and outcomes are not created by this tour.", icon: Vote, status: "Guardrail" }, { id: "analytics", title: "Analytics", description: "Learn how to read evidence-backed measurements.", detail: "No live users, revenue, growth, latency, forecasts, or platform metrics are shown here.", icon: TrendingUp, status: "Preview" }, { id: "community", title: "Community and care", description: "Review social, wellbeing, and safety surfaces.", detail: "Identity, relationships, moderation, health, privacy, and support claims require their own evidence.", icon: Heart, status: "Guardrail" }, { id: "commerce", title: "Commerce", description: "Review marketplace and wallet boundaries.", detail: "No price, balance, ownership, payment, transfer, payout, or custody claim is made.", icon: ShoppingCart, status: "High risk" }];
const FEATURES = [{ label: "Evidence-led", desc: "Claims have a visible boundary", icon: ShieldCheck }, { label: "Local state", desc: "Progress stays in this preview", icon: CheckCircle2 }, { label: "No secrets", desc: "No tokens or keys requested", icon: LockKeyhole }, { label: "Accessible path", desc: "Step navigation remains explicit", icon: Users }];

export default function Onboarding() {
  const [, navigate] = useLocation();
  const [current, setCurrent] = useState(0);
  const [completed, setCompleted] = useState<string[]>([]);
  const [interest, setInterest] = useState("Explore broadly");
  const step = STEPS[current];
  const Icon = step.icon;
  const progress = Math.round(((current + 1) / STEPS.length) * 100);
  const completeCurrent = () => setCompleted((items) => items.includes(step.id) ? items : [...items, step.id]);
  const next = () => { completeCurrent(); if (current < STEPS.length - 1) setCurrent(current + 1); };
  const reset = () => { setCurrent(0); setCompleted([]); setInterest("Explore broadly"); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Sparkles} eyebrow="Onboarding · Orientation preview" title="Start with evidence, then explore the ecosystem." description="Walk through synthetic module surfaces, choose a local interest lens, and track orientation progress. This tour does not create an account, issue an airdrop, enable a service, assert a vote, or guarantee a feature." badge="First-run preview"><div className="flex flex-wrap gap-2"><Button onClick={next} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200">{current === STEPS.length - 1 ? "Mark tour complete locally" : "Continue locally"}<ArrowRight className="ml-2 size-4" /></Button><Button onClick={() => navigate("/dashboard")} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">Leave preview</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Step", value: `${current + 1}/${STEPS.length}`, hint: "Local orientation", icon: Circle }, { label: "Progress", value: `${progress}%`, hint: `${completed.length} completed locally`, icon: CheckCircle2, tone: "cyan" }, { label: "Interest", value: interest === "Explore broadly" ? "Broad" : interest, hint: "Local preference", icon: Users, tone: "violet" }, { label: "Services", value: "Off", hint: "No activation", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="Onboarding evidence boundary"><strong>Step progress, interest selection, module descriptions, and local completion are orientation fixtures—not account creation, personalization, entitlement, airdrop allocation, token distribution, product activation, service availability, or proof of production readiness.</strong> Production onboarding requires identity, consent, persistence, authorization, accessibility, data minimization, recoverable progress, feature flags, support, and clear service-state contracts.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.8fr_1.2fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Orientation map</p><h2 className="mt-2 text-2xl font-black">Choose a step</h2><div className="mt-5 space-y-2">{STEPS.map((item, index) => { const ItemIcon = item.icon; const done = completed.includes(item.id); return <button key={item.id} onClick={() => setCurrent(index)} className={`flex w-full items-center gap-3 rounded-xl border p-3 text-left ${index === current ? "border-cyan-300/40 bg-cyan-300/[0.08]" : done ? "border-violet-300/20 bg-violet-300/[0.05]" : "border-white/10 bg-black/10"}`}><div className="flex size-8 items-center justify-center rounded-lg bg-white/5 text-cyan-200">{done ? <Check className="size-4" /> : <ItemIcon className="size-4" />}</div><div className="flex-1"><p className="text-sm font-black">{item.title}</p><p className="mt-1 text-xs text-slate-500">{item.status}</p></div>{index === current && <Badge variant="outline" className="border-cyan-300/30 text-cyan-200">Current</Badge>}</button>; })}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-7"><div className="flex items-start gap-5"><div className="flex size-14 shrink-0 items-center justify-center rounded-2xl bg-gradient-to-br from-cyan-400/30 to-violet-400/30 text-cyan-100"><Icon className="size-7" /></div><div><div className="flex flex-wrap items-center gap-2"><Badge variant="outline" className="border-white/10 text-slate-400">Step {current + 1}</Badge><Badge variant="outline" className="border-violet-300/30 text-violet-200">{step.status}</Badge></div><h2 className="mt-3 text-3xl font-black">{step.title}</h2><p className="mt-3 text-lg text-slate-300">{step.description}</p><p className="mt-4 leading-7 text-slate-400">{step.detail}</p></div></div><div className="mt-7 rounded-2xl border border-cyan-300/20 bg-cyan-300/[0.05] p-5"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200">Local orientation prompt</p><p className="mt-2 text-sm leading-6 text-slate-300">Which lens should shape your preview?</p><div className="mt-4 flex flex-wrap gap-2">{["Explore broadly", "Build and learn", "Play and connect", "Review safety"].map((item) => <button key={item} onClick={() => setInterest(item)} className={`rounded-xl border px-3 py-2 text-xs ${interest === item ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-100" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div></div><div className="mt-7 flex flex-wrap gap-3"><Button onClick={() => setCurrent(Math.max(0, current - 1))} disabled={current === 0} variant="outline" className="border-white/10 text-white">Previous</Button><Button onClick={next} className="bg-gradient-to-r from-cyan-400 to-violet-500 text-slate-950 hover:from-cyan-300 hover:to-violet-400">{current === STEPS.length - 1 ? "Complete locally" : "Mark and continue"}<ArrowRight className="ml-2 size-4" /></Button></div></CardContent></Card></section><section className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{FEATURES.map((item) => { const FeatureIcon = item.icon; return <Card key={item.label} className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><FeatureIcon className="size-5 text-cyan-300" /><p className="mt-4 font-black">{item.label}</p><p className="mt-2 text-sm leading-6 text-slate-500">{item.desc}</p></CardContent></Card>; })}</section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Readiness checklist</p><h2 className="mt-2 text-2xl font-black">Required before onboarding claims</h2><div className="mt-5 space-y-3">{["Account identity, consent, privacy, persistence, deletion, and recovery", "Feature availability, service health, authorization, and route contracts", "Evidence for financial, crypto, AI, education, health, social, and safety claims", "Accessible keyboard flow, clear errors, loading, retry, and support states", "No fake rewards, balances, users, votes, analytics, certificates, or completion"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><FileCheck2 className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake welcome</h2><p className="mt-3 text-sm leading-6 text-slate-400">This orientation changes local state only. It does not grant tokens, create accounts, enroll users, activate services, record votes, publish metrics, or certify completion.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "A tour is not activation", description: "Local steps preserve discovery UX without account, reward, service, or feature claims.", icon: Sparkles, status: "Guardrail" }, { title: "First-run needs recovery", description: "Identity, persistence, feature state, accessibility, support, and evidence need explicit contracts.", icon: ShieldCheck, status: "Required" }, { title: "No fake reward", description: "The preview makes no airdrop, token, entitlement, vote, payment, or backend request.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
