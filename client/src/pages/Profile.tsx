import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { BarChart3, Check, Eye, FileText, Heart, LockKeyhole, Play, RefreshCw, ShieldAlert, Sparkles, UserRound, Users, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const tabs = ["Posts", "Media", "Liked", "Analytics"];
export default function Profile() {
  const [tab, setTab] = useState(tabs[0]);
  const [displayName, setDisplayName] = useState("Skycoin explorer");
  const [bio, setBio] = useState("A local profile draft for exploring the SKYCOIN4444 ecosystem.");
  const [saved, setSaved] = useState(false);
  const [showEdit, setShowEdit] = useState(false);
  const reset = () => { setTab(tabs[0]); setDisplayName("Skycoin explorer"); setBio("A local profile draft for exploring the SKYCOIN4444 ecosystem."); setSaved(false); setShowEdit(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={UserRound} eyebrow="Profile · Account surface preview" title="Make identity useful without inventing account history." description="Review a local profile concept with posts, media, liked, and analytics tabs. No account persistence, verification, followers, posts, media, earnings, views, subscribers, engagement, or social activity is asserted or synced." badge="Profile workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Profile saved locally" : "Save profile locally"}</Button><Button onClick={() => setShowEdit((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showEdit ? <X className="mr-2 size-4" /> : <FileText className="mr-2 size-4" />}{showEdit ? "Close editor" : "Edit profile draft"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset profile</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Identity", value: "Local", hint: "Not account synced", icon: UserRound, tone: "cyan" }, { label: "Posts", value: "Unmeasured", hint: "No social source", icon: FileText, tone: "violet" }, { label: "Audience", value: "Unknown", hint: "No follower source", icon: Users, tone: "amber" }, { label: "Analytics", value: "Off", hint: "No event ledger", icon: BarChart3, tone: "slate" }]} /><ScreenPreviewBanner title="Profile evidence boundary"><strong>This is a local profile preview, not a verified account record.</strong> Name, bio, tabs, privacy labels, and empty states are browser-state concepts. No user identity, verification, followers, posts, likes, media, earnings, views, subscribers, engagement, or social connection is measured or persisted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.8fr_1.2fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start gap-4"><div className="flex size-20 items-center justify-center rounded-3xl bg-cyan-300/10"><UserRound className="size-9 text-cyan-200" /></div><div className="flex-1"><div className="flex flex-wrap items-center gap-2"><h2 className="text-2xl font-black">{displayName}</h2><Badge variant="outline" className="border-white/10 text-slate-400">Local draft</Badge></div><p className="mt-3 text-sm leading-6 text-slate-400">{bio}</p><div className="mt-4 flex flex-wrap gap-2"><Badge variant="outline" className="border-amber-300/20 text-amber-200">Verification unavailable</Badge><Badge variant="outline" className="border-white/10 text-slate-500">Followers unknown</Badge></div></div></div>{showEdit && <div className="mt-6 space-y-4 rounded-xl border border-cyan-300/20 bg-cyan-300/[0.05] p-4"><label className="block text-sm text-slate-400">Display name<input value={displayName} onChange={(event) => setDisplayName(event.target.value)} className="mt-2 w-full rounded-lg border border-white/10 bg-black/20 px-3 py-3 text-white outline-none" /></label><label className="block text-sm text-slate-400">Bio<textarea value={bio} onChange={(event) => setBio(event.target.value)} className="mt-2 min-h-24 w-full rounded-lg border border-white/10 bg-black/20 p-3 text-white outline-none" /></label><p className="text-xs text-slate-500">Draft edits remain in browser state until a real account/profile service is connected.</p></div>}</CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Profile posture</p><h2 className="mt-2 text-2xl font-black">Account state unknown</h2></div><LockKeyhole className="size-5 text-amber-200" /></div><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Account", value: "Not connected" }, { label: "Visibility", value: "Unconfigured" }, { label: "Verification", value: "Unavailable" }, { label: "Social graph", value: "Not loaded" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-6 rounded-2xl border border-amber-300/25 bg-amber-300/[0.06] p-5"><div className="flex items-start gap-3"><ShieldAlert className="mt-0.5 size-5 shrink-0 text-amber-200" /><div><p className="font-semibold text-amber-100">Social data unavailable</p><p className="mt-2 text-sm leading-6 text-slate-400">No authenticated profile, privacy policy, social graph, post store, media store, analytics ledger, or account sync is connected.</p></div></div></div></CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex flex-wrap gap-2">{tabs.map((item) => <button key={item} onClick={() => setTab(item)} className={`flex items-center gap-2 rounded-lg border px-4 py-2 text-sm ${tab === item ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-200" : "border-white/10 text-slate-500"}`}>{item === "Posts" && <FileText className="size-4" />}{item === "Media" && <Play className="size-4" />}{item === "Liked" && <Heart className="size-4" />}{item === "Analytics" && <BarChart3 className="size-4" />}{item}</button>)}</div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-12 text-center">{tab === "Posts" && <FileText className="mx-auto size-10 text-cyan-300/40" />}{tab === "Media" && <Play className="mx-auto size-10 text-violet-300/40" />}{tab === "Liked" && <Heart className="mx-auto size-10 text-pink-300/40" />}{tab === "Analytics" && <BarChart3 className="mx-auto size-10 text-amber-300/40" />}<h3 className="mt-4 text-xl font-black">{tab} unavailable in preview</h3><p className="mx-auto mt-2 max-w-xl text-sm leading-6 text-slate-500">This tab is preserved as a useful state, but no {tab.toLowerCase()} data source is connected. Nothing is fabricated to make the profile appear active.</p><Button disabled className="mt-5 bg-slate-700 text-slate-400">{tab === "Analytics" ? "View analytics unavailable" : `Load ${tab.toLowerCase()} unavailable`}</Button></div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Profile surface preserved", description: "Identity summary, local editing, posts, media, liked, analytics tabs, privacy labels, save/reset, and unavailable states remain interactive.", icon: UserRound, status: "Local account" }, { title: "No social theater", description: "Verification, followers, posts, likes, media, earnings, views, subscribers, engagement, and activity are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Account service required", description: "Real profile behavior needs authenticated persistence, privacy controls, social data, moderation, analytics, export, and deletion paths.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
