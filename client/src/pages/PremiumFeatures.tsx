import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Check, Crown, Eye, LockKeyhole, RefreshCw, Search, ShieldAlert, Sparkles, Star, X, Zap } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

type Feature = { id: string; name: string; category: string; description: string; status: string; evidence: string };
const features: Feature[] = [
  { id: "ai", name: "Advanced AI workspace", category: "AI", description: "A premium product concept for expanded AI workflows and controls.", status: "Preview", evidence: "No entitlement check" },
  { id: "analytics", name: "Predictive analytics", category: "Insights", description: "A premium product concept for governed analytics and model review.", status: "Preview", evidence: "No data connector" },
  { id: "wallet", name: "Wallet insights", category: "Crypto", description: "A premium product concept for wallet intelligence with explicit custody boundaries.", status: "Preview", evidence: "No wallet service" },
  { id: "school", name: "Learning pathways", category: "Education", description: "A premium product concept for richer course planning and progress experiences.", status: "Preview", evidence: "No progress service" },
  { id: "support", name: "Priority support", category: "Service", description: "A premium service concept that requires support routing and an account plan.", status: "Unavailable", evidence: "No support connector" },
];
const plans = [{ name: "Explorer", note: "Baseline access concept", highlight: "Preview" }, { name: "Creator", note: "Expanded tools concept", highlight: "Preview" }, { name: "Enterprise", note: "Governed team access concept", highlight: "Unavailable" }];
export default function PremiumFeatures() {
  const [query, setQuery] = useState("");
  const [category, setCategory] = useState("All");
  const [selectedId, setSelectedId] = useState(features[0].id);
  const [saved, setSaved] = useState(false);
  const [showPlan, setShowPlan] = useState(false);
  const visible = useMemo(() => features.filter((item) => (category === "All" || item.category === category) && `${item.name} ${item.category} ${item.description}`.toLowerCase().includes(query.toLowerCase())), [category, query]);
  const selected = features.find((item) => item.id === selectedId) ?? features[0];
  const reset = () => { setQuery(""); setCategory("All"); setSelectedId(features[0].id); setSaved(false); setShowPlan(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Crown} eyebrow="PremiumFeatures · Access preview" title="Explore premium value without pretending access was purchased." description="Review premium feature concepts, plan positioning, categories, and evidence boundaries. No price, entitlement, checkout, subscription, payment, upgrade, or premium access is asserted or executed from this preview." badge="Premium catalog"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Interest saved locally" : "Save local interest"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset catalog</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Feature concepts", value: String(features.length), hint: "Local catalog", icon: Sparkles, tone: "cyan" }, { label: "Categories", value: "5", hint: "Product areas", icon: Star, tone: "violet" }, { label: "Entitlement", value: "Unknown", hint: "No account check", icon: LockKeyhole, tone: "amber" }, { label: "Checkout", value: "Off", hint: "No billing path", icon: ShieldAlert, tone: "slate" }]} /><ScreenPreviewBanner title="Premium access evidence boundary"><strong>This is a product catalog preview, not a purchase flow.</strong> Feature labels and plan cards are local concepts. The page does not claim a user is authenticated, entitled, subscribed, upgraded, charged, refunded, supported, or eligible for premium access. No payment or billing provider is contacted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.84fr_1.16fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Feature catalog</p><h2 className="mt-2 text-2xl font-black">Choose a capability</h2></div><Badge variant="outline" className="border-white/10 text-amber-200">{visible.length} listed</Badge></div><div className="relative mt-5"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search premium concepts…" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-9 pr-3 text-sm text-white outline-none focus:border-cyan-300/50" /></div><div className="mt-4 flex flex-wrap gap-2">{["All", "AI", "Insights", "Crypto", "Education", "Service"].map((item) => <button key={item} onClick={() => setCategory(item)} className={`rounded-lg border px-3 py-2 text-xs ${category === item ? "border-cyan-300/40 bg-cyan-300/10 text-cyan-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div><div className="mt-4 space-y-3">{visible.map((item) => <button key={item.id} onClick={() => setSelectedId(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected.id === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start gap-3"><div className="flex size-10 shrink-0 items-center justify-center rounded-xl bg-violet-300/10"><Sparkles className="size-5 text-violet-200" /></div><div className="flex-1"><div className="flex flex-wrap items-center gap-2"><p className="font-semibold">{item.name}</p><Badge variant="outline" className="border-white/10 text-slate-400">{item.category}</Badge></div><p className="mt-2 text-sm leading-5 text-slate-500">{item.description}</p><div className="mt-3 flex flex-wrap gap-2 text-xs text-slate-400"><span>{item.status}</span><span>·</span><span>{item.evidence}</span></div></div></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Feature detail</p><h2 className="mt-2 text-2xl font-black">{selected.name}</h2><p className="mt-2 text-sm text-slate-500">{selected.description}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{selected.status}</Badge></div><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Category", value: selected.category }, { label: "Evidence", value: selected.evidence }, { label: "Entitlement", value: "Unavailable" }, { label: "Billing", value: "Not configured" }, { label: "Access", value: "Not granted" }, { label: "Support", value: "Unverified" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-6 rounded-2xl border border-amber-300/25 bg-amber-300/[0.06] p-5"><div className="flex items-start gap-3"><LockKeyhole className="mt-0.5 size-5 shrink-0 text-amber-200" /><div><p className="font-semibold text-amber-100">Upgrade is unavailable here</p><p className="mt-2 text-sm leading-6 text-slate-400">A real upgrade requires authenticated identity, catalog version, price and tax calculation, checkout, payment confirmation, entitlement issuance, cancellation/refund handling, and access enforcement.</p><Button disabled className="mt-4 bg-slate-700 text-slate-400"><Zap className="mr-2 size-4" />Upgrade unavailable</Button></div></div></div></CardContent></Card></section><section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Plan concepts</p><h2 className="mt-2 text-2xl font-black">Compare positioning, not prices</h2></div><Button onClick={() => setShowPlan((value) => !value)} variant="outline" className="border-white/10 text-slate-300">{showPlan ? <X className="size-4" /> : <Eye className="mr-2 size-4" />} {showPlan ? "Close" : "Show plan concepts"}</Button></div>{showPlan && <div className="mt-5 grid gap-4 md:grid-cols-3">{plans.map((plan) => <div key={plan.name} className="rounded-2xl border border-white/10 p-5"><div className="flex items-center justify-between"><h3 className="font-bold">{plan.name}</h3><Badge variant="outline" className="border-white/10 text-amber-200">{plan.highlight}</Badge></div><p className="mt-3 text-sm leading-6 text-slate-500">{plan.note}</p><div className="mt-5 space-y-2 text-xs text-slate-400"><p className="flex items-center gap-2"><Check className="size-3 text-cyan-200" />Feature grouping concept</p><p className="flex items-center gap-2"><Check className="size-3 text-cyan-200" />No price displayed</p><p className="flex items-center gap-2"><X className="size-3 text-amber-200" />No entitlement proof</p></div></div>)}</div>}</CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Discovery surface preserved", description: "Premium categories, feature cards, search, selection, detail, plan concepts, local interest, and reset remain interactive.", icon: Sparkles, status: "Local catalog" }, { title: "No billing theater", description: "No price, checkout, payment, subscription, upgrade, refund, entitlement, or support claim is fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Access requires proof", description: "Production access needs identity, billing, entitlement issuance, authorization enforcement, and audit behavior.", icon: LockKeyhole, status: "Required" }]} /></main></div>;
}
