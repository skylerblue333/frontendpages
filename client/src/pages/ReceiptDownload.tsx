import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Check, Download, FileText, Filter, LockKeyhole, RefreshCw, Search, ShieldAlert, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const receipts = [{ id: 1, name: "Activity receipt", category: "Account", status: "Preview only", detail: "A local document concept awaiting authenticated record, ownership, and retention policy." }, { id: 2, name: "Learning completion receipt", category: "Education", status: "Unavailable", detail: "A document concept awaiting learner identity, completion evidence, and certificate authority." }, { id: 3, name: "Transaction receipt", category: "Financial", status: "Blocked", detail: "A high-risk document concept that must not imply payment, settlement, or blockchain finality." }, { id: 4, name: "Support case receipt", category: "Operations", status: "Unconnected", detail: "A case-document concept awaiting permission, redaction, audit, and export controls." }];
export default function ReceiptDownload() {
  const [query, setQuery] = useState(""); const [category, setCategory] = useState("All"); const [selected, setSelected] = useState(1); const [format, setFormat] = useState("Format not selected"); const [access, setAccess] = useState("Access not configured"); const [saved, setSaved] = useState(false); const [showGates, setShowGates] = useState(false);
  const categories = ["All", ...Array.from(new Set(receipts.map((item) => item.category)))]; const filtered = useMemo(() => receipts.filter((item) => (category === "All" || item.category === category) && `${item.name} ${item.category} ${item.detail}`.toLowerCase().includes(query.toLowerCase())), [category, query]); const receipt = receipts.find((item) => item.id === selected) ?? receipts[0];
  const reset = () => { setQuery(""); setCategory("All"); setSelected(1); setFormat("Format not selected"); setAccess("Access not configured"); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Download} eyebrow="ReceiptDownload · Document preview" title="Verify the record before offering the file." description="Explore local receipt-document concepts with search, categories, metadata, preview, access, format, redaction, export, download, retention, audit, and recovery gates. No live record, user, file, timestamp, amount, transaction, or download is connected." badge="Receipt workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "View saved locally" : "Save view locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review receipt gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset view</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Receipts", value: `${receipts.length} local`, hint: "No records loaded", icon: FileText, tone: "cyan" }, { label: "Files", value: "Unavailable", hint: "No storage source", icon: Download, tone: "violet" }, { label: "Metadata", value: "Unverified", hint: "No record source", icon: Filter, tone: "amber" }, { label: "Access", value: "Review needed", hint: "No identity source", icon: LockKeyhole, tone: "slate" }]} /><ScreenPreviewBanner title="Receipt evidence boundary"><strong>This is a local document and export preview, not evidence of a real record.</strong> Receipt labels, metadata posture, format and access intent, redaction, saved state, empty file state, and evidence gates are browser concepts. No user, record, amount, timestamp, payment, transaction, certificate, file, download, or audit event is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Search className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local receipt concepts" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-4 flex flex-wrap gap-2">{categories.map((entry) => <Button key={entry} onClick={() => setCategory(entry)} size="sm" variant="outline" className={category === entry ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-100" : "border-white/10 text-slate-400"}><Filter className="mr-1 size-3" />{entry}</Button>)}</div><div className="mt-6 space-y-3">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{item.name}</p><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.status}</Badge></div><div className="mt-4"><Badge variant="outline" className="border-white/10 text-slate-500">{item.category}</Badge></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected receipt concept</p><h2 className="mt-2 text-2xl font-black">{receipt.name}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{receipt.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Status", value: receipt.status }, { label: "Format", value: format }, { label: "Access", value: access }, { label: "Record", value: "Unavailable" }, { label: "Timestamp", value: "Unavailable" }, { label: "Audit", value: "Not connected" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2"><label className="text-sm text-slate-400">File format<select value={format} onChange={(event) => setFormat(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Format not selected</option><option>PDF intent</option><option>CSV intent</option><option>JSON intent</option></select></label><label className="text-sm text-slate-400">Access posture<select value={access} onChange={(event) => setAccess(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Access not configured</option><option>Owner-only intent</option><option>Admin-review intent</option><option>Support-share intent</option></select></label></div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-8 text-center"><FileText className="mx-auto size-7 text-slate-600" /><p className="mt-3 font-semibold">No receipt file loaded</p><p className="mt-2 text-sm text-slate-500">Connect a governed record and storage source before showing content, metadata, ownership, or download controls.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Preview unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Download unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Share unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No file or record claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A receipt concept does not prove that a record exists, a file was generated, or a download occurred.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Receipt gates</p><h2 className="mt-2 text-2xl font-black">What a real receipt download must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Record identity, ownership, authorization, tenant isolation, privacy, retention, deletion, and legal holds", "Source-of-truth data, timestamps, time zone, currency, rounding, signatures, status, and provenance", "File generation, templates, encoding, integrity, checksums, versioning, storage, expiry, and access logs", "Redaction, sensitive fields, permissions, share links, revocation, download limits, and abuse controls", "Export formats, localization, accessibility, support, retries, failure handling, and idempotency", "Audit, notifications, incident response, recovery, reconciliation, and user-visible status"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Receipt surface preserved", description: "Receipt concepts, search, categories, metadata, preview, access, format, redaction, export, download, retention, audit, save/reset, and gates remain interactive.", icon: FileText, status: "Local documents" }, { title: "No document theater", description: "Records, users, amounts, timestamps, payments, transactions, certificates, files, downloads, and audit events are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Source before export", description: "Real receipts need authenticated records, storage integrity, access controls, redaction, audit, and recovery.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
