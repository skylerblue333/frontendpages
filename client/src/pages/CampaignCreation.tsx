import { useState } from "react";
import { ArrowLeft, ArrowRight, BadgeCheck, Check, FileText, Flag, Megaphone, RefreshCw, ShieldAlert, Target, UserRound, WifiOff } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const STEPS = ["Identity", "Objective", "Audience", "Governance"];
export default function CampaignCreation() { const [step, setStep] = useState(0); const [name, setName] = useState("Preview campaign"); const [owner, setOwner] = useState("Owner not verified"); const [objective, setObjective] = useState("Education"); const [audience, setAudience] = useState("Not defined"); const [saved, setSaved] = useState(false); const [publish, setPublish] = useState(false); return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Megaphone} eyebrow="Growth · Campaign Setup" title="Set the rules before the campaign exists." description="Walk through identity, objective, audience, and governance in a local setup wizard. This page does not create a campaign, target a person, publish creative, spend budget, or report performance." badge="Preview campaign setup"><div className="flex flex-wrap gap-2"><Button onClick={() => { setSaved(true); setPublish(false); }} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><FileText className="mr-2 size-4" />Save local setup</Button><Button onClick={() => setPublish(true)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><Flag className="mr-2 size-4" />Review publish boundary</Button><Button onClick={() => { setStep(0); setSaved(false); setPublish(false); }} variant="ghost" className="text-slate-400 hover:text-white"><RefreshCw className="mr-2 size-4" />Reset</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Step", value: `${step + 1} / ${STEPS.length}`, hint: STEPS[step], icon: Target }, { label: "Setup", value: saved ? "Saved locally" : "Draft", hint: "No campaign record", icon: FileText, tone: "violet" }, { label: "Owner", value: "Unverified", hint: "No account binding", icon: UserRound, tone: "amber" }, { label: "Publish", value: "Unavailable", hint: "No publishing connector", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="Campaign setup evidence boundary">A wizard step is not a production campaign. Identity, objectives, audience, consent, creative claims, payment authority, distribution, measurement, and publishing need separate verified contracts and auditable state transitions.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.75fr_1.25fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Setup steps</p><div className="mt-5 space-y-2">{STEPS.map((item, index) => <button key={item} onClick={() => setStep(index)} className={`flex w-full items-center gap-3 rounded-xl border p-4 text-left ${step === index ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-black/15"}`}><span className={`flex size-7 items-center justify-center rounded-full text-xs ${index < step ? "bg-emerald-300/15 text-emerald-200" : index === step ? "bg-cyan-300/15 text-cyan-200" : "bg-white/5 text-slate-500"}`}>{index < step ? <Check className="size-4" /> : index + 1}</span><span className="flex-1 text-sm font-semibold">{item}</span>{index < step && <Badge variant="outline" className="border-emerald-300/20 text-emerald-200">Reviewed locally</Badge>}</button>)}</div><div className="mt-6 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex items-center gap-2 text-sm font-medium text-amber-200"><ShieldAlert className="size-4" />No live progress claim</div><p className="mt-2 text-xs leading-5 text-slate-400">Changing steps only changes this local preview. It does not create or update a server-side campaign.</p></div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">{STEPS[step]}</p><h2 className="mt-1 text-2xl font-black">{step === 0 ? "Name the brief" : step === 1 ? "Choose the question" : step === 2 ? "Describe the audience" : "Set the safety gates"}</h2></div><Badge variant="outline" className="border-cyan-300/20 text-cyan-200">Local step</Badge></div>{step === 0 && <div className="mt-6 space-y-4"><label className="block text-sm text-slate-300" htmlFor="campaign-name">Campaign name</label><Input id="campaign-name" value={name} onChange={(event) => { setName(event.target.value); setSaved(false); }} className="border-white/10 bg-black/20 text-white" /><label className="block text-sm text-slate-300" htmlFor="campaign-owner">Owner label</label><Input id="campaign-owner" value={owner} onChange={(event) => { setOwner(event.target.value); setSaved(false); }} className="border-white/10 bg-black/20 text-white" /><p className="text-xs text-slate-500">Typed identity labels do not authenticate an operator or establish responsibility.</p></div>}{step === 1 && <div className="mt-6 grid gap-3 sm:grid-cols-2">{["Education", "Awareness", "Consideration", "Conversion"].map((item) => <button key={item} onClick={() => { setObjective(item); setSaved(false); }} className={`rounded-xl border p-4 text-left ${objective === item ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-200" : "border-white/10 text-slate-400"}`}><Target className="size-5" /><p className="mt-3 font-semibold">{item}</p><p className="mt-1 text-xs text-slate-500">Requires a defined success event</p></button>)}</div>}{step === 2 && <div className="mt-6 space-y-3">{["Not defined", "Consented cohort placeholder", "Public educational audience", "Partner-supplied segment"].map((item) => <button key={item} onClick={() => { setAudience(item); setSaved(false); }} className={`flex w-full items-center gap-3 rounded-xl border p-4 text-left ${audience === item ? "border-violet-300/40 bg-violet-300/[0.08] text-violet-200" : "border-white/10 text-slate-400"}`}><UserRound className="size-5" /><span className="flex-1"><span className="block font-semibold">{item}</span><span className="mt-1 block text-xs text-slate-500">No eligibility or consent is inferred</span></span></button>)}</div>}{step === 3 && <div className="mt-6 space-y-3">{["Consent and targeting basis", "Creative claims review", "Budget and payment authority", "Measurement and stop criteria", "Privacy and retention review"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 bg-black/15 p-4"><ShieldAlert className="size-4 text-amber-300" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div>}<div className="mt-8 flex justify-between gap-3"><Button onClick={() => setStep((current) => Math.max(0, current - 1))} disabled={step === 0} variant="outline" className="border-white/15 text-white"><ArrowLeft className="mr-2 size-4" />Back</Button><Button onClick={() => setStep((current) => Math.min(STEPS.length - 1, current + 1))} disabled={step === STEPS.length - 1} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200">Next step<ArrowRight className="ml-2 size-4" /></Button></div>{saved && <div className="mt-5 rounded-xl border border-cyan-300/20 bg-cyan-300/[0.06] p-4"><p className="font-semibold text-cyan-200">Local setup saved</p><p className="mt-1 text-sm text-slate-400">{name} · {objective} · {audience}. No campaign record was created.</p></div>}{publish && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-200">Publishing unavailable</p><p className="mt-1 text-sm text-slate-400">No audience, creative, spend, message, or campaign status was submitted.</p></div>}</CardContent></Card></section><section className="grid gap-4 md:grid-cols-3"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><BadgeCheck className="size-5 text-cyan-300" /><h3 className="mt-3 font-semibold">Steps are contracts</h3><p className="mt-2 text-sm leading-6 text-slate-400">Each transition should validate the information and authority required for the next stage.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><Target className="size-5 text-violet-300" /><h3 className="mt-3 font-semibold">Objective is not outcome</h3><p className="mt-2 text-sm leading-6 text-slate-400">Selecting conversion does not prove a conversion event, customer, or revenue.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><WifiOff className="size-5 text-amber-300" /><h3 className="mt-3 font-semibold">No fake publishing</h3><p className="mt-2 text-sm leading-6 text-slate-400">A local wizard is not a live campaign, platform publish, payment, or message delivery.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Wizard with guardrails", description: "Separate identity, objective, audience, and governance rather than mixing them into an ambiguous form.", icon: Megaphone, status: "Pattern" }, { title: "Governance before activation", description: "Consent, claims, payment, measurement, and privacy gates belong before publish.", icon: ShieldAlert, status: "Required" }, { title: "No fake lifecycle", description: "Moving through steps is not campaign creation, launch, delivery, or performance.", icon: WifiOff, status: "Guardrail" }]} /></main></div>; }
