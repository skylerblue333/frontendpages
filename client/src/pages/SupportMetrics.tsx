import { useState } from "react";
import { Activity, BarChart3, Check, Clock3, Download, Filter, Info, LockKeyhole, RefreshCw, Search, ShieldAlert, Ticket, Users, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

const metrics = [
  { name: "Ticket volume concept", state: "Unmeasured", detail: "Requires authoritative ticket events, tenant scope, deduplication, and time-window rules." },
  { name: "Response-time concept", state: "Unverified", detail: "Requires timestamps, business-hours policy, timezone handling, and sampling rules." },
  { name: "Resolution concept", state: "Unavailable", detail: "Requires status transitions, reopen semantics, ownership, and audit history." },
  { name: "SLA concept", state: "Blocked", detail: "Requires an approved service contract, exclusions, clocks, and accountable review." },
];

export default function SupportMetrics() {
  const [query, setQuery] = useState("");
  const [range, setRange] = useState("Range not configured");
  const [segment, setSegment] = useState("All queues");
  const [saved, setSaved] = useState(false);
  const [showBoundary, setShowBoundary] = useState(false);
  const [refreshing, setRefreshing] = useState(false);
  const refresh = () => { setRefreshing(true); window.setTimeout(() => setRefreshing(false), 500); };
  const reset = () => { setQuery(""); setRange("Range not configured"); setSegment("All queues"); setSaved(false); setShowBoundary(false); setRefreshing(false); };
  const filtered = metrics.filter((metric) => `${metric.name} ${metric.state} ${metric.detail}`.toLowerCase().includes(query.toLowerCase()));
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={BarChart3} eyebrow="Support metrics · Operations preview" title="Define support KPIs without inventing service performance." description="Review a local support-analytics workspace with ticket volume, response, resolution, backlog, SLA, satisfaction, queues, filters, export intent, retry, save/reset, and evidence boundaries. No ticket, agent, user, SLA, or performance data is connected." badge="Evidence-bounded support analytics"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save metrics view"}</Button><Button onClick={() => setShowBoundary((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showBoundary ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showBoundary ? "Close evidence boundary" : "Review evidence gates"}</Button><Button onClick={refresh} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className={`mr-2 size-4 ${refreshing ? "animate-spin" : ""}`} />{refreshing ? "Retrying local state" : "Retry metrics"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><X className="mr-2 size-4" />Reset metrics</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Tickets", value: "Unmeasured", hint: "No event source", icon: Ticket, tone: "cyan" }, { label: "Response", value: "Unverified", hint: "No timestamp source", icon: Clock3, tone: "violet" }, { label: "Resolution", value: "Unavailable", hint: "No workflow source", icon: Check, tone: "amber" }, { label: "SLA", value: "Blocked", hint: "No contract source", icon: ShieldAlert, tone: "slate" }]} /><ScreenPreviewBanner title="Support metrics evidence boundary"><strong>This is a local KPI-design preview, not evidence that tickets, users, agents, queues, response times, resolution rates, backlog, satisfaction, SLA compliance, or support outcomes exist.</strong> Metric cards, filters, visual scaffolding, local saved state, retry feedback, and disabled query/export actions are browser concepts. No ticket volume, identity, timestamp, performance, contract, or business result is asserted.</ScreenPreviewBanner><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="grid gap-4 md:grid-cols-3"><label className="text-sm font-semibold text-slate-300"><span className="flex items-center gap-2"><Search className="size-4 text-slate-500" />Search metric concepts</span><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search tickets, SLA, response..." className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white outline-none" /></label><label className="text-sm font-semibold text-slate-300"><span className="flex items-center gap-2"><Filter className="size-4 text-slate-500" />Time range</span><select value={range} onChange={(event) => setRange(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white outline-none"><option>Range not configured</option><option>Last 7 days intent</option><option>Last 30 days intent</option><option>Quarter-to-date intent</option></select></label><label className="text-sm font-semibold text-slate-300"><span className="flex items-center gap-2"><Users className="size-4 text-slate-500" />Queue segment</span><select value={segment} onChange={(event) => setSegment(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm font-normal text-white outline-none"><option>All queues</option><option>Technical support intent</option><option>Billing support intent</option><option>Creator support intent</option></select></label></div></CardContent></Card><section className="grid gap-4 md:grid-cols-2">{filtered.map((metric) => <Card key={metric.name} className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="flex items-start justify-between"><div className="flex size-11 items-center justify-center rounded-2xl bg-cyan-300/10 text-cyan-200"><Activity className="size-5" /></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{metric.state}</Badge></div><h2 className="mt-4 font-bold">{metric.name}</h2><p className="mt-3 text-sm leading-6 text-slate-400">{metric.detail}</p><p className="mt-4 text-xs text-slate-500">No source · definition required</p></CardContent></Card>)}</section><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-200">Trend scaffold</p><h2 className="mt-2 text-2xl font-black">No support series loaded</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Unmeasured</Badge></div><div className="mt-7 flex h-40 items-end gap-3">{[38, 54, 43, 65, 48, 72, 56, 61, 45, 68, 51, 58].map((height, index) => <div key={index} className="flex-1 rounded-t-lg bg-gradient-to-t from-cyan-400/25 to-violet-400/70" style={{ height: `${height}%` }} />)}</div><p className="mt-5 text-sm leading-6 text-slate-500">Bars are visual scaffolding, not ticket counts, rates, trends, forecasts, staffing requirements, or SLA results.</p></CardContent></Card><ScreenStatePanel type="empty" title="No support source connected" description="Connect authorized ticket events, identity and tenant scope, queue definitions, business calendars, SLA contracts, privacy controls, and audit data before rendering support metrics." /></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><Info className="size-5 text-cyan-300" /><div><p className="font-bold">Support metric contract checklist</p><p className="mt-1 text-sm text-slate-400">Every KPI needs a source, definition, population, clock, timezone, aggregation rule, privacy basis, freshness, error handling, and reproducible query.</p></div></div><div className="mt-5 grid gap-3 md:grid-cols-2">{["Ticket event provenance and deduplication", "Response and resolution timestamp semantics", "Queue, tenant, agent, and cohort scope", "Business calendars, timezone, and SLA clock rules", "Privacy, retention, access, and redaction controls", "Freshness, reconciliation, export, and audit review"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3 text-sm text-slate-300"><LockKeyhole className="size-4 text-amber-300" />{item}<span className="ml-auto text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><div className="flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Load live metrics unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500"><Download className="mr-2 size-4" />Export unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Compare queues unavailable</Button></div>{showBoundary && <Card className="border-amber-300/20 bg-amber-300/[0.06]"><CardContent className="p-6"><p className="font-semibold text-amber-100">No support-performance claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local KPI panel does not prove ticket volume, response time, resolution, backlog, satisfaction, staffing need, SLA compliance, or customer outcome.</p></CardContent></Card>}<ScreenFeatureGrid features={[{ title: "Support metrics surface preserved", description: "Tickets, response, resolution, backlog, SLA, satisfaction, queues, filters, trends, export intent, retry, save/reset, and metric contracts remain visible.", icon: BarChart3, status: "Local support" }, { title: "No KPI theater", description: "Counts, times, rates, backlog, satisfaction, staffing, SLA, and customer outcomes are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Evidence before reporting", description: "Real support metrics require ticket provenance, clock rules, privacy, contract semantics, freshness, reconciliation, and audit.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
