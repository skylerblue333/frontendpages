import { useMemo, useState } from "react";
import { Activity, ArrowDown, ArrowRight, Check, CheckCircle2, ChevronDown, ChevronRight, CircleDashed, Clock3, Code2, Database, DollarSign, FileCheck2, Layers, LockKeyhole, MessageSquare, Network, Rss, Shield, Sparkles, Target, Timer, Unplug, WalletCards, WifiOff, Zap } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid, ScreenStatePanel } from "@/components/ScreenExperience";

type Phase = { phase: number; title: string; goal: string; dependency: string; evidence: string[]; icon: typeof Database; color: string; status: "Reference sequence" | "In review" | "Unverified" };
const PHASES: Phase[] = [
  { phase: 1, title: "Core foundation", goal: "Identity, data boundaries, and observability exist before feature complexity.", dependency: "None", evidence: ["Auth contract", "Schema and migrations", "Error logging", "Environment template"], icon: Database, color: "text-cyan-300", status: "Reference sequence" },
  { phase: 2, title: "Communication", goal: "Users can exchange information with clear persistence and failure states.", dependency: "Core foundation", evidence: ["Message contract", "Delivery states", "Moderation controls", "Retry policy"], icon: MessageSquare, color: "text-blue-300", status: "Reference sequence" },
  { phase: 3, title: "Content surfaces", goal: "Feed and content retrieval are measurable, typed, and honest about freshness.", dependency: "Communication", evidence: ["Query contract", "Pagination", "Empty and error states", "Cache correctness"], icon: Rss, color: "text-cyan-300", status: "Reference sequence" },
  { phase: 4, title: "AI layer", goal: "AI is introduced after input, output, privacy, cost, and fallback boundaries are explicit.", dependency: "Content surfaces", evidence: ["Model policy", "Prompt boundary", "Safety fallback", "Usage telemetry"], icon: Sparkles, color: "text-violet-300", status: "In review" },
  { phase: 5, title: "Action engine", goal: "Actions become auditable state transitions rather than optimistic buttons.", dependency: "AI layer", evidence: ["Authorization", "Idempotency", "Audit event", "Failure recovery"], icon: Zap, color: "text-amber-300", status: "Unverified" },
  { phase: 6, title: "Realtime sync", goal: "Live updates are added only after canonical state and reconnect behavior are proven.", dependency: "Action engine", evidence: ["Event schema", "Reconnect policy", "Ordering", "Backfill strategy"], icon: Activity, color: "text-yellow-300", status: "Unverified" },
  { phase: 7, title: "Simulation", goal: "Synthetic worlds and automation are separated from production facts and user actions.", dependency: "Realtime sync", evidence: ["Synthetic labels", "Pause controls", "Quota limits", "Deterministic replay"], icon: Network, color: "text-cyan-300", status: "Unverified" },
  { phase: 8, title: "Money and custody", goal: "Financial workflows follow identity, authorization, settlement, and reconciliation proof.", dependency: "Action engine", evidence: ["Payment provider", "Custody boundary", "Ledger", "Refund and dispute policy"], icon: DollarSign, color: "text-emerald-300", status: "Unverified" },
  { phase: 9, title: "Unified product shell", goal: "Multiple surfaces share navigation, design tokens, and state conventions without losing context.", dependency: "Content surfaces", evidence: ["Route inventory", "Design system", "Accessibility audit", "Visual regression"], icon: Layers, color: "text-violet-300", status: "In review" },
  { phase: 10, title: "Scale readiness", goal: "Optimize what has evidence, with rate limits, monitoring, recovery, and deployment proof.", dependency: "All proven phases", evidence: ["Load profile", "Rate limits", "SLO definitions", "Rollback runbook"], icon: Shield, color: "text-rose-300", status: "Unverified" },
];

export default function BuildOrder() { const [selectedPhase, setSelectedPhase] = useState(1); const [showEvidence, setShowEvidence] = useState(false); const selected = PHASES.find((phase) => phase.phase === selectedPhase) ?? PHASES[0]; const provenCount = 0; const reviewCount = PHASES.filter((phase) => phase.status === "In review").length; const readiness = Math.round((provenCount / PHASES.length) * 100); const next = PHASES.find((phase) => phase.phase === selectedPhase + 1); const setPhase = (phase: number) => { setSelectedPhase(phase); setShowEvidence(false); }; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Layers} eyebrow="Engineering Planning · Dependency Map" title="Sequence the work that makes the next screen trustworthy." description="Explore an illustrative dependency order for platform capabilities. This page does not claim that phases are complete, that timelines are committed, or that production infrastructure exists without engineering evidence." badge="Preview build order"><div className="flex flex-wrap gap-2"><Button onClick={() => setShowEvidence(!showEvidence)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200">{showEvidence ? <><ChevronDown className="mr-2 size-4" />Hide evidence gates</> : <><FileCheck2 className="mr-2 size-4" />Show evidence gates</>}</Button><Button onClick={() => setPhase(next?.phase ?? 1)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">Next dependency <ArrowRight className="ml-2 size-4" /></Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Dependency phases", value: String(PHASES.length), hint: "Illustrative sequence", icon: Layers }, { label: "Proven complete", value: String(provenCount), hint: "No phase claim made", icon: CircleDashed, tone: "amber" }, { label: "In review", value: String(reviewCount), hint: "Needs engineering proof", icon: Clock3, tone: "violet" }, { label: "Readiness", value: `${readiness}%`, hint: "Evidence-based only", icon: Shield, tone: "slate" }]} /><ScreenPreviewBanner title="Build-order evidence boundary">The sequence below is a planning reference, not a project status report. Phase labels, dependencies, goals, and evidence gates are useful for frontend review; they do not prove that a backend, API, payment rail, realtime system, AI service, wallet, or deployment is complete.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.9fr_1.1fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="mb-4 flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Dependency sequence</p><h2 className="mt-1 text-xl font-bold">Select a phase</h2></div><Target className="size-5 text-cyan-300" /></div><div className="space-y-2">{PHASES.map((phase, index) => { const Icon = phase.icon; return <button key={phase.phase} onClick={() => setPhase(phase.phase)} className={`relative flex w-full items-center gap-3 rounded-xl border p-4 text-left transition ${selectedPhase === phase.phase ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-black/15 hover:bg-white/[0.05]"}`}><span className={`flex size-8 shrink-0 items-center justify-center rounded-full border text-xs font-bold ${selectedPhase === phase.phase ? "border-cyan-300 bg-cyan-300 text-slate-950" : "border-white/15 text-slate-500"}`}>{phase.phase}</span><Icon className={`size-4 ${phase.color}`} /><span className="flex-1"><span className="block text-sm font-semibold">{phase.title}</span><span className="mt-1 block text-xs text-slate-500">Depends on: {phase.dependency}</span></span><Badge variant="outline" className="border-amber-300/20 text-amber-200">{phase.status}</Badge>{index < PHASES.length - 1 && <span className="pointer-events-none absolute -bottom-3 left-7 z-10 h-4 border-l border-white/15" />}</button>; })}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Phase {selected.phase}</p><h2 className="mt-2 text-3xl font-black">{selected.title}</h2><p className="mt-3 max-w-2xl text-lg leading-8 text-slate-300">{selected.goal}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{selected.status}</Badge></div><div className="mt-7 rounded-xl border border-white/10 bg-black/15 p-5"><div className="flex items-center gap-3"><ArrowDown className="size-4 text-cyan-300" /><div><p className="text-xs uppercase tracking-wider text-slate-500">Dependency</p><p className="mt-1 font-semibold">{selected.dependency}</p></div></div></div><div className="mt-6 flex flex-wrap gap-2"><Button onClick={() => setShowEvidence(!showEvidence)} variant="outline" className="border-white/15 text-white hover:bg-white/10"><FileCheck2 className="mr-2 size-4" />{showEvidence ? "Hide" : "View"} evidence gates</Button><Button onClick={() => setPhase(Math.max(1, selected.phase - 1))} disabled={selected.phase === 1} variant="ghost" className="text-slate-400">Previous</Button><Button onClick={() => setPhase(Math.min(PHASES.length, selected.phase + 1))} disabled={selected.phase === PHASES.length} variant="ghost" className="text-cyan-300">Next phase <ChevronRight className="ml-1 size-4" /></Button></div>{showEvidence && <div className="mt-6 grid gap-3 sm:grid-cols-2">{selected.evidence.map((item) => <div key={item} className="rounded-lg border border-white/10 bg-black/15 p-4"><CheckCircle2 className="size-4 text-emerald-300" /><p className="mt-3 text-sm text-slate-300">{item}</p><p className="mt-1 text-xs text-slate-500">Evidence required</p></div>)}</div>}</CardContent></Card></section><section className="grid gap-4 md:grid-cols-3"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><Database className="size-5 text-cyan-300" /><h3 className="mt-3 font-semibold">Dependency before polish</h3><p className="mt-2 text-sm leading-6 text-slate-400">A beautiful surface cannot compensate for missing identity, contracts, persistence, or failure handling.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><Code2 className="size-5 text-violet-300" /><h3 className="mt-3 font-semibold">Proof before status</h3><p className="mt-2 text-sm leading-6 text-slate-400">Mark a phase complete only when tests, browser evidence, integration behavior, and documentation agree.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><WalletCards className="size-5 text-amber-300" /><h3 className="mt-3 font-semibold">High-risk last</h3><p className="mt-2 text-sm leading-6 text-slate-400">Money, custody, automation, and realtime claims need stronger proof than a rendered page.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "No invented timelines", description: "Dates and day ranges are omitted because no committed plan or staffing evidence was provided.", icon: Timer, status: "Guardrail" }, { title: "Dependencies stay visible", description: "Each phase names the capability it relies on so later polish does not mask earlier gaps.", icon: Network, status: "Pattern" }, { title: "Completion is earned", description: "A status label is a UX state until tests, contracts, and operational evidence establish the underlying claim.", icon: Shield, status: "Required" }]} /></main></div>; }
