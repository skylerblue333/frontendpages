import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ClipboardList, FileText, LockKeyhole, MessageSquare, Plus, RefreshCw, Search, ShieldAlert, Truck, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
type Request = { id: number; title: string; category: string; lines: number; status: string; detail: string };
const initial: Request[] = [{ id: 1, title: "Compute capacity request", category: "Infrastructure", lines: 3, status: "Draft", detail: "A local request structure for comparing capacity requirements without supplier quotes." }, { id: 2, title: "Learning-content services", category: "Education", lines: 2, status: "Needs review", detail: "A sourcing brief awaiting scope, evaluation criteria, and procurement approval." }, { id: 3, title: "Community event supplies", category: "Operations", lines: 4, status: "Blocked", detail: "A planning draft that must not imply inventory, availability, or purchase commitment." }];
export default function RFQSystem() {
  const [requests, setRequests] = useState(initial);
  const [query, setQuery] = useState("");
  const [selected, setSelected] = useState(1);
  const [saved, setSaved] = useState(false);
  const [showGates, setShowGates] = useState(false);
  const [deadline, setDeadline] = useState("Deadline not set");
  const [approval, setApproval] = useState("Approval not configured");
  const filtered = useMemo(() => requests.filter((item) => `${item.title} ${item.category} ${item.detail}`.toLowerCase().includes(query.toLowerCase())), [query, requests]);
  const request = requests.find((item) => item.id === selected) ?? requests[0];
  const create = () => { const id = Math.max(...requests.map((item) => item.id), 0) + 1; const item: Request = { id, title: `Local RFQ draft ${id}`, category: "Unassigned", lines: 1, status: "Draft", detail: "A browser-only quote request awaiting scope and supplier governance." }; setRequests((current) => [...current, item]); setSelected(id); };
  const reset = () => { setRequests(initial); setQuery(""); setSelected(1); setSaved(false); setShowGates(false); setDeadline("Deadline not set"); setApproval("Approval not configured"); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={ClipboardList} eyebrow="RFQSystem · Sourcing preview" title="Define the request before soliciting the quote." description="Explore local request-for-quote drafts with supplier search, line items, quantities, totals, approvals, messages, attachments, deadlines, export, and evidence gates. No vendors, prices, inventory, tax, orders, or commitments are connected." badge="RFQ workspace"><div className="flex flex-wrap gap-2"><Button onClick={create} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Plus className="mr-2 size-4" />New local request</Button><Button onClick={() => setSaved(true)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{saved ? "View saved locally" : "Save request locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review RFQ gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset workspace</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Requests", value: `${requests.length} local`, hint: "Not submitted", icon: ClipboardList, tone: "cyan" }, { label: "Suppliers", value: "Unknown", hint: "No directory", icon: Truck, tone: "violet" }, { label: "Pricing", value: "Unavailable", hint: "No quotes", icon: FileText, tone: "amber" }, { label: "Commitments", value: "None", hint: "No purchase action", icon: LockKeyhole, tone: "slate" }]} /><ScreenPreviewBanner title="RFQ evidence boundary"><strong>This is a local sourcing-request preview, not a procurement system.</strong> Drafts, line items, quantities, totals posture, approval intent, deadline, saved state, and empty supplier states are browser concepts. No vendor, quote, price, tax, currency, inventory, availability, purchase order, contract, delivery, or financial commitment is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Search className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local RFQ drafts" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-6 space-y-3">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{item.title}</p><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.status}</Badge></div><div className="mt-4 flex gap-2"><Badge variant="outline" className="border-white/10 text-slate-500">{item.category}</Badge><Badge variant="outline" className="border-white/10 text-slate-500">{item.lines} line-item concepts</Badge></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected request</p><h2 className="mt-2 text-2xl font-black">{request.title}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{request.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Category", value: request.category }, { label: "Line items", value: `${request.lines} local concepts` }, { label: "Suppliers", value: "Unknown" }, { label: "Quote total", value: "Unavailable" }, { label: "Tax/shipping", value: "Not configured" }, { label: "Approval", value: approval }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2"><label className="text-sm text-slate-400">Response deadline<select value={deadline} onChange={(event) => setDeadline(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Deadline not set</option><option>Target date intent</option><option>7-day response intent</option><option>30-day response intent</option></select></label><label className="text-sm text-slate-400">Approval posture<select value={approval} onChange={(event) => setApproval(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Approval not configured</option><option>Review required</option><option>Budget owner intent</option><option>Procurement review intent</option></select></label></div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-8 text-center"><Truck className="mx-auto size-7 text-slate-600" /><p className="mt-3 font-semibold">No supplier responses loaded</p><p className="mt-2 text-sm text-slate-500">Connect an approved supplier directory and quote workflow before comparing offers.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Send RFQ unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Export unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500"><MessageSquare className="mr-2 size-4" />Messages unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No procurement claim</p><p className="mt-2 text-sm leading-6 text-slate-400">A local RFQ draft does not contact suppliers, establish a quote, reserve inventory, calculate a payable total, create an order, or commit funds.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">RFQ gates</p><h2 className="mt-2 text-2xl font-black">What a real quote workflow must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Request ownership, supplier permissions, scope, specifications, units, quantities, and attachments", "Supplier identity, sanctions/vendor screening, contact controls, response authenticity, and confidentiality", "Price, currency, tax, shipping, lead time, availability, warranty, terms, expiry, and assumptions", "Budget, approval thresholds, segregation of duties, conflicts, audit, and purchase policy", "Messages, revisions, deadlines, receipts, retries, disputes, and immutable activity history", "Selection, purchase order, contract, delivery, invoice, payment, cancellation, and rollback"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Sourcing surface preserved", description: "RFQ drafts, search, line items, quantities, totals posture, approvals, messages, attachments, deadlines, export, save/reset, and gates remain interactive.", icon: ClipboardList, status: "Local requests" }, { title: "No purchasing theater", description: "Vendors, quotes, prices, inventory, tax, currency, orders, deliveries, contracts, payments, and commitments are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Governance before solicitation", description: "Real RFQs need supplier controls, budget approval, quote authenticity, audit, terms, and rollback.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
