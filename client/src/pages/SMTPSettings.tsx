import { useMemo, useState } from "react";
import { Check, Filter, KeyRound, LockKeyhole, Mail, RefreshCw, Send, ShieldAlert, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const settings = [{ id: 1, name: "Transactional mail provider", category: "Provider", detail: "A local provider concept requiring server-side credentials, sender identity, TLS, delivery events, retries, bounce handling, privacy, and support.", state: "Unconfigured" }, { id: 2, name: "Sender identity", category: "Sender", detail: "A sender concept requiring verified domain, authentication records, purpose, reply handling, localization, and approval.", state: "Needs evidence" }, { id: 3, name: "Delivery and bounce webhooks", category: "Delivery", detail: "A webhook concept requiring signature verification, replay protection, idempotency, status mapping, retries, and secret rotation.", state: "Blocked" }, { id: 4, name: "Email privacy policy", category: "Privacy", detail: "A privacy concept requiring recipient provenance, purpose, consent semantics, retention, deletion, export, and lawful-access handling.", state: "Unconnected" }];
export default function SMTPSettings() { const [query, setQuery] = useState(""); const [category, setCategory] = useState("All"); const [selected, setSelected] = useState(1); const [transport, setTransport] = useState("Transport not configured"); const [credential, setCredential] = useState("Credential boundary not configured"); const [saved, setSaved] = useState(false); const [showGates, setShowGates] = useState(false); const categories = ["All", ...Array.from(new Set(settings.map((item) => item.category)))]; const filtered = useMemo(() => settings.filter((item) => (category === "All" || item.category === category) && `${item.name} ${item.category} ${item.detail}`.toLowerCase().includes(query.toLowerCase())), [category, query]); const setting = settings.find((item) => item.id === selected) ?? settings[0]; const reset = () => { setQuery(""); setCategory("All"); setSelected(1); setTransport("Transport not configured"); setCredential("Credential boundary not configured"); setSaved(false); setShowGates(false); }; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Mail} eyebrow="SMTP settings · Email connectivity preview" title="Secure the transport before sending a message." description="Explore local provider, sender, delivery, and privacy email-integration concepts with search, category filters, transport and credential intent, TLS and bounce gates, save/reset, and blocked connection/test-send actions. No provider, credential, mailbox, sender, recipient, email, delivery, bounce, or compliance outcome is connected." badge="Email governance workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Saved locally" : "Save setting locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review email gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset setting</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Settings", value: `${settings.length} local`, hint: "No provider source", icon: Mail, tone: "cyan" }, { label: "Transport", value: "Unconfigured", hint: "No SMTP source", icon: Send, tone: "violet" }, { label: "Credentials", value: "Blocked", hint: "Server boundary required", icon: KeyRound, tone: "amber" }, { label: "Delivery", value: "Unavailable", hint: "No webhook source", icon: ShieldAlert, tone: "slate" }]} /><ScreenPreviewBanner title="Email-integration evidence boundary"><strong>This is a local SMTP-governance preview, not evidence that an email provider, secret, sender, recipient, mailbox, or delivery event exists.</strong> Setting cards, filters, transport and credential intent, saved state, TLS and bounce gates, and disabled connection/test-send actions are browser concepts. No host, username, password, API key, mailbox, email, delivery, bounce, spam, cost, or compliance outcome is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Filter className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local SMTP settings" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-4 flex flex-wrap gap-2">{categories.map((entry) => <Button key={entry} onClick={() => setCategory(entry)} size="sm" variant="outline" className={category === entry ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-100" : "border-white/10 text-slate-400"}>{entry}</Button>)}</div><div className="mt-6 space-y-3">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{item.name}</p><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{item.state}</Badge></div><div className="mt-4"><Badge variant="outline" className="border-white/10 text-slate-500">{item.category}</Badge></div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected email setting concept</p><h2 className="mt-2 text-2xl font-black">{setting.name}</h2></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">{setting.state}</Badge></div><p className="mt-2 text-sm leading-6 text-slate-400">{setting.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Category", value: setting.category }, { label: "Transport", value: transport }, { label: "Credential", value: credential }, { label: "Sender", value: "Unconfigured" }, { label: "TLS", value: "Required" }, { label: "Bounce", value: "Required" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2"><label className="text-sm text-slate-400">Transport intent<select value={transport} onChange={(event) => setTransport(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Transport not configured</option><option>SMTP intent</option><option>Provider API intent</option><option>Managed connector intent</option><option>Test transport intent</option></select></label><label className="text-sm text-slate-400">Credential boundary<select value={credential} onChange={(event) => setCredential(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 text-sm text-white outline-none"><option>Credential boundary not configured</option><option>Server-side secret intent</option><option>Managed secret intent</option><option>Rotation-required intent</option><option>Least-privilege intent</option></select></label></div><div className="mt-6 rounded-2xl border border-dashed border-white/10 p-8 text-center"><Mail className="mx-auto size-8 text-slate-600" /><p className="mt-3 font-semibold">No email integration evidence loaded</p><p className="mt-2 text-sm text-slate-500">Connect governed provider configuration, server-side secrets, TLS, sender verification, delivery and bounce webhooks, retries, privacy, rate limits, monitoring, and audit before connecting.</p></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400">Connect unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Verify unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Send test unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500">Rotate unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No provider or delivery claim</p><p className="mt-2 text-sm leading-6 text-slate-400">An email setting concept does not prove a provider account, secret, sender, mailbox, recipient, message, delivery, bounce, spam, cost, security, or compliance.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">SMTP governance gates</p><h2 className="mt-2 text-2xl font-black">What a real email integration must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Server-side provider credentials, sender identity, domain authentication, tenant, purpose, secret rotation, and environment separation", "TLS, certificate validation, least privilege, provider API/SMTP semantics, timeout, retry, idempotency, and failure handling", "Delivery, bounce, complaint, unsubscribe, suppression, cost, provider status, callbacks, support, monitoring, and recovery", "Recipient provenance, consent, purpose, privacy, sensitive data, retention, deletion, export, lawful communications, and audit", "Transactional, marketing, education, community, financial, security, and user-impact claims require domain review", "Connect, test send, rotate, disconnect, rollback, notifications, accessibility, localization, and accountable approval"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Email settings surface preserved", description: "Provider, sender, delivery, privacy settings, filters, transport, credentials, TLS, bounces, verification, test send, rotation, save/reset, and gates remain interactive.", icon: Mail, status: "Local settings" }, { title: "No secret theater", description: "Providers, credentials, senders, mailboxes, recipients, messages, deliveries, costs, security, and compliance are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Server boundary before connect", description: "Real email integration requires server-side secret handling, TLS, provider evidence, signed callbacks, privacy, suppression, rate limits, and audit.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>; }
