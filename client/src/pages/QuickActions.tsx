import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Check, Command, ExternalLink, Filter, Keyboard, LockKeyhole, Play, RefreshCw, Search, ShieldAlert, Star, X } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";
const initial = [{ id: 1, name: "Open portfolio review", category: "Navigation", risk: "Low", detail: "Open a local review surface; no account or portfolio data is loaded." }, { id: 2, name: "Review evidence gates", category: "Governance", risk: "Low", detail: "Show a local checklist for claims before exposure." }, { id: 3, name: "Export local draft", category: "Workspace", risk: "Medium", detail: "Would require a real file and storage contract; dry-run only." }, { id: 4, name: "Connect wallet", category: "Financial", risk: "High", detail: "Requires explicit custody, network, identity, and transaction controls." }];
export default function QuickActions() {
  const [query, setQuery] = useState("");
  const [category, setCategory] = useState("All");
  const [selected, setSelected] = useState(1);
  const [favorites, setFavorites] = useState<number[]>([]);
  const [dryRun, setDryRun] = useState(true);
  const [saved, setSaved] = useState(false);
  const [showGates, setShowGates] = useState(false);
  const categories = ["All", ...Array.from(new Set(initial.map((item) => item.category)))];
  const filtered = useMemo(() => initial.filter((item) => (category === "All" || item.category === category) && `${item.name} ${item.category} ${item.detail}`.toLowerCase().includes(query.toLowerCase())), [category, query]);
  const action = initial.find((item) => item.id === selected) ?? initial[0];
  const toggleFavorite = () => setFavorites((current) => current.includes(action.id) ? current.filter((id) => id !== action.id) : [...current, action.id]);
  const reset = () => { setQuery(""); setCategory("All"); setSelected(1); setFavorites([]); setDryRun(true); setSaved(false); setShowGates(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Command} eyebrow="QuickActions · Action preview" title="Confirm the action before claiming the outcome." description="Explore a local launchpad with action search, categories, favorites, risk labels, permissions, confirmation, dry-run posture, shortcuts, and history boundaries. No account, device, wallet, payment, external service, destructive action, or automation is connected." badge="Action workspace"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Launchpad saved locally" : "Save launchpad locally"}</Button><Button onClick={() => setShowGates((value) => !value)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">{showGates ? <X className="mr-2 size-4" /> : <ShieldAlert className="mr-2 size-4" />}{showGates ? "Close gates" : "Review action gates"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset launchpad</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Actions", value: `${initial.length} local`, hint: "No execution", icon: Command, tone: "cyan" }, { label: "Favorites", value: `${favorites.length}`, hint: "Browser only", icon: Star, tone: "violet" }, { label: "Mode", value: dryRun ? "Dry-run" : "Review", hint: "No side effects", icon: Play, tone: "amber" }, { label: "Permissions", value: "Unknown", hint: "No account source", icon: LockKeyhole, tone: "slate" }]} /><ScreenPreviewBanner title="Action evidence boundary"><strong>This is a local action-launchpad preview, not an execution engine.</strong> Search, category, favorite, risk, dry-run, confirmation, shortcut, selected state, and saved state are browser concepts. No navigation, account mutation, wallet connection, payment, device action, file export, external request, destructive operation, notification, or automation is asserted.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Search className="absolute left-3 top-3 size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search local quick actions" className="w-full rounded-xl border border-white/10 bg-black/20 py-3 pl-10 pr-3 text-sm text-white outline-none" /></div><div className="mt-4 flex flex-wrap gap-2">{categories.map((entry) => <Button key={entry} onClick={() => setCategory(entry)} size="sm" variant="outline" className={category === entry ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-100" : "border-white/10 text-slate-400"}><Filter className="mr-1 size-3" />{entry}</Button>)}</div><div className="mt-6 space-y-3">{filtered.map((item) => <button key={item.id} onClick={() => setSelected(item.id)} className={`w-full rounded-xl border p-4 text-left ${selected === item.id ? "border-cyan-300/40 bg-cyan-300/[0.06]" : "border-white/10"}`}><div className="flex items-start justify-between gap-2"><div><p className="font-semibold">{item.name}</p><p className="mt-1 text-sm leading-6 text-slate-500">{item.detail}</p></div><Badge variant="outline" className={item.risk === "High" ? "border-rose-300/20 text-rose-200" : "border-amber-300/20 text-amber-200"}>{item.risk} risk</Badge></div><Badge variant="outline" className="mt-4 border-white/10 text-slate-500">{item.category}</Badge></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Selected action</p><h2 className="mt-2 text-2xl font-black">{action.name}</h2></div><Button onClick={toggleFavorite} variant="outline" size="icon" className="border-white/10 text-amber-200"><Star className={favorites.includes(action.id) ? "fill-current" : ""} /></Button></div><p className="mt-2 text-sm leading-6 text-slate-400">{action.detail}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{[{ label: "Category", value: action.category }, { label: "Risk", value: `${action.risk} · unassessed` }, { label: "Permission", value: "Unknown" }, { label: "Confirmation", value: "Required" }, { label: "History", value: "Empty preview" }, { label: "Side effects", value: "Blocked" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 flex items-center justify-between rounded-xl border border-white/10 p-4"><div><p className="font-semibold">Dry-run mode</p><p className="mt-1 text-sm text-slate-500">Preview only; no side effects</p></div><button onClick={() => setDryRun((value) => !value)} className={`h-6 w-11 rounded-full p-1 ${dryRun ? "bg-cyan-300" : "bg-slate-700"}`}><span className={`block size-4 rounded-full bg-slate-950 transition-transform ${dryRun ? "translate-x-5" : ""}`} /></button></div><div className="mt-5 flex flex-wrap gap-2"><Button disabled className="bg-slate-700 text-slate-400"><Play className="mr-2 size-4" />Execute unavailable</Button><Button disabled variant="outline" className="border-white/10 text-slate-500"><ExternalLink className="mr-2 size-4" />External action unavailable</Button></div>{showGates && <div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-100">No side-effect claim</p><p className="mt-2 text-sm leading-6 text-slate-400">An action card does not grant permission or perform navigation, payments, wallet actions, device changes, exports, or deletion.</p></div>}</CardContent></Card></section><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Action gates</p><h2 className="mt-2 text-2xl font-black">What a real action must prove</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Authenticated user, role, permission, scope, confirmation, and accessible error state", "Target, input validation, idempotency, authorization, and audit event", "Financial, wallet, device, file, notification, external, and destructive safety review", "Loading, success, failure, retry, cancellation, timeout, rollback, and recovery", "Privacy, consent, rate limits, secrets, network policy, and provider boundaries", "History, observability, support, incident response, and user-visible result"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><LockKeyhole className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><ScreenFeatureGrid features={[{ title: "Action surface preserved", description: "Search, categories, favorites, risk, permissions, confirmation, dry-run, shortcuts, history, save/reset, and blocked execution remain interactive.", icon: Command, status: "Local launchpad" }, { title: "No execution theater", description: "Accounts, devices, wallets, payments, files, external requests, destructive actions, and automation are not fabricated.", icon: ShieldAlert, status: "Guardrail" }, { title: "Proof before side effects", description: "Real actions need authorization, validation, loading, result, audit, privacy, rollback, support, and recovery.", icon: LockKeyhole, status: "Blocked" }]} /></main></div>;
}
