import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { AlertTriangle, Ban, Check, Eye, EyeOff, Flag, Heart, LockKeyhole, MessageCircle, RefreshCw, Search, Shield, ShieldCheck, UserRound, Users, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const POSTS = [{ creator: "ShadowCreator", title: "Studio lighting study", tier: "Preview", mode: "SFW preview", detail: "Non-explicit synthetic card for testing visibility and creator-safety UX." }, { creator: "CryptoMuse", title: "Behind-the-scenes notes", tier: "Preview", mode: "SFW preview", detail: "A local creator concept with no real media, account, or engagement data." }, { creator: "NightOwl", title: "Late-night color study", tier: "Review", mode: "Content review", detail: "Synthetic moderation fixture; no adult content is displayed or linked." }, { creator: "PixelDreamer", title: "Digital art series", tier: "Preview", mode: "Art preview", detail: "A non-explicit artwork concept with rights and provenance unverified." }, { creator: "MidnightMuse", title: "Creator workspace", tier: "Restricted", mode: "Policy gate", detail: "A local policy-gate card, not a paid or private post." }, { creator: "BlueHour", title: "Community guidelines", tier: "Safety", mode: "Safety note", detail: "Synthetic safety education card for reporting and consent review." }];
const FILTERS = ["All", "SFW preview", "Content review", "Art preview", "Policy gate", "Safety note"];

export default function NSFWFeed() {
  const [consent, setConsent] = useState(false);
  const [filter, setFilter] = useState("All");
  const [query, setQuery] = useState("");
  const [selected, setSelected] = useState(POSTS[0].title);
  const [saved, setSaved] = useState<string[]>([]);
  const [reported, setReported] = useState<string[]>([]);
  const [blocked, setBlocked] = useState<string[]>([]);
  const [safeMode, setSafeMode] = useState(true);
  const post = POSTS.find((item) => item.title === selected) ?? POSTS[0];
  const visible = useMemo(() => POSTS.filter((item) => (filter === "All" || item.mode === filter) && `${item.creator} ${item.title} ${item.mode} ${item.detail}`.toLowerCase().includes(query.toLowerCase())), [filter, query]);
  const toggle = (list: string[], value: string, setter: (next: string[]) => void) => setter(list.includes(value) ? list.filter((item) => item !== value) : [...list, value]);
  const reset = () => { setConsent(false); setFilter("All"); setQuery(""); setSelected(POSTS[0].title); setSaved([]); setReported([]); setBlocked([]); setSafeMode(true); };
  return <div className="min-h-screen bg-[#09060b] text-white"><ScreenHero icon={Shield} eyebrow="NSFWFeed · Consent and safety preview" title="Design the safety layer before showing a feed." description="Review non-explicit synthetic creator cards, consent and visibility states, content-policy filters, and local report/block controls. No adult media, age assurance, performer verification, paid content, engagement counts, recommendation ranking, or revenue claim is made." badge="Safety preview"><div className="flex flex-wrap gap-2"><Button onClick={() => setConsent(true)} className="bg-rose-300 text-slate-950 hover:bg-rose-200"><Check className="mr-2 size-4" />{consent ? "Preview consent recorded locally" : "Enter safety preview"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset safety state</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Cards", value: String(POSTS.length), hint: "Non-explicit synthetic", icon: Eye }, { label: "Consent", value: consent ? "Local" : "Off", hint: "Not age assurance", icon: Shield, tone: "violet" }, { label: "Saved", value: String(saved.length), hint: "Local only", icon: Heart, tone: "violet" }, { label: "Safety mode", value: safeMode ? "On" : "Off", hint: "Local preference", icon: EyeOff, tone: "amber" }]} /><ScreenPreviewBanner title="NSFWFeed evidence boundary"><strong>Creator names, titles, tier labels, visibility modes, saved state, report state, and policy cards are synthetic safety fixtures—not adult media, age verification, legal compliance, performer documentation, consent records, creator identity, subscriber entitlements, engagement counts, recommendation ranking, or revenue share.</strong> Production adult-content infrastructure requires jurisdiction-aware age assurance, lawful consent and records, content provenance, moderation, CSAM prevention, privacy, payment controls, takedown, reporting, appeals, and incident response.</ScreenPreviewBanner>{!consent && <Card className="border-rose-300/20 bg-rose-300/[0.06]"><CardContent className="flex flex-col gap-4 p-5 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-start gap-3"><AlertTriangle className="mt-0.5 size-5 shrink-0 text-rose-200" /><div><p className="font-black text-rose-100">Preview is locked by default</p><p className="mt-1 text-sm leading-6 text-slate-400">Local consent here is only a UI state. It is not legal age verification, identity assurance, parental-control bypass, or permission to display adult content.</p></div></div><Button onClick={() => setConsent(true)} className="bg-rose-500 text-white hover:bg-rose-400">Open non-explicit preview</Button></CardContent></Card>}<section className="grid gap-6 lg:grid-cols-[1fr_0.95fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center gap-3"><Search className="size-4 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search synthetic safety cards" className="w-full bg-transparent text-white outline-none placeholder:text-slate-500" /></div><div className="mt-4 flex gap-2 overflow-x-auto">{FILTERS.map((item) => <button key={item} onClick={() => setFilter(item)} className={`rounded-xl border px-3 py-2 text-xs whitespace-nowrap ${filter === item ? "border-rose-300/40 bg-rose-300/[0.1] text-rose-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2">{visible.map((item) => <button key={item.title} disabled={!consent} onClick={() => setSelected(item.title)} className={`rounded-2xl border p-4 text-left disabled:cursor-not-allowed disabled:opacity-60 ${selected === item.title ? "border-rose-300/40 bg-rose-300/[0.07]" : "border-white/10 bg-black/10"}`}><div className="aspect-[4/3] rounded-xl bg-gradient-to-br from-rose-300/10 via-violet-300/10 to-slate-300/10 p-3"><div className="flex h-full flex-col justify-between"><Badge variant="outline" className="w-fit border-white/10 text-[10px] text-slate-400">{safeMode ? "Safety blur" : item.mode}</Badge><div><p className="text-[10px] uppercase tracking-[0.2em] text-rose-200">{item.tier}</p><p className="mt-1 font-black">{item.title}</p></div></div></div><div className="mt-3 flex items-center gap-2"><div className="flex size-7 items-center justify-center rounded-full bg-rose-300/10 text-rose-200"><UserRound className="size-4" /></div><div><p className="text-sm font-semibold">{item.creator}</p><p className="text-xs text-slate-500">{item.mode}</p></div></div><p className="mt-2 text-sm text-slate-400">{item.detail}</p></button>)}{visible.length === 0 && <div className="col-span-full p-8 text-center text-slate-500">No synthetic cards match this view.</div>}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-rose-200">Selected safety card</p><h2 className="mt-2 text-3xl font-black">{post.title}</h2></div><Badge variant="outline" className="border-white/10 text-slate-400">{post.mode}</Badge></div><p className="mt-4 text-slate-400">{post.detail}</p><div className="mt-5 rounded-2xl border border-white/10 bg-black/20 p-5"><div className="flex items-center gap-3"><div className="flex size-11 items-center justify-center rounded-xl bg-rose-300/10 text-rose-200"><Shield className="size-5" /></div><div><p className="font-black">Non-explicit safety state</p><p className="text-xs text-slate-500">No media, identity, or entitlement is attached</p></div></div><div className="mt-5 grid gap-3 sm:grid-cols-2">{[{ label: "Creator", value: "Synthetic" }, { label: "Age", value: "Not verified" }, { label: "Consent", value: "Not sourced" }, { label: "Access", value: "Preview only" }, { label: "Price", value: "Not set" }, { label: "Engagement", value: "Not measured" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div></div><div className="mt-5 flex flex-wrap gap-2"><Button onClick={() => toggle(saved, post.title, setSaved)} className="bg-violet-500 text-white hover:bg-violet-400"><Heart className="mr-2 size-4" />{saved.includes(post.title) ? "Remove local save" : "Save locally"}</Button><Button onClick={() => toggle(reported, post.title, setReported)} variant="outline" className="border-white/10 text-white"><Flag className="mr-2 size-4" />{reported.includes(post.title) ? "Report noted locally" : "Report preview"}</Button><Button onClick={() => toggle(blocked, post.creator, setBlocked)} variant="outline" className="border-white/10 text-white"><Ban className="mr-2 size-4" />{blocked.includes(post.creator) ? "Creator blocked locally" : "Block preview"}</Button></div><Button disabled className="mt-5 w-full bg-white/10 text-white/40"><MessageCircle className="mr-2 size-4" />Message or unlock unavailable</Button><div className="mt-4 flex items-start gap-3 text-xs leading-5 text-slate-500"><LockKeyhole className="mt-0.5 size-4 shrink-0 text-rose-200" />Reports and blocks are local UI state; no moderator queue, creator notification, payment, or access change occurs.</div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-rose-200">Safety readiness</p><h2 className="mt-2 text-2xl font-black">Required before content claims</h2><div className="mt-5 space-y-3">{["Jurisdiction-aware age assurance and parental safety", "Performer identity, lawful consent, rights, and records", "Content classification, moderation, CSAM prevention, and review", "Privacy, payments, subscriptions, takedown, and dispute handling", "Reports, appeals, audit logs, creator support, and incident response"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><Shield className="size-5 text-rose-200" /><h2 className="mt-4 text-xl font-black">No fake compliance</h2><p className="mt-3 text-sm leading-6 text-slate-400">The preview does not verify age, performers, consent, legal documentation, payment access, adult-media rights, compliance, or platform safety.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "A label is not age assurance", description: "Local consent state preserves safety UX without proving legal age, identity, or jurisdictional eligibility.", icon: Shield, status: "Guardrail" }, { title: "Moderation needs evidence", description: "Rights, consent, classification, review, reporting, appeals, and incident handling need operational systems.", icon: ShieldCheck, status: "Required" }, { title: "No fake access", description: "Non-explicit cards and local safety actions do not unlock content, charge users, notify creators, or alter accounts.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
