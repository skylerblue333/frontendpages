import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { BrainCircuit, Check, ChevronRight, Cpu, FileCode2, Gauge, LockKeyhole, RefreshCw, Search, ShieldCheck, Sparkles, Wrench, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const MODELS = [{ name: "Atlas Reasoning", family: "Reasoning", summary: "Synthetic profile for multi-step analysis, planning, and careful comparison.", strengths: ["Long-form reasoning", "Structured planning", "Trade-off analysis"], boundary: "No live endpoint attached" }, { name: "Lumen Vision", family: "Multimodal", summary: "Synthetic profile for image-aware interpretation and visual question framing.", strengths: ["Image interpretation", "Visual summaries", "Grounded questions"], boundary: "No vision request attached" }, { name: "Swift Draft", family: "Fast draft", summary: "Synthetic profile for lightweight drafting, rewriting, and interface copy.", strengths: ["Fast drafts", "Rewrite support", "Short responses"], boundary: "No latency measured" }, { name: "Cedar Code", family: "Code", summary: "Synthetic profile for code review, test planning, and typed implementation notes.", strengths: ["Code review", "Test design", "Type safety"], boundary: "No repository action attached" }];
const TASKS = ["Compare options", "Analyze an image", "Draft a response", "Review code"];

export default function MultiModelSelector() {
  const [task, setTask] = useState(TASKS[0]);
  const [selected, setSelected] = useState(MODELS[0].name);
  const [query, setQuery] = useState("");
  const [saved, setSaved] = useState(false);
  const model = MODELS.find((item) => item.name === selected) ?? MODELS[0];
  const visible = useMemo(() => MODELS.filter((item) => `${item.name} ${item.family} ${item.summary} ${item.strengths.join(" ")}`.toLowerCase().includes(query.toLowerCase())), [query]);
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={BrainCircuit} eyebrow="MultiModelSelector · AI routing" title="Choose a model profile before claiming a model result." description="Compare local synthetic model profiles by task, strengths, and integration gates. No provider catalog, model availability, pricing, latency, benchmark, context limit, privacy certification, reasoning guarantee, or tool execution is asserted." badge="Model routing preview"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Routing plan saved locally" : "Save routing plan"}</Button><Button onClick={() => { setTask(TASKS[0]); setSelected(MODELS[0].name); setQuery(""); setSaved(false); }} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset selector</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Profiles", value: String(MODELS.length), hint: "Synthetic routing cards", icon: Cpu }, { label: "Task", value: task, hint: "Local selection", icon: Sparkles, tone: "violet" }, { label: "Provider", value: "Unset", hint: "No endpoint attached", icon: Wrench, tone: "amber" }, { label: "Privacy", value: "Unverified", hint: "No data policy source", icon: LockKeyhole, tone: "slate" }]} /><ScreenPreviewBanner title="MultiModelSelector evidence boundary"><strong>Model names, families, strengths, task choices, and routing notes are synthetic planning fixtures—not live provider models, capability guarantees, benchmark results, pricing, latency, context windows, privacy certifications, reasoning traces, or tool calls.</strong> A production selector needs an authoritative model registry, versioned contracts, data handling policy, quotas, errors, fallbacks, cost controls, and evaluation evidence.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1fr_0.95fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="relative"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search model profiles" className="w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 pl-9 text-white placeholder:text-slate-500" /></div><div className="mt-4"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Task profile</p><div className="mt-3 flex flex-wrap gap-2">{TASKS.map((item) => <button key={item} onClick={() => { setTask(item); setSaved(false); }} className={`rounded-xl border px-3 py-2 text-xs ${task === item ? "border-cyan-300/40 bg-cyan-300/[0.1] text-cyan-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div></div><div className="mt-5 space-y-3">{visible.map((item) => <button key={item.name} onClick={() => { setSelected(item.name); setSaved(false); }} className={`w-full rounded-2xl border p-4 text-left ${selected === item.name ? "border-cyan-300/40 bg-cyan-300/[0.07]" : "border-white/10 bg-black/10"}`}><div className="flex items-start gap-3"><div className="flex size-10 items-center justify-center rounded-xl bg-violet-300/10 text-violet-200"><Cpu className="size-5" /></div><div className="min-w-0 flex-1"><div className="flex flex-wrap items-center justify-between gap-2"><h2 className="font-black">{item.name}</h2><Badge variant="outline" className="border-amber-300/20 text-[10px] text-amber-200">{item.family}</Badge></div><p className="mt-2 text-sm text-slate-400">{item.summary}</p><div className="mt-3 flex flex-wrap gap-2">{item.strengths.map((strength) => <span key={strength} className="rounded-full border border-white/10 px-2 py-1 text-[10px] text-slate-500">{strength}</span>)}</div></div></div></button>)}{visible.length === 0 && <div className="p-8 text-center text-slate-500">No model profiles match this search.</div>}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-300">Selected profile</p><h2 className="mt-2 text-3xl font-black">{model.name}</h2></div><Badge variant="outline" className="border-white/10 text-slate-400">{model.family}</Badge></div><p className="mt-4 text-slate-400">{model.summary}</p><div className="mt-5 rounded-2xl border border-white/10 bg-black/20 p-5"><div className="flex items-center gap-3"><div className="flex size-11 items-center justify-center rounded-xl bg-cyan-300/10 text-cyan-200"><Gauge className="size-5" /></div><div><p className="font-black">Routing worksheet</p><p className="text-xs text-slate-500">Task: {task}</p></div></div><div className="mt-5 grid gap-3 sm:grid-cols-2">{[{ label: "Provider", value: "Not connected" }, { label: "Version", value: "Not assigned" }, { label: "Cost", value: "Not sourced" }, { label: "Latency", value: "Not measured" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div></div><div className="mt-5 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><p className="font-semibold text-amber-200">Integration boundary</p><p className="mt-2 text-sm leading-6 text-slate-400">{model.boundary}. No prompt, image, code, secret, or tool request is sent from this preview.</p></div><Button disabled className="mt-5 w-full bg-white/10 text-white/40"><ChevronRight className="mr-2 size-4" />Run model unavailable</Button></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Production gates</p><h2 className="mt-2 text-2xl font-black">Required before routing</h2><div className="mt-5 space-y-3">{["Authoritative provider/model/version registry", "Input privacy, retention, and redaction policy", "Quota, cost, timeout, retry, and fallback behavior", "Capability and evaluation evidence for each task", "Tool permissions, audit events, and output validation"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><FileCode2 className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake AI call</h2><p className="mt-3 text-sm leading-6 text-slate-400">The selector does not invoke a model, claim reasoning quality, expose hidden reasoning, execute tools, or report a generated answer.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "A profile is not a provider", description: "Synthetic profiles preserve routing UX without model availability, version, cost, or capability claims.", icon: Cpu, status: "Guardrail" }, { title: "Privacy needs evidence", description: "Prompt handling, retention, redaction, training use, and access controls need a real data policy.", icon: LockKeyhole, status: "Required" }, { title: "No fake invocation", description: "The preview does not send data, execute tools, or fabricate a model response.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
