import { useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Activity, BarChart3, Check, Clock3, Database, FileCheck2, Filter, Gauge, LockKeyhole, RefreshCw, ShieldCheck, SlidersHorizontal, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const METRICS = [{ name: "Requests", unit: "count", value: "—", definition: "Number of observed requests in a defined window." }, { name: "Error rate", unit: "%", value: "—", definition: "Errors divided by observed requests; denominator and status taxonomy required." }, { name: "Latency", unit: "ms", value: "—", definition: "Time definition, percentile, sampling, and source required." }, { name: "Throughput", unit: "events/s", value: "—", definition: "Processed events over time; workload and measurement point required." }, { name: "Active users", unit: "count", value: "—", definition: "Identity, consent, session, and deduplication rules required." }, { name: "Revenue", unit: "currency", value: "—", definition: "Payment ledger, currency, refunds, fees, and reconciliation required." }];
const WINDOWS = ["Last hour", "Last day", "Last 7 days", "Custom window"];
const AGGREGATIONS = ["Raw sample", "Average", "p50 / p95", "Rate per minute"];

export default function NetworkStatistics() {
  const [window, setWindow] = useState(WINDOWS[1]);
  const [aggregation, setAggregation] = useState(AGGREGATIONS[0]);
  const [selected, setSelected] = useState(METRICS[0].name);
  const [note, setNote] = useState("");
  const [saved, setSaved] = useState(false);
  const [refreshed, setRefreshed] = useState(false);
  const metric = METRICS.find((item) => item.name === selected) ?? METRICS[0];
  const reset = () => { setWindow(WINDOWS[1]); setAggregation(AGGREGATIONS[0]); setSelected(METRICS[0].name); setNote(""); setSaved(false); setRefreshed(false); };
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={BarChart3} eyebrow="NetworkStatistics · Measurement design" title="Define the statistic before displaying the number." description="Plan metric definitions, windows, aggregation, freshness, and source ownership with empty synthetic values. No traffic, users, requests, revenue, latency, throughput, uptime, or analytics dataset is connected." badge="Statistics preview"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Metric plan saved locally" : "Save metric plan"}</Button><Button onClick={() => setRefreshed(true)} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />{refreshed ? "Fixture refreshed" : "Refresh fixture"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10">Reset</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Metrics", value: String(METRICS.length), hint: "Definitions only", icon: BarChart3 }, { label: "Window", value: window, hint: "Local selection", icon: Clock3, tone: "violet" }, { label: "Aggregation", value: aggregation, hint: "Local selection", icon: SlidersHorizontal, tone: "cyan" }, { label: "Source", value: "None", hint: "No telemetry", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="NetworkStatistics evidence boundary"><strong>Metric names, units, definitions, empty values, windows, and aggregation controls are measurement-design fixtures—not live counts, users, requests, revenue, latency, throughput, uptime, growth, or analytics output.</strong> Production statistics require instrumented sources, event schemas, identity and consent policy, timestamps, sampling rules, privacy controls, reconciliation, retention, access control, and documented calculation owners.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Metric catalog</p><h2 className="mt-2 text-3xl font-black">Select a definition</h2></div><Badge variant="outline" className="border-white/10 text-slate-400">Values unavailable</Badge></div><div className="mt-5 grid gap-3 sm:grid-cols-2">{METRICS.map((item) => <button key={item.name} onClick={() => { setSelected(item.name); setSaved(false); }} className={`rounded-2xl border p-4 text-left ${selected === item.name ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-black/10"}`}><div className="flex items-center justify-between"><span className="font-black">{item.name}</span><span className="text-xs text-slate-500">{item.unit}</span></div><p className="mt-2 text-sm text-slate-400">{item.definition}</p><div className="mt-4 flex items-center gap-2 text-xs text-amber-200"><Database className="size-3" />Value not sourced</div></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Measurement worksheet</p><h2 className="mt-2 text-3xl font-black">{metric.name}</h2><p className="mt-3 text-slate-400">{metric.definition}</p><div className="mt-5 space-y-4"><label className="block"><span className="text-sm font-semibold text-slate-300">Time window</span><select value={window} onChange={(event) => { setWindow(event.target.value); setSaved(false); }} className="mt-2 w-full rounded-xl border border-white/10 bg-slate-950 px-3 py-3 text-white">{WINDOWS.map((item) => <option key={item}>{item}</option>)}</select></label><label className="block"><span className="text-sm font-semibold text-slate-300">Aggregation</span><select value={aggregation} onChange={(event) => { setAggregation(event.target.value); setSaved(false); }} className="mt-2 w-full rounded-xl border border-white/10 bg-slate-950 px-3 py-3 text-white">{AGGREGATIONS.map((item) => <option key={item}>{item}</option>)}</select></label></div><div className="mt-5 rounded-2xl border border-cyan-300/20 bg-cyan-300/[0.06] p-5"><div className="flex items-center justify-between"><div><p className="font-black">Selected result</p><p className="text-xs text-slate-500">{window} · {aggregation}</p></div><span className="text-3xl font-black text-cyan-200">—</span></div><p className="mt-3 text-xs leading-5 text-slate-500">No source, denominator, timestamp, sample, or calculation has been supplied.</p></div><textarea value={note} onChange={(event) => { setNote(event.target.value); setSaved(false); }} placeholder="Write a local metric-owner or source note…" className="mt-5 min-h-24 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-sm text-white placeholder:text-slate-500" /></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Statistics readiness</p><h2 className="mt-2 text-2xl font-black">Required before number claims</h2><div className="mt-5 space-y-3">{["Event schema, measurement point, source owner, and timestamp", "Identity, consent, deduplication, sampling, and privacy policy", "Units, denominator, percentile, window, aggregation, and currency rules", "Freshness, reconciliation, backfill, correction, and audit trail", "Retention, access controls, export, monitoring, and incident response"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><FileCheck2 className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake statistics</h2><p className="mt-3 text-sm leading-6 text-slate-400">The planner does not collect telemetry, identify users, count traffic, calculate revenue, report latency, or recommend an operational decision.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "A metric is not a result", description: "Definitions and empty values preserve analytics UX without live-data or performance claims.", icon: Gauge, status: "Guardrail" }, { title: "Numbers need provenance", description: "Sources, timestamps, denominators, privacy, reconciliation, and owners need evidence.", icon: ShieldCheck, status: "Required" }, { title: "No fake telemetry", description: "The preview makes no instrumentation, analytics, traffic, revenue, or monitoring request.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
