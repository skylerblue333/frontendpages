import { useMemo, useState } from "react";
import { AlertTriangle, Check, CheckCircle2, Clock3, Code2, Database, FileCheck2, Filter, Gauge, HardDrive, Info, KeyRound, LockKeyhole, RefreshCw, Search, ShieldAlert, ShieldCheck, Timer, Trash2, Unplug, WifiOff, Zap } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const POLICIES = [{ key: "public-shell", pattern: "/", ttl: "300", swr: "60", behavior: "Cache candidate" }, { key: "immutable-assets", pattern: "/assets/*", ttl: "31536000", swr: "0", behavior: "Immutable candidate" }, { key: "api-no-store", pattern: "/api/*", ttl: "0", swr: "0", behavior: "No-store safety default" }, { key: "private-session", pattern: "/private/*", ttl: "0", swr: "0", behavior: "Private no-store" }];
const GATES = ["Key normalization and vary headers", "Authorization and cookie bypass", "Freshness and revalidation", "Purge scope and authorization", "Provider metrics and alerting"];

export default function CacheManagement() { const [policyKey, setPolicyKey] = useState(POLICIES[0].key); const [query, setQuery] = useState(""); const [ttl, setTtl] = useState(POLICIES[0].ttl); const [swr, setSwr] = useState(POLICIES[0].swr); const [prepared, setPrepared] = useState(false); const selected = POLICIES.find((policy) => policy.key === policyKey) ?? POLICIES[0]; const visible = useMemo(() => POLICIES.filter((policy) => `${policy.key} ${policy.pattern} ${policy.behavior}`.toLowerCase().includes(query.toLowerCase())), [query]); const select = (key: string) => { const policy = POLICIES.find((item) => item.key === key) ?? POLICIES[0]; setPolicyKey(key); setTtl(policy.ttl); setSwr(policy.swr); setPrepared(false); }; return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={Zap} eyebrow="Performance · Cache Policy" title="Tune freshness without hiding correctness." description="Review cache keys, TTL and stale-while-revalidate previews, no-store safeguards, and purge requirements. This page does not connect a cache provider, measure hit ratio, invalidate keys, or claim live latency." badge="Preview cache control plane"><div className="flex flex-wrap gap-2"><Button onClick={() => setPrepared(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><FileCheck2 className="mr-2 size-4" />Prepare local policy</Button><Button onClick={() => { setQuery(""); select(POLICIES[0].key); }} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset policy</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Policies", value: String(POLICIES.length), hint: "Reference configurations", icon: KeyRound }, { label: "Provider", value: "Unconnected", hint: "No live cache telemetry", icon: WifiOff, tone: "amber" }, { label: "Hit ratio", value: "Unavailable", hint: "No measured window", icon: Gauge, tone: "violet" }, { label: "Purge", value: "Unexecuted", hint: "No keys invalidated", icon: Trash2, tone: "slate" }]} /><ScreenPreviewBanner title="Cache evidence boundary">A policy preview is not a deployed cache. Hit ratio, storage, eviction, latency, freshness, origin load, provider health, and purge propagation require real telemetry and an authenticated control plane. Never cache private, personalized, token-bearing, or mutable API responses without a reviewed policy.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[0.9fr_1.1fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Policy catalog</p><h2 className="mt-1 text-xl font-bold">Select a key pattern</h2></div><Filter className="size-5 text-cyan-300" /></div><div className="relative mt-5"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500" /><Input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search policies" className="border-white/10 bg-black/20 pl-9 text-white placeholder:text-slate-500" /></div><div className="mt-4 space-y-2">{visible.map((policy) => <button key={policy.key} onClick={() => select(policy.key)} className={`w-full rounded-xl border p-4 text-left transition ${selected.key === policy.key ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-black/15 hover:bg-white/[0.05]"}`}><div className="flex items-center gap-3"><KeyRound className="size-4 text-cyan-300" /><span className="font-semibold">{policy.key}</span><Badge variant="outline" className="ml-auto border-white/10 text-slate-400">{policy.behavior}</Badge></div><p className="mt-2 font-mono text-xs text-slate-500">{policy.pattern} · TTL {policy.ttl}s · SWR {policy.swr}s</p></button>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Policy editor</p><h2 className="mt-2 text-2xl font-black">{selected.key}</h2><p className="mt-2 font-mono text-sm text-slate-400">{selected.pattern}</p></div><Badge variant="outline" className="border-amber-300/20 text-amber-200">Local only</Badge></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><div><label className="mb-2 block text-sm text-slate-300" htmlFor="cache-ttl">TTL seconds</label><Input id="cache-ttl" value={ttl} onChange={(event) => { setTtl(event.target.value); setPrepared(false); }} className="border-white/10 bg-black/20 text-white" /></div><div><label className="mb-2 block text-sm text-slate-300" htmlFor="cache-swr">Stale-while-revalidate seconds</label><Input id="cache-swr" value={swr} onChange={(event) => { setSwr(event.target.value); setPrepared(false); }} className="border-white/10 bg-black/20 text-white" /></div></div><div className="mt-5 grid gap-3 sm:grid-cols-3"><Detail label="Key scope" value={selected.pattern} icon={KeyRound} /><Detail label="Freshness" value={`${ttl}s preview`} icon={Timer} /><Detail label="SWR" value={`${swr}s preview`} icon={Clock3} /></div><div className="mt-6 rounded-xl border border-amber-300/20 bg-amber-300/[0.06] p-4"><div className="flex items-center gap-2 font-medium text-amber-200"><ShieldAlert className="size-4" />No-store safety check</div><p className="mt-2 text-sm leading-6 text-slate-300">If this pattern can include authorization, cookies, user data, secrets, or mutable API output, the safe default is bypass or no-store until reviewed.</p></div>{prepared && <div className="mt-6 rounded-xl border border-cyan-300/20 bg-cyan-300/[0.06] p-4"><div className="flex items-center gap-2 font-medium text-cyan-200"><Check className="size-4" />Local cache policy prepared</div><p className="mt-2 text-sm leading-6 text-slate-300">No provider was called, no key was written, no response was cached, and no purge was executed.</p></div>}</CardContent></Card></section><section className="grid gap-4 md:grid-cols-2 xl:grid-cols-5">{GATES.map((gate) => <Card key={gate} className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><FileCheck2 className="size-5 text-cyan-300" /><p className="mt-3 text-sm font-semibold">{gate}</p><p className="mt-2 text-xs text-slate-500">Required before rollout</p></CardContent></Card>)}</section><section className="grid gap-4 md:grid-cols-3"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><HardDrive className="size-5 text-cyan-300" /><h3 className="mt-3 font-semibold">Freshness is a contract</h3><p className="mt-2 text-sm leading-6 text-slate-400">TTL and stale data are product decisions, not merely performance knobs.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><Database className="size-5 text-violet-300" /><h3 className="mt-3 font-semibold">Keys define safety</h3><p className="mt-2 text-sm leading-6 text-slate-400">Headers, cookies, locale, authorization, and query parameters can change response identity.</p></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-5"><Trash2 className="size-5 text-amber-300" /><h3 className="mt-3 font-semibold">Purge needs proof</h3><p className="mt-2 text-sm leading-6 text-slate-400">A purge plan is not an invalidated key, propagated change, or warm-cache result.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "Policy before speed", description: "Cache correctness and privacy come before hit rate or latency claims.", icon: ShieldCheck, status: "Guardrail" }, { title: "No fake cache metrics", description: "Without a provider and defined window, hit ratio, size, eviction, and latency are unavailable.", icon: WifiOff, status: "Required" }, { title: "Purge is a mutation", description: "Invalidation requires scope, authorization, audit history, and propagation evidence.", icon: Trash2, status: "Required" }]} /></main></div>; }
function Detail({ label, value, icon: Icon }: { label: string; value: string; icon: typeof KeyRound }) { return <div className="rounded-lg border border-white/10 bg-black/15 p-4"><Icon className="size-4 text-slate-400" /><p className="mt-3 text-xs uppercase tracking-wider text-slate-500">{label}</p><p className="mt-1 truncate text-sm text-slate-200">{value}</p></div>; }
