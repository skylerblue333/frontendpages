import { useMemo, useState } from "react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Archive, Bell, Check, Clock3, FileCheck2, FolderOpen, LockKeyhole, Mail, RefreshCw, ShieldCheck, Smartphone, WifiOff } from "lucide-react";
import { ScreenFeatureGrid, ScreenHero, ScreenPreviewBanner, ScreenStatGrid } from "@/components/ScreenExperience";

const ITEMS = [{ id: "c1", lane: "Needs review", title: "Evidence review reminder", detail: "A synthetic workspace item with no sender or delivery record." }, { id: "c2", lane: "Safety", title: "Security boundary check", detail: "Review access, secrets, consent, and recovery before integration." }, { id: "c3", lane: "Learning", title: "Study plan checkpoint", detail: "A local learning concept without enrollment, grade, or instructor state." }, { id: "c4", lane: "Community", title: "Digest concept", detail: "A local community concept with no account, member, or message." }, { id: "c5", lane: "Finance", title: "Wallet safety note", detail: "Never enter recovery material into a preview or unverified integration." }];
const LANES = ["All", "Needs review", "Safety", "Learning", "Community", "Finance"];

export default function NotificationsCenter() {
  const [lane, setLane] = useState("All");
  const [selectedId, setSelectedId] = useState(ITEMS[0].id);
  const [read, setRead] = useState<string[]>([]);
  const [archived, setArchived] = useState<string[]>([]);
  const [digest, setDigest] = useState(false);
  const [saved, setSaved] = useState(false);
  const visible = useMemo(() => ITEMS.filter((item) => (lane === "All" || item.lane === lane) && !archived.includes(item.id)), [archived, lane]);
  const selected = ITEMS.find((item) => item.id === selectedId) ?? ITEMS[0];
  const reset = () => { setLane("All"); setSelectedId(ITEMS[0].id); setRead([]); setArchived([]); setDigest(false); setSaved(false); };
  const markRead = (id: string) => setRead((current) => current.includes(id) ? current.filter((item) => item !== id) : [...current, id]);
  return <div className="min-h-screen bg-[#070a16] text-white"><ScreenHero icon={FolderOpen} eyebrow="NotificationsCenter · Hub preview" title="Organize the signal before promising the system." description="Review synthetic notification lanes, local read/archive state, digest planning, and channel readiness. No live events, unread count, sender identity, delivery, persistence, automation, or backend synchronization is claimed." badge="Hub preview"><div className="flex flex-wrap gap-2"><Button onClick={() => setSaved(true)} className="bg-cyan-300 text-slate-950 hover:bg-cyan-200"><Check className="mr-2 size-4" />{saved ? "Hub plan saved locally" : "Save hub plan locally"}</Button><Button onClick={reset} variant="outline" className="border-white/15 bg-white/5 text-white hover:bg-white/10"><RefreshCw className="mr-2 size-4" />Reset hub</Button></div></ScreenHero><main className="mx-auto max-w-7xl space-y-8 px-4 py-8 sm:px-6 lg:px-8"><ScreenStatGrid items={[{ label: "Items", value: String(visible.length), hint: "Synthetic lanes", icon: FolderOpen }, { label: "Unread", value: String(ITEMS.filter((item) => !read.includes(item.id) && !archived.includes(item.id)).length), hint: "Local state", icon: Bell, tone: "violet" }, { label: "Digest", value: digest ? "On" : "Off", hint: "Local plan", icon: Mail, tone: "cyan" }, { label: "Delivery", value: "Off", hint: "No provider", icon: WifiOff, tone: "slate" }]} /><ScreenPreviewBanner title="NotificationsCenter evidence boundary"><strong>Lanes, cards, local read/archive state, digest planning, and channel labels are notification-hub fixtures—not live event routing, unread counts, sender identity, timestamps, delivery receipts, user activity, automation, analytics, or backend records.</strong> Production hubs require durable event contracts, identity, consent, providers, retries, rate limits, quiet hours, privacy, unsubscribe, auditability, support, and incident response.</ScreenPreviewBanner><section className="grid gap-6 lg:grid-cols-[1fr_0.9fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-300">Synthetic lanes</p><h2 className="mt-2 text-3xl font-black">Hub overview</h2></div><Badge variant="outline" className="border-white/10 text-slate-400">Routing unavailable</Badge></div><div className="mt-4 flex gap-2 overflow-x-auto">{LANES.map((item) => <button key={item} onClick={() => setLane(item)} className={`rounded-xl border px-3 py-2 text-xs whitespace-nowrap ${lane === item ? "border-cyan-300/40 bg-cyan-300/[0.08] text-cyan-200" : "border-white/10 text-slate-500"}`}>{item}</button>)}</div><div className="mt-5 space-y-3">{visible.map((item) => <button key={item.id} onClick={() => { setSelectedId(item.id); markRead(item.id); }} className={`w-full rounded-2xl border p-4 text-left ${selectedId === item.id ? "border-cyan-300/40 bg-cyan-300/[0.08]" : "border-white/10 bg-black/10"}`}><div className="flex items-start gap-3"><div className={`mt-1 size-2 rounded-full ${read.includes(item.id) ? "bg-slate-600" : "bg-cyan-300"}`} /><div className="flex-1"><div className="flex items-center justify-between gap-2"><p className="font-black">{item.title}</p><Badge variant="outline" className="border-white/10 text-[10px] text-slate-400">{item.lane}</Badge></div><p className="mt-2 text-sm text-slate-400">{item.detail}</p><p className="mt-2 text-xs text-slate-500">{read.includes(item.id) ? "Read locally" : "Unread locally"}</p></div></div></button>)}{visible.length === 0 && <p className="py-8 text-center text-slate-500">No synthetic items match this lane.</p>}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-violet-200">Hub controls</p><h2 className="mt-2 text-3xl font-black">{selected.title}</h2><p className="mt-3 text-slate-400">{selected.detail}</p><div className="mt-5 grid gap-3 sm:grid-cols-2">{[{ label: "Lane", value: selected.lane }, { label: "Source", value: "Synthetic" }, { label: "Timestamp", value: "Not sourced" }, { label: "Delivery", value: "Unavailable" }, { label: "Provider", value: "Unset" }, { label: "Automation", value: "Off" }].map((item) => <div key={item.label} className="rounded-xl border border-white/10 p-3"><p className="text-xs text-slate-500">{item.label}</p><p className="mt-2 text-sm font-semibold text-amber-200">{item.value}</p></div>)}</div><div className="mt-5 flex flex-wrap gap-2"><Button onClick={() => markRead(selected.id)} className="bg-violet-500 text-white hover:bg-violet-400"><Check className="mr-2 size-4" />{read.includes(selected.id) ? "Mark unread locally" : "Mark read locally"}</Button><Button onClick={() => setArchived((current) => [...current, selected.id])} variant="outline" className="border-white/10 text-white"><Archive className="mr-2 size-4" />Archive locally</Button></div><div className="mt-5 rounded-2xl border border-white/10 p-4"><div className="flex items-center justify-between"><div><p className="font-black">Digest plan</p><p className="mt-1 text-xs text-slate-500">No provider, schedule, or account is configured.</p></div><button onClick={() => { setDigest(!digest); setSaved(false); }} className={`rounded-xl border px-3 py-2 text-xs ${digest ? "border-cyan-300/40 text-cyan-200" : "border-white/10 text-slate-500"}`}>{digest ? "Local on" : "Off"}</button></div></div><div className="mt-5 flex items-start gap-3 text-xs leading-5 text-slate-500"><LockKeyhole className="mt-0.5 size-4 shrink-0 text-cyan-300" />Local actions do not send, delete, restore, export, route, or update a backend notification.</div></CardContent></Card></section><section className="grid gap-6 lg:grid-cols-[1fr_0.8fr]"><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-amber-200">Hub readiness</p><h2 className="mt-2 text-2xl font-black">Required before hub claims</h2><div className="mt-5 space-y-3">{["Event contracts, durable records, timestamps, and idempotency", "Identity, consent, channel authorization, and quiet hours", "Provider routing, tokens, retries, receipts, and failure handling", "Read/unread, archive, deduplication, rate limits, and unsubscribe", "Privacy, accessibility, support, auditability, deletion, and incidents"].map((item) => <div key={item} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><ShieldCheck className="size-4 text-slate-500" /><span className="flex-1 text-sm text-slate-300">{item}</span><span className="text-xs text-amber-200">Required</span></div>)}</div></CardContent></Card><Card className="border-white/10 bg-white/[0.04]"><CardContent className="p-6"><FileCheck2 className="size-5 text-cyan-300" /><h2 className="mt-4 text-xl font-black">No fake hub</h2><p className="mt-3 text-sm leading-6 text-slate-400">This hub changes local state only. It does not route notifications, contact providers, identify users, calculate metrics, or persist backend activity.</p></CardContent></Card></section><ScreenFeatureGrid features={[{ title: "A lane is not routing", description: "Synthetic categories preserve hub UX without delivery, sender, timestamp, or backend claims.", icon: FolderOpen, status: "Guardrail" }, { title: "Hub needs lifecycle", description: "Durability, consent, providers, retries, privacy, unsubscribe, audit, and support need evidence.", icon: ShieldCheck, status: "Required" }, { title: "No fake routing", description: "Read, archive, and digest actions make no provider, email, push, or backend request.", icon: WifiOff, status: "Unavailable" }]} /></main></div>;
}
