# WebhookManagement visual findings

The 390×844 mobile render keeps the Webhook Management header, unavailable-service disclosure, refresh-unavailable control, delivery-readiness explanation, endpoint registration, and secret-handling boundaries readable without horizontal overflow. The 1440×1000 desktop render shows the complete integration-readiness workspace with endpoint, credential, event contract, signature verification, delivery attempt, retry/idempotency, inbound request, delivery status, and audit states explicitly withheld, plus clear server-side secret handling guidance. Register endpoint, send test event, and rotate secret remain disabled. No endpoint, secret, event, inbound request, signature verification, delivery attempt, retry, or audit result is fabricated.
