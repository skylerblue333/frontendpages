# ShadowChat-Core Frontend Harvest — 2026-08-23

## Canonical target

`skylerblue333/frontendpages` remains the canonical frontend foundation. Source repositories are harvested for capabilities that are genuinely distinct; they are not blindly copied or deleted.

## First harvested capability

**CryptoRedditFeed**

Source:

- Repository: `skylerblue333/ShadowChat-Core`
- Source file: `client/src/pages/generated/Cryptoredditfeed_168.tsx`
- Source revision: `adeaa169a211d72a5f8de7202a6b585541849831`

The source screen was explicitly an autogenerated draft and used `@ts-nocheck`, proxy-based tRPC stubs, local stub queries, and mock mutation handlers. The source therefore supplied a useful UX concept but was not treated as production-ready implementation.

## Canonical implementation

Migrated into:

- `client/src/pages/CryptoRedditFeed.tsx`
- `client/src/lib/cryptoRedditFeed.ts`
- `server/cryptoRedditFeed.test.ts`

### Improvements made

- Removed `@ts-nocheck`.
- Removed proxy-based fake tRPC hooks.
- Removed fake mutation/query behavior.
- Added explicit TypeScript domain types.
- Added deterministic local fixtures with provenance metadata.
- Added reusable filtering and pagination helpers.
- Added accessible search and pagination controls.
- Added automated Vitest coverage for filtering, pagination, provenance, and invalid input.
- Preserved truthful status: no live external feed is claimed until a verified backend contract exists.

## Route integration

The repository already has a generator-of-record at `generate_app.py` that discovers every `client/src/pages/*.tsx` file and generates the lazy import and route entries in `client/src/App.tsx`. The new page therefore participates in the canonical route-generation model rather than requiring a second routing system.

`generate_routes.js` and `routes-config.txt` provide the corresponding route inventory model.

## Verification policy

This harvest is considered **implementation-complete for the frontend capability** only when:

1. the source page exists in the canonical repository;
2. the helper layer is typed and deterministic;
3. tests cover the migrated behavior;
4. route generation includes the new page;
5. repository CI passes TypeScript, tests, route inventory, audit, and production build checks;
6. no live backend capability is claimed without an actual verified contract.

The next harvested screen must follow the same inspect → compare → integrate → test → CI → document → verify loop.
