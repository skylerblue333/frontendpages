# BlockUser review

The `/block-user` route is currently a shared `FeatureUnavailable` boundary rather than a working blockchain-account workflow. It correctly discloses that the backend contract, authorization, persistence, loading and error states, tests, and operational evidence are incomplete. No account, address, block, transaction, balance, ownership, wallet state, identity, permission, block action, or financial success outcome is fabricated.

This route should remain unavailable until verified chain identity, account-scoped data, address validation, wallet or custody boundaries, authorization, transaction provenance, persistence, failure and retry states, and acceptance evidence exist. The remaining task is visual verification at desktop and 390×844 mobile widths.
