# Frontend Integration Queue

This queue records evidence-based harvesting decisions. It is intentionally conservative: unique functionality is preserved, but draft/mock implementations are not promoted unchanged.

## Queue item: CryptoRedditFeed

Source: `skylerblue333/ShadowChat-Core`
Source path: `client/src/pages/generated/Cryptoredditfeed_168.tsx`
Target: `skylerblue333/frontendpages`
Decision: **HARVEST CONCEPT / REBUILD IMPLEMENTATION**

### Evidence

- The source page provides a distinct crypto/social feed experience with search, pagination, loading state, error state, empty state, and post cards.
- The source file is explicitly an autogenerated draft and contains `// @ts-nocheck`.
- It injects local tRPC/query/mutation stubs and has no verified backend data source.
- The query hook returns no real data by default, so copying the file directly would add a visually complete but functionally unverified screen.

### Integration action

1. Preserve the route/product concept.
2. Rebuild the page using `frontendpages` canonical UI primitives and strict TypeScript.
3. Define a real feed contract before wiring data.
4. Add loading/error/empty/pagination states using shared components.
5. Add route-level tests and accessibility checks.
6. Only mark the capability production-ready after the real data path is verified.

### Quality rule

A source page with stronger product behavior can replace this approach only after direct comparison and verification. Source size or repository age is not a quality score.
