# Badges review

The `/badges` route is currently a shared `FeatureUnavailable` boundary rather than a working achievement workflow. It correctly discloses that the backend contract, authorization, persistence, loading and error states, tests, and operational evidence are incomplete. No badge, achievement, criterion, award, rarity, progress, user identity, verification result, unlock, share, ranking, or gamification outcome is fabricated.

This route should remain unavailable until account-scoped achievement rules, event provenance, anti-abuse controls, persistence, verification, sharing permissions, accessibility, failure states, and acceptance evidence exist. The remaining task is visual verification at desktop and 390×844 mobile widths.
